/** must match `port.name` in background `onConnect` for tRPC fetch relay */
export const TRPC_BRIDGE_PORT_NAME = 'chromatika-trpc';

/** must match `port.name` for dapp request relay */
export const DAPP_BRIDGE_PORT_NAME = 'chromatika-dapp';
