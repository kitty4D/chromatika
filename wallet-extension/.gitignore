# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# PC-Token self-deploy workspace (created by `pnpm setup:pc-token`)
.pc-token-deploy/

# SolVanityCL upstream clone + venv (created by `node scripts/setup-sol-vanity.mjs`)
.sol-vanity-deploy/

# preview HTML mockups (generated by `pnpm preview:build`, deployable to marketing site)
preview-dist/
