{
  "name": "@chromatika/team-funder",
  "private": true,
  "version": "0.1.0",
  "description": "team faucet — Cloudflare Worker that pushes a small amount of SUI + IKA to a freshly-onboarded chromatika user's Sui address so they can create their first 2 dWallets without acquiring funds first. mainnet-only, per-address one-shot, daily + lifetime caps as anti-abuse.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mysten/sui": "2.13.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.0",
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.85.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}
