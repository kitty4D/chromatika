function Hg(e,n){for(var t=0;t<n.length;t++){const r=n[t];if(typeof r!="string"&&!Array.isArray(r)){for(const a in r)if(a!=="default"&&!(a in e)){const s=Object.getOwnPropertyDescriptor(r,a);s&&Object.defineProperty(e,a,s.get?s:{enumerable:!0,get:()=>r[a]})}}}return Object.freeze(Object.defineProperty(e,Symbol.toStringTag,{value:"Module"}))}(function(){const n=document.createElement("link").relList;if(n&&n.supports&&n.supports("modulepreload"))return;for(const a of document.querySelectorAll('link[rel="modulepreload"]'))r(a);new MutationObserver(a=>{for(const s of a)if(s.type==="childList")for(const i of s.addedNodes)i.tagName==="LINK"&&i.rel==="modulepreload"&&r(i)}).observe(document,{childList:!0,subtree:!0});function t(a){const s={};return a.integrity&&(s.integrity=a.integrity),a.referrerPolicy&&(s.referrerPolicy=a.referrerPolicy),a.crossOrigin==="use-credentials"?s.credentials="include":a.crossOrigin==="anonymous"?s.credentials="omit":s.credentials="same-origin",s}function r(a){if(a.ep)return;a.ep=!0;const s=t(a);fetch(a.href,s)}})();var ws=typeof globalThis<"u"?globalThis:typeof window<"u"?window:typeof global<"u"?global:typeof self<"u"?self:{};function Sl(e){return e&&e.__esModule&&Object.prototype.hasOwnProperty.call(e,"default")?e.default:e}var oh={exports:{}},$s={},lh={exports:{}},Q={};/**
 * @license React
 * react.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Pa=Symbol.for("react.element"),qg=Symbol.for("react.portal"),Vg=Symbol.for("react.fragment"),Gg=Symbol.for("react.strict_mode"),$g=Symbol.for("react.profiler"),Qg=Symbol.for("react.provider"),Jg=Symbol.for("react.context"),Yg=Symbol.for("react.forward_ref"),Xg=Symbol.for("react.suspense"),Zg=Symbol.for("react.memo"),ef=Symbol.for("react.lazy"),Hc=Symbol.iterator;function nf(e){return e===null||typeof e!="object"?null:(e=Hc&&e[Hc]||e["@@iterator"],typeof e=="function"?e:null)}var ch={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}},uh=Object.assign,dh={};function yr(e,n,t){this.props=e,this.context=n,this.refs=dh,this.updater=t||ch}yr.prototype.isReactComponent={};yr.prototype.setState=function(e,n){if(typeof e!="object"&&typeof e!="function"&&e!=null)throw Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");this.updater.enqueueSetState(this,e,n,"setState")};yr.prototype.forceUpdate=function(e){this.updater.enqueueForceUpdate(this,e,"forceUpdate")};function hh(){}hh.prototype=yr.prototype;function xl(e,n,t){this.props=e,this.context=n,this.refs=dh,this.updater=t||ch}var Pl=xl.prototype=new hh;Pl.constructor=xl;uh(Pl,yr.prototype);Pl.isPureReactComponent=!0;var qc=Array.isArray,ph=Object.prototype.hasOwnProperty,_l={current:null},mh={key:!0,ref:!0,__self:!0,__source:!0};function gh(e,n,t){var r,a={},s=null,i=null;if(n!=null)for(r in n.ref!==void 0&&(i=n.ref),n.key!==void 0&&(s=""+n.key),n)ph.call(n,r)&&!mh.hasOwnProperty(r)&&(a[r]=n[r]);var o=arguments.length-2;if(o===1)a.children=t;else if(1<o){for(var l=Array(o),c=0;c<o;c++)l[c]=arguments[c+2];a.children=l}if(e&&e.defaultProps)for(r in o=e.defaultProps,o)a[r]===void 0&&(a[r]=o[r]);return{$$typeof:Pa,type:e,key:s,ref:i,props:a,_owner:_l.current}}function tf(e,n){return{$$typeof:Pa,type:e.type,key:n,ref:e.ref,props:e.props,_owner:e._owner}}function El(e){return typeof e=="object"&&e!==null&&e.$$typeof===Pa}function rf(e){var n={"=":"=0",":":"=2"};return"$"+e.replace(/[=:]/g,function(t){return n[t]})}var Vc=/\/+/g;function xi(e,n){return typeof e=="object"&&e!==null&&e.key!=null?rf(""+e.key):n.toString(36)}function as(e,n,t,r,a){var s=typeof e;(s==="undefined"||s==="boolean")&&(e=null);var i=!1;if(e===null)i=!0;else switch(s){case"string":case"number":i=!0;break;case"object":switch(e.$$typeof){case Pa:case qg:i=!0}}if(i)return i=e,a=a(i),e=r===""?"."+xi(i,0):r,qc(a)?(t="",e!=null&&(t=e.replace(Vc,"$&/")+"/"),as(a,n,t,"",function(c){return c})):a!=null&&(El(a)&&(a=tf(a,t+(!a.key||i&&i.key===a.key?"":(""+a.key).replace(Vc,"$&/")+"/")+e)),n.push(a)),1;if(i=0,r=r===""?".":r+":",qc(e))for(var o=0;o<e.length;o++){s=e[o];var l=r+xi(s,o);i+=as(s,n,t,l,a)}else if(l=nf(e),typeof l=="function")for(e=l.call(e),o=0;!(s=e.next()).done;)s=s.value,l=r+xi(s,o++),i+=as(s,n,t,l,a);else if(s==="object")throw n=String(e),Error("Objects are not valid as a React child (found: "+(n==="[object Object]"?"object with keys {"+Object.keys(e).join(", ")+"}":n)+"). If you meant to render a collection of children, use an array instead.");return i}function Ka(e,n,t){if(e==null)return e;var r=[],a=0;return as(e,r,"","",function(s){return n.call(t,s,a++)}),r}function af(e){if(e._status===-1){var n=e._result;n=n(),n.then(function(t){(e._status===0||e._status===-1)&&(e._status=1,e._result=t)},function(t){(e._status===0||e._status===-1)&&(e._status=2,e._result=t)}),e._status===-1&&(e._status=0,e._result=n)}if(e._status===1)return e._result.default;throw e._result}var je={current:null},ss={transition:null},sf={ReactCurrentDispatcher:je,ReactCurrentBatchConfig:ss,ReactCurrentOwner:_l};function fh(){throw Error("act(...) is not supported in production builds of React.")}Q.Children={map:Ka,forEach:function(e,n,t){Ka(e,function(){n.apply(this,arguments)},t)},count:function(e){var n=0;return Ka(e,function(){n++}),n},toArray:function(e){return Ka(e,function(n){return n})||[]},only:function(e){if(!El(e))throw Error("React.Children.only expected to receive a single React element child.");return e}};Q.Component=yr;Q.Fragment=Vg;Q.Profiler=$g;Q.PureComponent=xl;Q.StrictMode=Gg;Q.Suspense=Xg;Q.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=sf;Q.act=fh;Q.cloneElement=function(e,n,t){if(e==null)throw Error("React.cloneElement(...): The argument must be a React element, but you passed "+e+".");var r=uh({},e.props),a=e.key,s=e.ref,i=e._owner;if(n!=null){if(n.ref!==void 0&&(s=n.ref,i=_l.current),n.key!==void 0&&(a=""+n.key),e.type&&e.type.defaultProps)var o=e.type.defaultProps;for(l in n)ph.call(n,l)&&!mh.hasOwnProperty(l)&&(r[l]=n[l]===void 0&&o!==void 0?o[l]:n[l])}var l=arguments.length-2;if(l===1)r.children=t;else if(1<l){o=Array(l);for(var c=0;c<l;c++)o[c]=arguments[c+2];r.children=o}return{$$typeof:Pa,type:e.type,key:a,ref:s,props:r,_owner:i}};Q.createContext=function(e){return e={$$typeof:Jg,_currentValue:e,_currentValue2:e,_threadCount:0,Provider:null,Consumer:null,_defaultValue:null,_globalName:null},e.Provider={$$typeof:Qg,_context:e},e.Consumer=e};Q.createElement=gh;Q.createFactory=function(e){var n=gh.bind(null,e);return n.type=e,n};Q.createRef=function(){return{current:null}};Q.forwardRef=function(e){return{$$typeof:Yg,render:e}};Q.isValidElement=El;Q.lazy=function(e){return{$$typeof:ef,_payload:{_status:-1,_result:e},_init:af}};Q.memo=function(e,n){return{$$typeof:Zg,type:e,compare:n===void 0?null:n}};Q.startTransition=function(e){var n=ss.transition;ss.transition={};try{e()}finally{ss.transition=n}};Q.unstable_act=fh;Q.useCallback=function(e,n){return je.current.useCallback(e,n)};Q.useContext=function(e){return je.current.useContext(e)};Q.useDebugValue=function(){};Q.useDeferredValue=function(e){return je.current.useDeferredValue(e)};Q.useEffect=function(e,n){return je.current.useEffect(e,n)};Q.useId=function(){return je.current.useId()};Q.useImperativeHandle=function(e,n,t){return je.current.useImperativeHandle(e,n,t)};Q.useInsertionEffect=function(e,n){return je.current.useInsertionEffect(e,n)};Q.useLayoutEffect=function(e,n){return je.current.useLayoutEffect(e,n)};Q.useMemo=function(e,n){return je.current.useMemo(e,n)};Q.useReducer=function(e,n,t){return je.current.useReducer(e,n,t)};Q.useRef=function(e){return je.current.useRef(e)};Q.useState=function(e){return je.current.useState(e)};Q.useSyncExternalStore=function(e,n,t){return je.current.useSyncExternalStore(e,n,t)};Q.useTransition=function(){return je.current.useTransition()};Q.version="18.3.1";lh.exports=Q;var T=lh.exports;const yh=Sl(T),of=Hg({__proto__:null,default:yh},[T]);/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var lf=T,cf=Symbol.for("react.element"),uf=Symbol.for("react.fragment"),df=Object.prototype.hasOwnProperty,hf=lf.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner,pf={key:!0,ref:!0,__self:!0,__source:!0};function wh(e,n,t){var r,a={},s=null,i=null;t!==void 0&&(s=""+t),n.key!==void 0&&(s=""+n.key),n.ref!==void 0&&(i=n.ref);for(r in n)df.call(n,r)&&!pf.hasOwnProperty(r)&&(a[r]=n[r]);if(e&&e.defaultProps)for(r in n=e.defaultProps,n)a[r]===void 0&&(a[r]=n[r]);return{$$typeof:cf,type:e,key:s,ref:i,props:a,_owner:hf.current}}$s.Fragment=uf;$s.jsx=wh;$s.jsxs=wh;oh.exports=$s;var h=oh.exports,po={},vh={exports:{}},rn={},kh={exports:{}},bh={};/**
 * @license React
 * scheduler.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */(function(e){function n(B,F){var v=B.length;B.push(F);e:for(;0<v;){var N=v-1>>>1,O=B[N];if(0<a(O,F))B[N]=F,B[v]=O,v=N;else break e}}function t(B){return B.length===0?null:B[0]}function r(B){if(B.length===0)return null;var F=B[0],v=B.pop();if(v!==F){B[0]=v;e:for(var N=0,O=B.length,b=O>>>1;N<b;){var H=2*(N+1)-1,Se=B[H],ee=H+1,Ae=B[ee];if(0>a(Se,v))ee<O&&0>a(Ae,Se)?(B[N]=Ae,B[ee]=v,N=ee):(B[N]=Se,B[H]=v,N=H);else if(ee<O&&0>a(Ae,v))B[N]=Ae,B[ee]=v,N=ee;else break e}}return F}function a(B,F){var v=B.sortIndex-F.sortIndex;return v!==0?v:B.id-F.id}if(typeof performance=="object"&&typeof performance.now=="function"){var s=performance;e.unstable_now=function(){return s.now()}}else{var i=Date,o=i.now();e.unstable_now=function(){return i.now()-o}}var l=[],c=[],d=1,u=null,p=3,m=!1,f=!1,k=!1,P=typeof setTimeout=="function"?setTimeout:null,g=typeof clearTimeout=="function"?clearTimeout:null,y=typeof setImmediate<"u"?setImmediate:null;typeof navigator<"u"&&navigator.scheduling!==void 0&&navigator.scheduling.isInputPending!==void 0&&navigator.scheduling.isInputPending.bind(navigator.scheduling);function w(B){for(var F=t(c);F!==null;){if(F.callback===null)r(c);else if(F.startTime<=B)r(c),F.sortIndex=F.expirationTime,n(l,F);else break;F=t(c)}}function _(B){if(k=!1,w(B),!f)if(t(l)!==null)f=!0,de(E);else{var F=t(c);F!==null&&oe(_,F.startTime-B)}}function E(B,F){f=!1,k&&(k=!1,g(M),M=-1),m=!0;var v=p;try{for(w(F),u=t(l);u!==null&&(!(u.expirationTime>F)||B&&!L());){var N=u.callback;if(typeof N=="function"){u.callback=null,p=u.priorityLevel;var O=N(u.expirationTime<=F);F=e.unstable_now(),typeof O=="function"?u.callback=O:u===t(l)&&r(l),w(F)}else r(l);u=t(l)}if(u!==null)var b=!0;else{var H=t(c);H!==null&&oe(_,H.startTime-F),b=!1}return b}finally{u=null,p=v,m=!1}}var S=!1,A=null,M=-1,j=5,x=-1;function L(){return!(e.unstable_now()-x<j)}function W(){if(A!==null){var B=e.unstable_now();x=B;var F=!0;try{F=A(!0,B)}finally{F?J():(S=!1,A=null)}}else S=!1}var J;if(typeof y=="function")J=function(){y(W)};else if(typeof MessageChannel<"u"){var te=new MessageChannel,G=te.port2;te.port1.onmessage=W,J=function(){G.postMessage(null)}}else J=function(){P(W,0)};function de(B){A=B,S||(S=!0,J())}function oe(B,F){M=P(function(){B(e.unstable_now())},F)}e.unstable_IdlePriority=5,e.unstable_ImmediatePriority=1,e.unstable_LowPriority=4,e.unstable_NormalPriority=3,e.unstable_Profiling=null,e.unstable_UserBlockingPriority=2,e.unstable_cancelCallback=function(B){B.callback=null},e.unstable_continueExecution=function(){f||m||(f=!0,de(E))},e.unstable_forceFrameRate=function(B){0>B||125<B?console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"):j=0<B?Math.floor(1e3/B):5},e.unstable_getCurrentPriorityLevel=function(){return p},e.unstable_getFirstCallbackNode=function(){return t(l)},e.unstable_next=function(B){switch(p){case 1:case 2:case 3:var F=3;break;default:F=p}var v=p;p=F;try{return B()}finally{p=v}},e.unstable_pauseExecution=function(){},e.unstable_requestPaint=function(){},e.unstable_runWithPriority=function(B,F){switch(B){case 1:case 2:case 3:case 4:case 5:break;default:B=3}var v=p;p=B;try{return F()}finally{p=v}},e.unstable_scheduleCallback=function(B,F,v){var N=e.unstable_now();switch(typeof v=="object"&&v!==null?(v=v.delay,v=typeof v=="number"&&0<v?N+v:N):v=N,B){case 1:var O=-1;break;case 2:O=250;break;case 5:O=1073741823;break;case 4:O=1e4;break;default:O=5e3}return O=v+O,B={id:d++,callback:F,priorityLevel:B,startTime:v,expirationTime:O,sortIndex:-1},v>N?(B.sortIndex=v,n(c,B),t(l)===null&&B===t(c)&&(k?(g(M),M=-1):k=!0,oe(_,v-N))):(B.sortIndex=O,n(l,B),f||m||(f=!0,de(E))),B},e.unstable_shouldYield=L,e.unstable_wrapCallback=function(B){var F=p;return function(){var v=p;p=F;try{return B.apply(this,arguments)}finally{p=v}}}})(bh);kh.exports=bh;var mf=kh.exports;/**
 * @license React
 * react-dom.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var gf=T,tn=mf;function I(e){for(var n="https://reactjs.org/docs/error-decoder.html?invariant="+e,t=1;t<arguments.length;t++)n+="&args[]="+encodeURIComponent(arguments[t]);return"Minified React error #"+e+"; visit "+n+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings."}var Sh=new Set,ra={};function Lt(e,n){cr(e,n),cr(e+"Capture",n)}function cr(e,n){for(ra[e]=n,e=0;e<n.length;e++)Sh.add(n[e])}var Wn=!(typeof window>"u"||typeof window.document>"u"||typeof window.document.createElement>"u"),mo=Object.prototype.hasOwnProperty,ff=/^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/,Gc={},$c={};function yf(e){return mo.call($c,e)?!0:mo.call(Gc,e)?!1:ff.test(e)?$c[e]=!0:(Gc[e]=!0,!1)}function wf(e,n,t,r){if(t!==null&&t.type===0)return!1;switch(typeof n){case"function":case"symbol":return!0;case"boolean":return r?!1:t!==null?!t.acceptsBooleans:(e=e.toLowerCase().slice(0,5),e!=="data-"&&e!=="aria-");default:return!1}}function vf(e,n,t,r){if(n===null||typeof n>"u"||wf(e,n,t,r))return!0;if(r)return!1;if(t!==null)switch(t.type){case 3:return!n;case 4:return n===!1;case 5:return isNaN(n);case 6:return isNaN(n)||1>n}return!1}function Fe(e,n,t,r,a,s,i){this.acceptsBooleans=n===2||n===3||n===4,this.attributeName=r,this.attributeNamespace=a,this.mustUseProperty=t,this.propertyName=e,this.type=n,this.sanitizeURL=s,this.removeEmptyString=i}var Ce={};"children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(e){Ce[e]=new Fe(e,0,!1,e,null,!1,!1)});[["acceptCharset","accept-charset"],["className","class"],["htmlFor","for"],["httpEquiv","http-equiv"]].forEach(function(e){var n=e[0];Ce[n]=new Fe(n,1,!1,e[1],null,!1,!1)});["contentEditable","draggable","spellCheck","value"].forEach(function(e){Ce[e]=new Fe(e,2,!1,e.toLowerCase(),null,!1,!1)});["autoReverse","externalResourcesRequired","focusable","preserveAlpha"].forEach(function(e){Ce[e]=new Fe(e,2,!1,e,null,!1,!1)});"allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(e){Ce[e]=new Fe(e,3,!1,e.toLowerCase(),null,!1,!1)});["checked","multiple","muted","selected"].forEach(function(e){Ce[e]=new Fe(e,3,!0,e,null,!1,!1)});["capture","download"].forEach(function(e){Ce[e]=new Fe(e,4,!1,e,null,!1,!1)});["cols","rows","size","span"].forEach(function(e){Ce[e]=new Fe(e,6,!1,e,null,!1,!1)});["rowSpan","start"].forEach(function(e){Ce[e]=new Fe(e,5,!1,e.toLowerCase(),null,!1,!1)});var Cl=/[\-:]([a-z])/g;function Al(e){return e[1].toUpperCase()}"accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(e){var n=e.replace(Cl,Al);Ce[n]=new Fe(n,1,!1,e,null,!1,!1)});"xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(e){var n=e.replace(Cl,Al);Ce[n]=new Fe(n,1,!1,e,"http://www.w3.org/1999/xlink",!1,!1)});["xml:base","xml:lang","xml:space"].forEach(function(e){var n=e.replace(Cl,Al);Ce[n]=new Fe(n,1,!1,e,"http://www.w3.org/XML/1998/namespace",!1,!1)});["tabIndex","crossOrigin"].forEach(function(e){Ce[e]=new Fe(e,1,!1,e.toLowerCase(),null,!1,!1)});Ce.xlinkHref=new Fe("xlinkHref",1,!1,"xlink:href","http://www.w3.org/1999/xlink",!0,!1);["src","href","action","formAction"].forEach(function(e){Ce[e]=new Fe(e,1,!1,e.toLowerCase(),null,!0,!0)});function Il(e,n,t,r){var a=Ce.hasOwnProperty(n)?Ce[n]:null;(a!==null?a.type!==0:r||!(2<n.length)||n[0]!=="o"&&n[0]!=="O"||n[1]!=="n"&&n[1]!=="N")&&(vf(n,t,a,r)&&(t=null),r||a===null?yf(n)&&(t===null?e.removeAttribute(n):e.setAttribute(n,""+t)):a.mustUseProperty?e[a.propertyName]=t===null?a.type===3?!1:"":t:(n=a.attributeName,r=a.attributeNamespace,t===null?e.removeAttribute(n):(a=a.type,t=a===3||a===4&&t===!0?"":""+t,r?e.setAttributeNS(r,n,t):e.setAttribute(n,t))))}var zn=gf.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED,Wa=Symbol.for("react.element"),Ht=Symbol.for("react.portal"),qt=Symbol.for("react.fragment"),Tl=Symbol.for("react.strict_mode"),go=Symbol.for("react.profiler"),xh=Symbol.for("react.provider"),Ph=Symbol.for("react.context"),Rl=Symbol.for("react.forward_ref"),fo=Symbol.for("react.suspense"),yo=Symbol.for("react.suspense_list"),Ml=Symbol.for("react.memo"),Qn=Symbol.for("react.lazy"),_h=Symbol.for("react.offscreen"),Qc=Symbol.iterator;function Rr(e){return e===null||typeof e!="object"?null:(e=Qc&&e[Qc]||e["@@iterator"],typeof e=="function"?e:null)}var me=Object.assign,Pi;function Ur(e){if(Pi===void 0)try{throw Error()}catch(t){var n=t.stack.trim().match(/\n( *(at )?)/);Pi=n&&n[1]||""}return`
`+Pi+e}var _i=!1;function Ei(e,n){if(!e||_i)return"";_i=!0;var t=Error.prepareStackTrace;Error.prepareStackTrace=void 0;try{if(n)if(n=function(){throw Error()},Object.defineProperty(n.prototype,"props",{set:function(){throw Error()}}),typeof Reflect=="object"&&Reflect.construct){try{Reflect.construct(n,[])}catch(c){var r=c}Reflect.construct(e,[],n)}else{try{n.call()}catch(c){r=c}e.call(n.prototype)}else{try{throw Error()}catch(c){r=c}e()}}catch(c){if(c&&r&&typeof c.stack=="string"){for(var a=c.stack.split(`
`),s=r.stack.split(`
`),i=a.length-1,o=s.length-1;1<=i&&0<=o&&a[i]!==s[o];)o--;for(;1<=i&&0<=o;i--,o--)if(a[i]!==s[o]){if(i!==1||o!==1)do if(i--,o--,0>o||a[i]!==s[o]){var l=`
`+a[i].replace(" at new "," at ");return e.displayName&&l.includes("<anonymous>")&&(l=l.replace("<anonymous>",e.displayName)),l}while(1<=i&&0<=o);break}}}finally{_i=!1,Error.prepareStackTrace=t}return(e=e?e.displayName||e.name:"")?Ur(e):""}function kf(e){switch(e.tag){case 5:return Ur(e.type);case 16:return Ur("Lazy");case 13:return Ur("Suspense");case 19:return Ur("SuspenseList");case 0:case 2:case 15:return e=Ei(e.type,!1),e;case 11:return e=Ei(e.type.render,!1),e;case 1:return e=Ei(e.type,!0),e;default:return""}}function wo(e){if(e==null)return null;if(typeof e=="function")return e.displayName||e.name||null;if(typeof e=="string")return e;switch(e){case qt:return"Fragment";case Ht:return"Portal";case go:return"Profiler";case Tl:return"StrictMode";case fo:return"Suspense";case yo:return"SuspenseList"}if(typeof e=="object")switch(e.$$typeof){case Ph:return(e.displayName||"Context")+".Consumer";case xh:return(e._context.displayName||"Context")+".Provider";case Rl:var n=e.render;return e=e.displayName,e||(e=n.displayName||n.name||"",e=e!==""?"ForwardRef("+e+")":"ForwardRef"),e;case Ml:return n=e.displayName||null,n!==null?n:wo(e.type)||"Memo";case Qn:n=e._payload,e=e._init;try{return wo(e(n))}catch{}}return null}function bf(e){var n=e.type;switch(e.tag){case 24:return"Cache";case 9:return(n.displayName||"Context")+".Consumer";case 10:return(n._context.displayName||"Context")+".Provider";case 18:return"DehydratedFragment";case 11:return e=n.render,e=e.displayName||e.name||"",n.displayName||(e!==""?"ForwardRef("+e+")":"ForwardRef");case 7:return"Fragment";case 5:return n;case 4:return"Portal";case 3:return"Root";case 6:return"Text";case 16:return wo(n);case 8:return n===Tl?"StrictMode":"Mode";case 22:return"Offscreen";case 12:return"Profiler";case 21:return"Scope";case 13:return"Suspense";case 19:return"SuspenseList";case 25:return"TracingMarker";case 1:case 0:case 17:case 2:case 14:case 15:if(typeof n=="function")return n.displayName||n.name||null;if(typeof n=="string")return n}return null}function dt(e){switch(typeof e){case"boolean":case"number":case"string":case"undefined":return e;case"object":return e;default:return""}}function Eh(e){var n=e.type;return(e=e.nodeName)&&e.toLowerCase()==="input"&&(n==="checkbox"||n==="radio")}function Sf(e){var n=Eh(e)?"checked":"value",t=Object.getOwnPropertyDescriptor(e.constructor.prototype,n),r=""+e[n];if(!e.hasOwnProperty(n)&&typeof t<"u"&&typeof t.get=="function"&&typeof t.set=="function"){var a=t.get,s=t.set;return Object.defineProperty(e,n,{configurable:!0,get:function(){return a.call(this)},set:function(i){r=""+i,s.call(this,i)}}),Object.defineProperty(e,n,{enumerable:t.enumerable}),{getValue:function(){return r},setValue:function(i){r=""+i},stopTracking:function(){e._valueTracker=null,delete e[n]}}}}function ja(e){e._valueTracker||(e._valueTracker=Sf(e))}function Ch(e){if(!e)return!1;var n=e._valueTracker;if(!n)return!0;var t=n.getValue(),r="";return e&&(r=Eh(e)?e.checked?"true":"false":e.value),e=r,e!==t?(n.setValue(e),!0):!1}function vs(e){if(e=e||(typeof document<"u"?document:void 0),typeof e>"u")return null;try{return e.activeElement||e.body}catch{return e.body}}function vo(e,n){var t=n.checked;return me({},n,{defaultChecked:void 0,defaultValue:void 0,value:void 0,checked:t??e._wrapperState.initialChecked})}function Jc(e,n){var t=n.defaultValue==null?"":n.defaultValue,r=n.checked!=null?n.checked:n.defaultChecked;t=dt(n.value!=null?n.value:t),e._wrapperState={initialChecked:r,initialValue:t,controlled:n.type==="checkbox"||n.type==="radio"?n.checked!=null:n.value!=null}}function Ah(e,n){n=n.checked,n!=null&&Il(e,"checked",n,!1)}function ko(e,n){Ah(e,n);var t=dt(n.value),r=n.type;if(t!=null)r==="number"?(t===0&&e.value===""||e.value!=t)&&(e.value=""+t):e.value!==""+t&&(e.value=""+t);else if(r==="submit"||r==="reset"){e.removeAttribute("value");return}n.hasOwnProperty("value")?bo(e,n.type,t):n.hasOwnProperty("defaultValue")&&bo(e,n.type,dt(n.defaultValue)),n.checked==null&&n.defaultChecked!=null&&(e.defaultChecked=!!n.defaultChecked)}function Yc(e,n,t){if(n.hasOwnProperty("value")||n.hasOwnProperty("defaultValue")){var r=n.type;if(!(r!=="submit"&&r!=="reset"||n.value!==void 0&&n.value!==null))return;n=""+e._wrapperState.initialValue,t||n===e.value||(e.value=n),e.defaultValue=n}t=e.name,t!==""&&(e.name=""),e.defaultChecked=!!e._wrapperState.initialChecked,t!==""&&(e.name=t)}function bo(e,n,t){(n!=="number"||vs(e.ownerDocument)!==e)&&(t==null?e.defaultValue=""+e._wrapperState.initialValue:e.defaultValue!==""+t&&(e.defaultValue=""+t))}var zr=Array.isArray;function tr(e,n,t,r){if(e=e.options,n){n={};for(var a=0;a<t.length;a++)n["$"+t[a]]=!0;for(t=0;t<e.length;t++)a=n.hasOwnProperty("$"+e[t].value),e[t].selected!==a&&(e[t].selected=a),a&&r&&(e[t].defaultSelected=!0)}else{for(t=""+dt(t),n=null,a=0;a<e.length;a++){if(e[a].value===t){e[a].selected=!0,r&&(e[a].defaultSelected=!0);return}n!==null||e[a].disabled||(n=e[a])}n!==null&&(n.selected=!0)}}function So(e,n){if(n.dangerouslySetInnerHTML!=null)throw Error(I(91));return me({},n,{value:void 0,defaultValue:void 0,children:""+e._wrapperState.initialValue})}function Xc(e,n){var t=n.value;if(t==null){if(t=n.children,n=n.defaultValue,t!=null){if(n!=null)throw Error(I(92));if(zr(t)){if(1<t.length)throw Error(I(93));t=t[0]}n=t}n==null&&(n=""),t=n}e._wrapperState={initialValue:dt(t)}}function Ih(e,n){var t=dt(n.value),r=dt(n.defaultValue);t!=null&&(t=""+t,t!==e.value&&(e.value=t),n.defaultValue==null&&e.defaultValue!==t&&(e.defaultValue=t)),r!=null&&(e.defaultValue=""+r)}function Zc(e){var n=e.textContent;n===e._wrapperState.initialValue&&n!==""&&n!==null&&(e.value=n)}function Th(e){switch(e){case"svg":return"http://www.w3.org/2000/svg";case"math":return"http://www.w3.org/1998/Math/MathML";default:return"http://www.w3.org/1999/xhtml"}}function xo(e,n){return e==null||e==="http://www.w3.org/1999/xhtml"?Th(n):e==="http://www.w3.org/2000/svg"&&n==="foreignObject"?"http://www.w3.org/1999/xhtml":e}var Fa,Rh=function(e){return typeof MSApp<"u"&&MSApp.execUnsafeLocalFunction?function(n,t,r,a){MSApp.execUnsafeLocalFunction(function(){return e(n,t,r,a)})}:e}(function(e,n){if(e.namespaceURI!=="http://www.w3.org/2000/svg"||"innerHTML"in e)e.innerHTML=n;else{for(Fa=Fa||document.createElement("div"),Fa.innerHTML="<svg>"+n.valueOf().toString()+"</svg>",n=Fa.firstChild;e.firstChild;)e.removeChild(e.firstChild);for(;n.firstChild;)e.appendChild(n.firstChild)}});function aa(e,n){if(n){var t=e.firstChild;if(t&&t===e.lastChild&&t.nodeType===3){t.nodeValue=n;return}}e.textContent=n}var qr={animationIterationCount:!0,aspectRatio:!0,borderImageOutset:!0,borderImageSlice:!0,borderImageWidth:!0,boxFlex:!0,boxFlexGroup:!0,boxOrdinalGroup:!0,columnCount:!0,columns:!0,flex:!0,flexGrow:!0,flexPositive:!0,flexShrink:!0,flexNegative:!0,flexOrder:!0,gridArea:!0,gridRow:!0,gridRowEnd:!0,gridRowSpan:!0,gridRowStart:!0,gridColumn:!0,gridColumnEnd:!0,gridColumnSpan:!0,gridColumnStart:!0,fontWeight:!0,lineClamp:!0,lineHeight:!0,opacity:!0,order:!0,orphans:!0,tabSize:!0,widows:!0,zIndex:!0,zoom:!0,fillOpacity:!0,floodOpacity:!0,stopOpacity:!0,strokeDasharray:!0,strokeDashoffset:!0,strokeMiterlimit:!0,strokeOpacity:!0,strokeWidth:!0},xf=["Webkit","ms","Moz","O"];Object.keys(qr).forEach(function(e){xf.forEach(function(n){n=n+e.charAt(0).toUpperCase()+e.substring(1),qr[n]=qr[e]})});function Mh(e,n,t){return n==null||typeof n=="boolean"||n===""?"":t||typeof n!="number"||n===0||qr.hasOwnProperty(e)&&qr[e]?(""+n).trim():n+"px"}function Dh(e,n){e=e.style;for(var t in n)if(n.hasOwnProperty(t)){var r=t.indexOf("--")===0,a=Mh(t,n[t],r);t==="float"&&(t="cssFloat"),r?e.setProperty(t,a):e[t]=a}}var Pf=me({menuitem:!0},{area:!0,base:!0,br:!0,col:!0,embed:!0,hr:!0,img:!0,input:!0,keygen:!0,link:!0,meta:!0,param:!0,source:!0,track:!0,wbr:!0});function Po(e,n){if(n){if(Pf[e]&&(n.children!=null||n.dangerouslySetInnerHTML!=null))throw Error(I(137,e));if(n.dangerouslySetInnerHTML!=null){if(n.children!=null)throw Error(I(60));if(typeof n.dangerouslySetInnerHTML!="object"||!("__html"in n.dangerouslySetInnerHTML))throw Error(I(61))}if(n.style!=null&&typeof n.style!="object")throw Error(I(62))}}function _o(e,n){if(e.indexOf("-")===-1)return typeof n.is=="string";switch(e){case"annotation-xml":case"color-profile":case"font-face":case"font-face-src":case"font-face-uri":case"font-face-format":case"font-face-name":case"missing-glyph":return!1;default:return!0}}var Eo=null;function Dl(e){return e=e.target||e.srcElement||window,e.correspondingUseElement&&(e=e.correspondingUseElement),e.nodeType===3?e.parentNode:e}var Co=null,rr=null,ar=null;function eu(e){if(e=Ca(e)){if(typeof Co!="function")throw Error(I(280));var n=e.stateNode;n&&(n=Zs(n),Co(e.stateNode,e.type,n))}}function Bh(e){rr?ar?ar.push(e):ar=[e]:rr=e}function Lh(){if(rr){var e=rr,n=ar;if(ar=rr=null,eu(e),n)for(e=0;e<n.length;e++)eu(n[e])}}function Nh(e,n){return e(n)}function Kh(){}var Ci=!1;function Wh(e,n,t){if(Ci)return e(n,t);Ci=!0;try{return Nh(e,n,t)}finally{Ci=!1,(rr!==null||ar!==null)&&(Kh(),Lh())}}function sa(e,n){var t=e.stateNode;if(t===null)return null;var r=Zs(t);if(r===null)return null;t=r[n];e:switch(n){case"onClick":case"onClickCapture":case"onDoubleClick":case"onDoubleClickCapture":case"onMouseDown":case"onMouseDownCapture":case"onMouseMove":case"onMouseMoveCapture":case"onMouseUp":case"onMouseUpCapture":case"onMouseEnter":(r=!r.disabled)||(e=e.type,r=!(e==="button"||e==="input"||e==="select"||e==="textarea")),e=!r;break e;default:e=!1}if(e)return null;if(t&&typeof t!="function")throw Error(I(231,n,typeof t));return t}var Ao=!1;if(Wn)try{var Mr={};Object.defineProperty(Mr,"passive",{get:function(){Ao=!0}}),window.addEventListener("test",Mr,Mr),window.removeEventListener("test",Mr,Mr)}catch{Ao=!1}function _f(e,n,t,r,a,s,i,o,l){var c=Array.prototype.slice.call(arguments,3);try{n.apply(t,c)}catch(d){this.onError(d)}}var Vr=!1,ks=null,bs=!1,Io=null,Ef={onError:function(e){Vr=!0,ks=e}};function Cf(e,n,t,r,a,s,i,o,l){Vr=!1,ks=null,_f.apply(Ef,arguments)}function Af(e,n,t,r,a,s,i,o,l){if(Cf.apply(this,arguments),Vr){if(Vr){var c=ks;Vr=!1,ks=null}else throw Error(I(198));bs||(bs=!0,Io=c)}}function Nt(e){var n=e,t=e;if(e.alternate)for(;n.return;)n=n.return;else{e=n;do n=e,n.flags&4098&&(t=n.return),e=n.return;while(e)}return n.tag===3?t:null}function jh(e){if(e.tag===13){var n=e.memoizedState;if(n===null&&(e=e.alternate,e!==null&&(n=e.memoizedState)),n!==null)return n.dehydrated}return null}function nu(e){if(Nt(e)!==e)throw Error(I(188))}function If(e){var n=e.alternate;if(!n){if(n=Nt(e),n===null)throw Error(I(188));return n!==e?null:e}for(var t=e,r=n;;){var a=t.return;if(a===null)break;var s=a.alternate;if(s===null){if(r=a.return,r!==null){t=r;continue}break}if(a.child===s.child){for(s=a.child;s;){if(s===t)return nu(a),e;if(s===r)return nu(a),n;s=s.sibling}throw Error(I(188))}if(t.return!==r.return)t=a,r=s;else{for(var i=!1,o=a.child;o;){if(o===t){i=!0,t=a,r=s;break}if(o===r){i=!0,r=a,t=s;break}o=o.sibling}if(!i){for(o=s.child;o;){if(o===t){i=!0,t=s,r=a;break}if(o===r){i=!0,r=s,t=a;break}o=o.sibling}if(!i)throw Error(I(189))}}if(t.alternate!==r)throw Error(I(190))}if(t.tag!==3)throw Error(I(188));return t.stateNode.current===t?e:n}function Fh(e){return e=If(e),e!==null?Uh(e):null}function Uh(e){if(e.tag===5||e.tag===6)return e;for(e=e.child;e!==null;){var n=Uh(e);if(n!==null)return n;e=e.sibling}return null}var zh=tn.unstable_scheduleCallback,tu=tn.unstable_cancelCallback,Tf=tn.unstable_shouldYield,Rf=tn.unstable_requestPaint,fe=tn.unstable_now,Mf=tn.unstable_getCurrentPriorityLevel,Bl=tn.unstable_ImmediatePriority,Oh=tn.unstable_UserBlockingPriority,Ss=tn.unstable_NormalPriority,Df=tn.unstable_LowPriority,Hh=tn.unstable_IdlePriority,Qs=null,An=null;function Bf(e){if(An&&typeof An.onCommitFiberRoot=="function")try{An.onCommitFiberRoot(Qs,e,void 0,(e.current.flags&128)===128)}catch{}}var kn=Math.clz32?Math.clz32:Kf,Lf=Math.log,Nf=Math.LN2;function Kf(e){return e>>>=0,e===0?32:31-(Lf(e)/Nf|0)|0}var Ua=64,za=4194304;function Or(e){switch(e&-e){case 1:return 1;case 2:return 2;case 4:return 4;case 8:return 8;case 16:return 16;case 32:return 32;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return e&4194240;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return e&130023424;case 134217728:return 134217728;case 268435456:return 268435456;case 536870912:return 536870912;case 1073741824:return 1073741824;default:return e}}function xs(e,n){var t=e.pendingLanes;if(t===0)return 0;var r=0,a=e.suspendedLanes,s=e.pingedLanes,i=t&268435455;if(i!==0){var o=i&~a;o!==0?r=Or(o):(s&=i,s!==0&&(r=Or(s)))}else i=t&~a,i!==0?r=Or(i):s!==0&&(r=Or(s));if(r===0)return 0;if(n!==0&&n!==r&&!(n&a)&&(a=r&-r,s=n&-n,a>=s||a===16&&(s&4194240)!==0))return n;if(r&4&&(r|=t&16),n=e.entangledLanes,n!==0)for(e=e.entanglements,n&=r;0<n;)t=31-kn(n),a=1<<t,r|=e[t],n&=~a;return r}function Wf(e,n){switch(e){case 1:case 2:case 4:return n+250;case 8:case 16:case 32:case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return n+5e3;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return-1;case 134217728:case 268435456:case 536870912:case 1073741824:return-1;default:return-1}}function jf(e,n){for(var t=e.suspendedLanes,r=e.pingedLanes,a=e.expirationTimes,s=e.pendingLanes;0<s;){var i=31-kn(s),o=1<<i,l=a[i];l===-1?(!(o&t)||o&r)&&(a[i]=Wf(o,n)):l<=n&&(e.expiredLanes|=o),s&=~o}}function To(e){return e=e.pendingLanes&-1073741825,e!==0?e:e&1073741824?1073741824:0}function qh(){var e=Ua;return Ua<<=1,!(Ua&4194240)&&(Ua=64),e}function Ai(e){for(var n=[],t=0;31>t;t++)n.push(e);return n}function _a(e,n,t){e.pendingLanes|=n,n!==536870912&&(e.suspendedLanes=0,e.pingedLanes=0),e=e.eventTimes,n=31-kn(n),e[n]=t}function Ff(e,n){var t=e.pendingLanes&~n;e.pendingLanes=n,e.suspendedLanes=0,e.pingedLanes=0,e.expiredLanes&=n,e.mutableReadLanes&=n,e.entangledLanes&=n,n=e.entanglements;var r=e.eventTimes;for(e=e.expirationTimes;0<t;){var a=31-kn(t),s=1<<a;n[a]=0,r[a]=-1,e[a]=-1,t&=~s}}function Ll(e,n){var t=e.entangledLanes|=n;for(e=e.entanglements;t;){var r=31-kn(t),a=1<<r;a&n|e[r]&n&&(e[r]|=n),t&=~a}}var ne=0;function Vh(e){return e&=-e,1<e?4<e?e&268435455?16:536870912:4:1}var Gh,Nl,$h,Qh,Jh,Ro=!1,Oa=[],tt=null,rt=null,at=null,ia=new Map,oa=new Map,Yn=[],Uf="mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset submit".split(" ");function ru(e,n){switch(e){case"focusin":case"focusout":tt=null;break;case"dragenter":case"dragleave":rt=null;break;case"mouseover":case"mouseout":at=null;break;case"pointerover":case"pointerout":ia.delete(n.pointerId);break;case"gotpointercapture":case"lostpointercapture":oa.delete(n.pointerId)}}function Dr(e,n,t,r,a,s){return e===null||e.nativeEvent!==s?(e={blockedOn:n,domEventName:t,eventSystemFlags:r,nativeEvent:s,targetContainers:[a]},n!==null&&(n=Ca(n),n!==null&&Nl(n)),e):(e.eventSystemFlags|=r,n=e.targetContainers,a!==null&&n.indexOf(a)===-1&&n.push(a),e)}function zf(e,n,t,r,a){switch(n){case"focusin":return tt=Dr(tt,e,n,t,r,a),!0;case"dragenter":return rt=Dr(rt,e,n,t,r,a),!0;case"mouseover":return at=Dr(at,e,n,t,r,a),!0;case"pointerover":var s=a.pointerId;return ia.set(s,Dr(ia.get(s)||null,e,n,t,r,a)),!0;case"gotpointercapture":return s=a.pointerId,oa.set(s,Dr(oa.get(s)||null,e,n,t,r,a)),!0}return!1}function Yh(e){var n=Pt(e.target);if(n!==null){var t=Nt(n);if(t!==null){if(n=t.tag,n===13){if(n=jh(t),n!==null){e.blockedOn=n,Jh(e.priority,function(){$h(t)});return}}else if(n===3&&t.stateNode.current.memoizedState.isDehydrated){e.blockedOn=t.tag===3?t.stateNode.containerInfo:null;return}}}e.blockedOn=null}function is(e){if(e.blockedOn!==null)return!1;for(var n=e.targetContainers;0<n.length;){var t=Mo(e.domEventName,e.eventSystemFlags,n[0],e.nativeEvent);if(t===null){t=e.nativeEvent;var r=new t.constructor(t.type,t);Eo=r,t.target.dispatchEvent(r),Eo=null}else return n=Ca(t),n!==null&&Nl(n),e.blockedOn=t,!1;n.shift()}return!0}function au(e,n,t){is(e)&&t.delete(n)}function Of(){Ro=!1,tt!==null&&is(tt)&&(tt=null),rt!==null&&is(rt)&&(rt=null),at!==null&&is(at)&&(at=null),ia.forEach(au),oa.forEach(au)}function Br(e,n){e.blockedOn===n&&(e.blockedOn=null,Ro||(Ro=!0,tn.unstable_scheduleCallback(tn.unstable_NormalPriority,Of)))}function la(e){function n(a){return Br(a,e)}if(0<Oa.length){Br(Oa[0],e);for(var t=1;t<Oa.length;t++){var r=Oa[t];r.blockedOn===e&&(r.blockedOn=null)}}for(tt!==null&&Br(tt,e),rt!==null&&Br(rt,e),at!==null&&Br(at,e),ia.forEach(n),oa.forEach(n),t=0;t<Yn.length;t++)r=Yn[t],r.blockedOn===e&&(r.blockedOn=null);for(;0<Yn.length&&(t=Yn[0],t.blockedOn===null);)Yh(t),t.blockedOn===null&&Yn.shift()}var sr=zn.ReactCurrentBatchConfig,Ps=!0;function Hf(e,n,t,r){var a=ne,s=sr.transition;sr.transition=null;try{ne=1,Kl(e,n,t,r)}finally{ne=a,sr.transition=s}}function qf(e,n,t,r){var a=ne,s=sr.transition;sr.transition=null;try{ne=4,Kl(e,n,t,r)}finally{ne=a,sr.transition=s}}function Kl(e,n,t,r){if(Ps){var a=Mo(e,n,t,r);if(a===null)Wi(e,n,r,_s,t),ru(e,r);else if(zf(a,e,n,t,r))r.stopPropagation();else if(ru(e,r),n&4&&-1<Uf.indexOf(e)){for(;a!==null;){var s=Ca(a);if(s!==null&&Gh(s),s=Mo(e,n,t,r),s===null&&Wi(e,n,r,_s,t),s===a)break;a=s}a!==null&&r.stopPropagation()}else Wi(e,n,r,null,t)}}var _s=null;function Mo(e,n,t,r){if(_s=null,e=Dl(r),e=Pt(e),e!==null)if(n=Nt(e),n===null)e=null;else if(t=n.tag,t===13){if(e=jh(n),e!==null)return e;e=null}else if(t===3){if(n.stateNode.current.memoizedState.isDehydrated)return n.tag===3?n.stateNode.containerInfo:null;e=null}else n!==e&&(e=null);return _s=e,null}function Xh(e){switch(e){case"cancel":case"click":case"close":case"contextmenu":case"copy":case"cut":case"auxclick":case"dblclick":case"dragend":case"dragstart":case"drop":case"focusin":case"focusout":case"input":case"invalid":case"keydown":case"keypress":case"keyup":case"mousedown":case"mouseup":case"paste":case"pause":case"play":case"pointercancel":case"pointerdown":case"pointerup":case"ratechange":case"reset":case"resize":case"seeked":case"submit":case"touchcancel":case"touchend":case"touchstart":case"volumechange":case"change":case"selectionchange":case"textInput":case"compositionstart":case"compositionend":case"compositionupdate":case"beforeblur":case"afterblur":case"beforeinput":case"blur":case"fullscreenchange":case"focus":case"hashchange":case"popstate":case"select":case"selectstart":return 1;case"drag":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"mousemove":case"mouseout":case"mouseover":case"pointermove":case"pointerout":case"pointerover":case"scroll":case"toggle":case"touchmove":case"wheel":case"mouseenter":case"mouseleave":case"pointerenter":case"pointerleave":return 4;case"message":switch(Mf()){case Bl:return 1;case Oh:return 4;case Ss:case Df:return 16;case Hh:return 536870912;default:return 16}default:return 16}}var Zn=null,Wl=null,os=null;function Zh(){if(os)return os;var e,n=Wl,t=n.length,r,a="value"in Zn?Zn.value:Zn.textContent,s=a.length;for(e=0;e<t&&n[e]===a[e];e++);var i=t-e;for(r=1;r<=i&&n[t-r]===a[s-r];r++);return os=a.slice(e,1<r?1-r:void 0)}function ls(e){var n=e.keyCode;return"charCode"in e?(e=e.charCode,e===0&&n===13&&(e=13)):e=n,e===10&&(e=13),32<=e||e===13?e:0}function Ha(){return!0}function su(){return!1}function an(e){function n(t,r,a,s,i){this._reactName=t,this._targetInst=a,this.type=r,this.nativeEvent=s,this.target=i,this.currentTarget=null;for(var o in e)e.hasOwnProperty(o)&&(t=e[o],this[o]=t?t(s):s[o]);return this.isDefaultPrevented=(s.defaultPrevented!=null?s.defaultPrevented:s.returnValue===!1)?Ha:su,this.isPropagationStopped=su,this}return me(n.prototype,{preventDefault:function(){this.defaultPrevented=!0;var t=this.nativeEvent;t&&(t.preventDefault?t.preventDefault():typeof t.returnValue!="unknown"&&(t.returnValue=!1),this.isDefaultPrevented=Ha)},stopPropagation:function(){var t=this.nativeEvent;t&&(t.stopPropagation?t.stopPropagation():typeof t.cancelBubble!="unknown"&&(t.cancelBubble=!0),this.isPropagationStopped=Ha)},persist:function(){},isPersistent:Ha}),n}var wr={eventPhase:0,bubbles:0,cancelable:0,timeStamp:function(e){return e.timeStamp||Date.now()},defaultPrevented:0,isTrusted:0},jl=an(wr),Ea=me({},wr,{view:0,detail:0}),Vf=an(Ea),Ii,Ti,Lr,Js=me({},Ea,{screenX:0,screenY:0,clientX:0,clientY:0,pageX:0,pageY:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,getModifierState:Fl,button:0,buttons:0,relatedTarget:function(e){return e.relatedTarget===void 0?e.fromElement===e.srcElement?e.toElement:e.fromElement:e.relatedTarget},movementX:function(e){return"movementX"in e?e.movementX:(e!==Lr&&(Lr&&e.type==="mousemove"?(Ii=e.screenX-Lr.screenX,Ti=e.screenY-Lr.screenY):Ti=Ii=0,Lr=e),Ii)},movementY:function(e){return"movementY"in e?e.movementY:Ti}}),iu=an(Js),Gf=me({},Js,{dataTransfer:0}),$f=an(Gf),Qf=me({},Ea,{relatedTarget:0}),Ri=an(Qf),Jf=me({},wr,{animationName:0,elapsedTime:0,pseudoElement:0}),Yf=an(Jf),Xf=me({},wr,{clipboardData:function(e){return"clipboardData"in e?e.clipboardData:window.clipboardData}}),Zf=an(Xf),ey=me({},wr,{data:0}),ou=an(ey),ny={Esc:"Escape",Spacebar:" ",Left:"ArrowLeft",Up:"ArrowUp",Right:"ArrowRight",Down:"ArrowDown",Del:"Delete",Win:"OS",Menu:"ContextMenu",Apps:"ContextMenu",Scroll:"ScrollLock",MozPrintableKey:"Unidentified"},ty={8:"Backspace",9:"Tab",12:"Clear",13:"Enter",16:"Shift",17:"Control",18:"Alt",19:"Pause",20:"CapsLock",27:"Escape",32:" ",33:"PageUp",34:"PageDown",35:"End",36:"Home",37:"ArrowLeft",38:"ArrowUp",39:"ArrowRight",40:"ArrowDown",45:"Insert",46:"Delete",112:"F1",113:"F2",114:"F3",115:"F4",116:"F5",117:"F6",118:"F7",119:"F8",120:"F9",121:"F10",122:"F11",123:"F12",144:"NumLock",145:"ScrollLock",224:"Meta"},ry={Alt:"altKey",Control:"ctrlKey",Meta:"metaKey",Shift:"shiftKey"};function ay(e){var n=this.nativeEvent;return n.getModifierState?n.getModifierState(e):(e=ry[e])?!!n[e]:!1}function Fl(){return ay}var sy=me({},Ea,{key:function(e){if(e.key){var n=ny[e.key]||e.key;if(n!=="Unidentified")return n}return e.type==="keypress"?(e=ls(e),e===13?"Enter":String.fromCharCode(e)):e.type==="keydown"||e.type==="keyup"?ty[e.keyCode]||"Unidentified":""},code:0,location:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,repeat:0,locale:0,getModifierState:Fl,charCode:function(e){return e.type==="keypress"?ls(e):0},keyCode:function(e){return e.type==="keydown"||e.type==="keyup"?e.keyCode:0},which:function(e){return e.type==="keypress"?ls(e):e.type==="keydown"||e.type==="keyup"?e.keyCode:0}}),iy=an(sy),oy=me({},Js,{pointerId:0,width:0,height:0,pressure:0,tangentialPressure:0,tiltX:0,tiltY:0,twist:0,pointerType:0,isPrimary:0}),lu=an(oy),ly=me({},Ea,{touches:0,targetTouches:0,changedTouches:0,altKey:0,metaKey:0,ctrlKey:0,shiftKey:0,getModifierState:Fl}),cy=an(ly),uy=me({},wr,{propertyName:0,elapsedTime:0,pseudoElement:0}),dy=an(uy),hy=me({},Js,{deltaX:function(e){return"deltaX"in e?e.deltaX:"wheelDeltaX"in e?-e.wheelDeltaX:0},deltaY:function(e){return"deltaY"in e?e.deltaY:"wheelDeltaY"in e?-e.wheelDeltaY:"wheelDelta"in e?-e.wheelDelta:0},deltaZ:0,deltaMode:0}),py=an(hy),my=[9,13,27,32],Ul=Wn&&"CompositionEvent"in window,Gr=null;Wn&&"documentMode"in document&&(Gr=document.documentMode);var gy=Wn&&"TextEvent"in window&&!Gr,ep=Wn&&(!Ul||Gr&&8<Gr&&11>=Gr),cu=" ",uu=!1;function np(e,n){switch(e){case"keyup":return my.indexOf(n.keyCode)!==-1;case"keydown":return n.keyCode!==229;case"keypress":case"mousedown":case"focusout":return!0;default:return!1}}function tp(e){return e=e.detail,typeof e=="object"&&"data"in e?e.data:null}var Vt=!1;function fy(e,n){switch(e){case"compositionend":return tp(n);case"keypress":return n.which!==32?null:(uu=!0,cu);case"textInput":return e=n.data,e===cu&&uu?null:e;default:return null}}function yy(e,n){if(Vt)return e==="compositionend"||!Ul&&np(e,n)?(e=Zh(),os=Wl=Zn=null,Vt=!1,e):null;switch(e){case"paste":return null;case"keypress":if(!(n.ctrlKey||n.altKey||n.metaKey)||n.ctrlKey&&n.altKey){if(n.char&&1<n.char.length)return n.char;if(n.which)return String.fromCharCode(n.which)}return null;case"compositionend":return ep&&n.locale!=="ko"?null:n.data;default:return null}}var wy={color:!0,date:!0,datetime:!0,"datetime-local":!0,email:!0,month:!0,number:!0,password:!0,range:!0,search:!0,tel:!0,text:!0,time:!0,url:!0,week:!0};function du(e){var n=e&&e.nodeName&&e.nodeName.toLowerCase();return n==="input"?!!wy[e.type]:n==="textarea"}function rp(e,n,t,r){Bh(r),n=Es(n,"onChange"),0<n.length&&(t=new jl("onChange","change",null,t,r),e.push({event:t,listeners:n}))}var $r=null,ca=null;function vy(e){mp(e,0)}function Ys(e){var n=Qt(e);if(Ch(n))return e}function ky(e,n){if(e==="change")return n}var ap=!1;if(Wn){var Mi;if(Wn){var Di="oninput"in document;if(!Di){var hu=document.createElement("div");hu.setAttribute("oninput","return;"),Di=typeof hu.oninput=="function"}Mi=Di}else Mi=!1;ap=Mi&&(!document.documentMode||9<document.documentMode)}function pu(){$r&&($r.detachEvent("onpropertychange",sp),ca=$r=null)}function sp(e){if(e.propertyName==="value"&&Ys(ca)){var n=[];rp(n,ca,e,Dl(e)),Wh(vy,n)}}function by(e,n,t){e==="focusin"?(pu(),$r=n,ca=t,$r.attachEvent("onpropertychange",sp)):e==="focusout"&&pu()}function Sy(e){if(e==="selectionchange"||e==="keyup"||e==="keydown")return Ys(ca)}function xy(e,n){if(e==="click")return Ys(n)}function Py(e,n){if(e==="input"||e==="change")return Ys(n)}function _y(e,n){return e===n&&(e!==0||1/e===1/n)||e!==e&&n!==n}var xn=typeof Object.is=="function"?Object.is:_y;function ua(e,n){if(xn(e,n))return!0;if(typeof e!="object"||e===null||typeof n!="object"||n===null)return!1;var t=Object.keys(e),r=Object.keys(n);if(t.length!==r.length)return!1;for(r=0;r<t.length;r++){var a=t[r];if(!mo.call(n,a)||!xn(e[a],n[a]))return!1}return!0}function mu(e){for(;e&&e.firstChild;)e=e.firstChild;return e}function gu(e,n){var t=mu(e);e=0;for(var r;t;){if(t.nodeType===3){if(r=e+t.textContent.length,e<=n&&r>=n)return{node:t,offset:n-e};e=r}e:{for(;t;){if(t.nextSibling){t=t.nextSibling;break e}t=t.parentNode}t=void 0}t=mu(t)}}function ip(e,n){return e&&n?e===n?!0:e&&e.nodeType===3?!1:n&&n.nodeType===3?ip(e,n.parentNode):"contains"in e?e.contains(n):e.compareDocumentPosition?!!(e.compareDocumentPosition(n)&16):!1:!1}function op(){for(var e=window,n=vs();n instanceof e.HTMLIFrameElement;){try{var t=typeof n.contentWindow.location.href=="string"}catch{t=!1}if(t)e=n.contentWindow;else break;n=vs(e.document)}return n}function zl(e){var n=e&&e.nodeName&&e.nodeName.toLowerCase();return n&&(n==="input"&&(e.type==="text"||e.type==="search"||e.type==="tel"||e.type==="url"||e.type==="password")||n==="textarea"||e.contentEditable==="true")}function Ey(e){var n=op(),t=e.focusedElem,r=e.selectionRange;if(n!==t&&t&&t.ownerDocument&&ip(t.ownerDocument.documentElement,t)){if(r!==null&&zl(t)){if(n=r.start,e=r.end,e===void 0&&(e=n),"selectionStart"in t)t.selectionStart=n,t.selectionEnd=Math.min(e,t.value.length);else if(e=(n=t.ownerDocument||document)&&n.defaultView||window,e.getSelection){e=e.getSelection();var a=t.textContent.length,s=Math.min(r.start,a);r=r.end===void 0?s:Math.min(r.end,a),!e.extend&&s>r&&(a=r,r=s,s=a),a=gu(t,s);var i=gu(t,r);a&&i&&(e.rangeCount!==1||e.anchorNode!==a.node||e.anchorOffset!==a.offset||e.focusNode!==i.node||e.focusOffset!==i.offset)&&(n=n.createRange(),n.setStart(a.node,a.offset),e.removeAllRanges(),s>r?(e.addRange(n),e.extend(i.node,i.offset)):(n.setEnd(i.node,i.offset),e.addRange(n)))}}for(n=[],e=t;e=e.parentNode;)e.nodeType===1&&n.push({element:e,left:e.scrollLeft,top:e.scrollTop});for(typeof t.focus=="function"&&t.focus(),t=0;t<n.length;t++)e=n[t],e.element.scrollLeft=e.left,e.element.scrollTop=e.top}}var Cy=Wn&&"documentMode"in document&&11>=document.documentMode,Gt=null,Do=null,Qr=null,Bo=!1;function fu(e,n,t){var r=t.window===t?t.document:t.nodeType===9?t:t.ownerDocument;Bo||Gt==null||Gt!==vs(r)||(r=Gt,"selectionStart"in r&&zl(r)?r={start:r.selectionStart,end:r.selectionEnd}:(r=(r.ownerDocument&&r.ownerDocument.defaultView||window).getSelection(),r={anchorNode:r.anchorNode,anchorOffset:r.anchorOffset,focusNode:r.focusNode,focusOffset:r.focusOffset}),Qr&&ua(Qr,r)||(Qr=r,r=Es(Do,"onSelect"),0<r.length&&(n=new jl("onSelect","select",null,n,t),e.push({event:n,listeners:r}),n.target=Gt)))}function qa(e,n){var t={};return t[e.toLowerCase()]=n.toLowerCase(),t["Webkit"+e]="webkit"+n,t["Moz"+e]="moz"+n,t}var $t={animationend:qa("Animation","AnimationEnd"),animationiteration:qa("Animation","AnimationIteration"),animationstart:qa("Animation","AnimationStart"),transitionend:qa("Transition","TransitionEnd")},Bi={},lp={};Wn&&(lp=document.createElement("div").style,"AnimationEvent"in window||(delete $t.animationend.animation,delete $t.animationiteration.animation,delete $t.animationstart.animation),"TransitionEvent"in window||delete $t.transitionend.transition);function Xs(e){if(Bi[e])return Bi[e];if(!$t[e])return e;var n=$t[e],t;for(t in n)if(n.hasOwnProperty(t)&&t in lp)return Bi[e]=n[t];return e}var cp=Xs("animationend"),up=Xs("animationiteration"),dp=Xs("animationstart"),hp=Xs("transitionend"),pp=new Map,yu="abort auxClick cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");function pt(e,n){pp.set(e,n),Lt(n,[e])}for(var Li=0;Li<yu.length;Li++){var Ni=yu[Li],Ay=Ni.toLowerCase(),Iy=Ni[0].toUpperCase()+Ni.slice(1);pt(Ay,"on"+Iy)}pt(cp,"onAnimationEnd");pt(up,"onAnimationIteration");pt(dp,"onAnimationStart");pt("dblclick","onDoubleClick");pt("focusin","onFocus");pt("focusout","onBlur");pt(hp,"onTransitionEnd");cr("onMouseEnter",["mouseout","mouseover"]);cr("onMouseLeave",["mouseout","mouseover"]);cr("onPointerEnter",["pointerout","pointerover"]);cr("onPointerLeave",["pointerout","pointerover"]);Lt("onChange","change click focusin focusout input keydown keyup selectionchange".split(" "));Lt("onSelect","focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" "));Lt("onBeforeInput",["compositionend","keypress","textInput","paste"]);Lt("onCompositionEnd","compositionend focusout keydown keypress keyup mousedown".split(" "));Lt("onCompositionStart","compositionstart focusout keydown keypress keyup mousedown".split(" "));Lt("onCompositionUpdate","compositionupdate focusout keydown keypress keyup mousedown".split(" "));var Hr="abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" "),Ty=new Set("cancel close invalid load scroll toggle".split(" ").concat(Hr));function wu(e,n,t){var r=e.type||"unknown-event";e.currentTarget=t,Af(r,n,void 0,e),e.currentTarget=null}function mp(e,n){n=(n&4)!==0;for(var t=0;t<e.length;t++){var r=e[t],a=r.event;r=r.listeners;e:{var s=void 0;if(n)for(var i=r.length-1;0<=i;i--){var o=r[i],l=o.instance,c=o.currentTarget;if(o=o.listener,l!==s&&a.isPropagationStopped())break e;wu(a,o,c),s=l}else for(i=0;i<r.length;i++){if(o=r[i],l=o.instance,c=o.currentTarget,o=o.listener,l!==s&&a.isPropagationStopped())break e;wu(a,o,c),s=l}}}if(bs)throw e=Io,bs=!1,Io=null,e}function le(e,n){var t=n[jo];t===void 0&&(t=n[jo]=new Set);var r=e+"__bubble";t.has(r)||(gp(n,e,2,!1),t.add(r))}function Ki(e,n,t){var r=0;n&&(r|=4),gp(t,e,r,n)}var Va="_reactListening"+Math.random().toString(36).slice(2);function da(e){if(!e[Va]){e[Va]=!0,Sh.forEach(function(t){t!=="selectionchange"&&(Ty.has(t)||Ki(t,!1,e),Ki(t,!0,e))});var n=e.nodeType===9?e:e.ownerDocument;n===null||n[Va]||(n[Va]=!0,Ki("selectionchange",!1,n))}}function gp(e,n,t,r){switch(Xh(n)){case 1:var a=Hf;break;case 4:a=qf;break;default:a=Kl}t=a.bind(null,n,t,e),a=void 0,!Ao||n!=="touchstart"&&n!=="touchmove"&&n!=="wheel"||(a=!0),r?a!==void 0?e.addEventListener(n,t,{capture:!0,passive:a}):e.addEventListener(n,t,!0):a!==void 0?e.addEventListener(n,t,{passive:a}):e.addEventListener(n,t,!1)}function Wi(e,n,t,r,a){var s=r;if(!(n&1)&&!(n&2)&&r!==null)e:for(;;){if(r===null)return;var i=r.tag;if(i===3||i===4){var o=r.stateNode.containerInfo;if(o===a||o.nodeType===8&&o.parentNode===a)break;if(i===4)for(i=r.return;i!==null;){var l=i.tag;if((l===3||l===4)&&(l=i.stateNode.containerInfo,l===a||l.nodeType===8&&l.parentNode===a))return;i=i.return}for(;o!==null;){if(i=Pt(o),i===null)return;if(l=i.tag,l===5||l===6){r=s=i;continue e}o=o.parentNode}}r=r.return}Wh(function(){var c=s,d=Dl(t),u=[];e:{var p=pp.get(e);if(p!==void 0){var m=jl,f=e;switch(e){case"keypress":if(ls(t)===0)break e;case"keydown":case"keyup":m=iy;break;case"focusin":f="focus",m=Ri;break;case"focusout":f="blur",m=Ri;break;case"beforeblur":case"afterblur":m=Ri;break;case"click":if(t.button===2)break e;case"auxclick":case"dblclick":case"mousedown":case"mousemove":case"mouseup":case"mouseout":case"mouseover":case"contextmenu":m=iu;break;case"drag":case"dragend":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"dragstart":case"drop":m=$f;break;case"touchcancel":case"touchend":case"touchmove":case"touchstart":m=cy;break;case cp:case up:case dp:m=Yf;break;case hp:m=dy;break;case"scroll":m=Vf;break;case"wheel":m=py;break;case"copy":case"cut":case"paste":m=Zf;break;case"gotpointercapture":case"lostpointercapture":case"pointercancel":case"pointerdown":case"pointermove":case"pointerout":case"pointerover":case"pointerup":m=lu}var k=(n&4)!==0,P=!k&&e==="scroll",g=k?p!==null?p+"Capture":null:p;k=[];for(var y=c,w;y!==null;){w=y;var _=w.stateNode;if(w.tag===5&&_!==null&&(w=_,g!==null&&(_=sa(y,g),_!=null&&k.push(ha(y,_,w)))),P)break;y=y.return}0<k.length&&(p=new m(p,f,null,t,d),u.push({event:p,listeners:k}))}}if(!(n&7)){e:{if(p=e==="mouseover"||e==="pointerover",m=e==="mouseout"||e==="pointerout",p&&t!==Eo&&(f=t.relatedTarget||t.fromElement)&&(Pt(f)||f[jn]))break e;if((m||p)&&(p=d.window===d?d:(p=d.ownerDocument)?p.defaultView||p.parentWindow:window,m?(f=t.relatedTarget||t.toElement,m=c,f=f?Pt(f):null,f!==null&&(P=Nt(f),f!==P||f.tag!==5&&f.tag!==6)&&(f=null)):(m=null,f=c),m!==f)){if(k=iu,_="onMouseLeave",g="onMouseEnter",y="mouse",(e==="pointerout"||e==="pointerover")&&(k=lu,_="onPointerLeave",g="onPointerEnter",y="pointer"),P=m==null?p:Qt(m),w=f==null?p:Qt(f),p=new k(_,y+"leave",m,t,d),p.target=P,p.relatedTarget=w,_=null,Pt(d)===c&&(k=new k(g,y+"enter",f,t,d),k.target=w,k.relatedTarget=P,_=k),P=_,m&&f)n:{for(k=m,g=f,y=0,w=k;w;w=Ut(w))y++;for(w=0,_=g;_;_=Ut(_))w++;for(;0<y-w;)k=Ut(k),y--;for(;0<w-y;)g=Ut(g),w--;for(;y--;){if(k===g||g!==null&&k===g.alternate)break n;k=Ut(k),g=Ut(g)}k=null}else k=null;m!==null&&vu(u,p,m,k,!1),f!==null&&P!==null&&vu(u,P,f,k,!0)}}e:{if(p=c?Qt(c):window,m=p.nodeName&&p.nodeName.toLowerCase(),m==="select"||m==="input"&&p.type==="file")var E=ky;else if(du(p))if(ap)E=Py;else{E=Sy;var S=by}else(m=p.nodeName)&&m.toLowerCase()==="input"&&(p.type==="checkbox"||p.type==="radio")&&(E=xy);if(E&&(E=E(e,c))){rp(u,E,t,d);break e}S&&S(e,p,c),e==="focusout"&&(S=p._wrapperState)&&S.controlled&&p.type==="number"&&bo(p,"number",p.value)}switch(S=c?Qt(c):window,e){case"focusin":(du(S)||S.contentEditable==="true")&&(Gt=S,Do=c,Qr=null);break;case"focusout":Qr=Do=Gt=null;break;case"mousedown":Bo=!0;break;case"contextmenu":case"mouseup":case"dragend":Bo=!1,fu(u,t,d);break;case"selectionchange":if(Cy)break;case"keydown":case"keyup":fu(u,t,d)}var A;if(Ul)e:{switch(e){case"compositionstart":var M="onCompositionStart";break e;case"compositionend":M="onCompositionEnd";break e;case"compositionupdate":M="onCompositionUpdate";break e}M=void 0}else Vt?np(e,t)&&(M="onCompositionEnd"):e==="keydown"&&t.keyCode===229&&(M="onCompositionStart");M&&(ep&&t.locale!=="ko"&&(Vt||M!=="onCompositionStart"?M==="onCompositionEnd"&&Vt&&(A=Zh()):(Zn=d,Wl="value"in Zn?Zn.value:Zn.textContent,Vt=!0)),S=Es(c,M),0<S.length&&(M=new ou(M,e,null,t,d),u.push({event:M,listeners:S}),A?M.data=A:(A=tp(t),A!==null&&(M.data=A)))),(A=gy?fy(e,t):yy(e,t))&&(c=Es(c,"onBeforeInput"),0<c.length&&(d=new ou("onBeforeInput","beforeinput",null,t,d),u.push({event:d,listeners:c}),d.data=A))}mp(u,n)})}function ha(e,n,t){return{instance:e,listener:n,currentTarget:t}}function Es(e,n){for(var t=n+"Capture",r=[];e!==null;){var a=e,s=a.stateNode;a.tag===5&&s!==null&&(a=s,s=sa(e,t),s!=null&&r.unshift(ha(e,s,a)),s=sa(e,n),s!=null&&r.push(ha(e,s,a))),e=e.return}return r}function Ut(e){if(e===null)return null;do e=e.return;while(e&&e.tag!==5);return e||null}function vu(e,n,t,r,a){for(var s=n._reactName,i=[];t!==null&&t!==r;){var o=t,l=o.alternate,c=o.stateNode;if(l!==null&&l===r)break;o.tag===5&&c!==null&&(o=c,a?(l=sa(t,s),l!=null&&i.unshift(ha(t,l,o))):a||(l=sa(t,s),l!=null&&i.push(ha(t,l,o)))),t=t.return}i.length!==0&&e.push({event:n,listeners:i})}var Ry=/\r\n?/g,My=/\u0000|\uFFFD/g;function ku(e){return(typeof e=="string"?e:""+e).replace(Ry,`
`).replace(My,"")}function Ga(e,n,t){if(n=ku(n),ku(e)!==n&&t)throw Error(I(425))}function Cs(){}var Lo=null,No=null;function Ko(e,n){return e==="textarea"||e==="noscript"||typeof n.children=="string"||typeof n.children=="number"||typeof n.dangerouslySetInnerHTML=="object"&&n.dangerouslySetInnerHTML!==null&&n.dangerouslySetInnerHTML.__html!=null}var Wo=typeof setTimeout=="function"?setTimeout:void 0,Dy=typeof clearTimeout=="function"?clearTimeout:void 0,bu=typeof Promise=="function"?Promise:void 0,By=typeof queueMicrotask=="function"?queueMicrotask:typeof bu<"u"?function(e){return bu.resolve(null).then(e).catch(Ly)}:Wo;function Ly(e){setTimeout(function(){throw e})}function ji(e,n){var t=n,r=0;do{var a=t.nextSibling;if(e.removeChild(t),a&&a.nodeType===8)if(t=a.data,t==="/$"){if(r===0){e.removeChild(a),la(n);return}r--}else t!=="$"&&t!=="$?"&&t!=="$!"||r++;t=a}while(t);la(n)}function st(e){for(;e!=null;e=e.nextSibling){var n=e.nodeType;if(n===1||n===3)break;if(n===8){if(n=e.data,n==="$"||n==="$!"||n==="$?")break;if(n==="/$")return null}}return e}function Su(e){e=e.previousSibling;for(var n=0;e;){if(e.nodeType===8){var t=e.data;if(t==="$"||t==="$!"||t==="$?"){if(n===0)return e;n--}else t==="/$"&&n++}e=e.previousSibling}return null}var vr=Math.random().toString(36).slice(2),Cn="__reactFiber$"+vr,pa="__reactProps$"+vr,jn="__reactContainer$"+vr,jo="__reactEvents$"+vr,Ny="__reactListeners$"+vr,Ky="__reactHandles$"+vr;function Pt(e){var n=e[Cn];if(n)return n;for(var t=e.parentNode;t;){if(n=t[jn]||t[Cn]){if(t=n.alternate,n.child!==null||t!==null&&t.child!==null)for(e=Su(e);e!==null;){if(t=e[Cn])return t;e=Su(e)}return n}e=t,t=e.parentNode}return null}function Ca(e){return e=e[Cn]||e[jn],!e||e.tag!==5&&e.tag!==6&&e.tag!==13&&e.tag!==3?null:e}function Qt(e){if(e.tag===5||e.tag===6)return e.stateNode;throw Error(I(33))}function Zs(e){return e[pa]||null}var Fo=[],Jt=-1;function mt(e){return{current:e}}function ce(e){0>Jt||(e.current=Fo[Jt],Fo[Jt]=null,Jt--)}function ie(e,n){Jt++,Fo[Jt]=e.current,e.current=n}var ht={},De=mt(ht),He=mt(!1),It=ht;function ur(e,n){var t=e.type.contextTypes;if(!t)return ht;var r=e.stateNode;if(r&&r.__reactInternalMemoizedUnmaskedChildContext===n)return r.__reactInternalMemoizedMaskedChildContext;var a={},s;for(s in t)a[s]=n[s];return r&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=n,e.__reactInternalMemoizedMaskedChildContext=a),a}function qe(e){return e=e.childContextTypes,e!=null}function As(){ce(He),ce(De)}function xu(e,n,t){if(De.current!==ht)throw Error(I(168));ie(De,n),ie(He,t)}function fp(e,n,t){var r=e.stateNode;if(n=n.childContextTypes,typeof r.getChildContext!="function")return t;r=r.getChildContext();for(var a in r)if(!(a in n))throw Error(I(108,bf(e)||"Unknown",a));return me({},t,r)}function Is(e){return e=(e=e.stateNode)&&e.__reactInternalMemoizedMergedChildContext||ht,It=De.current,ie(De,e),ie(He,He.current),!0}function Pu(e,n,t){var r=e.stateNode;if(!r)throw Error(I(169));t?(e=fp(e,n,It),r.__reactInternalMemoizedMergedChildContext=e,ce(He),ce(De),ie(De,e)):ce(He),ie(He,t)}var Bn=null,ei=!1,Fi=!1;function yp(e){Bn===null?Bn=[e]:Bn.push(e)}function Wy(e){ei=!0,yp(e)}function gt(){if(!Fi&&Bn!==null){Fi=!0;var e=0,n=ne;try{var t=Bn;for(ne=1;e<t.length;e++){var r=t[e];do r=r(!0);while(r!==null)}Bn=null,ei=!1}catch(a){throw Bn!==null&&(Bn=Bn.slice(e+1)),zh(Bl,gt),a}finally{ne=n,Fi=!1}}return null}var Yt=[],Xt=0,Ts=null,Rs=0,ln=[],cn=0,Tt=null,Ln=1,Nn="";function bt(e,n){Yt[Xt++]=Rs,Yt[Xt++]=Ts,Ts=e,Rs=n}function wp(e,n,t){ln[cn++]=Ln,ln[cn++]=Nn,ln[cn++]=Tt,Tt=e;var r=Ln;e=Nn;var a=32-kn(r)-1;r&=~(1<<a),t+=1;var s=32-kn(n)+a;if(30<s){var i=a-a%5;s=(r&(1<<i)-1).toString(32),r>>=i,a-=i,Ln=1<<32-kn(n)+a|t<<a|r,Nn=s+e}else Ln=1<<s|t<<a|r,Nn=e}function Ol(e){e.return!==null&&(bt(e,1),wp(e,1,0))}function Hl(e){for(;e===Ts;)Ts=Yt[--Xt],Yt[Xt]=null,Rs=Yt[--Xt],Yt[Xt]=null;for(;e===Tt;)Tt=ln[--cn],ln[cn]=null,Nn=ln[--cn],ln[cn]=null,Ln=ln[--cn],ln[cn]=null}var en=null,Ze=null,ue=!1,vn=null;function vp(e,n){var t=dn(5,null,null,0);t.elementType="DELETED",t.stateNode=n,t.return=e,n=e.deletions,n===null?(e.deletions=[t],e.flags|=16):n.push(t)}function _u(e,n){switch(e.tag){case 5:var t=e.type;return n=n.nodeType!==1||t.toLowerCase()!==n.nodeName.toLowerCase()?null:n,n!==null?(e.stateNode=n,en=e,Ze=st(n.firstChild),!0):!1;case 6:return n=e.pendingProps===""||n.nodeType!==3?null:n,n!==null?(e.stateNode=n,en=e,Ze=null,!0):!1;case 13:return n=n.nodeType!==8?null:n,n!==null?(t=Tt!==null?{id:Ln,overflow:Nn}:null,e.memoizedState={dehydrated:n,treeContext:t,retryLane:1073741824},t=dn(18,null,null,0),t.stateNode=n,t.return=e,e.child=t,en=e,Ze=null,!0):!1;default:return!1}}function Uo(e){return(e.mode&1)!==0&&(e.flags&128)===0}function zo(e){if(ue){var n=Ze;if(n){var t=n;if(!_u(e,n)){if(Uo(e))throw Error(I(418));n=st(t.nextSibling);var r=en;n&&_u(e,n)?vp(r,t):(e.flags=e.flags&-4097|2,ue=!1,en=e)}}else{if(Uo(e))throw Error(I(418));e.flags=e.flags&-4097|2,ue=!1,en=e}}}function Eu(e){for(e=e.return;e!==null&&e.tag!==5&&e.tag!==3&&e.tag!==13;)e=e.return;en=e}function $a(e){if(e!==en)return!1;if(!ue)return Eu(e),ue=!0,!1;var n;if((n=e.tag!==3)&&!(n=e.tag!==5)&&(n=e.type,n=n!=="head"&&n!=="body"&&!Ko(e.type,e.memoizedProps)),n&&(n=Ze)){if(Uo(e))throw kp(),Error(I(418));for(;n;)vp(e,n),n=st(n.nextSibling)}if(Eu(e),e.tag===13){if(e=e.memoizedState,e=e!==null?e.dehydrated:null,!e)throw Error(I(317));e:{for(e=e.nextSibling,n=0;e;){if(e.nodeType===8){var t=e.data;if(t==="/$"){if(n===0){Ze=st(e.nextSibling);break e}n--}else t!=="$"&&t!=="$!"&&t!=="$?"||n++}e=e.nextSibling}Ze=null}}else Ze=en?st(e.stateNode.nextSibling):null;return!0}function kp(){for(var e=Ze;e;)e=st(e.nextSibling)}function dr(){Ze=en=null,ue=!1}function ql(e){vn===null?vn=[e]:vn.push(e)}var jy=zn.ReactCurrentBatchConfig;function Nr(e,n,t){if(e=t.ref,e!==null&&typeof e!="function"&&typeof e!="object"){if(t._owner){if(t=t._owner,t){if(t.tag!==1)throw Error(I(309));var r=t.stateNode}if(!r)throw Error(I(147,e));var a=r,s=""+e;return n!==null&&n.ref!==null&&typeof n.ref=="function"&&n.ref._stringRef===s?n.ref:(n=function(i){var o=a.refs;i===null?delete o[s]:o[s]=i},n._stringRef=s,n)}if(typeof e!="string")throw Error(I(284));if(!t._owner)throw Error(I(290,e))}return e}function Qa(e,n){throw e=Object.prototype.toString.call(n),Error(I(31,e==="[object Object]"?"object with keys {"+Object.keys(n).join(", ")+"}":e))}function Cu(e){var n=e._init;return n(e._payload)}function bp(e){function n(g,y){if(e){var w=g.deletions;w===null?(g.deletions=[y],g.flags|=16):w.push(y)}}function t(g,y){if(!e)return null;for(;y!==null;)n(g,y),y=y.sibling;return null}function r(g,y){for(g=new Map;y!==null;)y.key!==null?g.set(y.key,y):g.set(y.index,y),y=y.sibling;return g}function a(g,y){return g=ct(g,y),g.index=0,g.sibling=null,g}function s(g,y,w){return g.index=w,e?(w=g.alternate,w!==null?(w=w.index,w<y?(g.flags|=2,y):w):(g.flags|=2,y)):(g.flags|=1048576,y)}function i(g){return e&&g.alternate===null&&(g.flags|=2),g}function o(g,y,w,_){return y===null||y.tag!==6?(y=Gi(w,g.mode,_),y.return=g,y):(y=a(y,w),y.return=g,y)}function l(g,y,w,_){var E=w.type;return E===qt?d(g,y,w.props.children,_,w.key):y!==null&&(y.elementType===E||typeof E=="object"&&E!==null&&E.$$typeof===Qn&&Cu(E)===y.type)?(_=a(y,w.props),_.ref=Nr(g,y,w),_.return=g,_):(_=gs(w.type,w.key,w.props,null,g.mode,_),_.ref=Nr(g,y,w),_.return=g,_)}function c(g,y,w,_){return y===null||y.tag!==4||y.stateNode.containerInfo!==w.containerInfo||y.stateNode.implementation!==w.implementation?(y=$i(w,g.mode,_),y.return=g,y):(y=a(y,w.children||[]),y.return=g,y)}function d(g,y,w,_,E){return y===null||y.tag!==7?(y=At(w,g.mode,_,E),y.return=g,y):(y=a(y,w),y.return=g,y)}function u(g,y,w){if(typeof y=="string"&&y!==""||typeof y=="number")return y=Gi(""+y,g.mode,w),y.return=g,y;if(typeof y=="object"&&y!==null){switch(y.$$typeof){case Wa:return w=gs(y.type,y.key,y.props,null,g.mode,w),w.ref=Nr(g,null,y),w.return=g,w;case Ht:return y=$i(y,g.mode,w),y.return=g,y;case Qn:var _=y._init;return u(g,_(y._payload),w)}if(zr(y)||Rr(y))return y=At(y,g.mode,w,null),y.return=g,y;Qa(g,y)}return null}function p(g,y,w,_){var E=y!==null?y.key:null;if(typeof w=="string"&&w!==""||typeof w=="number")return E!==null?null:o(g,y,""+w,_);if(typeof w=="object"&&w!==null){switch(w.$$typeof){case Wa:return w.key===E?l(g,y,w,_):null;case Ht:return w.key===E?c(g,y,w,_):null;case Qn:return E=w._init,p(g,y,E(w._payload),_)}if(zr(w)||Rr(w))return E!==null?null:d(g,y,w,_,null);Qa(g,w)}return null}function m(g,y,w,_,E){if(typeof _=="string"&&_!==""||typeof _=="number")return g=g.get(w)||null,o(y,g,""+_,E);if(typeof _=="object"&&_!==null){switch(_.$$typeof){case Wa:return g=g.get(_.key===null?w:_.key)||null,l(y,g,_,E);case Ht:return g=g.get(_.key===null?w:_.key)||null,c(y,g,_,E);case Qn:var S=_._init;return m(g,y,w,S(_._payload),E)}if(zr(_)||Rr(_))return g=g.get(w)||null,d(y,g,_,E,null);Qa(y,_)}return null}function f(g,y,w,_){for(var E=null,S=null,A=y,M=y=0,j=null;A!==null&&M<w.length;M++){A.index>M?(j=A,A=null):j=A.sibling;var x=p(g,A,w[M],_);if(x===null){A===null&&(A=j);break}e&&A&&x.alternate===null&&n(g,A),y=s(x,y,M),S===null?E=x:S.sibling=x,S=x,A=j}if(M===w.length)return t(g,A),ue&&bt(g,M),E;if(A===null){for(;M<w.length;M++)A=u(g,w[M],_),A!==null&&(y=s(A,y,M),S===null?E=A:S.sibling=A,S=A);return ue&&bt(g,M),E}for(A=r(g,A);M<w.length;M++)j=m(A,g,M,w[M],_),j!==null&&(e&&j.alternate!==null&&A.delete(j.key===null?M:j.key),y=s(j,y,M),S===null?E=j:S.sibling=j,S=j);return e&&A.forEach(function(L){return n(g,L)}),ue&&bt(g,M),E}function k(g,y,w,_){var E=Rr(w);if(typeof E!="function")throw Error(I(150));if(w=E.call(w),w==null)throw Error(I(151));for(var S=E=null,A=y,M=y=0,j=null,x=w.next();A!==null&&!x.done;M++,x=w.next()){A.index>M?(j=A,A=null):j=A.sibling;var L=p(g,A,x.value,_);if(L===null){A===null&&(A=j);break}e&&A&&L.alternate===null&&n(g,A),y=s(L,y,M),S===null?E=L:S.sibling=L,S=L,A=j}if(x.done)return t(g,A),ue&&bt(g,M),E;if(A===null){for(;!x.done;M++,x=w.next())x=u(g,x.value,_),x!==null&&(y=s(x,y,M),S===null?E=x:S.sibling=x,S=x);return ue&&bt(g,M),E}for(A=r(g,A);!x.done;M++,x=w.next())x=m(A,g,M,x.value,_),x!==null&&(e&&x.alternate!==null&&A.delete(x.key===null?M:x.key),y=s(x,y,M),S===null?E=x:S.sibling=x,S=x);return e&&A.forEach(function(W){return n(g,W)}),ue&&bt(g,M),E}function P(g,y,w,_){if(typeof w=="object"&&w!==null&&w.type===qt&&w.key===null&&(w=w.props.children),typeof w=="object"&&w!==null){switch(w.$$typeof){case Wa:e:{for(var E=w.key,S=y;S!==null;){if(S.key===E){if(E=w.type,E===qt){if(S.tag===7){t(g,S.sibling),y=a(S,w.props.children),y.return=g,g=y;break e}}else if(S.elementType===E||typeof E=="object"&&E!==null&&E.$$typeof===Qn&&Cu(E)===S.type){t(g,S.sibling),y=a(S,w.props),y.ref=Nr(g,S,w),y.return=g,g=y;break e}t(g,S);break}else n(g,S);S=S.sibling}w.type===qt?(y=At(w.props.children,g.mode,_,w.key),y.return=g,g=y):(_=gs(w.type,w.key,w.props,null,g.mode,_),_.ref=Nr(g,y,w),_.return=g,g=_)}return i(g);case Ht:e:{for(S=w.key;y!==null;){if(y.key===S)if(y.tag===4&&y.stateNode.containerInfo===w.containerInfo&&y.stateNode.implementation===w.implementation){t(g,y.sibling),y=a(y,w.children||[]),y.return=g,g=y;break e}else{t(g,y);break}else n(g,y);y=y.sibling}y=$i(w,g.mode,_),y.return=g,g=y}return i(g);case Qn:return S=w._init,P(g,y,S(w._payload),_)}if(zr(w))return f(g,y,w,_);if(Rr(w))return k(g,y,w,_);Qa(g,w)}return typeof w=="string"&&w!==""||typeof w=="number"?(w=""+w,y!==null&&y.tag===6?(t(g,y.sibling),y=a(y,w),y.return=g,g=y):(t(g,y),y=Gi(w,g.mode,_),y.return=g,g=y),i(g)):t(g,y)}return P}var hr=bp(!0),Sp=bp(!1),Ms=mt(null),Ds=null,Zt=null,Vl=null;function Gl(){Vl=Zt=Ds=null}function $l(e){var n=Ms.current;ce(Ms),e._currentValue=n}function Oo(e,n,t){for(;e!==null;){var r=e.alternate;if((e.childLanes&n)!==n?(e.childLanes|=n,r!==null&&(r.childLanes|=n)):r!==null&&(r.childLanes&n)!==n&&(r.childLanes|=n),e===t)break;e=e.return}}function ir(e,n){Ds=e,Vl=Zt=null,e=e.dependencies,e!==null&&e.firstContext!==null&&(e.lanes&n&&(Oe=!0),e.firstContext=null)}function pn(e){var n=e._currentValue;if(Vl!==e)if(e={context:e,memoizedValue:n,next:null},Zt===null){if(Ds===null)throw Error(I(308));Zt=e,Ds.dependencies={lanes:0,firstContext:e}}else Zt=Zt.next=e;return n}var _t=null;function Ql(e){_t===null?_t=[e]:_t.push(e)}function xp(e,n,t,r){var a=n.interleaved;return a===null?(t.next=t,Ql(n)):(t.next=a.next,a.next=t),n.interleaved=t,Fn(e,r)}function Fn(e,n){e.lanes|=n;var t=e.alternate;for(t!==null&&(t.lanes|=n),t=e,e=e.return;e!==null;)e.childLanes|=n,t=e.alternate,t!==null&&(t.childLanes|=n),t=e,e=e.return;return t.tag===3?t.stateNode:null}var Jn=!1;function Jl(e){e.updateQueue={baseState:e.memoizedState,firstBaseUpdate:null,lastBaseUpdate:null,shared:{pending:null,interleaved:null,lanes:0},effects:null}}function Pp(e,n){e=e.updateQueue,n.updateQueue===e&&(n.updateQueue={baseState:e.baseState,firstBaseUpdate:e.firstBaseUpdate,lastBaseUpdate:e.lastBaseUpdate,shared:e.shared,effects:e.effects})}function Kn(e,n){return{eventTime:e,lane:n,tag:0,payload:null,callback:null,next:null}}function it(e,n,t){var r=e.updateQueue;if(r===null)return null;if(r=r.shared,Z&2){var a=r.pending;return a===null?n.next=n:(n.next=a.next,a.next=n),r.pending=n,Fn(e,t)}return a=r.interleaved,a===null?(n.next=n,Ql(r)):(n.next=a.next,a.next=n),r.interleaved=n,Fn(e,t)}function cs(e,n,t){if(n=n.updateQueue,n!==null&&(n=n.shared,(t&4194240)!==0)){var r=n.lanes;r&=e.pendingLanes,t|=r,n.lanes=t,Ll(e,t)}}function Au(e,n){var t=e.updateQueue,r=e.alternate;if(r!==null&&(r=r.updateQueue,t===r)){var a=null,s=null;if(t=t.firstBaseUpdate,t!==null){do{var i={eventTime:t.eventTime,lane:t.lane,tag:t.tag,payload:t.payload,callback:t.callback,next:null};s===null?a=s=i:s=s.next=i,t=t.next}while(t!==null);s===null?a=s=n:s=s.next=n}else a=s=n;t={baseState:r.baseState,firstBaseUpdate:a,lastBaseUpdate:s,shared:r.shared,effects:r.effects},e.updateQueue=t;return}e=t.lastBaseUpdate,e===null?t.firstBaseUpdate=n:e.next=n,t.lastBaseUpdate=n}function Bs(e,n,t,r){var a=e.updateQueue;Jn=!1;var s=a.firstBaseUpdate,i=a.lastBaseUpdate,o=a.shared.pending;if(o!==null){a.shared.pending=null;var l=o,c=l.next;l.next=null,i===null?s=c:i.next=c,i=l;var d=e.alternate;d!==null&&(d=d.updateQueue,o=d.lastBaseUpdate,o!==i&&(o===null?d.firstBaseUpdate=c:o.next=c,d.lastBaseUpdate=l))}if(s!==null){var u=a.baseState;i=0,d=c=l=null,o=s;do{var p=o.lane,m=o.eventTime;if((r&p)===p){d!==null&&(d=d.next={eventTime:m,lane:0,tag:o.tag,payload:o.payload,callback:o.callback,next:null});e:{var f=e,k=o;switch(p=n,m=t,k.tag){case 1:if(f=k.payload,typeof f=="function"){u=f.call(m,u,p);break e}u=f;break e;case 3:f.flags=f.flags&-65537|128;case 0:if(f=k.payload,p=typeof f=="function"?f.call(m,u,p):f,p==null)break e;u=me({},u,p);break e;case 2:Jn=!0}}o.callback!==null&&o.lane!==0&&(e.flags|=64,p=a.effects,p===null?a.effects=[o]:p.push(o))}else m={eventTime:m,lane:p,tag:o.tag,payload:o.payload,callback:o.callback,next:null},d===null?(c=d=m,l=u):d=d.next=m,i|=p;if(o=o.next,o===null){if(o=a.shared.pending,o===null)break;p=o,o=p.next,p.next=null,a.lastBaseUpdate=p,a.shared.pending=null}}while(!0);if(d===null&&(l=u),a.baseState=l,a.firstBaseUpdate=c,a.lastBaseUpdate=d,n=a.shared.interleaved,n!==null){a=n;do i|=a.lane,a=a.next;while(a!==n)}else s===null&&(a.shared.lanes=0);Mt|=i,e.lanes=i,e.memoizedState=u}}function Iu(e,n,t){if(e=n.effects,n.effects=null,e!==null)for(n=0;n<e.length;n++){var r=e[n],a=r.callback;if(a!==null){if(r.callback=null,r=t,typeof a!="function")throw Error(I(191,a));a.call(r)}}}var Aa={},In=mt(Aa),ma=mt(Aa),ga=mt(Aa);function Et(e){if(e===Aa)throw Error(I(174));return e}function Yl(e,n){switch(ie(ga,n),ie(ma,e),ie(In,Aa),e=n.nodeType,e){case 9:case 11:n=(n=n.documentElement)?n.namespaceURI:xo(null,"");break;default:e=e===8?n.parentNode:n,n=e.namespaceURI||null,e=e.tagName,n=xo(n,e)}ce(In),ie(In,n)}function pr(){ce(In),ce(ma),ce(ga)}function _p(e){Et(ga.current);var n=Et(In.current),t=xo(n,e.type);n!==t&&(ie(ma,e),ie(In,t))}function Xl(e){ma.current===e&&(ce(In),ce(ma))}var he=mt(0);function Ls(e){for(var n=e;n!==null;){if(n.tag===13){var t=n.memoizedState;if(t!==null&&(t=t.dehydrated,t===null||t.data==="$?"||t.data==="$!"))return n}else if(n.tag===19&&n.memoizedProps.revealOrder!==void 0){if(n.flags&128)return n}else if(n.child!==null){n.child.return=n,n=n.child;continue}if(n===e)break;for(;n.sibling===null;){if(n.return===null||n.return===e)return null;n=n.return}n.sibling.return=n.return,n=n.sibling}return null}var Ui=[];function Zl(){for(var e=0;e<Ui.length;e++)Ui[e]._workInProgressVersionPrimary=null;Ui.length=0}var us=zn.ReactCurrentDispatcher,zi=zn.ReactCurrentBatchConfig,Rt=0,pe=null,ke=null,xe=null,Ns=!1,Jr=!1,fa=0,Fy=0;function Ie(){throw Error(I(321))}function ec(e,n){if(n===null)return!1;for(var t=0;t<n.length&&t<e.length;t++)if(!xn(e[t],n[t]))return!1;return!0}function nc(e,n,t,r,a,s){if(Rt=s,pe=n,n.memoizedState=null,n.updateQueue=null,n.lanes=0,us.current=e===null||e.memoizedState===null?Hy:qy,e=t(r,a),Jr){s=0;do{if(Jr=!1,fa=0,25<=s)throw Error(I(301));s+=1,xe=ke=null,n.updateQueue=null,us.current=Vy,e=t(r,a)}while(Jr)}if(us.current=Ks,n=ke!==null&&ke.next!==null,Rt=0,xe=ke=pe=null,Ns=!1,n)throw Error(I(300));return e}function tc(){var e=fa!==0;return fa=0,e}function _n(){var e={memoizedState:null,baseState:null,baseQueue:null,queue:null,next:null};return xe===null?pe.memoizedState=xe=e:xe=xe.next=e,xe}function mn(){if(ke===null){var e=pe.alternate;e=e!==null?e.memoizedState:null}else e=ke.next;var n=xe===null?pe.memoizedState:xe.next;if(n!==null)xe=n,ke=e;else{if(e===null)throw Error(I(310));ke=e,e={memoizedState:ke.memoizedState,baseState:ke.baseState,baseQueue:ke.baseQueue,queue:ke.queue,next:null},xe===null?pe.memoizedState=xe=e:xe=xe.next=e}return xe}function ya(e,n){return typeof n=="function"?n(e):n}function Oi(e){var n=mn(),t=n.queue;if(t===null)throw Error(I(311));t.lastRenderedReducer=e;var r=ke,a=r.baseQueue,s=t.pending;if(s!==null){if(a!==null){var i=a.next;a.next=s.next,s.next=i}r.baseQueue=a=s,t.pending=null}if(a!==null){s=a.next,r=r.baseState;var o=i=null,l=null,c=s;do{var d=c.lane;if((Rt&d)===d)l!==null&&(l=l.next={lane:0,action:c.action,hasEagerState:c.hasEagerState,eagerState:c.eagerState,next:null}),r=c.hasEagerState?c.eagerState:e(r,c.action);else{var u={lane:d,action:c.action,hasEagerState:c.hasEagerState,eagerState:c.eagerState,next:null};l===null?(o=l=u,i=r):l=l.next=u,pe.lanes|=d,Mt|=d}c=c.next}while(c!==null&&c!==s);l===null?i=r:l.next=o,xn(r,n.memoizedState)||(Oe=!0),n.memoizedState=r,n.baseState=i,n.baseQueue=l,t.lastRenderedState=r}if(e=t.interleaved,e!==null){a=e;do s=a.lane,pe.lanes|=s,Mt|=s,a=a.next;while(a!==e)}else a===null&&(t.lanes=0);return[n.memoizedState,t.dispatch]}function Hi(e){var n=mn(),t=n.queue;if(t===null)throw Error(I(311));t.lastRenderedReducer=e;var r=t.dispatch,a=t.pending,s=n.memoizedState;if(a!==null){t.pending=null;var i=a=a.next;do s=e(s,i.action),i=i.next;while(i!==a);xn(s,n.memoizedState)||(Oe=!0),n.memoizedState=s,n.baseQueue===null&&(n.baseState=s),t.lastRenderedState=s}return[s,r]}function Ep(){}function Cp(e,n){var t=pe,r=mn(),a=n(),s=!xn(r.memoizedState,a);if(s&&(r.memoizedState=a,Oe=!0),r=r.queue,rc(Tp.bind(null,t,r,e),[e]),r.getSnapshot!==n||s||xe!==null&&xe.memoizedState.tag&1){if(t.flags|=2048,wa(9,Ip.bind(null,t,r,a,n),void 0,null),Pe===null)throw Error(I(349));Rt&30||Ap(t,n,a)}return a}function Ap(e,n,t){e.flags|=16384,e={getSnapshot:n,value:t},n=pe.updateQueue,n===null?(n={lastEffect:null,stores:null},pe.updateQueue=n,n.stores=[e]):(t=n.stores,t===null?n.stores=[e]:t.push(e))}function Ip(e,n,t,r){n.value=t,n.getSnapshot=r,Rp(n)&&Mp(e)}function Tp(e,n,t){return t(function(){Rp(n)&&Mp(e)})}function Rp(e){var n=e.getSnapshot;e=e.value;try{var t=n();return!xn(e,t)}catch{return!0}}function Mp(e){var n=Fn(e,1);n!==null&&bn(n,e,1,-1)}function Tu(e){var n=_n();return typeof e=="function"&&(e=e()),n.memoizedState=n.baseState=e,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:ya,lastRenderedState:e},n.queue=e,e=e.dispatch=Oy.bind(null,pe,e),[n.memoizedState,e]}function wa(e,n,t,r){return e={tag:e,create:n,destroy:t,deps:r,next:null},n=pe.updateQueue,n===null?(n={lastEffect:null,stores:null},pe.updateQueue=n,n.lastEffect=e.next=e):(t=n.lastEffect,t===null?n.lastEffect=e.next=e:(r=t.next,t.next=e,e.next=r,n.lastEffect=e)),e}function Dp(){return mn().memoizedState}function ds(e,n,t,r){var a=_n();pe.flags|=e,a.memoizedState=wa(1|n,t,void 0,r===void 0?null:r)}function ni(e,n,t,r){var a=mn();r=r===void 0?null:r;var s=void 0;if(ke!==null){var i=ke.memoizedState;if(s=i.destroy,r!==null&&ec(r,i.deps)){a.memoizedState=wa(n,t,s,r);return}}pe.flags|=e,a.memoizedState=wa(1|n,t,s,r)}function Ru(e,n){return ds(8390656,8,e,n)}function rc(e,n){return ni(2048,8,e,n)}function Bp(e,n){return ni(4,2,e,n)}function Lp(e,n){return ni(4,4,e,n)}function Np(e,n){if(typeof n=="function")return e=e(),n(e),function(){n(null)};if(n!=null)return e=e(),n.current=e,function(){n.current=null}}function Kp(e,n,t){return t=t!=null?t.concat([e]):null,ni(4,4,Np.bind(null,n,e),t)}function ac(){}function Wp(e,n){var t=mn();n=n===void 0?null:n;var r=t.memoizedState;return r!==null&&n!==null&&ec(n,r[1])?r[0]:(t.memoizedState=[e,n],e)}function jp(e,n){var t=mn();n=n===void 0?null:n;var r=t.memoizedState;return r!==null&&n!==null&&ec(n,r[1])?r[0]:(e=e(),t.memoizedState=[e,n],e)}function Fp(e,n,t){return Rt&21?(xn(t,n)||(t=qh(),pe.lanes|=t,Mt|=t,e.baseState=!0),n):(e.baseState&&(e.baseState=!1,Oe=!0),e.memoizedState=t)}function Uy(e,n){var t=ne;ne=t!==0&&4>t?t:4,e(!0);var r=zi.transition;zi.transition={};try{e(!1),n()}finally{ne=t,zi.transition=r}}function Up(){return mn().memoizedState}function zy(e,n,t){var r=lt(e);if(t={lane:r,action:t,hasEagerState:!1,eagerState:null,next:null},zp(e))Op(n,t);else if(t=xp(e,n,t,r),t!==null){var a=We();bn(t,e,r,a),Hp(t,n,r)}}function Oy(e,n,t){var r=lt(e),a={lane:r,action:t,hasEagerState:!1,eagerState:null,next:null};if(zp(e))Op(n,a);else{var s=e.alternate;if(e.lanes===0&&(s===null||s.lanes===0)&&(s=n.lastRenderedReducer,s!==null))try{var i=n.lastRenderedState,o=s(i,t);if(a.hasEagerState=!0,a.eagerState=o,xn(o,i)){var l=n.interleaved;l===null?(a.next=a,Ql(n)):(a.next=l.next,l.next=a),n.interleaved=a;return}}catch{}finally{}t=xp(e,n,a,r),t!==null&&(a=We(),bn(t,e,r,a),Hp(t,n,r))}}function zp(e){var n=e.alternate;return e===pe||n!==null&&n===pe}function Op(e,n){Jr=Ns=!0;var t=e.pending;t===null?n.next=n:(n.next=t.next,t.next=n),e.pending=n}function Hp(e,n,t){if(t&4194240){var r=n.lanes;r&=e.pendingLanes,t|=r,n.lanes=t,Ll(e,t)}}var Ks={readContext:pn,useCallback:Ie,useContext:Ie,useEffect:Ie,useImperativeHandle:Ie,useInsertionEffect:Ie,useLayoutEffect:Ie,useMemo:Ie,useReducer:Ie,useRef:Ie,useState:Ie,useDebugValue:Ie,useDeferredValue:Ie,useTransition:Ie,useMutableSource:Ie,useSyncExternalStore:Ie,useId:Ie,unstable_isNewReconciler:!1},Hy={readContext:pn,useCallback:function(e,n){return _n().memoizedState=[e,n===void 0?null:n],e},useContext:pn,useEffect:Ru,useImperativeHandle:function(e,n,t){return t=t!=null?t.concat([e]):null,ds(4194308,4,Np.bind(null,n,e),t)},useLayoutEffect:function(e,n){return ds(4194308,4,e,n)},useInsertionEffect:function(e,n){return ds(4,2,e,n)},useMemo:function(e,n){var t=_n();return n=n===void 0?null:n,e=e(),t.memoizedState=[e,n],e},useReducer:function(e,n,t){var r=_n();return n=t!==void 0?t(n):n,r.memoizedState=r.baseState=n,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:e,lastRenderedState:n},r.queue=e,e=e.dispatch=zy.bind(null,pe,e),[r.memoizedState,e]},useRef:function(e){var n=_n();return e={current:e},n.memoizedState=e},useState:Tu,useDebugValue:ac,useDeferredValue:function(e){return _n().memoizedState=e},useTransition:function(){var e=Tu(!1),n=e[0];return e=Uy.bind(null,e[1]),_n().memoizedState=e,[n,e]},useMutableSource:function(){},useSyncExternalStore:function(e,n,t){var r=pe,a=_n();if(ue){if(t===void 0)throw Error(I(407));t=t()}else{if(t=n(),Pe===null)throw Error(I(349));Rt&30||Ap(r,n,t)}a.memoizedState=t;var s={value:t,getSnapshot:n};return a.queue=s,Ru(Tp.bind(null,r,s,e),[e]),r.flags|=2048,wa(9,Ip.bind(null,r,s,t,n),void 0,null),t},useId:function(){var e=_n(),n=Pe.identifierPrefix;if(ue){var t=Nn,r=Ln;t=(r&~(1<<32-kn(r)-1)).toString(32)+t,n=":"+n+"R"+t,t=fa++,0<t&&(n+="H"+t.toString(32)),n+=":"}else t=Fy++,n=":"+n+"r"+t.toString(32)+":";return e.memoizedState=n},unstable_isNewReconciler:!1},qy={readContext:pn,useCallback:Wp,useContext:pn,useEffect:rc,useImperativeHandle:Kp,useInsertionEffect:Bp,useLayoutEffect:Lp,useMemo:jp,useReducer:Oi,useRef:Dp,useState:function(){return Oi(ya)},useDebugValue:ac,useDeferredValue:function(e){var n=mn();return Fp(n,ke.memoizedState,e)},useTransition:function(){var e=Oi(ya)[0],n=mn().memoizedState;return[e,n]},useMutableSource:Ep,useSyncExternalStore:Cp,useId:Up,unstable_isNewReconciler:!1},Vy={readContext:pn,useCallback:Wp,useContext:pn,useEffect:rc,useImperativeHandle:Kp,useInsertionEffect:Bp,useLayoutEffect:Lp,useMemo:jp,useReducer:Hi,useRef:Dp,useState:function(){return Hi(ya)},useDebugValue:ac,useDeferredValue:function(e){var n=mn();return ke===null?n.memoizedState=e:Fp(n,ke.memoizedState,e)},useTransition:function(){var e=Hi(ya)[0],n=mn().memoizedState;return[e,n]},useMutableSource:Ep,useSyncExternalStore:Cp,useId:Up,unstable_isNewReconciler:!1};function yn(e,n){if(e&&e.defaultProps){n=me({},n),e=e.defaultProps;for(var t in e)n[t]===void 0&&(n[t]=e[t]);return n}return n}function Ho(e,n,t,r){n=e.memoizedState,t=t(r,n),t=t==null?n:me({},n,t),e.memoizedState=t,e.lanes===0&&(e.updateQueue.baseState=t)}var ti={isMounted:function(e){return(e=e._reactInternals)?Nt(e)===e:!1},enqueueSetState:function(e,n,t){e=e._reactInternals;var r=We(),a=lt(e),s=Kn(r,a);s.payload=n,t!=null&&(s.callback=t),n=it(e,s,a),n!==null&&(bn(n,e,a,r),cs(n,e,a))},enqueueReplaceState:function(e,n,t){e=e._reactInternals;var r=We(),a=lt(e),s=Kn(r,a);s.tag=1,s.payload=n,t!=null&&(s.callback=t),n=it(e,s,a),n!==null&&(bn(n,e,a,r),cs(n,e,a))},enqueueForceUpdate:function(e,n){e=e._reactInternals;var t=We(),r=lt(e),a=Kn(t,r);a.tag=2,n!=null&&(a.callback=n),n=it(e,a,r),n!==null&&(bn(n,e,r,t),cs(n,e,r))}};function Mu(e,n,t,r,a,s,i){return e=e.stateNode,typeof e.shouldComponentUpdate=="function"?e.shouldComponentUpdate(r,s,i):n.prototype&&n.prototype.isPureReactComponent?!ua(t,r)||!ua(a,s):!0}function qp(e,n,t){var r=!1,a=ht,s=n.contextType;return typeof s=="object"&&s!==null?s=pn(s):(a=qe(n)?It:De.current,r=n.contextTypes,s=(r=r!=null)?ur(e,a):ht),n=new n(t,s),e.memoizedState=n.state!==null&&n.state!==void 0?n.state:null,n.updater=ti,e.stateNode=n,n._reactInternals=e,r&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=a,e.__reactInternalMemoizedMaskedChildContext=s),n}function Du(e,n,t,r){e=n.state,typeof n.componentWillReceiveProps=="function"&&n.componentWillReceiveProps(t,r),typeof n.UNSAFE_componentWillReceiveProps=="function"&&n.UNSAFE_componentWillReceiveProps(t,r),n.state!==e&&ti.enqueueReplaceState(n,n.state,null)}function qo(e,n,t,r){var a=e.stateNode;a.props=t,a.state=e.memoizedState,a.refs={},Jl(e);var s=n.contextType;typeof s=="object"&&s!==null?a.context=pn(s):(s=qe(n)?It:De.current,a.context=ur(e,s)),a.state=e.memoizedState,s=n.getDerivedStateFromProps,typeof s=="function"&&(Ho(e,n,s,t),a.state=e.memoizedState),typeof n.getDerivedStateFromProps=="function"||typeof a.getSnapshotBeforeUpdate=="function"||typeof a.UNSAFE_componentWillMount!="function"&&typeof a.componentWillMount!="function"||(n=a.state,typeof a.componentWillMount=="function"&&a.componentWillMount(),typeof a.UNSAFE_componentWillMount=="function"&&a.UNSAFE_componentWillMount(),n!==a.state&&ti.enqueueReplaceState(a,a.state,null),Bs(e,t,a,r),a.state=e.memoizedState),typeof a.componentDidMount=="function"&&(e.flags|=4194308)}function mr(e,n){try{var t="",r=n;do t+=kf(r),r=r.return;while(r);var a=t}catch(s){a=`
Error generating stack: `+s.message+`
`+s.stack}return{value:e,source:n,stack:a,digest:null}}function qi(e,n,t){return{value:e,source:null,stack:t??null,digest:n??null}}function Vo(e,n){try{console.error(n.value)}catch(t){setTimeout(function(){throw t})}}var Gy=typeof WeakMap=="function"?WeakMap:Map;function Vp(e,n,t){t=Kn(-1,t),t.tag=3,t.payload={element:null};var r=n.value;return t.callback=function(){js||(js=!0,tl=r),Vo(e,n)},t}function Gp(e,n,t){t=Kn(-1,t),t.tag=3;var r=e.type.getDerivedStateFromError;if(typeof r=="function"){var a=n.value;t.payload=function(){return r(a)},t.callback=function(){Vo(e,n)}}var s=e.stateNode;return s!==null&&typeof s.componentDidCatch=="function"&&(t.callback=function(){Vo(e,n),typeof r!="function"&&(ot===null?ot=new Set([this]):ot.add(this));var i=n.stack;this.componentDidCatch(n.value,{componentStack:i!==null?i:""})}),t}function Bu(e,n,t){var r=e.pingCache;if(r===null){r=e.pingCache=new Gy;var a=new Set;r.set(n,a)}else a=r.get(n),a===void 0&&(a=new Set,r.set(n,a));a.has(t)||(a.add(t),e=ow.bind(null,e,n,t),n.then(e,e))}function Lu(e){do{var n;if((n=e.tag===13)&&(n=e.memoizedState,n=n!==null?n.dehydrated!==null:!0),n)return e;e=e.return}while(e!==null);return null}function Nu(e,n,t,r,a){return e.mode&1?(e.flags|=65536,e.lanes=a,e):(e===n?e.flags|=65536:(e.flags|=128,t.flags|=131072,t.flags&=-52805,t.tag===1&&(t.alternate===null?t.tag=17:(n=Kn(-1,1),n.tag=2,it(t,n,1))),t.lanes|=1),e)}var $y=zn.ReactCurrentOwner,Oe=!1;function Ne(e,n,t,r){n.child=e===null?Sp(n,null,t,r):hr(n,e.child,t,r)}function Ku(e,n,t,r,a){t=t.render;var s=n.ref;return ir(n,a),r=nc(e,n,t,r,s,a),t=tc(),e!==null&&!Oe?(n.updateQueue=e.updateQueue,n.flags&=-2053,e.lanes&=~a,Un(e,n,a)):(ue&&t&&Ol(n),n.flags|=1,Ne(e,n,r,a),n.child)}function Wu(e,n,t,r,a){if(e===null){var s=t.type;return typeof s=="function"&&!hc(s)&&s.defaultProps===void 0&&t.compare===null&&t.defaultProps===void 0?(n.tag=15,n.type=s,$p(e,n,s,r,a)):(e=gs(t.type,null,r,n,n.mode,a),e.ref=n.ref,e.return=n,n.child=e)}if(s=e.child,!(e.lanes&a)){var i=s.memoizedProps;if(t=t.compare,t=t!==null?t:ua,t(i,r)&&e.ref===n.ref)return Un(e,n,a)}return n.flags|=1,e=ct(s,r),e.ref=n.ref,e.return=n,n.child=e}function $p(e,n,t,r,a){if(e!==null){var s=e.memoizedProps;if(ua(s,r)&&e.ref===n.ref)if(Oe=!1,n.pendingProps=r=s,(e.lanes&a)!==0)e.flags&131072&&(Oe=!0);else return n.lanes=e.lanes,Un(e,n,a)}return Go(e,n,t,r,a)}function Qp(e,n,t){var r=n.pendingProps,a=r.children,s=e!==null?e.memoizedState:null;if(r.mode==="hidden")if(!(n.mode&1))n.memoizedState={baseLanes:0,cachePool:null,transitions:null},ie(nr,Xe),Xe|=t;else{if(!(t&1073741824))return e=s!==null?s.baseLanes|t:t,n.lanes=n.childLanes=1073741824,n.memoizedState={baseLanes:e,cachePool:null,transitions:null},n.updateQueue=null,ie(nr,Xe),Xe|=e,null;n.memoizedState={baseLanes:0,cachePool:null,transitions:null},r=s!==null?s.baseLanes:t,ie(nr,Xe),Xe|=r}else s!==null?(r=s.baseLanes|t,n.memoizedState=null):r=t,ie(nr,Xe),Xe|=r;return Ne(e,n,a,t),n.child}function Jp(e,n){var t=n.ref;(e===null&&t!==null||e!==null&&e.ref!==t)&&(n.flags|=512,n.flags|=2097152)}function Go(e,n,t,r,a){var s=qe(t)?It:De.current;return s=ur(n,s),ir(n,a),t=nc(e,n,t,r,s,a),r=tc(),e!==null&&!Oe?(n.updateQueue=e.updateQueue,n.flags&=-2053,e.lanes&=~a,Un(e,n,a)):(ue&&r&&Ol(n),n.flags|=1,Ne(e,n,t,a),n.child)}function ju(e,n,t,r,a){if(qe(t)){var s=!0;Is(n)}else s=!1;if(ir(n,a),n.stateNode===null)hs(e,n),qp(n,t,r),qo(n,t,r,a),r=!0;else if(e===null){var i=n.stateNode,o=n.memoizedProps;i.props=o;var l=i.context,c=t.contextType;typeof c=="object"&&c!==null?c=pn(c):(c=qe(t)?It:De.current,c=ur(n,c));var d=t.getDerivedStateFromProps,u=typeof d=="function"||typeof i.getSnapshotBeforeUpdate=="function";u||typeof i.UNSAFE_componentWillReceiveProps!="function"&&typeof i.componentWillReceiveProps!="function"||(o!==r||l!==c)&&Du(n,i,r,c),Jn=!1;var p=n.memoizedState;i.state=p,Bs(n,r,i,a),l=n.memoizedState,o!==r||p!==l||He.current||Jn?(typeof d=="function"&&(Ho(n,t,d,r),l=n.memoizedState),(o=Jn||Mu(n,t,o,r,p,l,c))?(u||typeof i.UNSAFE_componentWillMount!="function"&&typeof i.componentWillMount!="function"||(typeof i.componentWillMount=="function"&&i.componentWillMount(),typeof i.UNSAFE_componentWillMount=="function"&&i.UNSAFE_componentWillMount()),typeof i.componentDidMount=="function"&&(n.flags|=4194308)):(typeof i.componentDidMount=="function"&&(n.flags|=4194308),n.memoizedProps=r,n.memoizedState=l),i.props=r,i.state=l,i.context=c,r=o):(typeof i.componentDidMount=="function"&&(n.flags|=4194308),r=!1)}else{i=n.stateNode,Pp(e,n),o=n.memoizedProps,c=n.type===n.elementType?o:yn(n.type,o),i.props=c,u=n.pendingProps,p=i.context,l=t.contextType,typeof l=="object"&&l!==null?l=pn(l):(l=qe(t)?It:De.current,l=ur(n,l));var m=t.getDerivedStateFromProps;(d=typeof m=="function"||typeof i.getSnapshotBeforeUpdate=="function")||typeof i.UNSAFE_componentWillReceiveProps!="function"&&typeof i.componentWillReceiveProps!="function"||(o!==u||p!==l)&&Du(n,i,r,l),Jn=!1,p=n.memoizedState,i.state=p,Bs(n,r,i,a);var f=n.memoizedState;o!==u||p!==f||He.current||Jn?(typeof m=="function"&&(Ho(n,t,m,r),f=n.memoizedState),(c=Jn||Mu(n,t,c,r,p,f,l)||!1)?(d||typeof i.UNSAFE_componentWillUpdate!="function"&&typeof i.componentWillUpdate!="function"||(typeof i.componentWillUpdate=="function"&&i.componentWillUpdate(r,f,l),typeof i.UNSAFE_componentWillUpdate=="function"&&i.UNSAFE_componentWillUpdate(r,f,l)),typeof i.componentDidUpdate=="function"&&(n.flags|=4),typeof i.getSnapshotBeforeUpdate=="function"&&(n.flags|=1024)):(typeof i.componentDidUpdate!="function"||o===e.memoizedProps&&p===e.memoizedState||(n.flags|=4),typeof i.getSnapshotBeforeUpdate!="function"||o===e.memoizedProps&&p===e.memoizedState||(n.flags|=1024),n.memoizedProps=r,n.memoizedState=f),i.props=r,i.state=f,i.context=l,r=c):(typeof i.componentDidUpdate!="function"||o===e.memoizedProps&&p===e.memoizedState||(n.flags|=4),typeof i.getSnapshotBeforeUpdate!="function"||o===e.memoizedProps&&p===e.memoizedState||(n.flags|=1024),r=!1)}return $o(e,n,t,r,s,a)}function $o(e,n,t,r,a,s){Jp(e,n);var i=(n.flags&128)!==0;if(!r&&!i)return a&&Pu(n,t,!1),Un(e,n,s);r=n.stateNode,$y.current=n;var o=i&&typeof t.getDerivedStateFromError!="function"?null:r.render();return n.flags|=1,e!==null&&i?(n.child=hr(n,e.child,null,s),n.child=hr(n,null,o,s)):Ne(e,n,o,s),n.memoizedState=r.state,a&&Pu(n,t,!0),n.child}function Yp(e){var n=e.stateNode;n.pendingContext?xu(e,n.pendingContext,n.pendingContext!==n.context):n.context&&xu(e,n.context,!1),Yl(e,n.containerInfo)}function Fu(e,n,t,r,a){return dr(),ql(a),n.flags|=256,Ne(e,n,t,r),n.child}var Qo={dehydrated:null,treeContext:null,retryLane:0};function Jo(e){return{baseLanes:e,cachePool:null,transitions:null}}function Xp(e,n,t){var r=n.pendingProps,a=he.current,s=!1,i=(n.flags&128)!==0,o;if((o=i)||(o=e!==null&&e.memoizedState===null?!1:(a&2)!==0),o?(s=!0,n.flags&=-129):(e===null||e.memoizedState!==null)&&(a|=1),ie(he,a&1),e===null)return zo(n),e=n.memoizedState,e!==null&&(e=e.dehydrated,e!==null)?(n.mode&1?e.data==="$!"?n.lanes=8:n.lanes=1073741824:n.lanes=1,null):(i=r.children,e=r.fallback,s?(r=n.mode,s=n.child,i={mode:"hidden",children:i},!(r&1)&&s!==null?(s.childLanes=0,s.pendingProps=i):s=si(i,r,0,null),e=At(e,r,t,null),s.return=n,e.return=n,s.sibling=e,n.child=s,n.child.memoizedState=Jo(t),n.memoizedState=Qo,e):sc(n,i));if(a=e.memoizedState,a!==null&&(o=a.dehydrated,o!==null))return Qy(e,n,i,r,o,a,t);if(s){s=r.fallback,i=n.mode,a=e.child,o=a.sibling;var l={mode:"hidden",children:r.children};return!(i&1)&&n.child!==a?(r=n.child,r.childLanes=0,r.pendingProps=l,n.deletions=null):(r=ct(a,l),r.subtreeFlags=a.subtreeFlags&14680064),o!==null?s=ct(o,s):(s=At(s,i,t,null),s.flags|=2),s.return=n,r.return=n,r.sibling=s,n.child=r,r=s,s=n.child,i=e.child.memoizedState,i=i===null?Jo(t):{baseLanes:i.baseLanes|t,cachePool:null,transitions:i.transitions},s.memoizedState=i,s.childLanes=e.childLanes&~t,n.memoizedState=Qo,r}return s=e.child,e=s.sibling,r=ct(s,{mode:"visible",children:r.children}),!(n.mode&1)&&(r.lanes=t),r.return=n,r.sibling=null,e!==null&&(t=n.deletions,t===null?(n.deletions=[e],n.flags|=16):t.push(e)),n.child=r,n.memoizedState=null,r}function sc(e,n){return n=si({mode:"visible",children:n},e.mode,0,null),n.return=e,e.child=n}function Ja(e,n,t,r){return r!==null&&ql(r),hr(n,e.child,null,t),e=sc(n,n.pendingProps.children),e.flags|=2,n.memoizedState=null,e}function Qy(e,n,t,r,a,s,i){if(t)return n.flags&256?(n.flags&=-257,r=qi(Error(I(422))),Ja(e,n,i,r)):n.memoizedState!==null?(n.child=e.child,n.flags|=128,null):(s=r.fallback,a=n.mode,r=si({mode:"visible",children:r.children},a,0,null),s=At(s,a,i,null),s.flags|=2,r.return=n,s.return=n,r.sibling=s,n.child=r,n.mode&1&&hr(n,e.child,null,i),n.child.memoizedState=Jo(i),n.memoizedState=Qo,s);if(!(n.mode&1))return Ja(e,n,i,null);if(a.data==="$!"){if(r=a.nextSibling&&a.nextSibling.dataset,r)var o=r.dgst;return r=o,s=Error(I(419)),r=qi(s,r,void 0),Ja(e,n,i,r)}if(o=(i&e.childLanes)!==0,Oe||o){if(r=Pe,r!==null){switch(i&-i){case 4:a=2;break;case 16:a=8;break;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:a=32;break;case 536870912:a=268435456;break;default:a=0}a=a&(r.suspendedLanes|i)?0:a,a!==0&&a!==s.retryLane&&(s.retryLane=a,Fn(e,a),bn(r,e,a,-1))}return dc(),r=qi(Error(I(421))),Ja(e,n,i,r)}return a.data==="$?"?(n.flags|=128,n.child=e.child,n=lw.bind(null,e),a._reactRetry=n,null):(e=s.treeContext,Ze=st(a.nextSibling),en=n,ue=!0,vn=null,e!==null&&(ln[cn++]=Ln,ln[cn++]=Nn,ln[cn++]=Tt,Ln=e.id,Nn=e.overflow,Tt=n),n=sc(n,r.children),n.flags|=4096,n)}function Uu(e,n,t){e.lanes|=n;var r=e.alternate;r!==null&&(r.lanes|=n),Oo(e.return,n,t)}function Vi(e,n,t,r,a){var s=e.memoizedState;s===null?e.memoizedState={isBackwards:n,rendering:null,renderingStartTime:0,last:r,tail:t,tailMode:a}:(s.isBackwards=n,s.rendering=null,s.renderingStartTime=0,s.last=r,s.tail=t,s.tailMode=a)}function Zp(e,n,t){var r=n.pendingProps,a=r.revealOrder,s=r.tail;if(Ne(e,n,r.children,t),r=he.current,r&2)r=r&1|2,n.flags|=128;else{if(e!==null&&e.flags&128)e:for(e=n.child;e!==null;){if(e.tag===13)e.memoizedState!==null&&Uu(e,t,n);else if(e.tag===19)Uu(e,t,n);else if(e.child!==null){e.child.return=e,e=e.child;continue}if(e===n)break e;for(;e.sibling===null;){if(e.return===null||e.return===n)break e;e=e.return}e.sibling.return=e.return,e=e.sibling}r&=1}if(ie(he,r),!(n.mode&1))n.memoizedState=null;else switch(a){case"forwards":for(t=n.child,a=null;t!==null;)e=t.alternate,e!==null&&Ls(e)===null&&(a=t),t=t.sibling;t=a,t===null?(a=n.child,n.child=null):(a=t.sibling,t.sibling=null),Vi(n,!1,a,t,s);break;case"backwards":for(t=null,a=n.child,n.child=null;a!==null;){if(e=a.alternate,e!==null&&Ls(e)===null){n.child=a;break}e=a.sibling,a.sibling=t,t=a,a=e}Vi(n,!0,t,null,s);break;case"together":Vi(n,!1,null,null,void 0);break;default:n.memoizedState=null}return n.child}function hs(e,n){!(n.mode&1)&&e!==null&&(e.alternate=null,n.alternate=null,n.flags|=2)}function Un(e,n,t){if(e!==null&&(n.dependencies=e.dependencies),Mt|=n.lanes,!(t&n.childLanes))return null;if(e!==null&&n.child!==e.child)throw Error(I(153));if(n.child!==null){for(e=n.child,t=ct(e,e.pendingProps),n.child=t,t.return=n;e.sibling!==null;)e=e.sibling,t=t.sibling=ct(e,e.pendingProps),t.return=n;t.sibling=null}return n.child}function Jy(e,n,t){switch(n.tag){case 3:Yp(n),dr();break;case 5:_p(n);break;case 1:qe(n.type)&&Is(n);break;case 4:Yl(n,n.stateNode.containerInfo);break;case 10:var r=n.type._context,a=n.memoizedProps.value;ie(Ms,r._currentValue),r._currentValue=a;break;case 13:if(r=n.memoizedState,r!==null)return r.dehydrated!==null?(ie(he,he.current&1),n.flags|=128,null):t&n.child.childLanes?Xp(e,n,t):(ie(he,he.current&1),e=Un(e,n,t),e!==null?e.sibling:null);ie(he,he.current&1);break;case 19:if(r=(t&n.childLanes)!==0,e.flags&128){if(r)return Zp(e,n,t);n.flags|=128}if(a=n.memoizedState,a!==null&&(a.rendering=null,a.tail=null,a.lastEffect=null),ie(he,he.current),r)break;return null;case 22:case 23:return n.lanes=0,Qp(e,n,t)}return Un(e,n,t)}var em,Yo,nm,tm;em=function(e,n){for(var t=n.child;t!==null;){if(t.tag===5||t.tag===6)e.appendChild(t.stateNode);else if(t.tag!==4&&t.child!==null){t.child.return=t,t=t.child;continue}if(t===n)break;for(;t.sibling===null;){if(t.return===null||t.return===n)return;t=t.return}t.sibling.return=t.return,t=t.sibling}};Yo=function(){};nm=function(e,n,t,r){var a=e.memoizedProps;if(a!==r){e=n.stateNode,Et(In.current);var s=null;switch(t){case"input":a=vo(e,a),r=vo(e,r),s=[];break;case"select":a=me({},a,{value:void 0}),r=me({},r,{value:void 0}),s=[];break;case"textarea":a=So(e,a),r=So(e,r),s=[];break;default:typeof a.onClick!="function"&&typeof r.onClick=="function"&&(e.onclick=Cs)}Po(t,r);var i;t=null;for(c in a)if(!r.hasOwnProperty(c)&&a.hasOwnProperty(c)&&a[c]!=null)if(c==="style"){var o=a[c];for(i in o)o.hasOwnProperty(i)&&(t||(t={}),t[i]="")}else c!=="dangerouslySetInnerHTML"&&c!=="children"&&c!=="suppressContentEditableWarning"&&c!=="suppressHydrationWarning"&&c!=="autoFocus"&&(ra.hasOwnProperty(c)?s||(s=[]):(s=s||[]).push(c,null));for(c in r){var l=r[c];if(o=a!=null?a[c]:void 0,r.hasOwnProperty(c)&&l!==o&&(l!=null||o!=null))if(c==="style")if(o){for(i in o)!o.hasOwnProperty(i)||l&&l.hasOwnProperty(i)||(t||(t={}),t[i]="");for(i in l)l.hasOwnProperty(i)&&o[i]!==l[i]&&(t||(t={}),t[i]=l[i])}else t||(s||(s=[]),s.push(c,t)),t=l;else c==="dangerouslySetInnerHTML"?(l=l?l.__html:void 0,o=o?o.__html:void 0,l!=null&&o!==l&&(s=s||[]).push(c,l)):c==="children"?typeof l!="string"&&typeof l!="number"||(s=s||[]).push(c,""+l):c!=="suppressContentEditableWarning"&&c!=="suppressHydrationWarning"&&(ra.hasOwnProperty(c)?(l!=null&&c==="onScroll"&&le("scroll",e),s||o===l||(s=[])):(s=s||[]).push(c,l))}t&&(s=s||[]).push("style",t);var c=s;(n.updateQueue=c)&&(n.flags|=4)}};tm=function(e,n,t,r){t!==r&&(n.flags|=4)};function Kr(e,n){if(!ue)switch(e.tailMode){case"hidden":n=e.tail;for(var t=null;n!==null;)n.alternate!==null&&(t=n),n=n.sibling;t===null?e.tail=null:t.sibling=null;break;case"collapsed":t=e.tail;for(var r=null;t!==null;)t.alternate!==null&&(r=t),t=t.sibling;r===null?n||e.tail===null?e.tail=null:e.tail.sibling=null:r.sibling=null}}function Te(e){var n=e.alternate!==null&&e.alternate.child===e.child,t=0,r=0;if(n)for(var a=e.child;a!==null;)t|=a.lanes|a.childLanes,r|=a.subtreeFlags&14680064,r|=a.flags&14680064,a.return=e,a=a.sibling;else for(a=e.child;a!==null;)t|=a.lanes|a.childLanes,r|=a.subtreeFlags,r|=a.flags,a.return=e,a=a.sibling;return e.subtreeFlags|=r,e.childLanes=t,n}function Yy(e,n,t){var r=n.pendingProps;switch(Hl(n),n.tag){case 2:case 16:case 15:case 0:case 11:case 7:case 8:case 12:case 9:case 14:return Te(n),null;case 1:return qe(n.type)&&As(),Te(n),null;case 3:return r=n.stateNode,pr(),ce(He),ce(De),Zl(),r.pendingContext&&(r.context=r.pendingContext,r.pendingContext=null),(e===null||e.child===null)&&($a(n)?n.flags|=4:e===null||e.memoizedState.isDehydrated&&!(n.flags&256)||(n.flags|=1024,vn!==null&&(sl(vn),vn=null))),Yo(e,n),Te(n),null;case 5:Xl(n);var a=Et(ga.current);if(t=n.type,e!==null&&n.stateNode!=null)nm(e,n,t,r,a),e.ref!==n.ref&&(n.flags|=512,n.flags|=2097152);else{if(!r){if(n.stateNode===null)throw Error(I(166));return Te(n),null}if(e=Et(In.current),$a(n)){r=n.stateNode,t=n.type;var s=n.memoizedProps;switch(r[Cn]=n,r[pa]=s,e=(n.mode&1)!==0,t){case"dialog":le("cancel",r),le("close",r);break;case"iframe":case"object":case"embed":le("load",r);break;case"video":case"audio":for(a=0;a<Hr.length;a++)le(Hr[a],r);break;case"source":le("error",r);break;case"img":case"image":case"link":le("error",r),le("load",r);break;case"details":le("toggle",r);break;case"input":Jc(r,s),le("invalid",r);break;case"select":r._wrapperState={wasMultiple:!!s.multiple},le("invalid",r);break;case"textarea":Xc(r,s),le("invalid",r)}Po(t,s),a=null;for(var i in s)if(s.hasOwnProperty(i)){var o=s[i];i==="children"?typeof o=="string"?r.textContent!==o&&(s.suppressHydrationWarning!==!0&&Ga(r.textContent,o,e),a=["children",o]):typeof o=="number"&&r.textContent!==""+o&&(s.suppressHydrationWarning!==!0&&Ga(r.textContent,o,e),a=["children",""+o]):ra.hasOwnProperty(i)&&o!=null&&i==="onScroll"&&le("scroll",r)}switch(t){case"input":ja(r),Yc(r,s,!0);break;case"textarea":ja(r),Zc(r);break;case"select":case"option":break;default:typeof s.onClick=="function"&&(r.onclick=Cs)}r=a,n.updateQueue=r,r!==null&&(n.flags|=4)}else{i=a.nodeType===9?a:a.ownerDocument,e==="http://www.w3.org/1999/xhtml"&&(e=Th(t)),e==="http://www.w3.org/1999/xhtml"?t==="script"?(e=i.createElement("div"),e.innerHTML="<script><\/script>",e=e.removeChild(e.firstChild)):typeof r.is=="string"?e=i.createElement(t,{is:r.is}):(e=i.createElement(t),t==="select"&&(i=e,r.multiple?i.multiple=!0:r.size&&(i.size=r.size))):e=i.createElementNS(e,t),e[Cn]=n,e[pa]=r,em(e,n,!1,!1),n.stateNode=e;e:{switch(i=_o(t,r),t){case"dialog":le("cancel",e),le("close",e),a=r;break;case"iframe":case"object":case"embed":le("load",e),a=r;break;case"video":case"audio":for(a=0;a<Hr.length;a++)le(Hr[a],e);a=r;break;case"source":le("error",e),a=r;break;case"img":case"image":case"link":le("error",e),le("load",e),a=r;break;case"details":le("toggle",e),a=r;break;case"input":Jc(e,r),a=vo(e,r),le("invalid",e);break;case"option":a=r;break;case"select":e._wrapperState={wasMultiple:!!r.multiple},a=me({},r,{value:void 0}),le("invalid",e);break;case"textarea":Xc(e,r),a=So(e,r),le("invalid",e);break;default:a=r}Po(t,a),o=a;for(s in o)if(o.hasOwnProperty(s)){var l=o[s];s==="style"?Dh(e,l):s==="dangerouslySetInnerHTML"?(l=l?l.__html:void 0,l!=null&&Rh(e,l)):s==="children"?typeof l=="string"?(t!=="textarea"||l!=="")&&aa(e,l):typeof l=="number"&&aa(e,""+l):s!=="suppressContentEditableWarning"&&s!=="suppressHydrationWarning"&&s!=="autoFocus"&&(ra.hasOwnProperty(s)?l!=null&&s==="onScroll"&&le("scroll",e):l!=null&&Il(e,s,l,i))}switch(t){case"input":ja(e),Yc(e,r,!1);break;case"textarea":ja(e),Zc(e);break;case"option":r.value!=null&&e.setAttribute("value",""+dt(r.value));break;case"select":e.multiple=!!r.multiple,s=r.value,s!=null?tr(e,!!r.multiple,s,!1):r.defaultValue!=null&&tr(e,!!r.multiple,r.defaultValue,!0);break;default:typeof a.onClick=="function"&&(e.onclick=Cs)}switch(t){case"button":case"input":case"select":case"textarea":r=!!r.autoFocus;break e;case"img":r=!0;break e;default:r=!1}}r&&(n.flags|=4)}n.ref!==null&&(n.flags|=512,n.flags|=2097152)}return Te(n),null;case 6:if(e&&n.stateNode!=null)tm(e,n,e.memoizedProps,r);else{if(typeof r!="string"&&n.stateNode===null)throw Error(I(166));if(t=Et(ga.current),Et(In.current),$a(n)){if(r=n.stateNode,t=n.memoizedProps,r[Cn]=n,(s=r.nodeValue!==t)&&(e=en,e!==null))switch(e.tag){case 3:Ga(r.nodeValue,t,(e.mode&1)!==0);break;case 5:e.memoizedProps.suppressHydrationWarning!==!0&&Ga(r.nodeValue,t,(e.mode&1)!==0)}s&&(n.flags|=4)}else r=(t.nodeType===9?t:t.ownerDocument).createTextNode(r),r[Cn]=n,n.stateNode=r}return Te(n),null;case 13:if(ce(he),r=n.memoizedState,e===null||e.memoizedState!==null&&e.memoizedState.dehydrated!==null){if(ue&&Ze!==null&&n.mode&1&&!(n.flags&128))kp(),dr(),n.flags|=98560,s=!1;else if(s=$a(n),r!==null&&r.dehydrated!==null){if(e===null){if(!s)throw Error(I(318));if(s=n.memoizedState,s=s!==null?s.dehydrated:null,!s)throw Error(I(317));s[Cn]=n}else dr(),!(n.flags&128)&&(n.memoizedState=null),n.flags|=4;Te(n),s=!1}else vn!==null&&(sl(vn),vn=null),s=!0;if(!s)return n.flags&65536?n:null}return n.flags&128?(n.lanes=t,n):(r=r!==null,r!==(e!==null&&e.memoizedState!==null)&&r&&(n.child.flags|=8192,n.mode&1&&(e===null||he.current&1?be===0&&(be=3):dc())),n.updateQueue!==null&&(n.flags|=4),Te(n),null);case 4:return pr(),Yo(e,n),e===null&&da(n.stateNode.containerInfo),Te(n),null;case 10:return $l(n.type._context),Te(n),null;case 17:return qe(n.type)&&As(),Te(n),null;case 19:if(ce(he),s=n.memoizedState,s===null)return Te(n),null;if(r=(n.flags&128)!==0,i=s.rendering,i===null)if(r)Kr(s,!1);else{if(be!==0||e!==null&&e.flags&128)for(e=n.child;e!==null;){if(i=Ls(e),i!==null){for(n.flags|=128,Kr(s,!1),r=i.updateQueue,r!==null&&(n.updateQueue=r,n.flags|=4),n.subtreeFlags=0,r=t,t=n.child;t!==null;)s=t,e=r,s.flags&=14680066,i=s.alternate,i===null?(s.childLanes=0,s.lanes=e,s.child=null,s.subtreeFlags=0,s.memoizedProps=null,s.memoizedState=null,s.updateQueue=null,s.dependencies=null,s.stateNode=null):(s.childLanes=i.childLanes,s.lanes=i.lanes,s.child=i.child,s.subtreeFlags=0,s.deletions=null,s.memoizedProps=i.memoizedProps,s.memoizedState=i.memoizedState,s.updateQueue=i.updateQueue,s.type=i.type,e=i.dependencies,s.dependencies=e===null?null:{lanes:e.lanes,firstContext:e.firstContext}),t=t.sibling;return ie(he,he.current&1|2),n.child}e=e.sibling}s.tail!==null&&fe()>gr&&(n.flags|=128,r=!0,Kr(s,!1),n.lanes=4194304)}else{if(!r)if(e=Ls(i),e!==null){if(n.flags|=128,r=!0,t=e.updateQueue,t!==null&&(n.updateQueue=t,n.flags|=4),Kr(s,!0),s.tail===null&&s.tailMode==="hidden"&&!i.alternate&&!ue)return Te(n),null}else 2*fe()-s.renderingStartTime>gr&&t!==1073741824&&(n.flags|=128,r=!0,Kr(s,!1),n.lanes=4194304);s.isBackwards?(i.sibling=n.child,n.child=i):(t=s.last,t!==null?t.sibling=i:n.child=i,s.last=i)}return s.tail!==null?(n=s.tail,s.rendering=n,s.tail=n.sibling,s.renderingStartTime=fe(),n.sibling=null,t=he.current,ie(he,r?t&1|2:t&1),n):(Te(n),null);case 22:case 23:return uc(),r=n.memoizedState!==null,e!==null&&e.memoizedState!==null!==r&&(n.flags|=8192),r&&n.mode&1?Xe&1073741824&&(Te(n),n.subtreeFlags&6&&(n.flags|=8192)):Te(n),null;case 24:return null;case 25:return null}throw Error(I(156,n.tag))}function Xy(e,n){switch(Hl(n),n.tag){case 1:return qe(n.type)&&As(),e=n.flags,e&65536?(n.flags=e&-65537|128,n):null;case 3:return pr(),ce(He),ce(De),Zl(),e=n.flags,e&65536&&!(e&128)?(n.flags=e&-65537|128,n):null;case 5:return Xl(n),null;case 13:if(ce(he),e=n.memoizedState,e!==null&&e.dehydrated!==null){if(n.alternate===null)throw Error(I(340));dr()}return e=n.flags,e&65536?(n.flags=e&-65537|128,n):null;case 19:return ce(he),null;case 4:return pr(),null;case 10:return $l(n.type._context),null;case 22:case 23:return uc(),null;case 24:return null;default:return null}}var Ya=!1,Re=!1,Zy=typeof WeakSet=="function"?WeakSet:Set,K=null;function er(e,n){var t=e.ref;if(t!==null)if(typeof t=="function")try{t(null)}catch(r){ge(e,n,r)}else t.current=null}function Xo(e,n,t){try{t()}catch(r){ge(e,n,r)}}var zu=!1;function ew(e,n){if(Lo=Ps,e=op(),zl(e)){if("selectionStart"in e)var t={start:e.selectionStart,end:e.selectionEnd};else e:{t=(t=e.ownerDocument)&&t.defaultView||window;var r=t.getSelection&&t.getSelection();if(r&&r.rangeCount!==0){t=r.anchorNode;var a=r.anchorOffset,s=r.focusNode;r=r.focusOffset;try{t.nodeType,s.nodeType}catch{t=null;break e}var i=0,o=-1,l=-1,c=0,d=0,u=e,p=null;n:for(;;){for(var m;u!==t||a!==0&&u.nodeType!==3||(o=i+a),u!==s||r!==0&&u.nodeType!==3||(l=i+r),u.nodeType===3&&(i+=u.nodeValue.length),(m=u.firstChild)!==null;)p=u,u=m;for(;;){if(u===e)break n;if(p===t&&++c===a&&(o=i),p===s&&++d===r&&(l=i),(m=u.nextSibling)!==null)break;u=p,p=u.parentNode}u=m}t=o===-1||l===-1?null:{start:o,end:l}}else t=null}t=t||{start:0,end:0}}else t=null;for(No={focusedElem:e,selectionRange:t},Ps=!1,K=n;K!==null;)if(n=K,e=n.child,(n.subtreeFlags&1028)!==0&&e!==null)e.return=n,K=e;else for(;K!==null;){n=K;try{var f=n.alternate;if(n.flags&1024)switch(n.tag){case 0:case 11:case 15:break;case 1:if(f!==null){var k=f.memoizedProps,P=f.memoizedState,g=n.stateNode,y=g.getSnapshotBeforeUpdate(n.elementType===n.type?k:yn(n.type,k),P);g.__reactInternalSnapshotBeforeUpdate=y}break;case 3:var w=n.stateNode.containerInfo;w.nodeType===1?w.textContent="":w.nodeType===9&&w.documentElement&&w.removeChild(w.documentElement);break;case 5:case 6:case 4:case 17:break;default:throw Error(I(163))}}catch(_){ge(n,n.return,_)}if(e=n.sibling,e!==null){e.return=n.return,K=e;break}K=n.return}return f=zu,zu=!1,f}function Yr(e,n,t){var r=n.updateQueue;if(r=r!==null?r.lastEffect:null,r!==null){var a=r=r.next;do{if((a.tag&e)===e){var s=a.destroy;a.destroy=void 0,s!==void 0&&Xo(n,t,s)}a=a.next}while(a!==r)}}function ri(e,n){if(n=n.updateQueue,n=n!==null?n.lastEffect:null,n!==null){var t=n=n.next;do{if((t.tag&e)===e){var r=t.create;t.destroy=r()}t=t.next}while(t!==n)}}function Zo(e){var n=e.ref;if(n!==null){var t=e.stateNode;switch(e.tag){case 5:e=t;break;default:e=t}typeof n=="function"?n(e):n.current=e}}function rm(e){var n=e.alternate;n!==null&&(e.alternate=null,rm(n)),e.child=null,e.deletions=null,e.sibling=null,e.tag===5&&(n=e.stateNode,n!==null&&(delete n[Cn],delete n[pa],delete n[jo],delete n[Ny],delete n[Ky])),e.stateNode=null,e.return=null,e.dependencies=null,e.memoizedProps=null,e.memoizedState=null,e.pendingProps=null,e.stateNode=null,e.updateQueue=null}function am(e){return e.tag===5||e.tag===3||e.tag===4}function Ou(e){e:for(;;){for(;e.sibling===null;){if(e.return===null||am(e.return))return null;e=e.return}for(e.sibling.return=e.return,e=e.sibling;e.tag!==5&&e.tag!==6&&e.tag!==18;){if(e.flags&2||e.child===null||e.tag===4)continue e;e.child.return=e,e=e.child}if(!(e.flags&2))return e.stateNode}}function el(e,n,t){var r=e.tag;if(r===5||r===6)e=e.stateNode,n?t.nodeType===8?t.parentNode.insertBefore(e,n):t.insertBefore(e,n):(t.nodeType===8?(n=t.parentNode,n.insertBefore(e,t)):(n=t,n.appendChild(e)),t=t._reactRootContainer,t!=null||n.onclick!==null||(n.onclick=Cs));else if(r!==4&&(e=e.child,e!==null))for(el(e,n,t),e=e.sibling;e!==null;)el(e,n,t),e=e.sibling}function nl(e,n,t){var r=e.tag;if(r===5||r===6)e=e.stateNode,n?t.insertBefore(e,n):t.appendChild(e);else if(r!==4&&(e=e.child,e!==null))for(nl(e,n,t),e=e.sibling;e!==null;)nl(e,n,t),e=e.sibling}var _e=null,wn=!1;function Vn(e,n,t){for(t=t.child;t!==null;)sm(e,n,t),t=t.sibling}function sm(e,n,t){if(An&&typeof An.onCommitFiberUnmount=="function")try{An.onCommitFiberUnmount(Qs,t)}catch{}switch(t.tag){case 5:Re||er(t,n);case 6:var r=_e,a=wn;_e=null,Vn(e,n,t),_e=r,wn=a,_e!==null&&(wn?(e=_e,t=t.stateNode,e.nodeType===8?e.parentNode.removeChild(t):e.removeChild(t)):_e.removeChild(t.stateNode));break;case 18:_e!==null&&(wn?(e=_e,t=t.stateNode,e.nodeType===8?ji(e.parentNode,t):e.nodeType===1&&ji(e,t),la(e)):ji(_e,t.stateNode));break;case 4:r=_e,a=wn,_e=t.stateNode.containerInfo,wn=!0,Vn(e,n,t),_e=r,wn=a;break;case 0:case 11:case 14:case 15:if(!Re&&(r=t.updateQueue,r!==null&&(r=r.lastEffect,r!==null))){a=r=r.next;do{var s=a,i=s.destroy;s=s.tag,i!==void 0&&(s&2||s&4)&&Xo(t,n,i),a=a.next}while(a!==r)}Vn(e,n,t);break;case 1:if(!Re&&(er(t,n),r=t.stateNode,typeof r.componentWillUnmount=="function"))try{r.props=t.memoizedProps,r.state=t.memoizedState,r.componentWillUnmount()}catch(o){ge(t,n,o)}Vn(e,n,t);break;case 21:Vn(e,n,t);break;case 22:t.mode&1?(Re=(r=Re)||t.memoizedState!==null,Vn(e,n,t),Re=r):Vn(e,n,t);break;default:Vn(e,n,t)}}function Hu(e){var n=e.updateQueue;if(n!==null){e.updateQueue=null;var t=e.stateNode;t===null&&(t=e.stateNode=new Zy),n.forEach(function(r){var a=cw.bind(null,e,r);t.has(r)||(t.add(r),r.then(a,a))})}}function fn(e,n){var t=n.deletions;if(t!==null)for(var r=0;r<t.length;r++){var a=t[r];try{var s=e,i=n,o=i;e:for(;o!==null;){switch(o.tag){case 5:_e=o.stateNode,wn=!1;break e;case 3:_e=o.stateNode.containerInfo,wn=!0;break e;case 4:_e=o.stateNode.containerInfo,wn=!0;break e}o=o.return}if(_e===null)throw Error(I(160));sm(s,i,a),_e=null,wn=!1;var l=a.alternate;l!==null&&(l.return=null),a.return=null}catch(c){ge(a,n,c)}}if(n.subtreeFlags&12854)for(n=n.child;n!==null;)im(n,e),n=n.sibling}function im(e,n){var t=e.alternate,r=e.flags;switch(e.tag){case 0:case 11:case 14:case 15:if(fn(n,e),Pn(e),r&4){try{Yr(3,e,e.return),ri(3,e)}catch(k){ge(e,e.return,k)}try{Yr(5,e,e.return)}catch(k){ge(e,e.return,k)}}break;case 1:fn(n,e),Pn(e),r&512&&t!==null&&er(t,t.return);break;case 5:if(fn(n,e),Pn(e),r&512&&t!==null&&er(t,t.return),e.flags&32){var a=e.stateNode;try{aa(a,"")}catch(k){ge(e,e.return,k)}}if(r&4&&(a=e.stateNode,a!=null)){var s=e.memoizedProps,i=t!==null?t.memoizedProps:s,o=e.type,l=e.updateQueue;if(e.updateQueue=null,l!==null)try{o==="input"&&s.type==="radio"&&s.name!=null&&Ah(a,s),_o(o,i);var c=_o(o,s);for(i=0;i<l.length;i+=2){var d=l[i],u=l[i+1];d==="style"?Dh(a,u):d==="dangerouslySetInnerHTML"?Rh(a,u):d==="children"?aa(a,u):Il(a,d,u,c)}switch(o){case"input":ko(a,s);break;case"textarea":Ih(a,s);break;case"select":var p=a._wrapperState.wasMultiple;a._wrapperState.wasMultiple=!!s.multiple;var m=s.value;m!=null?tr(a,!!s.multiple,m,!1):p!==!!s.multiple&&(s.defaultValue!=null?tr(a,!!s.multiple,s.defaultValue,!0):tr(a,!!s.multiple,s.multiple?[]:"",!1))}a[pa]=s}catch(k){ge(e,e.return,k)}}break;case 6:if(fn(n,e),Pn(e),r&4){if(e.stateNode===null)throw Error(I(162));a=e.stateNode,s=e.memoizedProps;try{a.nodeValue=s}catch(k){ge(e,e.return,k)}}break;case 3:if(fn(n,e),Pn(e),r&4&&t!==null&&t.memoizedState.isDehydrated)try{la(n.containerInfo)}catch(k){ge(e,e.return,k)}break;case 4:fn(n,e),Pn(e);break;case 13:fn(n,e),Pn(e),a=e.child,a.flags&8192&&(s=a.memoizedState!==null,a.stateNode.isHidden=s,!s||a.alternate!==null&&a.alternate.memoizedState!==null||(lc=fe())),r&4&&Hu(e);break;case 22:if(d=t!==null&&t.memoizedState!==null,e.mode&1?(Re=(c=Re)||d,fn(n,e),Re=c):fn(n,e),Pn(e),r&8192){if(c=e.memoizedState!==null,(e.stateNode.isHidden=c)&&!d&&e.mode&1)for(K=e,d=e.child;d!==null;){for(u=K=d;K!==null;){switch(p=K,m=p.child,p.tag){case 0:case 11:case 14:case 15:Yr(4,p,p.return);break;case 1:er(p,p.return);var f=p.stateNode;if(typeof f.componentWillUnmount=="function"){r=p,t=p.return;try{n=r,f.props=n.memoizedProps,f.state=n.memoizedState,f.componentWillUnmount()}catch(k){ge(r,t,k)}}break;case 5:er(p,p.return);break;case 22:if(p.memoizedState!==null){Vu(u);continue}}m!==null?(m.return=p,K=m):Vu(u)}d=d.sibling}e:for(d=null,u=e;;){if(u.tag===5){if(d===null){d=u;try{a=u.stateNode,c?(s=a.style,typeof s.setProperty=="function"?s.setProperty("display","none","important"):s.display="none"):(o=u.stateNode,l=u.memoizedProps.style,i=l!=null&&l.hasOwnProperty("display")?l.display:null,o.style.display=Mh("display",i))}catch(k){ge(e,e.return,k)}}}else if(u.tag===6){if(d===null)try{u.stateNode.nodeValue=c?"":u.memoizedProps}catch(k){ge(e,e.return,k)}}else if((u.tag!==22&&u.tag!==23||u.memoizedState===null||u===e)&&u.child!==null){u.child.return=u,u=u.child;continue}if(u===e)break e;for(;u.sibling===null;){if(u.return===null||u.return===e)break e;d===u&&(d=null),u=u.return}d===u&&(d=null),u.sibling.return=u.return,u=u.sibling}}break;case 19:fn(n,e),Pn(e),r&4&&Hu(e);break;case 21:break;default:fn(n,e),Pn(e)}}function Pn(e){var n=e.flags;if(n&2){try{e:{for(var t=e.return;t!==null;){if(am(t)){var r=t;break e}t=t.return}throw Error(I(160))}switch(r.tag){case 5:var a=r.stateNode;r.flags&32&&(aa(a,""),r.flags&=-33);var s=Ou(e);nl(e,s,a);break;case 3:case 4:var i=r.stateNode.containerInfo,o=Ou(e);el(e,o,i);break;default:throw Error(I(161))}}catch(l){ge(e,e.return,l)}e.flags&=-3}n&4096&&(e.flags&=-4097)}function nw(e,n,t){K=e,om(e)}function om(e,n,t){for(var r=(e.mode&1)!==0;K!==null;){var a=K,s=a.child;if(a.tag===22&&r){var i=a.memoizedState!==null||Ya;if(!i){var o=a.alternate,l=o!==null&&o.memoizedState!==null||Re;o=Ya;var c=Re;if(Ya=i,(Re=l)&&!c)for(K=a;K!==null;)i=K,l=i.child,i.tag===22&&i.memoizedState!==null?Gu(a):l!==null?(l.return=i,K=l):Gu(a);for(;s!==null;)K=s,om(s),s=s.sibling;K=a,Ya=o,Re=c}qu(e)}else a.subtreeFlags&8772&&s!==null?(s.return=a,K=s):qu(e)}}function qu(e){for(;K!==null;){var n=K;if(n.flags&8772){var t=n.alternate;try{if(n.flags&8772)switch(n.tag){case 0:case 11:case 15:Re||ri(5,n);break;case 1:var r=n.stateNode;if(n.flags&4&&!Re)if(t===null)r.componentDidMount();else{var a=n.elementType===n.type?t.memoizedProps:yn(n.type,t.memoizedProps);r.componentDidUpdate(a,t.memoizedState,r.__reactInternalSnapshotBeforeUpdate)}var s=n.updateQueue;s!==null&&Iu(n,s,r);break;case 3:var i=n.updateQueue;if(i!==null){if(t=null,n.child!==null)switch(n.child.tag){case 5:t=n.child.stateNode;break;case 1:t=n.child.stateNode}Iu(n,i,t)}break;case 5:var o=n.stateNode;if(t===null&&n.flags&4){t=o;var l=n.memoizedProps;switch(n.type){case"button":case"input":case"select":case"textarea":l.autoFocus&&t.focus();break;case"img":l.src&&(t.src=l.src)}}break;case 6:break;case 4:break;case 12:break;case 13:if(n.memoizedState===null){var c=n.alternate;if(c!==null){var d=c.memoizedState;if(d!==null){var u=d.dehydrated;u!==null&&la(u)}}}break;case 19:case 17:case 21:case 22:case 23:case 25:break;default:throw Error(I(163))}Re||n.flags&512&&Zo(n)}catch(p){ge(n,n.return,p)}}if(n===e){K=null;break}if(t=n.sibling,t!==null){t.return=n.return,K=t;break}K=n.return}}function Vu(e){for(;K!==null;){var n=K;if(n===e){K=null;break}var t=n.sibling;if(t!==null){t.return=n.return,K=t;break}K=n.return}}function Gu(e){for(;K!==null;){var n=K;try{switch(n.tag){case 0:case 11:case 15:var t=n.return;try{ri(4,n)}catch(l){ge(n,t,l)}break;case 1:var r=n.stateNode;if(typeof r.componentDidMount=="function"){var a=n.return;try{r.componentDidMount()}catch(l){ge(n,a,l)}}var s=n.return;try{Zo(n)}catch(l){ge(n,s,l)}break;case 5:var i=n.return;try{Zo(n)}catch(l){ge(n,i,l)}}}catch(l){ge(n,n.return,l)}if(n===e){K=null;break}var o=n.sibling;if(o!==null){o.return=n.return,K=o;break}K=n.return}}var tw=Math.ceil,Ws=zn.ReactCurrentDispatcher,ic=zn.ReactCurrentOwner,hn=zn.ReactCurrentBatchConfig,Z=0,Pe=null,ve=null,Ee=0,Xe=0,nr=mt(0),be=0,va=null,Mt=0,ai=0,oc=0,Xr=null,ze=null,lc=0,gr=1/0,Dn=null,js=!1,tl=null,ot=null,Xa=!1,et=null,Fs=0,Zr=0,rl=null,ps=-1,ms=0;function We(){return Z&6?fe():ps!==-1?ps:ps=fe()}function lt(e){return e.mode&1?Z&2&&Ee!==0?Ee&-Ee:jy.transition!==null?(ms===0&&(ms=qh()),ms):(e=ne,e!==0||(e=window.event,e=e===void 0?16:Xh(e.type)),e):1}function bn(e,n,t,r){if(50<Zr)throw Zr=0,rl=null,Error(I(185));_a(e,t,r),(!(Z&2)||e!==Pe)&&(e===Pe&&(!(Z&2)&&(ai|=t),be===4&&Xn(e,Ee)),Ve(e,r),t===1&&Z===0&&!(n.mode&1)&&(gr=fe()+500,ei&&gt()))}function Ve(e,n){var t=e.callbackNode;jf(e,n);var r=xs(e,e===Pe?Ee:0);if(r===0)t!==null&&tu(t),e.callbackNode=null,e.callbackPriority=0;else if(n=r&-r,e.callbackPriority!==n){if(t!=null&&tu(t),n===1)e.tag===0?Wy($u.bind(null,e)):yp($u.bind(null,e)),By(function(){!(Z&6)&&gt()}),t=null;else{switch(Vh(r)){case 1:t=Bl;break;case 4:t=Oh;break;case 16:t=Ss;break;case 536870912:t=Hh;break;default:t=Ss}t=gm(t,lm.bind(null,e))}e.callbackPriority=n,e.callbackNode=t}}function lm(e,n){if(ps=-1,ms=0,Z&6)throw Error(I(327));var t=e.callbackNode;if(or()&&e.callbackNode!==t)return null;var r=xs(e,e===Pe?Ee:0);if(r===0)return null;if(r&30||r&e.expiredLanes||n)n=Us(e,r);else{n=r;var a=Z;Z|=2;var s=um();(Pe!==e||Ee!==n)&&(Dn=null,gr=fe()+500,Ct(e,n));do try{sw();break}catch(o){cm(e,o)}while(!0);Gl(),Ws.current=s,Z=a,ve!==null?n=0:(Pe=null,Ee=0,n=be)}if(n!==0){if(n===2&&(a=To(e),a!==0&&(r=a,n=al(e,a))),n===1)throw t=va,Ct(e,0),Xn(e,r),Ve(e,fe()),t;if(n===6)Xn(e,r);else{if(a=e.current.alternate,!(r&30)&&!rw(a)&&(n=Us(e,r),n===2&&(s=To(e),s!==0&&(r=s,n=al(e,s))),n===1))throw t=va,Ct(e,0),Xn(e,r),Ve(e,fe()),t;switch(e.finishedWork=a,e.finishedLanes=r,n){case 0:case 1:throw Error(I(345));case 2:St(e,ze,Dn);break;case 3:if(Xn(e,r),(r&130023424)===r&&(n=lc+500-fe(),10<n)){if(xs(e,0)!==0)break;if(a=e.suspendedLanes,(a&r)!==r){We(),e.pingedLanes|=e.suspendedLanes&a;break}e.timeoutHandle=Wo(St.bind(null,e,ze,Dn),n);break}St(e,ze,Dn);break;case 4:if(Xn(e,r),(r&4194240)===r)break;for(n=e.eventTimes,a=-1;0<r;){var i=31-kn(r);s=1<<i,i=n[i],i>a&&(a=i),r&=~s}if(r=a,r=fe()-r,r=(120>r?120:480>r?480:1080>r?1080:1920>r?1920:3e3>r?3e3:4320>r?4320:1960*tw(r/1960))-r,10<r){e.timeoutHandle=Wo(St.bind(null,e,ze,Dn),r);break}St(e,ze,Dn);break;case 5:St(e,ze,Dn);break;default:throw Error(I(329))}}}return Ve(e,fe()),e.callbackNode===t?lm.bind(null,e):null}function al(e,n){var t=Xr;return e.current.memoizedState.isDehydrated&&(Ct(e,n).flags|=256),e=Us(e,n),e!==2&&(n=ze,ze=t,n!==null&&sl(n)),e}function sl(e){ze===null?ze=e:ze.push.apply(ze,e)}function rw(e){for(var n=e;;){if(n.flags&16384){var t=n.updateQueue;if(t!==null&&(t=t.stores,t!==null))for(var r=0;r<t.length;r++){var a=t[r],s=a.getSnapshot;a=a.value;try{if(!xn(s(),a))return!1}catch{return!1}}}if(t=n.child,n.subtreeFlags&16384&&t!==null)t.return=n,n=t;else{if(n===e)break;for(;n.sibling===null;){if(n.return===null||n.return===e)return!0;n=n.return}n.sibling.return=n.return,n=n.sibling}}return!0}function Xn(e,n){for(n&=~oc,n&=~ai,e.suspendedLanes|=n,e.pingedLanes&=~n,e=e.expirationTimes;0<n;){var t=31-kn(n),r=1<<t;e[t]=-1,n&=~r}}function $u(e){if(Z&6)throw Error(I(327));or();var n=xs(e,0);if(!(n&1))return Ve(e,fe()),null;var t=Us(e,n);if(e.tag!==0&&t===2){var r=To(e);r!==0&&(n=r,t=al(e,r))}if(t===1)throw t=va,Ct(e,0),Xn(e,n),Ve(e,fe()),t;if(t===6)throw Error(I(345));return e.finishedWork=e.current.alternate,e.finishedLanes=n,St(e,ze,Dn),Ve(e,fe()),null}function cc(e,n){var t=Z;Z|=1;try{return e(n)}finally{Z=t,Z===0&&(gr=fe()+500,ei&&gt())}}function Dt(e){et!==null&&et.tag===0&&!(Z&6)&&or();var n=Z;Z|=1;var t=hn.transition,r=ne;try{if(hn.transition=null,ne=1,e)return e()}finally{ne=r,hn.transition=t,Z=n,!(Z&6)&&gt()}}function uc(){Xe=nr.current,ce(nr)}function Ct(e,n){e.finishedWork=null,e.finishedLanes=0;var t=e.timeoutHandle;if(t!==-1&&(e.timeoutHandle=-1,Dy(t)),ve!==null)for(t=ve.return;t!==null;){var r=t;switch(Hl(r),r.tag){case 1:r=r.type.childContextTypes,r!=null&&As();break;case 3:pr(),ce(He),ce(De),Zl();break;case 5:Xl(r);break;case 4:pr();break;case 13:ce(he);break;case 19:ce(he);break;case 10:$l(r.type._context);break;case 22:case 23:uc()}t=t.return}if(Pe=e,ve=e=ct(e.current,null),Ee=Xe=n,be=0,va=null,oc=ai=Mt=0,ze=Xr=null,_t!==null){for(n=0;n<_t.length;n++)if(t=_t[n],r=t.interleaved,r!==null){t.interleaved=null;var a=r.next,s=t.pending;if(s!==null){var i=s.next;s.next=a,r.next=i}t.pending=r}_t=null}return e}function cm(e,n){do{var t=ve;try{if(Gl(),us.current=Ks,Ns){for(var r=pe.memoizedState;r!==null;){var a=r.queue;a!==null&&(a.pending=null),r=r.next}Ns=!1}if(Rt=0,xe=ke=pe=null,Jr=!1,fa=0,ic.current=null,t===null||t.return===null){be=1,va=n,ve=null;break}e:{var s=e,i=t.return,o=t,l=n;if(n=Ee,o.flags|=32768,l!==null&&typeof l=="object"&&typeof l.then=="function"){var c=l,d=o,u=d.tag;if(!(d.mode&1)&&(u===0||u===11||u===15)){var p=d.alternate;p?(d.updateQueue=p.updateQueue,d.memoizedState=p.memoizedState,d.lanes=p.lanes):(d.updateQueue=null,d.memoizedState=null)}var m=Lu(i);if(m!==null){m.flags&=-257,Nu(m,i,o,s,n),m.mode&1&&Bu(s,c,n),n=m,l=c;var f=n.updateQueue;if(f===null){var k=new Set;k.add(l),n.updateQueue=k}else f.add(l);break e}else{if(!(n&1)){Bu(s,c,n),dc();break e}l=Error(I(426))}}else if(ue&&o.mode&1){var P=Lu(i);if(P!==null){!(P.flags&65536)&&(P.flags|=256),Nu(P,i,o,s,n),ql(mr(l,o));break e}}s=l=mr(l,o),be!==4&&(be=2),Xr===null?Xr=[s]:Xr.push(s),s=i;do{switch(s.tag){case 3:s.flags|=65536,n&=-n,s.lanes|=n;var g=Vp(s,l,n);Au(s,g);break e;case 1:o=l;var y=s.type,w=s.stateNode;if(!(s.flags&128)&&(typeof y.getDerivedStateFromError=="function"||w!==null&&typeof w.componentDidCatch=="function"&&(ot===null||!ot.has(w)))){s.flags|=65536,n&=-n,s.lanes|=n;var _=Gp(s,o,n);Au(s,_);break e}}s=s.return}while(s!==null)}hm(t)}catch(E){n=E,ve===t&&t!==null&&(ve=t=t.return);continue}break}while(!0)}function um(){var e=Ws.current;return Ws.current=Ks,e===null?Ks:e}function dc(){(be===0||be===3||be===2)&&(be=4),Pe===null||!(Mt&268435455)&&!(ai&268435455)||Xn(Pe,Ee)}function Us(e,n){var t=Z;Z|=2;var r=um();(Pe!==e||Ee!==n)&&(Dn=null,Ct(e,n));do try{aw();break}catch(a){cm(e,a)}while(!0);if(Gl(),Z=t,Ws.current=r,ve!==null)throw Error(I(261));return Pe=null,Ee=0,be}function aw(){for(;ve!==null;)dm(ve)}function sw(){for(;ve!==null&&!Tf();)dm(ve)}function dm(e){var n=mm(e.alternate,e,Xe);e.memoizedProps=e.pendingProps,n===null?hm(e):ve=n,ic.current=null}function hm(e){var n=e;do{var t=n.alternate;if(e=n.return,n.flags&32768){if(t=Xy(t,n),t!==null){t.flags&=32767,ve=t;return}if(e!==null)e.flags|=32768,e.subtreeFlags=0,e.deletions=null;else{be=6,ve=null;return}}else if(t=Yy(t,n,Xe),t!==null){ve=t;return}if(n=n.sibling,n!==null){ve=n;return}ve=n=e}while(n!==null);be===0&&(be=5)}function St(e,n,t){var r=ne,a=hn.transition;try{hn.transition=null,ne=1,iw(e,n,t,r)}finally{hn.transition=a,ne=r}return null}function iw(e,n,t,r){do or();while(et!==null);if(Z&6)throw Error(I(327));t=e.finishedWork;var a=e.finishedLanes;if(t===null)return null;if(e.finishedWork=null,e.finishedLanes=0,t===e.current)throw Error(I(177));e.callbackNode=null,e.callbackPriority=0;var s=t.lanes|t.childLanes;if(Ff(e,s),e===Pe&&(ve=Pe=null,Ee=0),!(t.subtreeFlags&2064)&&!(t.flags&2064)||Xa||(Xa=!0,gm(Ss,function(){return or(),null})),s=(t.flags&15990)!==0,t.subtreeFlags&15990||s){s=hn.transition,hn.transition=null;var i=ne;ne=1;var o=Z;Z|=4,ic.current=null,ew(e,t),im(t,e),Ey(No),Ps=!!Lo,No=Lo=null,e.current=t,nw(t),Rf(),Z=o,ne=i,hn.transition=s}else e.current=t;if(Xa&&(Xa=!1,et=e,Fs=a),s=e.pendingLanes,s===0&&(ot=null),Bf(t.stateNode),Ve(e,fe()),n!==null)for(r=e.onRecoverableError,t=0;t<n.length;t++)a=n[t],r(a.value,{componentStack:a.stack,digest:a.digest});if(js)throw js=!1,e=tl,tl=null,e;return Fs&1&&e.tag!==0&&or(),s=e.pendingLanes,s&1?e===rl?Zr++:(Zr=0,rl=e):Zr=0,gt(),null}function or(){if(et!==null){var e=Vh(Fs),n=hn.transition,t=ne;try{if(hn.transition=null,ne=16>e?16:e,et===null)var r=!1;else{if(e=et,et=null,Fs=0,Z&6)throw Error(I(331));var a=Z;for(Z|=4,K=e.current;K!==null;){var s=K,i=s.child;if(K.flags&16){var o=s.deletions;if(o!==null){for(var l=0;l<o.length;l++){var c=o[l];for(K=c;K!==null;){var d=K;switch(d.tag){case 0:case 11:case 15:Yr(8,d,s)}var u=d.child;if(u!==null)u.return=d,K=u;else for(;K!==null;){d=K;var p=d.sibling,m=d.return;if(rm(d),d===c){K=null;break}if(p!==null){p.return=m,K=p;break}K=m}}}var f=s.alternate;if(f!==null){var k=f.child;if(k!==null){f.child=null;do{var P=k.sibling;k.sibling=null,k=P}while(k!==null)}}K=s}}if(s.subtreeFlags&2064&&i!==null)i.return=s,K=i;else e:for(;K!==null;){if(s=K,s.flags&2048)switch(s.tag){case 0:case 11:case 15:Yr(9,s,s.return)}var g=s.sibling;if(g!==null){g.return=s.return,K=g;break e}K=s.return}}var y=e.current;for(K=y;K!==null;){i=K;var w=i.child;if(i.subtreeFlags&2064&&w!==null)w.return=i,K=w;else e:for(i=y;K!==null;){if(o=K,o.flags&2048)try{switch(o.tag){case 0:case 11:case 15:ri(9,o)}}catch(E){ge(o,o.return,E)}if(o===i){K=null;break e}var _=o.sibling;if(_!==null){_.return=o.return,K=_;break e}K=o.return}}if(Z=a,gt(),An&&typeof An.onPostCommitFiberRoot=="function")try{An.onPostCommitFiberRoot(Qs,e)}catch{}r=!0}return r}finally{ne=t,hn.transition=n}}return!1}function Qu(e,n,t){n=mr(t,n),n=Vp(e,n,1),e=it(e,n,1),n=We(),e!==null&&(_a(e,1,n),Ve(e,n))}function ge(e,n,t){if(e.tag===3)Qu(e,e,t);else for(;n!==null;){if(n.tag===3){Qu(n,e,t);break}else if(n.tag===1){var r=n.stateNode;if(typeof n.type.getDerivedStateFromError=="function"||typeof r.componentDidCatch=="function"&&(ot===null||!ot.has(r))){e=mr(t,e),e=Gp(n,e,1),n=it(n,e,1),e=We(),n!==null&&(_a(n,1,e),Ve(n,e));break}}n=n.return}}function ow(e,n,t){var r=e.pingCache;r!==null&&r.delete(n),n=We(),e.pingedLanes|=e.suspendedLanes&t,Pe===e&&(Ee&t)===t&&(be===4||be===3&&(Ee&130023424)===Ee&&500>fe()-lc?Ct(e,0):oc|=t),Ve(e,n)}function pm(e,n){n===0&&(e.mode&1?(n=za,za<<=1,!(za&130023424)&&(za=4194304)):n=1);var t=We();e=Fn(e,n),e!==null&&(_a(e,n,t),Ve(e,t))}function lw(e){var n=e.memoizedState,t=0;n!==null&&(t=n.retryLane),pm(e,t)}function cw(e,n){var t=0;switch(e.tag){case 13:var r=e.stateNode,a=e.memoizedState;a!==null&&(t=a.retryLane);break;case 19:r=e.stateNode;break;default:throw Error(I(314))}r!==null&&r.delete(n),pm(e,t)}var mm;mm=function(e,n,t){if(e!==null)if(e.memoizedProps!==n.pendingProps||He.current)Oe=!0;else{if(!(e.lanes&t)&&!(n.flags&128))return Oe=!1,Jy(e,n,t);Oe=!!(e.flags&131072)}else Oe=!1,ue&&n.flags&1048576&&wp(n,Rs,n.index);switch(n.lanes=0,n.tag){case 2:var r=n.type;hs(e,n),e=n.pendingProps;var a=ur(n,De.current);ir(n,t),a=nc(null,n,r,e,a,t);var s=tc();return n.flags|=1,typeof a=="object"&&a!==null&&typeof a.render=="function"&&a.$$typeof===void 0?(n.tag=1,n.memoizedState=null,n.updateQueue=null,qe(r)?(s=!0,Is(n)):s=!1,n.memoizedState=a.state!==null&&a.state!==void 0?a.state:null,Jl(n),a.updater=ti,n.stateNode=a,a._reactInternals=n,qo(n,r,e,t),n=$o(null,n,r,!0,s,t)):(n.tag=0,ue&&s&&Ol(n),Ne(null,n,a,t),n=n.child),n;case 16:r=n.elementType;e:{switch(hs(e,n),e=n.pendingProps,a=r._init,r=a(r._payload),n.type=r,a=n.tag=dw(r),e=yn(r,e),a){case 0:n=Go(null,n,r,e,t);break e;case 1:n=ju(null,n,r,e,t);break e;case 11:n=Ku(null,n,r,e,t);break e;case 14:n=Wu(null,n,r,yn(r.type,e),t);break e}throw Error(I(306,r,""))}return n;case 0:return r=n.type,a=n.pendingProps,a=n.elementType===r?a:yn(r,a),Go(e,n,r,a,t);case 1:return r=n.type,a=n.pendingProps,a=n.elementType===r?a:yn(r,a),ju(e,n,r,a,t);case 3:e:{if(Yp(n),e===null)throw Error(I(387));r=n.pendingProps,s=n.memoizedState,a=s.element,Pp(e,n),Bs(n,r,null,t);var i=n.memoizedState;if(r=i.element,s.isDehydrated)if(s={element:r,isDehydrated:!1,cache:i.cache,pendingSuspenseBoundaries:i.pendingSuspenseBoundaries,transitions:i.transitions},n.updateQueue.baseState=s,n.memoizedState=s,n.flags&256){a=mr(Error(I(423)),n),n=Fu(e,n,r,t,a);break e}else if(r!==a){a=mr(Error(I(424)),n),n=Fu(e,n,r,t,a);break e}else for(Ze=st(n.stateNode.containerInfo.firstChild),en=n,ue=!0,vn=null,t=Sp(n,null,r,t),n.child=t;t;)t.flags=t.flags&-3|4096,t=t.sibling;else{if(dr(),r===a){n=Un(e,n,t);break e}Ne(e,n,r,t)}n=n.child}return n;case 5:return _p(n),e===null&&zo(n),r=n.type,a=n.pendingProps,s=e!==null?e.memoizedProps:null,i=a.children,Ko(r,a)?i=null:s!==null&&Ko(r,s)&&(n.flags|=32),Jp(e,n),Ne(e,n,i,t),n.child;case 6:return e===null&&zo(n),null;case 13:return Xp(e,n,t);case 4:return Yl(n,n.stateNode.containerInfo),r=n.pendingProps,e===null?n.child=hr(n,null,r,t):Ne(e,n,r,t),n.child;case 11:return r=n.type,a=n.pendingProps,a=n.elementType===r?a:yn(r,a),Ku(e,n,r,a,t);case 7:return Ne(e,n,n.pendingProps,t),n.child;case 8:return Ne(e,n,n.pendingProps.children,t),n.child;case 12:return Ne(e,n,n.pendingProps.children,t),n.child;case 10:e:{if(r=n.type._context,a=n.pendingProps,s=n.memoizedProps,i=a.value,ie(Ms,r._currentValue),r._currentValue=i,s!==null)if(xn(s.value,i)){if(s.children===a.children&&!He.current){n=Un(e,n,t);break e}}else for(s=n.child,s!==null&&(s.return=n);s!==null;){var o=s.dependencies;if(o!==null){i=s.child;for(var l=o.firstContext;l!==null;){if(l.context===r){if(s.tag===1){l=Kn(-1,t&-t),l.tag=2;var c=s.updateQueue;if(c!==null){c=c.shared;var d=c.pending;d===null?l.next=l:(l.next=d.next,d.next=l),c.pending=l}}s.lanes|=t,l=s.alternate,l!==null&&(l.lanes|=t),Oo(s.return,t,n),o.lanes|=t;break}l=l.next}}else if(s.tag===10)i=s.type===n.type?null:s.child;else if(s.tag===18){if(i=s.return,i===null)throw Error(I(341));i.lanes|=t,o=i.alternate,o!==null&&(o.lanes|=t),Oo(i,t,n),i=s.sibling}else i=s.child;if(i!==null)i.return=s;else for(i=s;i!==null;){if(i===n){i=null;break}if(s=i.sibling,s!==null){s.return=i.return,i=s;break}i=i.return}s=i}Ne(e,n,a.children,t),n=n.child}return n;case 9:return a=n.type,r=n.pendingProps.children,ir(n,t),a=pn(a),r=r(a),n.flags|=1,Ne(e,n,r,t),n.child;case 14:return r=n.type,a=yn(r,n.pendingProps),a=yn(r.type,a),Wu(e,n,r,a,t);case 15:return $p(e,n,n.type,n.pendingProps,t);case 17:return r=n.type,a=n.pendingProps,a=n.elementType===r?a:yn(r,a),hs(e,n),n.tag=1,qe(r)?(e=!0,Is(n)):e=!1,ir(n,t),qp(n,r,a),qo(n,r,a,t),$o(null,n,r,!0,e,t);case 19:return Zp(e,n,t);case 22:return Qp(e,n,t)}throw Error(I(156,n.tag))};function gm(e,n){return zh(e,n)}function uw(e,n,t,r){this.tag=e,this.key=t,this.sibling=this.child=this.return=this.stateNode=this.type=this.elementType=null,this.index=0,this.ref=null,this.pendingProps=n,this.dependencies=this.memoizedState=this.updateQueue=this.memoizedProps=null,this.mode=r,this.subtreeFlags=this.flags=0,this.deletions=null,this.childLanes=this.lanes=0,this.alternate=null}function dn(e,n,t,r){return new uw(e,n,t,r)}function hc(e){return e=e.prototype,!(!e||!e.isReactComponent)}function dw(e){if(typeof e=="function")return hc(e)?1:0;if(e!=null){if(e=e.$$typeof,e===Rl)return 11;if(e===Ml)return 14}return 2}function ct(e,n){var t=e.alternate;return t===null?(t=dn(e.tag,n,e.key,e.mode),t.elementType=e.elementType,t.type=e.type,t.stateNode=e.stateNode,t.alternate=e,e.alternate=t):(t.pendingProps=n,t.type=e.type,t.flags=0,t.subtreeFlags=0,t.deletions=null),t.flags=e.flags&14680064,t.childLanes=e.childLanes,t.lanes=e.lanes,t.child=e.child,t.memoizedProps=e.memoizedProps,t.memoizedState=e.memoizedState,t.updateQueue=e.updateQueue,n=e.dependencies,t.dependencies=n===null?null:{lanes:n.lanes,firstContext:n.firstContext},t.sibling=e.sibling,t.index=e.index,t.ref=e.ref,t}function gs(e,n,t,r,a,s){var i=2;if(r=e,typeof e=="function")hc(e)&&(i=1);else if(typeof e=="string")i=5;else e:switch(e){case qt:return At(t.children,a,s,n);case Tl:i=8,a|=8;break;case go:return e=dn(12,t,n,a|2),e.elementType=go,e.lanes=s,e;case fo:return e=dn(13,t,n,a),e.elementType=fo,e.lanes=s,e;case yo:return e=dn(19,t,n,a),e.elementType=yo,e.lanes=s,e;case _h:return si(t,a,s,n);default:if(typeof e=="object"&&e!==null)switch(e.$$typeof){case xh:i=10;break e;case Ph:i=9;break e;case Rl:i=11;break e;case Ml:i=14;break e;case Qn:i=16,r=null;break e}throw Error(I(130,e==null?e:typeof e,""))}return n=dn(i,t,n,a),n.elementType=e,n.type=r,n.lanes=s,n}function At(e,n,t,r){return e=dn(7,e,r,n),e.lanes=t,e}function si(e,n,t,r){return e=dn(22,e,r,n),e.elementType=_h,e.lanes=t,e.stateNode={isHidden:!1},e}function Gi(e,n,t){return e=dn(6,e,null,n),e.lanes=t,e}function $i(e,n,t){return n=dn(4,e.children!==null?e.children:[],e.key,n),n.lanes=t,n.stateNode={containerInfo:e.containerInfo,pendingChildren:null,implementation:e.implementation},n}function hw(e,n,t,r,a){this.tag=n,this.containerInfo=e,this.finishedWork=this.pingCache=this.current=this.pendingChildren=null,this.timeoutHandle=-1,this.callbackNode=this.pendingContext=this.context=null,this.callbackPriority=0,this.eventTimes=Ai(0),this.expirationTimes=Ai(-1),this.entangledLanes=this.finishedLanes=this.mutableReadLanes=this.expiredLanes=this.pingedLanes=this.suspendedLanes=this.pendingLanes=0,this.entanglements=Ai(0),this.identifierPrefix=r,this.onRecoverableError=a,this.mutableSourceEagerHydrationData=null}function pc(e,n,t,r,a,s,i,o,l){return e=new hw(e,n,t,o,l),n===1?(n=1,s===!0&&(n|=8)):n=0,s=dn(3,null,null,n),e.current=s,s.stateNode=e,s.memoizedState={element:r,isDehydrated:t,cache:null,transitions:null,pendingSuspenseBoundaries:null},Jl(s),e}function pw(e,n,t){var r=3<arguments.length&&arguments[3]!==void 0?arguments[3]:null;return{$$typeof:Ht,key:r==null?null:""+r,children:e,containerInfo:n,implementation:t}}function fm(e){if(!e)return ht;e=e._reactInternals;e:{if(Nt(e)!==e||e.tag!==1)throw Error(I(170));var n=e;do{switch(n.tag){case 3:n=n.stateNode.context;break e;case 1:if(qe(n.type)){n=n.stateNode.__reactInternalMemoizedMergedChildContext;break e}}n=n.return}while(n!==null);throw Error(I(171))}if(e.tag===1){var t=e.type;if(qe(t))return fp(e,t,n)}return n}function ym(e,n,t,r,a,s,i,o,l){return e=pc(t,r,!0,e,a,s,i,o,l),e.context=fm(null),t=e.current,r=We(),a=lt(t),s=Kn(r,a),s.callback=n??null,it(t,s,a),e.current.lanes=a,_a(e,a,r),Ve(e,r),e}function ii(e,n,t,r){var a=n.current,s=We(),i=lt(a);return t=fm(t),n.context===null?n.context=t:n.pendingContext=t,n=Kn(s,i),n.payload={element:e},r=r===void 0?null:r,r!==null&&(n.callback=r),e=it(a,n,i),e!==null&&(bn(e,a,i,s),cs(e,a,i)),i}function zs(e){if(e=e.current,!e.child)return null;switch(e.child.tag){case 5:return e.child.stateNode;default:return e.child.stateNode}}function Ju(e,n){if(e=e.memoizedState,e!==null&&e.dehydrated!==null){var t=e.retryLane;e.retryLane=t!==0&&t<n?t:n}}function mc(e,n){Ju(e,n),(e=e.alternate)&&Ju(e,n)}function mw(){return null}var wm=typeof reportError=="function"?reportError:function(e){console.error(e)};function gc(e){this._internalRoot=e}oi.prototype.render=gc.prototype.render=function(e){var n=this._internalRoot;if(n===null)throw Error(I(409));ii(e,n,null,null)};oi.prototype.unmount=gc.prototype.unmount=function(){var e=this._internalRoot;if(e!==null){this._internalRoot=null;var n=e.containerInfo;Dt(function(){ii(null,e,null,null)}),n[jn]=null}};function oi(e){this._internalRoot=e}oi.prototype.unstable_scheduleHydration=function(e){if(e){var n=Qh();e={blockedOn:null,target:e,priority:n};for(var t=0;t<Yn.length&&n!==0&&n<Yn[t].priority;t++);Yn.splice(t,0,e),t===0&&Yh(e)}};function fc(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11)}function li(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11&&(e.nodeType!==8||e.nodeValue!==" react-mount-point-unstable "))}function Yu(){}function gw(e,n,t,r,a){if(a){if(typeof r=="function"){var s=r;r=function(){var c=zs(i);s.call(c)}}var i=ym(n,r,e,0,null,!1,!1,"",Yu);return e._reactRootContainer=i,e[jn]=i.current,da(e.nodeType===8?e.parentNode:e),Dt(),i}for(;a=e.lastChild;)e.removeChild(a);if(typeof r=="function"){var o=r;r=function(){var c=zs(l);o.call(c)}}var l=pc(e,0,!1,null,null,!1,!1,"",Yu);return e._reactRootContainer=l,e[jn]=l.current,da(e.nodeType===8?e.parentNode:e),Dt(function(){ii(n,l,t,r)}),l}function ci(e,n,t,r,a){var s=t._reactRootContainer;if(s){var i=s;if(typeof a=="function"){var o=a;a=function(){var l=zs(i);o.call(l)}}ii(n,i,e,a)}else i=gw(t,n,e,a,r);return zs(i)}Gh=function(e){switch(e.tag){case 3:var n=e.stateNode;if(n.current.memoizedState.isDehydrated){var t=Or(n.pendingLanes);t!==0&&(Ll(n,t|1),Ve(n,fe()),!(Z&6)&&(gr=fe()+500,gt()))}break;case 13:Dt(function(){var r=Fn(e,1);if(r!==null){var a=We();bn(r,e,1,a)}}),mc(e,1)}};Nl=function(e){if(e.tag===13){var n=Fn(e,134217728);if(n!==null){var t=We();bn(n,e,134217728,t)}mc(e,134217728)}};$h=function(e){if(e.tag===13){var n=lt(e),t=Fn(e,n);if(t!==null){var r=We();bn(t,e,n,r)}mc(e,n)}};Qh=function(){return ne};Jh=function(e,n){var t=ne;try{return ne=e,n()}finally{ne=t}};Co=function(e,n,t){switch(n){case"input":if(ko(e,t),n=t.name,t.type==="radio"&&n!=null){for(t=e;t.parentNode;)t=t.parentNode;for(t=t.querySelectorAll("input[name="+JSON.stringify(""+n)+'][type="radio"]'),n=0;n<t.length;n++){var r=t[n];if(r!==e&&r.form===e.form){var a=Zs(r);if(!a)throw Error(I(90));Ch(r),ko(r,a)}}}break;case"textarea":Ih(e,t);break;case"select":n=t.value,n!=null&&tr(e,!!t.multiple,n,!1)}};Nh=cc;Kh=Dt;var fw={usingClientEntryPoint:!1,Events:[Ca,Qt,Zs,Bh,Lh,cc]},Wr={findFiberByHostInstance:Pt,bundleType:0,version:"18.3.1",rendererPackageName:"react-dom"},yw={bundleType:Wr.bundleType,version:Wr.version,rendererPackageName:Wr.rendererPackageName,rendererConfig:Wr.rendererConfig,overrideHookState:null,overrideHookStateDeletePath:null,overrideHookStateRenamePath:null,overrideProps:null,overridePropsDeletePath:null,overridePropsRenamePath:null,setErrorHandler:null,setSuspenseHandler:null,scheduleUpdate:null,currentDispatcherRef:zn.ReactCurrentDispatcher,findHostInstanceByFiber:function(e){return e=Fh(e),e===null?null:e.stateNode},findFiberByHostInstance:Wr.findFiberByHostInstance||mw,findHostInstancesForRefresh:null,scheduleRefresh:null,scheduleRoot:null,setRefreshHandler:null,getCurrentFiber:null,reconcilerVersion:"18.3.1-next-f1338f8080-20240426"};if(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__<"u"){var Za=__REACT_DEVTOOLS_GLOBAL_HOOK__;if(!Za.isDisabled&&Za.supportsFiber)try{Qs=Za.inject(yw),An=Za}catch{}}rn.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=fw;rn.createPortal=function(e,n){var t=2<arguments.length&&arguments[2]!==void 0?arguments[2]:null;if(!fc(n))throw Error(I(200));return pw(e,n,null,t)};rn.createRoot=function(e,n){if(!fc(e))throw Error(I(299));var t=!1,r="",a=wm;return n!=null&&(n.unstable_strictMode===!0&&(t=!0),n.identifierPrefix!==void 0&&(r=n.identifierPrefix),n.onRecoverableError!==void 0&&(a=n.onRecoverableError)),n=pc(e,1,!1,null,null,t,!1,r,a),e[jn]=n.current,da(e.nodeType===8?e.parentNode:e),new gc(n)};rn.findDOMNode=function(e){if(e==null)return null;if(e.nodeType===1)return e;var n=e._reactInternals;if(n===void 0)throw typeof e.render=="function"?Error(I(188)):(e=Object.keys(e).join(","),Error(I(268,e)));return e=Fh(n),e=e===null?null:e.stateNode,e};rn.flushSync=function(e){return Dt(e)};rn.hydrate=function(e,n,t){if(!li(n))throw Error(I(200));return ci(null,e,n,!0,t)};rn.hydrateRoot=function(e,n,t){if(!fc(e))throw Error(I(405));var r=t!=null&&t.hydratedSources||null,a=!1,s="",i=wm;if(t!=null&&(t.unstable_strictMode===!0&&(a=!0),t.identifierPrefix!==void 0&&(s=t.identifierPrefix),t.onRecoverableError!==void 0&&(i=t.onRecoverableError)),n=ym(n,null,e,1,t??null,a,!1,s,i),e[jn]=n.current,da(e),r)for(e=0;e<r.length;e++)t=r[e],a=t._getVersion,a=a(t._source),n.mutableSourceEagerHydrationData==null?n.mutableSourceEagerHydrationData=[t,a]:n.mutableSourceEagerHydrationData.push(t,a);return new oi(n)};rn.render=function(e,n,t){if(!li(n))throw Error(I(200));return ci(null,e,n,!1,t)};rn.unmountComponentAtNode=function(e){if(!li(e))throw Error(I(40));return e._reactRootContainer?(Dt(function(){ci(null,null,e,!1,function(){e._reactRootContainer=null,e[jn]=null})}),!0):!1};rn.unstable_batchedUpdates=cc;rn.unstable_renderSubtreeIntoContainer=function(e,n,t,r){if(!li(t))throw Error(I(200));if(e==null||e._reactInternals===void 0)throw Error(I(38));return ci(e,n,t,!1,r)};rn.version="18.3.1-next-f1338f8080-20240426";function vm(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>"u"||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!="function"))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(vm)}catch(e){console.error(e)}}vm(),vh.exports=rn;var ww=vh.exports,Xu=ww;po.createRoot=Xu.createRoot,po.hydrateRoot=Xu.hydrateRoot;/**
 * @remix-run/router v1.23.2
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */function ka(){return ka=Object.assign?Object.assign.bind():function(e){for(var n=1;n<arguments.length;n++){var t=arguments[n];for(var r in t)Object.prototype.hasOwnProperty.call(t,r)&&(e[r]=t[r])}return e},ka.apply(this,arguments)}var nt;(function(e){e.Pop="POP",e.Push="PUSH",e.Replace="REPLACE"})(nt||(nt={}));const Zu="popstate";function vw(e){e===void 0&&(e={});function n(r,a){let{pathname:s,search:i,hash:o}=r.location;return il("",{pathname:s,search:i,hash:o},a.state&&a.state.usr||null,a.state&&a.state.key||"default")}function t(r,a){return typeof a=="string"?a:Os(a)}return bw(n,t,null,e)}function ye(e,n){if(e===!1||e===null||typeof e>"u")throw new Error(n)}function yc(e,n){if(!e){typeof console<"u"&&console.warn(n);try{throw new Error(n)}catch{}}}function kw(){return Math.random().toString(36).substr(2,8)}function ed(e,n){return{usr:e.state,key:e.key,idx:n}}function il(e,n,t,r){return t===void 0&&(t=null),ka({pathname:typeof e=="string"?e:e.pathname,search:"",hash:""},typeof n=="string"?kr(n):n,{state:t,key:n&&n.key||r||kw()})}function Os(e){let{pathname:n="/",search:t="",hash:r=""}=e;return t&&t!=="?"&&(n+=t.charAt(0)==="?"?t:"?"+t),r&&r!=="#"&&(n+=r.charAt(0)==="#"?r:"#"+r),n}function kr(e){let n={};if(e){let t=e.indexOf("#");t>=0&&(n.hash=e.substr(t),e=e.substr(0,t));let r=e.indexOf("?");r>=0&&(n.search=e.substr(r),e=e.substr(0,r)),e&&(n.pathname=e)}return n}function bw(e,n,t,r){r===void 0&&(r={});let{window:a=document.defaultView,v5Compat:s=!1}=r,i=a.history,o=nt.Pop,l=null,c=d();c==null&&(c=0,i.replaceState(ka({},i.state,{idx:c}),""));function d(){return(i.state||{idx:null}).idx}function u(){o=nt.Pop;let P=d(),g=P==null?null:P-c;c=P,l&&l({action:o,location:k.location,delta:g})}function p(P,g){o=nt.Push;let y=il(k.location,P,g);c=d()+1;let w=ed(y,c),_=k.createHref(y);try{i.pushState(w,"",_)}catch(E){if(E instanceof DOMException&&E.name==="DataCloneError")throw E;a.location.assign(_)}s&&l&&l({action:o,location:k.location,delta:1})}function m(P,g){o=nt.Replace;let y=il(k.location,P,g);c=d();let w=ed(y,c),_=k.createHref(y);i.replaceState(w,"",_),s&&l&&l({action:o,location:k.location,delta:0})}function f(P){let g=a.location.origin!=="null"?a.location.origin:a.location.href,y=typeof P=="string"?P:Os(P);return y=y.replace(/ $/,"%20"),ye(g,"No window.location.(origin|href) available to create URL for href: "+y),new URL(y,g)}let k={get action(){return o},get location(){return e(a,i)},listen(P){if(l)throw new Error("A history only accepts one active listener");return a.addEventListener(Zu,u),l=P,()=>{a.removeEventListener(Zu,u),l=null}},createHref(P){return n(a,P)},createURL:f,encodeLocation(P){let g=f(P);return{pathname:g.pathname,search:g.search,hash:g.hash}},push:p,replace:m,go(P){return i.go(P)}};return k}var nd;(function(e){e.data="data",e.deferred="deferred",e.redirect="redirect",e.error="error"})(nd||(nd={}));function Sw(e,n,t){return t===void 0&&(t="/"),xw(e,n,t)}function xw(e,n,t,r){let a=typeof n=="string"?kr(n):n,s=wc(a.pathname||"/",t);if(s==null)return null;let i=km(e);Pw(i);let o=null;for(let l=0;o==null&&l<i.length;++l){let c=Nw(s);o=Dw(i[l],c)}return o}function km(e,n,t,r){n===void 0&&(n=[]),t===void 0&&(t=[]),r===void 0&&(r="");let a=(s,i,o)=>{let l={relativePath:o===void 0?s.path||"":o,caseSensitive:s.caseSensitive===!0,childrenIndex:i,route:s};l.relativePath.startsWith("/")&&(ye(l.relativePath.startsWith(r),'Absolute route path "'+l.relativePath+'" nested under path '+('"'+r+'" is not valid. An absolute child route path ')+"must start with the combined path of all its parent routes."),l.relativePath=l.relativePath.slice(r.length));let c=ut([r,l.relativePath]),d=t.concat(l);s.children&&s.children.length>0&&(ye(s.index!==!0,"Index routes must not have child routes. Please remove "+('all child routes from route path "'+c+'".')),km(s.children,n,d,c)),!(s.path==null&&!s.index)&&n.push({path:c,score:Rw(c,s.index),routesMeta:d})};return e.forEach((s,i)=>{var o;if(s.path===""||!((o=s.path)!=null&&o.includes("?")))a(s,i);else for(let l of bm(s.path))a(s,i,l)}),n}function bm(e){let n=e.split("/");if(n.length===0)return[];let[t,...r]=n,a=t.endsWith("?"),s=t.replace(/\?$/,"");if(r.length===0)return a?[s,""]:[s];let i=bm(r.join("/")),o=[];return o.push(...i.map(l=>l===""?s:[s,l].join("/"))),a&&o.push(...i),o.map(l=>e.startsWith("/")&&l===""?"/":l)}function Pw(e){e.sort((n,t)=>n.score!==t.score?t.score-n.score:Mw(n.routesMeta.map(r=>r.childrenIndex),t.routesMeta.map(r=>r.childrenIndex)))}const _w=/^:[\w-]+$/,Ew=3,Cw=2,Aw=1,Iw=10,Tw=-2,td=e=>e==="*";function Rw(e,n){let t=e.split("/"),r=t.length;return t.some(td)&&(r+=Tw),n&&(r+=Cw),t.filter(a=>!td(a)).reduce((a,s)=>a+(_w.test(s)?Ew:s===""?Aw:Iw),r)}function Mw(e,n){return e.length===n.length&&e.slice(0,-1).every((r,a)=>r===n[a])?e[e.length-1]-n[n.length-1]:0}function Dw(e,n,t){let{routesMeta:r}=e,a={},s="/",i=[];for(let o=0;o<r.length;++o){let l=r[o],c=o===r.length-1,d=s==="/"?n:n.slice(s.length)||"/",u=Bw({path:l.relativePath,caseSensitive:l.caseSensitive,end:c},d),p=l.route;if(!u)return null;Object.assign(a,u.params),i.push({params:a,pathname:ut([s,u.pathname]),pathnameBase:Uw(ut([s,u.pathnameBase])),route:p}),u.pathnameBase!=="/"&&(s=ut([s,u.pathnameBase]))}return i}function Bw(e,n){typeof e=="string"&&(e={path:e,caseSensitive:!1,end:!0});let[t,r]=Lw(e.path,e.caseSensitive,e.end),a=n.match(t);if(!a)return null;let s=a[0],i=s.replace(/(.)\/+$/,"$1"),o=a.slice(1);return{params:r.reduce((c,d,u)=>{let{paramName:p,isOptional:m}=d;if(p==="*"){let k=o[u]||"";i=s.slice(0,s.length-k.length).replace(/(.)\/+$/,"$1")}const f=o[u];return m&&!f?c[p]=void 0:c[p]=(f||"").replace(/%2F/g,"/"),c},{}),pathname:s,pathnameBase:i,pattern:e}}function Lw(e,n,t){n===void 0&&(n=!1),t===void 0&&(t=!0),yc(e==="*"||!e.endsWith("*")||e.endsWith("/*"),'Route path "'+e+'" will be treated as if it were '+('"'+e.replace(/\*$/,"/*")+'" because the `*` character must ')+"always follow a `/` in the pattern. To get rid of this warning, "+('please change the route path to "'+e.replace(/\*$/,"/*")+'".'));let r=[],a="^"+e.replace(/\/*\*?$/,"").replace(/^\/*/,"/").replace(/[\\.*+^${}|()[\]]/g,"\\$&").replace(/\/:([\w-]+)(\?)?/g,(i,o,l)=>(r.push({paramName:o,isOptional:l!=null}),l?"/?([^\\/]+)?":"/([^\\/]+)"));return e.endsWith("*")?(r.push({paramName:"*"}),a+=e==="*"||e==="/*"?"(.*)$":"(?:\\/(.+)|\\/*)$"):t?a+="\\/*$":e!==""&&e!=="/"&&(a+="(?:(?=\\/|$))"),[new RegExp(a,n?void 0:"i"),r]}function Nw(e){try{return e.split("/").map(n=>decodeURIComponent(n).replace(/\//g,"%2F")).join("/")}catch(n){return yc(!1,'The URL path "'+e+'" could not be decoded because it is is a malformed URL segment. This is probably due to a bad percent '+("encoding ("+n+").")),e}}function wc(e,n){if(n==="/")return e;if(!e.toLowerCase().startsWith(n.toLowerCase()))return null;let t=n.endsWith("/")?n.length-1:n.length,r=e.charAt(t);return r&&r!=="/"?null:e.slice(t)||"/"}const Kw=/^(?:[a-z][a-z0-9+.-]*:|\/\/)/i,Ww=e=>Kw.test(e);function jw(e,n){n===void 0&&(n="/");let{pathname:t,search:r="",hash:a=""}=typeof e=="string"?kr(e):e,s;if(t)if(Ww(t))s=t;else{if(t.includes("//")){let i=t;t=t.replace(/\/\/+/g,"/"),yc(!1,"Pathnames cannot have embedded double slashes - normalizing "+(i+" -> "+t))}t.startsWith("/")?s=rd(t.substring(1),"/"):s=rd(t,n)}else s=n;return{pathname:s,search:zw(r),hash:Ow(a)}}function rd(e,n){let t=n.replace(/\/+$/,"").split("/");return e.split("/").forEach(a=>{a===".."?t.length>1&&t.pop():a!=="."&&t.push(a)}),t.length>1?t.join("/"):"/"}function Qi(e,n,t,r){return"Cannot include a '"+e+"' character in a manually specified "+("`to."+n+"` field ["+JSON.stringify(r)+"].  Please separate it out to the ")+("`to."+t+"` field. Alternatively you may provide the full path as ")+'a string in <Link to="..."> and the router will parse it for you.'}function Fw(e){return e.filter((n,t)=>t===0||n.route.path&&n.route.path.length>0)}function vc(e,n){let t=Fw(e);return n?t.map((r,a)=>a===t.length-1?r.pathname:r.pathnameBase):t.map(r=>r.pathnameBase)}function kc(e,n,t,r){r===void 0&&(r=!1);let a;typeof e=="string"?a=kr(e):(a=ka({},e),ye(!a.pathname||!a.pathname.includes("?"),Qi("?","pathname","search",a)),ye(!a.pathname||!a.pathname.includes("#"),Qi("#","pathname","hash",a)),ye(!a.search||!a.search.includes("#"),Qi("#","search","hash",a)));let s=e===""||a.pathname==="",i=s?"/":a.pathname,o;if(i==null)o=t;else{let u=n.length-1;if(!r&&i.startsWith("..")){let p=i.split("/");for(;p[0]==="..";)p.shift(),u-=1;a.pathname=p.join("/")}o=u>=0?n[u]:"/"}let l=jw(a,o),c=i&&i!=="/"&&i.endsWith("/"),d=(s||i===".")&&t.endsWith("/");return!l.pathname.endsWith("/")&&(c||d)&&(l.pathname+="/"),l}const ut=e=>e.join("/").replace(/\/\/+/g,"/"),Uw=e=>e.replace(/\/+$/,"").replace(/^\/*/,"/"),zw=e=>!e||e==="?"?"":e.startsWith("?")?e:"?"+e,Ow=e=>!e||e==="#"?"":e.startsWith("#")?e:"#"+e;function Hw(e){return e!=null&&typeof e.status=="number"&&typeof e.statusText=="string"&&typeof e.internal=="boolean"&&"data"in e}const Sm=["post","put","patch","delete"];new Set(Sm);const qw=["get",...Sm];new Set(qw);/**
 * React Router v6.30.3
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */function ba(){return ba=Object.assign?Object.assign.bind():function(e){for(var n=1;n<arguments.length;n++){var t=arguments[n];for(var r in t)Object.prototype.hasOwnProperty.call(t,r)&&(e[r]=t[r])}return e},ba.apply(this,arguments)}const bc=T.createContext(null),Vw=T.createContext(null),ft=T.createContext(null),ui=T.createContext(null),On=T.createContext({outlet:null,matches:[],isDataRoute:!1}),xm=T.createContext(null);function Gw(e,n){let{relative:t}=n===void 0?{}:n;br()||ye(!1);let{basename:r,navigator:a}=T.useContext(ft),{hash:s,pathname:i,search:o}=Em(e,{relative:t}),l=i;return r!=="/"&&(l=i==="/"?r:ut([r,i])),a.createHref({pathname:l,search:o,hash:s})}function br(){return T.useContext(ui)!=null}function Sr(){return br()||ye(!1),T.useContext(ui).location}function Pm(e){T.useContext(ft).static||T.useLayoutEffect(e)}function _m(){let{isDataRoute:e}=T.useContext(On);return e?iv():$w()}function $w(){br()||ye(!1);let e=T.useContext(bc),{basename:n,future:t,navigator:r}=T.useContext(ft),{matches:a}=T.useContext(On),{pathname:s}=Sr(),i=JSON.stringify(vc(a,t.v7_relativeSplatPath)),o=T.useRef(!1);return Pm(()=>{o.current=!0}),T.useCallback(function(c,d){if(d===void 0&&(d={}),!o.current)return;if(typeof c=="number"){r.go(c);return}let u=kc(c,JSON.parse(i),s,d.relative==="path");e==null&&n!=="/"&&(u.pathname=u.pathname==="/"?n:ut([n,u.pathname])),(d.replace?r.replace:r.push)(u,d.state,d)},[n,r,i,s,e])}function Kt(){let{matches:e}=T.useContext(On),n=e[e.length-1];return n?n.params:{}}function Em(e,n){let{relative:t}=n===void 0?{}:n,{future:r}=T.useContext(ft),{matches:a}=T.useContext(On),{pathname:s}=Sr(),i=JSON.stringify(vc(a,r.v7_relativeSplatPath));return T.useMemo(()=>kc(e,JSON.parse(i),s,t==="path"),[e,i,s,t])}function Qw(e,n){return Jw(e,n)}function Jw(e,n,t,r){br()||ye(!1);let{navigator:a}=T.useContext(ft),{matches:s}=T.useContext(On),i=s[s.length-1],o=i?i.params:{};i&&i.pathname;let l=i?i.pathnameBase:"/";i&&i.route;let c=Sr(),d;if(n){var u;let P=typeof n=="string"?kr(n):n;l==="/"||(u=P.pathname)!=null&&u.startsWith(l)||ye(!1),d=P}else d=c;let p=d.pathname||"/",m=p;if(l!=="/"){let P=l.replace(/^\//,"").split("/");m="/"+p.replace(/^\//,"").split("/").slice(P.length).join("/")}let f=Sw(e,{pathname:m}),k=nv(f&&f.map(P=>Object.assign({},P,{params:Object.assign({},o,P.params),pathname:ut([l,a.encodeLocation?a.encodeLocation(P.pathname).pathname:P.pathname]),pathnameBase:P.pathnameBase==="/"?l:ut([l,a.encodeLocation?a.encodeLocation(P.pathnameBase).pathname:P.pathnameBase])})),s,t,r);return n&&k?T.createElement(ui.Provider,{value:{location:ba({pathname:"/",search:"",hash:"",state:null,key:"default"},d),navigationType:nt.Pop}},k):k}function Yw(){let e=sv(),n=Hw(e)?e.status+" "+e.statusText:e instanceof Error?e.message:JSON.stringify(e),t=e instanceof Error?e.stack:null,a={padding:"0.5rem",backgroundColor:"rgba(200,200,200, 0.5)"};return T.createElement(T.Fragment,null,T.createElement("h2",null,"Unexpected Application Error!"),T.createElement("h3",{style:{fontStyle:"italic"}},n),t?T.createElement("pre",{style:a},t):null,null)}const Xw=T.createElement(Yw,null);class Zw extends T.Component{constructor(n){super(n),this.state={location:n.location,revalidation:n.revalidation,error:n.error}}static getDerivedStateFromError(n){return{error:n}}static getDerivedStateFromProps(n,t){return t.location!==n.location||t.revalidation!=="idle"&&n.revalidation==="idle"?{error:n.error,location:n.location,revalidation:n.revalidation}:{error:n.error!==void 0?n.error:t.error,location:t.location,revalidation:n.revalidation||t.revalidation}}componentDidCatch(n,t){console.error("React Router caught the following error during render",n,t)}render(){return this.state.error!==void 0?T.createElement(On.Provider,{value:this.props.routeContext},T.createElement(xm.Provider,{value:this.state.error,children:this.props.component})):this.props.children}}function ev(e){let{routeContext:n,match:t,children:r}=e,a=T.useContext(bc);return a&&a.static&&a.staticContext&&(t.route.errorElement||t.route.ErrorBoundary)&&(a.staticContext._deepestRenderedBoundaryId=t.route.id),T.createElement(On.Provider,{value:n},r)}function nv(e,n,t,r){var a;if(n===void 0&&(n=[]),t===void 0&&(t=null),r===void 0&&(r=null),e==null){var s;if(!t)return null;if(t.errors)e=t.matches;else if((s=r)!=null&&s.v7_partialHydration&&n.length===0&&!t.initialized&&t.matches.length>0)e=t.matches;else return null}let i=e,o=(a=t)==null?void 0:a.errors;if(o!=null){let d=i.findIndex(u=>u.route.id&&(o==null?void 0:o[u.route.id])!==void 0);d>=0||ye(!1),i=i.slice(0,Math.min(i.length,d+1))}let l=!1,c=-1;if(t&&r&&r.v7_partialHydration)for(let d=0;d<i.length;d++){let u=i[d];if((u.route.HydrateFallback||u.route.hydrateFallbackElement)&&(c=d),u.route.id){let{loaderData:p,errors:m}=t,f=u.route.loader&&p[u.route.id]===void 0&&(!m||m[u.route.id]===void 0);if(u.route.lazy||f){l=!0,c>=0?i=i.slice(0,c+1):i=[i[0]];break}}}return i.reduceRight((d,u,p)=>{let m,f=!1,k=null,P=null;t&&(m=o&&u.route.id?o[u.route.id]:void 0,k=u.route.errorElement||Xw,l&&(c<0&&p===0?(ov("route-fallback"),f=!0,P=null):c===p&&(f=!0,P=u.route.hydrateFallbackElement||null)));let g=n.concat(i.slice(0,p+1)),y=()=>{let w;return m?w=k:f?w=P:u.route.Component?w=T.createElement(u.route.Component,null):u.route.element?w=u.route.element:w=d,T.createElement(ev,{match:u,routeContext:{outlet:d,matches:g,isDataRoute:t!=null},children:w})};return t&&(u.route.ErrorBoundary||u.route.errorElement||p===0)?T.createElement(Zw,{location:t.location,revalidation:t.revalidation,component:k,error:m,children:y(),routeContext:{outlet:null,matches:g,isDataRoute:!0}}):y()},null)}var Cm=function(e){return e.UseBlocker="useBlocker",e.UseRevalidator="useRevalidator",e.UseNavigateStable="useNavigate",e}(Cm||{}),Am=function(e){return e.UseBlocker="useBlocker",e.UseLoaderData="useLoaderData",e.UseActionData="useActionData",e.UseRouteError="useRouteError",e.UseNavigation="useNavigation",e.UseRouteLoaderData="useRouteLoaderData",e.UseMatches="useMatches",e.UseRevalidator="useRevalidator",e.UseNavigateStable="useNavigate",e.UseRouteId="useRouteId",e}(Am||{});function tv(e){let n=T.useContext(bc);return n||ye(!1),n}function rv(e){let n=T.useContext(Vw);return n||ye(!1),n}function av(e){let n=T.useContext(On);return n||ye(!1),n}function Im(e){let n=av(),t=n.matches[n.matches.length-1];return t.route.id||ye(!1),t.route.id}function sv(){var e;let n=T.useContext(xm),t=rv(),r=Im();return n!==void 0?n:(e=t.errors)==null?void 0:e[r]}function iv(){let{router:e}=tv(Cm.UseNavigateStable),n=Im(Am.UseNavigateStable),t=T.useRef(!1);return Pm(()=>{t.current=!0}),T.useCallback(function(a,s){s===void 0&&(s={}),t.current&&(typeof a=="number"?e.navigate(a):e.navigate(a,ba({fromRouteId:n},s)))},[e,n])}const ad={};function ov(e,n,t){ad[e]||(ad[e]=!0)}function lv(e,n){e==null||e.v7_startTransition,e==null||e.v7_relativeSplatPath}function Ia(e){let{to:n,replace:t,state:r,relative:a}=e;br()||ye(!1);let{future:s,static:i}=T.useContext(ft),{matches:o}=T.useContext(On),{pathname:l}=Sr(),c=_m(),d=kc(n,vc(o,s.v7_relativeSplatPath),l,a==="path"),u=JSON.stringify(d);return T.useEffect(()=>c(JSON.parse(u),{replace:t,state:r,relative:a}),[c,u,a,t,r]),null}function Le(e){ye(!1)}function cv(e){let{basename:n="/",children:t=null,location:r,navigationType:a=nt.Pop,navigator:s,static:i=!1,future:o}=e;br()&&ye(!1);let l=n.replace(/^\/*/,"/"),c=T.useMemo(()=>({basename:l,navigator:s,static:i,future:ba({v7_relativeSplatPath:!1},o)}),[l,o,s,i]);typeof r=="string"&&(r=kr(r));let{pathname:d="/",search:u="",hash:p="",state:m=null,key:f="default"}=r,k=T.useMemo(()=>{let P=wc(d,l);return P==null?null:{location:{pathname:P,search:u,hash:p,state:m,key:f},navigationType:a}},[l,d,u,p,m,f,a]);return k==null?null:T.createElement(ft.Provider,{value:c},T.createElement(ui.Provider,{children:t,value:k}))}function uv(e){let{children:n,location:t}=e;return Qw(ol(n),t)}new Promise(()=>{});function ol(e,n){n===void 0&&(n=[]);let t=[];return T.Children.forEach(e,(r,a)=>{if(!T.isValidElement(r))return;let s=[...n,a];if(r.type===T.Fragment){t.push.apply(t,ol(r.props.children,s));return}r.type!==Le&&ye(!1),!r.props.index||!r.props.children||ye(!1);let i={id:r.props.id||s.join("-"),caseSensitive:r.props.caseSensitive,element:r.props.element,Component:r.props.Component,index:r.props.index,path:r.props.path,loader:r.props.loader,action:r.props.action,errorElement:r.props.errorElement,ErrorBoundary:r.props.ErrorBoundary,hasErrorBoundary:r.props.ErrorBoundary!=null||r.props.errorElement!=null,shouldRevalidate:r.props.shouldRevalidate,handle:r.props.handle,lazy:r.props.lazy};r.props.children&&(i.children=ol(r.props.children,s)),t.push(i)}),t}/**
 * React Router DOM v6.30.3
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */function ll(){return ll=Object.assign?Object.assign.bind():function(e){for(var n=1;n<arguments.length;n++){var t=arguments[n];for(var r in t)Object.prototype.hasOwnProperty.call(t,r)&&(e[r]=t[r])}return e},ll.apply(this,arguments)}function dv(e,n){if(e==null)return{};var t={},r=Object.keys(e),a,s;for(s=0;s<r.length;s++)a=r[s],!(n.indexOf(a)>=0)&&(t[a]=e[a]);return t}function hv(e){return!!(e.metaKey||e.altKey||e.ctrlKey||e.shiftKey)}function pv(e,n){return e.button===0&&(!n||n==="_self")&&!hv(e)}const mv=["onClick","relative","reloadDocument","replace","state","target","to","preventScrollReset","viewTransition"],gv="6";try{window.__reactRouterVersion=gv}catch{}const fv="startTransition",sd=of[fv];function yv(e){let{basename:n,children:t,future:r,window:a}=e,s=T.useRef();s.current==null&&(s.current=vw({window:a,v5Compat:!0}));let i=s.current,[o,l]=T.useState({action:i.action,location:i.location}),{v7_startTransition:c}=r||{},d=T.useCallback(u=>{c&&sd?sd(()=>l(u)):l(u)},[l,c]);return T.useLayoutEffect(()=>i.listen(d),[i,d]),T.useEffect(()=>lv(r),[r]),T.createElement(cv,{basename:n,children:t,location:o.location,navigationType:o.action,navigator:i,future:r})}const wv=typeof window<"u"&&typeof window.document<"u"&&typeof window.document.createElement<"u",vv=/^(?:[a-z][a-z0-9+.-]*:|\/\/)/i,z=T.forwardRef(function(n,t){let{onClick:r,relative:a,reloadDocument:s,replace:i,state:o,target:l,to:c,preventScrollReset:d,viewTransition:u}=n,p=dv(n,mv),{basename:m}=T.useContext(ft),f,k=!1;if(typeof c=="string"&&vv.test(c)&&(f=c,wv))try{let w=new URL(window.location.href),_=c.startsWith("//")?new URL(w.protocol+c):new URL(c),E=wc(_.pathname,m);_.origin===w.origin&&E!=null?c=E+_.search+_.hash:k=!0}catch{}let P=Gw(c,{relative:a}),g=kv(c,{replace:i,state:o,target:l,preventScrollReset:d,relative:a,viewTransition:u});function y(w){r&&r(w),w.defaultPrevented||g(w)}return T.createElement("a",ll({},p,{href:f||P,onClick:k||s?r:y,ref:t,target:l}))});var id;(function(e){e.UseScrollRestoration="useScrollRestoration",e.UseSubmit="useSubmit",e.UseSubmitFetcher="useSubmitFetcher",e.UseFetcher="useFetcher",e.useViewTransitionState="useViewTransitionState"})(id||(id={}));var od;(function(e){e.UseFetcher="useFetcher",e.UseFetchers="useFetchers",e.UseScrollRestoration="useScrollRestoration"})(od||(od={}));function kv(e,n){let{target:t,replace:r,state:a,preventScrollReset:s,relative:i,viewTransition:o}=n===void 0?{}:n,l=_m(),c=Sr(),d=Em(e,{relative:i});return T.useCallback(u=>{if(pv(u,t)){u.preventDefault();let p=r!==void 0?r:Os(c)===Os(d);l(e,{replace:p,state:a,preventScrollReset:s,relative:i,viewTransition:o})}},[c,l,d,r,a,t,e,s,i,o])}function Tm({body:e}){return h.jsx("div",{className:"article-body",children:e.map((n,t)=>{const r=n.split(`
`).map(s=>s.trim());return r.every(s=>s.startsWith("- "))&&r.length>0?h.jsx("ul",{className:"article-list",children:r.map((s,i)=>h.jsx("li",{children:s.replace(/^-\s*/,"")},i))},t):h.jsx("p",{className:"article-p",children:n},t)})})}const ld=["await trpc.unlockVault.mutate({ password })","const { dkg } = await ika.requestDWalletDKG(tx)","parseSignatureFromSignOutput(bytes, Curve.SECP256K1)","tx.moveCall({ target: `${ika}::coordinator::request_sign` })","keyring.deriveIkaRootSeed(feeKeypair)","presignPool.take(SECP256K1_ECDSA)","const vault = await aesGcm.decrypt(blob, key)","Argon2id(salt, password, { t: 3, m: 65536, p: 4 })","ethers.Transaction.from(unsignedSerialized)","adapter.ikaClient.ensureInitialized()","dwalletMeta[vaultId] ?? createDwalletMeta()","verifyMessage(keccak256(preimage), signature)","getRequiredCoinAmounts(ikaClient)","tx.transferObjects([splitIka, splitSui], owner)","chrome.storage.session.set(unlockCache)","acceptEncryptedUserShare(shareId, capId)","const presign = await ikaClient.getPresign(presignID)","Route → /guide/first-run-side-panel","prefers-reduced-motion: reduce","oklch(0.78 0.14 265)"];function bv({children:e,options:n}){const{count:t=14,minLifespanSec:r=14,maxLifespanSec:a=30,peakOpacity:s=.78,peakWidth:i=.06,color:o="rgba(158, 180, 255, 1)",colors:l=["rgba(158, 180, 255, 1)","rgba(255, 140, 210, 1)","rgba(140, 255, 210, 1)","rgba(255, 220, 140, 1)","rgba(200, 160, 255, 1)","rgba(140, 220, 255, 1)"],colorCycleSec:c=14,maxBlurPx:d=2.4,maxYawDeg:u=18,maxZPx:p=90,bendWindow:m=.28,targetSelectors:f,bandPaddingPx:k=5,bandHeightPx:P=8}=n??{},g=T.useRef(null),y=T.useRef(null);return T.useEffect(()=>{if(typeof window>"u"||window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;const w=y.current,_=g.current;if(!w||!_)return;const E=(N,O)=>N+Math.random()*(O-N),S=()=>ld[Math.floor(Math.random()*ld.length)],A=l.length>0?l:[o],M=N=>{const O=N.match(/rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/);return O?[+O[1],+O[2],+O[3]]:[255,255,255]},j=A.map(M);let x=[];function L(){const N=g.current;if(!N)return;if(!(f!=null&&f.length)){x=[{minY:6,maxY:Math.max(12,N.clientHeight-24)}];return}const O=N.getBoundingClientRect(),b=[];for(const H of f)for(const Se of Array.from(N.querySelectorAll(H))){const ee=Se.getBoundingClientRect(),Ae=ee.top-O.top,$e=ee.bottom-O.top,sn={minY:Math.max(0,Ae-k-P),maxY:Math.max(0,Ae-k)},on={minY:Math.min(O.height,$e+k),maxY:Math.min(O.height,$e+k+P)};sn.maxY>sn.minY+1&&b.push(sn),on.maxY>on.minY+1&&b.push(on)}x=b.length?b:[{minY:6,maxY:Math.max(12,N.clientHeight-24)}]}L();const W=new ResizeObserver(()=>L());if(W.observe(_),f!=null&&f.length)for(const N of f)for(const O of Array.from(_.querySelectorAll(N)))W.observe(O);function J(){const N=x[Math.floor(Math.random()*x.length)]??{minY:6,maxY:20};return E(N.minY,N.maxY)}function te(N){const O=document.createElement("div");O.className="fx-drifter";const b=S();return O.textContent=b,w.appendChild(O),{el:O,dir:Math.random()<.5?1:-1,y:J(),bobAmp:E(.6,2.2),bobSpeed:E(.18,.5),bobPhase:E(0,Math.PI*2),lifespan:E(r,a),age:E(0,a),peakAt:E(.25,.75),peakWidth:i*E(.7,1.4),snippet:b,colorPhase:Math.random()}}function G(N){N.dir=Math.random()<.5?1:-1,N.y=J(),N.bobAmp=E(.6,2.2),N.bobSpeed=E(.18,.5),N.bobPhase=E(0,Math.PI*2),N.lifespan=E(r,a),N.age=0,N.peakAt=E(.25,.75),N.peakWidth=i*E(.7,1.4),N.snippet=S(),N.el.textContent=N.snippet,N.colorPhase=Math.random()}const de=Array.from({length:t},()=>te());let oe=0,B=performance.now(),F=!0;function v(N){if(!F)return;const O=Math.min(.05,(N-B)/1e3);B=N;const b=_.clientWidth;for(const H of de){H.age+=O;const Se=H.age/H.lifespan;if(Se>=1){G(H);continue}const ee=H.el.offsetWidth||200,Ae=H.dir>0?-ee-20:b+20,$e=H.dir>0?b+20:-ee-20,sn=Ae+($e-Ae)*Se,on=Math.sin(H.age*H.bobSpeed+H.bobPhase)*H.bobAmp,wt=(Se-H.peakAt)/H.peakWidth,Da=Math.exp(-(wt*wt)),vi=s*Da,Ba=d*(1-Da),_r=Math.max(-1,Math.min(1,(Se-H.peakAt)/m)),La=1-Math.abs(_r),Na=-H.dir*u*_r,ki=p*La;if(H.el.style.transform=`translate3d(${sn}px, ${H.y+on}px, ${ki}px) rotateY(${Na}deg)`,H.el.style.opacity=String(vi),j.length>1){const jt=((H.age/c+H.colorPhase)%1+1)%1*j.length,Er=Math.floor(jt)%j.length,bi=(Er+1)%j.length,Cr=jt-Math.floor(jt),qn=j[Er],Ar=j[bi],Si=Math.round(qn[0]+(Ar[0]-qn[0])*Cr),C=Math.round(qn[1]+(Ar[1]-qn[1])*Cr),D=Math.round(qn[2]+(Ar[2]-qn[2])*Cr);H.el.style.color=`rgb(${Si}, ${C}, ${D})`}else H.el.style.color||(H.el.style.color=A[0]);Ba>.15?H.el.style.filter=`blur(${Ba}px)`:H.el.style.filter!==""&&(H.el.style.filter="")}oe=requestAnimationFrame(v)}return oe=requestAnimationFrame(v),()=>{F=!1,cancelAnimationFrame(oe),W.disconnect();for(const N of de)N.el.remove()}},[t,r,a,s,i,d,u,p,m,f,k,P,l,o,c]),h.jsxs("div",{ref:g,className:"fx-root",style:{"--fx-color":o},children:[h.jsx("div",{ref:y,className:"fx-layer","aria-hidden":"true"}),h.jsx("div",{className:"fx-content",children:e})]})}const Gn=4,Sv=5,xv=4,Pv=1.2;function _v({variant:e="hero"}){const n=T.useRef(null),t=T.useRef({targetRx:0,targetRy:0,rx:0,ry:0,scrollBonusRy:0});return T.useEffect(()=>{if(typeof window>"u"||window.matchMedia("(prefers-reduced-motion: reduce)").matches||!n.current)return;let r=0;function a(c,d){const u=n.current;if(!u)return;const p=u.getBoundingClientRect();if(p.width<1||p.height<1)return;const m=(c-p.left)/p.width-.5,f=(d-p.top)/p.height-.5,k=t.current;k.targetRy=Math.max(-Gn,Math.min(Gn,m*Sv)),k.targetRx=Math.max(-Gn,Math.min(Gn,-f*xv))}function s(c){a(c.clientX,c.clientY)}function i(c){const d=c.touches[0];d&&a(d.clientX,d.clientY)}function o(){const c=t.current;c.scrollBonusRy=Math.sin(window.scrollY*.0028)*Pv}function l(){const c=t.current,d=Math.max(-Gn,Math.min(Gn,c.targetRx)),u=Math.max(-Gn,Math.min(Gn,c.targetRy+c.scrollBonusRy));c.rx+=(d-c.rx)*.12,c.ry+=(u-c.ry)*.12,c.targetRx*=.988,c.targetRy*=.988,n.current&&(n.current.style.transform=`perspective(1400px) rotateX(${c.rx.toFixed(3)}deg) rotateY(${c.ry.toFixed(3)}deg)`),r=requestAnimationFrame(l)}return window.addEventListener("mousemove",s,{passive:!0}),window.addEventListener("touchmove",i,{passive:!0}),window.addEventListener("scroll",o,{passive:!0}),r=requestAnimationFrame(l),()=>{cancelAnimationFrame(r),window.removeEventListener("mousemove",s),window.removeEventListener("touchmove",i),window.removeEventListener("scroll",o)}},[]),h.jsx("div",{className:`tilt-logo-stage tilt-logo-stage--${e}`,ref:n,children:h.jsx("img",{src:"/images/chromatika.svg",alt:"",className:"tilt-logo-img",width:560,height:560,decoding:"async"})})}const Ev=[{id:"btc",name:"Bitcoin",iconSrc:"/logos/btc.svg"},{id:"evm",name:"EVM",iconSrc:"/logos/eth.svg"},{id:"sol",name:"Solana",iconSrc:"/logos/sol.svg"},{id:"sui",name:"Sui",iconSrc:"/logos/sui.svg"},{id:"apt",name:"Aptos",iconSrc:"/logos/apt.svg"}],Cv=[{id:"ika",label:"ika",href:"https://www.ika.xyz/",iconSrc:"/logos/ika.svg"},{id:"encrypt",label:"encrypt",href:"https://github.com/dwallet-labs/ika/blob/main/docs/content/docs/sdk/cryptography.mdx",iconSrc:"/logos/encrypt.svg"},{id:"solana",label:"solana",href:"https://solana.com/",iconSrc:"/logos/sol.svg"},{id:"sui",label:"sui",href:"https://sui.io/",iconSrc:"/logos/sui.svg"}],Ji=[{id:"ika",title:"ika",logoSrc:"/logos/ika.svg",intro:"ika is building the coordination layer for programmable distributed signing: a network and tooling stack that turns dWallets into first-class identities you can actually ship in products. the mission is auditable cryptography, real cross-chain reach, and UX that does not ask end users to become protocol archaeologists.",howWeUse:"Chromatika routes dWallet creation, presign pools, ika-priced PTBs, and cross-chain signing through the Sui-base ika SDK path production uses today. we treat the coordinator, pricing tables, and user-share flows as the source of truth for anything that touches your vault's MPC identity.",howWeUseBullets:["DKG, accept-share, presign refill, and sign / re-encrypt PTBs share one client stack with the wallet vault.","Solana ika base stays behind the pre-alpha gate: mock signing only, never sold as production custody."],links:[{label:"ika.xyz",href:"https://www.ika.xyz/"},{label:"github",href:"https://github.com/dwallet-labs/ika"},{label:"sdk docs (book)",href:"https://github.com/dwallet-labs/ika/tree/main/docs/content/docs"},{label:"x / ika",href:"https://x.com/ikadotxyz"}]},{id:"encrypt",title:"encrypt",logoSrc:"/logos/encrypt.svg",intro:"Encrypt names the public cryptography story beside ika: documented ciphersuites, signature normalization, and user-share handling so MPC wallets stay interoperable instead of vibes-based. it is the reference for how bytes get hashed, wrapped, and verified when dWallets touch ed25519-class curves and personal payloads.",howWeUse:"we follow ika's published Ed25519 + SHA-512 personal-message path for `sui_signPersonalMessage` style bytes, not Mysten's BLAKE2b intent, and we lean on the same spec when reasoning about user share encryption and completed signature parsing.",howWeUseBullets:["`parseSignatureFromSignOutput` and curve / algorithm mapping stay aligned with ika's cryptography chapter.","dapps that assume Mysten-native personal-message verification need a compatibility conversation; the wallet documents the gap on purpose."],links:[{label:"cryptography.mdx",href:"https://github.com/dwallet-labs/ika/blob/main/docs/content/docs/sdk/cryptography.mdx"},{label:"ika repo",href:"https://github.com/dwallet-labs/ika"},{label:"ika.xyz",href:"https://www.ika.xyz/"},{label:"x / encrypt",href:"https://x.com/encrypt_xyz"}]},{id:"lazorkit",title:"LazorKit",logoSrc:"/logos/lazorkit.svg",intro:`LazorKit is Solana's passkey-native smart-wallet execution layer: WebAuthn on the front, program-owned accounts on chain, and a developer SDK that treats seed-phrase anxiety as a bug. they aim to make "sign in like a normal app" the default for Solana dapps that still want programmable custody.`,howWeUse:"Chromatika watches LazorKit the same way we watch Seeker and MWA: as the template for device-bound Solana auth without pasting secrets through the clipboard. onboarding flows and hardware metaphors on Solana ika base borrow language and expectations from their UX.",howWeUseBullets:["Choose-step entry on Solana surfaces Lazor-style passkey paths beside Seeker pairing.","when Solana-base ika grows real MPC, we expect passkey-anchored vault stories to interlock with these account models, not fight them."],links:[{label:"lazorkit.com",href:"https://www.lazorkit.com/"},{label:"docs",href:"https://docs.lazorkit.com/"},{label:"github org",href:"https://github.com/lazor-kit"},{label:"x",href:"https://x.com/lazorkit"}]},{id:"sui-passkeys",title:"Sui + passkeys",logoSrc:"/logos/sui.svg",intro:"Sui is Mysten's horizontally scaled Layer 1 for digital asset ownership: object-centric storage, Move for safer resource logic, and tooling (including GraphQL-first data planes) built for apps that need honest throughput without bespoke indexer hacks. passkeys and sponsor-ready flows are first-class topics on their product roadmap, not an aftermarket patch.",howWeUse:"production ika today is Sui-base: GraphQL core APIs feed vault reads, ika PTBs ride Mysten transactions, and Enoki-class passkey stories are on our radar wherever Mysten ships wallet infrastructure we can align with.",howWeUseBullets:["`SuiGraphQLClient` is the default transport anywhere core covers the read (JSON-RPC only for legacy gaps).","dWallet objects, caps, and ika-priced splits stay expressed as Move calls the wallet simulates with the same client you use in their docs."],links:[{label:"sui.io",href:"https://sui.io/"},{label:"build hub",href:"https://sui.io/build"},{label:"docs",href:"https://docs.sui.io/"},{label:"github / Mysten",href:"https://github.com/MystenLabs/sui"},{label:"x",href:"https://x.com/SuiNetwork"}]},{id:"seeker",title:"Solana Seeker",logoSrc:"/logos/skr.svg",intro:"Seeker is Solana Mobile's flagship hardware wallet phone: a pocket-sized Solana node with secure UI, travel-ready signing, and tight integration with the mobile wallet adapter stack. it is meant to feel like a premium consumer device, not a dev board duct-taped to Ledger principles.",howWeUse:"remote mobile-wallet adapter pairing lets Chromatika on desktop complete MWA sessions through Solana Mobile's reflector wire while Seeker (or any compatible wallet) holds the keys. chromatika persists reauthorization tokens so repeat signing skips QR gymnastics until the phone revokes trust.",howWeUseBullets:["pairing signs a fixed derivation message so ika user-share seeds stay deterministic per device without exporting an HD mnemonic from the phone.","local Android `solana-wallet://` intents remain the sibling path when extension and wallet share one device."],links:[{label:"solana mobile",href:"https://solanamobile.com/"},{label:"Seeker",href:"https://solanamobile.com/seeker"},{label:"docs",href:"https://docs.solanamobile.com/"},{label:"github / SDK",href:"https://github.com/solana-mobile/mobile-wallet-adapter"},{label:"x",href:"https://x.com/solanamobile"}]},{id:"waap",title:"WaaP",logoSrc:"/logos/WaaP.svg",intro:"WaaP is wallet as a protocol from the human.tech stack: one-click sign-on, trustless account recovery, and a product surface aimed toward borderless finance and identity. it sits with Human Passport and Human Network as open ecosystem infrastructure for keys, sessions, and identity.",howWeUse:"Chromatika offers WaaP as a sign-on path when you create a vault: you can open a new encrypted vault through WaaP instead of relying only on a seed phrase, key import, or hardware onboarding.",howWeUseBullets:["WaaP is an auth entry point into vault creation, not a replacement for Chromatika's local vault encryption or ika dWallet flows after you are in."],links:[{label:"waap.xyz",href:"https://waap.xyz/"},{label:"human.tech docs",href:"https://docs.human.tech/"},{label:"x / Waapxyz",href:"https://x.com/WaaPxyz"}]}];function Av(){return h.jsx("section",{className:"home-hero","aria-labelledby":"home-hero-title",children:h.jsx(bv,{options:{count:5,minLifespanSec:22,maxLifespanSec:38,peakOpacity:.3,maxBlurPx:1.1,maxYawDeg:6,maxZPx:26,peakWidth:.07,bendWindow:.22,bandPaddingPx:6,bandHeightPx:10,targetSelectors:[".home-hero-copy",".home-hero-logo"]},children:h.jsxs("div",{className:"home-hero-grid",children:[h.jsxs("div",{className:"home-hero-copy",children:[h.jsx("h1",{id:"home-hero-title",className:"home-hero-title",children:"one vault, many chains"}),h.jsx("ul",{className:"home-hero-chains","aria-label":"supported chains",children:Ev.map(e=>h.jsxs("li",{className:"home-hero-chain-item",title:e.name,children:[h.jsx("img",{src:e.iconSrc,alt:"",width:36,height:36,className:"home-hero-chain-icon",decoding:"async"}),h.jsx("span",{className:"visually-hidden",children:e.name})]},e.id))}),h.jsx("p",{className:"home-hero-lead",children:"Chromatika is a Chromium extension wallet: one Argon2id-encrypted vault, ika dWallet identity, and the usual chains (Bitcoin, EVM, Solana, Sui, Aptos) from a side panel and popup. this site is the companion hub: user guide, searchable knowledge base, and honest pre-release notes while you try the build or read how the pieces fit together."}),h.jsx("nav",{className:"home-hero-tech","aria-label":"core tech links",children:h.jsx("ul",{className:"home-hero-tech-list",children:Cv.map(e=>h.jsx("li",{children:h.jsxs("a",{className:"home-hero-tech-link",href:e.href,target:"_blank",rel:"noreferrer",children:[h.jsx("img",{src:e.iconSrc,alt:"",width:28,height:28,className:e.id==="encrypt"?"home-hero-tech-icon home-hero-tech-icon--encrypt":"home-hero-tech-icon",decoding:"async"}),h.jsx("span",{children:e.label})]})},e.id))})})]}),h.jsx("div",{className:"home-hero-logo",children:h.jsx(_v,{variant:"hero"})})]})})})}const Sc=[{id:"start",label:"start here",blurb:"what Chromatika is, how to try the build, where truth lives in the repo."},{id:"identity",label:"identity & dWallets",blurb:"ika base chains, dWallet Vault vs dWallet vs Chromatika vault: the words users trip on."},{id:"security",label:"security & vault",blurb:"encryption, session, auto-lock, and what we do not promise in pre-release."},{id:"chains",label:"chains & dapps",blurb:"EVM, Bitcoin, Solana, Sui, Aptos; providers; signing quirks worth knowing."},{id:"hardware",label:"hardware",blurb:"Ledger, Trezor (partial), Solana mobile wallet adapter and Seeker-style pairing."},{id:"product",label:"status & roadmap",blurb:"shipped vs gated vs stubs: a friendly mirror of STATUS.md for readers."}],di=[{slug:"what-is-chromatika",title:"what is Chromatika?",categoryId:"start",summary:"A pre-release Chrome MV3 wallet where your cross-chain face is ika dWallet-backed, not a pile of unrelated HD accounts.",body:["Chromatika is a browser extension (Manifest V3) that behaves as a multi-chain wallet with a deliberate rule: transactions, dapp connections, signing, and addresses shown on supported networks trace to an ika dWallet (2PC-MPC), not a separate plain HD identity per chain.","You get a local encrypted Chromatika vault (Argon2id + AES-256-GCM) that can hold multiple dWallet Vault records. Each is an owner keyring for a given ika base chain, with its own dWallets and keys.","Primary UI is the side panel; the popup covers quick actions, approvals, and hardware flows. This site is a knowledge base; the extension source and STATUS.md remain the technical source of truth."]},{slug:"try-the-extension",title:"try the extension (developers)",categoryId:"start",summary:"Clone the repo, pnpm install in wallet-extension, build, load unpacked from dist/.",body:["Chromatika is not positioned as a public Chrome Web Store drop yet. Developers load an unpacked build.","From the repo root: cd wallet-extension, pnpm install, pnpm run build, then in chrome://extensions enable developer mode and Load unpacked pointing at wallet-extension/dist.","pnpm test runs Vitest; pnpm run test:e2e builds and runs Playwright smoke tests. See the repo README for architecture links."]},{slug:"terminology-core",title:"terminology: dWallet Vault, dWallet, Chromatika vault",categoryId:"identity",summary:"Three layers of naming: get these right and the rest of the docs click.",body:["Chromatika vault: the local encrypted store unlocked with your app password. It holds JSON records for multiple dWallet Vaults plus metadata.","dWallet Vault: the owner wallet for a given ika base chain (Sui or Solana). It funds ika fees and owns dWallet objects on that base. Created from a new mnemonic, import paths, hardware, or dWallet-anchored flows.","dWallet: the ika MPC wallet used as canonical identity for dapps and user-visible addresses on supported chains.","In UX, avoid saying Vault alone. Say dWallet Vault (owner keyring) or Chromatika vault (encrypted app store) so users know which layer you mean."]},{slug:"ika-base-chains",title:"ika base chains (Sui vs Solana)",categoryId:"identity",summary:"Sui is the production ika path. Solana ika base is dev pre-alpha with mock signing, not custody you trust with real value.",body:["Base chain is where ika anchors a dWallet: Sui uses object IDs; Solana uses PDAs in future-facing code.","Sui: @ika.xyz/sdk, GraphQL-first Sui client shared with IkaClient. This is the path you can explore for real PTB / presign / sign flows today.","Solana ika base is behind a dev flag. It uses pre-alpha grpc and mock signing. All signatures come from a single mock signer. Treat it as SDK exploration only; program data can be wiped. Never promote as production MPC.","Product copy should label Solana pre-alpha surfaces clearly and avoid encouraging mainnet or real-asset reliance there."]},{slug:"vault-and-session",title:"vault encryption and session",categoryId:"security",summary:"Argon2id + AES-GCM vault; non-extractable CryptoKey while unlocked; session storage holds derived key bytes, not your password.",body:["The Chromatika vault blob uses Argon2id (RFC 9106 second option style parameters) and AES-256-GCM for confidentiality and integrity of stored records.","While unlocked, the background session keeps a non-extractable AES CryptoKey and KDF metadata so switching dWallet Vaults does not always re-prompt for the password.","Unlock rehydrate uses chrome.storage.session for derived key material, not the plaintext password. Auto-lock ties to idle alarms and OS screen lock signals.","Pre-release stance: schema and crypto may change without migrations for old installs. Clearing extension storage and re-onboarding is the recovery path when we break dev profiles."]},{slug:"dapp-permissions-and-phishing",title:"dapp permissions and phishing protection",categoryId:"security",summary:"Origin-scoped consent for injected providers; dynamic declarativeNetRequest rules cap out under Chrome limits.",body:["Injected bridges validate origin and event source before forwarding wallet actions. Treat every new dapp as untrusted until the user connects.","Phishing uses declarativeNetRequest with a bundled list plus periodic refresh of MetaMask eth-phishing-detect style data, capped under Chrome rule count limits.","This is defense in depth, not a guarantee. Users should still verify domains and transaction details."]},{slug:"supported-chains",title:"supported chains (overview)",categoryId:"chains",summary:"EVM, Bitcoin (segwit + taproot), Solana, Sui, Aptos: different clients, same dWallet identity rule.",body:["EVM uses ethers v6: personal_sign, typed data, and sends. Dapp eth_sendTransaction routes through an approval popup; wallet-initiated sends skip the double gate by design.","Bitcoin sends use bitcoinjs-lib with segwit and taproot paths as implemented in the extension.","Solana uses @solana/web3.js for sends and off-chain message signing in line with common dapp expectations.","Sui uses @mysten/sui 2.x with GraphQL as the default transport for Ika-adjacent reads. JSON-RPC remains for a few legacy service paths until migrated.","Aptos uses @aptos-labs/ts-sdk."]},{slug:"sui-personal-message-note",title:"Sui personal message signing (compatibility note)",categoryId:"chains",summary:"sui_signPersonalMessage follows ika Ed25519 + SHA512 for raw bytes, not Mysten's BLAKE2b personal-message intent.",body:["Some dapps only verify Mysten's native PersonalMessage intent (BLake2b). Chromatika intentionally signs the ika-compatible path for personal messages today.","Transaction signing uses Mysten intent + BLAKE2b via signBuiltSuiTransactionBytes as documented in STATUS.","Parity work to offer BLAKE2b personal-message mode alongside ika mode may land later. Track WALLET_SECURITY.md and STATUS.md for wording updates."]},{slug:"injected-providers",title:"injected providers (dapp bridge)",categoryId:"chains",summary:"EIP-1193 / 6963, Bitcoin window, Aptos, Solana and Sui shims plus Wallet Standard registration.",body:["Ethereum-compatible dapps see a standards-shaped provider with chain add/switch where implemented.","Solana and Sui wallet-standard style registration allows ecosystem dapps to discover Chromatika alongside other wallets.","Bitcoin and Aptos entry points exist for compatible sites, always gated behind explicit user consent like other namespaces."]},{slug:"hardware-wallets",title:"hardware wallets (Ledger, Trezor, MWA)",categoryId:"hardware",summary:"WebHID for Ledger in extension pages; Trezor Connect iframe path; Solana mobile wallet adapter for Seeker-class devices.",body:["Ledger uses WebHID from popup or side panel contexts, never from the service worker. Users prove addresses and sign on device rather than pasting seed material for hardware accounts.","Trezor ships partial support: discovery and several signing paths; Bitcoin may still error until PSBT decomposition is finished. STATUS lists the exact gap.","Solana Mobile Wallet Adapter supports local Android intents on the same phone and remote QR pairing from desktop to Seeker-class wallets. Remote flows persist auth tokens so repeat signing skips rescans until reauthorization fails."]},{slug:"whats-shipped",title:"what ships today vs gated vs stub",categoryId:"product",summary:"Mirror of high-level STATUS.md: vault v3, ika on Sui, multi-chain UI, swap flag, hardware surfaces.",body:["Shipped highlights: Argon2id vault v3, ika DKG/presign/sign/re-encrypt on Sui base, presign pools per vault, phishing dNR, multi-chain clients, side panel surfaces (assets, activity, send, NFTs, dapps, ika staking, settings).","Phase B Sui→IKA swap via Aftermath is implemented behind a feature flag that defaults on in tree. Still evaluate network liquidity yourself on testnets.","Gated: Solana ika base dev flag, some roadmap encrypt.xyz stubs returning not_wired.","Stubs: parts of SolanaIkaAdapter still throw for Sui-shaped reads; Trezor Bitcoin decomposition; offscreen media cache not present.","For the living list, read wallet-extension/docs/STATUS.md in the repository."]},{slug:"resource-library",title:"resource library (site vs local guides)",categoryId:"start",summary:"How the knowledge base, on-site user guide, and optional user vs tech guide markdown libraries fit together.",body:["Open /resources for how all layers fit together. The full markdown stacks render under /library (user and tech indices plus every page).","Themed knowledge base articles and the task-based user guide stay the lighter-weight surfaces; the library holds exhaustive reference in markdown.","After editing files under website/src/library/, run pnpm run sync:library so sibling links keep working. When anything disagrees with wallet-extension/docs/, trust the repo."]},{slug:"getting-help",title:"source docs and help",categoryId:"start",summary:"Where to read next once you leave this marketing-safe summary.",body:["Repository: github.com context from your checkout: README.md (root), wallet-extension/README.md (wallet detail), wallet-extension/docs/STATUS.md (truth index).","Terminology: wallet-extension/docs/TERMINOLOGY.md. Security practices and roadmap gaps: wallet-extension/docs/WALLET_SECURITY.md.","Multi-dWallet Vault phases: wallet-extension/docs/DWALLET_VAULT_MODEL.md. Architecture diagram: wallet-extension/docs/architecture-final.html.","This website summarizes for humans; see /resources for how our KB, guide, and optional local markdown libraries relate. When docs disagree, the docs folder in the repo wins."]}];function Iv(e){return di.find(n=>n.slug===e)}function Rm(e){return di.filter(n=>n.categoryId===e)}const xc=[{id:"start",label:"getting started",blurb:"install, first unlock, onboarding, where the side panel vs popup matters."},{id:"daily",label:"everyday use",blurb:"sends, balances, activity, networks, and dapp connections at a steady pace."},{id:"advanced",label:"advanced",blurb:"ika surfaces, presign context, custom networks, dev and power workflows."},{id:"hardware",label:"hardware",blurb:"Ledger, Trezor where supported, Solana MWA and Seeker-style remote pairing."}],Pc=[{slug:"how-to-use-this-guide",title:"how to use this guide",sectionId:"start",lastUpdated:"2026-04-28",summary:"what each section covers, how screenshots and video will show up, and when to read safety articles first.",body:["This guide is task-based. Each article walks through one job you might do in Chromatika, starting with the shortest safe path.","Screenshots and short videos will sit in a media strip under the steps when we publish captures. Until then you will see placeholders or notes where a visual is coming.","If a task can move money or expose a seed phrase, read the safety hub first. The wallet will still ask you to confirm risky actions, but your own judgment is the first layer.",`- Start with getting started if you are new.
- Jump to everyday use for sends and connections.
- Open advanced when you are tuning ika or networks.
- Use hardware when your keys live on a device.`]},{slug:"first-run-side-panel",title:"first run: side panel and popup",sectionId:"start",summary:"where Chromatika lives in Chrome, what the side panel is for vs the popup, and how approvals appear.",lastUpdated:"2026-04-28",body:["Chromatika's primary surface is the Chrome side panel, pinned next to the page you are browsing. The popup opens for quick actions and for flows that need a focused window such as some hardware approvals.","When a dapp asks to connect or sign, you may see a small Chrome popup spawned by the extension. Treat every popup as sensitive: verify the site and action before approving.","Screenshot placeholder: side panel open on a test page with the Chromatika mark visible in the header chrome."]},{slug:"create-unlock-vault",sectionId:"start",title:"create or unlock your Chromatika vault",summary:"app password, Argon2id vault blob, and why the wallet locks on idle: no secrets in this article, only what to expect on screen.",lastUpdated:"2026-04-28",body:["Creating a vault picks an app password and wraps your dWallet Vault records in the local encrypted store. Unlocking derives key material in memory; auto-lock clears it after idle or OS screen lock depending on platform signals.","You will not paste a seed phrase into this article. The real flow stays inside the extension. Follow on-screen prompts only.","Video placeholder: 45s silent walkthrough from cold install to unlocked side panel (to record)."]},{slug:"send-funds-overview",sectionId:"daily",title:"send funds (overview)",summary:"choose chain, enter amount and destination, confirm on device if hardware, with a sanity checklist before broadcast.",lastUpdated:"2026-04-28",body:["Open Send from the side panel, pick the asset and network that match what the recipient expects, and paste or scan their address.","Double-check the chain type. Many losses are “right address, wrong network.”","If you use a hardware account, the last step happens on the device. The extension never asks you to export a hardware seed phrase to “heal” a send.","Screenshot placeholder: send form with testnet amounts and a highlighted network row."]},{slug:"connect-dapp",sectionId:"daily",title:"connect to a dapp",summary:"EVM, Solana, Sui, Bitcoin, and Aptos bridges each have a consent step. Verify origin before you approve.",lastUpdated:"2026-04-28",body:["When a page requests a wallet, Chromatika checks the origin and shows what capability the site is asking for. If you do not recognize the domain, reject and navigate away.","After you connect, review transaction details on every signature request. Phishing sites love urgency.","Screenshot placeholder: approval sheet with origin hostname callout."]},{slug:"network-switcher",sectionId:"advanced",title:"networks and custom RPCs",summary:"built-in registry plus custom entries: know what you are trusting when you add a custom endpoint.",lastUpdated:"2026-04-28",body:["The network selector combines built-in entries with custom rows you add. A malicious RPC can lie about balances and txs. Only use endpoints you trust.","Testnet faucets and dev endpoints belong in dev profiles, not mixed with accounts you treat as valuable.","Screenshot placeholder: network list with testnet badge."]},{slug:"ledger-first-steps",sectionId:"hardware",title:"Ledger: first connection",summary:"WebHID in the extension page context, address proof on device, and where Bitcoin support differs from EVM.",lastUpdated:"2026-04-28",body:["Ledger connects over WebHID from the popup or side panel after you click connect. The service worker cannot open HID by itself.","Prove addresses on the device screen before you treat an account as yours. The extension should never ask for the Ledger seed phrase.","Video placeholder: plugging in, WebHID allowlist, and showing an address on device."]}];function Tv(e){return Pc.find(n=>n.slug===e)}function Mm(e){return Pc.filter(n=>n.sectionId===e)}const Rv=`# chromatika user guides

high-level "how to" reference for every feature in chromatika. each guide lists prerequisites + steps + every available option for that feature, no UI references.

these pages are published on the chromatika site alongside the extension; they are aimed at testers, devs, and AI agents who need to know "what the wallet can do and what conditions / inputs each operation requires" without reading the source.

chromatika is **pre-release** - storage and crypto are dev-only, and Solana ika base is a pre-alpha mock signer. labels and disclaimers in each guide reflect that.

## vault & authentication

- [create a vault](/library/user/create-vault) - first-time setup with a fresh BIP39 mnemonic
- [import a vault from a mnemonic](/library/user/import-vault-mnemonic) - restore from existing BIP39 phrase
- [import a vault from a private key](/library/user/import-vault-private-key) - sui bech32 or solana 64-byte secret
- [unlock and lock](/library/user/unlock-and-lock) - password / passkey / hardware / recovery unlock + autolock + manual lock
- [passkey-backed vault](/library/user/passkey-vault) - WebAuthn + PRF register / sign / recover
- [WAAP-backed vault](/library/user/waap-vault) - @human.tech email / phone / social login
- [Lazor-backed solana vault](/library/user/lazor-vault) - solana smart-wallet passkey
- [hardware-only vault](/library/user/hardware-vault) - Solana MWA / Seeker as primary identity
- [manage multiple vaults](/library/user/manage-vaults) - list, add sibling, switch, rename, remove, cross-chain reuse
- [BIP39 recovery words](/library/user/recovery-words) - using the recovery branch to unlock passkey / WAAP / Lazor envelopes

## dWallet management

- [create a dWallet](/library/user/create-dwallet) - DKG per curve + zero-trust accept-share
- [manage dWallets](/library/user/manage-dwallets) - discover, set active, names, ordering, address book, register encryption key
- [transfer a dWallet](/library/user/transfer-dwallet) - initiate, parse digest, accept inbound transfer
- [presign pool](/library/user/presign-pool) - query and refill the three presign pools

## hardware wallets

- [Ledger](/library/user/ledger) - WebHID pair + EVM / Sui / Solana / Bitcoin signing
- [Trezor](/library/user/trezor) - account discovery + EVM / Solana signing (no BTC PSBT, no Sui)
- [Solana MWA local](/library/user/seeker-local) - Android same-device Mobile Wallet Adapter
- [Solana MWA remote](/library/user/seeker-remote) - desktop ↔ phone QR-paired remote MWA
- [WalletConnect](/library/user/walletconnect) - WC remote signer (notably for the x402 Solana path)

## sending & signing

- [send EVM](/library/user/send-evm) - native + ERC20 + contract calls from the wallet UI
- [send SUI](/library/user/send-sui) - native SUI from the HD fee-payer
- [send Solana](/library/user/send-solana) - native SOL from the ED25519 dWallet
- [send Bitcoin](/library/user/send-bitcoin) - native BTC from the SECP256K1 dWallet's P2WPKH address
- [approve a dapp transaction](/library/user/approve-dapp-tx) - reviewing dapp \`eth_sendTransaction\` requests
- [sign messages across chains](/library/user/sign-messages) - personal_sign / typed-data / Sui / Solana / Aptos / BTC

## dapp bridge

- [connect a dapp](/library/user/connect-dapp) - approving connection requests with per-curve dWallet selection
- [manage dapp permissions](/library/user/manage-dapp-permissions) - list, revoke, switch active address per origin
- [dapp consent mode](/library/user/dapp-consent-mode) - compat vs strict
- [phishing protection](/library/user/phishing-protection) - dNR rules + manual checks

## networks

- [manage active networks](/library/user/manage-networks) - per-chain switching, vault vs dWallet tier, EVM RPC health, ika base mode
- [add custom networks](/library/user/add-custom-network) - manual EVM + Chainlist search + remove

## settings

- [app settings](/library/user/app-settings) - advanced mode, help hints, theme, explorer preferences
- [media safety mode](/library/user/media-safety-mode) - NFT / Ordinal image filtering
- [price source priority](/library/user/price-source-priority) - reorder the price waterfall

## agent surface, swap, x402

- [agent surface (MCP)](/library/user/agent-surface-mcp) - enable, port, token, approve sign, native messaging host
- [SUI → IKA swap](/library/user/sui-ika-swap) - Aftermath router quote + execute
- [x402 HTTP payments](/library/user/x402-payments) - approve fetch-intercepted USDC payments, caps, receipts

## safety + activity

- [safety alerts](/library/user/safety-alerts) - mute / opt-out / view history / set custom feed for the signed-alert subsystem
- [encrypted activity notes](/library/user/activity-notes) - per-tx encrypted notes via the encrypt.xyz pre-alpha backend

## assets, kiosks, staking

- [browse NFTs](/library/user/browse-nfts) - per chain (Sui, EVM, Solana, Aptos, Bitcoin Ordinals)
- [Sui Kiosks](/library/user/sui-kiosks) - listing kiosks and items
- [activity and portfolio](/library/user/view-activity-and-portfolio) - tx history, balances, addresses
- [Ika staking](/library/user/ika-staking) - validators, stake, withdraw

## ika ops

- [ika fee-payer (Solana base)](/library/user/ika-fee-management) - mode, top-up, drain, status

## encryption

- [encrypted dWallet labels](/library/user/encrypted-dwallet-labels) - per-dWallet on-chain label (Solana base, pre-alpha)
- [encryption lab](/library/user/encryption-lab) - encrypted-input creation + ciphertext reads (Solana base, dev / lab)
`,Mv=`# how to use encrypted activity notes

attach a private note (up to 2048 utf-8 bytes) to any transaction your wallet signed. the note is encrypted to your active dWallet's ed25519 key via the encrypt.xyz pre-alpha backend. only the same vault on the same install (or any device that recovers the same dWallet identity) can decrypt. switching to a different vault locks you out of that vault's notes.

**pre-alpha disclaimer**: encrypt.xyz is pre-alpha. ciphertexts may be plaintext on-chain in this phase. **don't put real secrets in notes** - treat as dev preview / functional placeholder until production.

## prerequisites

- chromatika vault is unlocked
- the active dWallet Vault is on **Solana ika base** (the encryption backend asserts Solana-base; throws on Sui-base)
- you signed the tx in question with **this** chromatika install (notes only attach to records the local tx-record service has)
- some IKA / SOL for the gRPC \`approve_message\` fees the encrypt path needs (~0.001 SOL per encrypt or decrypt)

## options at a glance

- **add a note**: encrypt a fresh plaintext to your dWallet's ed25519 key
- **view a note**: decrypt to read (1-3s latency due to MPC sign + gRPC round-trip)
- **edit a note**: re-encrypt with new plaintext, replacing the old ciphertext id
- **remove a note**: drop the local pointer (on-chain ciphertext stays but is unreferenceable from your local store)

## how to add a note to a transaction

1. find the transaction in your activity feed (only txs your local wallet signed are eligible - other people's txs you've received don't qualify)
2. open the note edit affordance for that tx
3. type plaintext (up to 2048 utf-8 bytes; the editor shows a byte counter)
4. save. background:
   - generates a fresh 32-byte AES-256 key + 12-byte AES-GCM IV
   - encrypts your plaintext under that AES key (AES-GCM)
   - splits the AES key into two 16-byte chunks
   - wraps each chunk via encrypt.xyz \`CreateInput\` as EUint128 ciphertext (one batched gRPC call)
   - persists the resulting \`EncryptedRef\` (two ciphertext-identifier hexes + body ciphertext + IV) on the local tx record at \`chromatika_signed_txs_v1\`
5. the activity row gets a lock badge indicating "this tx has an encrypted note"

## how to view a note

1. tap the lock badge on an activity row (or the "view note" button)
2. background:
   - reads the stored \`EncryptedRef\`
   - validates that your active dWallet's ed25519 pubkey still matches the recipient pubkey on the ref (different vault = decrypt rejected with \`wrong-vault\` error)
   - signs two \`ReadCiphertext\` requests via ika MPC (one per AES key chunk) - this is the 1-3s latency
   - reassembles the 32-byte AES key
   - imports as a non-extractable \`CryptoKey\`
   - AES-GCM decrypts the body
   - returns plaintext
3. plaintext shows in the modal (read-only)
4. close to dismiss; the plaintext is dropped from memory

## how to edit a note

1. open the modal as if to view
2. switch to edit mode
3. type the new plaintext
4. save: the same encrypt path runs fresh, producing a new \`EncryptedRef\` that replaces the old one on the record
5. the old on-chain ciphertext-identifier becomes unreferenced. on devnet it'll get wiped; on production its lifecycle is determined by encrypt.xyz's storage rules

## how to remove a note

1. open the modal
2. click remove
3. background: drops \`encryptedNote\` from the local tx record. idempotent
4. on-chain ciphertext is **not** deleted - just unreferenced from your local pointer
5. lock badge disappears from the activity row

## how to know if a tx has a note (without decrypting)

1. lock badge on the activity row indicates a note is attached
2. lightweight \`getActivityNoteStatus\` query under the hood - no decrypt, just a presence check
3. doesn't trigger MPC signing, no fee cost

## notes

- notes are **vault-keyed**: each vault encrypts to its own dWallet's ed25519 pubkey. switching active vault = can't decrypt the previous vault's notes (by design). you can switch back at any time to read them again
- notes are **per tx hash**: one note per tx hash per vault. re-saving replaces the previous note
- notes are **local-first**: the local \`chromatika_signed_txs_v1\` store holds the pointer; the encrypted ciphertext lives in encrypt.xyz on-chain accounts. lose the local store (uninstall, browser data wipe) and the pointer is gone - the on-chain ciphertext is unreferenceable
- decrypt latency is dominated by the **two sequential MPC signs** for \`ReadCiphertext\` calls. there's no batched-decrypt path today; the encrypt.xyz pre-alpha protocol requires per-chunk signed reads. tracked future
- the \`EncryptionBackend\` abstraction supports backends beyond encrypt.xyz (direct ed25519 X25519-ECDH, Seal Move policy). today only encrypt.xyz is wired; the others are stubs that throw \`not-implemented\`. when more backends ship, existing notes auto-route by backend tag on the ref
- a future "cross-recipient note" path could let you encrypt a note to **someone else's** dWallet ed25519 pubkey (e.g. share a tx description with a colleague). not implemented; tracked
- only **EVM send paths** record locally today - sui / solana / btc / aptos sends and message signing don't write a tx record, so notes can't attach to them yet. tracked future hardening
`,Dv="# how to add a custom network\n\nextend chromatika's built-in registry with a custom EVM chain (or in principle, a custom Sui / Solana / Aptos / Bitcoin Esplora network when those custom-add paths surface in the API). the EVM custom-add flow includes Chainlist search-and-import as a shortcut.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- you know the network parameters for the chain you want to add\n- the RPC URL is reachable and conforms to JSON-RPC (EVM) or the chain's native RPC\n\n## options at a glance\n\n- **manual EVM add**: full parameters supplied yourself\n- **EVM Chainlist search**: type a chain name or chainId; the wallet searches Chainlist and fills params for you\n- **non-EVM custom networks**: the architecture supports custom Sui / Solana / Aptos / Bitcoin Esplora networks via the same registry; today the surfaced custom-add procedure is EVM-specific\n\n## how to add a custom EVM chain manually\n\n1. submit `addCustomNetwork` with: `name`, `chainId` (decimal), `rpcUrl`, `symbol`, `decimals`, optional `explorerUrl`\n2. the wallet validates the RPC by sending a `eth_chainId` probe; if the RPC returns a different chainId than declared, the call rejects\n3. on success the network joins `chromatika_custom_networks_v1`; it's available everywhere the registry is read\n\n## how to find a chain on Chainlist\n\n1. submit `importFromChainlist` with `query` (a chain name string, partial match OK, or a chainId number)\n2. response is the matching candidate set (read-only - no state changes)\n3. once you've found the right chain, feed those parameters to `addCustomNetwork` to commit\n\n## how to remove a custom EVM chain\n\n1. submit `removeCustomNetwork` with `chainId`\n2. the entry is dropped. if the removed chain was the active EVM chain, the wallet falls back to a default\n\n## non-EVM custom networks\n\nthe registry supports per-chain types (Sui / Solana with `name + rpc`; Aptos `name + rpc`; Bitcoin `name + esplora url`) and architecture-final.html documents the schema. exposing add / remove for those via tRPC is in progress; check the latest router for `setActiveSuiNetwork` / `setActiveSolanaNetwork` / `setActiveBitcoinNetwork` / `setActiveAptosNetwork` networkIds the wallet currently knows about\n\n## notes\n\n- custom networks merge with built-ins under one logical registry per chain - the active selection works the same regardless of source\n- bad RPCs that don't validate are rejected before persistence to keep the registry clean\n- `getEvmRpcHealth` (see [manage-networks.md](/library/user/manage-networks)) checks the active EVM RPC, which can include any custom-added chain you're using\n- the architecture's \"EVM shortcut: chainlist\" is the read-only search path; you still commit via `addCustomNetwork` once you've picked\n",Bv="# how to use the agent surface (MCP)\n\nchromatika exposes a Model Context Protocol (MCP) surface so AI agents (Claude Desktop, Cursor, Cline, etc.) can read wallet state and request signed actions, with user approval popups for anything that signs. the bridge runs through a chrome native messaging host that listens on `127.0.0.1:<port>` (HTTP MCP) or accepts stdio bridging for clients that only speak stdio.\n\n## prerequisites\n\n- chromatika is installed and unlocked at least once (the read tier respects lock state)\n- you've registered the native host on your OS once: `pnpm setup:native-host --extension-id=<id>`. this writes the per-OS native messaging directory entry (and on Windows a `.bat` shim + `reg add`)\n- the agent client (Claude Desktop, Cursor, etc.) has chromatika configured as an MCP server pointing at the agent URL\n\n## options at a glance\n\n- **enable / disable**: turn the surface on or off (off = no listener, no native host)\n- **bearer token**: per-install token, gates the HTTP MCP listener. reveal, copy, or rotate at any time\n- **listen port**: random by default; you can pin one (1024-65535) so Claude Desktop config doesn't churn across chrome restarts\n- **stdio bridge**: same binary supports `--stdio-bridge` for stdio MCP clients. the bridge forwards line-delimited JSON-RPC to the chrome-spawned host\n- **read tier** (no popup): `listVaults`, `getActiveVault`, `getActiveNetworks`, `getLockState`\n- **approve tier** (popup-gated): `signMessage`, `sendEvmTx`, `signTransaction`\n\n## how to enable the agent surface\n\n1. call `mcpStatus` to see current state (`{ enabled, tokenHex, listenHost, listenPort, ... }`)\n2. submit `mcpEnable` - background generates a token if missing, spawns the native host via `chrome.runtime.connectNative`, exponential backoff on connect (5 attempts, 1s → 30s)\n3. poll `mcpStatus` until the host reports an active port\n4. configure your agent client with the agent URL (typically `http://127.0.0.1:<port>/mcp`) plus the bearer token\n\n## how to disable the agent surface\n\n1. submit `mcpDisable`\n2. background sends teardown to the host, drops the native port, clears stale state\n3. `mcpStatus` reflects the disabled state\n\n## how to view, copy, or rotate the bearer token\n\n1. read with `mcpStatus` (returns `tokenHex`)\n2. rotate with `mcpRotateToken` - generates a fresh token, pushes to the running host so the new token is live immediately. update your agent client to use the new token\n\n## how to set a fixed listen port\n\n1. submit `mcpSetDesiredPort` with `port` (1024-65535) or `null` to clear\n2. the host tries that port on startup; if it's already taken (other process), the host falls back to a random port and surfaces the actual port in `mcpStatus`\n3. setting a stable port avoids reconfiguring Claude Desktop after every chrome restart\n\n## how to approve an incoming MCP sign request\n\nread tier returns immediately. approve tier opens a popup at `?mcpapprove=<id>`:\n\n1. popup calls `getPendingMcpSignRequest` with the id\n2. response includes: `chain` (`'evm' | 'solana'`), `messageHex`, optional `evmChainId` (for `signMessage` of EVM messages)\n3. submit `approvePendingMcpSign` with the id - background signs via ika MPC, returns `{ chain, signatureHex, signerAddress }` to the agent\n4. submit `rejectPendingMcpSign` with id and `reason` (default `'user_canceled'`) to deny\n\n## how the approve tier maps to existing flows\n\n- `signMessage` ({ chain, messageHex, evmChainId? }) → `McpApprovalScreen` popup → ika MPC signs → returns `{ chain, signatureHex, signerAddress }`\n- `sendEvmTx` ({ to, value?, data?, chainId?, gas?, ... }) → existing tx-approval popup with full gas / sim UI → `signAndBroadcastEvm` → returns broadcast tx hash\n- `signTransaction` ({ to, value?, data?, chainId?, gas?, ... }) → same popup with \"sign only - no broadcast\" banner → `signEvmTxOnly` → returns `{ signedRawTx, txHash }` for relayer / bundler / abstract-wallet flows\n\n## notes\n\n- the read tier is genuinely no-popup: agents that just need to know your active vault / lock state can poll without bothering you\n- the approve tier always pops user-visible UI - chromatika does not let an agent sign silently\n- per-port rebind is handled by the host receiving `{ type: 'reconfigure-port' }`; if bind fails the host falls back to random and reports\n- a Solana sendTx MCP tool is tracked as future. today MCP can sign Solana messages (read tier returns the address), but sending Solana goes through the existing dapp / wallet UI paths\n- nonce-race for `signTransaction`: nonce is reserved at sign time but a slow caller can race; if real users hit this, a \"reserve nonce in extension state\" pattern is on the future list\n",Lv=`# how to configure chromatika app settings

control the cosmetic and ergonomic toggles that don't fit elsewhere - advanced mode, help hints, theme, explorer URL templates, and a couple of personal-touch knobs.

## prerequisites

- a Chromatika vault is unlocked

## options at a glance

- **advanced mode**: toggle expert surfaces (e.g. raw fee-payer addresses, debug fields)
- **help hints**: education / onboarding overlays
- **appearance**: light or dark theme
- **explorer preferences**: which explorer URL template to use per chain (Sui, Solana - per-chain choice + custom template)
- **rocket-seat cosmetics** (if you've discovered them): pilot head, passenger head, animation toggle

## how to toggle advanced mode

1. read with \`getAdvancedMode\`
2. set with \`setAdvancedMode\` (boolean)
3. when on, surfaces that show fee-payer addresses, raw payloads, and debug-only state become visible

## how to toggle help hints

1. read with \`getUiHelpHints\`
2. set with \`setUiHelpHints\` (boolean)
3. when on, education overlays surface on common screens; turn off if you've memorized the wallet

## how to set theme

1. read with \`getAppearance\`
2. set with \`setAppearance\` (\`'light' | 'dark'\`)
3. takes effect immediately; persisted to chrome.storage

## how to set explorer preferences

1. read with \`getExplorerPreferences\`
2. set with \`setExplorerPreferences\`. shape:
   - sui: \`{ preset: 'suiscan' | 'suivision' | 'custom', customTemplate?: string }\`
   - solana: \`{ preset: 'solscan' | 'solanaExplorer' | 'orb' | 'custom', customTemplate?: string }\`
3. custom templates use placeholders like \`{address}\`, \`{txDigest}\` per the renderer
4. these flow into \`ExplorerValueRow\` / \`explorer-href.ts\` so any read-only address / digest in the UI links to the right explorer

## notes

- per the project rule: every read-only UI surface that prints a wallet-facing chain address, dWallet / Cap object id, or on-chain transaction digest should use \`ExplorerValueRow\` (or a thin wrapper). the explorer preferences here drive those links
- advanced mode does not change behavior - it surfaces information. all signing flows are identical regardless
- help hints are persisted per install, not per vault
`,Nv=`# how to approve a dapp transaction

when a connected dapp calls \`eth_sendTransaction\`, chromatika queues the request and opens an approval popup. this guide is the user side of that approval - reviewing the request, choosing gas, simulating, and approving or rejecting.

## prerequisites

- a Chromatika vault is unlocked
- the dapp origin has an existing connection permission with at least one EVM dWallet selected (see [connect-dapp.md](/library/user/connect-dapp))
- the active dWallet has SECP256K1 and SOL / native gas funds for the chain

## options at a glance

- **gas presets**: slow, normal, fast, custom (EIP-1559 or legacy depending on chain)
- **simulation**: eth_call at the latest block, no third-party simulator
- **outcome**: approve (sign + broadcast) or reject

## how to fetch the pending dapp tx

1. when a dapp calls \`eth_sendTransaction\`, the wallet enqueues the tx and opens \`index.html?txapprove=<id>\`
2. the popup calls \`getTxApprovalRequest\` with the id to read: \`from\`, \`to\`, \`value\`, \`data\`, \`chainId\`, the requesting origin

## how to view a simulated outcome

1. call \`getTxSimulationPreview\` with the id
2. response is the eth_call result at latest block - decoded if the wallet recognizes the function selector (no third-party API; honest about its limitations on opaque calldata)

## how to view gas presets

1. call \`getTxGasOptions\` with the id
2. response is { slow, normal, fast } per EIP-1559 (or the legacy equivalent), with USD estimates derived from the price waterfall (see [price-source-priority.md](/library/user/price-source-priority))
3. user picks one or supplies a custom override

## how to approve

1. submit \`approveTxRequest\` with the id and optional gas overrides (\`maxFeePerGas\`, \`maxPriorityFeePerGas\` for 1559 chains, or \`gasPrice\` for legacy)
2. background runs \`signAndBroadcastEvm\` against the dapp's selected dWallet, returns the tx hash to the dapp through the bridge
3. popup closes

## how to reject

1. submit \`rejectTxRequest\` with the id and a \`reason\` string
2. dapp receives a JSON-RPC error 4001 (user rejected request)

## notes

- the wallet-UI \`sendEvmTx\` flow is separate and **does not** open this popup. only dapp-originated tx do. **never wire wallet-ui sends through this popup** - the user is already in the wallet making an intentional action
- if the user disconnects the dapp permission while a tx is pending (see [manage-dapp-permissions.md](/library/user/manage-dapp-permissions)), the pending request rejects automatically
- gas estimation uses the active EVM provider (\`getRpcProvider\`); if the chain in the request differs from the wallet's active EVM chain, the wallet uses \`getRpcProviderForChain\`
- simulation is "best effort" - opaque contract calls may not decode cleanly. always verify recipient + value before approving
`,Kv=`# how to browse NFTs

list the NFTs your active dWallet (or any address you specify) owns, per chain. backed by chain-specific indexers; respects MediaSafetyMode for image rendering.

## prerequisites

- a Chromatika vault is unlocked
- API keys configured for the chains you want full coverage on:
  - **EVM**: \`VITE_ALCHEMY_KEY\` (otherwise EVM NFT lists return empty)
  - **Solana**: \`VITE_HELIUS_KEY\` (otherwise Solana NFT lists return empty)
  - Sui, Aptos, Bitcoin Ordinals work without keys
- the address you're querying (defaults to the active dWallet's per-chain address; can also pass any address)

## options at a glance

- **per chain** discovery functions:
  - Sui: on-chain reads + Display
  - EVM: Alchemy
  - Solana: Helius DAS
  - Aptos: Token v2 indexer
  - Bitcoin: Hiro Ordinals
- **MediaSafetyMode** filters image rendering at the request layer (see [media-safety-mode.md](/library/user/media-safety-mode))
- **API hints**: read \`getNftApiHints\` to know which keys are configured

## how to list Sui NFTs for an address

1. call \`getSuiNfts\` with \`address\`
2. response is the NFT list with metadata + image URLs (filtered by MediaSafetyMode)
3. uses \`client.core.*\` (GraphQL) where possible; falls back to JSON-RPC for legacy reads

## how to list EVM NFTs for an address on a chain

1. call \`getEvmNfts\` with \`address\` and \`chainId\`
2. response uses Alchemy. without \`VITE_ALCHEMY_KEY\` the call returns empty (no error) - check \`getNftApiHints\` to see if the key is set
3. \`chainId\` lets you query NFTs on a specific EVM network independent of the active EVM chain

## how to list Solana NFTs for an address

1. call \`getSolanaNfts\` with \`address\`
2. response uses Helius DAS. without \`VITE_HELIUS_KEY\` the call returns empty
3. supports compressed NFTs through Helius DAS

## how to list Aptos NFTs for an address

1. call \`getAptosNfts\` with \`address\`
2. response uses the Token v2 indexer (no key required)

## how to list Bitcoin Ordinals for an address

1. call \`getBtcOrdinals\` with \`address\`
2. response uses the Hiro Ordinals indexer (no key required)
3. Ordinals are inscriptions, not NFTs in the traditional sense - the metadata shape is Ordinal-specific

## how to know which APIs are configured

1. call \`getNftApiHints\`
2. response is \`{ alchemyConfigured: boolean, heliusConfigured: boolean }\`
3. surfaces use this to display "API key required" hints when the relevant chain returns empty

## notes

- chromatika does not cache NFT lists across sessions today (no offscreen media cache yet - tracked future). every call hits the indexer
- MediaSafetyMode \`none\` will return the metadata but skip loading images. \`ipfs-arweave\` (default) only renders IPFS / Arweave URLs. \`all\` renders everything
- if you want NFTs across multiple addresses (e.g. you have several dWallets), call the per-chain function once per address - there's no batch primitive
- transferring NFTs is per-chain native: EVM via \`sendEvmTx\` calldata, Sui via dWallet sign + \`IkaTransaction\`, etc. there's no NFT-specific transfer wrapper today
`,Wv=`# how to connect a dapp

dapps reach chromatika through page-injected providers (EIP-1193 / EIP-6963 for EVM, Wallet Standard for Sui + Solana, \`window.bitcoin\`, \`window.aptos\`). when a dapp asks to connect (\`eth_requestAccounts\`, Wallet Standard \`connect\`, etc.), chromatika queues the request and opens an approval popup so you can pick which dWallet(s) the dapp can see.

## prerequisites

- a Chromatika vault is unlocked
- you've decided which dWallet you want to expose to the dapp - the connection scopes to a specific dWallet per curve, not the whole vault
- the dapp's origin is not phishing-flagged (\`checkPhishing\` runs first; see [phishing-protection.md](/library/user/phishing-protection))

## options at a glance

- **per-origin dWallet selection**: pick a SECP256K1 dWallet (for EVM) and / or an ED25519 dWallet (for Sui / Solana / Aptos)
- **standards supported**: EIP-1193, EIP-6963 (multi-injected discovery), EIP-3085 (\`wallet_addEthereumChain\`), EIP-3326 (\`wallet_switchEthereumChain\`), Wallet Standard for Sui + Solana, \`window.bitcoin\`, \`window.aptos\`
- **outcome**: approve (with dWallet choice), reject, or close (treated as reject)

## how to fetch the pending connection request

1. when a dapp calls a connect-style method, the wallet enqueues the request and opens the dapp-approval popup
2. popup calls \`getDappApprovalRequest\` with the request id
3. response contains: requesting origin, requested method, the dWallet choice options (which curves the dapp needs)

## how to approve a connection

1. submit \`approveDappConnection\` with: id, \`approved: true\`, and the dWallet ids you want exposed:
   - \`secpDwalletId\` for EVM-style methods
   - \`ed25519DwalletId\` for Solana / Sui / Aptos-style methods
   - either or both depending on the method - some dapps only need one curve
2. background records the permission in \`chromatika_dapp_permissions_v1\`, returns the resolved account list to the dapp through the bridge

## how to reject a connection

1. submit \`rejectDappConnection\` with id and \`reason\` string
2. dapp receives a JSON-RPC error 4001 (or the equivalent for non-EVM standards)

## how to view what consent options the request has

\`getDappApprovalRequest\` returns the candidate dWallets the user can pick from per curve - if you only have one ED25519 dWallet, that's the only choice the popup shows for non-EVM methods

## notes

- chromatika never auto-approves connections in the default settings. compat mode (see [dapp-consent-mode.md](/library/user/dapp-consent-mode)) relaxes some prompts but never connections
- the dapp permission is **per origin**, **per curve** - same origin can have a SECP and an ED25519 dWallet selected, but only one of each
- non-EVM connect uses **strict** consent gates that mirror EVM patterns where implemented (per architecture-final.html)
- Sui personal-message signing through the connected origin uses the ika SHA512 path, not Mysten's BLAKE2b - some dapps may not verify until Mysten-intent BLAKE2b parity ships (see [sign-messages.md](/library/user/sign-messages) notes)
`,jv="# how to create a dWallet\n\na dWallet is the ika MPC wallet (2PC-MPC) that signs on-chain transactions and dapp requests for you. each dWallet is bound to a curve (SECP256K1 for EVM/BTC, ED25519 for Sui/Solana/Aptos). this guide walks the DKG (distributed key generation) and zero-trust accept-share completion path.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active dWallet Vault is funded for DKG fees:\n  - **Sui base**: a few IKA + a few SUI on the active dWallet Vault address - exact minimums change with on-chain pricing, see `getRequiredCoinAmounts` (uses pricing from `coordinatorInner.pricing_and_fee_manager.current.pricing_map` with a 10% buffer)\n  - **Solana base** (pre-alpha): the in-extension fee-payer keypair has a small SOL balance for ika gRPC `approve_message` fees\n- decide which curve you need\n\n## options at a glance\n\n- **curve**: `SECP256K1` (EVM, BTC) or `ED25519` (Sui, Solana, Aptos)\n- **base chain**: inherited from the active dWallet Vault (sui or solana)\n- **per-curve active dWallet**: you can have multiple dWallets per curve but only one active at a time per curve\n\n## how to run DKG for a new dWallet\n\n1. confirm IKA + SUI (or SOL) balances meet the current minimum via `getRequiredCoinAmounts`\n2. submit `createDWallet` with the desired `curve`\n3. background calls `requestDWalletDKG` (Sui base via PTB on `IkaTransaction`, Solana base via gRPC). the call needs `&mut Coin<IKA>` + `&mut Coin<SUI>` references on Sui base; split coins are returned to the owner inside the same PTB\n4. dynamic pricing is read at PTB build time so a price bump between read and submit doesn't abort with code 1 / 2\n5. the DKG output usually lands in `awaiting_key_holder_signature` state - **continue to the accept-share step below**\n\n## how to complete the zero-trust accept-share step\n\n1. monitor state via `dWalletState` (or `refreshDWalletState` to force a re-fetch)\n2. once the dWallet sits in `awaiting_key_holder_signature`, run `completeDWalletZeroTrust` with the curve (and optional `dwalletId` if you have multiple in this state)\n3. the wallet calls `acceptEncryptedUserShare` on-chain with the encrypted share id from the DKG event - the dWallet transitions to `Active`\n\n## how to set the dWallet active for its curve\n\n1. once the dWallet is `Active`, call `setActiveDwallet` with the `dwalletId` to wire it up as the active signer for its curve\n2. dapp connections + sending operations now use this dWallet\n\n## notes\n\n- `requestDWalletDKG` returns a tuple - background reads `dkgResult[0]` (other commands like `requestSign` and `requestReEncryptUserShareFor` return no value, and `requestGlobalPresign` returns a drop-able value). this matters only if you write your own PTBs\n- ika dynamic pricing means hardcoded fees abort - always go through `getRequiredCoinAmounts`\n- on Solana base, all signatures from a dWallet come from a single mock signer until ika ships production MPC - never use Solana base for real value\n- for a Sui-base dWallet to sign on Solana too, you need a Solana-base sibling vault. dWallets do not cross base chains; the same mnemonic on the opposite base produces a different identity\n- to recover dWallets that already exist for your mnemonic / key, skip DKG and run `discoverDWallets` (see [manage-dwallets.md](/library/user/manage-dwallets))\n",Fv=`# how to create a chromatika vault

spin up a brand-new install with a fresh BIP39 mnemonic and a fresh dWallet Vault (the owner keyring for an ika base chain).

## prerequisites

- no chromatika vault already exists on this install (or you've wiped extension storage - chromatika is pre-release, no migrations)
- a password at least 8 characters
- decide your ika base chain
  - **Sui base** (default): no extra flag
  - **Solana base** (pre-alpha, dev only): the build needs \`VITE_SOLANA_IKA_BASE=true\`. signatures come from a single mock signer, not real MPC - never trust this for real value

## options at a glance

- **mnemonic word count**: 12 (default, BIP39 standard) or 24
- **mnemonic source**: auto-generated in the background, or provide your own
- **base chain**: sui or solana (pre-alpha)
- **label**: optional human-readable name for this dWallet Vault (rename later anytime)

## how to create a vault with an auto-generated mnemonic

1. request a setup mnemonic from the background (\`generateSetupMnemonic\`) and pick word count (12 or 24)
2. record the words offline. this is the only backup of your dWallet Vault root key - chromatika cannot recover it for you
3. submit \`createVault\` with: password (8+ chars), the mnemonic, base chain, optional label
4. the background derives the keyring (BIP39/44 + SLIP10 ed25519), encrypts the vault blob with Argon2id (RFC 9106 §4 second option: t=3, m=64 MiB, p=4) + AES-256-GCM, and writes it to \`chromatika_vault_v3\` in \`chrome.storage.local\`
5. session unlocks immediately - no need to re-enter the password

## how to create a vault with mnemonic you already have

1. supply your existing mnemonic words instead of asking for generation
2. submit \`createVault\` with: password, your mnemonic, base chain, optional label
3. same encryption + persistence path

## how to create your first dWallet right after vault setup

1. pick a curve: **SECP256K1** for EVM and BTC addresses, **ED25519** for Sui, Solana, and Aptos addresses
2. fund the dWallet Vault with the IKA + SUI (or solana) the DKG flow needs - call \`getRequiredCoinAmounts\` for the current minimum with a 10% buffer
3. run \`createDWallet\` for the chosen curve
4. complete the zero-trust accept-share step (\`completeDWalletZeroTrust\`) once DKG returns \`awaiting_key_holder_signature\`

## notes

- a fresh mnemonic + create flow is the only path that gets you a sovereign new identity. importing existing seeds, hardware vaults, passkey vaults, etc. each have their own guide
- the password never sits in RAM as a string - the unlock cache stores derived AES key bytes (b64) in \`chrome.storage.session\` only, never plaintext
- session auto-locks after the configured inactivity window (1-1440 minutes, default 30); see [unlock-and-lock.md](/library/user/unlock-and-lock)
- changing word count or base chain after creation is not a thing - it would be a new vault. use \`addVault\` (see [manage-vaults.md](/library/user/manage-vaults)) to keep both
- you don't need a separate "plain HD" identity for dapps. all dapp + on-chain activity goes through dWallets anchored to your dWallet Vault
`,Uv=`# how to set the dapp consent mode

chromatika has a **consent mode** that controls how strict the wallet is about non-essential dapp prompts. today this affects the bulk-signing primitives like Solana's \`signAllTransactions\`.

## prerequisites

- a Chromatika vault is unlocked

## options at a glance

- **compat** (default): relaxed mode. bulk-sign primitives go through with one prompt, matching the behavior most non-EVM dapps expect.
- **strict**: each item in a bulk operation prompts independently (where the underlying primitive supports per-item consent)

## how to view the current consent mode

1. call \`getDappConsentMode\`
2. response is \`'compat'\` or \`'strict'\`

## how to switch consent mode

1. submit \`setDappConsentMode\` with \`mode: 'compat' | 'strict'\`
2. takes effect immediately for the next dapp request
3. existing connections keep their permission entries; only the prompting behavior changes

## notes

- compat mode does not auto-approve connections - the dapp connect prompt always runs (see [connect-dapp.md](/library/user/connect-dapp))
- non-EVM connect uses strict consent gates that mirror EVM patterns regardless of this mode (per architecture-final.html); the mode mainly tunes bulk-sign UX
- if a dapp expects a specific behavior and isn't getting it, check the consent mode here before assuming the dapp is broken
`,zv="# how to use encrypted dWallet labels\n\non Solana ika base, you can attach an **encrypted on-chain label** to a dWallet using the Encrypt.xyz pre-alpha primitive. the label ciphertext lives on Solana; reveal goes through a signed `ReadCiphertext` call. this is a **lab-grade pre-alpha** feature - the Encrypt program disclaimer says ciphertexts can be plaintext on-chain in pre-alpha; treat it as exploratory.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active dWallet Vault is on **Solana base** (the procedures assert this and throw on Sui base)\n- the build has the encrypt-pre-alpha client wired\n- the active dWallet has the curve you want to label (SECP256K1 or ED25519)\n- session is enabled for label work (see `getDwalletLabelStatus`)\n\n## options at a glance\n\n- **label**: 1 to 64 utf-8 bytes (4 × EUint128 chunks of 16 bytes each)\n- **per curve**: SECP256K1 dWallet labels and ED25519 dWallet labels are independent\n- **on-chain status**: polled every 4 seconds; values include `verified` (✓ ciphertext present), `encrypting` (in flight), `missing` (devnet wipe or never written)\n\n## how to check label session status\n\n1. call `getDwalletLabelStatus` with `curve`\n2. response includes `enabledForSession` flag - tells you whether the wallet has the encryption material loaded for this session\n3. solana-base only; throws on Sui\n\n## how to query the on-chain label status for a dWallet\n\n1. call `getDwalletLabelOnChainStatus` with `curve`\n2. response is the status byte from the on-chain account: verified / encrypting / missing\n3. drives the surface pill state. `missing` after a devnet wipe means the on-chain ciphertext is gone (rebuild needed)\n\n## how to encrypt a label\n\n1. submit `encryptDwalletLabel` with: `curve`, `label` (1-64 chars), optional `networkEncryptionPublicKeyHex` (override; defaults to current network key)\n2. background packs the label into 4 × EUint128 chunks (16 bytes each), runs `mockEncryptScalarBytesFromBytes` to produce the **canonical 17-byte format** `[fhe_type(1) || value_le(16)]`, batches them in one `CreateInput` round-trip, writes the ciphertext on-chain\n3. on-chain status flips to `encrypting` then `verified` once the program confirms\n\n## how to reveal a stored label\n\n1. submit `revealDwalletLabel` with `curve`\n2. background loops over the chunk identifiers, runs signed `ReadCiphertext` for each (signed via the dWallet's ed25519 path - same as `signMessageSol`), concatenates the plaintext, returns the label\n\n## how to clear a label\n\n1. submit `clearDwalletLabel` with `curve`\n2. on-chain ciphertext is cleared; status returns to `missing`\n\n## notes\n\n- **17-byte canonical format**: `EncryptedInput.ciphertext_bytes` MUST be `[fhe_type(1) || value_le(16)]`. the published `@encrypt.xyz/pre-alpha-solana-client@0.1.0` `encryptValue` is still the pre-fix 16-byte form (known to misread multi-byte scalars - e.g. EUint64 returns `value >> 8`). chromatika hand-rolls via `mockEncryptScalarBytes` / `mockEncryptScalarBytesFromBytes` until upstream republishes\n- pre-alpha disclaimer: ciphertexts may be plaintext on-chain in this phase. don't put anything secret in a label\n- devnet wipe: solana ika devnet gets wiped periodically; on next reveal you'll see `missing` and need to clear + re-encrypt manually. automated rebuild on first reveal failure is tracked future\n- this is a **dWallet** label, not a vault label - distinct from the vault label set via `renameVault`\n",Ov="# how to use the encryption lab\n\na developer / lab surface for creating Encrypt.xyz pre-alpha encrypted inputs and reading ciphertexts. only works on **Solana ika base** (assertions throw on Sui). primarily for SDK exploration; real product flows for dWallet labels live in [encrypted-dwallet-labels.md](/library/user/encrypted-dwallet-labels).\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active dWallet Vault is on **Solana base** (the procedures assert this and throw on Sui base)\n- the build has the encrypt-pre-alpha client wired\n\n## options at a glance\n\n- **encrypted input creation**: single (`encryptLabCreateInput`) or batched up to 16 (`encryptLabCreateInputBatch`)\n- **ciphertext reads**: by ciphertext identifier hex, with optional epoch\n- **deposit / phase notes**: stub references for SPL Enc deposit and the PC phases (3 + 4); these are documentation pointers, not wired flows\n\n## how to create an encrypted input\n\n1. submit `encryptLabCreateInput` with `plainU64` (the value to encrypt) and optional `networkEncryptionPublicKeyHex` (override the active network key)\n2. background packs the value as `[fhe_type(1) || value_le(16)]` (17 bytes), submits `CreateInput`, returns the ciphertext identifier\n3. for several values at once: `encryptLabCreateInputBatch` with `plainU64s` (1-16 values) and the same optional key\n4. all encryption uses the canonical 17-byte format via the hand-rolled `mockEncryptScalarBytesFromBytes` (the published client's `encryptValue` is still 16-byte until upstream republishes)\n\n## how to read a ciphertext\n\n1. submit `encryptLabReadCiphertext` with `ciphertextIdentifierHex` and optional `epochDecimal`\n2. background runs signed `ReadCiphertext` against the active dWallet, returns the plaintext value\n3. used by lab UI / debugging to verify round-trips\n\n## how to view roadmap stub references\n\n- `encryptLabDepositHint` returns SPL Enc deposit implementation notes (read-only; no wired flow today)\n- `encryptSplEncDepositPath` returns the same as a separate label\n- `encryptPcTokenPhase3` returns Phase 3 (PC-Token) reference\n- `encryptPcSwapPhase4` returns Phase 4 (PC-Swap) reference\n\nthese are documentation surfaces only - tRPC returns `not_wired` for actual deposit / swap calls until the on-chain programs / SDK alignment lands\n\n## notes\n\n- **lab surface only** - production flows for encrypted dWallet labels are in [encrypted-dwallet-labels.md](/library/user/encrypted-dwallet-labels). don't build product UI on top of these procedures\n- 17-byte format fix landed upstream in `encrypt-pre-alpha` `303439d` (2026-04-26). the published `@encrypt.xyz/pre-alpha-solana-client@0.1.0` is still pre-fix; chromatika hand-rolls until republish\n- pre-alpha disclaimer applies: ciphertexts may not be encrypted on-chain in this phase. treat all values as exploratory\n- the gRPC fee-payer needs SOL for `approve_message` calls during `CreateInput` / `ReadCiphertext` (see [ika-fee-management.md](/library/user/ika-fee-management))\n",Hv=`# how to set up a hardware-only vault

create a chromatika vault whose **primary** signing identity is a hardware wallet, with no software-held mnemonic for the dWallet Vault. today the supported primary path is **Solana Mobile Wallet Adapter (MWA)** with a Seeker / phone wallet. Ledger and Trezor remain hardware accounts inside an existing software vault rather than primary vaults (per status doc).

chromatika **never** asks for a private key, seed phrase, or recovery export from a hardware device. anything that resembles "paste your hardware seed here" is wrong by design.

## prerequisites

- you've already paired the hardware account in chromatika and it shows up in \`getHardwareAccounts\`
  - for MWA local (Android Chrome same-device): see [seeker-local.md](/library/user/seeker-local)
  - for MWA remote (desktop ↔ phone QR pair): see [seeker-remote.md](/library/user/seeker-remote)
- the hardware-derived ika user-share signature is available - chromatika asks the device to sign \`IKA_USK_DERIVATION_MESSAGE\` (constant \`'ika.chromatika.user-share-encryption-key.v1'\`); \`keccak256(signature)\` is the deterministic 32-byte seed for \`UserShareEncryptionKeys\`
- a password 8+ chars (the password protects local artifacts like the in-extension fee-payer keypair, even though the dWallet Vault root is hardware-derived)
- base chain: Solana (the hardware-only path is solana-base today). Sui-base hardware-only vault is deferred

## options at a glance

- **vendor / transport**: MWA local (Android only), MWA remote (any desktop pairing with a Seeker / Phantom / Solflare phone), WalletConnect (for the x402 path)
- **label**: human-readable vault name

## how to create a hardware-primary vault (first vault)

1. pair the hardware account first (see [seeker-local.md](/library/user/seeker-local) or [seeker-remote.md](/library/user/seeker-remote))
2. ask the device to sign \`IKA_USK_DERIVATION_MESSAGE\` once - record the resulting \`ikaUskSignatureB64\`
3. submit \`createVaultHardware\` with: password, \`hardwareAccountId\`, \`ikaUskSignatureB64\`, \`baseChain: 'solana'\`, label, MWA / WalletConnect transport params
4. background derives the ika seed via \`keccak256(signature)\`, generates an in-extension Solana keypair for ika gRPC \`approve_message\` fees (\`ikaGrpcFeePayerSolSecretKeyB64\` on the vault record), encrypts the vault, persists
5. fund the in-extension fee-payer with a small amount of devnet SOL (~0.1) so ika operations have gas

## how to add a hardware-primary vault as a sibling

1. unlock the wallet (or include password)
2. follow the same pairing + signing flow as above, then submit \`addVaultHardware\`

## how to recover a hardware vault on a new install

1. on the new install, pair the **same** hardware account through the matching transport
2. ask the device to sign the same \`IKA_USK_DERIVATION_MESSAGE\` - because Ed25519 is deterministic per RFC 8032, the same key produces the same signature on any device, so \`keccak256(signature)\` is the same 32-byte seed
3. recreate the vault via \`createVaultHardware\` (you provide a new local password; the ika identity is recovered from the device, not the password)
4. discover dWallets on-chain (\`discoverDWallets\`) to repopulate the dWallet list

## how to drain the in-extension fee-payer back to your hardware account

1. when wrapping up or rotating, call \`drainIkaFeePayerToSeeker\` to send the residual SOL back to your phone wallet (default = full balance minus rent + a fee buffer)
2. for an abandoned \`seeker_direct\` vault, use \`drainAbandonedFeePayer\` to drain the persisted keypair without prompting the phone

## notes

- the in-extension fee-payer keypair only signs \`approve_message\` gRPC fees for ika pre-alpha. **every chain transaction** (sends, dapp signing, dWallet authorize) goes back to the hardware device. there is no software signing of "your money"
- a deprecated field \`ikaEncryptionOnlySolSecretKeyB64\` is read as a fallback for old dev installs but receives no new writes; those vaults are not portable and should be re-onboarded
- Ledger and Trezor are hardware **accounts** today (per \`getHardwareAccounts\` / \`addHardwareAccount\`). they sign tx and messages but are not a hardware-primary vault root - the dWallet Vault is still mnemonic-backed for those flows
`,qv='# how to manage ika fee-payer (Solana base)\n\non Solana ika base, the wallet auto-generates an in-extension Solana keypair to pay ika gRPC `approve_message` fees - hardware-backed Solana wallets (Seeker / Seed Vault) never expose secret bytes, so chromatika needs its own gas-only key. this guide covers querying status, configuring fee mode, manually topping up, and draining residual SOL back to your phone wallet.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active dWallet Vault is on **Solana base** (this guide does not apply to Sui-base vaults)\n- a Seeker / phone wallet is paired (for top-up and Seeker-direct mode)\n- you have a small amount of devnet SOL to fund the fee-payer (~0.1 SOL is plenty)\n\n## options at a glance\n\n- **fee mode**:\n  - `in_extension` (default): the in-extension fee-payer signs `approve_message` directly. fast, but the keypair must be funded\n  - `seeker_direct`: ika ops use a per-call signature from the Seeker; slower but no in-extension keypair to manage. the deprecated `ikaEncryptionOnlySolSecretKeyB64` is still readable as a fallback for old dev installs\n- **auto-refill** (for `in_extension`): when fee-payer balance dips below `thresholdLamports`, top up by `refillLamports` via Seeker hardware sign\n- **manual top-up**: trigger a one-shot top-up on demand\n- **drain**: send fee-payer balance back to the Seeker (default = full balance minus rent + a fee buffer)\n- **drain abandoned**: drain the residual keypair on a `seeker_direct` vault that was switched away from `in_extension`\n\n## how to read fee-payer status\n\n1. call `ikaFeePayerStatus` with `vaultId`\n2. response includes the fee-payer address, balance (lamports), thresholds, mode, and whether a keypair exists for the vault\n3. quick balance check for the active vault: `activeIkaFeePayerBalance` (returns balance or null if no keypair)\n\n## how to read or update fee settings\n\n- read: `getIkaFeeSettings` with `vaultId` returns `{ mode, autoRefill, refillLamports, thresholdLamports }`\n- update: `setIkaFeeSettings` with `vaultId` and any subset of: `mode`, `autoRefill` (boolean), `refillLamports`, `thresholdLamports`. partial update; non-supplied fields stay\n- defaults reference: `ikaFeeDefaults` returns the recommended tunables (no params)\n\n## how to manually top up the fee-payer\n\n1. submit `topUpIkaFeePayer` with `vaultId` and `lamports`\n2. wallet enqueues a Seeker hardware-sign request to send SOL from your phone to the fee-payer address\n3. you approve on the phone; signature returns; transaction broadcasts\n4. balance reflects after Solana confirms\n\n## how to drain the fee-payer back to Seeker\n\n1. submit `drainIkaFeePayerToSeeker` with `vaultId` and optional `lamports` (defaults to "full balance minus rent + fee buffer")\n2. wallet builds a transfer from fee-payer to the Seeker address, signs with the in-extension keypair, broadcasts\n3. no phone prompt needed - the in-extension keypair authorizes the transfer\n\n## how to drain a residual keypair from a seeker_direct vault\n\n1. submit `drainAbandonedFeePayer` with `vaultId`\n2. used when you\'ve switched the vault to `seeker_direct` mode but a residual `in_extension` keypair still has SOL\n3. drains via the persisted keypair without prompting the phone\n\n## notes\n\n- the **Seed Vault never reveals secret bytes**. that\'s why chromatika auto-generates an in-extension Solana keypair (`ikaGrpcFeePayerSolSecretKeyB64`) for ika gRPC fees. **all chain transactions** (sends, dapp signing, dWallet authorize) still go through the Seeker. there is no software signing of "your money"\n- Seeker pairing also derives the ika `UserShareEncryptionKeys` root seed from a signed `IKA_USK_DERIVATION_MESSAGE` (see [seeker-remote.md](/library/user/seeker-remote)). that seed and the fee-payer keypair are independent - one signs ika protocol, one pays gRPC fees\n- the deprecated `ikaEncryptionOnlySolSecretKeyB64` field reads as a fallback for old dev installs; new writes go to `ikaGrpcFeePayerSolSecretKeyB64`. those old vaults are not portable - re-onboard to migrate\n- runbook: `wallet-extension/docs/SEEKER_REMOTE_PAIRING.md`\n',Vv=`# how to stake IKA

stake IKA tokens with an Ika network validator, view your staked positions, and withdraw stake. validators are listed on-chain; stake is held in a \`StakedIka\` object that you withdraw from when ready.

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet Vault is on Sui base
- the vault has IKA + SUI to cover stake amount + gas (use \`getRequiredCoinAmounts\` for current minimums)
- a dWallet exists on the active vault for signing the stake / withdraw PTBs

## options at a glance

- **stake amount**: in IKA base units (the wallet handles unit conversion)
- **validator**: chosen from \`ikaStakingValidators\`
- **withdraw**: per \`StakedIka\` object id

## how to list active Ika validators

1. call \`ikaStakingValidators\`
2. response is the validator list with ids, commissions, names where available

## how to list your current staked positions

1. call \`ikaStakingPositions\`
2. response is your \`StakedIka\` positions: validator id, staked amount, epoch start, etc.

## how to stake IKA with a validator

1. choose a validator from the list
2. submit \`ikaStake\` with \`validatorId\` and \`amountBaseUnits\`
3. background builds a staking PTB on \`IkaTransaction\`, requires SECP256K1 dWallet sign (signing-progress popup may appear via \`signingProgress\`), broadcasts on Sui
4. once landed, the new position appears in \`ikaStakingPositions\`

## how to withdraw stake

1. submit \`ikaWithdrawStake\` with the \`stakedIkaObjectId\` of the position you want to unwind
2. background builds the unstake PTB, signs, broadcasts. claims rewards if the validator has paid out for completed epochs

## notes

- staking ops are dWallet-signed PTBs - they need active SECP256K1 (since dWallets on Sui base sign for IKA-related ops via SECP256K1 ECDSA) and presigns from the matching pool
- hardware vaults (Ledger / Trezor / Seeker) cannot stake from the wallet UI today - this is a dWallet-only flow per architecture
- on Solana base, staking is not implemented (the Solana ika program doesn't expose the same staking surface yet) - stake from a Sui-base vault
- watch out for staking minimums and unstake epoch boundaries enforced on-chain - the wallet surfaces errors verbatim
`,Gv=`# how to import a chromatika vault from an existing mnemonic

bring an existing BIP39 mnemonic into chromatika as a dWallet Vault. used when restoring onto a new install or migrating from another wallet with standard BIP39 derivation.

## prerequisites

- no chromatika vault exists on this install yet (or you've cleared storage)
- the BIP39 mnemonic words you want to import (12 or 24)
- a new local password 8+ chars (does not have to match any prior password)
- decide your ika base chain (Sui default; Solana needs \`VITE_SOLANA_IKA_BASE=true\`)

## options at a glance

- **base chain**: sui or solana (pre-alpha)
- **label**: human-readable vault name (set now, rename later)

## how to import the vault

1. submit \`importVault\` with: the mnemonic, password, base chain, optional label
2. background derives keyring, encrypts vault with Argon2id + AES-GCM, persists to \`chromatika_vault_v3\`
3. session unlocks immediately

## how to recover dWallets that already exist on-chain for this mnemonic

1. after import, trigger \`discoverDWallets\` per curve (SECP256K1 and ED25519)
2. dWallets the chain knows about for your derived addresses come back as active cards
3. set the dWallet you want to use as active per curve via \`setActiveDwallet\`

## notes

- dWallets are not in the mnemonic itself - they're MPC objects on Sui or Solana. importing the mnemonic restores your owner identity, on-chain discovery does the rest
- if no dWallet ever existed for this mnemonic, just create one (see [create-dwallet.md](/library/user/create-dwallet))
- the ika \`UserShareEncryptionKeys\` root seed is derived **per base chain**:
  - Sui base → fee-payer keypair via \`ikaRootSeedFromFeeKeypair\`
  - Solana base → Solana fee-payer keypair via \`ikaRootSeedFromSolanaKeypair\`
  - same mnemonic on a different base chain produces a **different** ika identity
- to reuse the same mnemonic on the opposite base chain as a sibling vault, use the cross-chain mnemonic reuse path (see [manage-vaults.md](/library/user/manage-vaults))
`,$v=`# how to import a chromatika vault from a private key

import a single raw private key (Sui or Solana flavor) as a dWallet Vault. handy for one-off accounts that aren't HD-derivable, or for Solana-only / Seeker-style onboarding where there is no Sui privkey to provide.

## prerequisites

- a password 8+ chars (or the wallet is already unlocked when adding as a sibling vault)
- the raw key in canonical form:
  - **Sui**: \`suiprivkey…\` bech32 string (the export format Sui Wallet, sui-keytool, etc. give you)
  - **Solana**: 64-byte secret key, base64-encoded (the same shape Phantom and \`solana-keygen\` JSON export use, just b64 instead of JSON array)
- decide your ika base chain - the key flavor must match. you cannot import a Sui privkey under Solana base or vice versa
- Solana base requires \`VITE_SOLANA_IKA_BASE=true\` in the build

## options at a glance

- **key flavor**: sui bech32 OR solana 64-byte secret (b64)
- **base chain**: must match key flavor
- **label**: human-readable vault name

## how to import as your first vault

1. submit \`importVaultFromPrivateKey\` with: password, base chain, the matching key field (\`suiPrivateKeyBech32\` for Sui, \`solanaSecretKeyB64\` for Solana), optional label
2. background reconstructs the keypair, derives ika encryption keys per base chain, encrypts the vault, writes \`chromatika_vault_v3\`
3. session unlocks immediately

## how to import as a sibling vault (existing chromatika install)

1. unlock the existing vault, or include the password in the call
2. submit \`addVaultImportedFromPrivateKey\` with the same fields
3. the new vault joins the list; switch to it via \`switchVault\`

## how to discover dWallets that already exist for this key

1. after import, trigger \`discoverDWallets\` per curve
2. owned dWallets on the chain return as cards
3. set active per curve via \`setActiveDwallet\`

## notes

- importing a raw key gives you a single account, not an HD tree. there are no derivation children
- on Solana base, the imported keypair seeds the ika \`UserShareEncryptionKeys\` root via \`keccak256(secretKey64 || encryption_key_index_le)\`. on Sui base, the equivalent for the bech32 key
- this is the same path Seeker / Solana-only onboarding uses internally, just driven by you instead of by Mobile Wallet Adapter pairing
- never paste a private key from a hardware wallet into this flow - hardware devices are designed to never reveal that material. use the hardware vault flow instead (see [hardware-vault.md](/library/user/hardware-vault))
`,Qv=`# how to use a Lazor-backed solana vault

create or unlock a chromatika vault using Lazor's smart-wallet portal - a Solana-anchored passkey flow with optional BIP39 recovery words.

## prerequisites

- a Lazor smart wallet provisioned through the Lazor portal
- the device + browser supports WebAuthn (passkey)
- decide network (mainnet or devnet) - must match the network where the Lazor smart wallet lives
- for vault creation: no existing vault (or you're adding as a sibling)

## options at a glance

- **network**: mainnet or devnet
- **recovery words**: optional BIP39 backup branch registered at create time
- **label**: human-readable vault name

## how to create a Lazor-backed vault (first vault)

1. complete Lazor portal flow to get: smart wallet pubkey, credential id, passkey params, Lazor program id, network choice
2. submit \`createVaultLazor\` with: password, \`lazorSmartWalletPubkey\`, \`credentialId\`, the passkey params, \`lazorProgramId\`, network, optional \`recoveryWords\`, optional label
3. background registers the envelope, encrypts the vault, writes \`chromatika_vault_v3\`
4. session unlocks immediately

## how to add a Lazor envelope to an existing vault

1. unlock the wallet (or include the password)
2. submit \`addVaultLazor\` with the same fields
3. envelope joins the vault list

## how to unlock with Lazor

1. read \`listVaultEnvelopes\` to find the Lazor envelope
2. run the Lazor passkey assertion ceremony (the portal handles it)
3. submit \`unlockVaultWalletSignature\` with envelope id, signature (b64), \`autoLockMinutes\`

## how to recover with the BIP39 words

1. only works if \`recoveryWords\` were set at create time
2. submit \`unlockVaultRecoveryWords\` with the envelope id, words, \`autoLockMinutes\`

## notes

- Lazor is Solana-only - the smart wallet lives on Solana, the program id determines which deployment you're talking to
- choosing devnet means the smart wallet, recovery, and any signed transactions all anchor to Solana devnet - good for testing, do not put real funds on devnet
- if you skip \`recoveryWords\` at create time, losing the passkey means losing access to that envelope. you can have other unlock paths on the vault as backup
`,Jv="# how to use a Ledger hardware wallet\n\npair a Ledger device over WebHID and use it as a hardware account inside chromatika. Ledger covers EVM (`personal_sign`, transactions, EIP-712 typed data), Sui transactions, Solana (transactions and off-chain messages), and Bitcoin PSBT signing. **chromatika never asks for your seed phrase or private key from the device** - all signing stays on-device.\n\n## prerequisites\n\n- Ledger device (Nano S Plus, Nano X, etc.) running compatible firmware\n- the chain-specific app installed on the device (Ethereum app, Sui app, Solana app, Bitcoin app)\n- a USB connection or compatible WebHID-supported transport\n- the pairing must run from a popup or side-panel context (WebHID requires a user gesture and is **not** available in the service worker)\n- your Chromatika vault is unlocked\n\n## options at a glance\n\n- **chains supported**: EVM, Sui, Solana, Bitcoin (P2WPKH bech32 + legacy paths)\n- **derivation path**: standard BIP44 paths per chain (e.g. `m/44'/60'/0'/0/0` for EVM)\n- **add as account**, not as primary vault root - the dWallet Vault root stays mnemonic-backed; Ledger is a hardware **account** sibling\n\n## how to pair a Ledger account\n\n1. unlock the Ledger and open the chain app you want to use\n2. trigger the WebHID pairing flow (browser asks you to grant access to the device)\n3. submit `addHardwareAccount` with: `vendor: 'ledger'`, `chain` (`evm` / `sui` / `solana` / `btc`), `derivationPath`, the device's `address`, and the `ed25519PublicKeyB64` if applicable (Sui / Solana)\n4. account shows up in `getHardwareAccounts`\n\n## how to sign with a Ledger\n\nsigning is initiated by chromatika operations (sends, dapp tx approval, message signing) when the active account for the relevant chain is a Ledger hardware account. the flow:\n\n1. wallet enqueues the sign request and opens a hardware-sign popup at `index.html?hwsign=ID`\n2. popup calls `getHardwareSignRequest` with the id to read the message / tx\n3. popup runs `TransportWebHID.create()` and the matching `hw-app-*` library to ask the device to sign\n4. you confirm on the device\n5. popup calls `resolveHardwareSign` with the returned signature - chromatika finishes the broadcast / dapp response\n6. cancel via `rejectHardwareSign` with a reason if you back out\n\n## how to remove a Ledger account\n\n1. call `removeHardwareAccount` with the account `id` from `getHardwareAccounts`\n2. the account is dropped from the local list; on-chain state is untouched\n\n## chain-specific notes\n\n- **EVM** (`personal_sign`, tx, EIP-712 typed data): standard `hw-app-eth` paths\n- **Sui** (`suiTx`): app + firmware floors documented in [LEDGER_SUI_LIMITS.md](/library/user/ledger_sui_limits) - if Ledger throws an unsupported-tx error, check version\n- **Solana** (tx + off-chain message): standard `hw-app-solana`\n- **Bitcoin PSBT**: `hw-app-btc@10.x` `signPsbtBuffer` for bech32 + legacy paths\n\n## notes\n\n- WebHID lives in popup / side-panel only. the service worker cannot drive it - that's why hardware signing always opens a separate popup window\n- Ledger gives you signatures over data, never over secrets. if any flow asks you to type a Ledger seed, it's wrong - reject it\n- the Ledger app on the device must be the right one for the chain you're signing for. switching apps mid-flow may cancel the pending request\n- two `@ledgerhq/live-network` versions are pinned + patched in chromatika today (`2.0.19` + `2.4.3`); reconciling these is a future cleanup task and should not affect users\n",Yv=`# how to manage dapp permissions

list the dapps that have a connection to chromatika, see what dWallets they have access to, switch which dWallet they see, or revoke their connection entirely.

## prerequisites

- a Chromatika vault is unlocked
- one or more dapps have completed a connection (see [connect-dapp.md](/library/user/connect-dapp))

## options at a glance

- **list permissions**: see all connected origins
- **revoke**: drop a specific origin's permission
- **switch active address per origin**: change which dWallet the dapp can see (handled implicitly via \`setActiveDwallet\`; the dapp gets the new address via account-changed events)
- **debug**: view the recent dapp request log for diagnostics

## how to list connected dapps

1. call \`getDappPermissions\`
2. response is a list of \`{ origin, selectedAddress, selectedDwalletId, scopes, ... }\` per connection, scoped per curve

## how to revoke a dapp's permission

1. submit \`revokeDappPermission\` with \`origin\`
2. the bridge broadcasts disconnect events to the page, drops the entry from \`chromatika_dapp_permissions_v1\`
3. any pending tx / sign requests from that origin are rejected immediately

## how to switch which dWallet a dapp sees

1. setting the active dWallet for a curve via \`setActiveDwallet\` re-emits the account-changed event to all connected origins
2. for finer-grained per-origin selection, revoke + reconnect (since approval at connect time is the canonical per-origin choice today)

## how to debug recent dapp requests

1. call \`dappBridgeDebug\`
2. response is the last 50 telemetry entries (request method, origin, timing, outcome)
3. useful when a dapp's flow is silently failing

## how to query the active tab's dapp connection state

1. call \`vaultHeaderDappContext\` to get the active browser tab + connection status (one of: connected, not connected, no origin)
2. used by surfaces that show "this dapp is connected" hints

## notes

- the bridge tracks origin + \`event.source\` validation in the content script - a dapp cannot fake messages for another origin
- if the wallet is locked, all dapp requests reject with a "wallet locked" error until unlock
- the architecture allows a future per-origin scope flag (e.g. \`accounts only\` vs \`accounts + sendTx\`); today scopes are inferred from the standards' default surfaces
`,Xv="# how to manage dWallets\n\noperations on dWallets that already exist (created or discovered) - listing, switching active, naming, ordering, looking up addresses, registering the encryption key for share transfers.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- at least one dWallet exists or can be discovered for the active vault\n\n## options at a glance\n\n- **discover**: scan the chain for dWallets owned by your derived addresses\n- **set active**: per curve, choose which dWallet drives signing\n- **list owned caps**: enumerate all dWallet capabilities for the vault\n- **address book / chain addresses**: derive every supported chain address for one dWallet (or look up active addresses for both curves)\n- **display name**: free-form per-dWallet label\n- **card order**: explicit ordering for the dWallet list\n- **register encryption key**: required before you can receive a transferred dWallet\n\n## how to discover dWallets that already exist on-chain\n\n1. call `discoverDWallets` per curve (`SECP256K1` and `ED25519`)\n2. the wallet walks the chain (Sui via GraphQL `client.core.*`, Solana via pre-alpha gRPC) for dWallets your derived addresses own and adds them to the local list\n3. some dWallets may be returned in `awaiting_key_holder_signature` state - run `completeDWalletZeroTrust` for those\n\n## how to list active dWallet capabilities\n\n1. call `listOwnedDWalletCaps` (returns the dWallet card list with curve, status, addresses, and metadata)\n2. use this for any UI / agent that needs the full picture\n\n## how to set the active dWallet per curve\n\n1. call `setActiveDwallet` with `dwalletId`\n2. the curve is inferred from the dWallet record. only one dWallet is active per curve at a time\n3. dapp bridge re-emits account-changed events to connected origins\n\n## how to look up addresses\n\n- for both curves on the active vault: `dwalletAddressBook` returns SECP256K1 (EVM + BTC) and ED25519 (Sui + Solana + Aptos) addresses\n- for any specific dWallet: `getDwalletChainAddresses` with `dwalletId` returns BTC (P2WPKH + P2TR), EVM, Solana, Sui, and Aptos addresses\n- for individual chains on the active vault: `getEvmAddress`, `getSolanaAddress`, `getAptosAddress`, `getBtcAddresses` (with `network: 'mainnet' | 'testnet'`)\n\n## how to set a display name\n\n1. call `setDwalletDisplayName` with `dwalletId` and `name` (max 64 chars)\n2. read all current names with `getDwalletDisplayNames`\n3. names are scoped to the active vault and persist in vault storage\n\n## how to reorder dWallet cards\n\n1. call `getDwalletCardOrder` to read the current order\n2. call `setDwalletCardOrder` with the desired ordered list of dWallet ids\n3. missing ids are auto-filled, unmentioned ids sorted to the end\n\n## how to register an encryption key for receiving transfers\n\n1. call `registerEncryptionKey` with the curve - this registers your encryption keypair on-chain so other users can encrypt a share to you when transferring a dWallet\n2. read status with `getSenderEncryptionKeyAddress` (returns the address of your registered encryption key)\n3. without registration, you cannot receive a transferred dWallet (see [transfer-dwallet.md](/library/user/transfer-dwallet))\n\n## how to query dWallet state\n\n- `dWalletState` with curve - returns the current lifecycle state for the active dWallet on that curve\n- `refreshDWalletState` to force a chain re-fetch\n- the most common transient state is `awaiting_key_holder_signature` between DKG and accept-share\n\n## how to sync dWallet meta to chrome.storage\n\n1. call `syncVaultMeta` if you've poked at session-only state and want the persisted overlay (`chromatika_dwallet_meta_v2_<vaultId>`) refreshed\n2. usually not needed - normal flows persist as they go\n\n## notes\n\n- dWallet meta is per vault: `chromatika_dwallet_meta_v2_<vaultId>` in chrome.storage. switching vaults loads a different overlay\n- `DWalletMeta.baseChain` is **required** when initializing new entries; `getIkaAdapter` reads this to dispatch the right adapter (Sui PTB vs Solana gRPC)\n- `SolanaIkaAdapter` still throws on Sui-only reads (`getPresignInParticularState`, `getEncryptedUserSecretKeyShare`, `getSign`, `executeTx`); DKG + sign on Solana base bypass the adapter and go through `SolanaIkaGrpcClient` directly\n- always set the base chain on `DWalletMeta` so adapter dispatch works\n",Zv=`# how to manage active networks

chromatika ships a network registry per chain (EVM, Solana, Sui, Aptos, Bitcoin) plus user-added custom networks. you switch the active network per chain to control where balances, RPC calls, and dapp injection point. some chains (Sui, Solana) have a **two-tier** active network: vault-tier (the dWallet Vault's owner-key environment) and dWallet-tier (which network the active dWallet operates on).

## prerequisites

- a Chromatika vault is unlocked
- the desired network already exists in the registry (built-in) or has been added (see [add-custom-network.md](/library/user/add-custom-network))

## options at a glance

- **EVM**: single active chain by chain id
- **Solana** and **Sui**: vault-tier + dWallet-tier (two separate active networks per chain)
- **Aptos**: single active network
- **Bitcoin**: single active network (mainnet, testnet, signet)
- **ika base mode**: global preference for which chain anchors ika dWallets (Sui or Solana). gated for Solana behind \`VITE_SOLANA_IKA_BASE=true\`

## how to list every network

1. call \`getNetworks\`
2. response merges built-in registry with custom networks per chain, plus the active selection per chain and per tier where applicable

## how to switch active EVM chain

1. call \`setActiveEvm\` with the target \`chainId\`
2. wallet refreshes the EVM provider (\`getRpcProvider\`), re-reads gas / balance
3. dapp bridge emits \`chainChanged\` to connected EVM origins

## how to switch active Sui network (vault tier or dWallet tier)

1. call \`setActiveSuiNetwork\` with \`networkId\` and \`tier: 'vault' | 'dwallet'\`
2. vault tier changes which Sui environment the dWallet Vault's HD fee-payer operates in (mainnet / testnet / devnet)
3. dWallet tier changes the network the active Sui-base dWallet works against - separate from vault tier so you can fund on one and operate on another briefly

## how to switch active Solana network (vault tier or dWallet tier)

1. call \`setActiveSolanaNetwork\` with \`networkId\` and \`tier: 'vault' | 'dwallet'\`
2. solana base ika dWallets need the vault tier and dWallet tier set consistently for ika gRPC + chain ops to align. the wallet warns when they diverge in dev surfaces

## how to switch active Aptos network

1. call \`setActiveAptosNetwork\` with \`networkId\`

## how to switch active Bitcoin network

1. call \`setActiveBitcoinNetwork\` with \`networkId\` (mainnet / testnet / signet for built-ins, or any custom Esplora network you've added)
2. address derivation rebuilds for the new network (testnet vs mainnet bech32 prefix differs)

## how to switch ika base mode

1. call \`getIkaBaseMode\` to see current preference (\`'sui' | 'solana'\`)
2. call \`setIkaBaseMode\` with the new mode
3. **gating**: solana base requires \`VITE_SOLANA_IKA_BASE=true\` in the build. base-mode switching is also constrained by whether you have a vault on the target base chain - the wallet falls back to the nearest available chain if not

## how to check EVM RPC health

1. call \`getEvmRpcHealth\`
2. response is latency + recent success / error counts for the active EVM RPC
3. helpful when sends or balance reads are slow / failing

## notes

- network registry uses a Trust-Wallet-style \`registry.json\` schema for custom networks (per architecture-final.html)
- chromatika prefers \`SuiGraphQLClient\` (\`client.core.*\`) for any Sui call Mysten exposes on GraphQL. JSON-RPC is for legacy gaps only (e.g. activity feed today)
- the global ika base mode (\`chromatika_ika_base_mode_v1\`) is **not** the same as per-dWallet \`baseChain\`, but should stay consistent as the product matures
- two-tier active networks let you e.g. fund a Sui vault on mainnet while pointing the dWallet at testnet to avoid paying real fees in dev. once you understand the model, this is genuinely useful; until then, keep both tiers on the same network
`,ek=`# how to manage multiple chromatika vaults

chromatika supports multiple dWallet Vaults under one app password / unlock. each vault has its own ika base chain, its own keyring, and its own dWallet list. this guide covers everything you do **after** the first vault exists.

## prerequisites

- at least one chromatika vault already exists
- session is unlocked, or you're willing to pass the password through on each call

## options at a glance

- **add sibling vault**: spawn another vault under the same app password
- **switch active**: change which vault drives dapp / signing flows
- **rename**: change the human label
- **remove**: delete a vault (cannot remove the last one)
- **list**: get all vault summaries
- **cross-chain mnemonic reuse**: preview re-using a mnemonic on the opposite ika base chain

## how to list all vaults

1. call \`listVaults\` (returns vault id, label, base chain, primary credential type, dWallet count)
2. read \`activeVaultId\` to know which one is currently driving signing

## how to add a sibling vault

pick the variant that matches how you want the new vault to authenticate:

- mnemonic (HD): \`addVault\` with password (or unlocked session), base chain, \`mnemonic\`, \`wordCount\`, label
- private key: \`addVaultImportedFromPrivateKey\` with the matching key field
- passkey: \`addVaultPasskey\` with the WebAuthn params
- WAAP: \`addVaultWaap\` with auth method + WAAP address / pubkey
- Lazor: \`addVaultLazor\` with smart-wallet pubkey + credentialId
- hardware: \`addVaultHardware\` with paired hardware account id + \`ikaUskSignatureB64\`
- dWallet-anchored: \`addVaultDwalletAnchored\` (vault rooted on an existing on-chain dWallet)

each path mirrors the equivalent \`createVault*\` call; see the respective vault guide for the input shape.

## how to switch active vault

1. call \`switchVault\` with the target \`vaultId\` (and password if locked)
2. session re-encrypts where required; \`activeVaultId\` updates; presign pools and dWallet meta scope to the new vault (\`chromatika_presign_pools_v3_<vaultId>\`, \`chromatika_dwallet_meta_v2_<vaultId>\`)
3. dapp bridge re-emits account-changed events to connected origins

## how to rename a vault

1. call \`renameVault\` with \`vaultId\` and the new \`label\`
2. label is purely cosmetic; keys / addresses don't change

## how to remove a vault

1. call \`removeVault\` with \`vaultId\` (and password if needed)
2. the encrypted record + per-vault overlays are dropped from storage
3. you cannot remove the last vault - chromatika needs at least one to function. add a new one first if you want to discard the current one

## how to preview cross-chain mnemonic reuse

1. call \`previewCrossChainReuseMnemonic\` with the source \`vaultId\` and the new base chain (Sui ↔ Solana)
2. preview shows what the new dWallet Vault would look like on the opposite base chain - new ika user-share root seed, new addresses
3. if you want to commit, follow up with \`addVault\` (mnemonic flow) using the previewed mnemonic and the new base chain

## notes

- presign pools, dWallet meta, and most session-state are **per vault**. switching vaults does not bleed pool entries between vaults
- the password is shared across all vaults in the install - one app password protects the whole \`chromatika_vault_v3\` blob (which contains every vault record)
- mnemonic reuse across base chains is not the same identity - the ika \`UserShareEncryptionKeys\` root seed is derived per base chain, so the dWallet identity differs even though the BIP39 words match
- syncing \`dwalletMeta\` to chrome.storage manually (e.g. after experimentation): \`syncVaultMeta\`
`,nk=`# how to set MediaSafetyMode

\`MediaSafetyMode\` controls how chromatika renders NFT images, Bitcoin Ordinal media, and Sui kiosk item images. it filters at the HTTP request layer, not at the cache layer.

## prerequisites

- a Chromatika vault is unlocked

## options at a glance

- **\`all\`**: render any image URL the indexer returns (no filtering)
- **\`ipfs-arweave\`** (default): only render images whose URL resolves to an IPFS or Arweave gateway. blocks arbitrary HTTP image hosts
- **\`none\`**: do not load images at all - text + metadata only

## how to read the current mode

1. call \`getMediaSafetyMode\`
2. response is one of the three options above

## how to change the mode

1. submit \`setMediaSafetyMode\` with the chosen mode
2. takes effect immediately for new requests; in-flight loads complete with the prior policy

## notes

- enforcement is **in-request only** today. there's no offscreen media cache yet (target only - manifest does not request \`offscreen\` permission until that ships)
- this affects: Sui NFTs (\`getSuiNfts\`), EVM NFTs (\`getEvmNfts\`), Solana NFTs (\`getSolanaNfts\`), Aptos NFTs (\`getAptosNfts\`), Bitcoin Ordinals (\`getBtcOrdinals\`), and Sui kiosk items (\`getKioskData\`)
- if you change mode while browsing NFTs, refresh the relevant list to see the new policy applied
- IPFS / Arweave rendering does not validate content - it validates the URL host. content authenticity is still on the user
`,tk="# how to use a passkey-backed vault\n\ncreate, sign with, and recover a chromatika vault that uses a WebAuthn passkey (with the PRF / HMAC-secret extension) as the unlock credential. the passkey can be the only credential, or coexist with a password.\n\n## prerequisites\n\n- the device + browser supports WebAuthn with the **PRF (HMAC-secret)** extension (recent Chrome / hardware authenticators with the right firmware)\n- a relying-party id (`rpId`) the wallet reaches over - in practice the chromatika extension origin\n- for vault creation: no existing vault on the install (or you're adding a passkey vault as a sibling)\n- for unlock / sign / recover: the matching authenticator is available\n\n## options at a glance\n\n- **passkey-only vault**: no password, ceremony unlocks alone\n- **passkey + password vault**: either credential unlocks; useful for backup access\n- **kind for sign**: `tx`, `personal`, or `raw` (passkey can sign different challenge shapes)\n\n## how to register a new passkey vault (first vault)\n\n1. submit `runPasskeyOnboarding` with: optional password, `rpId`, `rpName`, `userName`, `userDisplayName`, optional label\n2. the orchestrator enqueues a register request; the WebAuthn ceremony runs in a popup context (where `navigator.credentials.create` is allowed)\n3. the popup calls `getPasskeyRegisterRequest` to read the challenge + PRF salt, runs the ceremony, then posts artifacts back via `resolvePasskeyRegister` (credentialId, public key, PRF secret, `rpId`)\n4. background commits the vault encrypted with the PRF-derived key (and the password, if provided)\n\n## how to add a passkey to an existing vault\n\n1. unlock the wallet (or include the password in the call)\n2. submit `runPasskeyAddVault` with the same fields as onboarding\n3. ceremony runs the same register / resolve loop; the new passkey envelope is appended to the vault list\n\n## how to unlock with a passkey\n\n1. read `listVaultEnvelopes` to find the passkey envelope\n2. trigger the assertion ceremony in a popup context (the popup calls `getPasskeySignRequest`, runs `navigator.credentials.get` with PRF, posts back via `resolvePasskeySign`)\n3. submit `unlockVaultPasskey` with the envelope id, PRF secret (b64), and `autoLockMinutes`\n\n## how to sign an arbitrary challenge with a passkey\n\n1. queue the sign request via `enqueuePasskeySign` with: vault id, credentialId, `rpId`, public key, the challenge bytes, `kind` (tx / personal / raw), optional PRF salt\n2. the popup runs WebAuthn assertion, returns serialized signature (b64) and optional PRF secret via `resolvePasskeySign`\n3. cancel via `rejectPasskeySign` with a reason\n\n## how to recover a passkey vault\n\n1. queue recovery via `enqueuePasskeyRecover` with `rpId`, two probe challenges, PRF salt\n2. the popup runs the recover ceremony - it walks the user through reattesting on a backup device or restoring from a synced credential, then posts artifacts via `resolvePasskeyRecover`\n3. cancel via `rejectPasskeyRecover` if the user backs out\n\n## notes\n\n- the PRF / HMAC-secret extension is what makes the passkey usable as an encryption credential - without it, the passkey can authenticate but cannot derive the AES key\n- the PRF secret is treated like the AES key bytes path: held in `chrome.storage.session` only, dropped on lock\n- platform-bound passkeys (e.g. Windows Hello) only work on the device they were registered on. cross-device sync requires a roaming or synced authenticator\n- recovery requires that the original passkey envelope was set up with a recovery branch (e.g. backup BIP39 words via [recovery-words.md](/library/user/recovery-words))\n",rk=`# how to use chromatika's phishing protection

chromatika ships with phishing detection that uses Chrome's declarativeNetRequest (dNR) dynamic rules. flagged domains get redirected to a bundled warning page. the rule list is bundled at install time and refreshed daily from the MetaMask \`eth-phishing-detect\` config.

## prerequisites

- chromatika is installed (no separate vault state needed - phishing rules apply install-wide)
- chrome's dNR engine is operational (it always is, this is not user-tunable)

## options at a glance

- **bundled list** loads on \`onInstalled\` and \`onStartup\`
- **daily alarm** refetches the MetaMask remote \`config.json\` when online
- **rule cap**: 4,900 rules used (under the 5,000 dNR limit) so the wallet has headroom for additions
- **redirect**: flagged domains route to \`phishing-warning.html?blocked=DOMAIN\`
- **manual check**: \`checkPhishing\` returns true / false for a domain on demand

## how to check a domain on demand

1. submit \`checkPhishing\` with \`host\` (e.g. \`example.com\`)
2. response is true (flagged) or false (not flagged)
3. used by [connect-dapp.md](/library/user/connect-dapp) connection flows pre-approval

## how to refresh the phishing list manually

1. there's no exposed manual-refresh tRPC procedure today. the daily alarm handles refreshes
2. uninstall + reinstall would re-pull the bundled list at install time

## how to know the rules are working

- visiting a known flagged domain (e.g. test phishing URLs from the MetaMask list) routes you to the warning page
- the warning page has a \`?blocked=DOMAIN\` query param so you can see which host triggered

## notes

- MV3 cannot use blocking \`webRequest\`, which is why dNR is the chosen mechanism
- daily refetch only runs when online - if the device is offline at the alarm time, the next refetch is whenever it next fires (24h later) or on next chrome startup
- if a domain is mistakenly flagged, the only fix today is filing upstream with \`eth-phishing-detect\`. chromatika does not have a per-user allowlist override (tracked future)
- [connect-dapp.md](/library/user/connect-dapp) runs \`checkPhishing\` before showing the connection approval popup - flagged domains never get a connection prompt
`,ak=`# how to manage the presign pool

ika MPC signing uses **presigns** - signature material precomputed offline so on-line signing is fast. chromatika keeps three pools, refilling them on a schedule. this guide covers querying counts and triggering manual refills.

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet Vault has IKA + SUI (or SOL on Solana base) for presign DKG-style operations
- a dWallet exists for the curve you want to refill

## options at a glance

- **three pools, scoped per active dWallet Vault** (\`chromatika_presign_pools_v3_<vaultId>\`):
  - \`SECP256K1_ECDSA\` - EVM, generic ECDSA
  - \`SECP256K1_TAPROOT\` - BTC P2TR
  - \`ED25519_EDDSA\` - Sui, Solana, Aptos
- **automatic refill alarm**: every 5 minutes (\`chromatika-presign-refill\`); low-water = 2, refill count = 3, skipped if locked
- **manual refill**: 1 to 20 entries per call

## how to query presign pool counts

1. call \`presignPool\`
2. returns counts per pool key for the active vault
3. low or zero counts mean the next sign on that curve will block on a fresh presign

## how to manually refill a specific pool

1. call \`replenishPresign\` with: \`poolKey\` (one of the three above), \`count\` (1-20)
2. the wallet drives the refill against the active dWallet using the corresponding curve
3. on Sui base this is a PTB on \`IkaTransaction\`; on Solana base it's gRPC

## how to take a presign for signing

1. signing internally calls \`takePresign(poolKey)\` which pops one entry
2. shorthand \`takePresignId()\` is an alias for \`takePresign('SECP256K1_ECDSA')\` since EVM is the default
3. you don't usually call this directly - signing flows do it for you

## notes

- the auto-refill alarm only fires when the wallet is unlocked. if you stay locked, the pools drain
- pools are per active dWallet Vault. switching vaults loads a different pool set; switching back restores the original counts
- presign material counts as DKG-style ika ops, so each refill round needs its IKA + SUI (or SOL) per the dynamic pricing rules - low pool + insufficient funds = signing stalls until you top up
- if you see consistently low pools, increase manual refill frequency or check that the vault is staying unlocked across the alarm cadence
`,sk=`# how to configure price source priority

chromatika fetches USD prices through a waterfall of sources. you can reorder the waterfall to prefer one source over another. caches invalidate when the order changes so the new ranking applies immediately.

## prerequisites

- a Chromatika vault is unlocked
- if you want CoinMarketCap in the rotation, the build needs \`VITE_CMC_API_KEY\`

## options at a glance

- **available sources** (today): CoinGecko, DefiLlama, CoinMarketCap, Pyth, Chainlink (EVM proxy feeds), GeckoTerminal DEX TWAP (used for IKA)
- **default order**: CoinGecko → DefiLlama → CoinMarketCap → Pyth → Chainlink → GeckoTerminal
- **cache**: ~60 second TTL per source per symbol
- **Switchboard** is in the architecture comments but explicitly **dropped** from the current waterfall

## how to read the current order

1. call \`getPricePreferences\`
2. response is the ordered list of source ids

## how to set a new order

1. submit \`setPricePreferences\` with the desired ordered array (must contain at least one id, and ids must be unique)
2. caches clear; the new ordering applies on the next price fetch

## how to fetch a price (single)

1. call \`getPrice\` with \`symbol\` (e.g. \`'sui'\`, \`'eth'\`, \`'btc'\`, \`'ika'\`)
2. wallet walks the configured waterfall, returns the first successful USD value
3. failure across all sources surfaces an error; CoinGecko or DefiLlama almost always succeed for major assets

## how to fetch prices in batch

1. call \`getPrices\` with \`symbols\` (array)
2. same waterfall logic, parallelized across symbols where possible

## notes

- CoinGecko works without an API key but has rate limits. CoinMarketCap requires \`VITE_CMC_API_KEY\` in the build to participate
- BTC fiat conversion is sourced off-chain through this waterfall; everything else can use either the off-chain sources or the on-chain ones (Pyth, Chainlink)
- IKA price specifically falls to GeckoTerminal DEX TWAP since the centralized aggregators rarely have it. don't expect deep liquidity on that
- data services (price + NFT + kiosk) are described in architecture-final.html. the waterfall here is the real implementation - architecture's longer "target" list is for future hardening
`,ik=`# how to use BIP39 recovery words

a recovery branch is a BIP39 phrase registered alongside another credential (passkey, WAAP, or Lazor) so you can still unlock the matching envelope if the primary credential is lost.

## prerequisites

- the envelope was created with a recovery branch (you supplied \`recoveryWords\` at create time, or chose the equivalent option for passkey / WAAP)
- you have the BIP39 words written down somewhere safe
- the chromatika vault still exists locally (recovery words restore an unlock path, not the encrypted blob - if storage is wiped you also need the original install + envelope record)

## options at a glance

- **passkey envelope** with recovery branch
- **WAAP envelope** with recovery branch
- **Lazor envelope** with recovery branch
- recovery words **cannot** unlock a password-only or hardware-only envelope - those have their own primary credentials

## how to register recovery words

recovery is registered when the envelope is created:

- **Lazor**: pass \`recoveryWords\` to \`createVaultLazor\` or \`addVaultLazor\`
- **passkey** / **WAAP**: register a recovery probe at envelope create time per the create flow

if the envelope was created without a recovery branch, you cannot retroactively bolt one on - create a sibling envelope (or a new vault) that has one

## how to unlock with recovery words

1. call \`listVaultEnvelopes\` to find the target envelope id
2. submit \`unlockVaultRecoveryWords\` with: \`envelopeId\`, the BIP39 words, \`autoLockMinutes\` (1-1440, default 30)
3. the background derives the recovery key from the words, decrypts the envelope, parks the AES \`CryptoKey\` in session

## how to rotate primary credential after recovery

after unlocking via recovery, you usually want to register a fresh primary credential:

1. for passkey: \`runPasskeyAddVault\` to add a new passkey envelope to the same vault
2. for WAAP: \`addVaultWaap\` with a fresh WAAP login
3. for Lazor: \`addVaultLazor\` with a fresh smart wallet credential
4. optionally \`removeVault\` on the old envelope after the new one is verified

## notes

- BIP39 words for recovery are **not** the same as the dWallet Vault mnemonic - they are envelope-bound recovery probes. confusion here means failed unlocks
- treat these words like any other seed phrase: offline, never typed into a website, never shared
- if you lose both the primary credential and the recovery words, the envelope is unrecoverable. you can still use other envelopes / vaults on the same install
`,ok=`# how to use chromatika safety alerts

chromatika polls a signed feed every 5 minutes for in-the-wild attack reports (phishing sites, drainer contracts, scam dapps). verified alerts surface as in-app banners, chrome notifications (for critical), and chrome's URL-blocking layer (for critical with affected domains). all alerts are ed25519-signed by an allow-listed publisher; unsigned or wrong-publisher feeds are dropped.

## prerequisites

- chromatika is installed
- chrome's notifications permission was granted on install (manifest declares \`notifications\`)
- network access to \`https://chromatika.dev/safety-alerts.json\` (or the user-set custom feed URL) every 5 min

## options at a glance

- **severity filter** at the source: alerts are tagged \`critical\`, \`warning\`, or \`info\`. critical alerts produce all three surfaces (banner + notification + URL block); warning produces banner + notification; info produces banner only
- **mute**: silence chrome notifications and the in-app banner without disabling URL blocking
- **opt-out**: stop the poller entirely. URL-blocking rules clear on TTL expiry. you'll still see expired alerts in history
- **custom feed URL**: override the default with a custom URL (advanced; useful for testing or self-hosted feeds)
- **alert history**: every verified alert ever seen, regardless of dismiss / expire status, capped at 200
- **dev injection** (developer build only): paste a signed alert JSON to test the rendering / action pipeline locally

## how to view active alerts

1. active alerts (non-dismissed, non-expired) show as a banner near the top of the wallet, color-coded by severity
2. expand the banner to see the long-form body (markdown, capped at 4000 chars), affected domains, and the alert publisher
3. dismiss to remove from the banner; the alert stays in history with a "dismissed" tag

## how to mute alerts

1. mute toggle silences chrome notifications + the in-app banner
2. URL-blocking dNR rules **stay active** when muted - mute means "stop yelling," not "ignore threats"
3. unmute restores both surfaces

## how to opt out entirely

1. opt-out flag stops the 5-min poller
2. existing dNR rules clear on their per-rule TTL alarm (each rule has its own expiry timer)
3. alert history remains readable; you just stop fetching new alerts
4. unsetting opt-out resumes polling immediately + runs an immediate poll to catch up

## how to set a custom feed URL

1. in the alerts settings, paste a custom feed URL
2. saves trigger an immediate poll against the new URL
3. useful for: self-hosted feeds during development, alternative trusted publishers, testing
4. clearing the field reverts to the default (\`https://chromatika.dev/safety-alerts.json\` or \`VITE_ALERTS_FEED_URL\` if set at build time)

## how to view alert history

1. expand the alert history section in settings to see every verified alert, sorted by timestamp (newest first)
2. each entry shows: severity icon, title, affected domains, body preview, dismissed flag, publisher pubkey label
3. capped at 200 most recent entries (FIFO eviction)

## how to manually refresh

1. click the refresh button in the alerts settings to force an immediate poll
2. status indicator shows last-poll timestamp + any error from the most recent attempt

## how to view the publisher allowlist

1. expand the publisher allowlist section in settings (advanced)
2. read-only viewer of the bundled \`BUNDLED_PUBLISHERS\` (pubkey base64 + label)
3. pre-launch: this is a single placeholder dev key. production will replace with a real chromatika-team pubkey

## how to inject a signed alert (developer build only)

1. visible only when \`import.meta.env.DEV\` is true (developer build)
2. paste a signed alert JSON envelope
3. the wallet runs it through the same \`verifySignedAlert\` pipeline as a feed-fetched alert (signature check, time-bounds check, allowlist check). a malformed dev alert can't bypass verification
4. used for: testing notification + dNR rendering during local demos when the feed isn't reachable

## how alerts route from the feed to your wallet

at a glance:
1. every 5 minutes, the poller fetches the signed feed JSON
2. each alert in the feed is independently verified (ed25519 signature, publisher allowlist, time bounds)
3. dropped alerts are logged but don't poison the rest of the batch
4. new (not seen before) alerts trigger:
   - banner display (all severities)
   - chrome notification (critical + warning, unless muted)
   - dNR redirect rule for each affected domain (critical only) - typing a flagged URL routes to the warning page
5. dismiss / opt-out / mute affect surface visibility but don't bypass verification

## notes

- the feed is anonymous HTTP GET. the wallet doesn't send your address, chain id, or any identifying state. ISP / DNS resolvers can see you're polling chromatika.dev every 5 min, but not what's in your wallet
- the alerts subsystem is **separate** from the existing \`eth-phishing-detect\` MetaMask blocklist (which has its own daily refresh and ~3500 bundled domains). both layers compose: eth-phishing-detect rules use IDs 1-4900; chromatika alerts rules use IDs 10000-19999. no collision
- \`info\`-severity alerts only show as in-app banners. they don't trigger notifications or URL blocks. typical use: educational reminders, soft informational nudges
- if a critical alert lists a domain you legitimately use (false positive), report it. there's no in-wallet allowlist override today; the only fix is upstream feed correction. tracked future
- alert IDs are content-stable: the same alert (same id) won't double-trigger notifications even if it appears in multiple feed fetches
`,lk="# how to use Solana Mobile Wallet Adapter (local transport)\n\npair a Solana phone wallet (Seeker, Phantom Android, Solflare Android, Jupiter, etc.) running on the **same Android device** as chromatika via Mobile Wallet Adapter (MWA) `transact()`. the local transport launches the wallet via an Android intent (`solana-wallet://`) and gets a signature back over the same intent.\n\n## prerequisites\n\n- chromatika is running in a Chromium-based browser **on Android**\n- a Solana mobile wallet supporting MWA 2.0 is installed on the same device\n- WebView / system intent handling allows the `solana-wallet://` scheme\n- your Chromatika vault is unlocked\n\n## options at a glance\n\n- **transport**: `local` (this guide). for desktop ↔ phone QR pairing, see [seeker-remote.md](/library/user/seeker-remote)\n- **vendor**: `mwa` with `mwaTransport: 'local'` on the hardware vault record / hardware account\n\n## how to pair an MWA local account\n\n1. trigger MWA `transact()` from the popup or side-panel context (cannot run in the service worker - the lib touches `window` APIs)\n2. Android dispatches the intent; pick the Solana wallet you want to authorize\n3. the wallet returns the authorized account public key + auth metadata\n4. submit `addHardwareAccount` with: `vendor: 'mwa'`, `chain: 'solana'`, `derivationPath`, `address`, `ed25519PublicKeyB64`, plus the transport-specific metadata (`mwaTransport: 'local'`)\n\n## how to sign with MWA local\n\n1. operations that need a Solana signature on this account enqueue a hardware-sign request, open the hardware-sign popup\n2. popup runs `transact()` again to dispatch the sign intent to the matching wallet\n3. you approve on the wallet, signature returns over the intent\n4. popup calls `resolveHardwareSign` with the signature, or `rejectHardwareSign` to cancel\n\n## how to use this account as a hardware-primary vault root\n\nthe local-MWA account can also seed a hardware-primary chromatika vault (Solana base). see [hardware-vault.md](/library/user/hardware-vault) - same flow as remote, just dispatched over local intents instead of the QR-paired reflector\n\n## notes\n\n- the local transport only works because the wallet and chromatika are on the **same Android device**. on desktop or iOS, you have to use the remote transport\n- chromatika gates the MWA local entry on UA - it only surfaces the option when the user agent matches Android\n- intent dispatch is not catchable inside the service worker. the popup / side-panel context is mandatory\n- Seed Vault never reveals key bytes, so MWA + Solana base vaults still need an in-extension Solana keypair to pay ika gRPC `approve_message` fees - the vault auto-generates this (`ikaGrpcFeePayerSolSecretKeyB64`). see [ika-fee-management.md](/library/user/ika-fee-management)\n",ck="# how to use Solana Mobile Wallet Adapter (remote transport)\n\npair a Seeker (or any MWA-compliant phone wallet that supports the reflector protocol) with chromatika running on a **desktop** via QR-coded MWA remote transport. the wallet ↔ extension link uses a websocket reflector at `wss://reflect.solanamobile.com` and an `auth_token` so subsequent signs reauthorize without rescanning the QR.\n\n## prerequisites\n\n- chromatika is running on a desktop Chromium browser (popup or side-panel context)\n- a phone with an MWA 2.0 wallet that can scan a chromatika QR or accept a `solana-wallet://` association URL (Seeker built-in, Phantom Android, Solflare Android, Jupiter)\n- network access to `wss://reflect.solanamobile.com`\n- chromatika manifest allows wss connections (CSP `connect-src *` covers this today; will tighten when product surface stabilizes)\n- your Chromatika vault is unlocked\n\n## options at a glance\n\n- **transport**: `remote` (this guide). for same-device Android, see [seeker-local.md](/library/user/seeker-local)\n- **reflector host**: defaults to `MWA_REMOTE_HOST_AUTHORITY` (Solana Mobile's public reflector). a self-hosted Cloudflare Workers reflector exists in `/reflector` as a fallback; do not use it unless the public host is down\n- **persisted auth**: `auth_token` + `reflectorHost` saved on the vault record so reauth runs silently\n\n## how to pair a Seeker via QR\n\n1. trigger `startRemoteScenario()` from the popup / side-panel - this opens the websocket from `window`-having context (the service worker cannot, the library uses `btoa` / `atob`)\n2. read the resulting association URL and render it as a QR code\n3. scan the QR with the Seeker camera or the wallet's \"scan link\" input\n4. approve in Seed Vault (or whichever wallet you're using)\n5. wallet hands back: account pubkey, ed25519 public key, `auth_token`, the reflector host\n6. submit `addHardwareAccount` with: `vendor: 'mwa'`, `chain: 'solana'`, `mwaTransport: 'remote'`, `address`, `ed25519PublicKeyB64`, plus the auth metadata\n\n## how to sign with MWA remote\n\n1. signing operations enqueue a hardware-sign request, open the popup\n2. popup reauthorizes against the persisted `auth_token` (no new QR), dispatches the sign request through the reflector\n3. you approve on the phone (Seed Vault on Seeker)\n4. signature returns through the reflector, popup resolves via `resolveHardwareSign`\n\n## how to re-pair when authorization fails\n\n1. an `ERROR_AUTHORIZATION_FAILED` from the wallet means the `auth_token` is invalid (revoked, expired, wallet uninstalled, etc.)\n2. `MwaSigner` flips into `needsRepair` state; trigger `startRemoteScenario()` again to render a fresh QR\n3. scan + approve, the new `auth_token` overwrites the old one\n\n## how to derive the ika seed for a hardware-primary vault\n\nthe remote-MWA path is also how a hardware-primary Chromatika vault on Solana base gets its ika `UserShareEncryptionKeys` root seed:\n\n1. ask the wallet to sign `IKA_USK_DERIVATION_MESSAGE` (constant `'ika.chromatika.user-share-encryption-key.v1'`)\n2. Ed25519 is deterministic per RFC 8032, so the same wallet on a different device produces the same signature - means restore-on-new-machine works without any HD seed\n3. `keccak256(signature)` is the 32-byte seed\n4. see [hardware-vault.md](/library/user/hardware-vault) for the full vault create flow\n\n## notes\n\n- the public reflector at `development.reflector.solanamobile.com` is the tested host - all shipping wallets are tested against it. self-hosted reflectors can have wallet-side allowlists or untested paths cause silent UI freezes; don't switch hosts unless you have to\n- a separate fee-payer keypair (`ikaGrpcFeePayerSolSecretKeyB64`) is auto-generated per install for `approve_message` ika gRPC fees - fund it with ~0.1 devnet SOL after vault create. see [ika-fee-management.md](/library/user/ika-fee-management)\n- deprecated field `ikaEncryptionOnlySolSecretKeyB64` reads as fallback for old dev installs but receives no new writes\n- runbook: `wallet-extension/docs/SEEKER_REMOTE_PAIRING.md`\n",uk=`# how to send native BTC

transfer BTC from your active SECP256K1 dWallet's P2WPKH (segwit bech32) address to another Bitcoin address. chromatika supports segwit and taproot addresses on the receiving side and uses presign material from the SECP256K1 ECDSA pool.

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet has SECP256K1 (see [create-dwallet.md](/library/user/create-dwallet))
- the active Bitcoin network (mainnet / testnet) is set
- the dWallet's P2WPKH address has enough BTC to cover \`amount + fee\`
- the SECP256K1_ECDSA presign pool has at least one entry (see [presign-pool.md](/library/user/presign-pool))

## options at a glance

- **network**: mainnet or testnet (signet is also configurable in custom networks per the registry)
- **amount**: BTC in human-readable units (sats handled internally)
- **recipient**: any valid Bitcoin address (P2WPKH, P2TR, legacy)

## how to send

1. read your P2WPKH + P2TR addresses with \`getBtcAddresses\` (passing the desired \`network\`)
2. submit \`sendBtcNative\` with: \`to\`, \`amountBtc\`
3. background builds a UTXO transaction (bitcoinjs-lib), signs via ika MPC + SECP256K1_ECDSA presign, broadcasts via the configured Esplora endpoint
4. tx id returns once accepted by the mempool

## how to switch networks

1. call \`setActiveBitcoinNetwork\` with the \`networkId\`
2. submit \`sendBtcNative\` - it uses the active network's Esplora client

## notes

- chromatika's Bitcoin send uses bitcoinjs-lib for tx building; signing comes from ika MPC. P2TR (taproot) sending uses the SECP256K1_TAPROOT presign pool; P2WPKH uses SECP256K1_ECDSA
- dWallet-based BTC ops on Solana base are gated today (\`assertNotSolanaBaseForSecpSigning\`); BTC sends require Sui-base
- Ledger Bitcoin signing exists as a hardware account path (PSBT via \`hw-app-btc@10.x\`); Trezor BTC PSBT is currently unsupported
- if you see "insufficient signature material" on a send, check the presign pool counts and refill (see [presign-pool.md](/library/user/presign-pool))
`,dk="# how to send a transaction on EVM\n\nsend native ETH or any other EVM-native asset (and run any contract call you supply calldata for) from chromatika as the originating user. this is the **wallet UI flow** - direct broadcast, no dapp approval popup, since the user is already inside the wallet making an intentional action.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active dWallet has SECP256K1 (otherwise create one - see [create-dwallet.md](/library/user/create-dwallet))\n- the active EVM network is the chain you intend to send on (see [manage-networks.md](/library/user/manage-networks))\n- the EVM RPC for that network is reachable (`getEvmRpcHealth` returns success)\n- the active dWallet has enough native balance to cover `value + gas`\n- on the dapp side: this guide is for `sendEvmTx` - if the request originates from a dapp's `eth_sendTransaction`, see [approve-dapp-tx.md](/library/user/approve-dapp-tx) instead\n\n## options at a glance\n\n- **chain**: any EVM network the wallet knows (built-in or custom). `chainId` argument is optional - defaults to the active EVM chain\n- **value**: hex-encoded wei\n- **data**: hex-encoded calldata for contract calls (omit for plain transfers)\n- **gas overrides**: managed at sign time via the standard EVM gas semantics; for dapp-initiated tx, use the approval flow's slow / normal / fast / custom presets\n\n## how to send a plain native transfer\n\n1. read your active EVM address with `getEvmAddress` and confirm the source\n2. submit `sendEvmTx` with: `to` (recipient), `value` (hex wei), optionally `chainId`\n3. background runs `signAndBroadcastEvm` directly - no popup, signs via ika MPC against the SECP256K1 dWallet, broadcasts on the active EVM RPC\n4. tx hash returns synchronously (subject to RPC latency)\n\n## how to send a contract call (e.g. ERC20 transfer)\n\n1. craft calldata for the function you want (e.g. ERC20 `transfer(address,uint256)`)\n2. submit `sendEvmTx` with: `to` (the contract address), `value` (`0x0` for non-payable calls), `data` (the calldata hex), optional `chainId`\n3. same direct sign + broadcast path\n\n## how to use a different EVM chain than the active one\n\n1. pass `chainId` explicitly to `sendEvmTx`\n2. background looks up the matching RPC + provider via `getRpcProviderForChain` (separate from `getRpcProvider` which always uses the active chain)\n3. ensures the tx broadcasts on the chain you intend, even if the active EVM chain is something else\n\n## how to query gas options for a tx\n\nfor a pending dapp `eth_sendTransaction`, the approval flow exposes preset slow / normal / fast / custom gas options via `getTxGasOptions` plus a simulated output via `getTxSimulationPreview` (eth_call at latest block, no third-party API). the wallet UI flow does not stop on a popup - if you want to inspect gas first, use the approval-flow utilities yourself before submitting\n\n## notes\n\n- `sendEvmTx` from the wallet UI is **not** gated by an approval popup. **never add an approval popup to this flow** - the user is already inside the wallet UI making an intentional action\n- the dapp `eth_sendTransaction` path does open a popup - that's [approve-dapp-tx.md](/library/user/approve-dapp-tx)\n- ika ECDSA signing uses preimage passthrough: chromatika hands ika `tx.unsignedSerialized` and lets ika hash with KECCAK256 once. if you write your own EVM signing on top of ika, use `parseSignatureFromSignOutput` (`Curve.SECP256K1`, `SignatureAlgorithm.ECDSASecp256k1`) and pick `v` via `recoverAddress(keccak256(unsignedSerialized), …)` trying 27 vs 28\n- on Solana base, EVM SECP signing is gated by `assertNotSolanaBaseForSecpSigning` today (sui-base only). re-validate when Solana-base SECP parity ships\n- there's no separate \"plain HD\" EVM path for sends - the dWallet is the canonical EVM identity\n",hk=`# how to send native SOL

transfer SOL to another address from your active ED25519 dWallet on Solana. the dWallet is the canonical Solana identity for chromatika - this is the user-facing send path.

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet has ED25519 (otherwise create one - see [create-dwallet.md](/library/user/create-dwallet))
- the active Solana network (mainnet / devnet) is set, RPC reachable
- the active dWallet's Solana address has enough SOL to cover \`amount + fees\`
- if your active dWallet Vault uses Solana base (pre-alpha), the in-extension fee-payer also has SOL for ika gRPC \`approve_message\` fees - see [ika-fee-management.md](/library/user/ika-fee-management)

## options at a glance

- **amount**: SOL in human-readable units (lamports conversion handled internally)
- **recipient**: any valid base58 Solana address
- **base chain**: works on both Sui base (sign via Sui-anchored ED25519 dWallet that has a Solana address) and Solana base (pre-alpha mock signer)

## how to send

1. confirm the active dWallet's Solana address with \`getSolanaAddress\`
2. submit \`sendSolanaNative\` with: \`to\`, \`amountSol\`
3. background builds a Solana versioned transaction, signs via ika MPC against the ED25519 dWallet, broadcasts on the active RPC
4. signature / tx id returns once landed

## how to send to a different Solana network

1. switch network first via \`setActiveSolanaNetwork\` with \`networkId\` and \`tier: 'dwallet'\`
2. submit \`sendSolanaNative\`

## notes

- on Solana base (pre-alpha), the actual signature comes from a single mock signer. **never** send real-value SOL on a Solana-base dWallet
- chromatika does **not** add a software signing path that bypasses the dWallet - the dWallet is the user identity. if you want native-SOL sends from an imported keypair without a dWallet, use a hardware-vault flow with the keypair as the hardware account instead
- the dapp \`solana_signTransaction\` path is separate - it routes through the dapp bridge with origin consent and approval popups (see [connect-dapp.md](/library/user/connect-dapp)); \`sendSolanaNative\` is the wallet UI direct flow
`,pk=`# how to send native SUI

transfer SUI to another address from the **HD fee-payer** keypair on the active dWallet Vault. note this is **not** the MPC dWallet path - it's the same fee-payer key the wallet uses to fund DKG / presigns / on-chain ika ops.

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet Vault is on Sui base (see [manage-networks.md](/library/user/manage-networks) for switching base chain)
- the active Sui network (mainnet / testnet / devnet) is set; the Sui RPC / GraphQL endpoint is reachable
- the HD fee-payer has enough SUI to cover \`amount + gas\`

## options at a glance

- **amount**: SUI in human-readable units (the wallet handles MIST conversion)
- **recipient**: any valid Sui address

## how to send

1. confirm the active vault's HD fee-payer address (it's the address that funds ika ops on Sui base)
2. submit \`sendSuiNative\` with: \`to\` (recipient address), \`amountSui\`
3. background builds and submits a Sui transaction via \`SuiGraphQLClient\` (\`client.core.*\`), signs with the HD fee-payer keypair (Ed25519, native sui signing), broadcasts
4. tx digest returns once Sui finality is reached (reasonably fast on Sui)

## how to send to a different Sui network

1. switch the active Sui network first via \`setActiveSuiNetwork\` (with \`networkId\`, \`tier: 'vault'\`)
2. then submit \`sendSuiNative\` - the call uses the active network's GraphQL endpoint

## notes

- this path uses the HD fee-payer, not the dWallet. that means it's not the canonical "user identity" path on Sui - dapp connections + on-chain identity go through dWallets. the fee-payer is the gas key
- if you want to send SUI **from** a Sui-anchored dWallet's address (the user-facing identity), that's a Sui-base dWallet sign flow, not \`sendSuiNative\`. today the wallet UI exposes \`sendSuiNative\` for HD fee-payer sends; dWallet-based Sui sends route through the dapp / sign flows
- chromatika prefers GraphQL for any Sui call Mysten exposes on \`client.core.*\`. JSON-RPC stays for legacy code paths only. activity feed is one of the remaining JSON-RPC users (no GraphQL \`listTransactions\` yet)
`,mk="# how to sign messages across chains\n\nper-chain message signing - covers EVM `personal_sign` + `eth_signTypedData_v4`, Sui personal-message, Solana arbitrary-message, Aptos message, and Bitcoin message signing. these are user- or dapp-initiated message signs that don't broadcast a transaction.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active dWallet has the right curve for the chain you're signing on:\n  - EVM, BTC → SECP256K1\n  - Sui, Solana, Aptos → ED25519\n- the dapp / caller has an existing connection if originating from a dapp\n\n## options at a glance\n\n- **EVM**: `personal_sign` (EIP-191 prefixed) and `eth_signTypedData_v4` (EIP-712)\n- **Sui**: `signPersonalMessage` (ika SHA512 path; see notes for Mysten BLAKE2b parity gap)\n- **Solana**: arbitrary-message ed25519 sign\n- **Aptos**: arbitrary-message ed25519 sign\n- **Bitcoin**: hex message sign with the active BTC dWallet\n\n## how to sign an EVM message (`personal_sign`)\n\n1. submit `signEvm` with `message` (the plain string the user wants signed) and `chainId`\n2. background hex-encodes per EIP-191 (`\\x19Ethereum Signed Message:\\n` prefix), runs ika MPC ECDSA against the SECP256K1 dWallet\n3. returned signature is 65 bytes (r||s||v) with v picked via `recoverAddress`\n\n## how to sign typed data (`eth_signTypedData_v4`)\n\n1. submit the typed-data structure through the EVM bridge / signing surface\n2. **important**: chromatika passes ika the EIP-712 **preimage** via `TypedDataEncoder.encode()`, not the hashed digest. ika hashes once with KECCAK256 internally - if you double-hash, the recovered signer address is wrong\n3. signature normalizes through `parseSignatureFromSignOutput` (`Curve.SECP256K1`, `SignatureAlgorithm.ECDSASecp256k1`) and `v` is selected by trying 27 vs 28 against `recoverAddress(keccak256(encodedPreimage), ...)`\n\n## how to sign a Sui personal message\n\n1. submit `sui_signPersonalMessage` (or `signPersonalMessage` via the dapp bridge, depending on entry)\n2. background runs the **ika SHA512 path** - signs raw message bytes via ika ED25519 + SHA512, **not** the Mysten BLAKE2b PersonalMessage intent\n3. **caveat**: some dapps verify only via Mysten's BLAKE2b intent and may reject the chromatika sig until Mysten-intent BLAKE2b parity ships. tracked as future hardening; documented in [WALLET_SECURITY.md](/library/user/wallet_security)\n\n## how to sign a Solana message\n\n1. submit `signSol` with `messageB64` (base64 of the message bytes)\n2. background runs ika MPC ED25519 EdDSA against the active Solana dWallet\n3. for dapp-initiated solana sign-message via the bridge, the same path runs after origin consent\n\n## how to sign an Aptos message\n\n1. submit `signAptos` with `messageB64`\n2. ika MPC ED25519 path identical to Solana\n\n## how to sign a Bitcoin message\n\n1. submit `signBtc` with `messageHex`\n2. signs against the active SECP256K1 dWallet for the configured BTC address (P2WPKH or P2TR depending on selected address kind)\n\n## notes\n\n- the **preimage passthrough** rule applies anywhere ika signs over hashed data: hand ika the preimage bytes, never the hash. this includes both `personal_sign` (EIP-191 wrapped bytes) and typed-data (EIP-712 encoded preimage). double-hashing breaks signer recovery\n- on Solana base (pre-alpha), all signatures come from a single mock signer - **do not** use Solana-base for messages dapps will verify against real on-chain identities\n- Bitcoin message signing on Solana base is gated by `assertNotSolanaBaseForSecpSigning`\n- a Solana sendTx MCP tool (sibling of `sendEvmTx`) is tracked as future; today MCP exposes only `signMessage` (evm + solana), `sendEvmTx`, and `signTransaction` for EVM\n",gk=`# how to swap SUI for IKA

chromatika ships a SUI → IKA swap powered by the Aftermath router on Sui. you fetch a quote, then execute. this is the **Phase B** swap path; it's feature-flagged behind \`VITE_PHASE_B_SUI_SWAP\` (default true today, so it's effectively shipped).

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet Vault is on Sui base
- the active vault has SUI (above the minimum reserve) and you want IKA in return
- Aftermath router endpoints are reachable (\`/router/trade/route\` + \`/router/trade/transaction\`)
- \`VITE_PHASE_B_SUI_SWAP\` is enabled in the build (default: true)

## options at a glance

- **slippage**: 10 to 500 basis points (default 100 bps = 1%)
- **amount**: SUI in MIST (\`amountInMist\`) - optional input; the wallet can quote for a default fraction of available SUI if you don't supply
- **min reserve**: 50,000,000 MIST (0.05 SUI) is reserved on the vault for gas - the swap won't drain past this
- **quote cache**: 30 second TTL; quotes go stale fast in volatile markets

## how to check swap readiness

1. call \`phaseBFundingSpike\` to see if the swap is enabled and the spike is active
2. or call \`swapStatus\` for a richer snapshot - returns swap capability, balance info, spike status

## how to fetch a quote

1. submit \`swapQuote\` with \`amountInMist\` (optional) and \`slippageBps\` (10-500, default 100)
2. response includes: route description, expected IKA out, price impact, quote id, expiry
3. quote id is the handle to feed into \`executeSwap\`. quotes go stale after 30 seconds

## how to execute a quote

1. submit \`executeSwap\` with \`quoteId\` and the quote object
2. **long-running mutation** - 20s keepalive on the tRPC port (\`src/lib/trpc.ts\` exempts swap execution from the 12s default)
3. background fetches the serialized PTB from Aftermath (\`/router/trade/transaction\`), deserializes via \`Transaction.from()\`, signs with the active Sui keypair, broadcasts on the active Sui RPC
4. tx digest returns once Sui confirms. failure returns an actionable reason (insufficient funds, quote expired, slippage too tight, etc.)

## how to handle "swap is down" or empty IKA pool

the swap path degrades gracefully:

- if Aftermath is unreachable, \`swapQuote\` returns an error and the surface tells the user to fund manually
- if the IKA pool has no liquidity (e.g. a fresh testnet deploy), the quote returns zero-output and the surface offers manual funding instructions

## notes

- this is a SUI → IKA path only today. multichain funding aggregation (cross-chain bridging) is tracked as future (\`docs/future/FUNDING_STRATEGY.md\`)
- the swap signs with \`suiKeypair\` (HD fee-payer), not the dWallet. that's intentional - swaps fund the fee-payer, not the user identity
- minimum reserve \`MIN_SUI_RESERVE_MIST = 50_000_000n\` is a hard floor in \`swap-config.ts\` - the wallet will not let you swap below it
- \`DEFAULT_SLIPPAGE_BPS = 100\` and \`QUOTE_CACHE_TTL_MS = 30_000\` are also constants in \`swap-config.ts\`
`,fk=`# how to use Sui Kiosks

Sui Kiosks are on-chain object containers that hold tradeable items (often NFTs) with optional listings, transfer policies, and royalties. chromatika exposes kiosks owned or managed by an address, the items inside, and the listing / policy state.

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet Vault or the address you query has at least one Sui kiosk (otherwise the list returns empty)
- the active Sui network is the one where your kiosks live (mainnet / testnet)

## options at a glance

- **owned kiosks**: kiosks where the address is the owner
- **managed kiosks**: kiosks the address has manager rights on (different from ownership)
- **per-kiosk data**: items, listings, transfer policies, royalty config
- backed by \`@mysten/kiosk\` \`KioskClient\`

## how to list kiosks for an address

1. call \`getOwnedKiosks\` with \`address\`
2. response is a list of kiosk ids (both owned and managed where applicable, returned together)

## how to view what's inside a kiosk

1. call \`getKioskData\` with \`kioskId\`
2. response is the detailed kiosk state: items list (with display metadata), listings, transfer policies, royalty configs
3. images respect MediaSafetyMode

## how to interact with kiosk items

today chromatika exposes **read** for kiosks. listing items, taking items, transferring kiosk ownership, etc. are part of the kiosk product surface. those calls go through the kiosk client + an \`IkaTransaction\` PTB (or the dWallet sign path) and surface in the kiosk panel inside NFTs

## notes

- kiosks are currently a panel inside the NFTs surface; promoting kiosks to a dedicated page is tracked future per architecture-final.html
- transfer policies enforce royalty rules at the protocol level - a transfer that doesn't satisfy the policy aborts at simulation. always read the policy before crafting a transfer
- managed kiosks (where you're the manager but not owner) have a different operations set than owned kiosks; kiosk-client surfaces both but distinguishes them in the response
- chromatika prefers \`KioskClient\` on GraphQL where possible; some kiosk reads still fall back to JSON-RPC for legacy fields
`,yk=`# how to transfer a dWallet

dWallets can be transferred between users by re-encrypting the user share to a new owner's encryption key. this guide covers initiating an outbound transfer, accepting an incoming transfer, and parsing transfer-tx digest hints.

## prerequisites

- a Chromatika vault is unlocked
- for **outbound** transfer: you control an active dWallet and you know the recipient's Sui address
- for **inbound** transfer: you've already registered your encryption key (\`registerEncryptionKey\` per curve - see [manage-dwallets.md](/library/user/manage-dwallets)); the sender has sent you the transfer tx digest plus encrypted-share hints

## options at a glance

- **outbound**: \`transferDWallet\` - works on either curve
- **inbound**: \`acceptTransferredDWallet\` - finalizes the share so you can sign with it
- **digest parsing**: \`parseTransferTxDigest\` extracts the encrypted-share hints from a Sui transfer tx digest you were sent

## how to initiate an outbound transfer

1. make sure the dWallet you want to transfer is active for its curve
2. call \`transferDWallet\` with \`curve\` and \`recipientSuiAddress\`
3. background runs \`requestReEncryptUserShareFor\` against the recipient's registered encryption key - the recipient now has an encrypted share waiting on-chain
4. send the recipient the transfer-tx digest + curve so they can run the accept flow

## how to parse a transfer tx digest you received

1. call \`parseTransferTxDigest\` with the Sui transaction \`digest\`
2. wallet returns the encrypted-share id, sender encryption-key address, and other hints needed to accept
3. only Sui digests are supported today

## how to accept an incoming transfer

1. confirm you've registered your encryption key for the matching curve (\`getSenderEncryptionKeyAddress\` returns it)
2. call \`acceptTransferredDWallet\` with: \`curve\`, \`dwalletId\`, the sender's encryption-key address, and the encrypted-share hints from the parse step (or hints the sender provided manually)
3. the wallet runs \`acceptEncryptedUserShare\` on-chain; the dWallet flips to \`Active\` for you
4. the dWallet now appears in your owned caps - set it active via \`setActiveDwallet\` if you want it driving signing

## notes

- dWallet transfer is a **per-dWallet** action, not a vault-level action. the recipient gets exactly the one dWallet you send them, with its current state
- the recipient must have their encryption key registered **before** you initiate the transfer - if not, the \`requestReEncryptUserShareFor\` call has nowhere to encrypt to
- for Solana-base dWallets, the transfer surface depends on what the pre-alpha SDK exposes; today the Solana adapter's transfer-related reads still throw and the path is Sui-first. transferring solana-base dWallets is not generally usable
- after acceptance, the previous owner cannot sign with the dWallet anymore - the share is encrypted to your key
`,wk="# how to use a Trezor hardware wallet\n\npair a Trezor device via `@trezor/connect-web` and use it as a hardware account in chromatika for EVM message + typed-data + transaction signing, plus Solana transaction signing. **chromatika never asks for your seed phrase from the device** - all signing stays on-device.\n\n## prerequisites\n\n- Trezor Model One or Model T with current firmware\n- chromatika manifest CSP allows `frame-src https://connect.trezor.io` (already shipped)\n- the pairing must run from popup / side-panel (Trezor Connect uses a hosted iframe; the service worker cannot drive it)\n- your Chromatika vault is unlocked\n\n## options at a glance\n\n- **chains supported today**: EVM (message, typed data, tx), Solana (tx)\n- **chains not supported**: Bitcoin PSBT (Trezor needs decomposed UTXO inputs - chromatika throws an actionable error pointing at Ledger; tracked as future), Sui (Trezor Connect doesn't expose Sui)\n\n## how to pair a Trezor account\n\n1. plug the device in, unlock with your PIN\n2. trigger the Trezor Connect account-discovery flow - the hosted iframe walks through device authorization\n3. submit `addHardwareAccount` with: `vendor: 'trezor'`, `chain` (`evm` or `solana`), `derivationPath`, `address`, and `ed25519PublicKeyB64` for Solana\n\n## how to sign with a Trezor\n\n1. operations that require signing enqueue a hardware-sign request and open the popup at `index.html?hwsign=ID`\n2. popup calls `getHardwareSignRequest` with id, drives Trezor Connect (`TrezorConnect.ethereumSignMessage` / `signTypedData` / `signTransaction` / `solanaSignTransaction`)\n3. confirm on the device\n4. popup calls `resolveHardwareSign` with the signature, or `rejectHardwareSign` if cancelled\n\n## how to remove a Trezor account\n\n1. `removeHardwareAccount` with the account `id`\n\n## notes\n\n- attempting Bitcoin sign on a Trezor account throws with an actionable error message pointing the user at Ledger today\n- Sui is unsupported by Trezor Connect at the protocol level - chromatika does not pretend otherwise\n- if the Trezor Connect iframe fails to load, check that `https://connect.trezor.io` is reachable and that the CSP `frame-src` allows it\n",vk=`# how to unlock and lock chromatika

control the wallet's locked / unlocked state. when locked, every signing or sending operation is rejected and the dapp bridge reports disconnected. when unlocked, the session holds a non-extractable AES \`CryptoKey\` derived from your password (or from a passkey / hardware signature, depending on the unlock path).

## prerequisites

- a chromatika vault exists (created or imported)
- you know the password, or have a passkey / WAAP / hardware envelope registered, or have a recovery phrase

## options at a glance

- **password unlock**: classic path; works on any vault that has a password (most of them)
- **passkey unlock**: WebAuthn + PRF HMAC-secret derived key (no password needed if vault is passkey-only)
- **hardware / wallet-signature unlock**: WAAP, Seeker, or WalletConnect signature on an envelope-bound challenge
- **recovery-words unlock**: BIP39 phrase as a fallback when the primary credential is lost (only available if the envelope was set up with a recovery branch)
- **autolock window**: 1 to 1440 minutes; default 30
- **manual lock**: instant lock, broadcasts disconnect to all connected dapps

## how to unlock with a password

1. read \`lockState\` first to see if you're already unlocked (this call is cheap and runs before unlock prompts)
2. submit \`unlockVault\` with your password and the desired \`autoLockMinutes\` (1-1440)
3. background runs Argon2id to derive the AES key, decrypts \`chromatika_vault_v3\`, parks the non-extractable \`CryptoKey\` plus KDF meta in the session
4. dapp bridge reconnects to active permissions; presign refill alarm rearms

## how to unlock with a passkey

1. fetch unlock options via \`listVaultEnvelopes\` to find the passkey envelope id
2. trigger the WebAuthn ceremony (browser prompts the authenticator) - PRF returns a 32-byte secret
3. submit \`unlockVaultPasskey\` with: envelope id, the PRF secret (b64), \`autoLockMinutes\`
4. session unlocks identically to the password path

## how to unlock with a hardware / wallet signature (WAAP / Seeker / WalletConnect)

1. find the matching envelope via \`listVaultEnvelopes\`
2. ask the wallet device to sign the envelope challenge (the device determines the ceremony)
3. submit \`unlockVaultWalletSignature\` with the envelope id, the signature (b64), and \`autoLockMinutes\`

## how to unlock with recovery words

1. find the recovery envelope via \`listVaultEnvelopes\`
2. submit \`unlockVaultRecoveryWords\` with the envelope id, the BIP39 words, and \`autoLockMinutes\`
3. only works if a recovery branch was registered when the credential was created (passkey, WAAP, or Lazor flows can opt in)

## how to set or change the autolock window

1. pass \`autoLockMinutes\` (1-1440) on any unlock call - it sets the window for that session
2. the alarm rearms on every user interaction; once the window elapses with no activity, the wallet locks itself

## how to lock immediately

1. call \`lock\` - the session AES key is dropped, all pending signing requests reject, dapp bridge broadcasts disconnect to every connected origin

## notes

- the password (or passkey / signature) is **never** persisted to disk as plaintext. the unlock cache holds **derived AES key bytes** (b64) in \`chrome.storage.session\` only - that storage clears when chrome quits the worker
- legacy \`chromatika_unlock_cache_v1_local\` and any cache row containing a \`password\` field are removed on lock / unlock / write
- OS screen-lock (via \`chrome.idle\`) also triggers the wallet lock, regardless of the configured autolock minutes
- service worker cold restarts re-import the cached key bytes back into a non-extractable \`CryptoKey\` and forget them - you don't have to re-enter your password every time chrome unloads the worker
`,kk="# how to view activity and portfolio\n\nsee your transaction history across chains, balance overview, EVM token balances, and per-chain receive addresses. these are read-only queries.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active dWallet has dWallets / addresses on the chains you want to read\n- relevant API keys for fully populated lists (EVM tokens via the active provider; NFTs via Alchemy / Helius; ordinals via Hiro)\n\n## options at a glance\n\n- **activity feed**: aggregated multi-chain tx history (`getActivity`)\n- **portfolio rail balances**: native balances per chain rail (`portfolioRailBalances`)\n- **dWallet home gas**: native gas balance for each dWallet (`getDwalletHomeGas` single, `getDwalletHomeGasMany` batch)\n- **EVM token balances**: ERC20 balances for an address on a chain (`getEvmTokenBalances`)\n- **balance summary**: aggregated balances + price data (`balances`)\n- **per-chain addresses**: getEvmAddress, getSolanaAddress, getAptosAddress, getBtcAddresses\n- **dWallet address book**: active addresses for both curves on the active vault (`dwalletAddressBook`); per-dWallet chain addresses (`getDwalletChainAddresses`)\n\n## how to view activity\n\n1. call `getActivity` with optional `limit` (1-50, default 20)\n2. response is the recent multi-chain tx list - aggregated Sui, Solana, EVM activity sorted by time\n3. Sui activity still reads via `SuiJsonRpcClient.queryTransactionBlocks` because GraphQL doesn't yet expose a filtered / address-scoped list equivalent (tracked future on a Mysten SDK bump)\n\n## how to view portfolio rail balances\n\n1. call `portfolioRailBalances` with `rail` (one of `sui`, `solana`, `aptos`, `btcP2wpkh`, `btcP2tr`) and `address`\n2. response is a single-rail snapshot with native balance for the address on the active network\n\n## how to view dWallet gas balance\n\n- single: `getDwalletHomeGas` with `dwalletId` - returns home row + BTC / EVM / SOL balances for the dWallet\n- batch (up to 48 dWallets): `getDwalletHomeGasMany` with `dwalletIds` (array of length 1-48). **long-running** mutation (20s keepalive) since it sequences per dWallet\n\n## how to view EVM token balances\n\n1. call `getEvmTokenBalances` with `address` and `chainId`\n2. response is the list of ERC20 balances for that address on that chain\n\n## how to view aggregated balance summary\n\n1. call `balances` for the active vault\n2. response includes balances + price data (uses the price waterfall - see [price-source-priority.md](/library/user/price-source-priority))\n3. funding readiness flag (`funding.ready`) is consumed by surfaces that need to know \"is the vault topped up enough for ika ops\"\n\n## how to view receive addresses\n\n- EVM: `getEvmAddress` (returns EIP-55 checksummed)\n- Solana: `getSolanaAddress` (base58)\n- Aptos: `getAptosAddress` (hex)\n- Bitcoin: `getBtcAddresses` with `network` (`'mainnet' | 'testnet'`) - returns both P2WPKH and P2TR\n- both curves on active vault: `dwalletAddressBook` - returns SECP256K1 (EVM + BTC) and ED25519 (Sui + Solana + Aptos) addresses\n- specific dWallet: `getDwalletChainAddresses` with `dwalletId` - returns BTC, EVM, Solana, Sui, Aptos addresses for that dWallet\n\n## notes\n\n- activity is \"best-effort multichain\" - some chains may return empty if the underlying indexer is rate-limited or down. each chain reports independently\n- batch dWallet gas reads are slow on purpose (sequenced) to avoid hammering RPCs from a worker\n- per the project rule, addresses + tx digests in read-only UI link to chain explorers via `ExplorerValueRow` with copy-to-clipboard - the same data exposed by these tRPC reads drives those links\n",bk=`# how to use a WAAP-backed vault

create or unlock a chromatika vault using @human.tech's WAAP (Web Account Abstraction Protocol) - email, phone, or social login that resolves to a Sui address + public key, then signs an envelope-bound challenge to unlock the wallet.

## prerequisites

- a WAAP account on the auth method you want (email, phone, or one of the supported social providers)
- the build can talk to the WAAP service (network access to @human.tech)
- for vault creation: no existing vault (or you're adding a WAAP envelope as a sibling)
- for unlock: the same WAAP credential you used to create the envelope

## options at a glance

- **auth methods**: email, phone, google, discord, twitter, github, bluesky
- **seed source**: the WAAP envelope can carry a Sui-derived seed for the dWallet Vault, or a separate seed source
- **password coexist**: optional - either credential alone unlocks

## how to create a WAAP-backed vault (first vault)

1. complete WAAP login through @human.tech's flow - returns a Sui address + public key
2. submit \`createVaultWaap\` with: optional password, \`waapSuiAddress\`, \`waapPublicKey\`, \`authMethod\` (one of the seven), \`seedSource\`, optional label
3. background registers the envelope, encrypts the vault with the WAAP-derived key, writes \`chromatika_vault_v3\`
4. session unlocks immediately

## how to add a WAAP envelope to an existing vault

1. unlock the wallet (or include the password)
2. submit \`addVaultWaap\` with the same fields
3. the new envelope joins the vault list

## how to unlock with WAAP

1. read \`listVaultEnvelopes\` to find the WAAP envelope
2. complete WAAP login again, ask WAAP to sign the envelope's challenge
3. submit \`unlockVaultWalletSignature\` with envelope id, signature (b64), \`autoLockMinutes\`

## how to recover a WAAP vault

1. recovery uses the BIP39 recovery branch if one was registered (see [recovery-words.md](/library/user/recovery-words))
2. submit \`unlockVaultRecoveryWords\` with the envelope id, the words, \`autoLockMinutes\`

## notes

- WAAP is an external service - if it's down or your account is suspended, you fall back to the recovery branch (if registered) or you cannot unlock that envelope
- you can have multiple WAAP envelopes on the same vault (e.g. email + google) - any one of them unlocks
- the auth method is recorded on the envelope so the unlock UI can pick the right login flow per envelope
- session unlock cache behavior is identical to password / passkey paths - derived key bytes in \`chrome.storage.session\` only, no plaintext
`,Sk=`# how to use WalletConnect for remote signing

WalletConnect is a remote signer transport - phone wallets like Seeker, Phantom, and Solflare can sign for chromatika over the WalletConnect relay. today this is wired specifically for the **x402 Solana payment** path (so the ed25519 key never leaves the phone), and as a generic hardware-sign vendor (\`vendor: 'walletconnect'\`).

## prerequisites

- chromatika is running in popup or side-panel (WC transport touches \`window\` APIs)
- a WalletConnect-compatible phone wallet
- network access to the WalletConnect relay
- your Chromatika vault is unlocked

## options at a glance

- **vendor**: \`walletconnect\`
- **kind**: \`solanaTx\` for x402 payments (today). other WC kinds are open as the surface grows
- **session lifetime**: WC sessions live until revoked; chromatika persists session metadata to skip re-pair

## how to pair a WalletConnect account

1. trigger the WC pairing flow from the popup / side-panel - generates a WC URI
2. render the URI as a QR or hand it to the phone wallet's WC paste input
3. approve the WC session on the phone
4. submit \`addHardwareAccount\` with: \`vendor: 'walletconnect'\`, \`chain\` (\`solana\` for x402), \`address\`, \`ed25519PublicKeyB64\`, the WC session metadata

## how to sign with WalletConnect

1. operations that need a WC signature enqueue a hardware-sign request via \`enqueueHardwareSign({ vendor: 'walletconnect', kind: 'solanaTx' })\`
2. popup opens the hardware-sign route, dispatches the sign request through the WC relay
3. phone wallet receives the request, you approve in-wallet
4. signature returns over the WC session, popup calls \`resolveHardwareSign\`

## how the x402 path uses WalletConnect

when \`session.solanaWcAccount\` is set, the x402 payment flow auto-routes to the WC signer (\`x402-walletconnect-signer.ts\`) instead of the ika MPC signer:

1. fetch interception detects a 402 + payment-required header
2. dispatcher (\`x402-dispatch.ts\`) decides between ika MPC and WC based on \`session.solanaWcAccount\`
3. WC-paired phone signs the versioned tx in its own Seed Vault / signer
4. ika MPC is bypassed entirely - the ed25519 key never leaves the phone

see [x402-payments.md](/library/user/x402-payments) for the full x402 flow

## how to remove a WalletConnect account

1. revoke the WC session in the phone wallet (this is the cleaner path, since the phone holds the session)
2. then call \`removeHardwareAccount\` with the account id to drop the local record

## notes

- WC adds a relay hop vs MWA local / remote, but unlocks wallets that don't support MWA (notably Phantom on iOS, where MWA is Android-only)
- if the WC session expires or is revoked phone-side, signing fails and chromatika surfaces an actionable error - re-pair to recover
- sessions persist in chromatika storage so a chrome restart doesn't force re-pair
`,xk="# how to use x402 HTTP payments\n\nchromatika intercepts page-level `fetch` calls that get back `HTTP 402 Payment Required` with the `payment-required` header, opens an approval popup, signs a USDC payment on Solana, and lets the page retry with a `payment-signature` header. this implements the x402 v2.0 spec, `exact` scheme on Solana, USDC mint only.\n\n## prerequisites\n\n- a Chromatika vault is unlocked\n- the active vault has access to a Solana signer:\n  - **ika MPC path** (default): a Solana ED25519 dWallet exists with USDC on the right network\n  - **WalletConnect path**: a WC-paired phone wallet (Seeker / Phantom / Solflare) is set as the active Solana signer (`session.solanaWcAccount`)\n- the active dapp's page is making fetch calls to a 402-aware endpoint\n- USDC mint: `EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v` (mainnet)\n\n## options at a glance\n\n- **caps**: per-counterparty (per host) daily USD cap, plus a global daily USD cap. defaults: $5 per seller, $25 global. cap-breach pre-popup rejection so the user never sees a popup for an over-budget call\n- **default cap**: per-counterparty cap to assign to **new** hosts when seen for the first time\n- **receipt log**: 200 most recent payments stored in `chromatika_x402_receipts_v1`\n- **quality flag**: thumbs-up / thumbs-down on receipts for future allowlist seeding\n- **signing path**: ika MPC (default) or WalletConnect (when `session.solanaWcAccount` is set)\n\n## how the fetch interception works\n\n1. `inject.ts` installs `x402-fetch-wrapper.ts` for every page\n2. wrapper intercepts `window.fetch`. on a 402 + `payment-required`, the wrapper sends the encoded header to the dispatcher\n3. dispatcher (`x402-dispatch.ts`) decodes `PaymentRequirements`, validates (exact + Solana + USDC), runs `wouldExceedCaps` synchronously - if over budget, **rejects pre-signature** so no popup appears\n4. if within caps, opens approval popup at `?x402approve=<id>` - the **only** signing path is the approve tier\n5. on approval, the right signer runs (ika MPC or WC). dispatcher returns the `payment-signature` header value + receipt id to the wrapper\n6. wrapper retries the original fetch with `payment-signature`. server settles, returns 200 with `payment-response` header\n7. `x402RecordSettlement` (fire-and-forget) flips the receipt status to `settled` with the on-chain digest\n\n## how to set spending caps\n\n- per-counterparty: `x402SetPerCounterpartyCap` with `host` and `capUsd` (set null to remove the explicit cap; null means \"unlimited\" if no global cap, otherwise global applies)\n- global: `x402SetGlobalCap` with `capUsd` (or null to clear)\n- default for new hosts: `x402SetDefaultCap` with `capUsd`\n\n## how to view caps and today's spend\n\n1. call `x402GetCaps`\n2. response includes per-counterparty caps, global cap, default-for-new-hosts cap, and today's USD spend per host + global\n3. day buckets are local-timezone\n\n## how to view payment receipts\n\n1. call `x402ListReceipts` with optional `limit` (1-200; default returns all 200 cached)\n2. response is the receipt list: `{ id, host, amountUsd, status, txHash?, error?, quality, createdAt, ... }`\n3. status values: `pending` (signed, not yet settled) → `settled` (with on-chain hash) / `failed` (with error reason) / `rejected` (user canceled)\n\n## how to flag a receipt as good or bad\n\n1. submit `x402SetReceiptQuality` with `id` and `quality: 'good' | 'bad' | null`\n2. flags persist on the receipt; future allowlist / blocklist seeding will read them\n\n## how to approve or reject a pending payment\n\npopup runs through the standard pending-request pattern:\n\n1. `getPendingX402Request` with id - returns the requirements (host, amount in USD, destination ATA, scheme)\n2. `approvePendingX402` with id - dispatcher signs via the active path (ika MPC or WC), returns the `payment-signature` header to the page\n3. `rejectPendingX402` with id and `reason` (default `'user_canceled'`)\n\n## how to manually post a settlement record\n\nusually the page does this for you, but if you need to wire it manually:\n\n1. call `x402RecordSettlement` with `receiptId` and the page's `paymentResponseHeaderB64`\n2. flips receipt status from `pending` to `settled` (or `failed` if the response indicates an error)\n\n## notes\n\n- x402 today is **Solana USDC only**, `exact` scheme. extending to other chains / schemes is tracked future\n- the bridge methods on the existing dapp port that handle 402 routing without a new transport: `chromatika_x402_handle_402` and `chromatika_x402_record_settlement`\n- the WC signing path is the cleanest mitigation against the \"402-bridge breach\" class - the ed25519 key never leaves the Seeker / phone\n- a real Solana facilitator round-trip has not been exercised end-to-end; the wire format is spec-aligned but you should treat early payments as experimental until a stable facilitator (Coinbase CDP / Second State / x402-rs) is integrated\n- caps are USD-day local-timezone bucketed; a payment at 11:59 pm and one at 12:00 am can both be allowed even though they're \"back-to-back\"\n",Pk=`# 2PC-MPC overview (ika protocol)

ika dWallets are 2-Party Computation MPC threshold wallets. the secret signing key is **never assembled**. instead, two parties (the user via chromatika, and the ika network) each hold a share, and they collaboratively produce signatures without ever materializing the full key. this doc gives the high-level picture; the protocol-step docs (DKG, accept-share, presign, sign, re-encrypt) cover specifics.

## the protocol family

ika implements a 2-party version of threshold ECDSA (for SECP256K1) and threshold EdDSA (for ED25519). the math comes from a body of MPC research; the exact references and security proofs live in the ika protocol papers / docs (see [ika cryptography.mdx](https://github.com/dwallet-labs/ika/blob/main/docs/content/docs/sdk/cryptography.mdx) for the canonical write-up).

key properties:
- **2 parties**: chromatika holds one share (the "user share"), ika network holds the other (the "network share")
- **threshold = 2 of 2**: both parties must cooperate to produce a signature. neither alone can sign
- **non-interactive use**: presign material is precomputed; the actual signing round is fast and online
- **signature is canonical**: the output looks identical to a centrally-produced ECDSA / EdDSA signature - verifiers can't distinguish 2PC-MPC sigs from regular ones

## the user-share encryption keys (USK)

chromatika's local share is wrapped in a \`UserShareEncryptionKeys\` object - the per-vault, per-curve key material that lets the user participate in the MPC protocol. derived from a 32-byte root seed (see [ika-seed-derivation-overview.md](/library/tech/ika-seed-derivation-overview)).

both curves (SECP256K1 + ED25519) get a USK from the same root seed - so one ika identity drives EVM (SECP256K1 ECDSA), Bitcoin (SECP256K1 ECDSA + Taproot Schnorr), Sui (ED25519 EdDSA), Solana (ED25519 EdDSA), and Aptos (ED25519 EdDSA).

## what's stored on-chain (Sui base)

each dWallet has on-chain state on Sui (when ika is anchored to Sui base):
- a \`DWallet\` object with the dWallet id, the current public key, owner cap, state machine state
- presign objects (precomputed signature material, one per future signature)
- encrypted user share objects (the user's share, encrypted to their encryption key, used for share transfer)

## what's stored on-chain (Solana base, pre-alpha)

Solana ika base is pre-alpha. the dWallet state lives in Solana program accounts:
- a dWallet account (analog to the Sui object)
- attestation bytes (\`dwalletAttestationBytesB64\` persisted in chromatika's \`record.dwalletMeta\`)
- presign accounts

**critical pre-alpha caveat**: all Solana ika signatures come from a **single mock signer** today, not real distributed MPC. the dWallet keys, trust model, and signing protocol are not final. the Solana program and all on-chain data will be wiped periodically and **everything will be deleted** when ika transitions to Alpha 1.

## the lifecycle

every dWallet goes through:

1. **DKG (distributed key generation)** - both parties run the keygen protocol, produce their shares, the network publishes the dWallet object. see [ika-dkg-flow.md](/library/tech/ika-dkg-flow)
2. **zero-trust accept-share** - the user's share is encrypted to their encryption key on-chain, the user calls \`acceptEncryptedUserShare\` to take ownership. see [ika-accept-share-zerotrust.md](/library/tech/ika-accept-share-zerotrust)
3. **presign** - the user + network precompute signature material into the per-vault presign pool. see [ika-presign-pool-impl.md](/library/tech/ika-presign-pool-impl)
4. **sign** - on demand, the user + network combine a presign with the message to produce a signature. see [ika-sign-flow.md](/library/tech/ika-sign-flow)
5. **re-encrypt (transfer)** - the user can re-encrypt their share to a new owner's encryption key, transferring the dWallet. see [ika-re-encrypt-transfer.md](/library/tech/ika-re-encrypt-transfer)

## why the indirection

a "normal" wallet has the user hold the full secret key. attacker compromising the user's storage gets the key. ika's 2PC-MPC means an attacker compromising the user gets only **half** - they can't sign without the network. attacker compromising the network gets only the network's half - they can't sign without the user. **both** parties have to be compromised, simultaneously, to forge a signature.

it's similar to threshold-ECDSA in custody applications (Fireblocks, Coinbase Custody) but with the user (via the wallet) as one of the parties instead of all parties being institutional.

## ika curve / signature-algorithm constants

| name | u8 (curve) / sigalgo | use |
|------|----------------------|-----|
| Curve.SECP256K1 = 0 | curve 0 | EVM, BTC |
| Curve.SECP256R1 = 1 | curve 1 | (not used by chromatika today) |
| Curve.ED25519 = 2 | curve 2 | Sui, Solana, Aptos |
| Curve.RISTRETTO = 3 | curve 3 | (not used by chromatika today) |
| SignatureAlgorithm.ECDSASecp256k1 = 0 | algo 0 | EVM, generic ECDSA |
| SignatureAlgorithm.Taproot = 1 | algo 1 | BTC P2TR |
| SignatureAlgorithm.ECDSASecp256r1 = 2 | algo 2 | (not used by chromatika today) |
| SignatureAlgorithm.EdDSA = 3 | algo 3 | Sui, Solana, Aptos |
| SignatureAlgorithm.SchnorrkelSubstrate = 4 | algo 4 | (not used by chromatika today) |

\`fromCurveToNumber\` is **not** exported from \`@ika.xyz/sdk\` main entry (it's internal to \`hash-signature-validation.js\`). hardcode the constants above; don't try to import the helper.

## the BaseChain abstraction (Sui vs Solana)

ika runs on both Sui and Solana. chromatika has \`BaseChain = 'sui' | 'solana'\` and \`getIkaAdapter(session, baseChain)\` in \`@/background/ika/ika-adapter\`. **never call \`session.ikaClient.*\` directly in signing / dWallet flows** - always go through the adapter. this lets the same chromatika code path drive different ika anchor chains without if-statements scattered everywhere.

\`SolanaIkaAdapter\` stubs Sui-only reads (\`getPresignInParticularState\`, \`getEncryptedUserSecretKeyShare\`, \`getSign\`, \`executeTx\`); they throw on Solana base. Solana-base DKG + sign bypass the adapter and go through \`SolanaIkaGrpcClient\` directly.

\`DWalletMeta.baseChain\` is **required** when initializing new entries - \`getIkaAdapter\` reads this.

## where to read more

- ika cryptography: https://github.com/dwallet-labs/ika/blob/main/docs/content/docs/sdk/cryptography.mdx
- ika SDK: \`@ika.xyz/sdk\` (Sui base) and \`@ika.xyz/pre-alpha-solana-client\` (Solana base, pre-alpha)
- chromatika ika adapter: \`wallet-extension/src/background/ika/ika-adapter.ts\`
- chromatika ika operations: \`wallet-extension/src/background/ika/dwallet-lifecycle.ts\`, \`presign-pool.ts\`, \`signing.ts\`
`,_k=`# chromatika tech guides (deep dive)

deep-tech reference for **how** chromatika does what it does. each doc focuses on one technical element / action - exact bytes, exact functions, exact data flows. complement to the [user guides](/library/user/readme) (which describes **what** users can do).

these pages are published on the chromatika site alongside the extension. audience: testers, devs, AI agents driving the wallet via MCP, security reviewers.

chromatika is **pre-release**. Solana ika base is **pre-alpha** (single mock signer; never trust for real value). disclaimers in each doc reflect status.

## crypto primitives

- [argon2id KDF](/library/tech/argon2id-kdf) - the password → AES-key derivation (RFC 9106 §4 second profile)
- [AES-256-GCM vault encryption](/library/tech/aes-gcm-vault) - the vault blob encryption + auth
- [keccak256 uses](/library/tech/keccak256-uses) - ika seed derivation, EVM digest, address derivation
- [SHA-512 + BLAKE2b](/library/tech/sha512-and-blake2b) - the Sui personal-message divergence root cause
- [BIP39 mnemonics](/library/tech/bip39-mnemonic) - 12/24-word phrase generation + validation
- [BIP44 + SLIP10 derivation](/library/tech/bip44-slip10-derivation) - HD paths per chain (EVM / BTC / Sui / Solana / Aptos)
- [secp256k1 ECDSA](/library/tech/ecdsa-secp256k1) - EVM + BTC signing curve
- [ed25519 EdDSA](/library/tech/ed25519-eddsa) - Sui / Solana / Aptos signing curve + RFC 8032 determinism
- [taproot Schnorr (BIP340)](/library/tech/taproot-schnorr) - BTC P2TR signing

## vault encryption + envelopes

- [vault blob v3 format](/library/tech/vault-blob-v3-format) - \`chromatika_vault_v3\` outer + inner shape
- [multi-envelope design](/library/tech/multi-envelope-design) - one masterKey, many credentials (V4)
- [password envelope](/library/tech/password-envelope) - argon2id → envKey → unwrap masterKey
- [passkey PRF envelope](/library/tech/passkey-prf-envelope) - WebAuthn PRF / hmac-secret based
- [wallet-signature envelope](/library/tech/wallet-signature-envelope) - WAAP / Seeker / WC signature based
- [recovery-words envelope](/library/tech/recovery-words-envelope) - BIP39 phrase based
- [cold-SW unlock cache](/library/tech/cold-sw-unlock-cache) - \`chromatika_unlock_cache_v1\` in \`chrome.storage.session\`

## ika \`UserShareEncryptionKeys\` seed derivation

- [overview](/library/tech/ika-seed-derivation-overview) - the four formula variants, per-credential dispatch
- [Sui base + mnemonic](/library/tech/ika-seed-sui-mnemonic)
- [Sui base + private key](/library/tech/ika-seed-sui-private-key)
- [Sui base + passkey (WebAuthn PRF)](/library/tech/ika-seed-sui-passkey)
- [Sui base + WAAP](/library/tech/ika-seed-sui-waap)
- [Sui base + Ledger (key-copy)](/library/tech/ika-seed-sui-ledger-keycopy)
- [Solana base + mnemonic](/library/tech/ika-seed-solana-mnemonic)
- [Solana base + private key](/library/tech/ika-seed-solana-private-key)
- [Solana base + MWA / WalletConnect (signature-derived)](/library/tech/ika-seed-solana-mwa-walletconnect)
- [Solana base + Ledger (key-copy)](/library/tech/ika-seed-solana-ledger-keycopy)
- [Solana base + Lazor](/library/tech/ika-seed-solana-lazor)

## ika MPC operations

- [2PC-MPC overview](/library/tech/2pc-mpc-overview) - the threshold signing model, Sui vs Solana base
- [DKG flow](/library/tech/ika-dkg-flow) - distributed key generation per curve
- [accept-share zero-trust](/library/tech/ika-accept-share-zerotrust) - decrypt + verify before claiming dWallet
- [presign pool implementation](/library/tech/ika-presign-pool-impl) - 3 pools, 5-min refill, per-vault scoping
- [sign flow](/library/tech/ika-sign-flow) - presign + message → 2PC-MPC signature
- [re-encrypt + transfer](/library/tech/ika-re-encrypt-transfer) - move dWallets between users

## chain signing flows

- [EVM transaction send](/library/tech/evm-send-flow) - sendEvmTx + approveTxRequest converge at signAndBroadcastEvm
- [EVM personal_sign + signTypedData_v4](/library/tech/evm-personal-sign-and-typeddata) - the preimage passthrough rule
- [Sui transaction signing via ika](/library/tech/sui-tx-sign-via-ika) - intent + BLAKE2b + ika ED25519
- [Sui personal-message SHA-512 vs BLAKE2b divergence](/library/tech/sui-personal-message-divergence) - the dapp-verify gap
- [Solana transaction signing](/library/tech/solana-tx-sign) - versioned tx + ika ED25519
- [Bitcoin segwit + taproot signing](/library/tech/btc-tx-sign-segwit-taproot) - SECP256K1_ECDSA + SECP256K1_TAPROOT pools
- [Aptos signing](/library/tech/aptos-sign) - ed25519 + signing-message tag
- [signature normalization](/library/tech/signature-normalization) - parseSignatureFromSignOutput + v-recovery

## dapp bridge

- [EIP-1193 + EIP-6963](/library/tech/eip-1193-and-6963) - EVM provider + multi-discovery
- [EIP-3085 + EIP-3326](/library/tech/eip-3085-3326) - chain add / switch
- [Wallet Standard (Sui + Solana)](/library/tech/wallet-standard-sui-and-solana) - non-EVM dapp discovery
- [bridge message validation](/library/tech/dapp-bridge-message-validation) - origin + event.source checks across the page / content-script / SW boundary

## networks + external services

- [price waterfall + sources](/library/tech/price-waterfall-and-sources) - CoinGecko / DefiLlama / CMC / Pyth / Chainlink / GeckoTerminal
- [NFT API providers](/library/tech/nft-api-providers) - Alchemy / Helius / Hiro / Mysten Display / Aptos indexer
- [Aftermath router (Sui swap)](/library/tech/aftermath-router) - REST \`/router/trade/route\` + \`/router/trade/transaction\`
- [SuiGraphQLClient](/library/tech/sui-graphql-client) - the Mysten GraphQL transport + the chunking patch
- [\`@mysten/kiosk\` \`KioskClient\`](/library/tech/mysten-kiosk-client) - kiosk reads
- [\`eth-phishing-detect\`](/library/tech/eth-phishing-detect) - the MetaMask phishing list integration
- [Chainlist search](/library/tech/chainlist-search) - import EVM chains from chainid.network/chains.json

## browser primitives

- [\`chrome.runtime.connect\` (tRPC port)](/library/tech/chrome-runtime-connect-trpc-port) - UI ↔ background bridge
- [\`chrome.runtime.connectNative\` (MCP)](/library/tech/chrome-runtime-connectnative) - extension ↔ native host
- [\`chrome.storage\` local + session](/library/tech/chrome-storage-local-and-session) - persistence model
- [\`chrome.alarms\` + \`chrome.idle\`](/library/tech/chrome-alarms-and-idle) - timers + screen-lock detection
- [\`chrome.declarativeNetRequest\`](/library/tech/chrome-declarativenetrequest) - phishing rules
- [WebHID (popup context)](/library/tech/webhid-popup-context) - Ledger transport constraints

## hardware wallets + MWA

- [MWA 2.0 spec + reflector protocol](/library/tech/mwa-2-spec-and-reflector) - the underlying spec
- [MWA local (Android intent)](/library/tech/mwa-local-android-intent) - same-device transport
- [MWA remote (QR pairing)](/library/tech/mwa-remote-qr-pairing) - desktop ↔ phone transport via reflector
- [Ledger hw-app libraries](/library/tech/ledger-hw-app-libs) - per-chain Ledger drivers
- [Trezor Connect iframe](/library/tech/trezor-connect-iframe) - hosted iframe at connect.trezor.io
- [WalletConnect v2](/library/tech/walletconnect-v2) - relay-based phone pairing (mostly for x402)

## x402 HTTP payments

- [x402 v2 SVM \`exact\` scheme](/library/tech/x402-spec-svm-exact) - the wire spec
- [fetch interception](/library/tech/x402-fetch-interception) - the page-side \`window.fetch\` wrapper
- [Solana tx build](/library/tech/x402-solana-tx-build) - versioned tx + ATA + Memo v2
- [caps + receipts](/library/tech/x402-caps-receipts) - daily USD caps + 200-receipt log

## MCP (Model Context Protocol)

- [MCP protocol overview](/library/tech/mcp-protocol-overview) - chromatika hand-rolls the spec, no \`@modelcontextprotocol/sdk\`
- [chrome native messaging frame](/library/tech/mcp-native-messaging-frame) - 4-byte LE length + UTF-8 JSON
- [HTTP MCP transport](/library/tech/mcp-http-transport) - \`POST /mcp\` on \`127.0.0.1:<port>\`
- [stdio bridge mode](/library/tech/mcp-stdio-bridge) - \`--stdio-bridge\` for Claude Desktop
- [tool routing + correlation](/library/tech/mcp-tool-routing) - 5-min timeout, tool-call ↔ tool-result envelopes
- [bearer token auth](/library/tech/mcp-bearer-token-auth) - per-install token + rotation
- [reconfigure-port live rebind](/library/tech/mcp-reconfigure-port) - stable Claude Desktop config across chrome restarts
- [host spawn + per-OS setup](/library/tech/mcp-host-spawn-and-setup) - \`pnpm setup:native-host\`
- [\`listActiveAlerts\` read-tier tool](/library/tech/mcp-list-active-alerts-tool) - exposes safety alerts to AI agents

## safety alerts subsystem

- [overview](/library/tech/alerts-overview) - signed feed + ed25519 verify + 3 surfaces (banner / notification / dNR)
- [signed-feed format + verify pipeline](/library/tech/alerts-signed-feed-format) - \`SignedAlertV1\` + canonical JSON + ed25519 verify
- [\`publish-alert.mjs\` CLI](/library/tech/alerts-publish-cli) - keygen, sign, build feed, sample
- [poller + actions](/library/tech/alerts-poller-and-actions) - 5-min alarm + chrome notifications + dNR rule lifecycle
- [publisher allowlist](/library/tech/alerts-publisher-allowlist) - bundled \`BUNDLED_PUBLISHERS\` + revision bumps

## encrypted activity notes + tx-record + encryption module

- [signed-tx record + activity feed merge](/library/tech/signed-tx-record) - \`chromatika_signed_txs_v1\` per-vault FIFO 500
- [\`EncryptionBackend\` abstraction](/library/tech/encryption-backend-abstraction) - multi-backend interface (encrypt-xyz / direct-ed25519 stub / seal stub)
- [activity-notes encrypt + decrypt flows](/library/tech/activity-notes-encrypt-decrypt) - K-wrap via 2× EUint128 + AES-GCM body, 4 tRPC procedures

## Encrypt.xyz integration

- [pre-alpha overview](/library/tech/encrypt-pre-alpha-overview) - what we use, the disclaimer
- [17-byte canonical format](/library/tech/encrypt-17-byte-canonical-format) - \`[fhe_type(1) || value_le(16)]\` (the published-SDK-bug workaround)
- [gRPC-web fetch transport](/library/tech/encrypt-grpc-web-fetch-transport) - HTTP/1.1 wrapping of gRPC
- [protobuf wire codec](/library/tech/encrypt-protobuf-wire) - hand-rolled with \`@bufbuild/protobuf\` BinaryWriter
- [CreateInput flow](/library/tech/encrypt-create-input) - the encrypt path
- [ReadCiphertext (signed)](/library/tech/encrypt-read-ciphertext-signed) - the reveal path with ed25519 sig
- [multi-chunk labels](/library/tech/encrypt-multi-chunk-labels) - 1-64 utf-8 → 1-4 EUint128 chunks
- [on-chain status polling](/library/tech/encrypt-on-chain-status-polling) - the 4s \`verified\` / \`encrypting\` / \`missing\` pill

## other tech + internals

- [buffer polyfill](/library/tech/buffer-polyfill) - Node Buffer shim for browser
- [bitcoinjs-lib usage](/library/tech/bitcoinjs-lib-usage) - tx building + sighash + addresses
- [\`@noble/*\` + \`@scure/*\` crypto libs](/library/tech/noble-and-scure-crypto-libs) - the audit-grade primitive set
- [\`@mysten/sui\` pinning + patches](/library/tech/mysten-sui-pinning-and-patches) - 2.16.0 override + GraphQL chunking patch + postinstall
- [WebAuthn PRF / hmac-secret](/library/tech/webauthn-prf-hmac-secret) - the deterministic 32-byte secret extension
- [WAAP determinism probe](/library/tech/waap-determinism-probe) - sign-twice-and-compare at pairing
- [session state + multi-vault](/library/tech/session-state-multi-vault) - what's unlocked + how switchVault works
`,Ek=`# activity notes encrypt + decrypt flows

per-tx encrypted user notes. plaintext up to 2048 utf-8 bytes. encrypted via the \`EncryptionBackend\` abstraction (today: \`encrypt-xyz\` backend, self-recipient only). the encrypted ref persists on the local \`SignedTxRecord\` at \`chromatika_signed_txs_v1\`. tRPC procedures: \`encryptActivityNote\`, \`decryptActivityNote\`, \`removeActivityNote\`, \`getActivityNoteStatus\`.

## the four tRPC procedures

\`\`\`ts
// router: src/server/routers/activity-notes.ts

// encrypt + attach to an existing tx record
encryptActivityNote: mutation
  input: { txHash: string, plaintext: string }   // plaintext.min(1).max-utf8(2048)
  output: { ok: true, backend: string, createdAtMs: number }
  errors: 'no-record' | 'plaintext-empty' | 'plaintext-too-long' | EncryptionBackendError

// decrypt (mutation despite read-shape because MPC sign is required)
decryptActivityNote: mutation
  input: { txHash: string }
  output:
    | { plaintext: string, status: 'ok' }
    | { plaintext: null, status: 'none' }              // record exists, no note attached
    | { plaintext: null, status: 'error', errorReason: string, errorMessage: string }

// drop the encrypted ref (idempotent)
removeActivityNote: mutation
  input: { txHash: string }
  output: { ok: true, removed: boolean }

// presence check, no decrypt cost
getActivityNoteStatus: query
  input: { txHash: string }
  output:
    | { hasRecord: false, hasNote: false }
    | { hasRecord: true, hasNote: false, backend: null, createdAtMs: null, origin: string | null, kind: string }
    | { hasRecord: true, hasNote: true, backend: string, createdAtMs: number, origin: string | null, kind: string }
\`\`\`

router mounted in \`src/server/router.ts\` line 37 via \`...activityNotesProcedures\`.

## the encrypt flow

\`\`\`
1. tRPC encryptActivityNote({ txHash, plaintext })

2. fetch the local SignedTxRecord
   const rec = await getSignedTxByHash(txHash, sessionState.activeVaultId);
   if (!rec) throw 'no-record';

3. validate plaintext
   const utf8 = new TextEncoder().encode(plaintext);
   if (utf8.length === 0) throw 'plaintext-empty';
   if (utf8.length > 2048) throw 'plaintext-too-long';

4. resolve backend
   const backend = getEncryptionBackend('self-recipient-default');
   // → encryptXyzBackend (today)

5. encrypt via the backend
   const ref = await backend.encryptForRecipient(utf8, { kind: 'self' });
   // backend internally:
   //   a. generates K (32 random bytes), iv (12 random bytes)
   //   b. AES-GCM encrypts utf8 with K + iv
   //   c. splits K into halfA (16 bytes) + halfB (16 bytes)
   //   d. mock-encrypts each half to 17-byte canonical (FHE_TYPE_EUINT128 || halfBytesLE16)
   //   e. encrypt.xyz CreateInput batched call (one gRPC, two inputs)
   //   f. returns ref = {
   //        backend: 'encrypt-xyz',
   //        payload: {
   //          bodyCiphertextB64, bodyIvB64,
   //          wrappedKeyCiphertextIdHexes: [hexA, hexB],
   //          recipientPubkeyB64: base64Encode(activeDwalletEd25519Pubkey),
   //          recipientLockedAtVaultId: activeVaultId,
   //          programIdB58, fheTypePerHalf: 5,
   //        },
   //        createdAtMs: Date.now(),
   //      }
   //   g. zeroes K

6. patch the record
   await updateSignedTxNote(txHash, sessionState.activeVaultId, ref);

7. return { ok: true, backend: 'encrypt-xyz', createdAtMs: ref.createdAtMs }
\`\`\`

cost: one MPC \`approve_message\` gRPC call (the encrypt.xyz \`CreateInput\` carries an \`approve_message\` from the in-extension fee-payer keypair). milliseconds + tiny SOL fee.

note: encrypt **doesn't** need an ika MPC sign. only the network's \`approve_message\` (signed by the in-extension fee-payer keypair, no ika round-trip). that's why encrypt is fast (~500ms) while decrypt is slow (~1-3s).

## the decrypt flow

\`\`\`
1. tRPC decryptActivityNote({ txHash })

2. fetch the local SignedTxRecord
   const rec = await getSignedTxByHash(txHash, sessionState.activeVaultId);
   if (!rec) return { plaintext: null, status: 'none' };
   if (!rec.encryptedNote) return { plaintext: null, status: 'none' };
   const ref = rec.encryptedNote;

3. dispatch by backend
   try {
     const plaintext = await decryptRefViaRegistry(ref);
     // for encrypt-xyz, this:
     //   a. validates activeVaultId === ref.payload.recipientLockedAtVaultId
     //      throws 'wrong-vault' if not
     //   b. validates active dWallet ed25519 pubkey === ref.payload.recipientPubkeyB64
     //      throws 'wrong-vault' if not
     //   c. for each of the two wrappedKeyCiphertextIdHexes:
     //      - encodeReadCiphertextMessage(0, idBytes, empty, 0n)  // BCS layout
     //      - signMessageSol(message)  // ika MPC ED25519 sign
     //      - encryptGrpcReadCiphertext(GRPC_URL, encodeReadCiphertextRequest({
     //          message, signature: sigBytes, signer: pubkeyBytes
     //        }))
     //      - decode response, extract 16-byte plaintext value (the AES key half)
     //   d. concat halfA + halfB → K (32 bytes)
     //   e. importKey('raw', K, AES-GCM, non-extractable, ['decrypt'])
     //   f. AES-GCM decrypt body with K + iv
     //   g. zero K
     //   h. return plaintext bytes

     return { plaintext: new TextDecoder('utf-8', { fatal: false }).decode(plaintext), status: 'ok' };
   } catch (e: any) {
     if (e instanceof EncryptionBackendError) {
       return { plaintext: null, status: 'error', errorReason: e.info.reason, errorMessage: e.info.message };
     }
     return { plaintext: null, status: 'error', errorReason: 'unknown', errorMessage: String(e) };
   }
\`\`\`

cost: **two ika MPC ED25519 signs** + two encrypt.xyz \`ReadCiphertext\` gRPC calls. each MPC sign consumes one ED25519_EDDSA presign. total 1-3 seconds on devnet.

the two reads are **sequential**, not parallel. could in principle parallelize (each is independent gRPC), but pre-alpha encrypt.xyz throughput is fine and keeping the code linear is simpler.

## why decrypt is a mutation

tRPC convention: queries are read-only (no side effects). decrypt **costs a presign** (consumes signing material), so it's effectively a state-mutating operation. classifying as \`mutation\` lets the tRPC client cache differently and prevents accidental retry loops on UI re-render.

## the remove flow

\`\`\`
1. tRPC removeActivityNote({ txHash })
2. const removed = await updateSignedTxNote(txHash, sessionState.activeVaultId, null);
3. return { ok: true, removed }
\`\`\`

drops the \`encryptedNote\` field from the record. no on-chain action - the encrypt.xyz ciphertext stays on chain (and may eventually expire / get wiped on devnet).

idempotent: removing an already-removed note returns \`{ ok: true, removed: false }\`. safe to call without checking.

## the presence-check (\`getActivityNoteStatus\`)

lightweight query, no decrypt. used by:
- the activity feed merge to set \`item.hasEncryptedNote\`
- the note-edit modal to know "should I show 'view' or '+ note'"
- any UI that wants to reflect note presence without paying decrypt cost

\`\`\`ts
async function getActivityNoteStatus({ txHash }): Promise<...> {
  const rec = await getSignedTxByHash(txHash, sessionState.activeVaultId);
  if (!rec) return { hasRecord: false, hasNote: false };
  if (!rec.encryptedNote) return {
    hasRecord: true, hasNote: false,
    backend: null, createdAtMs: null,
    origin: rec.origin, kind: rec.kind,
  };
  return {
    hasRecord: true, hasNote: true,
    backend: rec.encryptedNote.backend,
    createdAtMs: rec.encryptedNote.createdAtMs,
    origin: rec.origin, kind: rec.kind,
  };
}
\`\`\`

returns the \`origin\` + \`kind\` fields too, since the UI often wants those alongside the note status (e.g. "this is a uniswap.org swap with a note attached").

## the UI integration

\`src/ui/components/NoteEditModal.tsx\`:

three states based on note presence + decrypt outcome:
- **no note yet** (or user clicked edit): textarea with "encrypt + save" button + utf-8 byte counter
- **note locked**: "decrypt to view" + "remove" buttons; pre-alpha disclaimer
- **note decrypted**: read-only plaintext display + "edit" + "remove" buttons

modal opens on click of "+ note" or "view note" buttons in \`ActivityPage.tsx\`. on mount, calls \`getActivityNoteStatus\` (lightweight). user-triggered actions call \`encryptActivityNote\` / \`decryptActivityNote\` / \`removeActivityNote\`.

\`src/ui/components/EncryptedNoteBadge.tsx\`:

lock icon (12px) rendered inline next to the tx label in the activity feed. visible only when \`item.hasEncryptedNote === true\`. purely visual; click handler is on the parent activity row.

## error handling

\`EncryptionBackendError\` reasons surface specifically:
- \`wrong-vault\` - "switch to <recipientLockedAtVaultId> to read this note"
- \`not-implemented\` - shouldn't happen for encrypt-xyz (only stubs throw this)
- \`aes-gcm-fail\` - "this note's ciphertext is corrupted; try removing + re-encrypting"
- \`gRPC error\` - "encrypt.xyz unavailable; try again"
- \`devnet-wipe\` - "this note's on-chain ciphertext was lost in a devnet wipe; clear + re-encrypt"

the UI maps these to specific copy strings rather than showing raw error.message.

## the pre-alpha disclaimer obligation

every note surface (modal, badge tooltip, settings copy) must include the encrypt.xyz pre-alpha disclaimer per CLAUDE.md and project status doc. ciphertexts may be plaintext on-chain in this phase. treat as **dev preview**, not production secrecy. do not put real secrets in notes.

## library

- internal: \`src/server/routers/activity-notes.ts\` for the four tRPC procedures
- internal: \`src/background/services/tx-record.ts\` for \`getSignedTxByHash\`, \`updateSignedTxNote\`
- internal: \`src/background/encryption/registry.ts\` for \`getEncryptionBackend\`, \`decryptRefViaRegistry\`
- internal: \`src/background/encryption/encrypt-xyz-backend.ts\` for the backend impl
- internal: \`src/ui/components/NoteEditModal.tsx\`, \`EncryptedNoteBadge.tsx\`

## related

- [signed-tx-record.md](/library/tech/signed-tx-record) - the \`SignedTxRecord\` store + \`EncryptedRef\` location
- [encryption-backend-abstraction.md](/library/tech/encryption-backend-abstraction) - the backend interface
- [encrypt-create-input.md](/library/tech/encrypt-create-input) - encrypt.xyz wrap layer (used by encrypt path)
- [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed) - encrypt.xyz unwrap layer (used by decrypt path)
- [activity-notes.md](/library/user/activity-notes) (user-guides) - the user-facing flow
`,Ck="# AES-256-GCM vault encryption\n\nevery chromatika vault blob (`chromatika_vault_v3` in `chrome.storage.local`) is sealed under AES-256 in GCM mode using the key derived from argon2id (see [argon2id-kdf.md](/library/tech/argon2id-kdf)). all signing material - mnemonics, imported private keys, ika user-share encryption keys, hardware-vault auth signatures, fee-payer keypairs - lives inside this single sealed blob.\n\n## why GCM and not CBC / CTR\n\nGCM is an authenticated encryption mode. it produces ciphertext **plus** a 16-byte authentication tag (Wegman-Carter MAC over GHASH) so we get confidentiality + integrity in one primitive. CBC needs a separate HMAC, CTR has no auth at all. GCM also parallelizes well and chrome's webcrypto offers it natively, so the implementation is `crypto.subtle.encrypt({ name: 'AES-GCM', iv, additionalData? }, key, plaintext)` - no JS-side crypto needed.\n\n## key bits\n\n- algorithm: AES-256 (256-bit key, 14 rounds)\n- mode: GCM\n- IV: **96 bits** = 12 bytes, NIST-recommended length. generated fresh via `crypto.getRandomValues` for **every** seal. never reused under the same key (IV reuse in GCM is catastrophic - leaks the auth key)\n- tag: 128 bits = 16 bytes appended to ciphertext (default web crypto behavior)\n- AAD (additional authenticated data): unused today. could carry vault-format version bytes in a future bump if we want format-confusion resistance baked in\n\n## the sealed payload\n\nthe plaintext that goes into AES-GCM is the JSON-encoded `{ v: 3, vaults: VaultRecord[], activeVaultId }`. each `VaultRecord` is a per-vault bundle: id, label, base chain, mnemonic (if any), `suiPrivateKeyBech32` / `solanaSecretKeyB64` (if imported), `ikaShareKeysB64` (per-curve user-share encryption keys), passkey artifacts, hardware metadata, multi-envelope state.\n\n## the unwrap\n\nunlock walks: argon2id(password, salt) → 32-byte raw key → `subtle.importKey('raw', keyBytes, { name: 'AES-GCM' }, false, ['decrypt'])` → non-extractable `CryptoKey`. then `subtle.decrypt({ name: 'AES-GCM', iv }, key, ciphertext)` returns the plaintext bytes; JSON.parse, hand the records to the session. if the auth tag doesn't verify (wrong password, tampered blob), `decrypt` throws synchronously - we never see ciphertext fragments.\n\n## legacy v2 reject\n\nchromatika **does not migrate** older PBKDF2 + AES-GCM blobs (`chromatika_vault_v2`). on parse, v2 is rejected; users clear extension storage and re-onboard. pre-release means no migration cost from this rule, and it kills the kind of \"we tried v2 first, fell back to v3\" code path that historically introduces format-confusion vulnerabilities.\n\n## storage key naming\n\n`chromatika_vault_v3` lives in `chrome.storage.local` (persists across browser restarts but cleared on uninstall). bumping the schema means bumping the integer suffix - convention is documented in CLAUDE.md.\n\n## the unlock cache (session-only)\n\nseparately, `chromatika_unlock_cache_v1` lives in `chrome.storage.session` only. it stores the **post-argon2id derived AES key bytes** (b64) plus KDF meta (salt, params) so a cold service worker can re-import them as a non-extractable `CryptoKey` and skip the argon2id round. **never** the password. **never** in `chrome.storage.local`. legacy `chromatika_unlock_cache_v1_local` and any row containing a `password` field are removed on lock / unlock / write. see [cold-sw-unlock-cache.md](/library/tech/cold-sw-unlock-cache).\n",Ak=`# Aftermath router (Sui swap)

chromatika's **Phase B** SUI → IKA swap is powered by Aftermath's REST router API. zero new npm deps - just \`fetch()\`. fetch a quote at \`/router/trade/route\`, then build the tx at \`/router/trade/transaction\`, deserialize via \`Transaction.from(...)\`, sign with the HD fee-payer keypair, broadcast on the active Sui RPC.

## the call sequence

\`\`\`
1. swapStatus / phaseBFundingSpike    - is swap available + active?
2. swapQuote { amountInMist?, slippageBps? }   - get a quote (cached 30s)
3. executeSwap { quoteId, quote }     - build, sign, broadcast (long-running)
\`\`\`

## the quote call

\`\`\`ts
async function getAftermathQuote({ amountInMist, slippageBps }) {
  const params = new URLSearchParams({
    coin_in: SUI_TYPE,                                      // '0x2::sui::SUI'
    coin_out: IKA_TYPE,                                     // ika coin object type
    amount_in: amountInMist.toString(),
    slippage: (slippageBps / 100).toString(),               // basis points → percent
  });
  const resp = await fetch(\`https://api.aftermath.finance/router/trade/route?\${params}\`);
  const route = await resp.json();
  return {
    quoteId: route.id,
    amountIn: BigInt(route.amount_in),
    amountOut: BigInt(route.amount_out),
    priceImpact: route.price_impact,
    routeDescription: route.route,
    expiresAtMs: Date.now() + QUOTE_CACHE_TTL_MS,           // 30_000 ms
  };
}
\`\`\`

returns the route description plus expected output amount + price impact. the route may go through multiple pools (e.g. SUI → wUSDC → IKA) - Aftermath optimizes.

## the build call

\`\`\`ts
async function buildAftermathTx({ quoteId, quote, sender }) {
  const resp = await fetch('https://api.aftermath.finance/router/trade/transaction', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      route_id: quoteId,
      walletAddress: sender,
      slippage: quote.slippageBps / 100,
    }),
  });
  const data = await resp.json();
  return data.tx_bytes_b64;   // base64-encoded serialized Sui Transaction
}
\`\`\`

response is a base64-encoded serialized PTB. chromatika deserializes via \`Transaction.from(base64Decode(tx_bytes_b64))\`, signs with the HD fee-payer Sui keypair, broadcasts.

## the execute path

\`\`\`ts
async function executeSwap({ quoteId, quote }) {
  const txBytes = await buildAftermathTx({ quoteId, quote, sender: hdFeePayerAddress });
  const tx = Transaction.from(base64Decode(txBytes));

  const sigResult = await suiKeypair.signTransaction(tx, suiGraphQLClient);
  // suiKeypair is the HD fee-payer Ed25519 keypair, NOT a dWallet
  // it signs locally - not via ika MPC

  const result = await suiGraphQLClient.executeTransactionBlock({
    transactionBlock: tx,
    signature: sigResult.signature,
    options: { showEffects: true, showObjectChanges: true },
  });

  return { digest: result.digest, status: result.effects?.status };
}
\`\`\`

note this signs with the **HD fee-payer keypair**, not a dWallet. swaps fund the fee-payer, not the user identity - so signing them with the HD key (which lives in extension memory) is consistent with the fee-payer's role.

## why HD fee-payer signs (not dWallet)

the swap moves SUI **from the HD fee-payer's address** to receive IKA **at the HD fee-payer's address**. it's a self-transfer through Aftermath's router. the HD fee-payer is the natural signer because:
- it's the address holding the SUI to swap
- it's the address that should receive the IKA
- ika MPC PTBs need IKA to fund DKG / presign / sign; this swap is the "where does that IKA come from" answer

if dWallet-anchored swaps were the design (swap user-facing SUI from a dWallet), that'd be a different flow.

## constants

\`\`\`ts
MIN_SUI_RESERVE_MIST = 50_000_000n;   // 0.05 SUI minimum reserve in fee-payer
DEFAULT_SLIPPAGE_BPS = 100;            // 1% default slippage
QUOTE_CACHE_TTL_MS = 30_000;           // quote stale after 30s
\`\`\`

\`MIN_SUI_RESERVE_MIST\` is a hard floor - the wallet won't let you swap so much that the fee-payer drops below 0.05 SUI. otherwise the next ika op might fail to pay gas.

\`DEFAULT_SLIPPAGE_BPS = 100\` (1%) is the default; user can override 10-500 bps (0.1%-5%) per call.

## the long-running mutation

\`executeSwap\` is a **long-running tRPC mutation** with a 20-second keepalive (rather than the default 12s timeout exempted in \`src/lib/trpc.ts\`). swaps can take longer because:
- Aftermath's \`/router/trade/transaction\` builds the PTB (slow if the route has many hops)
- network latency to Sui's GraphQL endpoint
- on-chain confirmation can be a few seconds

the 20s keepalive prevents the tRPC port from auto-disconnecting mid-swap.

## degraded modes

- **Aftermath unreachable**: \`swapQuote\` throws "swap service unavailable"; user sees an error and can retry
- **IKA pool empty / no liquidity** (e.g. fresh testnet deploy): quote returns \`amount_out: 0\`. UI surfaces "no liquidity" with a manual-fund fallback (send IKA from another source)
- **slippage exceeded**: tx aborts on-chain at the actual swap step. user sees a tx error; retry with higher slippage
- **stale quote**: if \`executeSwap\` is called >30s after quote, the quote may no longer reflect current pool state. Aftermath may build a tx that fails. user re-quotes

## feature flag

\`\`\`ts
const enabled = import.meta.env.VITE_PHASE_B_SUI_SWAP === 'true';   // default true today
\`\`\`

\`VITE_PHASE_B_SUI_SWAP\` is the build-time gate. default is true; can be disabled by setting \`'false'\` in env.

## library

- \`fetch\` (browser native)
- \`@mysten/sui\` \`Transaction.from\`, \`Ed25519Keypair\`, \`SuiGraphQLClient\`, \`executeTransactionBlock\`
- internal: \`wallet-extension/src/background/services/swap.ts\` \`swapQuote\`, \`executeSwap\`
- internal: \`wallet-extension/src/background/services/swap-config.ts\` for constants

## related

- [sui-tx-sign-via-ika.md](/library/tech/sui-tx-sign-via-ika) - the dWallet path (different from the HD fee-payer path used here)
- [sui-graphql-client.md](/library/tech/sui-graphql-client) - the GraphQL transport for execution
- [sui-ika-swap.md](/library/user/sui-ika-swap) (user-guides) - the user-facing flow
`,Ik=`# safety alerts subsystem overview

chromatika polls a signed JSON feed for in-the-wild attack reports (phishing, drainers, scam dapps), verifies each alert's ed25519 signature against a bundled publisher allowlist, and surfaces verified alerts via three layers: in-app banner, chrome notifications (critical), and chrome's \`declarativeNetRequest\` URL-blocking layer (critical with affected domains). complementary to the existing \`eth-phishing-detect\` MetaMask blocklist; non-overlapping rule ID range so the two coexist.

shipped per \`wallet-extension/docs/STATUS.md\` lines 91-101 with a pre-launch TODO to replace the placeholder dev publisher pubkey with the production key.

## the three severity levels

| severity | banner | chrome notification | dNR redirect rule |
|----------|--------|---------------------|-------------------|
| \`critical\` | ✓ | ✓ (unless muted) | ✓ for each affected domain |
| \`warning\` | ✓ | ✓ (unless muted) | ✗ |
| \`info\` | ✓ | ✗ | ✗ |

example uses:
- **critical**: phishing Uniswap clone with approval-draining contracts; token-account drain vector on Solana
- **warning**: sketchy NFT mint sites; suspicious DeFi protocols
- **info**: educational reminders ("revoke unused approvals periodically")

## the file layout

| file | role |
|------|------|
| \`src/background/alerts/alerts-types.ts\` | \`SignedAlertV1\` type + canonical JSON serializer |
| \`src/background/alerts/alerts-store.ts\` | persistence to \`chromatika_alerts_v1\`, dedup, dismiss |
| \`src/background/alerts/alerts-fetch.ts\` | HTTP fetch + per-alert verification pipeline |
| \`src/background/alerts/alerts-verify.ts\` | ed25519 signature verify + time-bounds + publisher allowlist |
| \`src/background/alerts/alerts-actions.ts\` | chrome.notifications + dNR rule append + per-rule TTL alarms |
| \`src/background/alerts/alerts-poller.ts\` | 5-min alarm + manual trigger |
| \`src/background/alerts/alerts-publishers.ts\` | bundled allowlist (\`BUNDLED_PUBLISHERS\`) + revision number |
| \`src/server/routers/alerts.ts\` | tRPC procedures (list / dismiss / settings / trigger / inject) |
| \`src/ui/components/AlertBanner.tsx\` | persistent banner in \`MainWalletShell\` |
| \`src/ui/components/AlertsSettingsSection.tsx\` | settings UI (mute / opt-out / custom feed / history) |
| \`scripts/publish-alert.mjs\` | CLI for generating keys, signing alerts, building feeds |

mount points:
- \`src/server/router.ts\` line 38: \`...alertsProcedures\` joins the tRPC root
- \`src/background/index.ts\` lines 113-185: alarm dispatchers + bootstrap

## the storage shape (\`chromatika_alerts_v1\`)

\`\`\`ts
interface AlertsState {
  v: 1,
  knownAlerts: SignedAlertV1[],           // verified alerts ever seen, capped 200, FIFO by timestamp
  dismissedIds: string[],                 // user-dismissed alert ids
  settings: {
    muted: boolean,                       // suppress notifications + banner (NOT dNR rules)
    customFeedUrl: string,                // empty = default
    optedOut: boolean,                    // poller stops; dNR rules clear on TTL
  },
  lastPolledAtMs: number,
  lastPollError: string | null,
  publishersRevision: number,             // bumped on allowlist rotation; triggers re-verify
}
\`\`\`

note: a separate \`chromatika_alerts_applied_rules_v1\` storage tracks \`{ alarmToRuleId: { alarmName → ruleId } }\` for granular per-rule TTL cleanup.

## the data flow

\`\`\`
1. publisher signs an alert offline:
   publish-alert.mjs sign --priv <b64> --in unsigned.json --out signed.json
   - canonicalizes JSON (key-sorted, undefined dropped)
   - ed25519_sign(canonical_bytes, priv_key) → signatureB64

2. publisher composes a feed:
   publish-alert.mjs feed --in signed-array.json --out feed.json
   - wraps as { v: 1, generatedAtMs, alerts: [...] }

3. publisher hosts feed at https://chromatika.dev/safety-alerts.json (or wherever)

4. chromatika polls every 5 min:
   chrome.alarms 'chromatika-alerts-poll'
   → fetchAndVerifyFeed(feedUrl)
     → HTTP GET (anonymous, 25s timeout)
     → parse JSON, validate shape with Zod
     → per-alert: verifySignedAlert (signature + allowlist + time-bounds)
     → drops bad alerts, keeps the good ones
   → mergeNewAlerts(verified)  // dedup by id, cap at 200
   → for each new alert: runNewAlertActions(alert, muted)
     → critical: append dNR redirect rules + chrome notification (unless muted)
     → warning: chrome notification (unless muted)
     → info: banner-only (no notification, no dNR)
   → setLastPollOutcome({ atMs, error })

5. UI surfaces:
   - AlertBanner (refetches listAlerts every 30s, shows highest-severity active)
   - AlertsSettingsSection (mute / opt-out / custom URL / history / dev injection)
   - chrome notifications (clicking opens side panel with ?alertId=<id>)
   - dNR rule (typing flagged URL redirects to phishing-warning.html?blocked=<domain>&alertId=<id>)
\`\`\`

## privacy properties

- feed fetch is anonymous HTTP GET. no cookies, no auth headers, no wallet-specific params
- the feed is **static JSON** - same URL for all users, same content regardless of who's polling. no per-user response variation possible
- ISP / DNS resolvers can see "this user polls chromatika.dev every 5 min" but not what's in the wallet
- dNR rules are local; no phone-home on navigation to flagged domains

## composition with eth-phishing-detect

| | eth-phishing-detect | chromatika alerts |
|--|---------------------|-------------------|
| source | bundled list + daily MetaMask config refresh | signed feed, 5-min poll |
| signing | none (trust upstream maintainer) | ed25519 per alert |
| dNR rule IDs | 1-4900 | 10000-19999 |
| severity | one tier (block) | three tiers (critical/warning/info) |
| in-app surface | none | banner + settings + notifications |
| TTL | full sync each refresh | per-alert \`expiresAtMs\` |

both layers operate in parallel without collision. eth-phishing-detect catches the long-tail of known-bad domains; chromatika alerts catch zero-day campaigns or chromatika-specific threats.

## todo before launch

per STATUS.md:

- replace \`PLACEHOLDER_DEV_PUBLISHER_PUBKEY_B64\` in \`alerts-publishers.ts\` with the production chromatika-team pubkey
- run \`node scripts/publish-alert.mjs --gen-key\`, save the privkey somewhere safe (hardware wallet, 1Password), paste pubkey into \`alerts-publishers.ts\`
- bump \`BUNDLED_PUBLISHERS_REVISION\` so cached alerts re-verify against the new allowlist

future hardening:
- on-chain Sui Move \`BroadcastChannel\` object + \`PublisherCap\` registry (decentralize allowlist)
- Walrus body storage for long-form alerts
- soft-block dapp-bridge \`window.ethereum\` connect on flagged sites (complementary to dNR)

## related deep dives

- [alerts-signed-feed-format.md](/library/tech/alerts-signed-feed-format) - SignedAlertV1 shape + canonical JSON + ed25519 verify pipeline
- [alerts-publish-cli.md](/library/tech/alerts-publish-cli) - publish-alert.mjs CLI usage
- [alerts-poller-and-actions.md](/library/tech/alerts-poller-and-actions) - polling schedule + notifications + dNR rule lifecycle
- [alerts-publisher-allowlist.md](/library/tech/alerts-publisher-allowlist) - bundled allowlist + revision bumps
- [mcp-list-active-alerts-tool.md](/library/tech/mcp-list-active-alerts-tool) - the new MCP read-tier tool that exposes alerts to AI agents
- [safety-alerts.md](/library/user/safety-alerts) (user-guides) - the user-facing flow
`,Tk=`# alerts poller + actions (notifications + dNR rules)

the alerts subsystem polls the feed every 5 minutes via \`chrome.alarms\`, verifies new alerts, then fires three side-effect categories: in-app banner state update, chrome notifications (critical + warning), and \`declarativeNetRequest\` redirect rules (critical with affected domains). per-rule TTL alarms clean up dNR rules when alerts expire.

## the poll alarm

\`\`\`ts
const ALERTS_POLL_PERIOD_MIN = 5;

chrome.alarms.create('chromatika-alerts-poll', { periodInMinutes: 5 });

chrome.alarms.onAlarm.addListener(alarm => {
  if (alarm.name === 'chromatika-alerts-poll') {
    void runAlertsPoll();
  }
});
\`\`\`

set up at install + at SW startup. \`ensureAlertsPollAlarm\` in \`alerts-poller.ts\` is idempotent - calling repeatedly doesn't create duplicates.

manual trigger via tRPC \`triggerAlertPoll\` for the settings page refresh button.

## \`runAlertsPoll()\`

\`\`\`ts
async function runAlertsPoll(): Promise<{ newAlerts: number, drops: number, error: string | null }> {
  const state = await getAlertsState();
  if (state.settings.optedOut) {
    return { newAlerts: 0, drops: 0, error: 'opted out' };
  }

  let result;
  try {
    result = await fetchAndVerifyFeed(resolveFeedUrl(state));
  } catch (e) {
    await setLastPollOutcome({ atMs: Date.now(), error: e.message });
    return { newAlerts: 0, drops: 0, error: e.message };
  }

  const { verified, drops } = result;
  const newAlerts = await mergeNewAlerts(verified);

  for (const alert of newAlerts) {
    await runNewAlertActions(alert, state.settings.muted);
  }

  await setLastPollOutcome({ atMs: Date.now(), error: null });
  return { newAlerts: newAlerts.length, drops: drops.length, error: null };
}
\`\`\`

never throws. exceptions bubble up to the caller as \`error: string\`. the alarm handler catches and logs but doesn't propagate further (would kill the SW).

## \`fetchAndVerifyFeed(feedUrl)\`

\`\`\`ts
async function fetchAndVerifyFeed(feedUrl: string): Promise<FetchResult> {
  // 1. HTTP GET with 25s timeout
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), 25_000);
  const resp = await fetch(feedUrl, { signal: controller.signal });
  clearTimeout(timer);

  if (!resp.ok) throw new Error(\`feed HTTP \${resp.status}\`);

  // 2. parse + Zod validate the feed envelope shape
  const feed = AlertsFeedSchema.parse(await resp.json());
  // { v: 1, generatedAtMs, alerts: unknown[].max(500) }

  // 3. per-alert verify
  const verified: SignedAlertV1[] = [];
  const drops: VerifyDrop[] = [];
  const nowMs = Date.now();

  for (let i = 0; i < feed.alerts.length; i++) {
    const result = await verifySignedAlert(feed.alerts[i], nowMs);
    if (result.ok) verified.push(result.alert);
    else drops.push({ index: i, reason: result.reason, detail: result.detail });
  }

  return { verified, drops, generatedAtMs: feed.generatedAtMs };
}
\`\`\`

per-alert verification means **one bad alert doesn't poison the batch**. the rest of the feed is still consumed.

## \`mergeNewAlerts(verified)\`

\`\`\`ts
async function mergeNewAlerts(verified: SignedAlertV1[]): Promise<SignedAlertV1[]> {
  const state = await getAlertsState();
  const known = new Set(state.knownAlerts.map(a => a.id));

  const newAlerts = verified.filter(a => !known.has(a.id));
  if (newAlerts.length === 0) return [];

  const updated = [...state.knownAlerts, ...newAlerts]
    .sort((a, b) => b.timestampMs - a.timestampMs)
    .slice(0, 200);   // FIFO cap

  await chrome.storage.local.set({
    chromatika_alerts_v1: { ...state, knownAlerts: updated },
  });

  return newAlerts;
}
\`\`\`

dedup by \`alert.id\`. cap at 200 (oldest pruned). only **new** alerts (first time seen) get returned for action firing - re-seeing an existing alert id is a no-op.

## \`runNewAlertActions(alert, muted)\`

per-alert side-effects. severity branches the path.

\`\`\`ts
async function runNewAlertActions(alert: SignedAlertV1, muted: boolean): Promise<void> {
  // 1. always update banner state (banner reads listAlerts; merge already updated)
  // (no explicit call needed - banner re-fetches on its own 30s interval)

  // 2. chrome notifications: critical + warning, unless muted
  if ((alert.severity === 'critical' || alert.severity === 'warning') && !muted) {
    await fireChromeNotificationForAlert(alert);
  }

  // 3. dNR redirect rules: critical with affected domains only
  if (alert.severity === 'critical' && alert.affectedDomains.length > 0) {
    await appendCriticalAlertDnrRules(alert);
  }
}
\`\`\`

**mute** silences notifications + (implicitly via banner-not-shown) the in-app surface, but does **not** stop dNR rules. mute = "stop yelling," not "ignore threats."

**opt-out** stops the poller entirely. existing dNR rules clear on TTL.

## \`fireChromeNotificationForAlert(alert)\`

\`\`\`ts
async function fireChromeNotificationForAlert(alert: SignedAlertV1): Promise<void> {
  const notificationId = \`chromatika-alert-\${alert.id}\`;
  await chrome.notifications.create(notificationId, {
    type: 'basic',
    iconUrl: chrome.runtime.getURL('icons/alert-icon-128.png'),
    title: \`[\${alert.severity.toUpperCase()}] \${alert.titleShort}\`,
    message: \`\${alert.bodyLong.slice(0, 200)}\${alert.bodyLong.length > 200 ? '…' : ''}\`,
    priority: 2,
    requireInteraction: true,
  });
}
\`\`\`

\`requireInteraction: true\` means the notification persists until the user dismisses or clicks (vs \`false\` which auto-dismisses after a few seconds). matches the criticality.

notification id format: \`chromatika-alert-<alert.id>\`. lets the global click handler in \`index.ts\` parse the alert id back out:

\`\`\`ts
chrome.notifications.onClicked.addListener((notificationId) => {
  if (!notificationId.startsWith('chromatika-alert-')) return;
  const alertId = notificationId.slice('chromatika-alert-'.length);
  void openSidePanelForAlert(alertId);   // opens side panel with ?alertId=<id>
  chrome.notifications.clear(notificationId);
});
\`\`\`

side panel reads \`?alertId=\` from its URL and auto-expands the matching alert in the banner.

## \`appendCriticalAlertDnrRules(alert)\`

\`\`\`ts
const ALERT_DNR_RULE_ID_BASE = 10_000;
const ALERT_DNR_RULE_ID_RANGE = 10_000;       // 10000-19999

async function appendCriticalAlertDnrRules(alert: SignedAlertV1): Promise<void> {
  const newRules: chrome.declarativeNetRequest.Rule[] = [];
  const newAlarms: { name: string, ruleId: number }[] = [];

  for (const domain of alert.affectedDomains) {
    const stableHash = sha256(\`\${alert.id}::\${domain}\`);
    const ruleId = ALERT_DNR_RULE_ID_BASE + (readUint32LE(stableHash) % ALERT_DNR_RULE_ID_RANGE);

    newRules.push({
      id: ruleId,
      priority: 2,
      action: {
        type: 'redirect',
        redirect: {
          regexSubstitution: chrome.runtime.getURL('phishing-warning.html')
            + \`?blocked=\${encodeURIComponent(domain)}\`
            + \`&alertId=\${encodeURIComponent(alert.id)}\`
            + \`&source=chromatika-safety-alert\`,
        },
      },
      condition: {
        urlFilter: \`||\${domain}^\`,
        resourceTypes: ['main_frame'],
      },
    });

    const alarmName = \`chromatika-alert-rule-\${ruleId}\`;
    newAlarms.push({ name: alarmName, ruleId });
    chrome.alarms.create(alarmName, { when: alert.expiresAtMs });
  }

  await chrome.declarativeNetRequest.updateDynamicRules({
    addRules: newRules,
    removeRuleIds: [],
  });

  // persist alarm → ruleId mapping for cleanup
  const applied = await chrome.storage.local.get('chromatika_alerts_applied_rules_v1');
  applied.alarmToRuleId = applied.alarmToRuleId ?? {};
  for (const { name, ruleId } of newAlarms) {
    applied.alarmToRuleId[name] = ruleId;
  }
  await chrome.storage.local.set({ chromatika_alerts_applied_rules_v1: applied });
}
\`\`\`

key invariants:
- **rule id range**: 10000-19999 - non-overlapping with \`eth-phishing-detect\` (1-4900). 10000 alerts × N domains is a hard ceiling
- **stable hashing**: \`sha256(alertId || '::' || domain)\` mod range gives the rule id. same alert + same domain always maps to the same id. no need for a global counter
- **priority 2**: higher than eth-phishing-detect's priority 1, so chromatika alerts win on collision
- **redirect target**: \`phishing-warning.html?blocked=<domain>&alertId=<id>&source=chromatika-safety-alert\`. the warning page reads these params to display context
- **per-rule TTL alarm**: each rule gets its own \`chromatika-alert-rule-<ruleId>\` alarm scheduled at \`alert.expiresAtMs\`. when the alarm fires, the rule is removed

## \`handleAlertRuleExpiryAlarm(alarmName)\`

\`\`\`ts
async function handleAlertRuleExpiryAlarm(alarmName: string): Promise<void> {
  const applied = await chrome.storage.local.get('chromatika_alerts_applied_rules_v1');
  const ruleId = applied.alarmToRuleId?.[alarmName];
  if (!ruleId) return;   // already cleaned up

  await chrome.declarativeNetRequest.updateDynamicRules({
    addRules: [],
    removeRuleIds: [ruleId],
  });

  delete applied.alarmToRuleId[alarmName];
  await chrome.storage.local.set({ chromatika_alerts_applied_rules_v1: applied });
}
\`\`\`

removes one rule + cleans up the index. doesn't clean up the corresponding alarm (chrome.alarms self-cleans after firing for non-recurring alarms).

global dispatcher in \`index.ts\`:
\`\`\`ts
chrome.alarms.onAlarm.addListener(alarm => {
  if (alarm.name.startsWith('chromatika-alert-rule-')) {
    void handleAlertRuleExpiryAlarm(alarm.name);
  }
});
\`\`\`

## the per-rule TTL design rationale

alternative approaches:
- **single sweeper alarm**: one alarm fires periodically, scans all alerts, removes expired ones. simple but coarse - up to N minutes of stale rules
- **per-alert alarm**: one alarm per alert. but an alert can have many domains; multi-domain rule cleanup needs care
- **per-rule alarm** (chosen): one alarm per (alert, domain) pair. precise expiry; clean cleanup logic

con: many alarms if many alerts × many domains. chrome.alarms isn't strictly limited but each alarm is overhead. for typical usage (handful of active alerts, each with 1-3 domains), this is fine.

## opt-out cleanup

opting out doesn't immediately clear all dNR rules - they cleanup on their per-rule TTL alarms, which still fire even if the poller stops. so:
- new poll → skipped (opt-out check)
- existing dNR rules → continue blocking until TTL
- alert history → readable

if you want immediate dNR cleanup on opt-out, that'd be a future enhancement (sweep \`applied.alarmToRuleId\`, remove all rules at once).

## library

- \`chrome.alarms\`, \`chrome.notifications\`, \`chrome.declarativeNetRequest\`, \`chrome.storage\`
- \`@noble/hashes/sha2\` \`sha256\` for stable rule id hashing
- \`zod\` for \`AlertsFeedSchema\` validation
- internal: all in \`src/background/alerts/*\`

## related

- [alerts-overview.md](/library/tech/alerts-overview) - subsystem overview
- [alerts-signed-feed-format.md](/library/tech/alerts-signed-feed-format) - the verification step inside the poll
- [chrome-alarms-and-idle.md](/library/tech/chrome-alarms-and-idle) - the alarm primitive
- [chrome-declarativenetrequest.md](/library/tech/chrome-declarativenetrequest) - the dNR primitive
- [eth-phishing-detect.md](/library/tech/eth-phishing-detect) - the complementary phishing layer
`,Rk=`# \`publish-alert.mjs\` CLI

the offline tool a publisher uses to generate keypairs, sign alerts, and build the feed JSON the wallet polls. zero npm deps beyond \`@noble/ed25519\` + \`@noble/hashes\`. runs anywhere with node 18+. the canonical JSON serializer matches \`alerts-types.ts\` exactly so signatures round-trip.

lives at \`wallet-extension/scripts/publish-alert.mjs\`. five sub-commands.

## \`--gen-key\`

generate a fresh ed25519 keypair for production publishing.

\`\`\`sh
node scripts/publish-alert.mjs --gen-key
\`\`\`

output:
\`\`\`
generated keypair
  pub (b64): +Qzgt7hrnGc94nPyvFFmQuv+EzRxCBvYsCN0XHHkWQA=
  priv (b64): <64-byte b64>

next steps:
  1. save priv (b64) somewhere safe - hardware wallet seed, 1Password, etc.
     this is the SIGNING KEY. anyone with it can sign alerts that wallets accept.
  2. paste pub (b64) into wallet-extension/src/background/alerts/alerts-publishers.ts
     under BUNDLED_PUBLISHERS, with a label and addedAt date
  3. bump BUNDLED_PUBLISHERS_REVISION so caches re-verify against the new allowlist
  4. ship a chromatika release that includes the new publisher
  5. start using \`sign --priv <b64>\` to issue alerts
\`\`\`

implementation:
\`\`\`js
const priv = ed.utils.randomPrivateKey();   // 64 bytes (32-byte seed + derived 32-byte priv state)
const pub = await ed.getPublicKeyAsync(priv);
console.log('  pub (b64):', base64Encode(pub));
console.log('  priv (b64):', base64Encode(priv));
\`\`\`

privkey is **64 bytes** (noble/ed25519 v3 returns the full canonical RFC 8032 secret which includes the secret seed + derived state). the wallet only needs the 32-byte pubkey.

## \`--gen-dev-key\`

derive the deterministic dev publisher keypair from the seed \`chromatika-dev-publisher-v0\`. anyone running this command produces the same keypair. used for local demos where the bundled \`PLACEHOLDER_DEV_PUBLISHER_PUBKEY_B64\` is the deterministic pubkey from this seed.

\`\`\`sh
node scripts/publish-alert.mjs --gen-dev-key
\`\`\`

output (always identical):
\`\`\`
deterministic dev publisher
  pub (b64): +Qzgt7hrnGc94nPyvFFmQuv+EzRxCBvYsCN0XHHkWQA=
  priv (b64): <derived from seed 'chromatika-dev-publisher-v0'>

note: this keypair is DETERMINISTIC and BUNDLED into the wallet's allowlist.
      for production, generate a fresh key with --gen-key.
\`\`\`

implementation:
\`\`\`js
const SEED = new TextEncoder().encode('chromatika-dev-publisher-v0');
const priv = sha512(SEED).slice(0, 32);   // 32-byte seed for ed25519
const pub = await ed.getPublicKeyAsync(priv);
\`\`\`

note: the dev seed isn't a real RFC 8032 seed - it's a deterministic 32 bytes derived from a fixed string. fine for dev demos, definitely not fine for production.

## \`sign --priv <b64> --in <unsigned.json> --out <signed.json>\`

sign an unsigned alert envelope.

\`\`\`sh
node scripts/publish-alert.mjs sign \\
  --priv <base64 64-byte privkey> \\
  --in unsigned.json \\
  --out signed.json
\`\`\`

input file (unsigned envelope - publisher fills \`id\`, \`severity\`, etc. but not \`signatureB64\`):
\`\`\`json
{
  "v": 1,
  "id": "uniswap-clone-2026-04-29",
  "severity": "critical",
  "timestampMs": 1712345678000,
  "expiresAtMs": 1712950478000,
  "affectedDomains": ["evilsite.io"],
  "affectedChains": ["evm"],
  "titleShort": "phishing uniswap clone",
  "bodyLong": "...",
  "publisherKeyB64": "<expected-pubkey-b64>"
}
\`\`\`

output:
- writes signed envelope to \`--out\` path
- includes \`signatureB64\` field
- echoes \`wrote signed alert to <path>\` on stdout

implementation:
\`\`\`js
const unsigned = JSON.parse(fs.readFileSync(args.in, 'utf-8'));

// canonical bytes (key-sorted, undefined dropped, no whitespace)
const canonical = canonicalAlertBytes(unsigned);

const priv = base64Decode(args.priv);
const sig = await ed.signAsync(canonical, priv.slice(0, 32));   // first 32 bytes is the seed

// optional: verify against the expected pubkey to catch input errors
const pub = await ed.getPublicKeyAsync(priv.slice(0, 32));
const expectedPub = unsigned.publisherKeyB64;
if (base64Encode(pub) !== expectedPub) {
  console.error('publisherKeyB64 in input does not match privkey-derived pubkey');
  process.exit(1);
}

const signed = { ...unsigned, signatureB64: base64Encode(sig) };
fs.writeFileSync(args.out, JSON.stringify(signed, null, 2));
\`\`\`

note the \`canonicalAlertBytes\` here matches the wallet's verifier exactly - same key sorting, same JSON output, same UTF-8 encoding. mismatch would mean the wallet rejects with \`bad-signature\` even though the signer thinks it produced a valid sig.

## \`feed --in <signed-array.json> --out <feed.json>\`

wrap a JSON array of signed alerts into the feed envelope the wallet expects.

\`\`\`sh
node scripts/publish-alert.mjs feed --in signed-alerts.json --out safety-alerts.json
\`\`\`

input file (an array of pre-signed alerts):
\`\`\`json
[
  { /* signed alert 1 */ },
  { /* signed alert 2 */ }
]
\`\`\`

output:
\`\`\`json
{
  "v": 1,
  "generatedAtMs": 1712345700000,
  "alerts": [
    { /* signed alert 1 */ },
    { /* signed alert 2 */ }
  ]
}
\`\`\`

\`generatedAtMs\` is the publisher's wall clock at feed-build time. wallets use this for "last updated N min ago" UI but don't strictly enforce it (the per-alert \`timestampMs\` is what time-bounds checks against).

## \`sample --priv <b64> --out <sample-feed.json>\`

generate a 3-alert demo feed: one critical, one warning, one info, all with 7-day expiry. used for local demos when the production feed isn't reachable.

\`\`\`sh
node scripts/publish-alert.mjs sample --priv <base64 priv> --out sample-feed.json
\`\`\`

output: a fully-formed \`AlertsFeedResponse\` with three alerts ready to drop into the wallet via the dev-injection tRPC procedure (\`injectSignedAlertForDev\`).

useful workflow:
\`\`\`sh
# 1. derive the dev key
node scripts/publish-alert.mjs --gen-dev-key
# (copy the priv b64)

# 2. generate a sample feed
node scripts/publish-alert.mjs sample --priv <priv-b64> --out sample.json

# 3. host it locally on a static server, OR

# 4. inject directly via the wallet's dev surface (paste each signed alert
#    from sample.json's "alerts" array into AlertsSettingsSection's
#    dev injection text field)
\`\`\`

## the canonical-JSON contract

the most subtle invariant: \`publish-alert.mjs\` and \`alerts-types.ts\` both implement the same \`canonicalAlertBytes\` function. they MUST produce identical bytes for the same input.

a single-character mismatch (e.g. extra space, different escape rule) means signatures from the CLI won't validate in the wallet and vice versa. both implementations:

- sort keys alphabetically at every level
- drop fields where value is \`undefined\` (NOT empty string, NOT null - those stay)
- output \`JSON.stringify(...)\` without space arg (no whitespace)
- preserve array internal order
- UTF-8 encode the result

if you change one, change the other. there's no automated test that they match across all inputs (would require shared canonical-output property tests). today the convention is "look at both side-by-side when modifying."

## library

- \`@noble/ed25519\` v3
- \`@noble/hashes/sha2\` \`sha512\` (injected into noble/ed25519)
- \`@noble/hashes/hmac\` \`hmac\` (also injected)
- node 18+ built-ins (\`fs\`, \`process\`, \`TextEncoder\`)
- internal: \`wallet-extension/scripts/publish-alert.mjs\`

## related

- [alerts-overview.md](/library/tech/alerts-overview) - the subsystem overview
- [alerts-signed-feed-format.md](/library/tech/alerts-signed-feed-format) - the wire format the CLI produces
- [alerts-publisher-allowlist.md](/library/tech/alerts-publisher-allowlist) - where the pubkey gets pasted
`,Mk=`# alerts publisher allowlist (\`alerts-publishers.ts\`)

the bundled list of pubkeys whose alerts the wallet trusts. an alert from any other publisher fails verification with \`unknown-publisher\`. lives in \`wallet-extension/src/background/alerts/alerts-publishers.ts\` as a hardcoded TypeScript array. shipped with the extension; updates require a release. future hardening: on-chain Sui Move \`PublisherCap\` registry for decentralized rotation.

## the shape

\`\`\`ts
export const BUNDLED_PUBLISHERS_REVISION = 1;

export const PLACEHOLDER_DEV_PUBLISHER_PUBKEY_B64 =
  '+Qzgt7hrnGc94nPyvFFmQuv+EzRxCBvYsCN0XHHkWQA=';

export const BUNDLED_PUBLISHERS: BundledPublisher[] = [
  {
    pubkeyB64: PLACEHOLDER_DEV_PUBLISHER_PUBKEY_B64,
    label: 'chromatika dev publisher (placeholder)',
    addedAt: '2026-04-30',
  },
  // TODO: add real chromatika-team production pubkey here
];

interface BundledPublisher {
  pubkeyB64: string;          // base64 32-byte ed25519 pubkey
  label: string;              // human-readable; surfaced in settings UI
  addedAt: string;            // ISO date "YYYY-MM-DD" of when this entry was added
}
\`\`\`

\`isAllowedPublisher(pubkeyB64)\` is a simple list-membership check:

\`\`\`ts
export function isAllowedPublisher(pubkeyB64: string): boolean {
  return BUNDLED_PUBLISHERS.some(p => p.pubkeyB64 === pubkeyB64);
}
\`\`\`

constant-time comparison would be ideal but here the threat is moot - the pubkey is public-equivalent material; an attacker doesn't gain anything from timing.

## the placeholder dev publisher

the current \`PLACEHOLDER_DEV_PUBLISHER_PUBKEY_B64\` is the deterministic ed25519 pubkey derived from the seed \`chromatika-dev-publisher-v0\`:

\`\`\`js
const SEED = new TextEncoder().encode('chromatika-dev-publisher-v0');
const priv = sha512(SEED).slice(0, 32);
const pub = await ed.getPublicKeyAsync(priv);
// pub b64 = '+Qzgt7hrnGc94nPyvFFmQuv+EzRxCBvYsCN0XHHkWQA='
\`\`\`

run \`node scripts/publish-alert.mjs --gen-dev-key\` to regenerate the same keypair anywhere.

since the privkey is deterministic from a known seed, **anyone can sign alerts that the bundled wallet accepts** today. this is intentional for local demos - a developer can locally sign + inject test alerts without setting up real publishing infrastructure.

## the pre-launch TODO

per \`wallet-extension/docs/STATUS.md\` lines 91-101:

> **Pre-launch TODO**: replace \`PLACEHOLDER_DEV_PUBLISHER_PUBKEY_B64\` with the real chromatika-team production pubkey. run \`node scripts/publish-alert.mjs --gen-key\`, save privkey somewhere safe, paste pubkey into \`BUNDLED_PUBLISHERS\`. bump \`BUNDLED_PUBLISHERS_REVISION\` so cached alerts re-verify against the new allowlist.

## the revision number

\`BUNDLED_PUBLISHERS_REVISION\` is an integer that bumps **every time the allowlist changes** (publisher added, removed, or label updated).

\`\`\`ts
export const BUNDLED_PUBLISHERS_REVISION = 1;
\`\`\`

stored alerts include the revision they were verified under (in the alerts state's \`publishersRevision\` field). on read, if the stored revision doesn't match the current bundled revision, **all cached alerts are re-verified**:

\`\`\`ts
async function getAlertsState(): Promise<AlertsState> {
  const raw = await chrome.storage.local.get('chromatika_alerts_v1');
  let state: AlertsState = raw.chromatika_alerts_v1 ?? defaultState();

  if (state.publishersRevision !== BUNDLED_PUBLISHERS_REVISION) {
    // re-verify all cached alerts
    const reverified: SignedAlertV1[] = [];
    for (const a of state.knownAlerts) {
      const r = await verifySignedAlert(a, Date.now());
      if (r.ok) reverified.push(a);
    }
    state = {
      ...state,
      knownAlerts: reverified,
      publishersRevision: BUNDLED_PUBLISHERS_REVISION,
    };
    await chrome.storage.local.set({ chromatika_alerts_v1: state });
  }

  return state;
}
\`\`\`

invariant: cached alerts that were verified under an old allowlist (which may have included publishers no longer trusted) get re-verified on the next read. ensures the wallet doesn't keep showing alerts from a publisher we've revoked.

## why hardcoded vs remote allowlist

- **hardcoded**: trust anchor is the extension binary itself. user trusts the chromatika team to ship a correct allowlist via signed CRX updates (chrome's own update verification)
- **remote**: more flexibility (rotate publishers without releases), but introduces a second trust anchor (the allowlist server). compromise of the allowlist server = compromise of the alerting system

production model: hardcoded for now, then move to **on-chain Sui Move \`PublisherCap\` registry** (per STATUS.md future hardening). \`PublisherCap\` objects represent the right to publish; transfer = rotate the publisher; on-chain governance = community-rotatable. wallet reads the registry on startup + caches; revocations propagate within minutes.

bundled allowlist becomes the bootstrap layer; on-chain registry becomes the dynamic layer.

## the future cross-publisher case

today the model is "all alerts come from the chromatika team." a single publisher key.

future: multiple publishers (e.g. Solana Foundation, Sui Foundation, third-party security firms). each gets their own pubkey in the allowlist. user can opt-in/opt-out per publisher. tracked future.

## adding a publisher (future workflow)

\`\`\`sh
# generate a fresh key (offline, secure machine)
node scripts/publish-alert.mjs --gen-key
# (save priv b64 in 1Password, hardware wallet, etc.)
# (copy pub b64)

# edit alerts-publishers.ts
# add entry:
#   { pubkeyB64: '<pub-b64>', label: 'solana foundation security team', addedAt: '2026-05-15' }
# bump BUNDLED_PUBLISHERS_REVISION

# ship a chromatika release with the updated allowlist

# from now on, sign alerts with the new privkey:
node scripts/publish-alert.mjs sign --priv <new-priv> --in unsigned.json --out signed.json
\`\`\`

## removing a publisher (future workflow)

\`\`\`sh
# edit alerts-publishers.ts
# remove the entry
# bump BUNDLED_PUBLISHERS_REVISION

# ship a chromatika release

# wallets receive the update; on next getAlertsState read, they
# re-verify cached alerts against the new (smaller) allowlist;
# alerts from the removed publisher are dropped from cache.
# the next poll won't include alerts from the revoked publisher
# (verifySignedAlert returns 'unknown-publisher').
\`\`\`

## library

- internal: \`wallet-extension/src/background/alerts/alerts-publishers.ts\`
- internal: \`wallet-extension/src/background/alerts/alerts-verify.ts\` for \`isAllowedPublisher\` consumer
- internal: \`wallet-extension/src/background/alerts/alerts-store.ts\` for the revision-based re-verification

## related

- [alerts-overview.md](/library/tech/alerts-overview) - subsystem overview
- [alerts-signed-feed-format.md](/library/tech/alerts-signed-feed-format) - the verify step that consults this allowlist
- [alerts-publish-cli.md](/library/tech/alerts-publish-cli) - the CLI that generates pubkeys for paste-in here
`,Dk=`# \`SignedAlertV1\` wire format + canonical JSON + ed25519 verify

every safety alert in chromatika is a \`SignedAlertV1\` JSON envelope: signed-once, immutable, ed25519-verifiable. the wire format and the canonical-JSON serialization rules are shared between the \`publish-alert.mjs\` CLI (signer) and the wallet (verifier). they **must produce identical bytes** or signatures will never validate.

## the envelope

\`\`\`ts
interface SignedAlertV1 {
  v: 1;
  id: string;                              // uuid or content-hash; stable identifier
  severity: 'critical' | 'warning' | 'info';
  timestampMs: number;                     // when published (ms since epoch)
  expiresAtMs: number;                     // alert's natural expiry (controls dNR TTL + history pruning)
  affectedDomains: string[];               // lowercased hostnames, no scheme, max 50
  affectedChains?: ('evm' | 'sui' | 'solana' | 'bitcoin' | 'aptos' | 'cross-chain')[];
  titleShort: string;                      // ≤100 chars
  bodyLong: string;                        // ≤4000 chars (markdown ok)
  publisherKeyB64: string;                 // 32-byte ed25519 pubkey, base64
  signatureB64: string;                    // 64-byte ed25519 sig, base64
}
\`\`\`

example:
\`\`\`jsonc
{
  "v": 1,
  "id": "uniswap-clone-2026-04-29",
  "severity": "critical",
  "timestampMs": 1712345678000,
  "expiresAtMs": 1712950478000,
  "affectedDomains": ["uniswap-clone-evil.io", "uniswap-v4-airdrop.app"],
  "affectedChains": ["evm"],
  "titleShort": "phishing uniswap clone draining USDC",
  "bodyLong": "Two domains run an exact uniswap v4 UI clone but the swap router transfers USDC to attacker-controlled addresses...",
  "publisherKeyB64": "+Qzgt7hrnGc94nPyvFFmQuv+EzRxCBvYsCN0XHHkWQA=",
  "signatureB64": "<base64 64-byte ed25519 sig>"
}
\`\`\`

## canonical JSON for signing

ed25519 signatures aren't aware of JSON whitespace or key ordering. to make signatures verifiable across implementations, both sides must serialize the **unsigned envelope** (every field except \`signatureB64\`) using the same canonical rules:

1. **alphabetical key ordering** at every level
2. **no whitespace** (no spaces, newlines, tabs in the output)
3. **\`undefined\` values dropped** (treated as if the field weren't there)
4. **UTF-8 encoded output bytes** become the signing input
5. **arrays preserve their internal order** (don't sort array elements)
6. **strings escaped per JSON spec** (control chars, quotes, backslashes)

implementation lives in \`src/background/alerts/alerts-types.ts\` (\`canonicalAlertBytes(unsigned)\`) and is mirrored in \`scripts/publish-alert.mjs\`. they share the same logic verbatim.

\`\`\`ts
function canonicalAlertBytes(unsigned: UnsignedAlertView): Uint8Array {
  const sorted = sortKeysRecursive(unsigned);
  const json = JSON.stringify(sorted);
  return new TextEncoder().encode(json);
}

function sortKeysRecursive(obj: any): any {
  if (Array.isArray(obj)) return obj.map(sortKeysRecursive);
  if (obj === null || typeof obj !== 'object') return obj;
  const out: Record<string, any> = {};
  for (const key of Object.keys(obj).sort()) {
    if (obj[key] !== undefined) out[key] = sortKeysRecursive(obj[key]);
  }
  return out;
}
\`\`\`

\`unsignedView(alert)\` is the helper that strips \`signatureB64\` from a signed alert before canonicalization.

## the signing primitive

\`\`\`ts
import * as ed from '@noble/ed25519';
import { sha512 } from '@noble/hashes/sha2';
import { hmac } from '@noble/hashes/hmac';

ed.hashes.sha512 = sha512;       // inject for noble/ed25519 v3
ed.hashes.hmacSha512 = (key, message) => hmac(sha512, key, message);

const canonicalBytes = canonicalAlertBytes(unsignedView(alert));
const sigBytes = await ed.signAsync(canonicalBytes, privKey32);
const signatureB64 = base64Encode(sigBytes);
\`\`\`

ed25519 RFC 8032 deterministic - same key + same message = same signature. so a publisher signing the same canonical bytes twice produces the same signatureB64.

## the verify pipeline

\`verifySignedAlert(raw, nowMs)\` in \`alerts-verify.ts\` runs:

\`\`\`ts
async function verifySignedAlert(raw: unknown, nowMs: number): Promise<VerifyResult> {
  // 1. shape validation via Zod
  const parsed = SignedAlertV1Schema.safeParse(raw);
  if (!parsed.success) return { ok: false, reason: 'invalid-shape', detail: parsed.error.message };
  const alert = parsed.data;

  // 2. publisher allowlist check
  if (!isAllowedPublisher(alert.publisherKeyB64)) {
    return { ok: false, reason: 'unknown-publisher', detail: alert.publisherKeyB64 };
  }

  // 3. decode signature + pubkey
  let pubkey: Uint8Array, sig: Uint8Array;
  try {
    pubkey = base64Decode(alert.publisherKeyB64);
    sig = base64Decode(alert.signatureB64);
  } catch (e) {
    return { ok: false, reason: 'invalid-pubkey', detail: e.message };
  }
  if (pubkey.length !== 32) return { ok: false, reason: 'invalid-pubkey', detail: 'not 32 bytes' };
  if (sig.length !== 64) return { ok: false, reason: 'invalid-signature-bytes', detail: 'not 64 bytes' };

  // 4. ed25519 signature verify against canonical bytes
  const canonical = canonicalAlertBytes(unsignedView(alert));
  const valid = await ed.verifyAsync(sig, canonical, pubkey);
  if (!valid) return { ok: false, reason: 'bad-signature' };

  // 5. time bounds: not too old, not from the future
  const ageMs = nowMs - alert.timestampMs;
  if (ageMs > 30 * 24 * 60 * 60 * 1000) return { ok: false, reason: 'time-out-of-range', detail: 'too old (>30d)' };
  if (ageMs < -5 * 60 * 1000) return { ok: false, reason: 'time-out-of-range', detail: 'from the future (>5min skew)' };

  return { ok: true, alert };
}
\`\`\`

failure modes (all surfaced as \`{ ok: false, reason, detail }\`):
- \`invalid-shape\` - Zod schema mismatch (missing field, wrong type, length cap exceeded)
- \`unknown-publisher\` - pubkey not in \`BUNDLED_PUBLISHERS\`
- \`invalid-pubkey\` - base64 decode failed or wrong length
- \`invalid-signature-bytes\` - base64 decode failed or wrong length
- \`bad-signature\` - ed25519 verify returned false
- \`time-out-of-range\` - older than 30 days or more than 5 min in the future

never throws. the poller iterates many alerts; one bad alert shouldn't poison the whole batch.

## why 30-day window for past + 5-min window for future

- **past 30 days**: prevents replaying ancient alerts as if they were fresh. an alert that's been expired for months shouldn't suddenly appear in a user's banner. honest publishers re-publish with a fresh timestamp if they want to keep an alert active beyond 30 days
- **future 5 minutes**: tolerates clock skew (NTP offsets, transit time, publisher being slightly ahead). but blocks an attacker who manages to sign with the right key from setting \`timestampMs\` far in the future to game the dedup logic

these are heuristic but reasonable defaults. tighter windows would catch edge cases more aggressively at the cost of false rejections.

## Zod schema

\`\`\`ts
const SignedAlertV1Schema = z.object({
  v: z.literal(1),
  id: z.string().min(1).max(128),
  severity: z.enum(['critical', 'warning', 'info']),
  timestampMs: z.number().int().nonnegative(),
  expiresAtMs: z.number().int().nonnegative(),
  affectedDomains: z.array(z.string().min(1).max(255)).max(50),
  affectedChains: z.array(z.enum(['evm', 'sui', 'solana', 'bitcoin', 'aptos', 'cross-chain'])).optional(),
  titleShort: z.string().min(1).max(100),
  bodyLong: z.string().min(0).max(4000),
  publisherKeyB64: z.string().min(1).max(64),
  signatureB64: z.string().min(1).max(128),
});
\`\`\`

caps protect against pathological input (e.g. an alert with 10000 affectedDomains would inflate dNR rule count).

## library

- \`@noble/ed25519\` v3 for sign + verify
- \`@noble/hashes/sha2\` \`sha512\` (noble/ed25519 v3 needs this injected)
- \`@noble/hashes/hmac\` \`hmac\` (also injected for HKDF / hmac-sha512)
- \`zod\` for shape validation
- internal: \`alerts-types.ts\` \`canonicalAlertBytes\`, \`unsignedView\`
- internal: \`alerts-verify.ts\` \`verifySignedAlert\`, \`isAllowedPublisher\`

## related

- [alerts-overview.md](/library/tech/alerts-overview) - the broader subsystem
- [alerts-publish-cli.md](/library/tech/alerts-publish-cli) - the signer side
- [alerts-publisher-allowlist.md](/library/tech/alerts-publisher-allowlist) - \`isAllowedPublisher\` source
- [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - underlying signature math
`,Bk=`# Aptos signing

Aptos transactions and messages signed via ika MPC ED25519 EDDSA. less surface than Sui because Aptos didn't have a hardware wallet first-mover advantage in the chromatika feature set, but the basic primitives are there: address derivation, message signing, transaction signing.

## the address

Aptos addresses are 32 bytes. derived from the public key:
\`\`\`
auth_key = SHA-3-256(pubkey || 0x00)   // 0x00 scheme byte for single-key Ed25519
address = auth_key   // 32 bytes
\`\`\`

note: SHA-3-256 (NIST FIPS 202), **not** keccak256 (which has different padding). same length output, different bytes.

\`getAptosAddress\` returns the EIP-55-style hex-encoded address (with leading \`0x\`).

multi-key (multi-sig, multi-curve) layouts use a different scheme byte and different \`auth_key\` derivation, but chromatika only deals with single-key Ed25519 today.

## message signing

\`\`\`ts
async function signAptosMessage({ messageB64 }) {
  const messageBytes = base64Decode(messageB64);
  const presignId = takePresign('ED25519_EDDSA');

  const sigBytes = await ikaSign({
    dwalletId: activeEd25519DwalletId,
    curve: Curve.ED25519,
    algorithm: SignatureAlgorithm.EdDSA,
    message: messageBytes,                            // raw bytes, ika sha-512s
    presignId,
  });

  const { R, S } = parseSignatureFromSignOutput(sigBytes, Curve.ED25519, SignatureAlgorithm.EdDSA);
  return { signature: bytesToHex(new Uint8Array([...R, ...S])) };
}
\`\`\`

raw 64-byte ed25519 signature output. dapps verify with standard ed25519_verify against the dWallet's pubkey.

note: there's no Aptos-specific "intent prefix" the way Sui has - Aptos messages are signed raw. dapps that want domain separation include their own prefix in the message bytes.

## transaction signing

Aptos transactions use the @aptos-labs/ts-sdk:

\`\`\`ts
async function signAptosTx(rawTransaction: RawTransaction) {
  // 1. SDK serializes to BCS bytes
  const txBytes = rawTransaction.bcsToBytes();

  // 2. wrap with Aptos signing-message prefix (per Aptos spec)
  // signingMessage = sha3-256("APTOS::RawTransaction") || bcs(rawTx)
  const tag = sha3_256(new TextEncoder().encode("APTOS::RawTransaction"));
  const signingMessage = new Uint8Array(tag.length + txBytes.length);
  signingMessage.set(tag, 0);
  signingMessage.set(txBytes, tag.length);

  // 3. sign via ika
  const presignId = takePresign('ED25519_EDDSA');
  const sigBytes = await ikaSign({
    dwalletId: activeEd25519DwalletId,
    curve: Curve.ED25519,
    algorithm: SignatureAlgorithm.EdDSA,
    message: signingMessage,                          // include the tag prefix
    presignId,
  });

  const { R, S } = parseSignatureFromSignOutput(sigBytes, Curve.ED25519, SignatureAlgorithm.EdDSA);
  const sig64 = new Uint8Array([...R, ...S]);

  // 4. wrap as Aptos AccountAuthenticatorEd25519
  const auth = new AccountAuthenticatorEd25519(
    new Ed25519PublicKey(getDwalletEd25519PublicKey()),
    new Ed25519Signature(sig64),
  );

  // 5. submit via SDK
  return aptos.transaction.submit.simple({ transaction: rawTransaction, senderAuthenticator: auth });
}
\`\`\`

key Aptos-specific bit: the **signing-message tag** (\`sha3-256("APTOS::RawTransaction")\`) prefixes the raw bytes before signing. this is Aptos's analog of Sui's \`[0x00, 0x00, 0x00]\` intent prefix - domain separation so a tx can't be replayed as a different signed object.

## hardware wallets

Aptos signing on Ledger / Trezor:
- Ledger: \`@ledgerhq/hw-app-aptos\` (if it exists - Aptos Ledger app is community-maintained; check current support)
- Trezor: not supported in \`@trezor/connect-web\` today

if hardware support is needed, route via the hardware-sign popup using the device's signing primitive. chromatika's main path is dWallet-via-ika.

## activity

Aptos activity reads via the Aptos Indexer (\`https://api.[mainnet|testnet].aptoslabs.com/v1\`). standard REST GET; no SDK fancier than \`@aptos-labs/ts-sdk\`'s \`Aptos.transactions.list...\`.

## NFTs

\`getAptosNfts({ address })\` reads via the Aptos token-v2 indexer. no API key required (Aptos provides a public-tier indexer for NFT metadata).

## library

- \`@aptos-labs/ts-sdk\` \`Aptos\`, \`RawTransaction\`, \`AccountAuthenticatorEd25519\`, \`Ed25519PublicKey\`, \`Ed25519Signature\`
- \`@noble/hashes/sha3\` \`sha3_256\` for the signing-message tag
- internal: \`wallet-extension/src/background/chains/aptos.ts\`

## related

- [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - signature algorithm
- [signature-normalization.md](/library/tech/signature-normalization) - parseSignatureFromSignOutput
`,Lk=`# argon2id KDF in chromatika

argon2id is the password-based key-derivation function chromatika uses to turn an app password (or recovery-phrase / signature material) into the AES-256-GCM key that wraps the vault blob. picked over PBKDF2 (legacy v2) because argon2id is memory-hard - it forces an attacker to spend RAM, not just CPU, per password guess.

## the parameters we use

per [RFC 9106 §4](https://datatracker.ietf.org/doc/html/rfc9106#section-4) the spec gives two recommended profiles. chromatika uses the **second option** (the lower-memory recommendation):

- \`t = 3\` (time cost / iteration count)
- \`m = 65536 KiB = 64 MiB\` (memory cost)
- \`p = 4\` (parallelism / lanes)
- output length: 32 bytes (the raw bytes that become the AES-256-GCM key)
- variant: argon2**id** (hybrid of argon2i and argon2d, side-channel resistant + GPU-resistant)

salt is a per-vault random 16 bytes generated at vault create time. associated data (\`AD\`) and secret (\`K\`) are unused (left empty).

## why these numbers

64 MiB sounds modest for an offline KDF but it's a **service-worker context**. chromium service workers can be torn down after ~30 seconds idle, and argon2id running too long in the SW risks the worker getting killed mid-derivation. 64 MiB × t=3 × p=4 lands around ~1-3 seconds on a modern desktop and ~3-6 seconds on a slow phone. that's enough to make a brute-forcer pay real money for each guess while still letting an honest user unlock without staring at a hung popup.

a future hardening pass could make these tunables (per RFC 9106 §4 first option = m=2 GiB t=1 p=4 if device profile allows). right now it's the lower profile across the board.

## where the derived key lives

- **never** as a plaintext key bytes outside the immediate derivation closure
- imported into \`crypto.subtle.importKey('raw', keyBytes, { name: 'AES-GCM' }, /* extractable */ false, ['encrypt', 'decrypt'])\`
- the resulting \`CryptoKey\` is the only thing the rest of the wallet sees
- on lock the \`CryptoKey\` reference drops, the GC collects it, the AES key is gone

## the unlock cache footnote

cold service-worker restarts re-import the AES key bytes from \`chromatika_unlock_cache_v1\` in \`chrome.storage.session\`. this means the **derived bytes** sit in browser-managed memory between the first unlock and the next lock - that's a deliberate compromise so users don't re-type their password every time chrome unloads the worker. **the password itself is never stored**; only the post-argon2id bytes (b64) plus KDF metadata are cached. legacy \`chromatika_unlock_cache_v1_local\` and any cache row containing a \`password\` field are removed on lock / unlock / write.

## the legacy PBKDF2 path

vault format **v2** used PBKDF2 with \`iterations ≈ 900_000\` against SHA-256. chromatika **rejects v2 blobs on parse** at startup - pre-release means no migration. the v3 format (current) only accepts argon2id-wrapped blobs.

## library

argon2id is provided by a wasm port of the reference implementation (loaded in the background script). check \`wallet-extension/src/background/keyring/\` or the relevant kdf module for the exact import; the parameters above are constants in the kdf helper.
`,Nk=`# BIP39 mnemonics in chromatika

BIP39 is the spec for "twelve or twenty-four word seed phrases" - the human-readable encoding of cryptographic entropy that every modern HD wallet uses. chromatika supports it for HD vault creation, vault import, and as a recovery branch on passkey / WAAP / Lazor envelopes.

## what BIP39 actually does

1. **entropy**: 128 bits (12-word) or 256 bits (24-word). chromatika supports both; default is 12. the spec also allows 15 / 18 / 21 word lengths but the wallet's UI / API expose only 12 + 24 to keep the surface clean
2. **checksum**: append \`entropy_bits / 32\` checksum bits to the entropy; the checksum is the first \`entropy_bits/32\` bits of \`SHA-256(entropy)\`. for 128 bits → 4 checksum bits → 132 total → 11-bit groups → 12 words. for 256 → 8 checksum bits → 264 total → 24 words
3. **wordlist**: BIP39 English wordlist (2048 words = 11 bits / word). chromatika does **not** support other languages today
4. **seed derivation**: PBKDF2-HMAC-SHA512 with **2048 iterations**, password = mnemonic UTF-8 NFKD normalized, salt = \`"mnemonic" + passphrase\` (UTF-8 NFKD). chromatika uses **empty passphrase** today. output is the 64-byte BIP39 seed that feeds BIP44 / SLIP10

## where chromatika runs BIP39

**all generation runs in the extension background** (the service worker), not in React, not on a server. this is the same trust boundary as encrypt / decrypt - if you can compromise the SW, you can read the vault, but importantly there's no network round-trip during creation.

generators / validators come from \`@scure/bip39\` and the English wordlist from \`@scure/bip39/wordlists/english\`. the generator pulls 16 / 32 bytes from \`crypto.getRandomValues\` (browser CSPRNG seeded from OS entropy) for entropy; checksum is computed locally; the words are returned as a single space-joined string.

\`validateWords(words)\` does:
1. split + length check (must be 12 / 15 / 18 / 21 / 24)
2. each word must be in the English wordlist
3. checksum bits must match \`SHA-256(entropy)\` first bits

invalid mnemonics are rejected at the API boundary (\`createVault\`, \`importVault\`, \`addVault\` etc.) before any state mutation.

## the seed → keypair pipeline (Sui base)

\`\`\`
mnemonic (12 or 24 words)
  → PBKDF2-HMAC-SHA512(mnemonic, "mnemonic" + "")  → 64-byte seed
  → Mysten Ed25519Keypair.deriveKeypair(mnemonic, "m/44'/784'/0'/0'/0'")
    → uses SLIP10 ed25519 derivation under the hood
    → 32-byte ed25519 secret + 32-byte ed25519 public key
\`\`\`

the derivation path \`m/44'/784'/0'/0'/0'\` is the standard Sui derivation per SLIP-44 (\`784\` is Sui's coin type). \`accountIndex = 0\` = the fee-payer account.

## the seed → keypair pipeline (Solana base)

\`\`\`
mnemonic (12 or 24 words)
  → mnemonicToSeedSync(mnemonic, "")  → 64-byte BIP39 seed (hex)
  → slip10Ed25519DerivePath("m/44'/501'/0'/0'", seedHex)
    → 32-byte ed25519 seed
  → solana.Keypair.fromSeed(seed32)
    → canonical 64-byte secretKey (32-byte seed || 32-byte pubkey)
\`\`\`

\`501\` is Solana's SLIP-44 coin type. Solana \`Keypair.secretKey\` is 64 bytes by convention - the first 32 bytes are the ed25519 seed, the last 32 are the public key. this is the same shape Phantom and \`solana-keygen\` JSON-export.

## storage

mnemonic is **stored in the encrypted vault blob** in plaintext (within the vault's own AES-GCM seal). this means anyone with the unlocked vault can read the mnemonic - that's by design, since the user explicitly asked to "remember" the mnemonic by setting up a vault. on lock the vault re-seals; the only thing left is the encrypted blob in \`chrome.storage.local\`.

private-key vaults (see [import-vault-private-key.md](/library/user/import-vault-private-key)) skip BIP39 entirely - they store \`suiPrivateKeyBech32\` or \`solanaSecretKeyB64\` in the vault blob instead of a mnemonic.

## passphrase support

not implemented today. BIP39 allows an optional 25th-word passphrase (effectively a salt that produces a different seed for the same mnemonic). chromatika treats the passphrase as empty. adding it would be a forward-compatible vault-record field plus UI work. tracked as future hardening if anyone asks.

## libraries

- \`@scure/bip39\` for mnemonic generation, validation, \`mnemonicToSeedSync\`
- \`@scure/bip39/wordlists/english\` for the English wordlist
- \`@mysten/sui\` \`Ed25519Keypair.deriveKeypair\` for Sui derivation (wraps SLIP10 internally)
- a SLIP10 ed25519 derivation helper for Solana

## what chromatika **doesn't** support

- non-English wordlists (Spanish, Japanese, etc.)
- 15 / 18 / 21 word phrases on the create / import surfaces (validation accepts them, but the generator only emits 12 / 24)
- BIP39 passphrase
- electrum / monero seed formats (different specs entirely)
`,Kk="# BIP44 + SLIP10 derivation paths\n\nafter BIP39 produces a 64-byte seed (see [bip39-mnemonic.md](/library/tech/bip39-mnemonic)), chromatika walks a hierarchical derivation path to produce a per-chain keypair. the rules differ between curves: secp256k1 (BIP32) for EVM and BTC; ed25519 (SLIP10) for Sui, Solana, Aptos.\n\n## paths chromatika uses\n\n| chain | curve | path | source |\n|-------|-------|------|--------|\n| EVM (Ethereum, L2s) | secp256k1 | `m/44'/60'/0'/0/0` | BIP44 (SLIP-44 coin 60) |\n| Bitcoin (P2WPKH segwit) | secp256k1 | `m/84'/0'/0'/0/0` | BIP84 (segwit native) |\n| Bitcoin (P2TR taproot) | secp256k1 | `m/86'/0'/0'/0/0` | BIP86 (taproot) |\n| Sui | ed25519 | `m/44'/784'/0'/0'/0'` | SLIP-44 coin 784 |\n| Solana | ed25519 | `m/44'/501'/0'/0'` | SLIP-44 coin 501 (4-segment) |\n| Aptos | ed25519 | `m/44'/637'/0'/0'/0'` | SLIP-44 coin 637 |\n\nthe trailing `'` denotes a hardened derivation step (per BIP32: hardened indices use the parent secret key, not the parent public key, in the HMAC input).\n\n## BIP32 (secp256k1) for EVM and BTC\n\nBIP32 master key derivation:\n```\nmaster_seed = BIP39_seed (64 bytes)\nmaster_node = HMAC-SHA512(key=\"Bitcoin seed\", data=master_seed)\nmaster_secret_key = master_node[0..32]\nmaster_chain_code = master_node[32..64]\n```\n\nthen per derivation step:\n```\nhardened (i >= 0x80000000):\n  data = 0x00 || parent_secret_key || ser32(i)\nnon-hardened:\n  data = serP(parent_pubkey) || ser32(i)\nhmac = HMAC-SHA512(key=parent_chain_code, data=data)\nchild_secret = (hmac[0..32] + parent_secret) mod n   // n = secp256k1 order\nchild_chain  = hmac[32..64]\n```\n\nwalking `m/44'/60'/0'/0/0` produces a 32-byte secp256k1 secret. EVM address = `last 20 bytes of keccak256(uncompressed_public_key_64bytes)` (see [keccak256-uses.md](/library/tech/keccak256-uses)).\n\nBIP84 (`m/84'/0'/0'/0/0`) and BIP86 (`m/86'/0'/0'/0/0`) walk the same BIP32 logic but with different \"purpose\" segments (84 vs 86 vs 44) so the derived keys are isolated per address kind. P2WPKH addresses use bech32 encoding with `bc1q…` (mainnet) or `tb1q…` (testnet); P2TR uses bech32m with `bc1p…` / `tb1p…`.\n\nlibrary: `@scure/bip32`.\n\n## SLIP10 (ed25519) for Sui, Solana, Aptos\n\nSLIP10 is the ed25519 equivalent of BIP32. critical difference: ed25519 child derivation **only supports hardened steps** (you cannot derive a child public key from a parent public key alone - the curve doesn't support the \"add scalar to point, get new pubkey\" trick BIP32 uses for non-hardened steps).\n\n```\nmaster_node = HMAC-SHA512(key=\"ed25519 seed\", data=BIP39_seed)\nmaster_secret_seed = master_node[0..32]\nmaster_chain_code  = master_node[32..64]\n\nper step (must be hardened):\n  data = 0x00 || parent_secret_seed || ser32(i + 0x80000000)\n  hmac = HMAC-SHA512(key=parent_chain_code, data=data)\n  child_secret = hmac[0..32]\n  child_chain  = hmac[32..64]\n```\n\nwalking `m/44'/784'/0'/0'/0'` (Sui) or `m/44'/501'/0'/0'` (Solana) or `m/44'/637'/0'/0'/0'` (Aptos) produces a 32-byte ed25519 seed. then `ed25519_keypair_from_seed(seed)` produces the 32-byte public key.\n\nnote Solana's path is **4 segments** (`m/44'/501'/0'/0'`) where Sui and Aptos use **5** (`m/44'/coin'/0'/0'/0'`). this is intentional - Solana follows the original SLIP-44 convention with `account/change` slots; Sui and Aptos add a final `address_index'`. all four are hardened.\n\nlibrary: `slip10-ed25519` style helper (chromatika uses an internal `slip10Ed25519DerivePath` plus `@noble/ed25519` for keypair construction). Mysten's `Ed25519Keypair.deriveKeypair(mnemonic, path)` wraps the same logic for Sui.\n\n## why path matters\n\n- two wallets that use the **same mnemonic** but **different paths** produce different addresses and different keys. this is intentional - it lets the same seed generate keys for multiple chains without collision\n- chromatika's chain registry pins paths globally - you don't pick a path per vault, the path is determined by the chain\n- if you import a mnemonic from a wallet that used a non-standard path (e.g. an old hardware wallet that used `m/44'/60'/0'/0/0` vs Trezor's `m/44'/60'/0'/0` change layer), the addresses differ. that's a \"your wallet uses different paths than chromatika\" problem, not a chromatika bug\n\n## fee-payer = account 0\n\nall chromatika fee-payer derivations use **account index 0**. this is the keypair that funds ika DKG / presign / sign on-chain operations on Sui base, and the keypair whose canonical 64-byte secret feeds `ikaRootSeedFromSolanaKeypair` / `ikaRootSeedFromFeeKeypair` for the ika user-share encryption keys.\n\nmulti-account expansion (account 1, 2, etc. for sibling dWallet Vaults under one mnemonic) is not exposed today. each chromatika vault uses its own mnemonic + account 0 - if you want a second vault, generate a fresh mnemonic via `addVault` rather than walking deeper into the existing tree.\n\n## hardware wallet derivation paths\n\nLedger and Trezor derive the same paths in firmware (their UI lets the user pick `m/44'/60'/0'/0/X` for X = 0, 1, 2, ...). chromatika exposes the standard `0` slot today. if you want to add a Ledger account with a non-zero index, the wallet stores `derivationPath` per `addHardwareAccount` call.\n",Wk=`# \`bitcoinjs-lib\` usage in chromatika

\`bitcoinjs-lib\` is chromatika's Bitcoin tx-building, sighash-computing, address-encoding library. used for P2WPKH segwit sends, P2TR taproot sends, BIP143 / BIP341 sighash computation, witness assembly, and address derivation. version pinned at ^7.0.1.

## what we use it for

| feature | bitcoinjs-lib API |
|---------|-------------------|
| build segwit tx | \`Psbt\`, \`Psbt.addInput\`, \`addOutput\` |
| compute BIP143 sighash | \`Transaction.hashForWitnessV0(...)\` |
| compute BIP341 sighash | \`Transaction.hashForWitnessV1(...)\` |
| derive P2WPKH address | \`payments.p2wpkh({ pubkey, network })\` |
| derive P2TR address | \`payments.p2tr({ internalPubkey, network })\` |
| encode DER ECDSA sig | \`script.signature.encode(...)\` |
| finalize PSBT input | \`Psbt.finalizeInput(...)\` |

## bech32 / bech32m address derivation

\`\`\`ts
import * as bitcoin from 'bitcoinjs-lib';

// P2WPKH (segwit) - bech32
const p2wpkh = bitcoin.payments.p2wpkh({
  pubkey: dwalletSecpPubkey,                                // 33-byte compressed
  network: bitcoin.networks.bitcoin,                         // mainnet
});
// p2wpkh.address = "bc1q..."

// P2TR (taproot) - bech32m
const p2tr = bitcoin.payments.p2tr({
  internalPubkey: dwalletSecpPubkey.slice(1),                // 32-byte x-only (drop 0x02/0x03 prefix)
  network: bitcoin.networks.bitcoin,
});
// p2tr.address = "bc1p..."
\`\`\`

bech32 = BIP173 (segwit v0). bech32m = BIP350 (segwit v1+, taproot). different checksum constants prevent address-format confusion.

## tx building (segwit P2WPKH)

\`\`\`ts
const psbt = new bitcoin.Psbt({ network: bitcoin.networks.bitcoin });

// add input
psbt.addInput({
  hash: prevTxId,                                             // 32-byte tx id
  index: prevVout,
  witnessUtxo: {
    script: bitcoin.address.toOutputScript(senderAddress, network),
    value: prevAmountSats,                                    // input value in sats
  },
});

// add output
psbt.addOutput({
  address: recipientAddress,
  value: amountSats,
});

// add change output if needed
psbt.addOutput({
  address: senderAddress,
  value: changeSats,
});

// compute sighash for input 0
const sighash = psbt.__CACHE.__TX.hashForWitnessV0(
  0,                                                          // input index
  bitcoin.payments.p2wpkh({ pubkey: senderPubkey }).output,
  prevAmountSats,
  bitcoin.Transaction.SIGHASH_ALL,
);
\`\`\`

\`hashForWitnessV0\` is the BIP143 sighash. signing this digest with secp256k1 ECDSA produces the witness signature.

## the ika MPC handoff

chromatika doesn't sign locally - hands sighash to ika MPC:

\`\`\`ts
const presignId = takePresign('SECP256K1_ECDSA');
const sigBytes = await ikaSign({
  dwalletId: activeSecpDwalletId,
  curve: Curve.SECP256K1,
  algorithm: SignatureAlgorithm.ECDSASecp256k1,
  message: sighash,                                           // already-hashed digest
  presignId,
});
const { r, s } = parseSignatureFromSignOutput(sigBytes, ...);

// DER-encode for bitcoin
const derSig = bitcoin.script.signature.encode(
  Buffer.concat([r, s]),
  bitcoin.Transaction.SIGHASH_ALL,
);

// write into PSBT
psbt.data.inputs[0].partialSig = [{
  pubkey: senderPubkey,
  signature: derSig,
}];

psbt.finalizeInput(0);
const txHex = psbt.extractTransaction().toHex();
\`\`\`

## the BIP341 taproot sighash

\`\`\`ts
const sighash = psbt.__CACHE.__TX.hashForWitnessV1(
  0,
  prevoutScripts,                                             // ALL input scripts (not just current)
  prevoutAmounts,                                             // ALL input amounts
  bitcoin.Transaction.SIGHASH_DEFAULT,                        // 0x00 (default for taproot)
);
\`\`\`

different from V0 (BIP143):
- takes **all** input scripts + amounts, not just the current input's
- uses tagged hash with \`"TapSighash"\` tag
- supports \`SIGHASH_DEFAULT\` (0x00) which is implicit and produces 64-byte signatures (no trailing sighash byte)

after signing via ika SECP256K1_TAPROOT pool, write the 64-byte signature directly into \`psbt.data.inputs[i].tapKeySig\`.

## broadcast

\`\`\`ts
const txHex = psbt.extractTransaction().toHex();
const resp = await fetch(\`\${esploraUrl}/tx\`, {
  method: 'POST',
  headers: { 'Content-Type': 'text/plain' },
  body: txHex,
});
const txid = await resp.text();
\`\`\`

chromatika uses Esplora-compatible APIs (mempool.space, blockstream.info) for broadcasting + UTXO queries. configurable per-network in \`chromatika_active_networks_v1\`.

## the buffer dependency

bitcoinjs-lib uses Node \`Buffer\` extensively. chromatika's \`buffer-polyfill.ts\` (see [buffer-polyfill.md](/library/tech/buffer-polyfill)) shims it.

## library

- \`bitcoinjs-lib\` ^7.0.1
- \`varuint-bitcoin\` ^2.0.0 (transitive, varint encoding)
- \`uint8array-tools\` ^0.0.9 (transitive)
- internal: \`wallet-extension/src/background/chains/bitcoin.ts\` for build / send orchestration
- internal: \`wallet-extension/src/background/chains/signing/btc.ts\` for sighash + ika handoff

## related

- [secp256k1-ecdsa.md](/library/tech/secp256k1-ecdsa), [taproot-schnorr.md](/library/tech/taproot-schnorr) - the underlying signature math
- [btc-tx-sign-segwit-taproot.md](/library/tech/btc-tx-sign-segwit-taproot) - the full signing flow
- [buffer-polyfill.md](/library/tech/buffer-polyfill) - the Node Buffer shim
`,jk=`# Bitcoin tx signing - segwit (P2WPKH) + taproot (P2TR)

Bitcoin transactions in chromatika are signed via two ika MPC paths: **SECP256K1_ECDSA** for P2WPKH segwit (BIP143 sighash) and **SECP256K1_TAPROOT** for P2TR taproot (BIP341 sighash + BIP340 Schnorr). presign material lives in two separate pools.

## the address layouts chromatika supports

| address kind | derivation path | signing curve | sig algorithm |
|--------------|----------------|---------------|---------------|
| P2WPKH (segwit bech32 \`bc1q...\`) | \`m/84'/0'/0'/0/0\` | secp256k1 | ECDSA (BIP143) |
| P2TR (taproot bech32m \`bc1p...\`) | \`m/86'/0'/0'/0/0\` | secp256k1 | Schnorr (BIP340) |

both addresses derive from the **same dWallet** (SECP256K1 curve). different address types from the same key - the dWallet's secp256k1 pubkey, encoded differently per address kind.

## P2WPKH signing flow

\`\`\`ts
async function signBtcP2wpkhTx(tx: bitcoinjs.Psbt, inputIndex: number) {
  // 1. compute BIP143 sighash
  // bitcoinjs-lib does this internally via finalizeAllInputs after signing
  // or expose via getInputHash:
  const sighash = tx.data.inputs[inputIndex].witnessUtxo
    ? tx.__tx.hashForWitnessV0(
        inputIndex,
        sighashScript,
        amountSats,
        bitcoinjs.Transaction.SIGHASH_ALL,   // 0x01
      )
    : throw 'P2WPKH input requires witnessUtxo';
  // sighash is 32 bytes - the BIP143 digest, double-SHA-256'd

  // 2. take a presign from SECP256K1_ECDSA pool
  const presignId = takePresign('SECP256K1_ECDSA');

  // 3. sign via ika
  // sighash is already a digest. ika hashes once with KECCAK256 internally
  // for SECP256K1_ECDSA, but bitcoin verifies with the SHA-256 sighash
  // there's a wrapper helper that handles the digest correctly - check
  // wallet-extension/src/background/chains/signing/btc.ts for the canonical
  // implementation
  const sigBytes = await ikaSign({
    dwalletId: activeSecpDwalletId,
    curve: Curve.SECP256K1,
    algorithm: SignatureAlgorithm.ECDSASecp256k1,
    message: sighash,                       // already-hashed BIP143 digest
    presignId,
  });

  // 4. parse - 64-byte compact r||s
  const { r, s } = parseSignatureFromSignOutput(sigBytes, Curve.SECP256K1, SignatureAlgorithm.ECDSASecp256k1);

  // 5. assemble bitcoinjs-style DER signature + sighash byte
  // bitcoin uses DER encoding for ECDSA sigs, with a trailing sighash byte (0x01 for SIGHASH_ALL)
  const derSig = encodeDerSignature(r, s);
  const sigWithType = Buffer.concat([derSig, Buffer.from([0x01])]);

  // 6. write into the PSBT input's partialSig
  const dwalletPubkey = getCompressedSecpPubkey();
  tx.data.inputs[inputIndex].partialSig = [{
    pubkey: dwalletPubkey,
    signature: sigWithType,
  }];

  // 7. finalize the input (bitcoinjs converts partialSig + script to witness)
  tx.finalizeInput(inputIndex);
}
\`\`\`

key bitcoinjs-lib bits:
- \`tx.__tx.hashForWitnessV0(...)\` computes the BIP143 sighash for a segwit input
- DER encoding is the canonical secp256k1 ECDSA signature serialization on Bitcoin (NOT compact r||s like EVM)
- \`partialSig\` is the PSBT field that carries one signer's sig
- \`finalizeInput\` converts the signed PSBT input into the final witness stack

## P2TR signing flow

\`\`\`ts
async function signBtcP2trTx(tx: bitcoinjs.Psbt, inputIndex: number) {
  // 1. compute BIP341 sighash (taproot key-path-only)
  const sighash = tx.__tx.hashForWitnessV1(
    inputIndex,
    prevoutScripts,                        // all input scripts
    prevoutAmounts,                        // all input amounts
    bitcoinjs.Transaction.SIGHASH_DEFAULT, // 0x00 (default for taproot, omitted from sig)
  );
  // sighash is 32 bytes - tagged_hash("TapSighash", 0x00 || sighash_data)

  // 2. take a presign from SECP256K1_TAPROOT pool (different from ECDSA!)
  const presignId = takePresign('SECP256K1_TAPROOT');

  // 3. sign via ika
  const sigBytes = await ikaSign({
    dwalletId: activeSecpDwalletId,
    curve: Curve.SECP256K1,
    algorithm: SignatureAlgorithm.Taproot,   // BIP340 Schnorr
    message: sighash,
    presignId,
  });

  // 4. parse - 64-byte BIP340 Schnorr (R || s)
  const { R, S } = parseSignatureFromSignOutput(sigBytes, Curve.SECP256K1, SignatureAlgorithm.Taproot);
  const sig64 = Buffer.concat([R, S]);

  // 5. for SIGHASH_DEFAULT (0x00), no trailing sighash byte needed
  // for any other sighash type, append it: sig64 || sighash_byte (65 bytes total)

  // 6. write into PSBT input's tapKeySig
  tx.data.inputs[inputIndex].tapKeySig = sig64;

  // 7. finalize
  tx.finalizeInput(inputIndex);
}
\`\`\`

P2TR key-path spending: the witness is \`[signature]\` only, no script. simpler than script-path.

## the BIP143 sighash digest

P2WPKH segwit signs over the BIP143-defined preimage:
\`\`\`
preimage = nVersion ||
           hashPrevouts ||
           hashSequence ||
           outpoint ||
           scriptCode ||
           amount ||
           nSequence ||
           hashOutputs ||
           nLocktime ||
           sighash_type
sighash = SHA-256(SHA-256(preimage))   // double-SHA-256
\`\`\`

bitcoinjs-lib encapsulates this. you compute the sighash once per input, hand to the signer, get back a sig.

## the BIP341 sighash digest

P2TR taproot signs over the BIP341-defined preimage with a tagged hash:
\`\`\`
sighash_data = SIGHASH_DEFAULT ||
               nVersion ||
               nLockTime ||
               sha256(amounts) ||
               sha256(scriptPubKeys) ||
               sha256(sequences) ||
               sha256(outputs) ||
               spend_type ||
               input_index ||
               ...
sighash = tagged_hash("TapSighash", 0x00 || sighash_data)
        = SHA-256(SHA-256("TapSighash") || SHA-256("TapSighash") || 0x00 || sighash_data)
\`\`\`

different from BIP143 in both contents (taproot adds amounts + scriptPubKeys) and hash function (tagged hash with the \`"TapSighash"\` tag).

## the v=0/1 vs taproot

ECDSA on Bitcoin uses DER encoding which already carries enough info to verify - no separate \`v\` byte. unlike EVM where we recover \`v\` to map to address, Bitcoin verifiers compute the digest and check \`(r, s)\` against the candidate pubkey directly.

Schnorr on Bitcoin (BIP340) doesn't need recovery either - the signature uses an x-only \`R\` form, but verification doesn't require recovery. just \`e = tagged_hash("BIP0340/challenge", R || P || m)\` then check \`s*G == R + e*P\`.

so neither Bitcoin path has the EVM-style v-recovery dance.

## the "do we use Solana-base BTC signing?"

\`assertNotSolanaBaseForSecpSigning\` gates BTC signing to Sui-base today. BTC sends require Sui-base vault. when Solana-base SECP signing parity ships, this guard relaxes.

## library

- \`bitcoinjs-lib\` for tx building, PSBT, BIP143 / BIP341 sighash computation, witness assembly
- \`@noble/hashes/sha256\` for SHA-256 (used internally by bitcoinjs)
- \`@noble/secp256k1\` for curve math (used internally; chromatika doesn't call it directly)
- internal: \`wallet-extension/src/background/chains/signing/btc.ts\`

## related

- [ecdsa-secp256k1.md](/library/tech/ecdsa-secp256k1) - the ECDSA path
- [taproot-schnorr.md](/library/tech/taproot-schnorr) - the Schnorr path
- [signature-normalization.md](/library/tech/signature-normalization) - parseSignatureFromSignOutput details
- [ika-presign-pool-impl.md](/library/tech/ika-presign-pool-impl) - the two BTC pools (SECP256K1_ECDSA + SECP256K1_TAPROOT)
`,Fk="# `buffer-polyfill.ts` (Node Buffer in browser)\n\nseveral chromatika dependencies use Node's `Buffer` global, which doesn't exist in browser environments. `wallet-extension/src/buffer-polyfill.ts` shims `globalThis.Buffer` via the `buffer` npm package so those libs work. **must be imported as the first line** of every entry point.\n\n## who needs it\n\n| dep | why |\n|-----|-----|\n| `@ledgerhq/devices/hid-framing.js` | uses `Buffer.from`, `Buffer.alloc` for HID frame parsing |\n| `bitcoinjs-lib` | uses `Buffer` extensively for tx serialization, script ops |\n| `@solana/web3.js` 1.x | uses `Buffer` for some serialization paths |\n\nattempting to use these without the polyfill causes runtime errors:\n```\nReferenceError: Buffer is not defined\n```\n\nat the first call that references `Buffer`.\n\n## the shim\n\n```ts\n// wallet-extension/src/buffer-polyfill.ts\nimport { Buffer } from 'buffer';\n\nif (typeof globalThis.Buffer === 'undefined') {\n  globalThis.Buffer = Buffer;\n}\n```\n\n`buffer` (lowercase, the npm package) provides a browser-compatible `Buffer` implementation. assigning to `globalThis.Buffer` makes it accessible as `Buffer.from(...)` etc. without explicit imports.\n\n## the import-order rule\n\n```ts\n// popup entry point: src/popup/main.tsx\nimport '../buffer-polyfill';   // FIRST LINE - before any other import\nimport './styles.css';\nimport { createRoot } from 'react-dom/client';\nimport App from './App';\n// ... rest\n```\n\nother imports might transitively require Buffer at module-load time (e.g. importing `bitcoinjs-lib` runs side-effecty top-level code that touches `Buffer`). the polyfill must be active before that happens.\n\nif you forget the polyfill on a new entry point and import a Buffer-using lib, you get the runtime error at startup.\n\n## entry points that need it\n\n- `src/popup/main.tsx` - popup UI\n- `src/side-panel/main.tsx` - side panel UI\n- `src/background/index.ts` - service worker\n- (any new entry point that loads code paths touching Ledger / Bitcoin / Solana 1.x)\n\n## the vite globalThis define\n\n`vite.config.ts` also defines `global: 'globalThis'` for packages that reference `global` (Node convention for `globalThis`):\n\n```ts\n// vite.config.ts\nexport default defineConfig({\n  define: {\n    global: 'globalThis',\n  },\n});\n```\n\ndifferent from Buffer - `global` is a separate Node-ism that some libs use (especially older crypto libs). the define replaces `global` with `globalThis` at bundle time so those references resolve correctly.\n\n## why not just use Uint8Array everywhere\n\n```ts\nconst buf = new Uint8Array([0x01, 0x02, 0x03]);   // works in browsers, no polyfill\n```\n\nmodern crypto libs (`@noble/hashes`, `@noble/curves`, `@scure/bip39`) use `Uint8Array` natively - no Buffer dependency. chromatika's own code prefers `Uint8Array`. Buffer is only there for **transitive** deps that pre-date the modern conventions (Ledger devices, bitcoinjs-lib, older Solana SDK).\n\nif those deps eventually drop Buffer (e.g. by migrating to noble-style libs internally), the polyfill becomes unnecessary. for now, it's load-bearing.\n\n## library\n\n- `buffer` npm package (browser Buffer impl)\n- internal: `wallet-extension/src/buffer-polyfill.ts`\n- vite config: `wallet-extension/vite.config.ts`\n\n## related\n\n- [bitcoinjs-lib-usage.md](/library/tech/bitcoinjs-lib-usage) - the largest Buffer-using consumer\n- [ledger-hw-app-libs.md](/library/tech/ledger-hw-app-libs) - HID framing uses Buffer\n",Uk=`# Chainlist search

Chainlist (\`chainlist.org\`) maintains a community-curated list of EVM chains with their RPC URLs, chain IDs, native currency info, and explorer URLs. chromatika doesn't depend on Chainlist's frontend - it queries the underlying JSON data ([chainlist.org/chains.json](https://chainlist.org/chains.json) or the \`chainid.network/chains.json\` source) for the wallet UI's "search and import" feature.

## how chromatika uses it

\`importFromChainlist({ query })\` lets the user search by chain name (string) or chain id (number) and get matching candidate networks back. the user picks one, then chromatika commits it via \`addCustomNetwork\` after validation.

## the search

\`\`\`ts
async function importFromChainlist({ query }: { query: string | number }) {
  const all = await fetchChainlist();   // cached
  const candidates = all.filter(c => {
    if (typeof query === 'number') return c.chainId === query;
    const q = query.toLowerCase();
    return (
      c.name.toLowerCase().includes(q) ||
      c.shortName.toLowerCase().includes(q) ||
      c.chainId.toString() === q
    );
  });
  return candidates.slice(0, 20).map(simplify);
}
\`\`\`

returns up to 20 matches. each carries:
- \`chainId\` (decimal)
- \`name\`, \`shortName\`
- \`nativeCurrency\` (\`{ name, symbol, decimals }\`)
- \`rpc\` (array of RPC URLs - we usually pick the first that doesn't require an API key)
- \`explorers\` (array of \`{ name, url, standard }\`)

## the underlying data source

\`\`\`
https://chainid.network/chains.json
\`\`\`

a regularly-updated JSON of every EVM chain ever registered. ~1 MB; chromatika caches in memory after first fetch. refreshed on: explicit user action ("refresh chainlist") or once per session.

## what chromatika does **not** do with this data

- doesn't auto-add chains. the user picks from the search results, then chromatika runs the standard \`addCustomNetwork\` validation (probe \`eth_chainId\` against the RPC, reject mismatch)
- doesn't persist the full Chainlist locally. only the chains the user explicitly adds end up in \`chromatika_custom_networks_v1\`
- doesn't trust Chainlist URLs without verification. several chains have multiple RPC entries; some are stale or rate-limited

## the validation when the user commits

after \`importFromChainlist\` shows candidates, the user picks one. chromatika then:

\`\`\`ts
async function addCustomNetwork(params) {
  // probe the suggested RPC
  const provider = new ethers.JsonRpcProvider(params.rpcUrl);
  const probedChainId = await provider.send('eth_chainId', []);
  if (parseInt(probedChainId, 16) !== params.chainId) {
    throw new Error('RPC reports different chainId than declared');
  }
  // commit
  await chrome.storage.local.update('chromatika_custom_networks_v1', list => [...list, params]);
}
\`\`\`

step 2 catches both:
- malicious RPC lying about which chain it serves
- a stale Chainlist entry pointing at an RPC that's been repurposed

## RPC selection

Chainlist often lists 5+ RPC URLs per chain. chromatika prefers:
1. URLs that look like they don't require an API key (no \`\${INFURA_API_KEY}\` placeholders)
2. URLs that don't have placeholder patterns (\`\${ALCHEMY_API_KEY}\`, etc.)
3. URLs hosted by chain teams themselves (e.g. \`arb1.arbitrum.io/rpc\` for Arbitrum)
4. fallback to public RPC services (\`drpc.org\`, \`1rpc.io\`, etc.)

if all top candidates need API keys, the user has to provide one or pick a public alternative.

## the EIP-3085 vs Chainlist split

EIP-3085 lets a **dapp** suggest a chain (see [eip-3085-3326.md](/library/tech/eip-3085-3326)). Chainlist is the **user-driven** version - the user searches a curated list rather than getting a parameter set from a dapp.

both end at the same \`addCustomNetwork\` validation. the difference is who initiates the suggestion.

## library

- \`fetch\` (browser native)
- \`ethers\` v6 \`JsonRpcProvider\` for the validation probe
- internal: \`wallet-extension/src/background/chains/chainlist.ts\` for \`fetchChainlist\`, \`importFromChainlist\`
- internal: \`wallet-extension/src/background/chains/evm-network.ts\` for \`addCustomNetwork\`

## related

- [eip-3085-3326.md](/library/tech/eip-3085-3326) - the dapp-driven equivalent
- [add-custom-network.md](/library/user/add-custom-network) (user-guides) - the user-facing search flow
- [manage-networks.md](/library/user/manage-networks) (user-guides) - the broader network management surface
`,zk=`# \`chrome.alarms\` + \`chrome.idle\`

two MV3 primitives chromatika uses for the auto-lock and presign-pool timers. \`chrome.alarms\` schedules background callbacks at fixed intervals or at specific times. \`chrome.idle\` reports OS user-activity state (active / idle / locked) so chromatika can react to OS screen-lock.

## chrome.alarms

\`\`\`ts
chrome.alarms.create('chromatika-presign-refill', { periodInMinutes: 5 });
chrome.alarms.create('chromatika-autolock', { when: lockAtMs });
chrome.alarms.create('chromatika-phishing-refresh', { periodInMinutes: 24 * 60 });

chrome.alarms.onAlarm.addListener((alarm) => {
  if (alarm.name === 'chromatika-presign-refill') void runPresignRefill();
  if (alarm.name === 'chromatika-autolock') void lock();
  if (alarm.name === 'chromatika-phishing-refresh') void syncPhishingRules();
});
\`\`\`

key behaviors:
- alarms persist across SW unloads (chrome stores them in extension state)
- when an alarm fires, chrome wakes the SW if it's idle - this is the only reliable way to schedule background work in MV3
- minimum interval is 30 seconds in production builds. dev builds allow 1 second
- \`when\` schedules a one-shot at a specific timestamp; \`periodInMinutes\` schedules recurring

## chromatika's alarms

| name | period | purpose |
|------|--------|---------|
| \`chromatika-presign-refill\` | 5 min | top up presign pools for active vault |
| \`chromatika-autolock\` | one-shot | lock the wallet at \`lockAtMs\` |
| \`chromatika-phishing-refresh\` | 24 hr | refetch MetaMask phishing config.json |

## the autolock alarm

\`\`\`ts
async function setAutolockTimer(autolockMinutes: number) {
  await chrome.alarms.clear('chromatika-autolock');   // cancel any pending
  if (autolockMinutes > 0) {
    const lockAtMs = Date.now() + autolockMinutes * 60_000;
    await chrome.alarms.create('chromatika-autolock', { when: lockAtMs });
    await chrome.storage.session.set({
      chromatika_unlock_cache_v1: { ...currentCache, lockAtMs },
    });
  }
}

// every user action resets the timer
async function recordUserActivity() {
  if (!unlocked) return;
  await setAutolockTimer(currentAutolockMinutes);
}
\`\`\`

every tRPC call from a UI page counts as user activity → reset timer. user idleness (no calls) → timer fires → \`lock()\`.

## chrome.idle

\`\`\`ts
chrome.idle.setDetectionInterval(60);   // seconds of no input before going 'idle'
chrome.idle.onStateChanged.addListener((state) => {
  if (state === 'locked') {
    void lock();   // OS screen-lock = wallet lock
  } else if (state === 'idle') {
    // 60s of no input - could shorten autolock here if user wants
  }
  // 'active' transitions don't trigger anything special
});
\`\`\`

\`chrome.idle\` reports three states:
- \`'active'\` - user is using the device
- \`'idle'\` - no input for \`setDetectionInterval(seconds)\` (default 60s, can set 15-3600s)
- \`'locked'\` - OS screen is locked (Windows lock screen, macOS lock, Linux screensaver, etc.)

chromatika treats \`'locked'\` as an immediate trigger to lock the wallet, regardless of the autolock timer. logic: if your screen is locked, your wallet should be too.

## the SW wakeup behavior

alarms are the **canonical way to wake a sleeping SW**. when an alarm fires:
1. chrome spins up the SW if needed
2. fires \`chrome.alarms.onAlarm\` listener
3. SW does its work
4. SW goes back to sleep ~30s later if no other activity

if you want a recurring background job, \`chrome.alarms\` is your tool. don't try \`setInterval\` - the SW gets killed; intervals don't persist.

## the dev-time alarm minimum quirk

production builds: minimum alarm period is 30 seconds. trying to set \`periodInMinutes: 0.5\` (30s) works; \`0.49\` (29.4s) doesn't.

dev builds: minimum is 1 second. useful for testing.

chromatika's 5-minute presign refill is well above either limit.

## the once-per-SW-startup pattern

some setup work needs to run on **every** SW startup (cold or restart):
- restore unlock cache from \`chrome.storage.session\`
- reconnect MCP host if enabled
- restore active vault id

\`\`\`ts
// runs on every SW startup
chrome.runtime.onStartup.addListener(() => {
  void restoreFromStorage();
});

// also runs on install
chrome.runtime.onInstalled.addListener(() => {
  void initializeFreshInstall();
});

// also runs whenever the SW imports for the first time after sleep
// (top-level code in service worker entry point)
void restoreFromStorage();
\`\`\`

three lifecycle events; chromatika handles each appropriately. \`onStartup\` fires when chrome boots; \`onInstalled\` fires on first install or update; SW top-level code runs every time SW spins up.

## the offscreen alternative (not used)

\`chrome.offscreen\` lets MV3 extensions create an offscreen DOM document for tasks SWs can't do (audio context, DOM parsing, etc.). chromatika **doesn't** use it today. target architecture has an offscreen media cache for NFT images; not shipped. manifest doesn't request \`offscreen\` permission.

## library

- browser native \`chrome.alarms\`, \`chrome.idle\`, \`chrome.runtime.onStartup\`, \`chrome.runtime.onInstalled\`
- internal: \`wallet-extension/src/background/index.ts\` for the wakeup wiring
- internal: \`wallet-extension/src/background/session.ts\` for the autolock + idle handling

## related

- [cold-sw-unlock-cache.md](/library/tech/cold-sw-unlock-cache) - the session-storage cache that survives SW unloads
- [chrome-storage-local-and-session.md](/library/tech/chrome-storage-local-and-session) - storage primitives
- [ika-presign-pool-impl.md](/library/tech/ika-presign-pool-impl) - the 5-min refill alarm consumer
- [eth-phishing-detect.md](/library/tech/eth-phishing-detect) - the daily refresh alarm consumer
`,Ok=`# \`chrome.declarativeNetRequest\` (dNR)

chromatika uses chrome's declarativeNetRequest (dNR) API for phishing protection. dNR lets extensions register URL-matching rules that chrome enforces at the network layer - block, redirect, modify headers, etc. - without the extension's JS being involved per-request. mandatory for MV3 since blocking \`webRequest\` was removed.

## why dNR (and not webRequest)

MV2 extensions could intercept network requests via \`chrome.webRequest.onBeforeRequest\` with a blocking listener that decides per-request whether to allow / redirect / cancel. chrome MV3 removed blocking webRequest for content scripts and most extension contexts, citing security + perf concerns.

dNR is the replacement: declare static / dynamic rules; chrome's network stack matches and acts. extension JS isn't woken per-request - the rules just live in chrome's matching engine.

trade-offs:
- **pro**: faster (no JS hop per request)
- **pro**: SW can stay asleep
- **con**: less flexible (no custom logic per-request)
- **con**: rule count limits (chrome caps at 5000 dynamic rules)

for chromatika's phishing use case, the trade-offs are net-positive: matching ~3500 known-bad domains against navigation requests is exactly what dNR is designed for.

## the rule shape

\`\`\`ts
const rule = {
  id: 1,                                                    // unique within the rule set
  priority: 1,                                              // higher = wins ties
  action: {
    type: 'redirect',
    redirect: {
      regexSubstitution: chrome.runtime.getURL('phishing-warning.html') + '?blocked=phishing-domain.com',
    },
  },
  condition: {
    urlFilter: '||phishing-domain.com^',                    // match any subdomain + path
    resourceTypes: ['main_frame'],                          // only navigation requests
  },
};
\`\`\`

\`urlFilter\` syntax is similar to AdBlock's:
- \`||\` matches the start of a hostname
- \`^\` matches a separator (path slash, query mark, end of url)
- \`*\` is a wildcard
- \`|...|\` matches the whole url

\`resourceTypes\` filters by what kind of request: \`main_frame\` (top-level navigation), \`sub_frame\` (iframes), \`xmlhttprequest\`, \`image\`, \`script\`, etc.

\`action.type\` can be:
- \`block\` - cancel the request
- \`redirect\` - reroute to a new URL
- \`modifyHeaders\` - add / remove / modify request or response headers
- \`allow\` - explicit allow (overrides higher-priority blocks)
- \`allowAllRequests\` - whitelist a tab

chromatika uses \`redirect\` so users see the warning page rather than a generic "site blocked" browser error.

## the dynamic vs static rule split

- **static rules**: declared in \`manifest.json\` under \`"rule_resources"\`. compiled at install time. chromatika doesn't use static rules
- **dynamic rules**: added via \`chrome.declarativeNetRequest.updateDynamicRules\` at runtime. survives SW unloads but is mutable. chromatika uses dynamic rules for phishing

dynamic limit: 5000 rules. chromatika caps at 4900 to leave headroom.

## the update API

\`\`\`ts
// add new rules + remove old
await chrome.declarativeNetRequest.updateDynamicRules({
  addRules: [rule1, rule2, ...],
  removeRuleIds: [oldId1, oldId2, ...],
});

// query existing rules
const existing = await chrome.declarativeNetRequest.getDynamicRules();
\`\`\`

chromatika's phishing sync (see [eth-phishing-detect.md](/library/tech/eth-phishing-detect)):
1. fetches the latest phishing config.json
2. computes new rule set (4900 highest-priority entries)
3. computes diff: new ids to add, old ids to remove
4. one \`updateDynamicRules\` call to apply

atomic: either all rules update or none do.

## the manifest declaration

\`manifest.json\`:
\`\`\`jsonc
{
  "permissions": [
    "declarativeNetRequest",
    "declarativeNetRequestWithHostAccess"   // lets us redirect across hosts
  ],
  "host_permissions": ["<all_urls>"]   // dNR needs broad host permission for cross-host redirects
}
\`\`\`

without \`host_permissions: ["<all_urls>"]\`, dNR can only match within the extension's own host (which would be useless for phishing protection that targets arbitrary domains).

## the redirect destination

\`\`\`
chrome-extension://<chromatika-id>/phishing-warning.html?blocked=evil-domain.com
\`\`\`

\`chrome.runtime.getURL('phishing-warning.html')\` returns the extension-scoped URL. the \`?blocked=\` query lets the warning page show which domain triggered.

\`web_accessible_resources\` in the manifest must include \`phishing-warning.html\` so dNR can redirect there:
\`\`\`jsonc
"web_accessible_resources": [{
  "resources": ["phishing-warning.html"],
  "matches": ["<all_urls>"]
}]
\`\`\`

## the rule cap (4900 vs 5000)

chrome's hard limit is 5000. chromatika uses 4900 to leave 100 rules of headroom for:
- future user-driven allowlist (override flagged domains the user knows are safe)
- typo-squat detection rules (per-pattern matchers)
- emergency rules (e.g. a critical zero-day campaign needs immediate blocking)

## what dNR doesn't catch

- requests that bypass chrome's network stack entirely (rare; mostly extension-to-extension messaging)
- DNS-over-HTTPS to non-chrome resolvers (chrome handles DoH internally where applicable)
- non-HTTP protocols (\`chrome:\`, \`about:\`, \`file:\` etc. aren't typical phishing vectors)
- subresources of allowed pages (the dapp itself loads scripts from CDNs; we don't block those even if the dapp is suspicious)

phishing protection is a **navigation** filter, not a deep-content filter.

## library

- browser native \`chrome.declarativeNetRequest\`, \`chrome.declarativeNetRequest.updateDynamicRules\`
- internal: \`wallet-extension/src/background/phishing.ts\` for the rule-build orchestration

## related

- [eth-phishing-detect.md](/library/tech/eth-phishing-detect) - the upstream data source
- [phishing-protection.md](/library/user/phishing-protection) (user-guides) - the user-facing surface
`,Hk=`# \`chrome.runtime.connect\` (tRPC port for UI ↔ background)

chromatika's UI (popup, side panel, full-page surfaces) talks to the background service worker over a long-lived \`chrome.runtime.Port\`. tRPC is layered on top: each tRPC procedure call serializes to a port message; the background dispatches to the handler; result returns over the same port. has a 12-second response timeout to defend against cold-SW silent stalls.

## why a port (not \`chrome.runtime.sendMessage\`)

\`chrome.runtime.sendMessage\` is one-shot - send a message, get a response. fine for read-only requests. but tRPC supports streaming, batching, request cancellation, and long-running operations. a long-lived port is the right primitive:
- one connection setup per UI session
- bidirectional messages (background can push events, e.g. \`'lock'\` notifications)
- in-order delivery
- automatic cleanup when the UI page closes (port disconnect event)

## the port lifecycle

\`\`\`ts
// UI side (popup/side-panel)
const port = chrome.runtime.connect({ name: 'chromatika-trpc' });
const trpcClient = createTRPCClient({
  links: [chromeRuntimePortLink({ port })],
});

// every trpcClient.someProcedure.query(...) call:
// 1. serialize the tRPC request (with superjson for bigint/Date support)
// 2. port.postMessage({ type: 'trpc-req', id, method, params })
// 3. await matching 'trpc-resp' on port.onMessage
// 4. resolve / reject the promise

// background side
chrome.runtime.onConnect.addListener(port => {
  if (port.name !== 'chromatika-trpc') return;
  port.onMessage.addListener(async (msg) => {
    if (msg.type !== 'trpc-req') return;
    try {
      const result = await trpcRouter[msg.method](msg.params);
      port.postMessage({ type: 'trpc-resp', id: msg.id, result });
    } catch (e) {
      port.postMessage({ type: 'trpc-resp', id: msg.id, error: serializeError(e) });
    }
  });
  port.onDisconnect.addListener(() => {
    // cleanup any per-port subscriptions
  });
});
\`\`\`

## the 12-second timeout

per \`src/lib/trpc.ts\`, every tRPC procedure has a **12-second port response timeout**. if the background doesn't respond in 12 seconds, the UI rejects the procedure with a timeout error. this defends against:
- cold service worker that silently fails to reach handler dispatch
- background hung on an unrelated operation
- network operations (e.g. RPC calls) that take too long

**exempted** procedures: long-running mutations like \`executeSwap\` (20s keepalive), \`getDwalletHomeGasMany\` (20s), \`presign refill\` if user-initiated, etc. these mark themselves as "long-running" via a tRPC meta and the client increases the timeout per-call.

## the sendMessage fallback

for cases where a port isn't available (e.g. content-script context that isn't UI), chromatika falls back to \`chrome.runtime.sendMessage\`. the dapp bridge (page → content-script → background) uses this. tRPC isn't layered on it; just bare message passing.

## the cold-SW handshake

chrome MV3 service workers go to sleep after ~30s idle. when a UI page (e.g. popup) opens after the SW slept:
1. UI calls \`chrome.runtime.connect\` - this implicitly **wakes** the SW
2. SW spins up, registers \`chrome.runtime.onConnect\` handlers
3. there's a brief race: the UI's first port message could land before the SW's listener registers
4. if so, chrome holds the message in queue and delivers when the listener attaches

usually this works seamlessly. occasionally a cold SW takes long enough that the 12s timeout fires before the listener is up - that's the "wallet not ready, retry" case.

## superjson for serialization

tRPC procedures often use \`bigint\` (e.g. wei amounts), \`Date\` (timestamps), \`Map\` (lookup tables) - JSON doesn't natively serialize these. chromatika uses **superjson** to wrap the tRPC payload:

\`\`\`ts
{
  json: { ... actual JSON-serializable shape ... },
  meta: { references: { "$.amount": "bigint" } }   // restoration map
}
\`\`\`

on the receiving side, superjson reads \`meta.references\` and restores types. transparent to handler code; just feels like native types passed across the boundary.

## per-tab isolation

each UI page (popup, side panel, settings page) gets its **own port**. the background tracks ports for:
- broadcasting events (e.g. lock state changed → notify all UI ports)
- per-port subscriptions (e.g. \`signingProgress\` polling)
- cleanup on disconnect

ports don't leak between UI surfaces - locking via the popup correctly broadcasts \`disconnect\` to the side panel's port too.

## library

- browser native \`chrome.runtime.connect\`, \`chrome.runtime.onConnect\`, \`chrome.runtime.Port\`
- \`@trpc/client\`, \`@trpc/server\` for the tRPC layer
- \`superjson\` for serialization
- internal: \`wallet-extension/src/lib/trpc.ts\` for the chrome-runtime-port tRPC link
- internal: \`wallet-extension/src/server/trpc.ts\` for the background-side router

## related

- [chrome-runtime-connectnative.md](/library/tech/chrome-runtime-connectnative) - the analog for MCP native messaging
- [cold-sw-unlock-cache.md](/library/tech/cold-sw-unlock-cache) - how the SW survives idle teardown
- [mcp-tool-routing.md](/library/tech/mcp-tool-routing) - tRPC procedures the MCP layer dispatches into
`,qk=`# \`chrome.runtime.connectNative\` (MCP native messaging)

chromatika spawns the MCP native messaging host via \`chrome.runtime.connectNative('com.chromatika.mcp_host')\`. unlike the tRPC port (which connects in-process between UI and SW), \`connectNative\` reaches an **out-of-process** native binary that chrome spawns as a child process and pipes stdio to. this is how chromatika reaches outside the browser to host the localhost HTTP MCP listener.

## the manifest dance

\`connectNative\` looks up a per-OS native messaging manifest (see [mcp-host-spawn-and-setup.md](/library/tech/mcp-host-spawn-and-setup)):
- the manifest's \`name\` field must match the \`connectNative\` argument
- \`allowed_origins\` must include the chromatika extension id
- \`path\` points at the host binary

chrome refuses to connect if any check fails - returns a port that immediately disconnects with a \`chrome.runtime.lastError\`.

## the wire protocol (chrome ↔ native host)

unlike \`chrome.runtime.connect\` which uses arbitrary JS messages, \`connectNative\` uses a specific binary frame protocol:

\`\`\`
[4-byte LE length prefix] [UTF-8 JSON body]
\`\`\`

per direction. host's stdin gets framed JSON from chrome; host writes framed JSON to stdout. see [mcp-native-messaging-frame.md](/library/tech/mcp-native-messaging-frame) for the full details.

## the chromatika side

\`\`\`ts
let nativePort: chrome.runtime.Port | null = null;
let reconnectAttempt = 0;

async function connectMcpHost() {
  if (nativePort) return;   // already connected

  nativePort = chrome.runtime.connectNative('com.chromatika.mcp_host');

  nativePort.onMessage.addListener((msg) => {
    handleNativeFrame(msg);   // see mcp-tool-routing for handlers
  });

  nativePort.onDisconnect.addListener(() => {
    const err = chrome.runtime.lastError;
    nativePort = null;
    if (mcpEnabled() && reconnectAttempt < 5) {
      const delay = Math.min(30_000, 1000 * Math.pow(2, reconnectAttempt));
      setTimeout(() => connectMcpHost(), delay);
      reconnectAttempt++;
    }
  });

  // push initial config
  const { tokenHex } = await chrome.storage.local.get('chromatika_mcp_v1');
  nativePort.postMessage({ type: 'config', tokenHex });
}
\`\`\`

## the auto-reconnect with backoff

\`\`\`
attempt 1: 1 second delay
attempt 2: 2 seconds
attempt 3: 4 seconds
attempt 4: 8 seconds
attempt 5: 16 seconds (capped at 30)
\`\`\`

after 5 failed attempts, give up. user has to manually re-enable MCP via \`mcpEnable\` to retry. prevents infinite reconnect loops on permanent failure (e.g. host binary missing).

\`reconnectAttempt\` resets to 0 on a successful connect.

## the lifecycle states chromatika tracks

\`\`\`ts
type McpHostState =
  | { kind: 'disabled' }
  | { kind: 'connecting' }
  | { kind: 'connected', port: number, host: string }
  | { kind: 'reconnecting', attempt: number, nextRetryAtMs: number }
  | { kind: 'failed', error: string };
\`\`\`

surfaced via \`mcpStatus\` for the agent settings UI to display.

## the cold-SW restart case

when the SW dies and respawns:
1. SW reads \`chromatika_mcp_v1.enabled\`
2. if enabled, calls \`connectMcpHost()\` (fresh connection)
3. host process **also** respawns (the previous host process exited when its stdin closed)
4. fresh handshake: chromatika pushes \`{ type: 'config', tokenHex }\`, host responds with \`{ kind: 'config-ack' }\` then \`{ kind: 'listen', port }\`

if the user pinned a desired port via \`mcpSetDesiredPort\`, chromatika pushes \`{ type: 'reconfigure-port', port }\` after receiving the initial \`{ kind: 'listen' }\`. host rebinds to the desired port, sends fresh \`{ kind: 'listen' }\`.

so the URL the user has in their Claude Desktop config stays stable across SW restarts (assuming no port conflict).

## why this is different from regular ports

- regular \`chrome.runtime.connect\` reaches another JS context inside the same extension (UI ↔ SW, SW ↔ content-script)
- \`chrome.runtime.connectNative\` reaches a **separate process** outside chrome
- separate process means the host can do things SW can't:
  - bind a TCP listener (SW can't listen on sockets)
  - run synchronous file I/O
  - speak protocols beyond what chrome's \`fetch\` can speak

trade-off: the native host requires per-OS setup (the manifest + binary registration). regular extension communication doesn't.

## what \`connectNative\` doesn't do

- doesn't authenticate the host beyond the manifest's \`allowed_origins\`. the host accepts whatever the manifest registered. if an attacker can write to the manifest directory (probably needs OS-level access), they can swap in a different host
- doesn't encrypt the stdio pipe. it's local IPC, not network. encryption isn't necessary
- doesn't rate-limit. chromatika handles rate-limiting at the MCP layer (per-tool-call)

## library

- browser native \`chrome.runtime.connectNative\`, \`chrome.runtime.Port\`, \`chrome.runtime.lastError\`
- internal: \`wallet-extension/src/background/mcp/mcp-native-bridge.ts\` for the connect / reconnect orchestration

## related

- [mcp-host-spawn-and-setup.md](/library/tech/mcp-host-spawn-and-setup) - the per-OS manifest setup
- [mcp-native-messaging-frame.md](/library/tech/mcp-native-messaging-frame) - the wire format
- [mcp-protocol-overview.md](/library/tech/mcp-protocol-overview) - what chromatika does once connected
- [chrome-runtime-connect-trpc-port.md](/library/tech/chrome-runtime-connect-trpc-port) - the in-process port analog
`,Vk="# `chrome.storage.local` + `chrome.storage.session`\n\nchromatika persists state in two chrome storage areas with very different semantics. `chrome.storage.local` survives chrome restarts (but is cleared on uninstall); `chrome.storage.session` only survives until chrome quits or the SW is unloaded. choosing the right area is security-critical.\n\n## the two areas\n\n| area | persists across browser quit? | persists across SW unload? | quota | use for |\n|------|-------------------------------|----------------------------|-------|---------|\n| `chrome.storage.local` | yes (until uninstall) | yes | 5 MB default, can request more via `'storage'` permission with unlimited storage | encrypted vault blob, dWallet meta, registry, settings |\n| `chrome.storage.session` | **no** | yes (within a chrome session) | a few MB | unlock cache (post-KDF AES key bytes), in-flight pending requests |\n\n`chrome.storage.sync` (a third area that syncs across browser profiles) is **not used** by chromatika - per-install scoping is intentional.\n\n## chromatika's keys in `chrome.storage.local`\n\nper CLAUDE.md convention `chromatika_<domain>_v<N>`:\n\n| key | content |\n|-----|---------|\n| `chromatika_vault_v3` | encrypted multi-vault blob |\n| `chromatika_dwallet_meta_v2_<vaultId>` | per-vault dWallet overlay (cap ids, addresses) |\n| `chromatika_presign_pools_v3_<vaultId>` | per-vault ika presign pools |\n| `chromatika_active_networks_v1` | per-chain active network selection |\n| `chromatika_custom_networks_v1` | user-added EVM (and others) custom networks |\n| `chromatika_media_safety_v1` | MediaSafetyMode value |\n| `chromatika_dapp_permissions_v1` | dapp connection permissions |\n| `chromatika_hw_accounts_v1` | hardware account list (Ledger / Trezor / MWA / WC) |\n| `chromatika_advanced_mode_v1` | advanced UI toggle |\n| `chromatika_ika_base_mode_v1` | global ika base preference (sui / solana) |\n| `chromatika_mcp_v1` | MCP agent surface state (token, port, enabled flag) |\n| `chromatika_x402_caps_v1` | x402 spending caps |\n| `chromatika_x402_receipts_v1` | x402 payment receipts (capped at 200) |\n\nbumping the integer suffix denotes a schema change. parser rejects older versions on load (chromatika is pre-release; no migration path yet).\n\n## chromatika's keys in `chrome.storage.session`\n\n| key | content |\n|-----|---------|\n| `chromatika_unlock_cache_v1` | post-argon2id AES key bytes + KDF meta (b64) |\n| `chromatika_pending_<requestId>` | in-flight tx-approval / sign-approval / x402-approval requests waiting on user action |\n\nnote: a legacy `chromatika_unlock_cache_v1_local` (in `local`, not `session`) is **explicitly removed on lock / unlock / write** by the current code. plaintext password fields are also defensively removed. session storage is the only correct place for unlock material. see [cold-sw-unlock-cache.md](/library/tech/cold-sw-unlock-cache).\n\n## reading + writing\n\n```ts\n// async API; returns a promise (or accepts callback in legacy code)\nconst { chromatika_vault_v3 } = await chrome.storage.local.get('chromatika_vault_v3');\nawait chrome.storage.local.set({ chromatika_vault_v3: newBlob });\nawait chrome.storage.local.remove('chromatika_unlock_cache_v1_local');\n\n// session same shape\nconst cached = await chrome.storage.session.get('chromatika_unlock_cache_v1');\n```\n\n`webextension-polyfill` (the cross-browser shim chromatika uses) wraps these; works on Firefox + Chrome with the same API.\n\n## the change-event subscription\n\n```ts\nchrome.storage.onChanged.addListener((changes, areaName) => {\n  if (areaName === 'local' && 'chromatika_vault_v3' in changes) {\n    // reload vault state\n  }\n});\n```\n\nuseful for cross-context sync. e.g. one popup updates `chromatika_dapp_permissions_v1`, another popup's listener picks it up immediately.\n\n## quota considerations\n\n- the encrypted vault blob can grow (each vault record adds bytes). 100 vaults each with mnemonic + ika keys + multiple envelopes ~ a few MB. fits comfortably in default quota\n- presign pools can grow to ~1 KB per entry × ~50 entries × 3 pools × N vaults\n- x402 receipts capped at 200 - bounded\n- if quota becomes an issue, request `'unlimitedStorage'` permission\n\n## clear-on-uninstall\n\nuninstalling chromatika **wipes** `chrome.storage.local` and `chrome.storage.session` for chromatika's origin. the encrypted vault blob is gone. user has to re-onboard with the mnemonic / passkey / hardware path on reinstall.\n\nthis is **the only** way to truly delete chromatika's local state. clearing browser cache / cookies doesn't touch extension storage.\n\n## the offscreen storage absence\n\nchromatika does **not** use `chrome.offscreen` (a relatively new MV3 surface for background DOM contexts). target architecture has an offscreen media cache for NFT images; not implemented today. when it ships, manifest will request the `offscreen` permission - until then, no.\n\n## library\n\n- `webextension-polyfill` for cross-browser API parity\n- browser native `chrome.storage.local`, `chrome.storage.session`, `chrome.storage.onChanged`\n- internal: per-domain helpers in `wallet-extension/src/background/storage/*` (e.g. `vault-storage.ts`, `dwallet-meta-storage.ts`, etc.)\n\n## related\n\n- [cold-sw-unlock-cache.md](/library/tech/cold-sw-unlock-cache) - the session-storage unlock cache details\n- [vault-blob-v3-format.md](/library/tech/vault-blob-v3-format) - the largest single record\n- [chrome-alarms-and-idle.md](/library/tech/chrome-alarms-and-idle) - storage-adjacent SW lifetime primitives\n",Gk=`# cold-SW unlock cache (\`chromatika_unlock_cache_v1\`)

chrome MV3 service workers get torn down after ~30 seconds of inactivity. when the user comes back, the service worker spins up "cold" - empty memory, no session state. without a cache, every cold start would force the user to re-enter their password (or re-run their passkey / hardware unlock). the unlock cache fixes that without ever persisting plaintext credentials.

## the cache record

stored in \`chrome.storage.session\`:

\`\`\`jsonc
{
  "chromatika_unlock_cache_v1": {
    "vaultBlobIvB64": "<the v3 vault blob's IV>",
    "masterKeyBytesB64": "<32 raw bytes of the master AES key>",
    "envelopeKindHint": "password" | "passkey-prf" | ...,
    "lockAtMs": 1700000123000,   // when autolock should fire
    "createdAtMs": 1700000000000
  }
}
\`\`\`

**the \`masterKeyBytesB64\` is the post-envelope-unwrap master key bytes** - what would normally be re-derived by going envelope → envKey → AES-GCM unwrap. by caching the bytes after unwrap, cold restarts skip both argon2id (slow!) and webauthn / wallet-signature ceremonies (UI-prompt!). the key bytes themselves are 32 random bytes - useful only if attacker can read \`chrome.storage.session\`, which is itself ephemeral.

## why \`chrome.storage.session\` and not \`chrome.storage.local\`

\`chrome.storage.session\`:
- cleared when the **chrome browser quits** (not just when the SW idles)
- cleared on **manual lock**
- cleared on **uninstall**
- cleared if the user clears site data
- not synced across devices
- not visible to content scripts

\`chrome.storage.local\`:
- persists across browser quits
- can be read by extension code from any context
- backed by disk (sqlite under the hood)
- persists until explicit \`.clear()\` or uninstall

caching the master key bytes in \`chrome.storage.local\` would **defeat the entire point** of the unlock - chrome restart would unlock the vault without any user action. session storage is the only correct place.

## the legacy \`chromatika_unlock_cache_v1_local\` reject

an older code path mistakenly cached unlock material in \`chrome.storage.local\`. that key (\`chromatika_unlock_cache_v1_local\`) is **explicitly removed on lock / unlock / write** by the current code. if you're poking at storage and see it, that's a stale leftover - safe to delete.

similarly: any cache row containing a \`password\` field (a plaintext password, ever) is **removed on touch**. plaintext passwords have **never** been correct in this cache; the rule is enforced defensively.

## the rehydrate flow

\`\`\`
1. service worker cold-starts (e.g. user opens the popup after 5 min idle)
2. tRPC handler initializes; reads chrome.storage.session for chromatika_unlock_cache_v1
3. if cache exists AND cache.lockAtMs > now:
     - import cache.masterKeyBytesB64 as a non-extractable AES CryptoKey
     - decrypt the v3 vault blob with this key
     - reconstruct sessionState from the decrypted records
     - resume autolock alarm with the remaining time until lockAtMs
4. if cache missing OR cache.lockAtMs <= now:
     - clear cache
     - require a fresh unlock (password / passkey / signature / words)
\`\`\`

## the lock flow

\`\`\`
1. user clicks lock OR autolock alarm fires OR OS screen-lock detected via chrome.idle:
     - clear chrome.storage.session keys related to chromatika
     - drop the in-memory CryptoKey reference
     - dapp bridge broadcasts disconnect to all connected origins
     - emit 'locked' event so UI surfaces re-render
\`\`\`

## the autolock interaction

autolock window is set per-unlock (1-1440 minutes, default 30). when the user unlocks:

\`\`\`
lockAtMs = now + autolockMinutes * 60_000
chrome.alarms.create('chromatika-autolock', { when: lockAtMs })
chrome.storage.session.set({ chromatika_unlock_cache_v1: { ..., lockAtMs } })
\`\`\`

each user action (tRPC call, UI interaction routed through tRPC) **resets** the alarm by computing a fresh \`lockAtMs = now + autolockMinutes * 60_000\`. user idleness lets the alarm fire on schedule.

## attack model

- **\`chrome.storage.session\` exfiltration**: if an attacker can read session storage, they get the master key bytes. they can decrypt the vault until the lock fires. mitigation: lock manually before you walk away; OS screen-lock triggers wallet lock. session storage is per-origin, so other extensions can't read it without exploit.
- **service worker memory dump**: if an attacker can dump SW memory, they get the imported \`CryptoKey\` (non-extractable, but the underlying buffer exists). same mitigations.
- **password leak via unlock cache**: not possible. password is never written to the cache. only the post-argon2id master key bytes are.

## what the cache holds, exactly

| field | what | is it a secret |
|-------|------|----------------|
| \`vaultBlobIvB64\` | the AES-GCM IV for the vault blob | no (random, public-equivalent) |
| \`masterKeyBytesB64\` | 32-byte master AES key | YES - root of trust during unlocked session |
| \`envelopeKindHint\` | which envelope was used to unlock (display) | no |
| \`lockAtMs\` | when autolock fires | no |
| \`createdAtMs\` | when this unlock happened | no |

\`masterKeyBytesB64\` is the only sensitive item. its lifetime is bounded by \`chrome.storage.session\`'s clear-on-quit + the autolock window.

## not in the cache

- the password (never)
- the passkey credential id (in vault blob; not needed for cache rehydrate since masterKey is already unwrapped)
- the BIP39 mnemonic (in vault blob; ditto)
- any hardware-wallet signature (in envelopes; ditto)
- ika user-share encryption keys (in vault blob; ditto)
- presign material (in \`chromatika_presign_pools_v3_<vaultId>\`)

the cache only stores what's needed to **decrypt the vault payload on cold start**. everything else lives in the (still-encrypted) vault blob.

## library

- \`chrome.storage.session.get\` / \`.set\` / \`.remove\` for cache R/W
- \`chrome.alarms\` for the autolock timer
- \`chrome.idle\` for OS screen-lock detection
- \`crypto.subtle.importKey\` to rehydrate the AES \`CryptoKey\`
`,$k=`# dapp bridge message validation

the chromatika dapp bridge spans three JS contexts: **page world** (where dapp code runs and our injected provider lives), **content-script isolated world** (where our content script runs), and **background service worker** (where everything signs). messages flow via \`window.postMessage\` (page ↔ content-script) and \`chrome.runtime.sendMessage\` / port (content-script ↔ background). every hop has validation to prevent spoofing.

## the threat model

without validation:
- a malicious iframe could \`postMessage\` claiming to be the parent origin, get its tx approved as if it came from the parent
- a sibling frame could intercept messages between the dapp and our content script
- a content script of another extension could in theory race to register a similar provider
- a compromised page could try to bypass approval by directly \`postMessage\`-ing the content script

## the page → content-script hop

page-script wraps \`window.ethereum.request(...)\`:
\`\`\`ts
function request(args) {
  return new Promise((resolve, reject) => {
    const requestId = randomId();
    pending.set(requestId, { resolve, reject });

    window.postMessage({
      type: 'chromatika_dapp_request',
      method: args.method,
      params: args.params,
      requestId,
    }, window.location.origin);    // explicit targetOrigin
  });
}

window.addEventListener('message', (event) => {
  // VALIDATION 1: must come from the same window
  if (event.source !== window) return;

  // VALIDATION 2: must have our message type
  if (event.data?.type !== 'chromatika_dapp_response') return;

  // VALIDATION 3: must have a known requestId
  const entry = pending.get(event.data.requestId);
  if (!entry) return;
  pending.delete(event.data.requestId);

  if (event.data.error) entry.reject(event.data.error);
  else entry.resolve(event.data.result);
});
\`\`\`

three checks:
- \`event.source === window\` (it came from our own window, not a sibling iframe)
- \`event.data.type\` matches our expected message type
- \`event.data.requestId\` was issued by us (i.e. we sent the matching request)

## the content-script side

content script listens for \`window\` postMessages too:
\`\`\`ts
window.addEventListener('message', async (event) => {
  // VALIDATION 1: must come from the same window
  if (event.source !== window) return;

  // VALIDATION 2: must be a request type we handle
  if (event.data?.type !== 'chromatika_dapp_request') return;

  // VALIDATION 3: validate origin matches the page's origin
  if (event.origin !== window.location.origin) return;

  // forward to background
  const result = await chrome.runtime.sendMessage({
    type: 'dapp_request',
    origin: event.origin,        // attached by content-script, not from page-supplied data
    method: event.data.method,
    params: event.data.params,
    requestId: event.data.requestId,
  });

  // post response back to page world
  window.postMessage({
    type: 'chromatika_dapp_response',
    requestId: event.data.requestId,
    result: result.result,
    error: result.error,
  }, window.location.origin);
});
\`\`\`

key points:
- \`event.origin\` is the **content-script's** view of the page origin, not something the page can spoof
- \`origin\` is attached to the message **by the content script** before sending to background, so background can trust it
- forwarding back uses an explicit \`targetOrigin\` so the message can't leak to cross-origin frames

## the content-script → background hop

\`chrome.runtime.sendMessage\` carries a \`sender\` object that chrome attaches:
\`\`\`ts
chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
  // sender.tab.url is the page url; sender.origin is the page origin
  // these are CHROME-ATTACHED, not from the message payload - trustworthy

  // VALIDATION 4: cross-check that message.origin matches sender.origin
  const senderOrigin = new URL(sender.tab.url).origin;
  if (message.origin !== senderOrigin) {
    sendResponse({ error: 'origin mismatch' });
    return true;
  }

  // dispatch to handler
  handleDappRequest(senderOrigin, message.method, message.params)
    .then(result => sendResponse({ result }))
    .catch(error => sendResponse({ error }));

  return true;   // async response
});
\`\`\`

chromium's \`MessageSender\` includes \`tab.url\` (the URL of the tab that sent the message) and \`origin\` (chromium-derived). these are **not page-influenced** - chrome populates them based on which tab actually sent. so even if the content-script's message claims \`origin: 'attacker.com'\`, \`sender.origin\` reveals the real page.

## iframes and embedded contexts

an iframe at \`https://embed.example.com\` running inside \`https://parent.example.com\` sees:
- \`window.location.origin === 'https://embed.example.com'\`
- content-script for that frame validates against \`embed.example.com\`
- chrome reports \`sender.tab.url\` of the parent tab BUT \`sender.frameId\` distinguishes the frame
- chromatika treats each frame's origin as its own origin for permissions

so a connection request from \`embed.example.com\` does NOT inherit \`parent.example.com\`'s permissions. each frame asks for its own connection.

## the \`event.source === window\` gotcha

inside an iframe, \`window\` is the iframe's window. \`event.source === window\` confirms the message came from the iframe's own document, not from the parent. this prevents a parent from injecting messages into the iframe's listener via \`iframe.contentWindow.postMessage(...)\` claiming to be the iframe.

(actually \`iframe.contentWindow.postMessage\` would arrive with \`event.source\` pointing at the parent, not the iframe's window. so the check correctly rejects.)

## what could go wrong

- **content-script not loaded yet**: page-script's request hangs until the content-script attaches. timeout with retry (or a visible "wallet not ready" error)
- **content-script crashed**: \`chrome.runtime.sendMessage\` returns \`{ error: 'No matching listener' }\` or similar. page-script surfaces a wallet error
- **cross-origin frames trying to read each other's messages**: \`postMessage\` is same-origin by default; \`targetOrigin: '*'\` would broadcast, but we always specify \`window.location.origin\`
- **rogue extension intercepting messages**: extensions don't have access to other extensions' content-script messages. our content-script's \`chrome.runtime.sendMessage\` goes to **chromatika's** background only

## library

- browser native \`window.postMessage\`, \`window.addEventListener('message', ...)\`
- \`chrome.runtime.sendMessage\`, \`chrome.runtime.onMessage\` for cross-context bridge
- internal: \`wallet-extension/src/content-script/dapp-bridge-listener.ts\` content-script side
- internal: \`wallet-extension/src/dapp-interface/inject.ts\` page-script side
- internal: \`wallet-extension/src/background/dapp-bridge.ts\` background dispatcher

## related

- [eip-1193-and-6963.md](/library/tech/eip-1193-and-6963) - the EVM provider this bridge serves
- [wallet-standard-sui-and-solana.md](/library/tech/wallet-standard-sui-and-solana) - non-EVM equivalent
- [eip-3085-3326.md](/library/tech/eip-3085-3326) - chain management subset
- [phishing-protection.md](/library/user/phishing-protection) (user-guides) - dNR-level URL filtering on top of this bridge
`,Qk=`# secp256k1 ECDSA in chromatika

secp256k1 is the elliptic curve that bitcoin and ethereum sign on. ECDSA is the signature scheme. chromatika uses ECDSA-secp256k1 for **all EVM signing** (transactions, \`personal_sign\`, EIP-712 typed data) and for **Bitcoin segwit P2WPKH** sends. the actual signing happens via ika MPC (2PC-MPC SECP256K1 ECDSA), not locally.

## the curve

- secp256k1 order: \`n = 0xFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFEBAAEDCE6AF48A03BBFD25E8CD0364141\`
- generator: standard secp256k1 generator G
- public key: 32-byte x + 32-byte y (uncompressed = 65 bytes with 0x04 prefix; compressed = 33 bytes with 0x02 / 0x03 prefix indicating y parity)
- secret key: 32-byte scalar in \`[1, n-1]\`

## signature shape

a raw ECDSA signature is \`(r, s)\` where each is a 32-byte scalar mod \`n\`. ika returns these as a 64-byte compact \`r || s\`. EVM additionally wants a recovery byte \`v\` so verifiers can recover the public key from the signature - chromatika picks \`v\` after the fact.

## the chromatika EVM signing pipeline

\`\`\`
1. build the unsigned transaction via ethers
   tx_unsigned = Transaction.from({ to, value, data, chainId, nonce, gas... })
2. compute the preimage bytes
   preimage = tx_unsigned.unsignedSerialized
3. hand ika the PREIMAGE (not the digest!)
   sig_64bytes = await ika.sign(preimage, dwallet, SECP256K1, ECDSASecp256k1)
4. ika hashes preimage with KECCAK256 once internally and signs
5. parse the response with parseSignatureFromSignOutput(Curve.SECP256K1, SignatureAlgorithm.ECDSASecp256k1)
   → asserts 64-byte compact r||s
6. compute digest yourself: digest = keccak256(preimage)
7. try both v values:
   addr_27 = recoverAddress(digest, { r, s, v: 27 })
   addr_28 = recoverAddress(digest, { r, s, v: 28 })
   pick the v whose recovered addr matches the dWallet's known EVM address
8. assemble final 65-byte sig: r || s || (v + chainId * 2 + 35)  // EIP-155 v adjustment for tx
   or: r || s || v  // for personal_sign (no chain mixing)
\`\`\`

step 3 is **the rule**: pass preimage, not hash. ika internally does \`digest = keccak256(preimage)\` and signs \`(r, s)\` over \`digest\`. if you pass the digest already-keccaked, ika hashes the digest again, signs the wrong thing, and the recovered address won't match.

step 7 is the v-recovery dance. ECDSA's recovery byte distinguishes between the two possible public-key candidates that satisfy \`r\`. EVM's convention is \`v ∈ {27, 28}\` (or the EIP-155 chain-mixed equivalent for transactions); we pick whichever recovers to our known address.

## \`personal_sign\` vs typed-data v4

\`personal_sign\` per EIP-191:
\`\`\`
preimage_msg = "\\x19Ethereum Signed Message:\\n" + len(message) + message
preimage_to_sign = preimage_msg
sig = ecdsa_sign(keccak256(preimage_msg), key)
\`\`\`

chromatika builds \`preimage_msg\` (the EIP-191 wrapped form), hands it to ika as the preimage, ika keccak256s + signs.

typed-data v4 per EIP-712:
\`\`\`
preimage_typed = TypedDataEncoder.encode(domain, types, value)
                 = 0x1901 || domainSeparator || hashStruct(message)
sig = ecdsa_sign(keccak256(preimage_typed), key)
\`\`\`

\`TypedDataEncoder.encode(...)\` from ethers v6 returns the encoded preimage - the bytes that should be keccak256'd to get the EIP-712 digest. chromatika hands ika **\`encode(...)\`**, not \`hash(...)\`. ika keccak256s + signs.

if you ever see code that calls \`TypedDataEncoder.hash(...)\` and passes the result to ika, that's a double-hash bug.

## bitcoin secp256k1 signing

P2WPKH signing uses ECDSA-secp256k1 against the segwit transaction sighash (BIP143). chromatika builds the transaction with bitcoinjs-lib, computes the BIP143 sighash (a \`SHA256(SHA256(...))\` double-hash over the witness preimage), and hands that **digest** to ika.

wait - if EVM passes preimage and bitcoin passes digest, what's the rule?

the rule is: ika hashes its input **once with KECCAK256** before signing for SECP256K1_ECDSA. so:
- for EVM, the natural digest is keccak256(preimage) → pass preimage, let ika hash
- for Bitcoin BIP143 sighash, the natural digest is \`SHA256(SHA256(preimage))\` (NOT keccak256) → there's no way to pre-keccak the input to land on the right sha256 digest, so chromatika passes the **already-double-sha256'd** digest as the "preimage" (which ika will then keccak256 a third time) - or more practically, chromatika uses the SECP256K1_ECDSA path for EVM only and SECP256K1_TAPROOT for Bitcoin where the hash function differs (see [taproot-schnorr.md](/library/tech/taproot-schnorr)).

actually, for Bitcoin P2WPKH chromatika still uses SECP256K1_ECDSA but with a wrapper that handles the SHA-256 sighash differently - check \`wallet-extension/src/background/chains/signing/btc.ts\` for the exact path. don't infer from this doc; the BTC signing helper has the canonical implementation.

## library

- \`@noble/secp256k1\` for the curve math + signature verification
- ethers v6 \`Transaction\`, \`TypedDataEncoder\`, \`recoverAddress\`, \`SigningKey.recoverPublicKey\` for EVM tx + sig handling
- \`bitcoinjs-lib\` for BIP143 sighash, transaction serialization, address encoding
- \`@ika.xyz/sdk\` \`parseSignatureFromSignOutput\` for normalizing ika output into \`(r, s)\`

## the v=0/1 vs 27/28 convention

different sources serialize \`v\` differently:
- raw ECDSA recovery: \`v ∈ {0, 1}\` (which y candidate)
- ethereum classic / EIP-155 transactions: \`v = 35 + chainId * 2 + (0 or 1)\`
- ethereum personal sign: \`v ∈ {27, 28}\` (the historical bitcoin-message-signing offset)

chromatika handles all three. ethers does most of the conversion; you only see raw \`{0, 1}\` if you're unwrapping ika output by hand.
`,Jk=`# ed25519 EdDSA in chromatika

ed25519 is the elliptic curve + signature scheme that Sui, Solana, Aptos, ika ED25519 dWallets, and a chunk of the WebAuthn surface use. chromatika does **not** locally sign with ed25519 except for HD fee-payer Sui sends - everything else routes through ika MPC (2PC-MPC ED25519 EDDSA) or hardware wallets.

## the curve

- curve: Edwards25519 (twisted Edwards form of curve25519)
- secret seed: 32 bytes (the "key" you generate / hold)
- secret scalar: derived deterministically from the seed via SHA-512(seed) → first 32 bytes clamped → scalar \`a\`
- public key: 32-byte point \`A = a * B\` where \`B\` is the base point
- canonical Solana / Phantom / \`solana-keygen\` "secret key" format: 64 bytes = \`[seed(32) || pubkey(32)]\`. this is the format that feeds \`ikaRootSeedFromSolanaKeypair\`

## signature shape

per RFC 8032, an ed25519 signature is 64 bytes = \`R(32) || S(32)\`. signing produces:
\`\`\`
nonce_prefix = SHA-512(seed)[32..64]
r = SHA-512(nonce_prefix || message) mod L   // L = curve order
R = r * B
k = SHA-512(R || A || message) mod L
S = (r + k * a) mod L
sig = R || S
\`\`\`

verify:
\`\`\`
check 8 * S * B == 8 * R + 8 * SHA-512(R || A || message) * A
\`\`\`

## the determinism property (RFC 8032)

ed25519 is **deterministic**: signing the same message with the same key produces the **same signature, every time**. this matters because chromatika's Solana hardware-vault flow (Seeker remote, WalletConnect) leans on this:

\`\`\`
seeker.sign(IKA_USK_DERIVATION_MESSAGE = "ika.chromatika.user-share-encryption-key.v1")
  → 64-byte signature (deterministic)
ika_user_share_seed = keccak256(signature || encryption_key_index_le)
\`\`\`

restore on a new device works because:
1. user pairs the same Seeker
2. asks Seeker to sign the same message
3. RFC 8032 determinism = same signature
4. same keccak preimage = same ika seed
5. same ika user-share encryption key = same dWallet

if Seeker (or any wallet) used a non-deterministic ed25519 implementation, this restore-on-new-device flow would fail. chromatika probes WAAP wallets at pairing time (sign twice, compare) to detect non-determinism and fall back to the recovery-words branch instead.

## ika ED25519 EDDSA signing path

ika MPC produces an ed25519 signature for a given preimage:

\`\`\`
1. chromatika hands ika the message bytes (raw, not pre-hashed)
2. ika MPC servers + chromatika local share collaboratively run the EdDSA protocol
3. internally ika hashes with SHA-512 per RFC 8032
4. result: 64-byte (R || S) ed25519 signature
5. parseSignatureFromSignOutput(Curve.ED25519, SignatureAlgorithm.EdDSA) returns (R, S)
\`\`\`

**rule**: pass raw bytes, not a digest. ika hashes internally per RFC 8032.

## the Sui SHA-512 vs Mysten BLAKE2b divergence

\`sui_signPersonalMessage\` is where ika and Mysten disagree. ika hands the raw user message to its EdDSA protocol → SHA-512 → R, S. Mysten's native flow wraps the message in an intent prefix and then BLAKE2b-256s it before ed25519 signing. dapps that use Mysten's \`verifyPersonalMessageSignature\` won't accept the ika sig until the BLAKE2b-intent path is added. see [sha512-and-blake2b.md](/library/tech/sha512-and-blake2b).

## ika curve / signature-algorithm constants

ika's protocol numbers (from CLAUDE.md):
- \`Curve.SECP256K1 = 0\`
- \`Curve.SECP256R1 = 1\`
- \`Curve.ED25519 = 2\`
- \`Curve.RISTRETTO = 3\`
- \`SignatureAlgorithm.ECDSASecp256k1 = 0\`
- \`SignatureAlgorithm.Taproot = 1\`
- \`SignatureAlgorithm.ECDSASecp256r1 = 2\`
- \`SignatureAlgorithm.EdDSA = 3\`
- \`SignatureAlgorithm.SchnorrkelSubstrate = 4\`

\`fromCurveToNumber\` is **not** exported from \`@ika.xyz/sdk\` main entry (it's internal to \`hash-signature-validation.js\`). if you need the mapping, hardcode the constants above; don't try to import the helper.

## chain-side public-key serialization

different chains serialize ed25519 public keys differently:
- **Sui**: \`flag_byte || pubkey(32)\` where \`flag_byte = 0x00\` for ed25519. address = \`blake2b_256(flag || pubkey)\`
- **Solana**: bare 32-byte pubkey, address = base58 of pubkey (no hash)
- **Aptos**: \`blake2b_256(pubkey || 0x00)\` for single-key (legacy). multi-key uses different layout
- **WebAuthn (passkey)**: COSE-encoded pubkey (cbor wrapping), or compressed secp256r1 - **not ed25519** for passkey vaults. WebAuthn supports both ed25519 and secp256r1 as algorithms; chromatika passkey vaults use **secp256r1** today (Sui SIP-9 with flag \`0x06\`)

## libraries

- \`@noble/ed25519\` for ed25519 keypair construction, signing, verification
- \`@solana/web3.js\` \`Keypair\` for Solana-side keypair serialization
- \`@mysten/sui\` \`Ed25519Keypair\` for Sui-side keypair serialization (wraps \`@noble/ed25519\`)
- ika MPC for the actual signature production (chromatika holds half the share, ika servers hold the other half)

## what chromatika does **not** do locally

- chromatika does not locally produce ed25519 signatures over user-facing messages. all such signing goes through ika MPC (or hardware via WebHID / MWA / WC)
- the **exception** is Sui HD fee-payer signing for \`sendSuiNative\` and the Sui PTB ops that pay fees - those use the locally-derived ed25519 keypair to sign the Sui transaction. this is a "we hold the gas key in software" tradeoff documented elsewhere
- locally-derived public keys (e.g. for address display, BLAKE2b-of-pubkey for Sui addresses) are computed without signing - those are pure derivations, no signing involved
`,Yk=`# EIP-1193 + EIP-6963 (EVM dapp injection)

EIP-1193 specifies the JavaScript provider interface every Ethereum dapp expects on \`window.ethereum\`. EIP-6963 (multi-injected provider discovery) lets multiple wallets coexist on the same page without clobbering \`window.ethereum\`. chromatika implements both - dapps see chromatika via either interface.

## EIP-1193 the interface

\`\`\`ts
interface Eip1193Provider {
  request(args: { method: string, params?: any[] | object }): Promise<any>;
  on(event: 'accountsChanged' | 'chainChanged' | 'connect' | 'disconnect', cb: Function): void;
  removeListener(event: string, cb: Function): void;
}
\`\`\`

key methods chromatika handles:
- \`eth_requestAccounts\` - opens connect approval popup, returns array of authorized addresses
- \`eth_accounts\` - returns currently authorized addresses (without prompting)
- \`eth_chainId\` - returns active EVM chain id (hex)
- \`eth_sendTransaction\` - opens TX_APPROVE popup, signs + broadcasts on approve
- \`personal_sign\` - opens sign popup, returns 65-byte hex signature
- \`eth_signTypedData_v4\` - opens sign popup, returns 65-byte hex signature
- \`eth_call\`, \`eth_getBalance\`, \`eth_estimateGas\`, \`eth_getCode\`, etc. - read-only RPCs pass through to the active EVM provider directly (no popup)
- \`wallet_addEthereumChain\` (EIP-3085) - see [eip-3085-3326.md](/library/tech/eip-3085-3326)
- \`wallet_switchEthereumChain\` (EIP-3326) - see [eip-3085-3326.md](/library/tech/eip-3085-3326)

events emitted:
- \`connect\` - on initial connection
- \`disconnect\` - on revoke / lock
- \`accountsChanged\` - when active dWallet for the origin changes
- \`chainChanged\` - when active EVM chain switches

## the page-script side (where the provider lives)

content script injects a page-script (\`dapp-interface/inject.ts\`) that runs in the page's JS world (not the isolated content-script world). this is the only context where:
- \`window.ethereum\` etc. can be set
- \`dispatchEvent(new CustomEvent('eip6963:announceProvider', ...))\` works
- the dapp's own JS can read / call our provider

the page-script communicates with the chromatika background via \`window.postMessage\` to the content-script, which forwards to background via \`chrome.runtime.sendMessage\`. round-trip: \`dapp.window.ethereum.request(...)\` → \`postMessage(content_script)\` → \`sendMessage(background)\` → chromatika handles → reverse path back.

## EIP-6963 multi-provider discovery

before EIP-6963, wallets fought over \`window.ethereum\` - last-injected wins, others get clobbered. now wallets announce themselves via custom events:

\`\`\`ts
// chromatika announces itself
const eip6963ProviderInfo = {
  uuid: '<random uuid>',
  name: 'Chromatika',
  icon: 'data:image/svg+xml;base64,...',
  rdns: 'xyz.chromatika',
};

window.dispatchEvent(new CustomEvent('eip6963:announceProvider', {
  detail: Object.freeze({
    info: eip6963ProviderInfo,
    provider: chromatikaProvider,   // the EIP-1193 instance
  }),
}));

// also re-announce on dapp request
window.addEventListener('eip6963:requestProvider', () => {
  window.dispatchEvent(new CustomEvent('eip6963:announceProvider', { detail: ... }));
});
\`\`\`

dapps that support EIP-6963 listen for \`eip6963:announceProvider\`, build a list of available providers, let the user pick. dapps that don't support EIP-6963 fall back to \`window.ethereum\` (chromatika still sets it for back-compat, but yields if another wallet was already there - polite coexistence).

## the per-origin permission model

connections are **per origin** + **per curve**:
- chromatika stores a permission record per origin in \`chromatika_dapp_permissions_v1\`
- each record has: \`origin\`, \`selectedSecpDwalletId\`, \`selectedEd25519DwalletId\`, \`scopes\`, \`lastUsedAtMs\`
- read methods (\`eth_call\`, \`eth_getBalance\`) are unauth (chain data, not user-private)
- write methods (\`eth_requestAccounts\`, \`eth_sendTransaction\`, \`personal_sign\`) require an active permission. without one, chromatika prompts via \`eth_requestAccounts\` flow

revocation: \`revokeDappPermission({ origin })\` drops the record. all subsequent write methods from that origin go back to needing approval.

## the bridge boundary

\`\`\`
[dapp page world]
  window.ethereum.request({ method: 'eth_sendTransaction', params: [...] })

[page-script EIP-1193 wrapper]
  postMessage(window, { type: 'chromatika_dapp_request', method, params, requestId })

[content-script (isolated world)]
  on 'chromatika_dapp_request' message + verify event.source === window:
    chrome.runtime.sendMessage({ type: 'dapp_request', origin, method, params, requestId })

[background service worker]
  validate origin permission
  dispatch to handler (e.g. enqueueTxApproval for sendTransaction)
  → opens popup, awaits user
  → on approve, signs + broadcasts
  → returns result via chrome.runtime.sendMessage callback

[content-script]
  postMessage(window, { type: 'chromatika_dapp_response', requestId, result | error })

[page-script]
  resolve / reject the dapp's promise
\`\`\`

per CLAUDE.md: bridge uses **\`chrome.runtime.sendMessage\` with origin + \`event.source\` validated**. content scripts validate that messages came from the page's own window (not an iframe spoofing a sibling page) before forwarding to background.

## what chromatika doesn't expose

- \`eth_sign\` (legacy raw signing) - dangerous (no domain separation), most dapps don't use it. chromatika rejects with "method not supported" rather than silently signing arbitrary bytes
- \`eth_decrypt\`, \`eth_getEncryptionPublicKey\` - the legacy MetaMask-only encryption surface. chromatika doesn't implement
- \`wallet_watchAsset\` (EIP-747) - asset management is in the wallet UI; not exposed to dapps for now

## library

- \`dapp-interface/eip1193-bridge.ts\` - the in-page provider implementation
- \`dapp-interface/eip6963-announce.ts\` - the discovery event dispatch
- \`content-script/dapp-bridge-listener.ts\` - the page ↔ background forwarder

## related

- [eip-3085-3326.md](/library/tech/eip-3085-3326) - chain management methods
- [wallet-standard-sui-and-solana.md](/library/tech/wallet-standard-sui-and-solana) - non-EVM equivalent
- [dapp-bridge-message-validation.md](/library/tech/dapp-bridge-message-validation) - the security boundary
- [evm-send-flow.md](/library/tech/evm-send-flow), [evm-personal-sign-and-typeddata.md](/library/tech/evm-personal-sign-and-typeddata) - what the bridge calls into
`,Xk=`# EIP-3085 (\`wallet_addEthereumChain\`) + EIP-3326 (\`wallet_switchEthereumChain\`)

these two EIPs let dapps suggest networks: "hey, my dapp is on Polygon - if you don't have Polygon, here are the parameters; if you do, please switch to it." chromatika implements both with user approval.

## \`wallet_switchEthereumChain\` (EIP-3326)

dapp asks chromatika to switch to a specific chain id:

\`\`\`ts
await window.ethereum.request({
  method: 'wallet_switchEthereumChain',
  params: [{ chainId: '0x89' }],   // hex chain id, e.g. 137 for Polygon
});
\`\`\`

chromatika handler:
\`\`\`
1. parse chainId from hex (0x89 → 137)
2. look up the chain in registry (built-in + custom networks)
3. if not in registry:
   - throw error code 4902 ("unrecognized chain id; use wallet_addEthereumChain to add it")
4. if in registry but not active:
   - open a small "switch chain?" approval popup with chain name + RPC
   - on user approve: setActiveEvm(chainId), emit 'chainChanged' event
   - on user reject: throw error code 4001 ("user rejected the request")
5. if already active:
   - return null (per EIP-3326 spec, success is null result)
\`\`\`

## \`wallet_addEthereumChain\` (EIP-3085)

dapp asks chromatika to add a custom network:

\`\`\`ts
await window.ethereum.request({
  method: 'wallet_addEthereumChain',
  params: [{
    chainId: '0xa4b1',
    chainName: 'Arbitrum One',
    nativeCurrency: { name: 'Ether', symbol: 'ETH', decimals: 18 },
    rpcUrls: ['https://arb1.arbitrum.io/rpc'],
    blockExplorerUrls: ['https://arbiscan.io'],
    iconUrls: ['data:image/svg+xml;base64,...'],
  }],
});
\`\`\`

chromatika handler:
\`\`\`
1. parse + validate parameters
   - chainId is hex
   - rpcUrls[0] is a valid HTTPS URL
   - nativeCurrency has decimals=18 (uncommon to differ; chromatika rejects non-18 today)
   - blockExplorerUrls is optional but recommended
2. validate the RPC by sending eth_chainId probe:
   - if response.chainId !== params.chainId, reject (RPC lies about its chain)
3. check if chain already exists:
   - in built-in registry? say "already supported, switching to it" (user still confirms)
   - in custom registry? same
   - new? proceed to step 4
4. open approval popup with the proposed chain details
5. on user approve:
   - addCustomNetwork(params)
   - setActiveEvm(chainId)
   - emit 'chainChanged' event
6. on user reject: throw 4001
\`\`\`

## the EIP-3085 abuse vector

a dapp could submit a custom RPC that lies about its chain id, then once active intercept all the user's tx + read calls. chromatika defends with:
- **chain id verification**: probe the RPC's \`eth_chainId\` first; reject mismatch
- **user approval**: every add-chain requires explicit consent, not silent acceptance
- **explorer URL display**: the popup shows the explorer URL so the user can sanity-check
- **default RPC for known chains**: if the dapp suggests adding a chain id that matches a built-in (e.g. mainnet ethereum), chromatika prefers the built-in RPC over the dapp-suggested one

## the chain registry

chromatika ships built-in entries for major chains (Ethereum, Polygon, Arbitrum, Optimism, Base, BNB, etc.) plus several testnets. dapps can add custom chains via this EIP-3085 flow, or users can add via the chromatika UI (or via Chainlist search; see [manage-networks.md](/library/user/manage-networks)).

custom chains live in \`chromatika_custom_networks_v1\` storage. removing a chain that's currently active falls back to a default.

## the active-tier vs vault-tier distinction

EIP-3085 / EIP-3326 only deal with the **EVM** active chain, which is a single chain id. Sui and Solana have a more complex "vault-tier" + "dWallet-tier" model (e.g. dWallet on Sui mainnet but vault running ops on Sui testnet briefly). EVM doesn't have that today - one active chain at a time.

if a dapp wanted analogous Sui / Solana network management, that's not EIP-3085's scope. Wallet Standard (Sui + Solana) has its own \`wallet:setNetwork\` style features but chromatika exposes those through different surfaces.

## edge cases

- **chain id changes mid-session**: technically possible if a dapp switches via EIP-3326 then submits a tx that targets a different chain. chromatika checks tx \`chainId\` matches active EVM chain at sign time; mismatch rejects with a clear error
- **chain id collision**: two custom chains with the same chain id is impossible (chain id is the registry key). adding a new custom that shadows a built-in chain id is also blocked - built-ins win
- **RPC unreachable after add**: the eth_chainId probe verifies the RPC works **at add time**. if the RPC dies later, that's a \`getEvmRpcHealth\` issue (separately surfaced)

## library

- internal: \`wallet-extension/src/background/chains/evm-network.ts\` for \`addCustomNetwork\`, \`setActiveEvm\`, etc.
- internal: \`dapp-interface/eip1193-bridge.ts\` routing for the add/switch methods

## related

- [eip-1193-and-6963.md](/library/tech/eip-1193-and-6963) - the broader EVM bridge
- [manage-networks.md](/library/user/manage-networks) (in user-guides) - the wallet-UI side of network management
- [dapp-bridge-message-validation.md](/library/tech/dapp-bridge-message-validation) - the bridge security model
`,Zk=`# Encrypt 17-byte canonical input format

every encrypted input chromatika sends to the Encrypt executor uses this exact 17-byte byte layout:

\`\`\`
[fhe_type(1 byte) || value_le(16 bytes)]
\`\`\`

without the leading \`fhe_type\` byte, the executor falls into a fallback path that misreads multi-byte scalars (e.g. \`EUint64\` returns \`value >> 8\`). the format fix landed upstream at \`encrypt-pre-alpha\` \`303439d\` (2026-04-26); the published \`@encrypt.xyz/pre-alpha-solana-client@0.1.0\`'s \`encryptValue\` is still pre-fix, so chromatika **hand-rolls** this format via two helpers.

## the bytes

| offset | length | what |
|--------|--------|------|
| 0 | 1 | \`fhe_type\` enum value |
| 1-16 | 16 | \`value_le\` little-endian bytes (zero-padded for values < 2^128) |

total: 17 bytes per encrypted input.

## fhe_type values

\`\`\`js
FHE_TYPE_EUINT64  = 4
FHE_TYPE_EUINT128 = 5
\`\`\`

other values exist in Encrypt's protocol (EUint8, EUint16, EUint32, etc.) but chromatika only writes EUint64 (lab demos) and EUint128 (labels). reads return whatever fheType the on-chain ciphertext was created with.

## the two helpers

\`\`\`ts
// 1. take a number / bigint, encode LE across 16 bytes, prepend fheType
function mockEncryptScalarBytes(value: number | bigint, fheType: number): Uint8Array {
  const buf = new Uint8Array(17);
  buf[0] = fheType;
  let v = BigInt(value);
  for (let i = 0; i < 16; i++) {
    buf[1 + i] = Number(v & 0xFFn);
    v >>= 8n;
  }
  return buf;
}

// 2. take pre-formatted 16-byte value (left-justified, zero-padded UTF-8 chunk),
//    prepend fheType. used by label encoder to avoid bigint round-trip
function mockEncryptScalarBytesFromBytes(valueLe16: Uint8Array, fheType: number): Uint8Array {
  if (valueLe16.length !== 16) throw new Error('expected 16-byte value');
  const buf = new Uint8Array(17);
  buf[0] = fheType;
  buf.set(valueLe16, 1);
  return buf;
}
\`\`\`

both live in \`wallet-extension/src/background/encrypt/encrypt-lab-service.ts\`.

## why "mock"

these helpers don't actually encrypt anything - they format raw plaintext bytes into the shape Encrypt's gRPC \`CreateInput\` expects. the **executor** does the encryption (or in pre-alpha, may store ciphertexts as plaintext per the disclaimer). naming them \`mockEncrypt*\` reflects that they don't perform crypto themselves; they're just packaging.

a future hardening step is to do client-side encryption to the network's encryption public key before submission, so the executor never sees plaintext. that's tracked but not implemented today.

## little-endian rationale

Encrypt's executor reads scalars in little-endian. a 16-byte LE encoding of \`value\` puts the least-significant byte first:

\`\`\`
value = 1
bytes = [0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]

value = 256
bytes = [0x00, 0x01, 0x00, 0x00, ...]

value = 0xFFFFFFFFFFFFFFFFFFFF (10 bytes of 0xFF)
bytes = [0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]
\`\`\`

for label chunks (UTF-8 bytes packed into 16-byte chunks), the chunk bytes are placed at offsets 1-16 directly with zero-padding at the high end. so a 5-byte label chunk like \`"hello"\` becomes:

\`\`\`
chunk_utf8 = [0x68, 0x65, 0x6C, 0x6C, 0x6F, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]
buf = [0x05, 0x68, 0x65, 0x6C, 0x6C, 0x6F, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]
        ^fhe_type=5      ^chunk bytes (LE) zero-padded
\`\`\`

note: although we call it "value_le", for label chunks the bytes are placed in their natural order (leftmost UTF-8 byte at offset 1) - that's still "LE" in the sense that lower-significance bits come first if you treat the chunk as a 128-bit integer. EUint128 is just a 128-bit unsigned int from the executor's perspective; we use it as a fixed-size byte container.

## what gets sent to CreateInput

\`CreateInputRequest.inputs[]\` is a list of \`EncryptedInput\`s. each \`EncryptedInput\` has:
\`\`\`jsonc
{
  "ciphertextBytes": "<17-byte buffer>",
  "fheType": 5
}
\`\`\`

note \`fheType\` is also redundantly included as a separate field. the executor uses both; mismatch is undefined behavior. we always set them consistently.

## reads return 16 bytes plus fheType

\`ReadCiphertextResponse\` returns:
\`\`\`jsonc
{
  "value": "<16-byte plaintext value, no leading type byte>",
  "fheType": 5,
  "digest": "<opaque executor-local proof state>"
}
\`\`\`

read response **strips** the type byte - the response carries \`fheType\` as a separate field. so if you write \`[5, 0x68, 0x65, 0x6C, 0x6C, 0x6F, ...]\` (17 bytes), you read back \`value = [0x68, 0x65, 0x6C, 0x6C, 0x6F, ...]\` (16 bytes) plus \`fheType = 5\`.

decoders need to know \`fheType\` to interpret the 16 bytes correctly:
- EUint64 → take low 8 bytes, interpret as u64 LE
- EUint128 → take all 16 bytes, interpret as u128 LE
- (when used for labels) ignore the int interpretation, use the bytes directly as UTF-8 chunk

## why not just use a real FHE client-side encrypt

production Encrypt **will** do client-side encryption to the network's encryption public key, producing actual ciphertexts that the executor can compute on without seeing plaintext. pre-alpha skips this for development speed. when the real client encrypt ships, chromatika swaps \`mockEncryptScalarBytesFromBytes\` for the real \`encrypt(networkPubKey, plaintext)\` and the executor receives cipher bytes instead of plaintext.

## library

- internal: \`mockEncryptScalarBytes\`, \`mockEncryptScalarBytesFromBytes\` in \`encrypt-lab-service.ts\`
- internal: \`encrypt-protobuf-wire.ts\` for serializing \`EncryptedInput\` to protobuf
`,eb=`# Encrypt CreateInput flow

chromatika's path for writing a value (or batch of values) to the Encrypt executor as on-chain ciphertext. labels and lab demos both go through this. produces a list of **ciphertext identifiers** that act as on-chain handles for later reveal / compute.

## the call signature

\`\`\`ts
async function createDwalletLabelCiphertext({
  curve,
  label,                              // string, 1-64 utf-8 bytes
  networkEncryptionPublicKeyHex?,     // optional override
}): Promise<{
  ciphertextIdentifierHexes: string[],
  fheType: 5,
  utf8Len: number,
  programId: string,
}> { ... }
\`\`\`

similar API for the lab demos:
\`\`\`ts
async function encryptLabCreateInputDemo({ plainU64, networkEncryptionPublicKeyHex? })
async function encryptLabCreateInputDemoBatch({ plainU64s, networkEncryptionPublicKeyHex? })   // up to 16
\`\`\`

## step-by-step (label flow)

\`\`\`
1. validate input
   trimmed = label.trim()
   utf8Bytes = UTF8Encoder.encode(trimmed)
   if utf8Bytes.length > 64: throw 'label too long'
   if utf8Bytes.length === 0: throw 'label empty'

2. resolve network encryption public key
   pubkey = await resolveNetworkEncryptionPublicKey(connection, programId, overrideHex)
   // tries override hex if provided, else queries on-chain PDA via:
   //   ['network_encryption_key', 'network-encryption-key', 'NetworkEncryptionKey']
   // reads bytes at offset 8 (40-byte account) or 0 (32-byte account)

3. slice into chunks of 16 bytes (left-justified, zero-padded)
   chunks = []
   for i in 0..ceil(utf8Bytes.length / 16):
     chunk = new Uint8Array(16)
     chunk.set(utf8Bytes.slice(i * 16, (i + 1) * 16))   // copies + auto-pads with zeros
     chunks.push(chunk)
   // 1-4 chunks for a 1-64 byte label

4. wrap each chunk in 17-byte canonical format
   inputs = chunks.map(chunk => ({
     ciphertextBytes: mockEncryptScalarBytesFromBytes(chunk, FHE_TYPE_EUINT128),  // 17 bytes
     fheType: FHE_TYPE_EUINT128,
   }))

5. encode protobuf request
   request = {
     chain: 0,                                        // Solana
     inputs,                                          // 1-4 EncryptedInput
     proof: new Uint8Array(),                         // empty in pre-alpha
     authorized: programId.toBytes(),                 // 32-byte program id
     networkEncryptionPublicKey: pubkey,              // 32 bytes
   }
   encoded = encodeCreateInputRequest(request)

6. send via gRPC-web
   responseBytes = await encryptGrpcCreateInput(GRPC_URL, encoded)

7. decode response
   { ciphertextIdentifiers } = decodeCreateInputResponse(responseBytes)
   // expect ciphertextIdentifiers.length === inputs.length
   if length mismatch: throw 'CreateInput returned N identifiers; expected M'

8. persist locally in dWallet meta
   record.dwalletMeta[curve_dwallet_idx].encryptedLabel = {
     ciphertextIdentifierHexes: ciphertextIdentifiers.map(toHex),
     fheType: 5,
     createdAtMs: Date.now(),
     programId: programId.toBase58(),
     utf8Len: utf8Bytes.length,
   }
   await saveDwalletMeta(vaultId, record.dwalletMeta)

9. return identifiers + metadata to the caller
\`\`\`

## the network encryption public key lookup

\`\`\`ts
async function resolveNetworkEncryptionPublicKey(
  connection: Connection,
  programId: PublicKey,
  overrideHex?: string,
): Promise<Uint8Array> {
  // 1. user-supplied override
  if (overrideHex && /^[0-9a-fA-F]{64}$/.test(overrideHex)) {
    return hexToBytes(overrideHex);
  }

  // 2. try seeds in order
  const seeds = ['network_encryption_key', 'network-encryption-key', 'NetworkEncryptionKey'];
  for (const seed of seeds) {
    const [pda] = PublicKey.findProgramAddressSync([Buffer.from(seed)], programId);
    const account = await connection.getAccountInfo(pda);
    if (!account) continue;
    if (account.data.length === 40) return account.data.slice(8, 40);   // 8-byte discriminator + 32 bytes
    if (account.data.length === 32) return account.data.slice(0, 32);
  }

  // 3. all seeds failed
  throw new Error(
    \`network encryption key PDA not found at any tried seed for program \${programId}; \` +
    \`paste 32-byte hex via networkEncryptionPublicKeyHex or confirm RPC + program id match Encrypt pre-alpha\`
  );
}
\`\`\`

three seed candidates because the on-chain layout has shifted between pre-alpha versions; we try each before bailing. providing a hex override skips the lookup entirely.

## what the response identifiers are

\`ciphertextIdentifierHexes\` are **executor-local handles**, not opaque secrets. each one corresponds to an on-chain account at a deterministic PDA derived from the executor's storage layout. the identifiers can be written to chain-public storage without leaking the underlying ciphertext (since the ciphertext itself is what protects the value, not the identifier).

chromatika persists identifiers in dWallet meta (see [vault-blob-v3-format.md](/library/tech/vault-blob-v3-format) for the overlay structure). on reveal, the identifiers are fed back into \`ReadCiphertext\` requests.

## error handling

- network failure (gRPC unreachable): wrapped, surfaced as "encrypt service unavailable"
- network encryption key not found: throws with actionable message (paste hex or confirm RPC / program id)
- length mismatch (server returned fewer identifiers than inputs sent): throws as protocol violation
- session not on Solana ika base: throws via \`assertEncryptSolanaIkaBase()\` before any network call
- rate limit (HTTP 429 from server): bubbled up; user retries

## the lab demos use the same machinery

\`\`\`ts
async function encryptLabCreateInputDemo({ plainU64, ... }) {
  const inputs = [{
    ciphertextBytes: mockEncryptScalarBytes(plainU64, FHE_TYPE_EUINT64),   // 17 bytes
    fheType: FHE_TYPE_EUINT64,
  }];
  // same encoding, same gRPC call, same decoding
  // returns single ciphertextIdentifierHex
}

async function encryptLabCreateInputDemoBatch({ plainU64s, ... }) {
  if (plainU64s.length > 16) throw 'batch capped at 16';
  const inputs = plainU64s.map(v => ({
    ciphertextBytes: mockEncryptScalarBytes(v, FHE_TYPE_EUINT64),
    fheType: FHE_TYPE_EUINT64,
  }));
  // same path; returns ciphertextIdentifierHexes (one per value, in order)
}
\`\`\`

label flow uses EUint128 chunks; lab demos use EUint64 scalars. the 17-byte format is the same shape.

## library

- internal: \`wallet-extension/src/background/encrypt/encrypt-lab-service.ts\` \`createDwalletLabelCiphertext\`, \`encryptLabCreateInputDemo\`, \`encryptLabCreateInputDemoBatch\`, \`resolveNetworkEncryptionPublicKey\`
- internal: \`encrypt-protobuf-wire.ts\` \`encodeCreateInputRequest\`, \`decodeCreateInputResponse\`
- internal: \`encrypt-grpc-web-fetch.ts\` \`encryptGrpcCreateInput\`
- internal: \`encrypt-lab-service.ts\` \`mockEncryptScalarBytesFromBytes\`, \`mockEncryptScalarBytes\`
- \`@solana/web3.js\` \`Connection\`, \`PublicKey.findProgramAddressSync\`

## related

- [encrypt-17-byte-canonical-format.md](/library/tech/encrypt-17-byte-canonical-format) - the byte layout we mock-encrypt to
- [encrypt-protobuf-wire.md](/library/tech/encrypt-protobuf-wire) - the request encoding
- [encrypt-grpc-web-fetch-transport.md](/library/tech/encrypt-grpc-web-fetch-transport) - the network transport
- [encrypt-multi-chunk-labels.md](/library/tech/encrypt-multi-chunk-labels) - the chunking strategy for >16-byte labels
- [encrypt-on-chain-status-polling.md](/library/tech/encrypt-on-chain-status-polling) - what happens after persistence (the 4s poll)
`,nb=`# Encrypt gRPC-web transport (HTTP/1.1 over fetch)

chromatika talks to Encrypt's gRPC service over the gRPC-web protocol. browsers can't speak native gRPC (HTTP/2 streaming with custom frame types), so gRPC-web wraps gRPC's wire format inside HTTP/1.1 POST requests with specific framing. chromatika's transport is \`encrypt-grpc-web-fetch.ts\`, a thin wrapper around \`fetch()\` plus \`@protobuf-ts/grpcweb-transport\` for the framing details.

## why gRPC-web

native gRPC requires:
- HTTP/2 (most fetch implementations don't expose enough control over HTTP/2 frames)
- bidirectional streaming (browsers don't have a clean way to do this)
- trailers (uncommon in browser fetch)

gRPC-web is a spec that maps gRPC onto vanilla HTTP/1.1 POST + base64-or-binary message frames + per-response trailer-equivalent metadata in a synthetic frame. it's the standard way to call gRPC services from a browser-style runtime.

## the wire shape

every gRPC-web request is a POST to \`<base_url>/<service>/<method>\`:

\`\`\`
POST /encrypt.v1.EncryptService/CreateInput HTTP/1.1
Host: pre-alpha-dev-1.encrypt.ika-network.net
Content-Type: application/grpc-web+proto
X-Grpc-Web: 1
X-User-Agent: grpc-web-javascript/0.1
Accept: application/grpc-web+proto

[5-byte length-prefix frame] [protobuf body]
\`\`\`

the body framing:
- byte 0: compression flag (0 = uncompressed)
- bytes 1-4: big-endian uint32 length of the next message
- bytes 5+: the protobuf-encoded message

response framing is similar, plus a synthetic **trailer frame** (compression flag = 0x80, then a length, then \`key1: value1\\r\\nkey2: value2\\r\\n\` style headers) at the end:

\`\`\`
[length-prefixed protobuf response frame]
[length-prefixed trailer frame]
  flag = 0x80
  body = "grpc-status: 0\\r\\ngrpc-message: \\r\\n"
\`\`\`

\`grpc-status: 0\` means success. nonzero is an error code per gRPC's status enum.

## chromatika's wrapper

\`\`\`ts
async function encryptGrpcUnary(
  baseUrl: string,
  service: string,
  method: string,
  encodedRequestBytes: Uint8Array,
  timeoutMs = 25_000
): Promise<Uint8Array> {
  const url = \`\${baseUrl}/\${service}/\${method}\`;

  const requestBody = createGrpcWebRequestBody(encodedRequestBytes);   // adds 5-byte prefix
  const requestHeaders = createGrpcWebRequestHeader();                 // sets Content-Type etc.

  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), timeoutMs);

  const resp = await fetch(url, {
    method: 'POST',
    headers: requestHeaders,
    body: requestBody,
    signal: controller.signal,
  });
  clearTimeout(timeoutId);

  if (!resp.ok) {
    throw new Error(\`grpc-web HTTP \${resp.status}: \${await resp.text()}\`);
  }

  const responseBuffer = new Uint8Array(await resp.arrayBuffer());
  const { dataFrames, trailers } = readGrpcWebResponseBody(responseBuffer);

  // check trailers for grpc-status: 0
  if (trailers['grpc-status'] && trailers['grpc-status'] !== '0') {
    throw new Error(
      \`grpc error \${trailers['grpc-status']}: \${trailers['grpc-message'] ?? 'unknown'}\`
    );
  }

  if (dataFrames.length === 0) throw new Error('no data frames in response');
  return dataFrames[0];   // first DATA frame = the protobuf response
}
\`\`\`

\`createGrpcWebRequestBody\`, \`createGrpcWebRequestHeader\`, \`readGrpcWebResponseBody\` come from \`@protobuf-ts/grpcweb-transport\`. they handle the 5-byte framing + trailer parsing.

## the two methods chromatika calls

\`\`\`ts
// CreateInput - upload encrypted inputs
encryptGrpcCreateInput(baseUrl, encodedRequest)
  → POST {baseUrl}/encrypt.v1.EncryptService/CreateInput
  → returns Uint8Array (protobuf-encoded CreateInputResponse)

// ReadCiphertext - read a stored ciphertext (signed)
encryptGrpcReadCiphertext(baseUrl, encodedRequest)
  → POST {baseUrl}/encrypt.v1.EncryptService/ReadCiphertext
  → returns Uint8Array (protobuf-encoded ReadCiphertextResponse)
\`\`\`

the \`encodedRequest\` is already protobuf-encoded - the body of one inside the gRPC-web frame.

## the 25-second timeout

\`\`\`js
const timeoutMs = 25_000;
\`\`\`

reasonable for a single round-trip to a pre-alpha devnet service. tweakable per call, but the default catches stuck connections without making the user wait too long.

## error handling

three error tiers:
1. **HTTP error** (4xx, 5xx response status) - network or server failure. \`throw new Error('grpc-web HTTP ' + status)\` with the body included
2. **gRPC error** (HTTP 200 with \`grpc-status\` trailer != 0) - server understood, but request was invalid. \`throw new Error('grpc error ' + status + ': ' + message)\`
3. **client abort** (timeout fired) - \`AbortError\` propagates

callers wrap these into chromatika-shaped errors before surfacing to the user.

## why not @grpc/grpc-js

\`@grpc/grpc-js\` is the standard node.js gRPC client. it's **not browser-compatible**:
- depends on node-only APIs (\`net\`, \`tls\`, \`dns\`)
- emits HTTP/2 frames directly
- ~5 MB minified

chromatika runs in a chrome service worker, where neither node APIs nor HTTP/2 control are available. gRPC-web over fetch is the only option.

## CORS

the Encrypt server at \`pre-alpha-dev-1.encrypt.ika-network.net\` sets permissive CORS headers so browsers can call it. specifically:
\`\`\`
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: POST, OPTIONS
Access-Control-Allow-Headers: Content-Type, X-Grpc-Web, X-User-Agent
\`\`\`

if CORS becomes more restrictive in the future (e.g. per-origin allowlist), chromatika may need to ship a fetch-proxy in the service worker. not a concern today.

## library

- \`fetch\` (browser native, works in service worker)
- \`@bufbuild/protobuf\` for the protobuf encoder / decoder (used by callers, not this transport)
- \`@protobuf-ts/grpcweb-transport\` for \`createGrpcWebRequestBody\`, \`createGrpcWebRequestHeader\`, \`readGrpcWebResponseBody\`
- internal: \`wallet-extension/src/background/encrypt/encrypt-grpc-web-fetch.ts\`

## related

- [encrypt-protobuf-wire.md](/library/tech/encrypt-protobuf-wire) - what the request/response bytes look like before/after framing
- [encrypt-create-input.md](/library/tech/encrypt-create-input) - the encrypt path that uses this transport
- [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed) - the reveal path
`,tb=`# Encrypt multi-chunk labels (1-64 utf-8 bytes → 1-4 EUint128 chunks)

a dWallet label is up to **64 utf-8 bytes**. since each Encrypt input is one fixed-size scalar (we use EUint128 = 16 bytes), a label larger than 16 bytes must be **sliced into chunks** and stored as a sequence of identifiers. chromatika packs labels into 1-4 chunks of 16 bytes each, encrypts them in a single batched \`CreateInput\` call, and reassembles on reveal.

## the encoding

\`\`\`ts
function encodeLabelToChunks(label: string): { chunks: Uint8Array[], utf8Len: number } {
  const trimmed = label.trim();
  const utf8Bytes = new TextEncoder().encode(trimmed);

  if (utf8Bytes.length === 0) throw new Error('label cannot be empty');
  if (utf8Bytes.length > 64) throw new Error('label too long (max 64 utf-8 bytes)');

  const chunkCount = Math.ceil(utf8Bytes.length / 16);
  const chunks: Uint8Array[] = [];
  for (let i = 0; i < chunkCount; i++) {
    const chunk = new Uint8Array(16);                                  // zero-initialized
    chunk.set(utf8Bytes.slice(i * 16, (i + 1) * 16));                 // copy the slice; remaining stays zero
    chunks.push(chunk);
  }

  return { chunks, utf8Len: utf8Bytes.length };
}
\`\`\`

example for label \`"hello world"\` (11 utf-8 bytes, 1 chunk needed):
\`\`\`
chunk_0 = [0x68, 0x65, 0x6C, 0x6C, 0x6F, 0x20, 0x77, 0x6F, 0x72, 0x6C, 0x64, 0x00, 0x00, 0x00, 0x00, 0x00]
              h     e     l     l     o    ' '    w     o     r     l     d    [zero padding]
utf8Len = 11
\`\`\`

example for label \`"my account label here for chromatika!!"\` (38 bytes, 3 chunks):
\`\`\`
chunk_0 = [m, y, ' ', a, c, c, o, u, n, t, ' ', l, a, b, e, l]                                            // 16 bytes, exactly filled
chunk_1 = [' ', h, e, r, e, ' ', f, o, r, ' ', c, h, r, o, m, a]                                          // 16 bytes
chunk_2 = [t, i, k, a, !, !, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]                // 6 bytes, padded
utf8Len = 38
\`\`\`

## the wrap to 17-byte format

each 16-byte chunk gets wrapped:
\`\`\`ts
const inputs = chunks.map(chunk => ({
  ciphertextBytes: mockEncryptScalarBytesFromBytes(chunk, FHE_TYPE_EUINT128),
  fheType: FHE_TYPE_EUINT128,
}));
\`\`\`

\`mockEncryptScalarBytesFromBytes\` prepends \`0x05\` (FHE_TYPE_EUINT128 = 5) → 17-byte canonical format. see [encrypt-17-byte-canonical-format.md](/library/tech/encrypt-17-byte-canonical-format).

## the batched CreateInput

all chunks go in **one** gRPC \`CreateInput\` request:
\`\`\`ts
const request = {
  chain: 0,
  inputs,                                // 1-4 EncryptedInput
  proof: new Uint8Array(),
  authorized: programId.toBytes(),
  networkEncryptionPublicKey: pubkey,
};
const responseBytes = await encryptGrpcCreateInput(GRPC_URL, encodeCreateInputRequest(request));
const { ciphertextIdentifiers } = decodeCreateInputResponse(responseBytes);

// expect: ciphertextIdentifiers.length === inputs.length
if (ciphertextIdentifiers.length !== inputs.length) {
  throw new Error(\`CreateInput returned \${ciphertextIdentifiers.length} identifiers; expected \${inputs.length}\`);
}
\`\`\`

one round-trip for the whole label, regardless of chunk count. each chunk gets its own on-chain account (and identifier).

## persistence

\`\`\`jsonc
record.dwalletMeta[curveDwalletIdx].encryptedLabel = {
  "ciphertextIdentifierHexes": ["a1b2c3...", "d4e5f6...", "789...", "..."],
  "fheType": 5,
  "createdAtMs": 1700000000000,
  "programId": "...",
  "utf8Len": 38                          // CRITICAL - tells reveal how many bytes to trim to
}
\`\`\`

\`utf8Len\` is the original byte length **before chunking**. without it, reveal would have to guess where the trailing zero-padding starts (and a label with intentional trailing zero bytes would be ambiguous). storing the length explicitly avoids this entire class of bug.

## reveal: the 4-roundtrip loop

\`\`\`ts
const chunkValues: Uint8Array[] = [];

for (const idHex of ciphertextIdentifierHexes) {
  const id = hexToBytes(idHex);
  const msg = encodeReadCiphertextMessage(0, id, new Uint8Array(0), 0n);
  const { signature } = await signMessageSol(msg);
  const sigBytes = signatureHexToEd25519Bytes(signature);
  const respBytes = await encryptGrpcReadCiphertext(GRPC_URL, encodeReadCiphertextRequest({
    message: msg, signature: sigBytes, signer: signerPk
  }));
  const { value } = decodeReadCiphertextResponse(respBytes);
  chunkValues.push(value);   // 16-byte chunk plaintext
}
\`\`\`

each chunk requires a separate sign + read because:
- \`ReadCiphertextRequest\` carries one identifier; you can't batch reads in pre-alpha
- the executor can't reveal chunks without authentication, and the signature is tied to the message which contains the identifier - one signature per identifier

for a 4-chunk label, that's:
- 4 ika MPC ED25519 signs (4 presign consumed)
- 4 gRPC round-trips
- typically <1 second total on a fast network

## reassembly

\`\`\`ts
const concatenated = new Uint8Array(chunkValues.length * 16);
for (let i = 0; i < chunkValues.length; i++) {
  concatenated.set(chunkValues[i], i * 16);
}
const trimmed = concatenated.slice(0, utf8Len);
const labelString = new TextDecoder('utf-8', { fatal: false }).decode(trimmed);
\`\`\`

\`fatal: false\` produces \`?�?�\` replacement chars for invalid UTF-8 rather than throwing. defensive against tampering, devnet weirdness, etc.

## partial failure semantics

if any chunk's read fails:
- the whole reveal throws
- chromatika does **not** show a partial label ("hello?�?�?...")
- user sees a "couldn't read chunk N" error and can retry

retrying re-signs each chunk fresh. presign pool keeps refilling; eventually you get all chunks.

## why 16 bytes not 32

EUint128 is the largest size we use. EUint256 doesn't exist in Encrypt's pre-alpha enum. so the natural chunk size is 16 bytes (128 bits = 16 bytes).

a future surface could extend to EUint256 (or arbitrary-length blobs) when Encrypt's protocol supports it. for now, 1-4 chunks of 16 bytes is the design.

## why 64 bytes not arbitrary

practical reasons:
- 64 utf-8 bytes is enough for a meaningful label (English, with allowance for emoji + non-Latin scripts)
- 4 chunks = 4 reveal round-trips, ~1 second total. more chunks = slower reveal
- protocol cap (CreateInput batch is technically up to 16 inputs in a single call, but reveal becomes O(n) sequential reads)

a label limit of 64 bytes covers virtually every UX use case. longer labels are typically arbitrary content (notes, descriptions) that don't belong in an encrypted on-chain wallet field anyway.

## library

- \`TextEncoder\` / \`TextDecoder\` (browser native)
- internal: \`encodeLabelToChunks\`, \`decodeLabelFromChunks\` in \`encrypt-lab-service.ts\`
- internal: \`mockEncryptScalarBytesFromBytes\` for the 17-byte wrap

## related

- [encrypt-create-input.md](/library/tech/encrypt-create-input) - the write path
- [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed) - the reveal path (per-chunk sign + read)
- [encrypt-17-byte-canonical-format.md](/library/tech/encrypt-17-byte-canonical-format) - how each chunk wraps
`,rb=`# Encrypt on-chain status polling (4s pill)

after writing an encrypted label, chromatika polls Solana every 4 seconds to read the **status byte** of each chunk's on-chain ciphertext account. the UI surfaces a status pill: \`verified\` ✓ / \`encrypting\` / \`missing\`. this lets the user know whether the label landed on chain (devnet wipe → \`missing\`; executor still committing → \`encrypting\`; happy path → \`verified\`).

## the on-chain layout

per Encrypt's account layout:

\`\`\`
Encrypt ciphertext account = 100 bytes total
[... 98 bytes of executor-internal data ...] [fheType (1 byte)] [statusByte (1 byte)]
                                              offset 98          offset 99
\`\`\`

the last byte is the **status byte**; the second-to-last is the \`fheType\` echo. the rest is opaque executor state.

## status byte values

\`\`\`
0 = pending  (executor still committing; ciphertext written but not yet verifiable)
1 = verified (ciphertext is on-chain and reading is allowed)
null (account missing) = devnet wipe (or never written)
\`\`\`

other values are reserved for future states. chromatika treats anything outside \`{0, 1, null}\` as "unknown" (display falls back to \`pending\`).

## the per-chunk poll

\`\`\`ts
async function getChunkStatus(connection, identifierHex, programId): Promise<ChunkStatus> {
  const id = hexToBytes(identifierHex);
  // identifier is the on-chain account public key; just use it directly
  const accountPk = new PublicKey(id);
  const accountInfo = await connection.getAccountInfo(accountPk);

  if (!accountInfo) {
    return { accountExists: false, statusByte: null, fheType: null };
  }
  if (accountInfo.data.length < 100) {
    return { accountExists: true, statusByte: null, fheType: null };   // unexpected layout
  }
  return {
    accountExists: true,
    statusByte: accountInfo.data[99],
    fheType: accountInfo.data[98],
  };
}
\`\`\`

just one Solana \`getAccountInfo\` call per chunk. accounts that don't exist → \`accountExists: false\`. accounts that exist but have unexpected layout → \`statusByte: null\` (defensive).

## the multi-chunk aggregation

\`\`\`ts
async function getDwalletEncryptedLabelOnChainStatus({ curve }): Promise<{
  status: 'verified' | 'pending' | 'missing' | 'no-label',
  chunks: Array<{
    ciphertextIdentifierHex: string,
    accountExists: boolean,
    statusByte: number | null,
    fheType: number | null,
  }>,
}> {
  const meta = readDwalletMeta(activeVaultId, curve);
  if (!meta?.encryptedLabel) return { status: 'no-label', chunks: [] };

  const connection = new Connection(SOLANA_RPC_URL);
  const programId = new PublicKey(meta.encryptedLabel.programId);

  const chunks = await Promise.all(
    meta.encryptedLabel.ciphertextIdentifierHexes.map(idHex =>
      getChunkStatus(connection, idHex, programId)
    )
  );

  // aggregation:
  // - any chunk missing → overall 'missing'
  // - any chunk pending → overall 'pending'
  // - all chunks verified → overall 'verified'
  if (chunks.some(c => !c.accountExists)) return { status: 'missing', chunks };
  if (chunks.some(c => c.statusByte === 0)) return { status: 'pending', chunks };
  if (chunks.every(c => c.statusByte === 1)) return { status: 'verified', chunks };
  return { status: 'pending', chunks };   // catch-all
}
\`\`\`

aggregation rules:
- **any** chunk missing → label is \`missing\` overall (devnet wipe scenario)
- **any** chunk pending → label is \`encrypting\` overall (executor still committing)
- **all** chunks verified → label is \`verified\` ✓

## the polling loop

UI uses \`setInterval\` with 4-second cadence:

\`\`\`tsx
// in DwalletEncryptedLabel.tsx
useEffect(() => {
  if (!status?.hasLabel || !status.enabledForSession) return;

  const id = window.setInterval(() => {
    void trpc.getDwalletLabelOnChainStatus
      .query({ curve })
      .then(setOnChainStatus)
      .catch(() => {
        // silent fail - keep showing previous state
      });
  }, 4000);

  return () => window.clearInterval(id);
}, [curve, status?.hasLabel, status?.enabledForSession]);
\`\`\`

silent error handling: if the RPC call fails (network blip, RPC down), keep showing the last known state rather than flipping to \`missing\` or \`pending\` based on a transient failure.

## why 4 seconds

executor commit latency is typically <1 second on devnet but can spike to several seconds under load. polling every 4 seconds:
- catches the \`pending → verified\` transition within ~4 seconds of completion (acceptable UX)
- doesn't hammer the RPC (one call per chunk per 4 seconds × N chunks × M dWallets)
- gives the executor breathing room before the next poll

faster polling (1 second) would be more responsive but uses more RPC budget. slower (10 seconds) would feel laggy. 4 seconds is the chosen middle.

## the polling lifecycle

\`\`\`
1. user creates a label → encrypt flow runs → ciphertextIdentifierHexes persisted → UI shows 'encrypting'
2. polling alarm fires every 4s
3. each poll:
   - aggregate status across all chunks
   - if it changed since last poll, update UI state
4. user sees pill flip from 'encrypting' to 'verified' typically within 4-12 seconds
5. polling continues as long as the component is mounted and label exists
6. unmount → cleanup → polling stops
\`\`\`

## the devnet wipe scenario

solana ika devnet gets **wiped periodically** (per CLAUDE.md disclaimer: "the Solana program and all on-chain data will be wiped periodically"). after a wipe:
- chromatika's local \`record.dwalletMeta.encryptedLabel.ciphertextIdentifierHexes\` still points at on-chain accounts
- those accounts no longer exist (they were wiped)
- polling returns \`accountExists: false\` for each chunk
- aggregated status becomes \`missing\`
- UI shows the alert pill: "missing on-chain"
- the reveal button auto-disables (you can't read a missing ciphertext)
- user must \`clearDwalletLabel\` (drops the local pointer) and re-encrypt

automated rebuild on first reveal failure (regenerate ciphertext from cached plaintext) is **tracked future**. today the user does it manually, since chromatika doesn't cache the original plaintext label after encryption.

## the no-label case

if the user has never encrypted a label for this dWallet:
- \`record.dwalletMeta.encryptedLabel\` is undefined
- \`status: 'no-label'\`, \`chunks: []\`
- UI shows no pill (just the encrypt button)

## library

- \`@solana/web3.js\` \`Connection\`, \`PublicKey\`, \`getAccountInfo\`
- internal: \`getDwalletEncryptedLabelOnChainStatus\` in \`encrypt-lab-service.ts\`
- internal: \`DwalletEncryptedLabel.tsx\` for the React polling hook

## related

- [encrypt-create-input.md](/library/tech/encrypt-create-input) - the write that produces ciphertextIdentifierHexes
- [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed) - the reveal path
- [encrypt-multi-chunk-labels.md](/library/tech/encrypt-multi-chunk-labels) - the chunking that produces multiple identifiers per label
`,ab='# Encrypt.xyz integration (pre-alpha overview)\n\nchromatika integrates `@encrypt.xyz/pre-alpha-solana-client@0.1.0` to provide encrypted dWallet labels and lab-grade encrypted-input experiments on Solana ika base. **lab-grade pre-alpha**: the Encrypt program disclaimer says ciphertexts can be plaintext on-chain in this phase. **never** store real secrets in encrypted labels.\n\n## what Encrypt is\n\nEncrypt.xyz is a Solana-native FHE (fully homomorphic encryption) executor. clients upload "encrypted inputs" via gRPC `CreateInput`; the network materializes them as on-chain accounts; computations on those ciphertexts can run via Encrypt program instructions; reading a ciphertext requires a signed `ReadCiphertext` request. chromatika uses two tiny corners of this surface:\n1. **encrypted dWallet labels**: encode a UTF-8 label as 1-4 EUint128 chunks, write via CreateInput, surface the on-chain status. reveal via signed ReadCiphertext (signature comes from the dWallet\'s ed25519 path).\n2. **encryption lab demos**: bare CreateInput (single + batched up to 16 EUint64s) and ReadCiphertext for SDK exploration.\n\nfuture surfaces (SPL deposit, PC-Token / PC-Swap phases 3-4) are stubbed (`encrypt-spl-deposit-stub.ts`, `encrypt-pc-phase-stub.ts`) - tRPC returns `not_wired` until program alignment.\n\n## the high-level data flow\n\n```\nplaintext value (e.g. label string)\n  → mock-encrypt to 17-byte canonical format          (encrypt-lab-service.ts)\n  → CreateInput gRPC over gRPC-web fetch              (encrypt-grpc-web-fetch.ts)\n  → on-chain ciphertext account materialized          (Solana program)\n  → identifier (b32 / b58 / hex) returned to client\n  → store identifier in chromatika dwallet meta       (per-vault overlay)\n\nlater, on reveal:\n\nstored identifier\n  → encodeReadCiphertextMessage (BCS layout)          (encrypt-read-msg.ts)\n  → signMessageSol (signs the BCS message via dWallet ed25519 path)\n  → ReadCiphertext gRPC over gRPC-web fetch\n  → 16-byte plaintext value + fheType + digest returned\n  → reassemble multi-chunk labels into UTF-8 string\n```\n\n## the Solana ika base requirement\n\nevery encrypt procedure asserts Solana ika base via `assertEncryptSolanaIkaBase()`:\n- Sui-base vaults throw at the API boundary\n- `VITE_SOLANA_IKA_BASE=true` must be set at build time\n- session must be unlocked + on a Solana-base vault\n\n`isEncryptAllowedForSession()` is the non-throwing variant for surfaces that just want to hide encrypt UI on Sui-base vaults.\n\n## constants\n\n```js\nGRPC_URL = "https://pre-alpha-dev-1.encrypt.ika-network.net:443"\nENCRYPT_SOLANA_PROGRAM_ID = "<base58 program id>"   // see encrypt-constants.ts\nLABEL_MAX_UTF8_BYTES = 64                            // 4 × 16-byte chunks\nFHE_TYPE_EUINT64 = 4\nFHE_TYPE_EUINT128 = 5                                // labels use this\n```\n\n## the published-SDK-bug situation\n\n`@encrypt.xyz/pre-alpha-solana-client@0.1.0` ships an `encryptValue` helper that produces the **pre-fix 16-byte format** (`[value_le(16)]`) - missing the 1-byte `fhe_type` prefix. this misreads multi-byte scalars (e.g. EUint64 returns `value >> 8`).\n\nthe upstream fix landed at commit `303439d` (2026-04-26) but hasn\'t been republished to npm. so chromatika **hand-rolls** the 17-byte format via `mockEncryptScalarBytes` / `mockEncryptScalarBytesFromBytes` in `encrypt-lab-service.ts`. anything that calls Encrypt\'s `CreateInput` with `ciphertextBytes` must use these helpers, not the package\'s `encryptValue`, until upstream republishes.\n\n## what\'s stored in chromatika\n\nper-curve dWallet meta overlay (`chromatika_dwallet_meta_v2_<vaultId>`) gets an `encryptedLabel` entry:\n\n```jsonc\n{\n  "encryptedLabel": {\n    "ciphertextIdentifierHexes": ["abc...", "def..."],   // 1-4 hex identifiers\n    "fheType": 5,                                         // EUINT128\n    "createdAtMs": 1700000000000,\n    "programId": "<base58 program id>",\n    "utf8Len": 8                                          // e.g. for "myLabel\\0" trim\n  }\n}\n```\n\nwe store the **identifiers** plus the **utf8 length** locally. the actual ciphertext lives on-chain in Solana program accounts. on reveal, we look up by identifier.\n\n## file map\n\n| file | purpose |\n|------|---------|\n| `encrypt-lab-service.ts` | core encrypt / reveal / status logic, label chunking, mockEncryptScalarBytes helpers, network key resolution, on-chain status polling |\n| `encrypt-constants.ts` | `GRPC_URL`, `RPC_URL`, program id, docs URLs |\n| `encrypt-read-msg.ts` | `encodeReadCiphertextMessage` BCS layout |\n| `encrypt-protobuf-wire.ts` | CreateInput / ReadCiphertext protobuf wire codec (no `.proto` files; hand-rolled with `@bufbuild/protobuf` `BinaryWriter` / `BinaryReader`) |\n| `encrypt-grpc-web-fetch.ts` | gRPC-web unary transport wrapper (fetch + `@protobuf-ts/grpcweb-transport`) |\n| `encrypt-guard.ts` | `assertEncryptSolanaIkaBase`, `isEncryptAllowedForSession` |\n| `DwalletEncryptedLabel.tsx` | React component: encrypt / reveal / clear UI, 4s on-chain status polling |\n| `encrypt.ts` (router) | tRPC procedures bridging UI to encrypt-lab-service |\n| `encrypt-executor-poll.ts` | generic polling helper (available, not directly used for labels) |\n| `encrypt-spl-deposit-stub.ts` | future SPL Enc deposit (returns `not_wired`) |\n| `encrypt-pc-phase-stub.ts` | future PC-Token / PC-Swap phases (returns `not_wired`) |\n\n## related deep-dives\n\n- [encrypt-17-byte-canonical-format.md](/library/tech/encrypt-17-byte-canonical-format) - the byte layout\n- [encrypt-grpc-web-fetch-transport.md](/library/tech/encrypt-grpc-web-fetch-transport) - HTTP/1.1 gRPC-web wrapping\n- [encrypt-protobuf-wire.md](/library/tech/encrypt-protobuf-wire) - hand-rolled protobuf encode / decode\n- [encrypt-create-input.md](/library/tech/encrypt-create-input) - the encrypt path\n- [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed) - the reveal path with signed messages\n- [encrypt-multi-chunk-labels.md](/library/tech/encrypt-multi-chunk-labels) - 64-byte UTF-8 → 4 × 16-byte chunks\n- [encrypt-on-chain-status-polling.md](/library/tech/encrypt-on-chain-status-polling) - the 4s status pill\n',sb=`# Encrypt protobuf wire codec (hand-rolled)

chromatika encodes / decodes Encrypt's \`CreateInput\` and \`ReadCiphertext\` protobuf messages **by hand** using \`@bufbuild/protobuf\`'s low-level \`BinaryWriter\` / \`BinaryReader\`. no \`.proto\` files, no codegen step. the message shapes are small enough that a focused hand-rolled codec is simpler than wiring up the full \`protoc-gen-*\` pipeline.

## why hand-rolled

the alternatives:
1. **bufbuild/protobuf-es codegen**: requires \`.proto\` files and a build step (\`buf generate\` or similar). overkill for two messages
2. **runtime reflection**: requires loading \`.proto\` files at runtime, parsing, registering. heavier than the schema warrants
3. **manual \`BinaryWriter\` / \`BinaryReader\`**: tiny, explicit, easy to audit

we picked manual. the pattern:
- field N with tag \`T\` and wire type \`W\`: \`writer.tag(N, W).type(value)\`
- on decode: \`while (reader.pos < reader.len) { switch (reader.tag()) { case N: ... } }\`

## protobuf wire types we use

| wire type | what | tag byte form |
|-----------|------|---------------|
| 0 (VARINT) | int32, int64, uint32, uint64, bool, enum | \`(field_number << 3) \\| 0\` |
| 2 (LEN) | string, bytes, embedded message, repeated of any | \`(field_number << 3) \\| 2\` |

field numbers and wire types combine into the **tag**: the first byte (or varint) of each field. for example, field 1, wire type 2 (LEN, e.g. bytes) → tag \`0x0A\` (\`(1 << 3) | 2 = 10\`). field 2, wire type 2 → tag \`0x12\` (\`(2 << 3) | 2 = 18\`). etc.

## CreateInputRequest encoding

\`\`\`js
function encodeCreateInputRequest(message: CreateInputRequestWire): Uint8Array {
  const writer = new BinaryWriter();

  // field 1: chain (int32) - tag 0x08
  if (message.chain !== 0) {
    writer.tag(1, WireType.Varint).int32(message.chain);
  }

  // field 2: inputs (repeated EncryptedInput) - tag 0x12
  for (const input of message.inputs) {
    const inputWriter = new BinaryWriter();
    // EncryptedInput field 1: ciphertextBytes (bytes) - tag 0x0A
    inputWriter.tag(1, WireType.LengthDelimited).bytes(input.ciphertextBytes);
    // EncryptedInput field 2: fheType (int32) - tag 0x10
    inputWriter.tag(2, WireType.Varint).int32(input.fheType);
    const inputBytes = inputWriter.finish();

    writer.tag(2, WireType.LengthDelimited).bytes(inputBytes);
  }

  // field 3: proof (bytes) - tag 0x1A
  writer.tag(3, WireType.LengthDelimited).bytes(message.proof);

  // field 4: authorized (bytes, 32-byte program id) - tag 0x22
  writer.tag(4, WireType.LengthDelimited).bytes(message.authorized);

  // field 5: networkEncryptionPublicKey (bytes, 32 bytes) - tag 0x2A
  writer.tag(5, WireType.LengthDelimited).bytes(message.networkEncryptionPublicKey);

  return writer.finish();
}
\`\`\`

field-by-field:
| field | wire type | tag byte | content |
|-------|-----------|----------|---------|
| 1: chain | varint | 0x08 | int32 (0 = Solana) |
| 2: inputs[] | LEN | 0x12 | repeated EncryptedInput (each is its own embedded message) |
| 3: proof | LEN | 0x1A | bytes (empty in pre-alpha) |
| 4: authorized | LEN | 0x22 | bytes (32-byte program id) |
| 5: networkEncryptionPublicKey | LEN | 0x2A | bytes (32-byte network pubkey) |

## CreateInputResponse decoding

\`\`\`js
function decodeCreateInputResponse(data: Uint8Array): CreateInputResponseWire {
  const reader = new BinaryReader(data);
  const ciphertextIdentifiers: Uint8Array[] = [];

  while (reader.pos < reader.len) {
    const [fieldNo, wireType] = reader.tag();
    if (fieldNo === 1 && wireType === WireType.LengthDelimited) {
      ciphertextIdentifiers.push(reader.bytes());
    } else {
      reader.skip(wireType);
    }
  }

  return { ciphertextIdentifiers };
}
\`\`\`

the response has just one repeated bytes field (field 1) - one identifier per input chunk. the server returns them in order.

## ReadCiphertextRequest encoding

\`\`\`js
function encodeReadCiphertextRequest(message: ReadCiphertextRequestWire): Uint8Array {
  const writer = new BinaryWriter();
  writer.tag(1, WireType.LengthDelimited).bytes(message.message);     // BCS-encoded message
  writer.tag(2, WireType.LengthDelimited).bytes(message.signature);   // 64-byte ed25519 sig
  writer.tag(3, WireType.LengthDelimited).bytes(message.signer);      // 32-byte ed25519 pubkey
  return writer.finish();
}
\`\`\`

| field | wire type | tag byte | content |
|-------|-----------|----------|---------|
| 1: message | LEN | 0x0A | BCS-encoded message bytes (see [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed)) |
| 2: signature | LEN | 0x12 | 64-byte ed25519 signature over \`message\` |
| 3: signer | LEN | 0x1A | 32-byte ed25519 public key |

## ReadCiphertextResponse decoding

\`\`\`js
function decodeReadCiphertextResponse(data: Uint8Array): ReadCiphertextResponseWire {
  const reader = new BinaryReader(data);
  let value = new Uint8Array();
  let fheType = 0;
  let digest = new Uint8Array();

  while (reader.pos < reader.len) {
    const [fieldNo, wireType] = reader.tag();
    if (fieldNo === 1 && wireType === WireType.LengthDelimited) {
      value = reader.bytes();
    } else if (fieldNo === 2 && wireType === WireType.Varint) {
      fheType = reader.int32();
    } else if (fieldNo === 3 && wireType === WireType.LengthDelimited) {
      digest = reader.bytes();
    } else {
      reader.skip(wireType);
    }
  }

  return { value, fheType, digest };
}
\`\`\`

response shape:
| field | wire type | content |
|-------|-----------|---------|
| 1: value | LEN | 16-byte plaintext (for EUint128) or 8-byte (for EUint64) |
| 2: fheType | varint | int32 (4 = EUint64, 5 = EUint128) |
| 3: digest | LEN | opaque executor-local proof bytes |

## the \`reader.skip(wireType)\` defensive case

if the server adds new fields in a future protobuf version, our decoder shouldn't crash - it should skip unknown fields. \`BinaryReader.skip(wireType)\` advances the cursor past the unknown field's bytes (varint reads + ignores; LEN reads length prefix + skips body).

protobuf's compatibility model is that unknown fields are silently ignored on decode and not re-emitted on encode. our codec follows this pattern.

## why no proof construction

\`CreateInputRequest.proof\` is empty in pre-alpha. when production Encrypt ships, the proof field will carry zero-knowledge proofs that the input was correctly encrypted. chromatika will then need to construct those proofs client-side (or use the network's encryption public key with a real client encrypt that produces them). today: empty bytes.

## library

- \`@bufbuild/protobuf\` \`BinaryWriter\`, \`BinaryReader\`, \`WireType\` enum
- internal: \`wallet-extension/src/background/encrypt/encrypt-protobuf-wire.ts\`

## related

- [encrypt-grpc-web-fetch-transport.md](/library/tech/encrypt-grpc-web-fetch-transport) - how the encoded bytes get to the network
- [encrypt-17-byte-canonical-format.md](/library/tech/encrypt-17-byte-canonical-format) - what's inside \`EncryptedInput.ciphertextBytes\`
- [encrypt-create-input.md](/library/tech/encrypt-create-input) - the full encrypt flow that calls this codec
- [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed) - the reveal flow
`,ib=`# Encrypt ReadCiphertext (signed) flow

reading a stored ciphertext requires a signed message. chromatika builds a BCS-encoded request (chain + identifier + epoch), signs it with the dWallet's ed25519 key (via \`signMessageSol\` - the same ika MPC path used elsewhere), wraps it in a protobuf \`ReadCiphertextRequest\`, and sends over gRPC-web. the executor returns the 16-byte plaintext value plus an opaque digest.

## the BCS-encoded message

per \`encrypt-read-msg.ts\`, the message layout is:

\`\`\`
[chain (1 byte)]
[ctIdLen (1 byte)] [ciphertextIdentifier (N bytes)]
[rekeyLen (1 byte)] [reencryptionKey (M bytes)]
[epoch (8 bytes, u64 LE)]
\`\`\`

BCS (Binary Canonical Serialization) is the encoding Sui / Move use; chromatika reuses the layout here for cross-protocol consistency.

\`\`\`ts
function encodeReadCiphertextMessage(
  chain: number,                  // 0 = Solana
  ciphertextIdentifier: Uint8Array,
  reencryptionKey: Uint8Array,    // empty for label reveal
  epoch: bigint,                  // 0n typically
): Uint8Array {
  const buf: number[] = [];
  buf.push(chain & 0xFF);
  buf.push(ciphertextIdentifier.length & 0xFF);
  for (const b of ciphertextIdentifier) buf.push(b);
  buf.push(reencryptionKey.length & 0xFF);
  for (const b of reencryptionKey) buf.push(b);
  // u64 LE
  let e = epoch;
  for (let i = 0; i < 8; i++) {
    buf.push(Number(e & 0xFFn));
    e >>= 8n;
  }
  return new Uint8Array(buf);
}
\`\`\`

for label reveal, we always pass \`chain=0\` (Solana), the chunk identifier, empty \`reencryptionKey\`, and \`epoch=0n\`. so the encoded message is:
\`\`\`
[0x00] [<idLen>] [<id bytes>] [0x00] [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]
\`\`\`

## signing

\`\`\`ts
const signerPk = await getDwalletEd25519PublicKey();   // 32-byte pubkey of active ED25519 dWallet
const msg = encodeReadCiphertextMessage(0, identifierBytes, new Uint8Array(0), 0n);
const { signature } = await signMessageSol(msg);
const sigBytes = signatureHexToEd25519Bytes(signature);   // 64 bytes
\`\`\`

\`signMessageSol\` is the chromatika helper that runs the ika MPC ED25519 signing flow with the message bytes (raw, not pre-hashed - ika SHA-512s internally per RFC 8032). returns a hex-encoded 64-byte ed25519 signature.

\`signatureHexToEd25519Bytes\` decodes hex to 64 bytes; trivial conversion.

## the protobuf request

\`\`\`ts
const request = {
  message: msg,            // BCS-encoded bytes
  signature: sigBytes,     // 64-byte ed25519 sig
  signer: signerPk,        // 32-byte ed25519 pubkey
};
const encoded = encodeReadCiphertextRequest(request);
const responseBytes = await encryptGrpcReadCiphertext(GRPC_URL, encoded);
const { value, fheType, digest } = decodeReadCiphertextResponse(responseBytes);
\`\`\`

protobuf wire shape (3 fields, see [encrypt-protobuf-wire.md](/library/tech/encrypt-protobuf-wire)):
| field | content |
|-------|---------|
| 1: message | BCS-encoded ReadCiphertextMessage |
| 2: signature | 64-byte ed25519 sig over message |
| 3: signer | 32-byte ed25519 public key |

server verifies the signature against the signer pubkey + message bytes. mismatch → gRPC error (\`grpc-status\` != 0).

## response decoding

\`ReadCiphertextResponse\`:
| field | content |
|-------|---------|
| 1: value | 16-byte plaintext (for EUint128) or 8-byte (for EUint64) |
| 2: fheType | int32 (5 = EUINT128 for labels, 4 = EUINT64 for lab demos) |
| 3: digest | opaque executor-local proof bytes |

\`value\` is the **raw plaintext bytes** that were stored. for labels, that's a 16-byte UTF-8 chunk (left-justified, zero-padded). for lab demos, it's the LE bytes of the original u64.

## why ed25519 sign here

the executor needs to authenticate the reader. authorization model: "you can read your own ciphertext if you can prove you control the dWallet." the dWallet's ed25519 key is the canonical "who you are" credential on Solana ika base. signing the read request with that key proves authorization.

note: this signs over the BCS message bytes, **not** a pre-hashed digest. ika's ED25519 EdDSA path SHA-512s the bytes internally per RFC 8032. **double-hashing breaks the signature** - same rule as everywhere else (see [ecdsa-secp256k1.md](/library/tech/ecdsa-secp256k1), [ed25519-eddsa.md](/library/tech/ed25519-eddsa)).

## the multi-chunk reveal loop

for a label spanning 4 chunks:

\`\`\`ts
const chunkValues: Uint8Array[] = [];
const digestHexes: string[] = [];

for (const idHex of ciphertextIdentifierHexes) {
  const id = hexToBytes(idHex);
  const msg = encodeReadCiphertextMessage(0, id, new Uint8Array(0), 0n);
  const { signature } = await signMessageSol(msg);
  const sigBytes = signatureHexToEd25519Bytes(signature);
  const encoded = encodeReadCiphertextRequest({ message: msg, signature: sigBytes, signer: signerPk });
  const respBytes = await encryptGrpcReadCiphertext(GRPC_URL, encoded);
  const { value, digest } = decodeReadCiphertextResponse(respBytes);

  chunkValues.push(value);              // 16 bytes
  digestHexes.push(bytesToHex(digest));
}

// reassemble
const labelString = decodeLabelFromChunks(chunkValues, utf8Len);
\`\`\`

each chunk requires a separate sign + read round-trip. for a 4-chunk label, that's 4 ika MPC signing operations (each consuming a presign from the ED25519_EDDSA pool) and 4 gRPC calls. not super fast, but fine for a "click reveal" UX.

partial failure: if any chunk's read fails (network error, executor error), the whole reveal fails - we don't show partial labels. simpler and avoids a "what does half a UTF-8 string look like" question.

## decoding chunks back to a string

\`\`\`ts
function decodeLabelFromChunks(chunkValues: Uint8Array[], utf8Len: number): string {
  const concatenated = new Uint8Array(chunkValues.length * 16);
  for (let i = 0; i < chunkValues.length; i++) {
    concatenated.set(chunkValues[i], i * 16);
  }
  const trimmed = concatenated.slice(0, utf8Len);
  return new TextDecoder('utf-8', { fatal: false }).decode(trimmed);
}
\`\`\`

\`utf8Len\` is the **stored** byte length from when the label was encrypted. trimming to that length strips the zero-padding within the last chunk. \`fatal: false\` lets the decoder produce replacement chars on bad UTF-8 rather than throwing - defensive against accidentally tampered chunks (which shouldn't happen on a healthy executor, but we'd rather see "?�?�" than crash).

## error handling

- session not on Solana ika base: throws via guard before any signing
- ed25519 dWallet not active: signing helper throws ("no active ed25519 dWallet")
- presign pool empty + no funds to refill: signing fails with pool-empty error
- gRPC error from executor (signature invalid, account missing): wrapped, surfaced verbatim
- chunk count mismatch (\`encryptedLabel.ciphertextIdentifierHexes.length === 0\`): "label not found" - probably devnet wipe

## what the digest is for

the executor returns an opaque \`digest\` per read. it's a proof that the read happened (timestamp, nonce, executor-local state). chromatika stores \`digestHexes[]\` in the reveal-result for diagnostics - if a label suddenly returns garbage, comparing digests across reads can show whether the underlying ciphertext changed.

production Encrypt may use the digest for ZK verification ("you read this exact ciphertext at this exact epoch"); pre-alpha treats it as opaque.

## library

- internal: \`encrypt-lab-service.ts\` \`revealDwalletLabelCiphertext\`, \`encryptLabReadCiphertextDemo\`
- internal: \`encrypt-read-msg.ts\` \`encodeReadCiphertextMessage\`
- internal: \`encrypt-protobuf-wire.ts\` \`encodeReadCiphertextRequest\`, \`decodeReadCiphertextResponse\`
- internal: \`encrypt-grpc-web-fetch.ts\` \`encryptGrpcReadCiphertext\`
- internal: \`signMessageSol\` from the dWallet signing helpers (same path EVM / Solana sign-message uses)

## related

- [encrypt-create-input.md](/library/tech/encrypt-create-input) - the write side
- [encrypt-multi-chunk-labels.md](/library/tech/encrypt-multi-chunk-labels) - the chunking strategy
- [encrypt-on-chain-status-polling.md](/library/tech/encrypt-on-chain-status-polling) - the 4s status pill
- [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - the signing primitive
`,ob=`# \`EncryptionBackend\` abstraction (\`src/background/encryption/\`)

a high-level, multi-backend interface for "encrypt arbitrary user data with the vault's master key." separate from \`keyring/\` (key derivation) and \`crypto/\` (low-level web crypto wrappers). lets activity notes, future encrypted contact book entries, and other user-data surfaces compose without each picking their own crypto stack. today only the \`encrypt-xyz\` backend is wired; \`direct-ed25519\` and \`seal\` are stubs that throw \`not-implemented\`.

## the interface

\`\`\`ts
export interface EncryptionBackend {
  readonly id: string;                                       // 'encrypt-xyz' | 'direct-ed25519' | 'seal'
  readonly capabilities: EncryptionBackendCapabilities;

  encryptForRecipient(plaintext: Uint8Array, recipient: RecipientId): Promise<EncryptedRef>;
  decrypt(ref: EncryptedRef): Promise<Uint8Array>;
}

export interface EncryptionBackendCapabilities {
  supportsCrossRecipient: boolean;                           // can encrypt to a different recipient
  supportsThresholdAccess: boolean;                          // threshold-of-N key servers (Seal)
  supportsInlineBody: boolean;                               // body fits inline without walrus blob storage
  maxInlinePlaintextBytes: number;                           // caller-enforced cap
}

export type RecipientId =
  | { kind: 'self' }                                         // active vault's own dWallet
  | { kind: 'ed25519'; pubkey: Uint8Array }                  // cross-recipient via X25519 ECDH (stub)
  | { kind: 'sui-address'; address: string };                // Seal Move policy (stub)

export type EncryptedRef =
  | { backend: 'encrypt-xyz'; payload: EncryptXyzPayload; createdAtMs: number }
  | { backend: 'direct-ed25519'; payload: DirectEd25519Payload; createdAtMs: number }
  | { backend: 'seal'; payload: SealPayload; createdAtMs: number };
\`\`\`

## the dispatch

\`getEncryptionBackend(useCase: string)\` returns the backend registered for a given use case. today:

\`\`\`ts
const backendRegistry: Record<string, EncryptionBackend> = {
  'self-recipient-default': encryptXyzBackend,
  // future: 'cross-recipient-default': directEd25519Backend
  // future: 'threshold-policy-default': sealBackend
};
\`\`\`

callers pass plaintext + a \`RecipientId\`, get back an \`EncryptedRef\`. they don't need to know which backend was used. the ref carries a backend tag so decrypt routes correctly:

\`\`\`ts
async function decryptRefViaRegistry(ref: EncryptedRef): Promise<Uint8Array> {
  switch (ref.backend) {
    case 'encrypt-xyz':     return encryptXyzBackend.decrypt(ref);
    case 'direct-ed25519':  return directEd25519Backend.decrypt(ref);  // stub
    case 'seal':            return sealBackend.decrypt(ref);            // stub
  }
}
\`\`\`

mixed-store tolerance: a single \`chromatika_signed_txs_v1\` could in principle hold notes encrypted under different backends (e.g. old encrypt-xyz refs from before, new direct-ed25519 refs after upgrade). decrypt dispatches per ref. no caller code changes.

## the \`encrypt-xyz\` backend

today's only implemented backend. uses encrypt.xyz pre-alpha gRPC for self-recipient encryption.

### \`EncryptXyzPayload\` shape

\`\`\`ts
interface EncryptXyzPayload {
  bodyCiphertextB64: string;                                 // AES-GCM ciphertext (no tag separation; webcrypto bundles)
  bodyIvB64: string;                                         // 12-byte AES-GCM IV
  wrappedKeyCiphertextIdHexes: [string, string];             // two encrypt.xyz ciphertext-identifier hexes for the K halves
  recipientPubkeyB64: string;                                // 32-byte ed25519 pubkey of the recipient (active vault's dWallet)
  recipientLockedAtVaultId: string;                          // vault id at encryption time (used to detect "wrong vault" on decrypt)
  programIdB58: string;                                      // encrypt program id used at encryption
  fheTypePerHalf: 5;                                         // EUint128 (always)
}
\`\`\`

### encrypt path

\`\`\`ts
async function encryptXyzEncryptForRecipient(plaintext: Uint8Array, recipient: RecipientId): Promise<EncryptedRef> {
  if (recipient.kind !== 'self') throw new EncryptionBackendError({ backend: 'encrypt-xyz', reason: 'unsupported-recipient', message: 'only self-recipient supported in pre-alpha' });

  // 1. generate fresh 32-byte AES-256 key + 12-byte IV
  const K = crypto.getRandomValues(new Uint8Array(32));
  const iv = crypto.getRandomValues(new Uint8Array(12));

  // 2. AES-GCM encrypt the body under K
  const aesKey = await crypto.subtle.importKey('raw', K, { name: 'AES-GCM' }, false, ['encrypt']);
  const ciphertext = new Uint8Array(await crypto.subtle.encrypt({ name: 'AES-GCM', iv }, aesKey, plaintext));

  // 3. wrap K in two halves via encrypt.xyz CreateInput (single batched gRPC call)
  const halfA = K.slice(0, 16);
  const halfB = K.slice(16, 32);
  const inputs = [
    { ciphertextBytes: mockEncryptScalarBytesFromBytes(halfA, FHE_TYPE_EUINT128), fheType: FHE_TYPE_EUINT128 },
    { ciphertextBytes: mockEncryptScalarBytesFromBytes(halfB, FHE_TYPE_EUINT128), fheType: FHE_TYPE_EUINT128 },
  ];
  const { ciphertextIdentifiers } = await encryptXyzCreateInput(GRPC_URL, encodeCreateInputRequest({
    chain: 0, inputs, proof: new Uint8Array(),
    authorized: programId.toBytes(),
    networkEncryptionPublicKey: networkPubkey,
  }));
  if (ciphertextIdentifiers.length !== 2) throw 'expected 2 identifiers';

  // 4. zero K
  K.fill(0);

  // 5. assemble the ref
  return {
    backend: 'encrypt-xyz',
    payload: {
      bodyCiphertextB64: base64Encode(ciphertext),
      bodyIvB64: base64Encode(iv),
      wrappedKeyCiphertextIdHexes: [bytesToHex(ciphertextIdentifiers[0]), bytesToHex(ciphertextIdentifiers[1])],
      recipientPubkeyB64: base64Encode(activeDwalletEd25519Pubkey),
      recipientLockedAtVaultId: activeVaultId,
      programIdB58: programId.toBase58(),
      fheTypePerHalf: 5,
    },
    createdAtMs: Date.now(),
  };
}
\`\`\`

key design choices:
- **K is per-encrypt random**, not derived from any persistent secret. each note has its own AES key. compromise of one K doesn't compromise others
- **two halves via EUint128**: encrypt.xyz EUint128 fits 16 bytes. AES-256 needs 32. split + wrap two halves; same gRPC round-trip
- **\`recipientLockedAtVaultId\`**: persists which vault was active at encryption time. on decrypt, validates the active vault id still matches - prevents accidentally trying to decrypt vault A's note while vault B is active

### decrypt path

\`\`\`ts
async function encryptXyzDecrypt(ref: EncryptedRef): Promise<Uint8Array> {
  if (ref.backend !== 'encrypt-xyz') throw 'wrong backend';
  const { payload } = ref;

  // 1. validate active vault matches what was active at encryption time
  if (sessionState.activeVaultId !== payload.recipientLockedAtVaultId) {
    throw new EncryptionBackendError({ backend: 'encrypt-xyz', reason: 'wrong-vault', message: 'active vault changed; switch to ' + payload.recipientLockedAtVaultId });
  }
  const activePubkey = await getDwalletEd25519PublicKey();
  if (base64Encode(activePubkey) !== payload.recipientPubkeyB64) {
    throw new EncryptionBackendError({ backend: 'encrypt-xyz', reason: 'wrong-vault', message: 'dWallet pubkey mismatch' });
  }

  // 2. unwrap K via two sequential ReadCiphertext gRPC calls (signed)
  const halfA = await unwrapEncryptHalfWithSignedRead(payload.wrappedKeyCiphertextIdHexes[0]);
  const halfB = await unwrapEncryptHalfWithSignedRead(payload.wrappedKeyCiphertextIdHexes[1]);
  const K = new Uint8Array(32);
  K.set(halfA, 0);
  K.set(halfB, 16);

  // 3. AES-GCM decrypt the body
  const aesKey = await crypto.subtle.importKey('raw', K, { name: 'AES-GCM' }, false, ['decrypt']);
  const ciphertext = base64Decode(payload.bodyCiphertextB64);
  const iv = base64Decode(payload.bodyIvB64);
  const plaintext = new Uint8Array(await crypto.subtle.decrypt({ name: 'AES-GCM', iv }, aesKey, ciphertext));

  // 4. zero K
  K.fill(0);

  return plaintext;
}
\`\`\`

\`unwrapEncryptHalfWithSignedRead(idHex)\` is what costs the user MPC fees:
1. encode \`ReadCiphertextMessage\` BCS-style (chain=0, ciphertext id, empty rekey, epoch=0)
2. ika MPC ED25519 signs the message via \`signMessageSol\`
3. sends \`ReadCiphertext\` gRPC request with the signed message
4. returns the 16-byte decrypted half

**two MPC signs** per decrypt (one per K half). that's the source of the 1-3s decrypt latency.

## the \`EncryptionBackendError\` class

structured errors for callers to render specific UI:

\`\`\`ts
class EncryptionBackendError extends Error {
  constructor(public info: { backend: string; reason: string; message: string }) {
    super(info.message);
  }
}

// reasons:
// 'wrong-vault'        - active vault doesn't match the recipient's vault
// 'wrong-recipient'    - active dWallet's pubkey doesn't match recipientPubkeyB64
// 'devnet-wipe'        - encrypt.xyz on-chain ciphertext is gone (devnet wiped)
// 'not-implemented'    - this backend isn't wired yet (direct-ed25519, seal)
// 'unsupported-recipient' - this backend doesn't support the recipient kind
// 'gRPC error'         - encrypt.xyz gRPC failed (network, auth, etc.)
// 'aes-gcm-fail'       - AES-GCM auth tag failed (corrupted ciphertext)
\`\`\`

UI catches and shows specific copy: \`wrong-vault\` → "switch to vault X to read this note", \`devnet-wipe\` → "this note's on-chain ciphertext was lost in a devnet wipe; clear and re-encrypt".

## the future stubs

### \`direct-ed25519\` (cross-recipient via X25519 ECDH)

\`\`\`ts
const directEd25519Backend: EncryptionBackend = {
  id: 'direct-ed25519',
  capabilities: { supportsCrossRecipient: true, supportsThresholdAccess: false, supportsInlineBody: true, maxInlinePlaintextBytes: 8192 },
  async encryptForRecipient(_plaintext, _recipient) {
    throw new EncryptionBackendError({ backend: 'direct-ed25519', reason: 'not-implemented', message: 'X25519 ECDH path not wired yet' });
  },
  async decrypt(_ref) {
    throw new EncryptionBackendError({ backend: 'direct-ed25519', reason: 'not-implemented', message: 'X25519 ECDH path not wired yet' });
  },
};
\`\`\`

would do: convert recipient's ed25519 pubkey to X25519, perform ECDH with caller's X25519 key, derive AES key, encrypt body. recipient does the symmetric ECDH on their side. cross-recipient without on-chain dependencies.

### \`seal\` (Sui Move policy via Mysten's Seal package)

threshold-of-N key servers + Move-based policy enforcement (e.g. "decrypt only allowed if the requester owns dWallet X" or "decrypt only after epoch Y"). powerful but heavyweight; needs Seal SDK + Sui RPC. tracked future.

## library

- internal: \`src/background/encryption/types.ts\` for the interface + types
- internal: \`src/background/encryption/encrypt-xyz-backend.ts\` for the encrypt-xyz impl
- internal: \`src/background/encryption/registry.ts\` for \`getEncryptionBackend\`, \`decryptRefViaRegistry\`
- internal: \`src/background/encrypt/encrypt-lab-service.ts\` for \`mockEncryptScalarBytesFromBytes\`, \`encryptXyzCreateInput\`, gRPC plumbing
- \`crypto.subtle\` for AES-GCM
- \`crypto.getRandomValues\` for K + IV generation

## related

- [activity-notes-encrypt-decrypt.md](/library/tech/activity-notes-encrypt-decrypt) - the activity-notes use case that drives this abstraction
- [signed-tx-record.md](/library/tech/signed-tx-record) - the \`EncryptedRef\` storage location
- [encrypt-create-input.md](/library/tech/encrypt-create-input) - the encrypt.xyz wrap layer
- [encrypt-read-ciphertext-signed.md](/library/tech/encrypt-read-ciphertext-signed) - the encrypt.xyz unwrap layer
`,lb=`# MetaMask \`eth-phishing-detect\` integration

chromatika ships with phishing protection that uses Chrome's declarativeNetRequest (dNR) dynamic rules. flagged domains get redirected to a bundled warning page. the rule list is bundled at install time and refreshed daily from MetaMask's \`eth-phishing-detect\` repo.

## what the dependency provides

\`eth-phishing-detect\` is an npm package + a remote \`config.json\` of known phishing / malicious domains. chromatika uses both:

- bundled package for the install-time list (always available even offline)
- daily fetch of the remote \`config.json\` for fresh updates

remote URL: \`https://raw.githubusercontent.com/MetaMask/eth-phishing-detect/main/src/config.json\`

## the data shape

\`\`\`jsonc
{
  "version": 2,
  "tolerance": 2,
  "fuzzylist": [],                  // domains to fuzzy-match against (typo-squat detection)
  "whitelist": [],                  // explicit allowlist (overrides blocklist)
  "blacklist": [
    "phishing-domain-1.com",
    "phishing-domain-2.com",
    // ...
  ]
}
\`\`\`

chromatika uses \`blacklist\` (and increasingly \`fuzzylist\` for typo-squat detection where dNR allows pattern matching) to build dNR rules.

## the dNR sync flow

\`\`\`ts
// on install / startup
async function syncPhishingRules() {
  // 1. load bundled list
  const bundled = await import('eth-phishing-detect/src/config.json');

  // 2. try to fetch remote (skip if offline)
  let remote = bundled;
  try {
    const resp = await fetch(REMOTE_PHISHING_URL);
    if (resp.ok) remote = await resp.json();
  } catch (e) {
    // offline; keep bundled
  }

  // 3. build dNR rules (capped at 4900 to leave headroom under chrome's 5000 limit)
  const rules = remote.blacklist.slice(0, 4900).map((domain, idx) => ({
    id: idx + 1,
    priority: 1,
    action: {
      type: 'redirect',
      redirect: {
        regexSubstitution: \`\${chrome.runtime.getURL('phishing-warning.html')}?blocked=\${domain}\`,
      },
    },
    condition: {
      urlFilter: \`||\${domain}^\`,                     // matches any path on the domain
      resourceTypes: ['main_frame'],
    },
  }));

  // 4. update dNR
  await chrome.declarativeNetRequest.updateDynamicRules({
    addRules: rules,
    removeRuleIds: previousRuleIds,
  });

  await chrome.storage.local.set({ phishing_last_sync_ms: Date.now() });
}

// schedule daily refresh
chrome.alarms.create('chromatika-phishing-refresh', { periodInMinutes: 24 * 60 });
chrome.alarms.onAlarm.addListener(alarm => {
  if (alarm.name === 'chromatika-phishing-refresh') void syncPhishingRules();
});
\`\`\`

key points:
- bundled list runs first (works offline)
- remote refresh on install / startup / daily alarm
- 4900 cap leaves chrome's 100-rule headroom for future features (e.g. user allowlist)
- \`urlFilter: ||\${domain}^\` matches the domain at any subdomain depth, any path

## the redirect destination

flagged domains route to \`phishing-warning.html\` bundled with the extension:
\`\`\`
chrome-extension://<id>/phishing-warning.html?blocked=phishing-domain.com
\`\`\`

the warning page reads \`?blocked=\` and displays "this domain (phishing-domain.com) is flagged as phishing. proceed at your own risk." plus a "go back" button. there's no override-and-proceed flow today (could add behind a "type the domain to confirm" pattern; tracked).

## manual domain check

for non-navigation contexts (e.g. checking whether a dapp connect request comes from a flagged domain):

\`\`\`ts
async function checkPhishing({ host }): Promise<boolean> {
  const list = await getCachedPhishingList();
  return list.blacklist.includes(host) || matchFuzzy(host, list.fuzzylist);
}
\`\`\`

[connect-dapp.md](/library/user/connect-dapp) (user-guides) calls this **before** showing the connection approval popup. flagged domains never get a chance to prompt.

## cache + storage

- \`chromatika_phishing_cache_v1\` (chrome.storage.local): the most recent fetched config + sync timestamp
- bundled fallback: \`import('eth-phishing-detect/src/config.json')\` direct dependency

## the dNR limit

chrome's \`declarativeNetRequest.MAX_NUMBER_OF_DYNAMIC_RULES\` is 5000. we use 4900 to leave headroom. if \`eth-phishing-detect\` ever pushes past 4900 entries (it's been growing slowly, currently ~3500), chromatika will start truncating. priorities:
1. exact-match blacklist first (most directly malicious)
2. fuzzy-match patterns next (typo-squats)

## the \`@onsol/tldparser\` companion

\`@onsol/tldparser\` is a small dep used for TLD parsing in phishing heuristics ("does this domain look like it's trying to impersonate a legit one"). lightweight wrapper around the public-suffix list.

## library

- \`eth-phishing-detect\` for the bundled list + remote URL (just the config.json; we don't import their JS validators since we're using dNR for matching)
- \`@onsol/tldparser\` for TLD heuristics
- \`chrome.declarativeNetRequest\`, \`chrome.alarms\`, \`chrome.storage.local\`
- internal: \`wallet-extension/src/background/phishing.ts\` for the sync logic
- internal: \`wallet-extension/src/phishing-warning.html\` for the warning page

## related

- [chrome-declarativenetrequest.md](/library/tech/chrome-declarativenetrequest) - the dNR primitive
- [phishing-protection.md](/library/user/phishing-protection) (user-guides) - the user-facing surface
- [connect-dapp.md](/library/user/connect-dapp) (user-guides) - where \`checkPhishing\` gates connections
`,cb=`# EVM personal_sign + signTypedData_v4

\`personal_sign\` and \`eth_signTypedData_v4\` are the two EVM message-signing primitives chromatika supports. both produce 65-byte secp256k1 signatures (\`r||s||v\`). the **rule**: hand ika the **preimage bytes**, not the keccak digest. ika keccak256s once internally per RFC; pre-hashing breaks v-recovery.

## personal_sign (EIP-191)

EIP-191 specifies the format:
\`\`\`
preimage = "\\x19Ethereum Signed Message:\\n" + len(message) + message
digest = keccak256(preimage)
signature = ecdsa_sign(digest, key)
\`\`\`

the \`\\x19\` prefix and \`Ethereum Signed Message:\\n\` marker domain-separate this from raw transaction signatures - a preimage that starts with \`\\x19...\` cannot accidentally collide with a valid RLP-encoded transaction (RLP uses different starting bytes). this prevents "you signed what you thought was a login challenge but it was actually a $1M tx" attacks.

### chromatika's personal_sign path

\`\`\`ts
async function signEvm({ message, chainId }) {
  // 1. wrap per EIP-191
  const messageBytes = typeof message === 'string'
    ? new TextEncoder().encode(message)
    : ethers.getBytes(message);
  const prefix = \`\\x19Ethereum Signed Message:\\n\${messageBytes.length}\`;
  const prefixBytes = new TextEncoder().encode(prefix);
  const preimage = new Uint8Array(prefixBytes.length + messageBytes.length);
  preimage.set(prefixBytes, 0);
  preimage.set(messageBytes, prefixBytes.length);

  // 2. take a presign
  const presignId = takePresign('SECP256K1_ECDSA');

  // 3. sign via ika - PASS THE PREIMAGE
  const sigBytes = await ikaSign({
    dwalletId: activeSecpDwalletId,
    curve: Curve.SECP256K1,
    algorithm: SignatureAlgorithm.ECDSASecp256k1,
    message: preimage,                                 // RAW PREIMAGE
    presignId,
  });

  // 4. parse + recover v
  const { r, s } = parseSignatureFromSignOutput(sigBytes, Curve.SECP256K1, SignatureAlgorithm.ECDSASecp256k1);
  const digest = ethers.keccak256(preimage);
  const dwalletAddress = await getEvmAddress();

  let v: 27 | 28 = 27;
  if (ethers.recoverAddress(digest, { r, s, v: 27 }).toLowerCase() !== dwalletAddress.toLowerCase()) {
    v = 28;
  }

  // 5. assemble 65-byte signature
  return { signature: ethers.concat([r, s, ethers.getBytes(\`0x\${v.toString(16)}\`)]) };
}
\`\`\`

## signTypedData_v4 (EIP-712)

EIP-712 is structured-data signing: instead of arbitrary bytes, the user signs a typed object (e.g. \`{ name: "Permit", domain: ..., message: { holder, spender, value } }\`). the rule is:
\`\`\`
preimage = 0x1901 || domainSeparator || hashStruct(message)
digest = keccak256(preimage)
signature = ecdsa_sign(digest, key)
\`\`\`

\`0x1901\` is the EIP-712 prefix (analog to EIP-191's \`\\x19\`). \`domainSeparator\` and \`hashStruct(message)\` are each 32-byte keccak hashes of the typed-data domain and message respectively, computed via the recursive \`encodeData\` rules in EIP-712.

### the critical preimage method

ethers v6 exposes both:
- \`TypedDataEncoder.encode(domain, types, value)\` - returns the **preimage** (\`0x1901 || domainSep || msgHash\`)
- \`TypedDataEncoder.hash(domain, types, value)\` - returns the **digest** (keccak256 of the preimage)

chromatika **must** call \`encode\` (preimage), not \`hash\` (digest). passing \`hash\` to ika causes ika to keccak256 the digest, signing over the wrong digest, and v-recovery fails.

### chromatika's typed-data path

\`\`\`ts
async function signTypedDataV4({ domain, types, value, chainId }) {
  // 1. compute the preimage (NOT the digest)
  const preimage = ethers.TypedDataEncoder.encode(domain, types, value);
  // preimage is hex string; convert to bytes
  const preimageBytes = ethers.getBytes(preimage);

  // 2. take a presign
  const presignId = takePresign('SECP256K1_ECDSA');

  // 3. sign via ika - PASS THE PREIMAGE
  const sigBytes = await ikaSign({
    dwalletId: activeSecpDwalletId,
    curve: Curve.SECP256K1,
    algorithm: SignatureAlgorithm.ECDSASecp256k1,
    message: preimageBytes,
    presignId,
  });

  // 4. parse + recover v
  const { r, s } = parseSignatureFromSignOutput(sigBytes, Curve.SECP256K1, SignatureAlgorithm.ECDSASecp256k1);
  const digest = ethers.keccak256(preimageBytes);
  const dwalletAddress = await getEvmAddress();

  let v: 27 | 28 = 27;
  if (ethers.recoverAddress(digest, { r, s, v: 27 }).toLowerCase() !== dwalletAddress.toLowerCase()) {
    v = 28;
  }

  return { signature: ethers.concat([r, s, ethers.getBytes(\`0x\${v.toString(16)}\`)]) };
}
\`\`\`

## the rule, summarized

| operation | what to pass to ika | what NOT to pass |
|-----------|---------------------|------------------|
| transaction send | \`tx.unsignedSerialized\` (RLP-encoded unsigned tx) | the keccak digest of the tx |
| \`personal_sign\` | the EIP-191 wrapped bytes | the keccak digest of the wrapped bytes |
| \`signTypedData_v4\` | \`TypedDataEncoder.encode(...)\` | \`TypedDataEncoder.hash(...)\` |

ika hashes once with KECCAK256 internally for SECP256K1_ECDSA. ALWAYS pass the **preimage bytes**.

## what double-hashing looks like

if you accidentally pre-hash:
\`\`\`ts
const digest = ethers.keccak256(preimage);
const sig = await ikaSign({ message: digest, ... });   // BAD - ika hashes the digest
const recovered = ethers.recoverAddress(ethers.keccak256(preimage), sig);   // recovered != expected
\`\`\`

ika has now signed \`keccak256(digest) = keccak256(keccak256(preimage))\`. when verifiers compute \`keccak256(preimage)\` and try to recover, they get a different address. the v-recovery loop tries 27 and 28 and neither matches.

if you ever see "signature does not recover to expected address" errors, suspect a double-hash bug first.

## chain id in the v byte (legacy chains only)

EIP-155 introduced chain-aware tx signing where \`v = 35 + chainId * 2 + (0 or 1)\`. for typed transactions (type 2 EIP-1559), \`chainId\` is a separate field and \`v\` is just the parity bit (0 or 1, written as 27 or 28 by ethers). chromatika defaults to type 2, so the v-recovery is straightforward.

\`personal_sign\` does NOT incorporate chain id - it's the same signature regardless of which network the user thinks they're on. that's a feature (off-chain signing for any network) and a footgun (replay across networks if a dapp doesn't include chain id in its message).

## library

- \`ethers\` v6 \`TypedDataEncoder\`, \`keccak256\`, \`recoverAddress\`, \`getBytes\`, \`concat\`
- internal: \`wallet-extension/src/background/chains/signing/evm.ts\` \`signEvm\`, \`signTypedDataV4\`
- internal: \`wallet-extension/src/background/ika/signing.ts\`

## related

- [evm-send-flow.md](/library/tech/evm-send-flow) - the transaction send path
- [ecdsa-secp256k1.md](/library/tech/ecdsa-secp256k1) - underlying signature math
- [signature-normalization.md](/library/tech/signature-normalization) - parseSignatureFromSignOutput details
`,ub=`# EVM transaction send flow

\`sendEvmTx\` (wallet-UI direct, no popup) and \`approveTxRequest\` (dapp \`eth_sendTransaction\` after popup approval) both end up at \`signAndBroadcastEvm\`. this doc traces the exact path: build with ethers v6, sign via ika MPC SECP256K1_ECDSA, broadcast via the active EVM provider.

## the call entrypoints

\`\`\`ts
// wallet UI direct - no popup
sendEvmTx({ to, value, data, chainId? })
  → signAndBroadcastEvm

// dapp originated, after approveTxRequest
approveTxRequest({ id, gasOverrides? })
  → signAndBroadcastEvm
\`\`\`

both produce the same broadcast result. **never** add an approval popup to the wallet-UI flow - the user is already inside the wallet making an intentional action.

## step-by-step

\`\`\`ts
async function signAndBroadcastEvm({ to, value, data, chainId, gas, ... }) {
  // 1. resolve provider for the target chain (may differ from active chain)
  const { provider, chainId: providerChainId, rpcUrl } = chainId
    ? await getRpcProviderForChain(chainId)
    : await getRpcProvider();

  // 2. resolve sender address (active SECP256K1 dWallet's EVM address)
  const from = await getEvmAddress();

  // 3. fetch nonce + gas
  const nonce = await provider.getTransactionCount(from, 'pending');
  const feeData = gas ?? await provider.getFeeData();   // EIP-1559 maxFeePerGas + maxPriorityFeePerGas

  // 4. estimate gas if not provided
  const gasLimit = gas?.gasLimit ?? await provider.estimateGas({
    from, to, value, data,
  });

  // 5. construct unsigned transaction
  const tx = {
    type: 2,                                              // EIP-1559
    chainId: providerChainId,
    to,
    value: value ?? 0n,
    data: data ?? '0x',
    nonce,
    gasLimit,
    maxFeePerGas: feeData.maxFeePerGas,
    maxPriorityFeePerGas: feeData.maxPriorityFeePerGas,
  };
  const txObj = Transaction.from(tx);                    // ethers v6
  const preimage = txObj.unsignedSerialized;             // RLP-encoded unsigned bytes

  // 6. sign via ika MPC SECP256K1_ECDSA
  const presignId = takePresign('SECP256K1_ECDSA');
  const sigBytes = await ikaSign({
    dwalletId: activeSecpDwalletId,
    curve: Curve.SECP256K1,
    algorithm: SignatureAlgorithm.ECDSASecp256k1,
    message: ethers.getBytes(preimage),                  // PREIMAGE, not digest
    presignId,
  });

  // 7. parse + recover v
  const { r, s } = parseSignatureFromSignOutput(sigBytes, Curve.SECP256K1, SignatureAlgorithm.ECDSASecp256k1);
  const digest = keccak256(preimage);
  let v: 27 | 28 = 27;
  if (recoverAddress(digest, { r, s, v: 27 }).toLowerCase() !== from.toLowerCase()) {
    v = 28;
    if (recoverAddress(digest, { r, s, v: 28 }).toLowerCase() !== from.toLowerCase()) {
      throw new Error('signature does not recover to expected address');
    }
  }

  // 8. assemble final EIP-155 v: v + 35 + chainId * 2 (for typed-tx, this is encoded as parity bit)
  // ethers Transaction.from handles this when you set the signature
  txObj.signature = { r, s, v };
  const signedRaw = txObj.serialized;

  // 9. broadcast
  const txResp = await provider.broadcastTransaction(signedRaw);
  return { hash: txResp.hash, signedRaw };
}
\`\`\`

## the preimage passthrough rule (again)

step 6 hands ika \`txObj.unsignedSerialized\` - the RLP-encoded unsigned tx bytes. ika keccak256s this internally and signs the digest. **never** pre-keccak the preimage; double-hashing breaks v-recovery.

## v-recovery dance

step 7 tries \`v=27\` and \`v=28\`. EVM's recovery byte distinguishes between the two y-candidates that satisfy \`r\`. ECDSA produces an ambiguous signature without v; we resolve by checking which candidate recovers to our known address.

if neither matches, something is wrong:
- preimage was double-hashed somewhere (bug)
- dWallet pubkey doesn't match the signature (network issue)
- ika returned malformed sig (rare)

we throw rather than guess.

## type 2 (EIP-1559) by default

post-London (Aug 2021), all major EVM chains support EIP-1559. chromatika defaults to type 2 transactions:
- \`maxFeePerGas\` (cap on total per-gas cost)
- \`maxPriorityFeePerGas\` (tip to validator)
- \`chainId\` baked into the tx (no EIP-155 v offset; the parity bit in v handles it instead)

legacy type 0 transactions (pre-EIP-1559) are supported for chains that don't have 1559 active. ethers v6's \`Transaction.from\` handles both.

## the wallet-UI vs dapp split

\`\`\`
wallet UI (e.g. SendPage in side panel)
  → sendEvmTx tRPC procedure
  → signAndBroadcastEvm
  → broadcast, return hash

dapp page → eth_sendTransaction RPC
  → bridge enqueues TX_APPROVE pending request
  → opens popup at index.html?txapprove=<id>
  → popup calls getTxApprovalRequest, getTxSimulationPreview, getTxGasOptions
  → user clicks approve
  → tRPC calls approveTxRequest({ id, gasOverrides })
  → signAndBroadcastEvm
  → broadcast, return hash to dapp via bridge
\`\`\`

both paths converge at \`signAndBroadcastEvm\`. the dapp path adds the popup gate; the wallet-UI path skips it because the user is already in the wallet.

## the gas-options helper

\`getTxGasOptions({ id })\` returns \`{ slow, normal, fast }\` presets:

\`\`\`ts
async function getTxGasOptions({ id }) {
  const pending = await getPendingTx(id);
  const provider = await getRpcProviderForChain(pending.chainId);
  const feeData = await provider.getFeeData();   // current network fees

  return {
    slow: {
      maxFeePerGas: feeData.maxFeePerGas * 80n / 100n,        // 80% of network estimate
      maxPriorityFeePerGas: feeData.maxPriorityFeePerGas * 80n / 100n,
      estimatedSeconds: 60,
    },
    normal: {
      maxFeePerGas: feeData.maxFeePerGas,
      maxPriorityFeePerGas: feeData.maxPriorityFeePerGas,
      estimatedSeconds: 30,
    },
    fast: {
      maxFeePerGas: feeData.maxFeePerGas * 130n / 100n,
      maxPriorityFeePerGas: feeData.maxPriorityFeePerGas * 130n / 100n,
      estimatedSeconds: 12,
    },
    custom: null,   // user supplies
  };
}
\`\`\`

USD estimates derive from the price waterfall (see [price-waterfall-and-sources.md](/library/tech/price-waterfall-and-sources)).

## the simulation helper

\`getTxSimulationPreview({ id })\` runs an \`eth_call\` at the latest block:

\`\`\`ts
async function getTxSimulationPreview({ id }) {
  const pending = await getPendingTx(id);
  const provider = await getRpcProviderForChain(pending.chainId);
  try {
    const result = await provider.call({
      from: pending.from,
      to: pending.to,
      value: pending.value,
      data: pending.data,
    });
    return { ok: true, returnData: result, decoded: tryDecodeReturnData(result, pending.data) };
  } catch (e) {
    return { ok: false, error: e.message };
  }
}
\`\`\`

best-effort decoding: if the function selector matches a known ABI, we decode the return data. otherwise raw hex. no third-party simulator.

## library

- \`ethers\` v6 \`Transaction\`, \`keccak256\`, \`recoverAddress\`, \`getBytes\`, \`JsonRpcProvider\`
- internal: \`wallet-extension/src/background/chains/evm-send.ts\` \`signAndBroadcastEvm\`, \`getRpcProvider\`, \`getRpcProviderForChain\`
- internal: \`wallet-extension/src/background/ika/signing.ts\` for ika sign dispatch

## related

- [evm-personal-sign-and-typeddata.md](/library/tech/evm-personal-sign-and-typeddata) - the message signing path
- [signature-normalization.md](/library/tech/signature-normalization) - parseSignatureFromSignOutput details
- [ecdsa-secp256k1.md](/library/tech/ecdsa-secp256k1) - the underlying curve / sig math
`,db=`# ika accept-share zero-trust flow

after DKG completes, the user's share lives on-chain encrypted to their encryption key. the dWallet is in \`awaiting_key_holder_signature\` state - meaning the network has done its part, but the user hasn't yet asserted ownership. \`acceptEncryptedUserShare\` is the call that closes the loop.

this is called "zero-trust" because chromatika doesn't have to trust that the network gave it a correct share; the user can decrypt and verify locally before signing the acceptance.

## prerequisites

- DKG has completed and the dWallet sits in \`awaiting_key_holder_signature\` state
- chromatika has the matching \`UserShareEncryptionKeys\` (decryption side)
- vault is unlocked
- the user's encryption-key address matches the address the share was encrypted to during DKG (this is automatic if the same vault drove DKG)

## the lifecycle state

dWallet states (Sui base):
- \`requesting_dkg_user_input\` - DKG starting
- \`awaiting_key_holder_signature\` - DKG done, share encrypted, waiting for accept
- \`active\` - accept ran, dWallet ready for sign
- some others for error / transitional states

\`dWalletState({ curve })\` returns the current state. \`refreshDWalletState({ curve })\` forces a chain re-fetch.

\`completeDWalletZeroTrust({ curve, dwalletId? })\` is the chromatika tRPC procedure that finishes the accept. if multiple dWallets are in \`awaiting_key_holder_signature\` (rare), pass \`dwalletId\` to disambiguate.

## the flow (Sui base)

\`\`\`
1. fetch the encrypted user share from chain
   - read DKG completion event for encrypted_share_id
   - or call ikaClient.getEncryptedUserSecretKeyShare(dwalletId) which queries the on-chain object

2. decrypt locally
   - usk = sessionState.ikaShareKeys[curve]   // UserShareEncryptionKeys for this curve
   - decryptedShare = usk.decryptUserSecretKeyShare(encryptedShareBytes)
   - validate the decrypted share matches the dWallet's published public key
     (this is the "zero-trust" check - if validation fails, the network gave us
      a bad share; abort)

3. build the accept PTB
   tx = new IkaTransaction()
   tx.acceptEncryptedUserShare({
     dwalletId,
     encryptedShareId,
     userSignatureOverShare,   // signed proof the user has accepted
   })

4. submit, wait for tx digest, wait for dWallet state to flip to 'active'

5. setActiveDwallet(dwalletId) to wire it up as the active signer for its curve
\`\`\`

## the flow (Solana base, pre-alpha)

\`\`\`
1. read the encrypted user share from the Solana ika program account
2. decrypt locally with the matching USK
3. validate
4. submit the accept call via gRPC
   - includes approve_message signed by in-extension fee-payer
5. wait for chain state, set active
\`\`\`

\`SolanaIkaAdapter.getEncryptedUserSecretKeyShare\` **throws** today (Sui-only read). Solana-base DKG + sign bypass the adapter and go through \`SolanaIkaGrpcClient\` directly, including the accept-share step.

## why "zero-trust"

the network could in principle hand chromatika a bogus encrypted share that doesn't match the dWallet's public key. zero-trust = chromatika decrypts the share and verifies before accepting. if validation fails, abort and don't accept.

what verification looks like:
- decrypt the share bytes with the user's encryption key (private side)
- derive the public component from the decrypted share
- compare to the on-chain dWallet's public key
- if they match: the share is correct, sign the acceptance
- if they don't: discard, alert the user, the dWallet is unusable

## what doesn't work

- **accept without DKG**: there's no encrypted share to accept; the call rejects
- **accept on a different vault than the one that drove DKG**: the user's encryption key won't match what the share was encrypted to. you'd have to transfer the dWallet (see [ika-re-encrypt-transfer.md](/library/tech/ika-re-encrypt-transfer)) first
- **accept with the wrong curve**: dWallets are bound to their curve at DKG; you can't promote a SECP256K1 dWallet to ED25519 or vice versa
- **timing**: there's no on-chain expiration on \`awaiting_key_holder_signature\` today, but if the user takes weeks to accept they should re-fetch state in case of indexer drift

## the multi-vault interaction

if a user has multiple dWallet Vaults under one chromatika install, each vault has its own \`UserShareEncryptionKeys\`. running DKG from vault A and trying to \`completeDWalletZeroTrust\` from vault B fails the validation - vault B can't decrypt a share that was encrypted to vault A's USK. always run accept-share from the vault that ran DKG.

if you want to move a dWallet to a different vault, that's the **transfer** path, not accept-share.

## library

- \`@ika.xyz/sdk\` \`IkaTransaction.acceptEncryptedUserShare\`
- internal: \`wallet-extension/src/background/ika/dwallet-lifecycle.ts\` for the orchestration logic
- internal: \`wallet-extension/src/background/ika/ika-adapter.ts\` for adapter dispatch
- internal: \`UserShareEncryptionKeys.decryptUserSecretKeyShare\` (or equivalent SDK method) for local decryption + validation
`,hb=`# ika DKG flow

DKG (distributed key generation) is the protocol step where chromatika and the ika network jointly produce a new dWallet. neither party ever sees the full signing key. at the end, the dWallet exists on-chain (Sui object id or Solana PDA) with a public key both parties have agreed on, and chromatika holds its share inside \`UserShareEncryptionKeys\`.

## prerequisites

- chromatika vault is unlocked
- the active dWallet Vault is funded with IKA + SUI (Sui base) or SOL (Solana base) above the minimum required by the ika dynamic pricing
- a \`UserShareEncryptionKeys\` per curve has been derived locally (from the vault's seed source)
- on Solana base: \`VITE_SOLANA_IKA_BASE=true\`, in-extension fee-payer keypair has SOL for \`approve_message\` gRPC fees

## the call

\`\`\`
createDWallet({ curve: 'SECP256K1' | 'ED25519' })
\`\`\`

dispatches to the right adapter via \`getIkaAdapter(session, baseChain)\`:
- Sui base: builds a Sui PTB through \`IkaTransaction\`
- Solana base: drives the protocol via \`SolanaIkaGrpcClient\` over gRPC

## Sui base DKG (PTB-driven)

\`\`\`
1. read current ika pricing
   pricing = await getRequiredCoinAmounts(ikaClient)
   // queries coordinatorInner.pricing_and_fee_manager.current.pricing_map
   // returns minimum IKA + SUI amounts plus a 10% buffer

2. build the PTB
   tx = new IkaTransaction()

   // split coins for the call
   [ikaCoin] = tx.splitCoins(IKA_COIN_OBJECT, [pricing.ikaAmount])
   [suiCoin] = tx.splitCoins(SUI_GAS, [pricing.suiAmount])

   // requestDWalletDKG takes &mut Coin<IKA> and &mut Coin<SUI>
   // returns a multi-value tuple - use indexed access (dkgResult[0])
   dkgResult = tx.requestDWalletDKG({
     curve,
     ikaCoin,
     suiCoin,
     userPublicKey: usk.publicKey,
   })

   // CRITICAL: ika coin args are &mut references, not by value
   // the split coins survive the moveCall; they MUST be transferred back to owner
   tx.transferObjects([ikaCoin, suiCoin], owner)

3. simulate first
   - if simulation fails with abort code 1 = insufficient IKA
   - if simulation fails with abort code 2 = insufficient SUI
   - if "Unused result without the drop ability" → forgot the transferObjects in step 2

4. submit the PTB
   - signs with the HD fee-payer keypair (Sui ed25519)
   - Sui RPC broadcasts, returns digest

5. wait for the DKG output event
   - the network publishes events when the DKG round completes
   - chromatika reads events via SuiGraphQLClient (client.core.*)

6. dWallet is now in awaiting_key_holder_signature state
   - encrypted user share lives on-chain
   - continue to ika-accept-share-zerotrust.md
\`\`\`

## Solana base DKG (gRPC-driven)

\`\`\`
1. derive needed gRPC parameters
   - dwallet_attestation_bytes from the user share encryption key
   - approve_message signed by the in-extension fee-payer keypair

2. request DKG over gRPC
   await ikaClient.requestDWalletDKG({
     curve,
     userPublicKey,
     dwalletAttestationBytes,
     approveMessage,
   })

3. on success, persist:
   - record.dwalletMeta[].dwalletAttestationBytesB64
   - record.dwalletMeta[].dwalletPublicKeyB64
   - record.dwalletMeta[].baseChain = 'solana'

4. dWallet flips to awaiting_key_holder_signature
5. continue to ika-accept-share-zerotrust.md
\`\`\`

note: Solana ika base is pre-alpha. all signatures come from a single mock signer; the protocol is not finalized. **never** trust Solana-base DKG output for real value.

## the multi-value return value

\`requestDWalletDKG\` returns **multiple values** in the PTB result. chromatika reads \`dkgResult[0]\` (indexed access). compare:
- \`requestSign\` and \`requestReEncryptUserShareFor\` return **no values** (void)
- \`requestGlobalPresign\` returns **a single value with drop ability**, safe to ignore

if you write your own ika PTBs and forget the indexed access, you get type errors during PTB construction.

## dynamic pricing

ika prices ika operations dynamically based on network load. **never hardcode coin split amounts** - always go through \`getRequiredCoinAmounts(ikaClient)\`. abort codes:
- code 1 = insufficient IKA (the price you supplied was less than the current \`pricing_map.ikaAmount\`)
- code 2 = insufficient SUI (same but for SUI)

these abort during simulation if your split is too low. retry with a fresh \`getRequiredCoinAmounts\` quote.

## where the user share lives after DKG

- **Sui base**: encrypted on-chain in an \`EncryptedUserSecretKeyShare\` object, owned by the user's encryption-key address. transferable via re-encryption (see [ika-re-encrypt-transfer.md](/library/tech/ika-re-encrypt-transfer))
- **Solana base**: in a Solana program account; same logical role but Solana account model

chromatika **does not** locally hold the unencrypted user share. it derives \`UserShareEncryptionKeys\` from the seed at unlock and uses those keys to encrypt / decrypt the on-chain share material as needed.

## what doesn't work

- **DKG without funding**: the PTB simulation aborts. fund the vault first
- **DKG with mismatched curve / \`userPublicKey\`**: the network rejects. ensure the USK passed matches the curve you're requesting
- **DKG on a locked wallet**: tRPC procedure fails with "wallet locked"; unlock first

## library

- \`@ika.xyz/sdk\` \`IkaTransaction\`, \`IkaClient\`, \`requestDWalletDKG\`
- \`@ika.xyz/pre-alpha-solana-client\` \`SolanaIkaGrpcClient\`
- \`@mysten/sui\` for PTB construction (used by \`IkaTransaction\`)
- internal: \`wallet-extension/src/background/ika/dwallet-lifecycle.ts\` for the orchestration
- internal: \`wallet-extension/src/background/ika/pricing.ts\` \`getRequiredCoinAmounts\`
- internal: \`wallet-extension/src/background/ika/ika-adapter.ts\` for \`getIkaAdapter\` dispatch
`,pb=`# ika presign pool implementation

ika MPC signing has two phases: an **online** phase (fast, runs at the moment a signature is needed) and an **offline** phase (slower, runs ahead of time and produces "presign material" that can be combined with any future message). the presign pool stores the precomputed material so signing is fast.

## the three pools

chromatika maintains three presign pools, each scoped per active dWallet Vault:

| pool key | what it's for |
|----------|---------------|
| \`SECP256K1_ECDSA\` | EVM signing, generic ECDSA |
| \`SECP256K1_TAPROOT\` | BTC P2TR Schnorr signing |
| \`ED25519_EDDSA\` | Sui, Solana, Aptos ed25519 signing |

separate pools exist because the presign math differs per algorithm (see [ecdsa-secp256k1.md](/library/tech/ecdsa-secp256k1), [taproot-schnorr.md](/library/tech/taproot-schnorr), [ed25519-eddsa.md](/library/tech/ed25519-eddsa)). a SECP256K1_ECDSA presign cannot be used for a Taproot signature even though both run on secp256k1.

## storage layout

\`\`\`
chrome.storage.local["chromatika_presign_pools_v3_<vaultId>"] = {
  SECP256K1_ECDSA: [
    { presignId: "0x...", state: "ready", createdAtMs: ... },
    { presignId: "0x...", state: "ready", createdAtMs: ... }
  ],
  SECP256K1_TAPROOT: [...],
  ED25519_EDDSA: [...]
}
\`\`\`

per-vault scoping (\`<vaultId>\` suffix) means switching active vault loads a different pool set. switching back restores. presign material is **per dWallet Vault** because the user share encryption keys differ per vault.

## the auto-refill alarm

\`chrome.alarms.create('chromatika-presign-refill', { periodInMinutes: 5 })\` fires every 5 minutes. on each fire:

\`\`\`
1. is the wallet locked? → skip (can't sign without USK)
2. for each pool key:
     count = pool[poolKey].length
     if count < LOW_WATER (2):
       refill(poolKey, REFILL_COUNT (3))   // top up by 3
3. persist updated pool to chrome.storage.local
\`\`\`

constants per CLAUDE.md:
- low-water = 2
- refill count = 3

so a fully drained pool tops up to 3, and a pool with 1 entry tops up to 4. pools above the low-water mark do nothing.

## manual refill

\`replenishPresign({ poolKey, count })\` triggers an immediate refill of the specified pool. count range is 1-20.

\`\`\`
for i in 1..count:
  if poolKey === 'SECP256K1_ECDSA' or 'SECP256K1_TAPROOT':
    presignId = await ikaClient.requestSecpPresign(poolKey, dwallet)
  else if poolKey === 'ED25519_EDDSA':
    presignId = await ikaClient.requestEddsaPresign(dwallet)
  pool[poolKey].push({ presignId, state: 'ready', createdAtMs: Date.now() })
persist
\`\`\`

each \`requestPresign\` call is its own transaction (Sui PTB or Solana gRPC). funding requirements: same dynamic pricing as DKG / sign - call \`getRequiredCoinAmounts\` first.

## taking a presign

\`takePresign(poolKey)\` pops one entry from the pool:
\`\`\`
function takePresign(poolKey):
  if pool[poolKey].length === 0:
    throw 'pool empty - call replenishPresign first'
  return pool[poolKey].shift()   // FIFO
\`\`\`

shorthand: \`takePresignId()\` is an alias for \`takePresign('SECP256K1_ECDSA')\` since EVM is the most common path.

once popped, the presign id is committed to one signature. it cannot be reused (security property: each presign produces one and only one signature; reusing a presign reveals the secret key).

## consumption flow during sign

\`\`\`
1. user requests a sign (e.g. signEvm(message))
2. wallet calls takePresign(SECP256K1_ECDSA) → presignId
3. wallet builds a sign PTB with the presignId + message
4. ika network completes the signature using the presign material
5. signature returns
6. on success, the popped presign stays consumed; pool moves on
7. on failure, the wallet may push the presign back if it's recoverable (rare)
\`\`\`

if \`takePresign\` returns "pool empty" mid-sign, the wallet can synchronously call \`replenishPresign(poolKey, 1)\` and retry. but this defeats the point of the precomputation - it adds latency to the sign call.

## per-vault isolation

\`\`\`
sessionState.activeVaultId === 'vault-a'
  → chromatika_presign_pools_v3_vault-a is the active pool
  → switching to vault-b loads chromatika_presign_pools_v3_vault-b
  → vault-a's pool stays cached on disk for when you switch back
\`\`\`

presign material is **specific to a dWallet** (it's tied to the user share). pool entries from vault A's dWallet cannot be used to sign with vault B's dWallet.

## what happens if the pool drains while locked

the auto-refill alarm only fires when the wallet is **unlocked**. if the user stays locked for hours, the pool drains naturally as auto-refill skips. on next unlock, the alarm refills. in the meantime, signing requests fail with pool-empty errors and the user sees a "refilling presign material..." progress UI.

## debugging presign issues

- \`presignPool\` tRPC query returns counts per pool for the active vault
- \`signingProgress\` returns the current signing-in-progress state if a sign is mid-flight
- \`replenishPresign\` to manually refill if the pool is stuck
- check \`chromatika_presign_pools_v3_<vaultId>\` directly via dev tools if you suspect storage corruption

## what doesn't work

- using a presign for the wrong algorithm (TAPROOT presign for ECDSA sign, etc.) - the protocol rejects
- reusing a presign across two signs - second sign rejects on chain (signature already consumed)
- presigns for a dWallet that's been transferred away - they're invalid; refill against the new dWallet

## library

- \`@ika.xyz/sdk\` \`IkaTransaction.requestSecpPresign\`, \`requestEddsaPresign\`, \`requestGlobalPresign\`
- internal: \`wallet-extension/src/background/ika/presign-pool.ts\` for pool R/W, refill orchestration, \`takePresign\` / \`replenishPresign\`
- internal: \`chrome.alarms\` for the 5-min refill alarm
`,mb=`# ika re-encrypt and dWallet transfer

a dWallet can move from one user to another by **re-encrypting the user share** to the new owner's encryption key. the dWallet itself stays at the same address; only "who can sign with it" changes. neither party ever sees the unencrypted share.

## the model

each dWallet has an **encrypted user share** stored on-chain (in the Sui object graph or Solana program account). that share is encrypted to the **owner's encryption key** (registered via \`registerEncryptionKey\`). to transfer:
1. sender re-encrypts their share to the recipient's encryption key
2. on-chain transition: dWallet is now "owned" by the recipient (in the sense that the encrypted share is theirs to decrypt)
3. recipient calls \`acceptTransferredDWallet\` to verify + finalize

the underlying ika MPC protocol is unchanged; only the user-share custody moves.

## prerequisites for sender

- vault unlocked
- the dWallet to transfer is \`active\` for its curve and the sender controls it
- recipient's Sui address is known
- recipient has registered their encryption key on-chain via \`registerEncryptionKey({ curve })\`

## prerequisites for recipient

- vault unlocked
- recipient has registered their encryption key for the matching curve **before** the transfer is initiated (otherwise the sender's \`requestReEncryptUserShareFor\` has nowhere to encrypt to)

## the sender flow

\`\`\`
1. lookup recipient's encryption key on-chain
   recipientPubkey = await ikaClient.getSenderEncryptionKeyAddress(recipientSuiAddress)
   // throws if recipient hasn't registered

2. transferDWallet({ curve, recipientSuiAddress })
   - resolves to ikaClient.requestReEncryptUserShareFor with curve, dwalletId, recipientPubkey
3. build PTB
   tx = new IkaTransaction()
   tx.requestReEncryptUserShareFor({
     dwalletId,
     curve,
     recipientEncryptionKeyAddress: recipientPubkey,
   })
   // requestReEncryptUserShareFor returns void

4. fund + simulate + submit
5. on success, the recipient now has an encrypted-share-for-them on-chain
6. send the recipient: tx digest + curve + dWallet id (off-chain channel - email, signal, etc.)
\`\`\`

## the recipient flow

\`\`\`
1. recipient receives: digest, curve, dwalletId from sender (off-chain)

2. parse hints from the digest
   hints = await parseTransferTxDigest({ digest })
   // returns { encryptedShareId, senderEncryptionKeyAddress, ... }

3. complete the accept call
   await acceptTransferredDWallet({
     curve,
     dwalletId,
     senderEncryptionKeyAddress: hints.senderEncryptionKeyAddress,
     encryptedShareId: hints.encryptedShareId,
   })

4. internally:
   - read encrypted share from chain
   - decrypt with recipient's USK (their own UserShareEncryptionKeys)
   - validate decrypted share matches dWallet's published public key (zero-trust check)
   - if validation passes, build PTB to call ikaClient.acceptEncryptedUserShare
   - submit, wait for state to flip to 'active' (for the recipient)

5. setActiveDwallet(dwalletId) to wire it up as the active signer
\`\`\`

after step 4, the **sender** can no longer sign with the dWallet - their share is no longer the canonical one for this dWallet. the recipient is now the sole signer.

## why re-encryption and not "send the secret"

in a normal wallet, transfer means "give the recipient the secret key". this is bad because:
- the recipient now holds plaintext key material
- the sender either keeps a copy (= both can sign, no clean handoff) or destroys their copy (= no provable handoff)

ika's re-encryption is cleaner:
- on-chain, there's exactly one encrypted share at any time (well, there's the old encrypted-to-sender and the new encrypted-to-recipient temporarily; the protocol manages the cutover)
- nobody ever sees the plaintext share
- the chain itself is the source of truth for "who owns this dWallet now"

## the encryption-key registration step

\`registerEncryptionKey({ curve })\` registers the user's encryption keypair on-chain. it's a one-time setup per curve per vault:
\`\`\`
1. usk = sessionState.ikaShareKeys[curve]
2. encryption_pubkey = usk.encryptionPublicKey
3. PTB: ikaClient.registerEncryptionKey(encryption_pubkey, curve)
4. on-chain: a per-user encryption-key object is created at a deterministic address (the user's encryption-key address)
\`\`\`

without registration, the user can't receive a transferred dWallet (the sender's \`requestReEncryptUserShareFor\` has nowhere to encrypt to).

\`getSenderEncryptionKeyAddress({ curve })\` queries the user's own registered encryption key address - useful to confirm registration succeeded.

## what about Solana base

Solana ika base re-encryption surface is **not generally usable today**. per the agent's exploration, \`SolanaIkaAdapter\`'s transfer-related reads (\`getEncryptedUserSecretKeyShare\`, etc.) **throw**. transferring a Solana-base dWallet is tracked future when the pre-alpha Solana SDK exposes the necessary primitives.

## what doesn't work

- transferring without recipient registration: \`requestReEncryptUserShareFor\` rejects (no target encryption key)
- transferring to your own address (self-transfer): protocol may reject or no-op; not a useful operation
- accept-side using a vault with a different USK than the encrypted-share's target: \`decryptUserSecretKeyShare\` throws (wrong key) - confirm the recipient is on the right vault
- transferring while a sign is in flight: race condition; the sign may complete before or after re-encryption, behavior depends on protocol ordering. avoid concurrent transfer + sign on the same dWallet

## the digest / hints flow

\`parseTransferTxDigest({ digest })\` is a chromatika utility that:
1. fetches the Sui transaction at \`digest\`
2. inspects events for the re-encryption event
3. extracts \`encryptedShareId\` (the on-chain id of the new encrypted share for the recipient) and \`senderEncryptionKeyAddress\`
4. returns these as hints

the recipient could in principle find these by walking on-chain state, but the digest+parse approach is faster and the user already has the digest from the sender's tx.

today only Sui digests are supported (Solana parse-from-digest is not implemented).

## library

- \`@ika.xyz/sdk\` \`IkaTransaction.requestReEncryptUserShareFor\`, \`acceptEncryptedUserShare\`, \`registerEncryptionKey\`
- internal: \`wallet-extension/src/background/ika/dwallet-lifecycle.ts\` for accept orchestration
- internal: \`wallet-extension/src/background/ika/transfer.ts\` (or similar) for transfer orchestration
- \`UserShareEncryptionKeys.decryptUserSecretKeyShare\` for recipient-side decrypt + validate
`,gb="# ika `UserShareEncryptionKeys` seed derivation overview\n\nevery chromatika dWallet has a per-curve `UserShareEncryptionKeys` (USK) - the local half of the ika 2PC-MPC threshold key. the USK is derived from a **32-byte root seed**. how the seed gets produced depends on the credential type the vault uses. this doc maps the matrix; the per-credential docs go deep on each path.\n\n## the four root-seed factories\n\nall four use `keccak256(preimage_bytes || encryption_key_index_le_4bytes)` (see [keccak256-uses.md](/library/tech/keccak256-uses)) - they only differ in **what `preimage_bytes` is**:\n\n| factory | preimage_bytes | preimage length |\n|---------|----------------|-----------------|\n| `ikaRootSeedFromFeeKeypair(suiKp)` | `SuiKeyPair.to_bytes()` = `[scheme_flag(1) \\|\\| secret(32)]` for ed25519 | 33 bytes |\n| `ikaRootSeedFromSolanaKeypair(solKp)` | canonical 64-byte `Keypair.secretKey` (`[seed(32) \\|\\| pubkey(32)]`) | 64 bytes |\n| `ikaRootSeedFromMwaSignature(sig)` | raw 64-byte ed25519 signature over `IKA_USK_DERIVATION_MESSAGE` | 64 bytes |\n| `ikaRootSeedFromPasskeyPRF(prf)` | 32-byte WebAuthn PRF / hmac-secret output | 32 bytes |\n| `ikaRootSeedFromRecoveryWords(words)` | 64-byte BIP39 PBKDF2 seed (`mnemonicToSeedSync(words, \"\")`) | 64 bytes |\n\n`encryption_key_index_le_4bytes` is the 4-byte LE encoding of a `u32` index. almost always `0` for the primary user-share key. an index > 0 would be how you produce a sibling user-share key from the same credential without collision (not exposed in the API today).\n\nafter derivation:\n```\nseed_32 = keccak256(preimage || index_le)\nikaUskSecpk1 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)\nikaUskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)\nseed_32.fill(0)   // zero the buffer to limit memory lifetime\n```\n\nboth curves are derived from the same seed - meaning a single chromatika vault can have one SECP256K1 dWallet (for EVM, BTC) and one ED25519 dWallet (for Sui, Solana, Aptos), and they share an \"identity\" rooted in this same 32-byte seed.\n\n## per-credential matrix\n\n| credential | base chain | factory used | how preimage gets produced |\n|------------|------------|--------------|---------------------------|\n| BIP39 mnemonic | Sui | `ikaRootSeedFromFeeKeypair` | mnemonic → SLIP10 ed25519 derive `m/44'/784'/0'/0'/0'` → Sui ed25519 keypair |\n| BIP39 mnemonic | Solana | `ikaRootSeedFromSolanaKeypair` | mnemonic → BIP39 seed → SLIP10 derive `m/44'/501'/0'/0'` → Solana keypair |\n| imported `suiprivkey…` bech32 | Sui | `ikaRootSeedFromFeeKeypair` | bech32 decode → Ed25519Keypair.fromSecretKey |\n| imported 64-byte solana b64 | Solana | `ikaRootSeedFromSolanaKeypair` | b64 decode → Keypair.fromSecretKey |\n| Passkey (WebAuthn PRF) | Sui only | `ikaRootSeedFromPasskeyPRF` | webauthn assertion with `prf.eval.first` salt → 32-byte HMAC-secret output |\n| WAAP (deterministic) | Sui only | `ikaRootSeedFromMwaSignature` | WAAP signs `IKA_USK_DERIVATION_MESSAGE` → 64-byte sig |\n| WAAP (non-deterministic fallback) | Sui only | `ikaRootSeedFromRecoveryWords` | user provides BIP39 phrase → mnemonicToSeedSync |\n| Lazor | Solana only | `ikaRootSeedFromRecoveryWords` | required BIP39 phrase → mnemonicToSeedSync |\n| Seeker MWA (local + remote) | Solana | `ikaRootSeedFromMwaSignature` | wallet signs `IKA_USK_DERIVATION_MESSAGE` → 64-byte sig |\n| WalletConnect | Solana | `ikaRootSeedFromMwaSignature` | wallet signs `IKA_USK_DERIVATION_MESSAGE` → 64-byte sig |\n| Ledger key-copy | Sui or Solana | (none - copy from source vault) | `ikaShareKeysB64` copied verbatim from another vault's record |\n\n## the `IKA_USK_DERIVATION_MESSAGE` constant\n\n```\nIKA_USK_DERIVATION_MESSAGE = \"ika.chromatika.user-share-encryption-key.v1\"\n```\n\nUTF-8 bytes. defined in `wallet-extension/src/background/keyring/hd.ts`. used by all signature-based seed derivation paths (Seeker, WalletConnect, WAAP-deterministic). signing this fixed string produces a deterministic 64-byte ed25519 signature per RFC 8032 - we keccak256 that with the index to get the seed.\n\n**this is a different message than vault-unlock challenges**. unlock challenges include vault id + envelope id; this is a constant. the same wallet on the same key produces the same signature here, regardless of which chromatika vault is asking.\n\n## why curves share a seed\n\nika's `UserShareEncryptionKeys.fromRootSeedKey(seed, curve)` performs internal domain separation per curve. the same seed produces different USK material for SECP256K1 and ED25519 - they're not the same key, just rooted in the same entropy. a single chromatika vault can drive both curves from one user identity without collision.\n\nif you need **different identities** per curve (e.g. one for \"personal\" SECP256K1 and one for \"work\" ED25519), that's two different vaults, not two indices on the same vault.\n\n## where the seed lives in memory\n\n- derived in `buildIkaShareKeys(makeSeed, stored)` in the keyring helper\n- `seed = makeSeed()` returns the 32 bytes\n- `UserShareEncryptionKeys.fromRootSeedKey(seed, curve)` is called once per curve\n- `seed.fill(0)` zeros the buffer\n- the seed is **never stored** - it's re-derived on every unlock from the vault's credential material (or skipped if both curves are already in `record.ikaShareKeysB64`)\n- only the resulting **`UserShareEncryptionKeys` serialized bytes** are persisted (encrypted in the vault payload)\n\nthis means: lose the credential, lose the ability to re-derive the seed. but if `ikaShareKeysB64` is already populated (and the user knows the password to decrypt the vault), they can still load the USK without re-deriving from the seed source.\n\n## library\n\n- `wallet-extension/src/background/keyring/hd.ts` - the canonical home of `ikaRootSeed*` helpers, `makeSeedFrom*` factory functions, `makeSeedForHdVault`, and `buildIkaShareKeys`\n- `@noble/hashes/sha3` for `keccak_256`\n- `@ika.xyz/sdk` `UserShareEncryptionKeys.fromRootSeedKey`\n- `@scure/bip39`, `@noble/ed25519`, `@solana/web3.js` `Keypair`, `@mysten/sui` `Ed25519Keypair` for keypair production\n\n## per-credential deep dives\n\n- [ika-seed-sui-mnemonic.md](/library/tech/ika-seed-sui-mnemonic)\n- [ika-seed-sui-private-key.md](/library/tech/ika-seed-sui-private-key)\n- [ika-seed-sui-passkey.md](/library/tech/ika-seed-sui-passkey)\n- [ika-seed-sui-waap.md](/library/tech/ika-seed-sui-waap)\n- [ika-seed-sui-ledger-keycopy.md](/library/tech/ika-seed-sui-ledger-keycopy)\n- [ika-seed-solana-mnemonic.md](/library/tech/ika-seed-solana-mnemonic)\n- [ika-seed-solana-private-key.md](/library/tech/ika-seed-solana-private-key)\n- [ika-seed-solana-mwa-walletconnect.md](/library/tech/ika-seed-solana-mwa-walletconnect)\n- [ika-seed-solana-ledger-keycopy.md](/library/tech/ika-seed-solana-ledger-keycopy)\n- [ika-seed-solana-lazor.md](/library/tech/ika-seed-solana-lazor)\n",fb=`# ika seed: Solana base + Lazor

vault \`seedSource: 'lazor-recovery-words'\` (effectively \`'recovery-words'\`), base chain \`'solana'\`. Lazor is a Solana smart-wallet portal (\`@lazorkit/wallet\`) that uses WebAuthn at the portal iframe layer plus a Solana anchor program. because the WebAuthn ceremony runs **inside the Lazor portal iframe at \`portal.lazor.sh\`**, chromatika can't access the raw PRF / HMAC-secret output. so Lazor vaults **require a 24-word BIP39 phrase at create time**, and that phrase is the ika seed source.

requires \`VITE_SOLANA_IKA_BASE=true\`. pre-alpha disclaimer applies.

## why recovery words and not PRF

a passkey-PRF approach would let us derive the ika seed deterministically from the WebAuthn assertion (see [ika-seed-sui-passkey.md](/library/tech/ika-seed-sui-passkey)). but PRF / HMAC-secret outputs are returned by **\`navigator.credentials.get\` in the calling document**. when the WebAuthn ceremony runs inside an iframe (\`portal.lazor.sh\`), only the iframe's parent (Lazor's portal page) sees the PRF result - chromatika does not.

so chromatika takes the BIP39 phrase route: deterministic from typed words, no need to reach into the iframe.

## inputs

- \`recoveryWords\`: 24 BIP39 English words (12 not allowed for Lazor; agent's exploration found \`recoveryWords\` is enforced at 24)
- \`lazorSmartWalletPubkeyB58\`: the Solana smart-wallet PDA address Lazor returns
- \`lazorCredentialIdB64\`, \`lazorPasskeyPubkeyB64\`: passkey artifacts from the portal
- \`lazorProgramId\`, \`lazorNetwork\` (mainnet | devnet), \`lazorPortalUrl\`
- \`encryption_key_index\`: always \`0\`

## step-by-step

\`\`\`
1. user picks Lazor flow, completes the portal-side passkey registration at portal.lazor.sh
   - returns lazorSmartWalletPubkeyB58, lazorCredentialIdB64, etc.

2. user supplies 24-word BIP39 phrase (chromatika UI prompts)
3. validateWords(words) → throws if invalid

4. derive in-extension fee-payer Solana keypair from the same words
   path = "m/44'/501'/0'/0'"
   feeKp = deriveSolanaKeypair(words, accountIndex=0)
   - bip39_seed = mnemonicToSeedSync(words, "")     // 64 bytes
   - SLIP10 ed25519 derive at path → 32-byte ed25519 seed
   - Keypair.fromSeed → 64-byte canonical secretKey

5. ika seed derivation
   bip39_seed_64 = mnemonicToSeedSync(words, "")    // 64 bytes (same as step 4 inner)
   indexLe = u32_le(0)
   preimage = bip39_seed_64 || indexLe              // 68 bytes
   seed_32 = keccak256(preimage)
   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)
   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)
   seed_32.fill(0)
\`\`\`

step 4 and step 5 use **the same BIP39 seed**:
- step 4: feeds SLIP10 derivation to produce a Solana keypair (for paying ika gRPC fees and as a known Solana address for funding)
- step 5: feeds keccak directly (no SLIP10) to produce the ika user-share encryption keys seed

different cryptographic outputs from the same source, fully deterministic.

## what gets stored

- \`record.lazorSmartWalletPubkeyB58\`: the user-facing Solana address (Lazor PDA)
- \`record.lazorCredentialIdB64\`, \`lazorPasskeyPubkeyB64\`: portal-returned passkey artifacts
- \`record.lazorProgramId\`, \`lazorNetwork\`, \`lazorPortalUrl\`: network metadata
- \`record.lazorWalletDevicePubkeyB58\`: optional device PDA (Lazor client can resolve)
- \`record.lazorIkaFeePayerSolSecretKeyB64\`: the Solana fee-payer keypair derived from words (b64-encoded canonical 64-byte form)
- \`record.recoveryWordsEncryptedB64\`: the **24 BIP39 words plaintext inside the encrypted vault payload** - so unlock can rebuild the fee-payer without prompting again
- \`record.ikaShareKeysB64\`: USK bytes for both curves
- multi-envelope:
  - \`PasswordEnvelope\` (always - chromatika requires a password for Lazor)
  - \`RecoveryWordsEnvelope\` (the 24 words, also wrap the master key)

both envelopes unwrap the same master key. user can unlock with the password OR the 24 words.

## the "phrase encrypted in vault" twist

unlike WAAP-recovery or passkey-recovery (where the phrase only matters as an envelope branch), Lazor stores the phrase **inside the encrypted vault payload**. why?

because the **fee-payer keypair** has to be available on every Solana ika operation, and re-prompting for the 24 words on every fee-paying op would be terrible UX. so chromatika decrypts the vault (which requires either the password or the words via envelope), reads \`record.recoveryWordsEncryptedB64\`, derives the fee-payer keypair, signs ika gRPC \`approve_message\` calls.

once the vault is unlocked, the words sit in memory inside the session state. on lock, that memory is dropped.

## restore on a new device

\`\`\`
1. user types 24 BIP39 words on new install
2. unlocks via RecoveryWordsEnvelope (or via password if both envelopes set up)
3. ika seed = keccak256(bip39_seed || index) - identical
4. fee-payer keypair = SLIP10 derive at m/44'/501'/0'/0' - identical
5. user re-pairs Lazor at the portal
   - Lazor's smart-wallet PDA is determined by lazorPasskeyPubkey + program; if user uses the same passkey at the portal, they get the same PDA
   - if the passkey was synced (iCloud / Google), the PDA reattaches automatically
6. discoverDWallets reattaches dWallets owned by the recovered identity
\`\`\`

deterministic per BIP39 + the Lazor portal returning the same smart-wallet PDA on the same passkey.

## what doesn't work

- **12-word phrases**: Lazor enforces 24 words. 128 bits of entropy isn't enough margin given the dual role (envelope unlock + fee-payer + ika seed)
- **passing through the Lazor portal's PRF output**: cross-frame WebAuthn extension result access is blocked by browser security model. chromatika can ask the portal for the PDA + credential id but not the PRF secret
- **mainnet vs devnet mismatch**: if you create a Lazor vault on devnet but try to use it on mainnet, the Lazor smart-wallet PDA won't exist on mainnet (programs are deployed per network). re-create the vault for the target network

## library

- \`@lazorkit/wallet\` for the portal flow
- \`@scure/bip39\` for \`validateWords\`, \`mnemonicToSeedSync\`
- internal \`slip10Ed25519DerivePath\` + \`@solana/web3.js\` \`Keypair.fromSeed\` for fee-payer derivation
- \`@noble/hashes/sha3\` \`keccak_256\`
- \`@ika.xyz/sdk\` \`UserShareEncryptionKeys.fromRootSeedKey\`
- internal: \`makeSeedFromRecoveryWords\`, \`ikaRootSeedFromRecoveryWords\` from \`keyring/hd.ts\`
`,yb=`# ika seed: Solana base + Ledger (key-copy from source vault)

a Ledger Solana hardware vault on Solana base does **not** derive its own ika seed. like the Sui-Ledger path (see [ika-seed-sui-ledger-keycopy.md](/library/tech/ika-seed-sui-ledger-keycopy)), it copies an existing vault's \`ikaShareKeysB64\`. the Ledger device handles fee-payer-style signing for Solana operations; the ika identity is borrowed.

## why key-copy and not signature-derivation

unlike Seeker / WalletConnect (which use the wallet's deterministic signature over \`IKA_USK_DERIVATION_MESSAGE\` as the ika seed source), Ledger Solana:

- exposes a sign-message API but routing it through chromatika's hardware-sign popup at vault-create time hasn't been implemented today
- is typically added as an additional account on top of an existing chromatika identity, not as a primary identity root

so today, Ledger Solana = **secondary vault** that uses an existing primary identity for ika and adds Ledger fee signing for Solana ops.

## inputs

- \`hardwareAccountId\`: the paired Ledger Solana account
- \`ikaShareKeysSourceVaultId\`: existing vault id whose USK we copy
- the Ledger account's Solana address (base58)

## step-by-step

\`\`\`
1. user has an unlocked source vault with both curves populated
2. user pairs Ledger Solana account via WebHID
3. submit addVaultHardware with:
   - vendor: 'ledger', chain: 'solana'
   - hardwareAccountId
   - ikaShareKeysSourceVaultId: source vault id
   - no solanaSecretKeyB64, no ikaUskSignatureB64

4. chromatika reads source vault's ikaShareKeysB64:
   - newRecord.ikaShareKeysB64 = { ...sourceVault.ikaShareKeysB64 }

5. chromatika persists fee-payer metadata:
   - newRecord.ledgerFeePayerSolPubkeyB58 = hw.address

6. on subsequent fee-paying ops:
   - dispatch via enqueueHardwareSign({ vendor: 'ledger', chain: 'solana', ... })
   - opens hardware-sign popup → WebHID → Ledger Solana app → user confirms → signature returns
\`\`\`

## what gets stored

- \`record.ikaShareKeysB64\`: copied from source vault (USK bytes)
- \`record.ledgerFeePayerSolPubkeyB58\`: the Ledger device's Solana address
- \`record.hardwareAccountId\`, \`hardwareVendor: 'ledger'\`, \`hardwareChain: 'solana'\`
- \`record.solanaSecretKeyB64\`: not present (no software keypair)
- \`record.ikaGrpcFeePayerSolSecretKeyB64\`: not present (Ledger pays fees directly via hardware sign)
- \`record.mnemonic\`: not present

## the design tradeoff (same as Sui-Ledger key-copy)

- **pro**: Ledger user gets device signing for Solana ops
- **pro**: same dWallet identity across software + Ledger vaults
- **con**: not hardware-rooted - the source vault's credential is the security floor for ika identity
- **con**: source vault credential compromise = identity compromise, even if Ledger is the active fee signer

contrast Seeker / WalletConnect Solana vaults where the ika seed is the **wallet's own signature** - the dWallet identity is fully hardware-rooted there.

## why not Seeker-style signature derivation for Ledger

a hardware-only Ledger Solana vault (ika seed derived from a Ledger-signed message) is achievable in principle:
- Ledger Solana app supports \`solana_signMessage\` or arbitrary-bytes signing
- a fixed message like \`IKA_USK_DERIVATION_MESSAGE\` could be signed at pairing
- \`keccak256(signature || index)\` would be the seed

what's missing today:
- the chromatika UX flow that pops the Ledger sign popup at vault-create time before any keys are committed
- testing across Ledger firmware versions (sign-message availability varies)
- handling the case where Ledger requires display approval for arbitrary bytes (the user has to physically confirm a fixed string they don't recognize)

tracked future. the key-copy path covers the practical use case for now.

## restore on a new device

\`\`\`
1. user re-onboards their source vault on the new install (same mnemonic / same passkey / etc.)
2. user pairs the same Ledger Solana account
3. addVaultHardware with the source vault id
4. ikaShareKeysB64 copied (deterministically rebuilt by source vault)
5. discoverDWallets reattaches dWallets
\`\`\`

requires both the source credential **and** the Ledger device. losing either prevents the Ledger vault from rebuilding (the source vault still works on its own).

## library

- \`@ledgerhq/hw-app-solana\` for the Ledger Solana app driver
- \`@ledgerhq/hw-transport-webhid\` for USB HID
- internal: \`enqueueHardwareSign\` for popup dispatch
- internal: \`keyring/hd.ts\` doesn't run a seed function for this path - keys come from \`record.ikaShareKeysB64\` via \`buildIkaShareKeys(makeSeed=null, stored)\`
`,wb=`# ika seed: Solana base + BIP39 mnemonic

vault \`seedSource: 'mnemonic'\`, base chain \`'solana'\`. the Solana-base equivalent of [ika-seed-sui-mnemonic.md](/library/tech/ika-seed-sui-mnemonic). uses \`ikaRootSeedFromSolanaKeypair\` instead of \`ikaRootSeedFromFeeKeypair\` because Solana keypairs have a different canonical byte layout than Sui keypairs.

requires \`VITE_SOLANA_IKA_BASE=true\` in the build (Solana ika base is **pre-alpha** with a single mock signer; never trust for real value).

## inputs

- \`mnemonic\`: 12 or 24 BIP39 English words
- \`accountIndex\`: always \`0\` for the fee-payer
- \`encryption_key_index\`: always \`0\`

## step-by-step

\`\`\`
1. validate mnemonic
   validateWords(mnemonic)

2. derive Solana ed25519 keypair at fee-payer path
   path = "m/44'/501'/0'/0'"   // 4 segments, all hardened, per Solana convention
   bip39_seed = mnemonicToSeedSync(mnemonic, "")   // 64-byte seed, hex-encoded
   childKey = slip10Ed25519DerivePath(path, bip39_seed_hex)   // SLIP10 ed25519 walk
   ed25519_seed_32 = childKey.key   // first 32 bytes of the derived node
   solKp = solana.Keypair.fromSeed(ed25519_seed_32)
   // Keypair.fromSeed produces canonical 64-byte secretKey:
   //   secretKey64 = [seed(32) || pubkey(32)]
   //   pubkey is computed as A = a*B from the clamped scalar derived from the seed

3. assemble keccak preimage
   indexLe = u32_le(0)
   preimage = solKp.secretKey || indexLe   // 64 + 4 = 68 bytes

4. hash
   seed_32 = keccak256(preimage)

5. derive both curves
   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)
   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)

6. zero seed_32
\`\`\`

## why the canonical 64-byte secretKey

Solana's \`Keypair.secretKey\` is **always** 64 bytes by convention - the ed25519 seed (32) concatenated with the public key (32). this is the same shape Phantom and \`solana-keygen\` JSON-export use:
\`\`\`
solana-keygen new -o keypair.json
cat keypair.json
# => "[12, 45, 233, ..., 12, 89, 76]"   // 64 numbers, the bytes
\`\`\`

chromatika uses this exact 64-byte form as the keccak preimage so:
- importing a Phantom export produces the same secretKey64
- importing the same mnemonic into Phantom and into chromatika produces the same secretKey64
- → same ika seed across tools

## what gets stored

- \`record.mnemonic\`: 12 / 24 words, plaintext inside the encrypted vault payload
- \`record.solanaSecretKeyB64\`: not used for mnemonic vaults (mnemonic regenerates it)
- \`record.ikaShareKeysB64\`: serialized USK bytes for both curves
- \`record.dwalletMeta\` entries persist \`dwalletAttestationBytesB64\` + \`dwalletPublicKeyB64\` per Solana dWallet (a schema break vs \`@ika.xyz/pre-alpha-solana-client@0.1.0\` dev installs - re-run DKG after upgrade)

## the fee-payer keypair (Solana base specific)

on Solana base, the same mnemonic-derived Solana keypair is **also**:
- the in-extension fee-payer for ika gRPC \`approve_message\` calls (\`ikaGrpcFeePayerSolSecretKeyB64\` is set to \`btoa(String.fromCharCode(...feeKp.secretKey))\`)
- the canonical Solana wallet address for the dWallet Vault

so a mnemonic-rooted Solana-base vault has one keypair doing three jobs:
1. deriving the ika user-share encryption keys (via keccak256)
2. paying ika gRPC fees (via direct ed25519 sign)
3. signing native Solana transactions (via the dWallet flow if both curves are linked, or via \`sendSolanaNative\` if you opt into the HD direct path)

## restore on a new device

\`\`\`
1. user types mnemonic on new install with VITE_SOLANA_IKA_BASE=true
2. importVault with baseChain='solana'
3. on first unlock with empty ikaShareKeysB64:
   - deriveSolanaKeypair(mnemonic, 0)
   - keccak preimage = solKp.secretKey || index_le
   - seed_32 = keccak256(preimage)
   - both curves derived
4. discoverDWallets walks the Solana ika program to find dWallets owned by this identity
\`\`\`

deterministic per:
- BIP39 → seed (PBKDF2-HMAC-SHA512, deterministic)
- SLIP10 ed25519 derive at \`m/44'/501'/0'/0'\` (deterministic)
- \`Keypair.fromSeed\` → canonical 64-byte secretKey (deterministic, ed25519 RFC 8032)
- keccak256 (deterministic)

## what doesn't work

- **same mnemonic on Sui base**: produces a totally different identity. Sui-base uses \`[scheme_flag(1) || secret(32)]\` (33 bytes) as preimage, Solana-base uses \`[seed(32) || pubkey(32)]\` (64 bytes). different preimage → different keccak → different seed → different dWallet
- **importing a Phantom mnemonic that used a non-default derivation path**: chromatika hardcodes \`m/44'/501'/0'/0'\`. some old Phantom versions used different paths; if your Phantom address doesn't match what chromatika derives, the paths differ. there's no UI to override the path today
- **Solana ika base on production**: per CLAUDE.md disclaimer, all Solana ika signatures come from a single mock signer until ika ships production MPC. **do not** sign mainnet value with a Solana-base vault

## library

- \`@scure/bip39\` for \`mnemonicToSeedSync\`
- internal \`slip10Ed25519DerivePath\` helper for the SLIP10 ed25519 walk
- \`@solana/web3.js\` \`Keypair.fromSeed\`
- \`@noble/hashes/sha3\` \`keccak_256\`
- \`@ika.xyz/sdk\` \`UserShareEncryptionKeys.fromRootSeedKey\`
- internal: \`wallet-extension/src/background/keyring/hd.ts\` \`makeSeedFromSolanaKeypair\`, \`ikaRootSeedFromSolanaKeypair\`, \`deriveSolanaKeypair\`
`,vb="# ika seed: Solana base + MWA / WalletConnect signature\n\nvault `seedSource: 'mwa-signature'` or `'walletconnect-signature'`, base chain `'solana'`. the wallet (Seeker built-in, Phantom Android, Solflare Android, Jupiter, or any MWA-2.0-compliant wallet via WalletConnect) signs `IKA_USK_DERIVATION_MESSAGE` once at pairing time. that 64-byte ed25519 signature is keccak-hashed into the ika seed. RFC 8032 determinism guarantees the same wallet on a different device produces the same signature → same dWallet → restore-on-new-device works without any seed phrase.\n\nthis is the **canonical hardware-rooted Solana base path**. the wallet's secret bytes never leave the device.\n\n## the IKA USK derivation message\n\n```\nIKA_USK_DERIVATION_MESSAGE = \"ika.chromatika.user-share-encryption-key.v1\"\n```\n\nUTF-8 bytes. defined in `wallet-extension/src/background/keyring/hd.ts`. fixed across all chromatika installs and wallets. signing this exact string produces a deterministic ed25519 signature for any RFC 8032 conforming implementation.\n\n## inputs\n\n- `signature`: 64-byte ed25519 signature over `IKA_USK_DERIVATION_MESSAGE` produced by the paired wallet\n- `encryption_key_index`: always `0` for the user-share key\n- (separately) `feePayerKeypairIndex`: `1` for the in-extension fee-payer keypair derivation - using a different index avoids collision\n\n## step-by-step (ika user-share seed)\n\n```\n1. wallet pairs (MWA local intent / MWA remote QR / WalletConnect QR)\n   - returns: address (base58), ed25519PublicKey (b64), session metadata\n\n2. wallet signs the IKA_USK_DERIVATION_MESSAGE\n   - via MWA: transact() with sign-message intent\n   - via WC: enqueueHardwareSign({ vendor: 'walletconnect', kind: 'solanaTx' or 'solanaMessage', ... })\n   - returns 64-byte raw ed25519 signature\n\n3. assemble keccak preimage\n   indexLe = u32_le(0)\n   preimage = signature_64 || indexLe   // 68 bytes\n\n4. hash\n   seed_32 = keccak256(preimage)\n\n5. derive both curves\n   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)\n   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)\n\n6. zero seed_32\n```\n\n## step-by-step (in-extension fee-payer keypair)\n\nika on Solana needs an SOL-paying keypair for `approve_message` gRPC fees. since Seed Vault never reveals secret bytes, chromatika derives an **in-extension** Solana keypair from the same wallet signature, but at a different index to avoid collision:\n\n```\n1. preimage_fee = signature_64 || u32_le(1)   // index 1, not 0\n2. fee_seed_32 = keccak256(preimage_fee)\n3. feeKp = Keypair.fromSeed(fee_seed_32)\n4. feeKp.secretKey is a deterministic 64-byte canonical Solana keypair\n5. encode as b64 and persist in record.ikaGrpcFeePayerSolSecretKeyB64\n```\n\ndeterminism: same wallet signature + same index = same keccak output = same ed25519 seed → same fee-payer address. user can fund the fee-payer with ~0.1 devnet SOL once, and on a new install the same Seeker re-pairs, signs the same message, derives the same fee-payer address, and the SOL is right there waiting.\n\n## what gets stored\n\n- `record.hardwareAccountId`: the paired hardware account\n- `record.hardwareVendor`: `'mwa'` or `'walletconnect'`\n- `record.hardwareChain`: `'solana'`\n- `record.mwaTransport`: `'local'` (Android same-device) or `'remote'` (desktop ↔ phone QR)\n- `record.ikaUskSignatureB64`: the **encrypted** signature (inside a `WalletSignatureEnvelope` for unlock)\n- `record.ikaShareKeysB64`: USK bytes for both curves\n- `record.ikaGrpcFeePayerSolSecretKeyB64`: the deterministic fee-payer keypair (b64), if `feeMode === 'in_extension'`\n- `record.ledgerFeePayerSolPubkeyB58`: the **paired wallet's** Solana address (NOT the fee-payer; this is the phone wallet's address - misnamed historically)\n- `record.solanaSecretKeyB64`: not present (no software user-key import)\n- `record.mnemonic`: not present\n- multi-envelope: `WalletSignatureEnvelope` for the wallet-signature unlock branch (the same signature is used for both unlock and ika seed - see below for nuance)\n\n## unlock vs ika seed: same signature, two purposes\n\nthe wallet signs `IKA_USK_DERIVATION_MESSAGE` **once** at pairing. that signature is used for two distinct things:\n1. **ika user-share seed**: `keccak256(signature || index_0_le)` → 32-byte seed for `UserShareEncryptionKeys`\n2. **unlock envelope key (optional)**: `HKDF-SHA256(signature, info='chromatika wallet-signature envelope v1', salt=kdfSalt)` → envelope key wrapping the master key\n\nthe signature itself is **encrypted** in the wallet-signature envelope. on unlock, the user re-signs the envelope's challenge string (a separate message, NOT `IKA_USK_DERIVATION_MESSAGE`) to produce the envelope key, which decrypts the persisted `ikaUskSignatureB64` (the original `IKA_USK_DERIVATION_MESSAGE` signature). that decrypted signature is then keccak'd to re-derive the ika seed if needed.\n\nso:\n- **ika seed signature**: `signature_over(IKA_USK_DERIVATION_MESSAGE)` - signed once, persisted encrypted\n- **unlock signature**: `signature_over(envelope_challenge)` - signed every unlock\n\nmost of the time the seed is already cached as `ikaShareKeysB64` and re-derivation isn't needed. but when it is needed (first unlock on a new install), the encrypted ika seed signature is decrypted and re-keccak'd.\n\n## fee mode toggle: `in_extension` vs `seeker_direct`\n\n- `in_extension` (default): the derived fee-payer keypair signs ika gRPC `approve_message` calls. fast - one signature per ika op, no popup\n- `seeker_direct`: every ika gRPC call routes through the phone wallet for signing. 3-5 phone prompts per ika tx (DKG / presign / sign each have their own sub-calls). slower but no in-extension keypair to manage\n\n`setIkaFeeSettings({ vaultId, mode })` toggles this. the in-extension fee-payer keypair persists on the vault record either way; in `seeker_direct` mode it just isn't used. `drainAbandonedFeePayer` empties it back to the phone if you switched modes.\n\n## restore on a new device\n\n```\n1. on new install, pair the same wallet (same Seeker / same WC-paired phone)\n2. wallet signs IKA_USK_DERIVATION_MESSAGE - RFC 8032 determinism gives the same 64-byte signature\n3. ika seed = keccak256(signature || index_0_le) - identical to before\n4. fee-payer keypair = keccak256(signature || index_1_le) - identical to before\n5. discoverDWallets reattaches existing dWallets owned by this identity\n6. the previously funded fee-payer SOL is right there at the same address\n```\n\nno seed phrase, no key export. the wallet's secret stays on the phone forever.\n\n## what doesn't work\n\n- **changing the wallet**: a different wallet (e.g. moving from Phantom to Solflare, or a brand-new Seeker after factory reset) produces a different ed25519 key → different signature → different ika seed → different dWallet. no migration path; you'd need to send funds to the new identity over Solana\n- **the WAAP determinism probe rule**: chromatika probes WAAP wallets at pairing because some are non-deterministic. for MWA / WalletConnect Solana wallets the probe is not strictly needed - the standard implementations follow RFC 8032 - but defensive code paths still verify\n- **secp256k1 wallets**: this entire flow is ed25519-specific. a hypothetical secp256k1-based MWA wallet would need RFC 6979 deterministic ECDSA to plug into the same pattern; chromatika doesn't have that path today\n\n## library\n\n- `@solana-mobile/mobile-wallet-adapter-protocol-web3js` for MWA local + remote\n- `@walletconnect/sign-client` for WalletConnect v2\n- `@solana/web3.js` `Keypair.fromSeed` for the fee-payer keypair construction\n- `@noble/hashes/sha3` `keccak_256`\n- `@ika.xyz/sdk` `UserShareEncryptionKeys.fromRootSeedKey`\n- internal: `makeSeedFromMwaSignature`, `ikaRootSeedFromMwaSignature`, `solanaFeeKeypairFromWalletSignature` from `keyring/hd.ts`\n- internal: `mwa-remote.ts` for the websocket reflector flow\n\n## related deep-dives\n\n- [wallet-signature-envelope.md](/library/tech/wallet-signature-envelope) - the unlock-envelope side\n- [mwa-remote-qr-pairing.md](/library/tech/mwa-remote-qr-pairing) - the QR + reflector pairing protocol\n- [ika-fee-management.md](/library/user/ika-fee-management) - in-extension fee-payer top-up + drain flows\n",kb=`# ika seed: Solana base + imported private key

vault \`seedSource: 'private-key'\`, base chain \`'solana'\`. user supplies a 64-byte canonical Solana secret key as base64 (the same shape Phantom and \`solana-keygen\` JSON-export). chromatika reconstructs the keypair, derives the ika seed exactly like a mnemonic-based Solana vault would.

requires \`VITE_SOLANA_IKA_BASE=true\`. pre-alpha disclaimer applies (single mock signer, not production MPC).

## inputs

- \`solanaSecretKeyB64\`: base64 encoding of the 64-byte secretKey (\`[seed(32) || pubkey(32)]\`)
- \`encryption_key_index\`: always \`0\`

## step-by-step

\`\`\`
1. decode the b64 input
   secretKey64 = base64Decode(solanaSecretKeyB64)
   assert secretKey64.length === 64

2. construct the Solana keypair
   solKp = Keypair.fromSecretKey(secretKey64)
   // Keypair.fromSecretKey takes the 64-byte form directly; the constructor
   // does NOT verify that the embedded pubkey matches the seed. if you pass
   // a tampered keypair, signing will produce signatures verifiable under
   // the embedded pubkey but inconsistent with what other tools expect from
   // the seed alone

3. assemble keccak preimage (identical to mnemonic Solana from step 3 onward)
   indexLe = u32_le(0)
   preimage = solKp.secretKey || indexLe   // 68 bytes

4. hash
   seed_32 = keccak256(preimage)

5. derive both curves
   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)
   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)

6. zero seed_32
\`\`\`

## what gets stored

- \`record.solanaSecretKeyB64\`: the b64 string, plaintext inside the encrypted vault payload
- \`record.mnemonic\`: not present
- \`record.ikaShareKeysB64\`: USK bytes for both curves
- \`record.ikaGrpcFeePayerSolSecretKeyB64\`: the same secretKey b64 (the imported keypair pays ika gRPC fees)

## the Phantom-export equivalence

\`solana-keygen new -o my-keypair.json\` produces a JSON array of 64 bytes:
\`\`\`
[12, 45, 233, ..., 12, 89, 76]
\`\`\`

base64-encoding that array (treating it as 64 raw bytes) gives \`solanaSecretKeyB64\`. Phantom's "export private key" produces base58 of the same 64 bytes - chromatika accepts b64 because it's the chrome-platform-friendly format; users coming from Phantom can convert via:
\`\`\`js
import bs58 from 'bs58';
const b58 = phantomExportString;
const bytes = bs58.decode(b58);   // 64 bytes
const b64 = btoa(String.fromCharCode(...bytes));
\`\`\`

(or just use a wallet that already exports as b64).

## restore on a new device

\`\`\`
1. user pastes the same solanaSecretKeyB64 on a new install
2. importVaultFromPrivateKey with baseChain='solana'
3. keccak preimage = secretKey64 || index_le
4. seed = keccak256(preimage)
5. both curves derived
6. discoverDWallets reattaches existing dWallets
\`\`\`

deterministic, identical to the mnemonic-Solana path from step 3 onward.

## the Seeker / Phantom interop angle

a Solana-only onboarding flow uses this exact path: the user already has a Solana wallet (Phantom, Seeker bundled, Solflare) and exports the secret. they import into chromatika under Solana ika base and now drive ika operations with the same identity their phone wallet uses.

contrast: Seeker remote MWA pairing uses **the wallet's signature**, not the secret bytes - because Seed Vault never reveals secret bytes. so:
- Phantom export → import-private-key vault (this doc)
- Seeker pairing → MWA-signature vault (see [ika-seed-solana-mwa-walletconnect.md](/library/tech/ika-seed-solana-mwa-walletconnect))

both produce a Solana-base identity but via different paths.

## the security tradeoff

**important**: importing a private key gives chromatika the **secret bytes** in plaintext (within the encrypted vault payload). if an attacker compromises the unlocked vault, they can sign **any** transaction with that key, on Solana or via ika.

with Seeker remote MWA, the secret stays on the phone. attacker compromising the unlocked chromatika vault gets the **wallet-signature** (signed once, used as ika seed) but cannot produce new arbitrary signatures - the phone remains the gatekeeper.

so import-private-key is convenient but less secure than hardware-rooted paths. the user is making the tradeoff explicitly when they paste their secret.

## what doesn't work

- **secret keys from non-standard formats**: chromatika expects the canonical 64-byte form (\`[seed || pubkey]\`). some tools export only the 32-byte ed25519 seed (the "private key" without the embedded pubkey) - that's incompatible. user has to expand to 64 bytes via \`Keypair.fromSeed(seed32).secretKey\` before importing
- **ed25519 keypairs from non-Solana sources**: a Sui ed25519 keypair has a different canonical form (\`scheme_flag || secret\`, 33 bytes). pasting Sui privkey under Solana base will fail decoding or produce a wrong identity. import paths are flavor-locked
- **cross-base reuse**: same private key under Sui base = different ika seed (different preimage layout). the \`previewCrossChainReuseMnemonic\` flow is BIP39-only; private-key vaults don't get cross-chain preview because there's no canonical conversion between sui privkey format and solana secret-key format

## library

- \`@solana/web3.js\` \`Keypair.fromSecretKey\`
- \`@noble/hashes/sha3\` \`keccak_256\`
- \`@ika.xyz/sdk\` \`UserShareEncryptionKeys.fromRootSeedKey\`
- internal: \`makeSeedFromSolanaKeypair\`, \`ikaRootSeedFromSolanaKeypair\` from \`keyring/hd.ts\`
`,bb=`# ika seed: Sui base + Ledger (key-copy from source vault)

a Ledger Sui hardware vault on Sui base does **not** derive its own ika seed. instead it **copies** an existing vault's serialized \`ikaShareKeysB64\` (per-curve user-share encryption keys) into a fresh hardware-vault record. the Ledger device handles fee-payer signing for Sui PTBs; the ika identity is borrowed from the source vault.

## why key-copy and not signature-derivation

Ledger's Sui app produces ed25519 signatures over Sui transaction bytes (and personal messages, with limits per [LEDGER_SUI_LIMITS.md](/library/tech/ledger_sui_limits)). but unlike Seeker / MWA / WalletConnect:

- Ledger doesn't expose a "sign this fixed message off-chain to derive a seed" primitive in a way that's easy to plumb through chromatika's hardware-sign popup flow today
- Ledger users typically already have a software vault (mnemonic / passkey / WAAP) - the Ledger is added as a "use my existing identity, but route fee-payer signing through hardware"
- chromatika can't introduce a hardware-only Sui base vault without solving "where does the fee-payer keypair come from?" - on Solana base, the in-extension keypair pattern works because Solana ika uses gRPC + the deterministic wallet-signature seed; on Sui base, every PTB is signed by the fee-payer keypair, and Ledger is the natural fee signer

so today, Ledger Sui = **secondary vault** on top of an existing primary identity.

## inputs

- \`hardwareAccountId\`: the paired Ledger Sui account (from \`getHardwareAccounts\`)
- \`ikaShareKeysSourceVaultId\`: the existing vault id whose \`ikaShareKeysB64\` we copy
- the Ledger device's ed25519 public key (\`hw.ed25519PublicKeyB64\`) for fee-signing dispatch

## step-by-step (key-copy at vault creation)

\`\`\`
1. user has an unlocked source vault (mnemonic / passkey / WAAP / etc.) with both curves populated
2. user pairs Ledger Sui account via WebHID
3. submit addVaultHardware with:
   - vendor: 'ledger', chain: 'sui'
   - hardwareAccountId: the paired account
   - ikaShareKeysSourceVaultId: source vault id
   - no suiPrivateKeyBech32, no ikaUskSignatureB64, no ed25519 derivation message

4. chromatika reads source vault's ikaShareKeysB64:
   - sourceShareKeys = sourceVault.ikaShareKeysB64   // { SECP256K1, ED25519 }
   - newRecord.ikaShareKeysB64 = { ...sourceShareKeys }   // direct copy

5. chromatika persists fee-payer metadata:
   - newRecord.ledgerFeePayerEd25519PublicKeyB64 = hw.ed25519PublicKeyB64

6. on subsequent fee-paying ops (DKG, presign, sign, sendSuiNative):
   - dispatch via enqueueHardwareSign({
       vendor: 'ledger',
       chain: 'sui',
       derivationPath: hwAcct.derivationPath,
       requestKind: 'sui_tx',
       ...
     })
   - opens hardware-sign popup → WebHID → Ledger Sui app → user confirms on device → signature returns
\`\`\`

## what gets stored

- \`record.ikaShareKeysB64\`: copied from source vault (USK bytes)
- \`record.ledgerFeePayerEd25519PublicKeyB64\`: the Ledger device's pubkey
- \`record.hardwareAccountId\`: the hardware account id
- \`record.hardwareVendor: 'ledger'\`, \`hardwareChain: 'sui'\`
- \`record.mnemonic\`: not present (the source vault has the mnemonic)
- \`record.suiPrivateKeyBech32\`: not present (no software keypair)
- \`seedSource\`: arguably "key-copy" or carries the source vault's \`seedSource\` for diagnostic - check the actual code for the canonical value

## restore on a new device

\`\`\`
1. user re-onboards their source vault on the new install (re-type mnemonic / re-pair passkey / re-login WAAP)
   - this rebuilds the source vault's ikaShareKeysB64 deterministically
2. user pairs the same Ledger account on the new install
3. addVaultHardware with the same source vault id and Ledger account
4. ikaShareKeysB64 copied again - same bytes since source vault deterministically rebuilds them
5. dWallets reattach via discoverDWallets
\`\`\`

restoring requires **both** the source vault's credential **and** the same Ledger device. losing either one prevents the Ledger vault from rebuilding (though dWallets owned by the source identity remain accessible from the source vault directly).

## the design tradeoff

key-copy means:
- **pro**: Ledger user gets the security of "Sui PTBs are signed on the device" for fee-paying ops
- **pro**: same dWallet identity across software + hardware vaults (no migration)
- **con**: not a true "hardware-rooted" identity - the source vault's credential is still the security floor for the ika identity
- **con**: if the source vault's credential is compromised, attacker can sign ika protocol messages with the same identity from any vault, even if Ledger is the fee signer

contrast with Solana hardware-only vaults (Seeker / WalletConnect) where the **ika seed itself** comes from the wallet's deterministic signature, so the dWallet identity is hardware-rooted.

## future: hardware-only Sui base vault

a hardware-only Sui-base vault (no source vault dependency, ika seed derived from a Ledger-signed message) is conceivable but not implemented. would require:
- a stable Ledger-signable derivation message (analog to \`IKA_USK_DERIVATION_MESSAGE\` for Solana wallets)
- a way to ask the Ledger Sui app to sign that message at pairing time and persist the signature
- routing fee-payer signing for Sui PTBs through the Ledger as today

tracked future. not blocking any current user since the key-copy path covers the practical use case.

## library

- \`@ledgerhq/hw-app-sui\` for the Ledger Sui app driver
- \`@ledgerhq/hw-transport-webhid\` for the USB HID transport
- internal: \`enqueueHardwareSign\` for popup dispatch
- internal: \`keyring/hd.ts\` doesn't actually run a seed derivation function for this path - the keys are copied from the source vault's \`ikaShareKeysB64\`. \`buildIkaShareKeys(makeSeed=null, stored)\` deserializes from \`stored\` directly
`,Sb=`# ika seed: Sui base + BIP39 mnemonic

vault \`seedSource: 'mnemonic'\`, base chain \`'sui'\`. this is the canonical "fresh chromatika install with 12 or 24 words" path.

## inputs

- \`mnemonic\`: 12 or 24 BIP39 English words (validated checksum)
- \`accountIndex\`: always \`0\` for the fee-payer (and thus the ika seed source)
- \`encryption_key_index\`: always \`0\` for the primary user-share key

## step-by-step

\`\`\`
1. validate mnemonic
   validateWords(mnemonic) → throws if invalid

2. derive Sui ed25519 keypair at fee-payer path
   path = "m/44'/784'/0'/0'/0'"
   suiKp = Ed25519Keypair.deriveKeypair(mnemonic, path)
   // Ed25519Keypair.deriveKeypair internally:
   //   - mnemonicToSeedSync(mnemonic, "")              // PBKDF2-HMAC-SHA512 2048 rounds → 64-byte seed
   //   - SLIP10-ed25519 derive at path                 // hardened-only steps, HMAC-SHA512
   //   - returns Ed25519Keypair with secret + pubkey

3. extract canonical Sui keypair bytes
   suiKpBytes = decodeSuiPrivateKey(suiKp.getSecretKey())
   // decodeSuiPrivateKey decodes the bech32 'suiprivkey…' form
   // returns { scheme: 'ED25519', secretKey: Uint8Array(32) }
   suiKpBytesPreimage = [scheme_flag(0x00) || secretKey(32)]   // 33 bytes total

4. assemble the keccak preimage
   indexLe = u32_le(0)   // [0x00, 0x00, 0x00, 0x00]
   preimage = [scheme_flag(1) || secretKey(32) || indexLe(4)]   // 37 bytes

5. hash
   seed_32 = keccak256(preimage)

6. derive both curves
   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)
   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)

7. zero
   seed_32.fill(0)
\`\`\`

## what gets stored

- \`record.mnemonic\`: the 12 / 24 words, **plaintext inside the encrypted vault payload**
- \`record.suiPrivateKeyBech32\`: not used for mnemonic vaults (the mnemonic regenerates it on demand)
- \`record.ikaShareKeysB64\`: the serialized USK bytes for both curves

on subsequent unlocks, the wallet can either:
- **re-derive** the USK from \`record.mnemonic\` → \`Ed25519Keypair.deriveKeypair(...)\` → \`ikaRootSeedFromFeeKeypair(...)\` (slow-ish, but reproducible)
- **deserialize** from \`record.ikaShareKeysB64\` directly (faster, doesn't touch the mnemonic)

\`buildIkaShareKeys(makeSeed, stored)\` picks: if \`stored\` has both curves, deserialize; else, call \`makeSeed()\` (which here is \`makeSeedFromSuiKeypair(suiKp)\`) and run \`fromRootSeedKey\` on each missing curve.

## fee-payer keypair coincidence

the same Sui keypair derived for the ika seed (\`m/44'/784'/0'/0'/0'\`, account 0) is **also** the fee-payer keypair that signs:
- \`sendSuiNative\` HD transfers
- ika DKG / presign / sign PTBs (via \`IkaTransaction\` on Sui base)
- any Sui-side gas funding

this is intentional - one keypair handles both "ika identity root" and "Sui gas funder" roles for mnemonic vaults. for hardware-vault flows where the keypair lives on the device and not in the extension, fee-payer signing dispatches to the device.

## restore on a new device

\`\`\`
1. user types mnemonic on new chromatika install
2. importVault → mnemonic stored in fresh vault payload
3. on first unlock, since record.ikaShareKeysB64 is empty for this fresh record:
   - makeSeed = makeSeedFromSuiKeypair(deriveSuiKeypair(mnemonic))
   - seed = makeSeed()
   - both curves derived
4. discoverDWallets(curve) walks Sui chain looking for dWallets owned by the recovered USK addresses
5. dWallets that exist on-chain reattach to the new install
\`\`\`

deterministic because:
- BIP39 → BIP39 seed via PBKDF2 (deterministic)
- BIP39 seed → SLIP10 derivation (deterministic)
- SLIP10 → ed25519 keypair (deterministic)
- keccak preimage = same bytes on any device (deterministic)
- keccak256 = deterministic
- USK derivation (deterministic)

→ same mnemonic + same path + same index = same USK = same dWallet.

## what doesn't work

- **swapping base chain**: same mnemonic on Solana base does **not** produce the same dWallet identity. Solana base uses a different keypair derivation path (\`m/44'/501'/0'/0'\`), a different keypair format (Solana's 64-byte secretKey vs Sui's 33-byte bech32-decoded form), and therefore a different keccak preimage. cross-chain mnemonic reuse is supported in the UI for convenience (one phrase, two vaults) but they're **two different identities**.
- **changing word count after creation**: the seed is path-dependent on the mnemonic - changing the mnemonic = different vault. you can \`addVault\` to keep both.

## library

- \`@scure/bip39\` for mnemonic + \`mnemonicToSeedSync\`
- \`@mysten/sui\` \`Ed25519Keypair.deriveKeypair\`, \`decodeSuiPrivateKey\`
- \`@noble/hashes/sha3\` \`keccak_256\`
- \`@ika.xyz/sdk\` \`UserShareEncryptionKeys.fromRootSeedKey\`
- internal: \`wallet-extension/src/background/keyring/hd.ts\` \`makeSeedFromSuiKeypair\`, \`ikaRootSeedFromFeeKeypair\`, \`makeSeedForHdVault\`, \`deriveSuiKeypair\`
`,xb=`# ika seed: Sui base + passkey (WebAuthn PRF)

vault \`seedSource: 'passkey-prf'\`, base chain \`'sui'\`. uses the WebAuthn PRF / HMAC-secret extension to produce a deterministic 32-byte secret on every assertion. that secret feeds **both** the unlock-envelope flow (see [passkey-prf-envelope.md](/library/tech/passkey-prf-envelope)) **and** the ika seed derivation - one PRF output, two purposes.

today this is a **Sui-only** path. Solana passkey vaults are not exposed (Lazor is a portal-mediated Solana passkey but uses recovery words for the seed, not PRF directly - see [ika-seed-solana-lazor.md](/library/tech/ika-seed-solana-lazor)).

## inputs

- \`prfSecret\`: 32-byte output from \`webauthn.assertion.extensions.prf.results.first\` (see [webauthn-prf-hmac-secret.md](/library/tech/webauthn-prf-hmac-secret))
- \`prfSalt\`: the per-vault 32-byte salt that was passed in \`prf.eval.first\` at register time and gets re-supplied on every assertion
- \`encryption_key_index\`: always \`0\`

note: the \`prfSecret\` is 32 bytes, **not** a sui ed25519 keypair. so the keccak preimage is **not** the \`[scheme_flag || secret || index]\` form used by mnemonic / private-key paths. it's a separate factory: \`ikaRootSeedFromPasskeyPRF\`.

## step-by-step

\`\`\`
1. webauthn assertion
   navigator.credentials.get({
     publicKey: {
       challenge: random32,
       rpId: envelope.rpId,
       allowCredentials: [{ id: decode(envelope.credentialIdB64Url), type: 'public-key' }],
       extensions: { prf: { eval: { first: envelope.prfSaltB64_decoded } } }
     }
   })

2. extract PRF result
   prfSecret = response.clientExtensionResults.prf.results.first   // 32 bytes

3. assemble keccak preimage
   indexLe = u32_le(0)
   preimage = prfSecret || indexLe   // 36 bytes total

4. hash
   seed_32 = keccak256(preimage)

5. derive both curves
   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)
   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)

6. zero seed_32 and prfSecret
\`\`\`

## what gets stored

- \`record.passkeyCredentialId\`: webauthn \`credential.rawId\` in base64url (RFC 4648 §5)
- \`record.passkeyPublicKeyB64\`: 33-byte compressed secp256r1 public key from \`attestationObject\` parse
- \`record.passkeyRpId\`: the relying-party id (chrome-extension origin)
- \`record.passkeyPrfSaltB64\`: the 32-byte salt the wallet generated at register time and **must** re-supply on every assertion
- \`record.ikaShareKeysB64\`: serialized USK bytes for both curves
- \`record.mnemonic\`, \`suiPrivateKeyBech32\`: not present
- the \`prfSecret\` is **never stored** - it's regenerated by the authenticator on every assertion

## the fee-payer problem

passkey vaults don't have a sui ed25519 fee-payer keypair derived locally - the passkey produces a secp256r1 signature, not an ed25519 sui keypair. so:

- ika seed: derived from \`prfSecret\` via \`ikaRootSeedFromPasskeyPRF\` - works fine
- fee-payer for Sui PTB ops (DKG, presign, sign, sendSuiNative): there's **no local sui keypair** to sign with

per the agent's exploration, \`feeMaterialFromVaultRecord\` on a passkey vault **throws** today - which means PTB-based ika operations on a Sui-passkey vault need a different fee-funding path. this might use the passkey-derived address (Sui SIP-9 passkey addresses are valid Sui addresses with flag \`0x06\`) and dispatch the actual signing through the passkey assertion popup.

practical implication: if you're driving a Sui-passkey vault via \`createDWallet\`, the wallet should pop a passkey-sign popup for each fee-paying PTB. the design tradeoff is "no software-held sui key = more popup interactions". this matches the user's mental model of "passkey = hardware-rooted security".

## restore on a new device

\`\`\`
1. user re-registers the same passkey on a new chromatika install
   - if the passkey is platform-bound (Touch ID), they must use the same physical device
   - if the passkey is roaming (yubikey) or synced (iCloud Passwords / Google Passkey), it works on any device
2. addVaultPasskey or createPasskeyVault re-creates the envelope with the same credentialId
3. assertion produces the same prfSecret given the same (credential, prfSalt) pair (per WebAuthn PRF spec determinism)
4. ika seed = keccak256(prfSecret || index_le) - identical
5. discoverDWallets reattaches existing dWallets owned by this identity
\`\`\`

determinism guaranteed by:
- WebAuthn PRF / HMAC-secret spec: same credential + same salt + same operation = same output
- credential public key is part of the PRF input behind the scenes, so even if two credentials shared the same salt, they'd produce different outputs
- chromatika persists \`prfSaltB64\` in the vault - same salt on restore = same PRF output

## what doesn't work

- **non-PRF authenticators**: some older passkey authenticators don't expose the PRF / HMAC-secret extension. chromatika **rejects** registration on those - no way to deterministically seed the ika identity. surface a clear error suggesting a different authenticator
- **lost passkey**: if the user loses all their passkey credentials and didn't register a recovery-words branch, the vault is unrecoverable. recovery words are an envelope-level fallback (see [recovery-words-envelope.md](/library/tech/recovery-words-envelope)); they unlock the vault but the **ika identity** still requires one of the registered seeds. for passkey vaults, the recovery-words branch has to either re-derive the same prfSecret somehow (impossible - it's authenticator-bound) or store the USK bytes separately so they can be re-loaded after recovery-words-only unlock. CLAUDE.md notes the recovery-words envelope unlocks the vault, after which the user usually re-registers a fresh primary credential
- **cross-curve identity reuse**: same passkey, same salt, different chromatika install = same ika seed = same dWallet. but two different passkey vaults on the same install with **different salts** = different identities even if it's the same authenticator

## the fee-payer "throws today" workaround

in practice, Sui-passkey vaults are an emerging surface. the canonical implementation either:
- pops the passkey assertion to sign each Sui PTB (slow but secure)
- pre-derives a sui ed25519 fee-payer keypair from the prfSecret (\`prfSecret\` → \`ikaRootSeedFromPasskeyPRF\` produces the ika seed; a separate keccak with a different domain tag could produce a sui keypair seed, but this isn't the current implementation)
- uses passkey-derived addresses for ika operations once ika supports passkey-anchored fee payment

for now, treat Sui-passkey vaults as "ika identity works, gas funding requires more thought" - check the current code path before relying on it for production flows.

## library

- browser native \`navigator.credentials\` (webauthn)
- helper: \`wallet-extension/src/ui/passkey/passkey-provider-with-prf.ts\`
- helper: \`passkey-derive.ts\` for register-time validation, public-key parsing, Sui address derivation (\`blake2b_256(0x06 || pk_compressed)\`)
- internal: \`makeSeedFromPasskeyPRF\`, \`ikaRootSeedFromPasskeyPRF\` from \`keyring/hd.ts\`
- \`@noble/hashes/sha3\` \`keccak_256\`
- \`@ika.xyz/sdk\` \`UserShareEncryptionKeys.fromRootSeedKey\`
`,Pb=`# ika seed: Sui base + imported private key

vault \`seedSource: 'private-key'\`, base chain \`'sui'\`. user supplies a \`suiprivkey…\` bech32 string (the export format Sui Wallet, sui-keytool, etc. use). chromatika reconstructs the keypair, derives the ika seed exactly like a mnemonic vault would.

## inputs

- \`suiPrivateKeyBech32\`: a string like \`suiprivkey1qz…\` (bech32 encoding of \`[scheme_flag(1) || secret(32)]\`)
- \`encryption_key_index\`: always \`0\`

## step-by-step

\`\`\`
1. parse the bech32 input
   { scheme, secretKey } = decodeSuiPrivateKey(suiPrivateKeyBech32)
   // scheme can be 'ED25519', 'Secp256k1', 'Secp256r1' per Sui spec
   // chromatika asserts scheme === 'ED25519' for ika seed derivation

2. construct the Sui keypair
   suiKp = Ed25519Keypair.fromSecretKey(secretKey)
   // accepts the 32-byte raw ed25519 secret seed; computes pubkey internally

3. assemble keccak preimage (identical to the mnemonic path from this point)
   suiKpBytes = [scheme_flag(0x00) || secret(32)]   // 33 bytes
   indexLe = u32_le(0)
   preimage = suiKpBytes || indexLe   // 37 bytes total

4. hash
   seed_32 = keccak256(preimage)

5. derive both curves
   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)
   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)

6. zero seed_32
\`\`\`

step 3 onward is **identical** to [ika-seed-sui-mnemonic.md](/library/tech/ika-seed-sui-mnemonic). the only difference is how the keypair is produced - imported from bech32 vs derived from a mnemonic.

## what gets stored

- \`record.suiPrivateKeyBech32\`: the bech32 string, plaintext inside the encrypted vault payload
- \`record.mnemonic\`: not used (private-key vaults skip BIP39)
- \`record.ikaShareKeysB64\`: serialized USK bytes for both curves

on subsequent unlocks, the wallet either deserializes from \`ikaShareKeysB64\` or re-derives via \`Ed25519Keypair.fromSecretKey(...)\` → \`ikaRootSeedFromFeeKeypair(...)\`.

## restore on a new device

\`\`\`
1. user pastes the same suiprivkey… bech32 on a new install
2. importVaultFromPrivateKey → key stored in fresh vault payload
3. on first unlock with empty ikaShareKeysB64:
   - reconstruct Ed25519Keypair from secretKey
   - keccak preimage = scheme_flag || secret || index_le
   - seed = keccak256(preimage)
   - both curves derived
4. discoverDWallets walks Sui to find dWallets owned by the recovered identity
\`\`\`

deterministic. same private key = same keypair = same preimage = same seed = same dWallet.

## what doesn't work

- **secp256k1 / secp256r1 Sui keypairs**: chromatika asserts \`scheme === 'ED25519'\`. Sui supports those schemes for native signing but ika seed derivation is the ed25519 path only today. importing a non-ed25519 sui privkey rejects at the API boundary.
- **cross-chain reuse**: a Sui privkey is **not** a Solana privkey - the curves and key formats differ. you cannot import a \`suiprivkey…\` under Solana base. the cross-chain reuse path (BIP39 mnemonic on both bases) only works for mnemonic vaults.

## key handling caveat

importing a private key permanently anchors that key inside the chromatika vault payload. if the user wanted to "rotate" the key (move funds to a new identity), that's a new vault, not an in-place rotation - chromatika can't re-derive a different identity from the same imported key.

contrast: hardware vaults (Ledger / Seeker) can rotate the **fee-payer** keypair without touching the **ika identity** because the two are decoupled there. mnemonic and private-key vaults conflate them.

## library

- \`@mysten/sui\` \`Ed25519Keypair.fromSecretKey\`, \`decodeSuiPrivateKey\`
- \`@noble/hashes/sha3\` \`keccak_256\`
- \`@ika.xyz/sdk\` \`UserShareEncryptionKeys.fromRootSeedKey\`
- internal: \`makeSeedFromSuiKeypair\`, \`ikaRootSeedFromFeeKeypair\` from \`keyring/hd.ts\`
`,_b=`# ika seed: Sui base + WAAP

vault \`seedSource: 'waap-signature'\` (deterministic path) or \`seedSource: 'recovery-words'\` (non-deterministic fallback). base chain \`'sui'\`. WAAP (\`@human.tech/waap-sdk\`) handles email / phone / social login and returns a Sui address + public key plus a wallet-standard \`signPersonalMessage\` capability. chromatika probes for determinism at pairing and dispatches accordingly.

today this is a **Sui-only** path.

## the determinism probe

WAAP wallets may or may not produce deterministic ed25519 signatures. chromatika probes at pairing time:

\`\`\`
1. wallet.signPersonalMessage(IKA_USK_DERIVATION_MESSAGE)   // first sign
   → signature_1 (64 bytes)
2. wallet.signPersonalMessage(IKA_USK_DERIVATION_MESSAGE)   // second sign
   → signature_2

3. if signature_1 === signature_2:
     deterministic = true
     seedSource = 'waap-signature'
   else:
     deterministic = false
     seedSource = 'recovery-words'   // fallback; require user to provide BIP39 phrase
\`\`\`

deterministic WAAP wallets that follow RFC 8032 ed25519 (which is most modern implementations) pass the probe. WAAP wallets that introduce randomness during signing (some legacy or experimental implementations) fail and fall back.

## deterministic path: \`seedSource: 'waap-signature'\`

\`\`\`
1. signature = wallet.signPersonalMessage(IKA_USK_DERIVATION_MESSAGE)   // 64 bytes
2. (the wallet-standard signPersonalMessage may apply Mysten's BLAKE2b PersonalMessage intent
    to the message before ed25519-signing it. that's fine - the signature bytes are still
    deterministic given the same inputs, which is what we need)

3. assemble keccak preimage
   indexLe = u32_le(0)
   preimage = signature || indexLe   // 68 bytes

4. hash
   seed_32 = keccak256(preimage)

5. derive both curves
   uskSecp = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.SECP256K1)
   uskEd25519 = UserShareEncryptionKeys.fromRootSeedKey(seed_32, Curve.ED25519)

6. zero seed_32
\`\`\`

## what gets stored (deterministic path)

- \`record.waapSuiAddress\`: the user-facing Sui address WAAP returns (not the dWallet's address - the WAAP-anchored address)
- \`record.waapSuiPublicKeyB64\`: the secp256k1 or ed25519 compressed pubkey from the WAAP wallet-standard account
- \`record.waapAuthMethod\`: \`'email' | 'phone' | 'social'\`
- \`record.waapSocialProvider\`: optional, e.g. \`'google'\`
- \`record.waapPairingSignatureB64\`: the **encrypted** signature bytes (encrypted in the wallet-signature envelope, NOT plaintext)
- \`record.ikaShareKeysB64\`: USK bytes for both curves
- multi-envelope:
  - \`WalletSignatureEnvelope\` for the WAAP signature (unlock branch)
  - optional \`RecoveryWordsEnvelope\` if user provided a backup BIP39 phrase

## non-deterministic fallback path: \`seedSource: 'recovery-words'\`

if the determinism probe fails:

\`\`\`
1. require user to provide a 12 / 24 BIP39 phrase
2. validateWords(words)
3. bip39_seed = mnemonicToSeedSync(words, "")   // 64 bytes
4. preimage = bip39_seed || u32_le(0)   // 68 bytes
5. seed_32 = keccak256(preimage)
6. derive both curves, zero seed_32
\`\`\`

the WAAP signature is **not** used for ika seed in this path - it's still used for unlock (via \`WalletSignatureEnvelope\`) but the ika identity is rooted in the BIP39 phrase. losing the WAAP login means losing one unlock path; losing the BIP39 phrase means losing the identity.

## what gets stored (recovery-words path)

- WAAP fields same as above
- additionally \`record.recoveryWordsEncryptedB64\`: the BIP39 phrase **plaintext inside the encrypted vault payload** (chromatika rebuilds the seed on every unlock from this)
- \`record.ikaShareKeysB64\`: USK bytes for both curves
- multi-envelope: \`RecoveryWordsEnvelope\` (primary), \`WalletSignatureEnvelope\` (secondary)
- \`seedSource: 'recovery-words'\`

## the WAAP login flow at pairing

\`\`\`
1. user picks auth method (email / phone / google / discord / twitter / github / bluesky)
2. WAAP SDK opens its login UI in a popup or new tab
3. on success, WAAP returns:
   - sui_address (string)
   - sui_public_key (b64)
   - auth_method (string)
   - wallet-standard account object that exposes signPersonalMessage
4. chromatika captures address + pubkey, runs the determinism probe
5. branches into one of the two paths above
\`\`\`

## restore on a new device

### deterministic path
\`\`\`
1. log into the same WAAP account on a new chromatika install
2. WAAP returns the same sui_address + pubkey + signing capability
3. wallet.signPersonalMessage(IKA_USK_DERIVATION_MESSAGE) → same 64-byte signature (RFC 8032 + same key)
4. ika seed = keccak256(signature || index) - identical
5. discoverDWallets reattaches existing dWallets
\`\`\`

### recovery-words path
\`\`\`
1. user types BIP39 phrase on new install
2. WAAP login is also re-paired to register the WalletSignatureEnvelope (secondary)
3. ika seed = keccak256(bip39_seed || index) - identical to before
4. discoverDWallets reattaches
\`\`\`

## what doesn't work

- **changing WAAP auth method**: switching from email-WAAP to google-WAAP gives you a **different** WAAP-anchored account → different signature → different ika seed → different dWallet. chromatika cannot migrate identities across WAAP auth methods. it can store multiple WAAP envelopes on the same vault for unlock-only purposes, but the ika identity is tied to whichever WAAP signature was the seed source at vault creation
- **lazor confusion**: WAAP and Lazor are unrelated. WAAP is Sui (and uses signature-based seed derivation when deterministic); Lazor is Solana (and always uses recovery-words for the seed). don't conflate

## library

- \`@human.tech/waap-sdk\` for the auth flow + wallet-standard account
- internal: \`makeSeedFromMwaSignature\` (same factory as Seeker / WC, since the formula is the same), \`ikaRootSeedFromMwaSignature\` from \`keyring/hd.ts\`
- internal: \`waapSeedFactoryFromInput\` dispatcher routes between the two paths based on \`seedSource\`
- \`@noble/hashes/sha3\` \`keccak_256\`
- \`@ika.xyz/sdk\` \`UserShareEncryptionKeys.fromRootSeedKey\`
`,Eb=`# ika sign flow

producing a signature with an active dWallet. combines a precomputed presign with the message + user share to produce a canonical ECDSA / EdDSA / Taproot signature. the user share never leaves chromatika; the network share never leaves the ika network. neither party alone can produce the signature.

## prerequisites

- a Chromatika vault is unlocked
- the active dWallet is in \`active\` state for the curve you're signing on (DKG completed + accept-share completed)
- the matching presign pool has at least one entry (auto-refill keeps this topped up; see [ika-presign-pool-impl.md](/library/tech/ika-presign-pool-impl))
- on Solana base: in-extension fee-payer has SOL for \`approve_message\` gRPC fees

## the call

per chain / message kind:
- \`signEvm({ message, chainId })\` - SECP256K1_ECDSA path with EIP-191 wrapping
- \`signBtc({ messageHex })\` - SECP256K1_ECDSA or SECP256K1_TAPROOT depending on address kind
- \`signSol({ messageB64 })\` - ED25519_EDDSA path
- \`signAptos({ messageB64 })\` - ED25519_EDDSA path
- internally for sends: \`signAndBroadcastEvm({ to, value, data, chainId })\` etc.

each maps to ika's underlying \`requestSign\` with the right curve / algorithm.

## the flow (Sui base, ECDSA example for EVM)

\`\`\`
1. takePresign(SECP256K1_ECDSA) → presignId
   - pops one entry from the pool

2. assemble the message preimage
   for personal_sign: msg = "\\x19Ethereum Signed Message:\\n" + len(message) + message
   for typed_data_v4: msg = TypedDataEncoder.encode(domain, types, value)
   for tx send: msg = tx.unsignedSerialized
   // PASS PREIMAGE, not digest. ika hashes once with KECCAK256 internally.

3. build the sign PTB
   tx = new IkaTransaction()
   tx.requestSign({
     dwalletId,
     curve: Curve.SECP256K1,
     algorithm: SignatureAlgorithm.ECDSASecp256k1,
     message: msg,
     presignId,
     userPartialSignature,   // produced locally with USK
   })
   // requestSign returns void - no value to capture

4. simulate, submit, wait for sign output event

5. read the signature from the completion event
   // event.signature_bytes = 64-byte compact r||s for ECDSA

6. normalize via ika SDK helper
   { r, s } = parseSignatureFromSignOutput(
     event.signature_bytes,
     Curve.SECP256K1,
     SignatureAlgorithm.ECDSASecp256k1
   )
   // assert 64-byte total

7. compute v
   digest = keccak256(msg)
   for v_candidate in [27, 28]:
     recovered = recoverAddress(digest, { r, s, v: v_candidate })
     if recovered === knownEvmAddress:
       v = v_candidate
       break

8. assemble final EVM signature: r || s || v   (or r || s || (v + 35 + chainId*2) for EIP-155 tx)
\`\`\`

## the flow (Solana base, ED25519 example)

\`\`\`
1. takePresign(ED25519_EDDSA) → presignId
2. message = msg_bytes   // raw, not hashed - ika SHA-512s internally per RFC 8032
3. send sign request via gRPC
   await ikaClient.requestSign({
     dwalletId,
     curve: Curve.ED25519,
     algorithm: SignatureAlgorithm.EdDSA,
     message,
     presignId,
     userPartialSignature,
     dwalletAttestationBytes,   // for Solana
     approveMessage,            // signed by in-extension fee-payer
   })
4. await completion (gRPC stream)
5. read signature_bytes (64 bytes for ed25519)
6. parseSignatureFromSignOutput(sig, Curve.ED25519, SignatureAlgorithm.EdDSA) → (R, S)
7. signature is just R || S (64 bytes), no v needed
\`\`\`

## preimage passthrough rule

a recurring rule across all signing paths:

> **hand ika the preimage bytes. ika hashes internally. never pre-hash.**

| sign type | preimage you pass | what ika hashes |
|-----------|-------------------|-----------------|
| EVM personal_sign | \`"\\x19Ethereum Signed Message:\\n" + len + msg\` | keccak256(preimage) |
| EVM typed_data_v4 | \`TypedDataEncoder.encode(...)\` | keccak256(preimage) |
| EVM tx send | \`tx.unsignedSerialized\` | keccak256(preimage) |
| Sui personal | raw msg bytes | sha-512(preimage) per RFC 8032 |
| Solana sign-msg | raw msg bytes | sha-512(preimage) per RFC 8032 |
| BTC P2WPKH | raw msg bytes (or BIP143 sighash, depending on path) | keccak256 (then chained?) |
| BTC P2TR | BIP341 sighash | tagged_hash("BIP0340/challenge", ...) per BIP340 |

if you double-hash (pass already-hashed bytes), ika hashes the digest itself, the resulting signature is over the wrong digest, and verification fails (or recovers a different address).

## signature normalization helpers

\`parseSignatureFromSignOutput(sigBytes, curve, sigAlgorithm)\` is the canonical helper. enforces:
- ECDSA: 64 bytes, returns (r, s) each 32 bytes; rejects non-64-byte input
- EdDSA: 64 bytes, returns (R, S)
- Taproot: 64 or 65 bytes (with optional sighash byte), returns (R, S) plus optional sighash flag

## v recovery for ECDSA-EVM

ika's ECDSA output is the bare 64-byte r||s. EVM verifiers want a 65-byte r||s||v signature with \`v ∈ {27, 28}\` (or \`v ∈ {35 + chainId*2 + 0, 35 + chainId*2 + 1}\` for EIP-155 transactions). chromatika picks \`v\` by trying both candidates against \`recoverAddress(keccak256(preimage), { r, s, v })\` and matching to the dWallet's known EVM address.

if neither v matches, something is wrong: either the preimage was hashed twice, or the dWallet pubkey doesn't match the signature, or the network produced a bad signature. all three are bugs.

## the EVM dapp-tx vs wallet-ui-tx split

- \`eth_sendTransaction\` from a dapp → \`enqueueTxApproval\` → approval popup → on approve, \`signAndBroadcastEvm\`
- wallet-ui \`sendEvmTx\` (tRPC from side panel) → \`signAndBroadcastEvm\` directly, no popup

both end up at \`signAndBroadcastEvm\`, which calls the ika sign flow above and then broadcasts via the EVM provider. **never add an approval gate to the wallet-ui flow** - the user is already in the wallet UI making an intentional action.

## what doesn't work

- signing on a locked wallet: tRPC fails with "wallet locked"
- signing on a dWallet not in \`active\` state: rejects with state error
- signing with no presigns available + can't refill (insufficient funds): pool-empty error, user must fund + refill
- signing with the wrong curve / algorithm combo (e.g. asking for Taproot on an ED25519 dWallet): rejects
- on Solana base today: signing produces a single-mock-signer output, not real distributed MPC. the signature is technically valid against the published dWallet pubkey but the trust model is **not** what production MPC will be

## library

- \`@ika.xyz/sdk\` \`IkaTransaction.requestSign\`, \`parseSignatureFromSignOutput\`
- \`@ika.xyz/pre-alpha-solana-client\` for Solana base sign over gRPC
- ethers v6 \`recoverAddress\`, \`keccak256\` for v-recovery
- internal: \`wallet-extension/src/background/ika/signing.ts\` orchestration
- internal: \`wallet-extension/src/background/ika/presign-pool.ts\` for \`takePresign\`
- internal: \`wallet-extension/src/background/chains/signing/*\` for per-chain message wrapping
`,Cb="# keccak256 in chromatika\n\nkeccak256 is the unmodified-padding variant of the SHA-3 family that ethereum standardized on (note: NIST's SHA3-256 uses a different padding rule and produces different output for the same input). chromatika uses keccak256 in three places, all of them load-bearing.\n\n## 1. ika `UserShareEncryptionKeys` root seed derivation\n\nevery ika user-share encryption key chromatika derives, regardless of base chain, runs through this 32-byte keccak output. the formula is always:\n\n```\nseed_32 = keccak256(preimage_bytes || encryption_key_index_le_4bytes)\n```\n\n`encryption_key_index` is a `u32` little-endian, almost always `0` for the primary user-share key. `preimage_bytes` differs per credential type (see [ika-seed-derivation-overview.md](/library/tech/ika-seed-derivation-overview) and the per-credential docs):\n\n- **Sui base + keypair-based credential** (`ikaRootSeedFromFeeKeypair`): preimage = full `SuiKeyPair.to_bytes()` = `[scheme_flag(1) || secret_key(32)]` = 33 bytes. for ed25519 that's `0x00 || secret`. total preimage 33 + 4 = 37 bytes.\n- **Solana base + keypair-based credential** (`ikaRootSeedFromSolanaKeypair`): preimage = canonical 64-byte `Keypair.secretKey` (`[seed(32) || pubkey(32)]` per ed25519 RFC 8032). total preimage 64 + 4 = 68 bytes.\n- **Solana hardware (MWA / Seeker / WalletConnect)** (`ikaRootSeedFromMwaSignature`): preimage = the raw 64-byte ed25519 signature over `IKA_USK_DERIVATION_MESSAGE`. total preimage 64 + 4 = 68 bytes.\n- **Passkey (Sui base)** (`ikaRootSeedFromPasskeyPRF`): preimage = the 32-byte WebAuthn PRF / hmac-secret output. total preimage 32 + 4 = 36 bytes.\n- **Recovery words / Lazor / WAAP-fallback** (`ikaRootSeedFromRecoveryWords`): preimage = the BIP39 PBKDF2-derived 64-byte seed (`mnemonicToSeedSync(words)`). total preimage 64 + 4 = 68 bytes.\n\nafter derivation the seed is fed into `UserShareEncryptionKeys.fromRootSeedKey(seed, curve)` - one call per curve (SECP256K1 + ED25519). the seed buffer is zeroed (`seed.fill(0)`) immediately after to limit lifetime in memory.\n\n## 2. EVM transaction digesting + signature `v` recovery\n\nEVM signing (`signBytesEvm`) hands ika the **preimage** bytes - either `tx.unsignedSerialized` for a transaction send or the EIP-191 wrapped bytes for `personal_sign` - and ika hashes once internally with KECCAK256. chromatika **never** double-hashes; passing the digest already-hashed would cause ika to keccak256 the digest again and produce a totally wrong recovered address.\n\nafter the 64-byte compact `r||s` sig comes back, chromatika computes the digest itself via `keccak256(unsignedSerialized)` and tries `recoverAddress(digest, { r, s, v: 27 })` and `v: 28`. whichever recovers to the dWallet's known EVM address wins. for typed-data v4 the same rule applies: pass `TypedDataEncoder.encode(...)` (preimage), not `TypedDataEncoder.hash(...)`.\n\n## 3. EVM address derivation\n\nan EVM address = `last 20 bytes of keccak256(uncompressed_public_key_64bytes)` per yellow paper. dWallet ed25519 / secp256k1 public keys are derived per ika; chromatika applies the keccak rule to produce the EIP-55 checksummed address surfaced via `getEvmAddress` (and `dwalletAddressBook` / `getDwalletChainAddresses`).\n\n## library\n\nprovided by `@noble/hashes` (`keccak_256` from `@noble/hashes/sha3`). pure-JS, no wasm. zero deps beyond `@noble/hashes` itself. faster than the historical js-keccak-bundle and audit-grade per noble's release process.\n\n## not used for\n\n- BLAKE2b (Sui native PersonalMessage intent digest)\n- SHA-512 (ika ed25519 signing path)\n- SHA-256 (BIP39 PBKDF2, BIP32 HMAC, Bitcoin tx ids - all separate primitives)\n\nif you see code that keccak256's something it shouldn't (e.g. running it on an already-keccaked digest, or substituting for BLAKE2b in the Sui flow), that's a bug - flag it, don't merge it.\n",Ab=`# Ledger hw-app libraries

chromatika supports Ledger devices for EVM, Sui, Solana, and Bitcoin signing via Ledger's per-chain \`hw-app-*\` libraries. all wrap the same WebHID transport (\`@ledgerhq/hw-transport-webhid\`) and run in popup / side-panel context only.

## the library set

| package | version | chain |
|---------|---------|-------|
| \`@ledgerhq/hw-transport-webhid\` | ^6.35.1 | transport (USB HID) |
| \`@ledgerhq/hw-transport\` | 6.35.1 | base transport types |
| \`@ledgerhq/hw-app-eth\` | ^7.8.0 | EVM (eth, polygon, etc.) |
| \`@ledgerhq/hw-app-sui\` | 1.9.0 | Sui |
| \`@ledgerhq/hw-app-solana\` | 7.10.1 | Solana |
| \`@ledgerhq/hw-app-btc\` | ^10.21.1 | Bitcoin (segwit + legacy) |
| \`@ledgerhq/devices/hid-framing.js\` | (transitive) | HID frame protocol |

## the transport pattern

\`\`\`ts
import TransportWebHID from '@ledgerhq/hw-transport-webhid';

async function withTransport<T>(fn: (transport: Transport) => Promise<T>): Promise<T> {
  const transport = await TransportWebHID.create();
  try {
    return await fn(transport);
  } finally {
    await transport.close();
  }
}
\`\`\`

\`TransportWebHID.create()\`:
- prompts user via WebHID dialog (first time only; subsequent connections silent if permission persists)
- requires user gesture (button click in popup)
- returns a \`Transport\` instance

\`transport.close()\` releases the device. always call in a \`finally\`.

## EVM signing

\`\`\`ts
import Eth from '@ledgerhq/hw-app-eth';

await withTransport(async (transport) => {
  const eth = new Eth(transport);

  // get address (no on-device confirm by default)
  const { address, publicKey } = await eth.getAddress("44'/60'/0'/0/0", false);

  // sign transaction
  const sig = await eth.signTransaction("44'/60'/0'/0/0", rawTxHex);
  // sig = { v: '1c', r: '...', s: '...' }

  // sign personal message (EIP-191)
  const sigMsg = await eth.signPersonalMessage("44'/60'/0'/0/0", messageHex);

  // sign typed data v4
  const sigTyped = await eth.signEIP712HashedMessage(
    "44'/60'/0'/0/0",
    domainSeparatorHex,
    hashStructMessageHex,
  );
});
\`\`\`

note: \`signEIP712HashedMessage\` takes the **two component hashes** of EIP-712 (domain separator hash + struct hash), not the full preimage. ethers v6 \`TypedDataEncoder.hashDomain()\` and \`hashStruct()\` produce these.

Ledger displays the tx / message contents on the device for user confirmation. user presses confirm/reject buttons on the Ledger.

## Sui signing

\`\`\`ts
import Sui from '@ledgerhq/hw-app-sui';

await withTransport(async (transport) => {
  const sui = new Sui(transport);

  const { address, publicKey } = await sui.getAddress("44'/784'/0'/0'/0'", false);

  const sig = await sui.signTransaction(
    "44'/784'/0'/0'/0'",
    txBytesWithIntent,   // 0x000000 || bcs(tx)
  );
  // sig = { signature: '<64-byte hex>' }
});
\`\`\`

Sui Ledger app limitations are documented in \`wallet-extension/docs/LEDGER_SUI_LIMITS.md\`. firmware version + app version floors apply; chromatika surfaces version errors.

## Solana signing

\`\`\`ts
import Solana from '@ledgerhq/hw-app-solana';

await withTransport(async (transport) => {
  const solana = new Solana(transport);

  const { address, publicKey } = await solana.getAddress("44'/501'/0'/0'");

  const sig = await solana.signTransaction("44'/501'/0'/0'", txBytes);
  // sig = { signature: <Buffer 64 bytes> }
});
\`\`\`

note Solana derivation path is **4 segments** (\`44'/501'/0'/0'\`), all hardened.

## Bitcoin signing (PSBT)

\`\`\`ts
import AppBtc from '@ledgerhq/hw-app-btc';

await withTransport(async (transport) => {
  const btc = new AppBtc({ transport, scrambleKey: 'BTC' });

  // segwit address
  const { address } = await btc.getWalletPublicKey("84'/0'/0'/0/0", { format: 'bech32' });

  // sign PSBT (segwit)
  const signedPsbt = await btc.signPsbtBuffer(
    psbtBufferIn,
    { startAt: 0, finalize: true },
  );
});
\`\`\`

\`signPsbtBuffer\` is the modern API (\`hw-app-btc@10.x\`). takes a PSBT, signs all inputs the device can sign, returns the updated PSBT. handles bech32 (P2WPKH segwit) + legacy paths. chromatika passes paths from BIP84 (\`84'/0'/...\`) for segwit and BIP86 (\`86'/0'/...\`) for taproot.

## the per-chain app on-device

Ledger devices run separate firmware "apps" per chain. user must open the right one before chromatika's \`hw-app-*\` calls succeed:
- "Ethereum" app for EVM
- "Sui" app for Sui
- "Solana" app for Solana
- "Bitcoin" app for BTC

if the wrong app is open, chromatika gets an "app not open" error from the device. surface "open the Ethereum app on your Ledger" with retry.

## the dual-version live-network workaround

per CLAUDE.md non-obvious bullet: two \`@ledgerhq/live-network\` versions are pinned + patched today (\`2.0.19\` + \`2.4.3\`). reconciling to one is tracked. blocks Ledger \`hw-app-*\` and \`hw-transport\` bumps because newer versions pull \`hw-transport@6.34+\` which splits into two resolved versions vs the direct \`6.31.0\` pin.

practical: don't bump Ledger libs without checking the live-network resolution.

## the buffer polyfill connection

\`@ledgerhq/devices/hid-framing.js\` uses Node's \`Buffer\` global. browsers don't have it. \`src/buffer-polyfill.ts\` shims via the \`buffer\` npm package. **import as the first line** of every entry point that touches Ledger code (popup, side panel).

## library

- per package above
- internal: \`wallet-extension/src/ui/hardware/LedgerSigner.tsx\` for the popup-side flow
- internal: \`wallet-extension/src/background/hardware/ledger-derivation.ts\` for derivation-path conventions

## related

- [webhid-popup-context.md](/library/tech/webhid-popup-context) - the WebHID transport
- [secp256k1-ecdsa.md](/library/tech/secp256k1-ecdsa), [taproot-schnorr.md](/library/tech/taproot-schnorr), [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - underlying signature math
- [ledger.md](/library/user/ledger) (user-guides) - the user-facing flow
`,Ib=`# MCP bearer-token auth

every chromatika install gets a per-install random bearer token used to gate the localhost HTTP MCP listener. the token rotates on demand, persists in extension storage, and is pushed to the native host via the \`{ type: 'config', tokenHex }\` native messaging frame. agent clients pass it as \`Authorization: Bearer <tokenHex>\` on every HTTP request.

## token format

\`\`\`
tokenHex = hex(crypto.getRandomValues(Uint8Array(32)))
        = 64-char hex string (256 bits of randomness)
\`\`\`

256 bits is overkill for offline brute-force resistance but cheap to generate. the token never leaves the local machine - it's not exposed to the internet, not signed over, not used for cryptographic protocols. it's a simple shared secret between chromatika and any local agent process the user has authorized.

## storage

\`\`\`jsonc
chrome.storage.local["chromatika_mcp_v1"] = {
  "enabled": true,
  "tokenHex": "abc...64chars...",
  "listenPort": 54321,
  "listenHost": "127.0.0.1",
  "nativeHostName": "com.chromatika.mcp_host",
  "desiredListenPort": null   // user-supplied stable port, or null for random
}
\`\`\`

\`chromatika_mcp_v1\` lives in \`chrome.storage.local\`. cleared on uninstall. surface for read / write:
- \`mcpStatus\` - returns the whole record (or sanitized subset)
- \`mcpEnable\` / \`mcpDisable\` - toggles \`enabled\`
- \`mcpRotateToken\` - generates a fresh \`tokenHex\`, pushes to running host
- \`mcpSetDesiredPort\` - sets \`desiredListenPort\`, triggers a \`reconfigure-port\` frame to the host

## generation

\`\`\`ts
async function generateToken() {
  const buf = new Uint8Array(32);
  crypto.getRandomValues(buf);
  return [...buf].map(b => b.toString(16).padStart(2, '0')).join('');
}
\`\`\`

generated by chromatika at:
- first \`mcpEnable\` if \`tokenHex\` is empty / null
- explicit \`mcpRotateToken\` call

## the push to host

on \`chrome.runtime.connectNative\`, chromatika pushes \`{ type: 'config', tokenHex }\` as the **first native messaging frame**. the host stores it in memory:

\`\`\`js
const STATE = { tokenHex: null };

function handleMessage(msg) {
  if (msg.type === 'config') {
    STATE.tokenHex = msg.tokenHex;
    sendFrame({ ok: true, kind: 'config-ack', hasToken: STATE.tokenHex != null });
    return;
  }
  // ... other frame handling
}
\`\`\`

the token is **never persisted on disk by the host** - only held in process memory. if the host process is killed and respawned, chromatika reconnects and pushes the token again.

## validation on HTTP requests

\`\`\`js
function httpHandler(req, res) {
  const auth = req.headers['authorization'] ?? '';
  const expected = STATE.tokenHex ? \`Bearer \${STATE.tokenHex}\` : null;

  if (!expected || auth !== expected) {
    res.statusCode = 401;
    res.setHeader('Content-Type', 'application/json');
    res.end(JSON.stringify({
      jsonrpc: '2.0',
      id: null,
      error: { code: -32001, message: 'unauthorized' }
    }));
    return;
  }

  // ... proceed to JSON-RPC handling
}
\`\`\`

constant-time comparison would be ideal here (defends against timing attacks). chromatika's host uses string equality which is **technically** non-constant-time but the threat is moot for localhost: an attacker who can time HTTP responses to localhost has already won (they can read storage, etc.). for paranoia, swap to \`crypto.timingSafeEqual(Buffer.from(actual), Buffer.from(expected))\`.

## rotation

\`\`\`ts
async function rotateToken() {
  const newToken = await generateToken();
  await chrome.storage.local.set({
    chromatika_mcp_v1: { ...current, tokenHex: newToken }
  });
  if (nativePort) {
    nativePort.postMessage({ type: 'config', tokenHex: newToken });
    // host overwrites STATE.tokenHex; old token immediately invalid for new requests
  }
}
\`\`\`

rotation is **immediate** for new requests - the host's \`STATE.tokenHex\` updates as soon as the config frame arrives. existing in-flight requests with the old token complete normally (they passed validation when they arrived). new requests with the old token return 401.

agent clients (Claude Desktop, Cursor) need to update their config with the new token. this is manual today; tracked future is some kind of token-broker handshake.

## per-install scoping

each chromatika install (each browser profile, each computer) generates its own token. the token is **not** synced across devices - chromatika doesn't write it to \`chrome.storage.sync\`. this is intentional: each install has its own MCP listener with its own port, its own host process, its own token. cross-device usage requires the user to grab the per-install URL + token and paste into their agent client config on each machine.

## attack model

| threat | mitigation |
|--------|-----------|
| internet attacker hits \`http://my-public-ip:port/mcp\` | listener bound to 127.0.0.1; not externally reachable |
| local attacker (same machine, same user) reads \`chrome.storage.local\` | they have the token. but they can also read your vault directly. game over at this layer |
| local attacker (different user on same machine) | OS file permissions on chrome's data dir prevent cross-user reads. attacker would need privesc |
| MITM between agent client and HTTP listener | localhost loopback. no MITM possible without local malware (which already wins) |
| browser-based attack (web page tries to reach localhost) | browsers block fetch to localhost without CORS; the listener doesn't set CORS headers. fails |
| token leak via screenshot / shoulder-surf | rotate the token via \`mcpRotateToken\`. takes effect immediately |

bottom line: bearer token + localhost binding gives "this requires a process on your machine that you've authorized" - not "this is encrypted in transit and authenticated cryptographically". that's the right model for local MCP.

## reveal in the UI

\`AgentsSettingsSection\` exposes:
- copy token to clipboard (one click)
- reveal token (click to show, hidden by default)
- rotate (button, generates fresh)
- copy agent URL (\`http://127.0.0.1:<port>/mcp\`)

the token is **not** masked in the underlying tRPC response (\`mcpStatus\` returns it in the clear) - the UI handles masking. agents reading via tRPC see the actual token.

## library

- \`crypto.getRandomValues\` (browser native) for generation
- \`chrome.storage.local\` for persistence
- \`port.postMessage({ type: 'config', tokenHex })\` for the push
- internal: \`mcp-storage.ts\` for storage shape + helpers
- internal: \`mcp-auth.ts\` (with \`mcp-auth.test.ts\`) for the validation logic on the host side

## related

- [mcp-http-transport.md](/library/tech/mcp-http-transport) - the HTTP listener auth happens in
- [mcp-native-messaging-frame.md](/library/tech/mcp-native-messaging-frame) - the \`{ type: 'config' }\` frame
- [mcp-protocol-overview.md](/library/tech/mcp-protocol-overview) - the MCP method set the token gates
`,Tb=`# MCP host spawn + per-OS setup

\`chrome.runtime.connectNative('com.chromatika.mcp_host')\` only works if the OS has a **native messaging manifest** registered for that host name + the chromatika extension id. the manifest tells chrome where the host binary lives and which extensions are allowed to spawn it. chromatika ships a setup script (\`pnpm setup:native-host --extension-id=<id>\`) that writes the manifest in the right per-OS location.

## the manifest

\`\`\`jsonc
{
  "name": "com.chromatika.mcp_host",
  "description": "Chromatika MCP native messaging host",
  "path": "/absolute/path/to/chromatika-mcp-host.mjs",
  "type": "stdio",
  "allowed_origins": [
    "chrome-extension://<your-extension-id>/"
  ]
}
\`\`\`

required fields:
- \`name\`: must match what chromatika passes to \`connectNative\`. **must be lowercase + dots/underscores only** per chrome's spec
- \`description\`: free-form
- \`path\`: absolute path to the host binary (or a wrapper script)
- \`type\`: always \`"stdio"\` for native messaging
- \`allowed_origins\`: list of \`chrome-extension://...\` origins permitted to spawn this host. **\`<your-extension-id>\` is install-specific** - hence the \`--extension-id=\` flag on the setup script

## per-OS manifest locations

per chrome's [native messaging docs](https://developer.chrome.com/docs/extensions/develop/concepts/native-messaging):

### macOS

user-level (no admin needed):
\`\`\`
~/Library/Application Support/Google/Chrome/NativeMessagingHosts/com.chromatika.mcp_host.json
\`\`\`

system-level (admin):
\`\`\`
/Library/Google/Chrome/NativeMessagingHosts/com.chromatika.mcp_host.json
\`\`\`

chromatika setup writes the user-level path.

### Linux

user-level:
\`\`\`
~/.config/google-chrome/NativeMessagingHosts/com.chromatika.mcp_host.json
\`\`\`

system-level:
\`\`\`
/etc/opt/chrome/native-messaging-hosts/com.chromatika.mcp_host.json
\`\`\`

chromatika setup writes user-level.

### Windows

user-level uses **registry** (no JSON manifest in a known directory; instead a registry value points at the manifest):
\`\`\`
HKEY_CURRENT_USER\\Software\\Google\\Chrome\\NativeMessagingHosts\\com.chromatika.mcp_host
  (Default) = "C:\\path\\to\\manifest.json"
\`\`\`

system-level:
\`\`\`
HKEY_LOCAL_MACHINE\\Software\\Google\\Chrome\\NativeMessagingHosts\\com.chromatika.mcp_host
\`\`\`

windows also requires the host to be invokable - \`node\` may not be on \`PATH\` for the chrome process, so chromatika ships a \`.bat\` shim:
\`\`\`bat
@echo off
node "%~dp0chromatika-mcp-host.mjs" %*
\`\`\`

manifest's \`path\` points at the \`.bat\`, which \`cmd.exe\` invokes via \`node\`. the manifest goes in a per-user directory; the \`reg add\` registers the path.

## the setup command

\`\`\`sh
pnpm setup:native-host --extension-id=<your-extension-id>
\`\`\`

the script lives at \`wallet-extension/native-host/setup.mjs\`. it:
1. detects platform (\`process.platform\`)
2. computes the manifest JSON with the right \`path\` and \`allowed_origins\`
3. writes the manifest to the OS-specific user-level location
4. on Windows, also drops the \`.bat\` shim and runs \`reg add\` for the registry value
5. validates by running \`chrome.runtime.connectNative\` from the extension and checking \`onDisconnect.lastError\`

on success, the extension can call \`connectNative('com.chromatika.mcp_host')\` and chrome spawns the host.

## the spawn lifecycle

\`\`\`
1. extension calls chrome.runtime.connectNative('com.chromatika.mcp_host')
2. chrome reads the manifest from the per-OS location
3. chrome verifies the calling extension's origin is in allowed_origins
4. chrome spawns the host binary as a child process:
     - stdin/stdout piped to chrome (4-byte LE length-prefixed frames)
     - stderr available to chrome's internal logging
5. chrome returns a Port object to the extension
6. host begins reading frames, ready to handle config push
7. extension pushes { type: 'config', tokenHex } as the first frame
8. host stores the token, opens HTTP MCP listener, sends { kind: 'listen' } frame
9. all subsequent MCP requests flow through this channel
\`\`\`

## what kills the host process

- extension calls \`port.disconnect()\` (e.g. user disabled MCP via \`mcpDisable\`)
- chrome itself shuts down (browser quit)
- host process crashes (exits)
- network host name change (rare)

on disconnect, chrome cleans up the child. host's \`process.stdin\` closes, the readline / buffer-process loop exits, host exits.

## the auto-reconnect behavior

chromatika's \`mcp-native-bridge.ts\` does **capped exponential backoff** on disconnect:
- max attempts: 5
- delay: starts at 1s, doubles each attempt, capped at 30s
- delays: 1s → 2s → 4s → 8s → 16s → 30s, 30s

after 5 attempts, give up; user has to manually \`mcpEnable\` again to retry. this prevents zombie reconnect loops if the host binary is missing / broken.

reconnect rebinds the same port if \`desiredListenPort\` is set, otherwise picks a fresh random port.

## the SW startup re-establish

on cold service worker start (chrome unloaded the SW after idle):
1. SW reads \`chromatika_mcp_v1.enabled\`
2. if \`enabled === true\`, attempt \`connectNative\` immediately
3. retry on failure with the backoff schedule

this keeps the host alive across SW restart cycles without user action.

## why the install needs the extension id

\`allowed_origins\` in the manifest is the security boundary - **only the listed extension(s) can spawn this host**. without it, any extension installed on the user's chrome could call \`connectNative\` and drive the wallet. that's why the setup script requires \`--extension-id=<id>\`:
1. user opens \`chrome://extensions\`
2. enables developer mode
3. copies the chromatika extension id
4. runs \`pnpm setup:native-host --extension-id=<that-id>\`

dev installs and production-published extensions have **different extension ids**, so the manifest needs to be regenerated when switching between them.

## what \`pnpm setup:native-host\` does (Windows)

\`\`\`
1. compute paths:
     hostBinary = wallet-extension/native-host/chromatika-mcp-host.mjs
     batShim    = wallet-extension/native-host/chromatika-mcp-host.bat
     manifest   = %APPDATA%/chromatika/com.chromatika.mcp_host.json

2. write batShim:
     @echo off
     node "%~dp0chromatika-mcp-host.mjs" %*

3. write manifest at the chosen path with \`path\` pointing at batShim

4. reg add HKCU\\Software\\Google\\Chrome\\NativeMessagingHosts\\com.chromatika.mcp_host /ve /d "<manifest-path>" /f

5. echo a one-line success line + the registered path
\`\`\`

## what \`pnpm setup:native-host\` does (macOS)

\`\`\`
1. hostBinary = wallet-extension/native-host/chromatika-mcp-host.mjs
2. ensure shebang: '#!/usr/bin/env node' at the top of hostBinary (it already has this)
3. chmod +x hostBinary
4. write manifest at:
     ~/Library/Application Support/Google/Chrome/NativeMessagingHosts/com.chromatika.mcp_host.json
   with path = absolute path to hostBinary
5. echo success
\`\`\`

## what \`pnpm setup:native-host\` does (Linux)

\`\`\`
1. hostBinary = wallet-extension/native-host/chromatika-mcp-host.mjs
2. ensure shebang + chmod +x
3. write manifest at:
     ~/.config/google-chrome/NativeMessagingHosts/com.chromatika.mcp_host.json
   with path = absolute path to hostBinary
\`\`\`

## library

- \`node:fs\` for writing the manifest
- \`node:os\` for \`homedir()\` resolution
- \`node:child_process\` for the Windows \`reg add\` invocation
- internal: \`wallet-extension/native-host/setup.mjs\` for the orchestration

## related

- [mcp-protocol-overview.md](/library/tech/mcp-protocol-overview) - what the host serves once spawned
- [mcp-native-messaging-frame.md](/library/tech/mcp-native-messaging-frame) - the wire format chrome ↔ host uses
- [mcp-http-transport.md](/library/tech/mcp-http-transport) - the localhost HTTP MCP the host opens
- [mcp-stdio-bridge.md](/library/tech/mcp-stdio-bridge) - the alternate \`--stdio-bridge\` invocation mode
`,Rb=`# MCP HTTP transport (\`POST /mcp\`)

the chromatika native messaging host hosts a localhost HTTP listener that speaks JSON-RPC 2.0 MCP. agent clients (Claude Desktop in HTTP MCP mode, Cursor, Cline) connect to \`http://127.0.0.1:<port>/mcp\` with a bearer token and call MCP methods. each request is one JSON-RPC frame; the response is one JSON-RPC response.

## the listener

\`\`\`
http://127.0.0.1:<port>/mcp
\`\`\`

- \`127.0.0.1\` only (never bound to \`0.0.0.0\`)
- port is **random by default** unless the user pinned a fixed port via settings (see [mcp-reconfigure-port.md](/library/tech/mcp-reconfigure-port))
- single endpoint: \`POST /mcp\`. no other paths. GETs return 404
- spawned by the native messaging host using node's \`node:http\` \`createServer\`

## request

\`\`\`http
POST /mcp HTTP/1.1
Host: 127.0.0.1:<port>
Content-Type: application/json
Authorization: Bearer <tokenHex>
Content-Length: <N>

{"jsonrpc":"2.0","id":"abc-123","method":"tools/list","params":{}}
\`\`\`

- \`Authorization: Bearer <tokenHex>\` is **required**. missing or wrong token returns \`401 Unauthorized\`
- \`Content-Type\` should be \`application/json\`
- body is a single JSON-RPC 2.0 frame

## response

\`\`\`http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: <N>

{"jsonrpc":"2.0","id":"abc-123","result":{"tools":[...]}}
\`\`\`

or for errors:
\`\`\`jsonc
{
  "jsonrpc": "2.0",
  "id": "abc-123",
  "error": {
    "code": -32603,
    "message": "Internal error",
    "data": { ... }
  }
}
\`\`\`

JSON-RPC error codes used by chromatika:
- \`-32700\` parse error
- \`-32600\` invalid request
- \`-32601\` method not found
- \`-32602\` invalid params
- \`-32603\` internal error
- \`-32000\` to \`-32099\` reserved for chromatika-specific errors (e.g. wallet locked, user canceled, timeout)

HTTP status codes:
- \`200 OK\` for any well-formed JSON-RPC response (whether \`result\` or \`error\`)
- \`400 Bad Request\` for non-JSON or malformed body
- \`401 Unauthorized\` for missing / wrong bearer token
- \`404 Not Found\` for any path other than \`/mcp\`
- \`405 Method Not Allowed\` for non-POST requests
- \`500 Internal Server Error\` for host-side crashes (rare; usually still returns JSON-RPC error)

## the auth flow

1. extension generates a per-install random token at first MCP enable: \`tokenHex = crypto.getRandomValues(32 bytes).toHex()\`
2. extension persists \`chromatika_mcp_v1.tokenHex\` in \`chrome.storage.local\`
3. on \`chrome.runtime.connectNative\`, extension pushes \`{ type: 'config', tokenHex }\` to the host
4. host stores it in memory: \`STATE.tokenHex = msg.tokenHex\`
5. on every HTTP request, host validates \`Authorization: Bearer <token>\` against \`STATE.tokenHex\`
6. on rotate (\`mcpRotateToken\`), extension generates a new random token, pushes a fresh \`{ type: 'config', tokenHex }\` to the host - the new token takes effect immediately for subsequent requests; existing in-flight requests with the old token complete normally

see [mcp-bearer-token-auth.md](/library/tech/mcp-bearer-token-auth) for the full auth flow.

## why HTTP and not pure stdio

- HTTP MCP transport is a first-class option in the MCP spec. clients like Cursor / Cline support it natively
- localhost HTTP is testable via \`curl\` from the user's terminal - useful for debugging
- the host can serve multiple agent clients simultaneously over HTTP without managing concurrent stdio frames
- stdio bridge mode (see [mcp-stdio-bridge.md](/library/tech/mcp-stdio-bridge)) layers on top: the bridge process forwards stdio MCP to this HTTP transport, letting Claude Desktop (which only speaks stdio) drive the wallet

## the request → tool-call lifecycle

\`\`\`
1. POST /mcp with method='tools/call' arrives
2. host validates auth
3. host parses JSON-RPC, extracts method + params
4. host writes a native-messaging frame to chrome:
   { type: 'tool-call', id: <correlation-id>, method, params }
5. host registers a pending Map entry { id → resolve, timeout }
6. extension service worker handles the tool-call, opens popups if needed,
   eventually responds with { type: 'tool-result', id, result|error }
7. host pops the pending entry, resolves the HTTP response
8. host writes JSON-RPC response back over HTTP
9. timeout: 5 minutes (TOOL_CALL_TIMEOUT_MS = 300_000). on timeout, return JSON-RPC error
   with code -32603 and message 'tool call timed out'
\`\`\`

read-tier tools (\`listVaults\`, \`getLockState\`, etc.) resolve in well under a second. approve-tier tools wait on the user clicking through a popup - 5 min is the cap to allow careful reads.

## a working \`curl\` example

\`\`\`sh
TOKEN=$(node -e "console.log(JSON.parse(fs.readFileSync('/path/to/chrome.storage.local')).chromatika_mcp_v1.tokenHex)")
curl -X POST http://127.0.0.1:54321/mcp \\
  -H "Authorization: Bearer $TOKEN" \\
  -H "Content-Type: application/json" \\
  -d '{"jsonrpc":"2.0","id":"1","method":"initialize","params":{}}'
\`\`\`

response:
\`\`\`json
{"jsonrpc":"2.0","id":"1","result":{"protocolVersion":"2025-03-26","serverInfo":{"name":"chromatika","version":"0.0.1"},"capabilities":{"tools":{"listChanged":false}}}}
\`\`\`

## CORS and security

- localhost HTTP only (\`127.0.0.1\`)
- no CORS - the listener doesn't set \`Access-Control-Allow-Origin\` headers, so browsers can't reach it from arbitrary websites (browsers block fetch to localhost without CORS, and the chrome extension itself uses native messaging anyway, not HTTP)
- agent clients are typically native processes (Claude Desktop, Cursor) - they don't run in browsers, so CORS doesn't apply
- bearer token gates everything; without the token, an attacker on the same machine can't call MCP methods

threat model: any local process running as the same user **could** read \`chrome.storage.local\` (chrome's data dir is user-scoped) and grab the token. that's why the user-facing UI lets the user rotate the token at any time. for higher-trust deployments, ship a wrapper that restricts host spawning behind additional auth (out of scope for chromatika today).

## library

- \`node:http\` for the listener
- node-native \`Buffer\`, \`JSON\`, etc.
- no npm deps
- chromatika extension side: \`mcp-storage.ts\` for \`chromatika_mcp_v1\` persistence, \`mcp-native-bridge.ts\` for the connect / disconnect logic

## related

- [mcp-bearer-token-auth.md](/library/tech/mcp-bearer-token-auth) - token generation / rotation / validation
- [mcp-tool-routing.md](/library/tech/mcp-tool-routing) - tool-call / tool-result correlation
- [mcp-stdio-bridge.md](/library/tech/mcp-stdio-bridge) - the stdio companion for clients that don't speak HTTP MCP
- [mcp-reconfigure-port.md](/library/tech/mcp-reconfigure-port) - live port rebind
`,Mb=`# \`listActiveAlerts\` MCP read-tier tool

new MCP read-tier tool that exposes chromatika's safety alerts to AI agents (Claude Desktop, Cursor, Cline, etc.). agents can query active alerts before recommending a dapp connection or transaction. no popup, no signing, no wallet-state mutation. read-only. auto-rejects when wallet is locked.

## the tool descriptor

\`\`\`jsonc
{
  "name": "listActiveAlerts",
  "description": "Returns active (non-dismissed, non-expired) chromatika safety alerts. Use to check whether a dapp domain is flagged as phishing/malicious before recommending interaction. Supports optional severity floor and domain filter. Read-only; no popup.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "domain": { "type": "string", "description": "Optional domain to filter on (case-insensitive substring match). If provided, returns only alerts whose affectedDomains include this domain." },
      "severity": { "type": "string", "enum": ["critical", "warning", "info"], "description": "Optional severity floor. 'critical' returns only critical; 'warning' returns critical+warning; 'info' returns all." }
    }
  }
}
\`\`\`

## the response shape

\`\`\`jsonc
{
  "alerts": [
    {
      "id": "uniswap-clone-2026-04-29",
      "severity": "critical",
      "timestampMs": 1712345678000,
      "expiresAtMs": 1712950478000,
      "affectedDomains": ["uniswap-clone-evil.io"],
      "affectedChains": ["evm"],
      "titleShort": "phishing uniswap clone draining USDC",
      "bodyLong": "Two domains run an exact uniswap v4 UI clone..."
    }
    // ... more alerts ...
  ],
  "lastPolledAtMs": 1712346000000,
  "lastPollError": null,
  "muted": false,
  "optedOut": false
}
\`\`\`

\`alerts\` is the filtered + sorted active list. UI metadata (\`lastPolledAtMs\`, \`muted\`, \`optedOut\`) is included so agents can surface "your alerts are muted" or "your last poll failed" context.

\`publisherKeyB64\` and \`signatureB64\` are **stripped** from the response - the alert was already verified by the wallet on ingest; agents don't need to re-verify and shouldn't be in the trust loop.

## the implementation

dispatched in \`src/background/mcp/mcp-tools.ts\` \`dispatchMcpToolCall\`:

\`\`\`ts
case 'listActiveAlerts': {
  const { listActiveAlertsForMcp } = await import('@/background/alerts/alerts-store');
  const result = await listActiveAlertsForMcp(params);
  return { ok: true, result };
}
\`\`\`

\`listActiveAlertsForMcp({ domain?, severity? })\` lives in \`alerts-store.ts\`:

\`\`\`ts
async function listActiveAlertsForMcp({ domain, severity }: { domain?: string, severity?: 'critical'|'warning'|'info' }): Promise<...> {
  const state = await getAlertsState();

  // active = non-dismissed, non-expired
  let active = activeAlertsFromState(state);

  // severity floor
  if (severity === 'critical') active = active.filter(a => a.severity === 'critical');
  else if (severity === 'warning') active = active.filter(a => a.severity === 'critical' || a.severity === 'warning');
  // 'info' = no filter (default)

  // domain filter (case-insensitive substring on any affectedDomain)
  if (domain) {
    const needle = domain.toLowerCase();
    active = active.filter(a => a.affectedDomains.some(d => d.toLowerCase().includes(needle)));
  }

  // strip pubkey + signature for the MCP response
  const sanitized = active.map(a => ({
    id: a.id,
    severity: a.severity,
    timestampMs: a.timestampMs,
    expiresAtMs: a.expiresAtMs,
    affectedDomains: a.affectedDomains,
    affectedChains: a.affectedChains,
    titleShort: a.titleShort,
    bodyLong: a.bodyLong,
  }));

  return {
    alerts: sanitized,
    lastPolledAtMs: state.lastPolledAtMs,
    lastPollError: state.lastPollError,
    muted: state.settings.muted,
    optedOut: state.settings.optedOut,
  };
}
\`\`\`

## the read-tier classification

per [mcp-protocol-overview.md](/library/tech/mcp-protocol-overview), MCP tools split into:
- **read tier** (no popup): \`listVaults\`, \`getActiveVault\`, \`getActiveNetworks\`, \`getLockState\`, **and now \`listActiveAlerts\`**
- **approve tier** (popup-gated): \`signMessage\`, \`sendEvmTx\`, \`signTransaction\`

\`listActiveAlerts\` joins read-tier because:
- it's read-only (no state mutation)
- the data is already-verified safety info (no signing needed)
- agents need fast access to flag dangerous domains before recommending interaction
- popping a UI for every agent query would defeat the use case

## the lock check

read-tier still respects the wallet lock state. if the wallet is locked, the tool returns JSON-RPC error \`-32001\` ("wallet locked") rather than reading from storage.

\`\`\`ts
if (!sessionState.unlocked) {
  return { ok: false, error: { code: -32001, message: 'wallet locked' } };
}
\`\`\`

rationale: alerts data is technically not user-secret (the feed is public), but the **active vault state** + **session metadata** in the response (muted/optedOut settings are user choices) is. simpler to gate on lock than to special-case "alerts are public, but settings are private."

## the agent use case

example agent prompt: "I want to swap USDC for ETH on uniswap.org. is this safe?"

agent flow:
1. \`tools/call listActiveAlerts({ domain: 'uniswap.org' })\` 
2. response: \`{ alerts: [], ... }\` (or \`{ alerts: [...] }\` with phishing matches)
3. agent reasons: empty result = no flags = recommend with normal caution. non-empty = surface the alert content to the user before any interaction
4. agent then might call \`sendEvmTx\` (approve tier) or just inform the user

## privacy properties for agents

- agents calling \`listActiveAlerts\` see verified alerts but cannot inject or modify alerts. they're consumers of the same feed the user sees
- agents see \`muted\` / \`optedOut\` settings. an agent could in principle nag the user about being muted; the user can ignore the nag. it's metadata, not authority
- agents do **not** see \`publisherKeyB64\` / \`signatureB64\`. the wallet is the trust anchor; agents don't re-verify

## what about subscription / push?

current MCP transport is request/response only (no SSE for server push). so agents have to **poll** \`listActiveAlerts\` to get fresh alerts. the tool description tells them to call before recommending a domain - no need for push if the agent always queries before acting.

future: when SSE arrives, push \`notifications/alerts/changed\` so agents can re-fetch when new alerts land. tracked in the broader MCP roadmap.

## library

- internal: \`src/background/mcp/mcp-tools.ts\` for the dispatch
- internal: \`src/background/alerts/alerts-store.ts\` for \`listActiveAlertsForMcp\`, \`getAlertsState\`, \`activeAlertsFromState\`

## related

- [mcp-protocol-overview.md](/library/tech/mcp-protocol-overview) - the broader MCP method set
- [mcp-tool-routing.md](/library/tech/mcp-tool-routing) - how tool-calls correlate
- [alerts-overview.md](/library/tech/alerts-overview) - the underlying alerts subsystem
- [alerts-signed-feed-format.md](/library/tech/alerts-signed-feed-format) - the verification that produced the data this tool returns
`,Db=`# chrome native messaging frame protocol

chrome native messaging is the channel between the chromatika service worker (via \`chrome.runtime.connectNative\`) and the spawned native host process (\`chromatika-mcp-host.mjs\`). it's a tiny binary protocol: each message is a **4-byte little-endian length prefix** followed by **UTF-8 JSON** body.

## the wire format

\`\`\`
+------------+-----------------+
| 4-byte LE  |  UTF-8 JSON     |
| length N   |  N bytes body   |
+------------+-----------------+
\`\`\`

example: sending \`{"type":"ping"}\` (15 bytes UTF-8):
\`\`\`
[0x0F, 0x00, 0x00, 0x00] [0x7B, 0x22, 0x74, 0x79, 0x70, 0x65, 0x22, 0x3A, 0x22, 0x70, 0x69, 0x6E, 0x67, 0x22, 0x7D]
   ^ length = 15           ^ {"type":"ping"}
\`\`\`

the prefix is **little-endian uint32**. on the host side:
\`\`\`js
function sendFrame(obj) {
  const body = Buffer.from(JSON.stringify(obj), 'utf8');
  const lenBuf = Buffer.alloc(4);
  lenBuf.writeUInt32LE(body.length, 0);
  STDOUT.write(Buffer.concat([lenBuf, body]));
}
\`\`\`

reading is the mirror image - read 4 bytes, parse as LE u32, read N more bytes, JSON.parse. the host has a buffered reader because chrome may split a single send across multiple stdin reads:

\`\`\`js
let buffer = Buffer.alloc(0);

function processBuffer() {
  while (buffer.length >= 4) {
    const msgLen = buffer.readUInt32LE(0);
    if (buffer.length < 4 + msgLen) break;   // wait for more bytes
    const body = buffer.subarray(4, 4 + msgLen);
    buffer = buffer.subarray(4 + msgLen);
    const msg = JSON.parse(body.toString('utf8'));
    handleMessage(msg);
  }
}
\`\`\`

## the 1 MiB limit

chrome rejects any single message larger than **1 MiB** in either direction (extension → host or host → extension). chromatika doesn't come close - the largest frames are tool-result responses with signature bytes, well under 10 KB.

if you ever need to send more (e.g. a large NFT inventory dump), split into multiple frames at the application layer. don't try to bypass chrome's limit.

## how chrome spawns the host

\`chrome.runtime.connectNative('com.chromatika.mcp_host')\` looks up the native messaging manifest by name in OS-specific locations:
- **Windows**: \`HKEY_CURRENT_USER\\SOFTWARE\\Google\\Chrome\\NativeMessagingHosts\\com.chromatika.mcp_host\` registry key pointing at the manifest JSON
- **macOS**: \`~/Library/Application Support/Google/Chrome/NativeMessagingHosts/com.chromatika.mcp_host.json\`
- **Linux**: \`~/.config/google-chrome/NativeMessagingHosts/com.chromatika.mcp_host.json\`

the manifest declares the host binary path + which extension ids are allowed to connect:
\`\`\`jsonc
{
  "name": "com.chromatika.mcp_host",
  "description": "Chromatika MCP native messaging host",
  "path": "/absolute/path/to/chromatika-mcp-host.mjs",
  "type": "stdio",
  "allowed_origins": ["chrome-extension://<your-extension-id>/"]
}
\`\`\`

\`pnpm setup:native-host --extension-id=<id>\` writes this manifest to the right OS-specific directory. on Windows it also drops a \`.bat\` shim and runs \`reg add\` for the registry entry.

## the connection lifecycle

\`\`\`
1. service worker calls chrome.runtime.connectNative('com.chromatika.mcp_host')
2. chrome looks up manifest, spawns the host as a child process with stdio piped
3. host begins reading 4-byte-prefixed frames from stdin
4. service worker pushes the first frame: { type: 'config', tokenHex: '<...>' }
5. host echoes { ok: true, kind: 'config-ack', hasToken: true }
6. host opens an HTTP listener (see mcp-http-transport.md), writes back { kind: 'listen', host: '127.0.0.1', port }
7. service worker stores the port for the agent URL
8. on every MCP call, host writes { type: 'tool-call', id, method, params } to stdout (toward extension);
   service worker handles, replies { type: 'tool-result', id, result|error }
9. on disconnect (extension side or host crash), the channel closes; chrome.runtime.connectNative returns
   onDisconnect; chromatika reconnects with exp backoff (5 attempts, 1s → 30s)
\`\`\`

## frame types chromatika sends

extension → host:
- \`{ type: 'config', tokenHex }\` - pushed on connect; sets the bearer token
- \`{ type: 'tool-result', id, result }\` - response to a tool-call
- \`{ type: 'tool-result', id, error }\` - error response
- \`{ type: 'reconfigure-port', port }\` - live port rebind request
- \`{ type: 'ping' }\` - liveness check

host → extension:
- \`{ ok: true, kind: 'config-ack', hasToken }\` - config acknowledged
- \`{ kind: 'listen', host, port }\` - HTTP listener bound
- \`{ kind: 'rebind-error', desiredPort, error }\` - failed to rebind to user-supplied port
- \`{ ok: true, kind: 'pong', tsMs }\` - ping response
- \`{ type: 'tool-call', id, method, params }\` - forward an MCP tool call to the extension
- \`{ ok: true, kind: 'echo', received }\` - debug echo for unknown frames

## the \`setupBuffer\` quirk

chrome may write multiple frames worth of bytes before the host's first read. the host accumulates everything in a \`buffer\` and processes complete frames in a loop (\`while (buffer.length >= 4 + msgLen)\`). this is correct for both single-frame writes and merged-write cases.

if you implement a similar host, **don't** assume one read = one frame. always buffer + parse-while-complete-frame-available.

## errors

- \`parse error: Unexpected token\`: a frame body isn't valid UTF-8 JSON. log to stderr, skip
- \`expected json object\`: a frame body is valid JSON but not an object (e.g. a literal string). log, skip
- \`stdout write failed\`: the parent (chrome) died. exit cleanly

stderr is the only diagnostic channel; chrome logs native host stderr to its own internal logs (\`chrome://extensions\` developer mode + chrome's stderr if launched from a terminal).

## library

- node.js built-ins: \`node:http\`, \`node:crypto\`, \`node:readline\`. **no npm dependencies**
- chromatika side: \`chrome.runtime.connectNative\` is browser native; \`mcp-native-bridge.ts\` wraps it for the service worker

## related

- [mcp-http-transport.md](/library/tech/mcp-http-transport) - what the host serves at \`127.0.0.1:<port>/mcp\`
- [mcp-tool-routing.md](/library/tech/mcp-tool-routing) - how \`tool-call\` and \`tool-result\` frames correlate
- [mcp-bearer-token-auth.md](/library/tech/mcp-bearer-token-auth) - the \`{ type: 'config', tokenHex }\` frame
- [mcp-reconfigure-port.md](/library/tech/mcp-reconfigure-port) - the \`{ type: 'reconfigure-port', port }\` frame
`,Bb=`# MCP protocol overview (chromatika)

chromatika exposes a **Model Context Protocol** (MCP) surface so AI agents (Claude Desktop, Cursor, Cline, Zed, etc.) can read wallet state and request signed actions, with user approval popups for anything that signs. MCP is Anthropic's spec for "tools an LLM can call". chromatika hand-rolls the protocol over JSON-RPC 2.0 - **no \`@modelcontextprotocol/sdk\` dependency**. the native messaging host (\`wallet-extension/native-host/chromatika-mcp-host.mjs\`) is a "zero-deps node script".

## protocol version

\`\`\`js
const MCP_PROTOCOL_VERSION = '2025-03-26';
\`\`\`

defined in the native host. clients negotiate this on \`initialize\`. when MCP spec bumps, update this constant + verify backward-compat with deployed agent clients.

## the methods we implement

JSON-RPC 2.0 methods chromatika serves over \`POST /mcp\`:

- \`initialize\` - client / server handshake. server returns \`{ protocolVersion, serverInfo, capabilities }\`
- \`tools/list\` - server returns the list of tools (name, description, JSON schema for params)
- \`tools/call\` - client invokes a tool by name with params. server returns the tool's structured result or an error

we **don't** implement (yet):
- \`tools/list_changed\` server-to-client notifications (no SSE transport yet)
- \`prompts/*\` (prompt templates surface)
- \`resources/*\` (resource discovery surface)
- \`logging/*\` (server-pushed log streaming)
- \`sampling/*\` (server requesting LLM sampling on the client)

these are MCP capabilities chromatika doesn't need today. the spec is layered - you can implement just \`tools/*\` and that's a valid minimal MCP server.

## server info we report

\`\`\`jsonc
{
  "name": "chromatika",
  "version": "0.0.1"
}
\`\`\`

returned in the \`initialize\` response. clients use this to display "connected to chromatika 0.0.1" in their agent settings.

## capabilities

\`\`\`jsonc
{
  "tools": {
    "listChanged": false   // we don't push tools/list_changed notifications today
  }
}
\`\`\`

minimal capability declaration. when SSE arrives, flip \`listChanged: true\`.

## the wire format

JSON-RPC 2.0 over either:
1. **HTTP** - \`POST http://127.0.0.1:<port>/mcp\` with \`Authorization: Bearer <tokenHex>\` and \`Content-Type: application/json\`. body is one JSON-RPC request, response is one JSON-RPC response. see [mcp-http-transport.md](/library/tech/mcp-http-transport)
2. **stdio bridge** - line-delimited JSON-RPC 2.0 on stdin / stdout. forwards to the HTTP transport. see [mcp-stdio-bridge.md](/library/tech/mcp-stdio-bridge)

every JSON-RPC frame has the standard shape:
\`\`\`jsonc
// request
{ "jsonrpc": "2.0", "id": "abc-123", "method": "tools/call", "params": { ... } }

// response
{ "jsonrpc": "2.0", "id": "abc-123", "result": { ... } }
// or
{ "jsonrpc": "2.0", "id": "abc-123", "error": { "code": -32603, "message": "...", "data": ... } }
\`\`\`

\`id\` is whatever the client sets - chromatika echoes it. this is how clients correlate request → response.

## the read tier (no popup)

these tools resolve immediately, no user interaction:
- \`listVaults\` - returns vault metadata (id, label, base chain, primary credential type, dWallet count)
- \`getActiveVault\` - returns the currently active vault id + summary
- \`getActiveNetworks\` - returns active EVM chain id, active Solana network, active Sui network, etc.
- \`getLockState\` - returns \`{ locked: bool, autoLockMinutes? }\`

internal implementation: each maps to an existing tRPC procedure. the native bridge correlates \`tool-call\` envelopes back to \`tool-result\` envelopes (see [mcp-tool-routing.md](/library/tech/mcp-tool-routing)).

## the approve tier (popup-gated)

these tools open a chromatika popup for user approval before signing:
- \`signMessage({ chain: 'evm' | 'solana', messageHex, evmChainId? })\` - opens \`McpApprovalScreen\` → ika MPC signs → returns \`{ chain, signatureHex, signerAddress }\`
- \`sendEvmTx({ to, value?, data?, chainId?, gas?, ... })\` - opens existing \`ApproveTxScreen\` with full gas / sim UI → \`signAndBroadcastEvm\` → returns broadcast tx hash
- \`signTransaction({ to, value?, data?, chainId?, gas?, ... })\` - same popup with "sign only - no broadcast" banner → \`signEvmTxOnly\` → returns \`{ signedRawTx, txHash }\`

the popup-gated tools share the same wait pattern: tool-call enqueues a request, opens the popup, waits up to 5 minutes (\`TOOL_CALL_TIMEOUT_MS = 300_000\`) for resolution. if the user closes the popup or doesn't respond, the tool call returns an error.

## tool definitions surface

tools are described to the LLM via JSON Schema:
\`\`\`jsonc
{
  "name": "signMessage",
  "description": "Sign an arbitrary message with the active dWallet. Opens a user approval popup. Returns { chain, signatureHex, signerAddress }.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "chain": { "type": "string", "enum": ["evm", "solana"] },
      "messageHex": { "type": "string", "pattern": "^(0x)?[0-9a-fA-F]+$" },
      "evmChainId": { "type": "integer", "minimum": 1, "nullable": true }
    },
    "required": ["chain", "messageHex"]
  }
}
\`\`\`

the LLM reads \`description\` + \`inputSchema\` to decide when / how to call the tool. clear, minimal descriptions matter - the LLM only knows what we tell it via these fields.

## why hand-roll vs \`@modelcontextprotocol/sdk\`

the SDK is great for production servers but adds dep weight and abstractions chromatika doesn't need:
- chromatika's MCP server is **stateless beyond the per-call envelope correlation**. no session memory, no streaming, no resource subscriptions
- chromatika needs to bridge **chrome native messaging** (4-byte LE length prefix) on one side and **HTTP MCP** on the other - the SDK doesn't have a chrome-native-messaging transport
- a "zero-deps node script" is easier to ship as a setup artifact (\`pnpm setup:native-host\` writes the script to the right OS-specific native messaging directory) - no node_modules to bundle
- the protocol itself is small. the entire native host is ~500 lines

if MCP gains complexity (SSE, sampling, resources), revisit.

## related docs

- [mcp-native-messaging-frame.md](/library/tech/mcp-native-messaging-frame) - how chrome ↔ host frames work
- [mcp-http-transport.md](/library/tech/mcp-http-transport) - the localhost HTTP MCP listener
- [mcp-stdio-bridge.md](/library/tech/mcp-stdio-bridge) - the \`--stdio-bridge\` mode for Claude Desktop
- [mcp-tool-routing.md](/library/tech/mcp-tool-routing) - tool-call envelope correlation through native messaging
- [mcp-bearer-token-auth.md](/library/tech/mcp-bearer-token-auth) - per-install token + rotation
- [mcp-reconfigure-port.md](/library/tech/mcp-reconfigure-port) - live port rebind protocol
- [mcp-host-spawn-and-setup.md](/library/tech/mcp-host-spawn-and-setup) - per-OS native host registration
`,Lb=`# MCP \`reconfigure-port\` live rebind

by default, the chromatika MCP HTTP listener binds to a **random** port on \`127.0.0.1\`. that's fine until you want to put the URL in Claude Desktop's MCP config (which doesn't reload on chrome restart) - the port changes after every chrome unloads the SW + respawns the host. the fix: \`mcpSetDesiredPort\` lets the user pin a fixed port (1024-65535). the extension pushes that desired port to the running host via a \`{ type: 'reconfigure-port', port }\` frame. the host opens a new listener on the desired port, **then** tears down the old one - if the new bind fails, the host stays on the current port and reports the error.

## why this is non-trivial

if we just close the old listener and try to open a new one, we have a brief window where neither is bound. agent clients with in-flight requests would see connection-refused. the live rebind handles this by:

1. building a replacement server first
2. binding it to the desired port
3. on success, closing the old listener
4. on failure, throwing away the replacement and keeping the old listener alive

it's a "open new before closing old" handoff with rollback on failure.

## the frame

\`\`\`jsonc
// extension → host
{
  "type": "reconfigure-port",
  "port": 54321
}
\`\`\`

port must be \`1024 ≤ port ≤ 65535\`. ports below 1024 require root on linux/mac and admin on windows; chromatika doesn't support that.

## the host-side handler

\`\`\`js
async function rebindToDesiredPort(desiredPort) {
  const currentAddr = server?.address?.();
  const currentPort = typeof currentAddr === 'object' && currentAddr ? currentAddr.port : null;

  // already on the right port - nothing to do
  if (currentPort === desiredPort) {
    sendFrame({ kind: 'listen', host: '127.0.0.1', port: desiredPort });
    return;
  }

  const oldServer = server;
  // build replacement first; only swap after successful bind
  const newServer = createServer(httpHandler);
  newServer.on('error', (e) => {
    logErr('replacement server error:', e?.message ?? e);
  });

  await new Promise((resolve) => {
    newServer.once('error', (e) => {
      logErr(\`rebind to port \${desiredPort} failed: \${e?.message ?? e}; staying on current port \${currentPort}\`);
      sendFrame({ kind: 'rebind-error', desiredPort, error: e?.message ?? String(e) });
      try { newServer.close(); } catch { /* noop */ }
      resolve();
    });
    newServer.listen(desiredPort, '127.0.0.1', () => {
      // bind success - tear down the old listener
      try { oldServer.close(); }
      catch (e) { logErr('failed to close prior listener after rebind:', e?.message ?? e); }
      server = newServer;
      sendFrame({ kind: 'listen', host: '127.0.0.1', port: desiredPort });
      logErr(\`chromatika-mcp-host rebound to 127.0.0.1:\${desiredPort}\`);
      resolve();
    });
  });
}
\`\`\`

key invariants:
- if \`desiredPort\` matches \`currentPort\`, no-op (don't touch a working listener)
- on bind error, stay on the current listener; report \`kind: 'rebind-error'\` with the error message
- on bind success, swap - new listener becomes \`server\`, old listener closes
- in-flight requests on the old listener complete normally (node's \`server.close()\` waits for active connections to finish before tearing down the listening socket)

## extension-side coordination

\`\`\`ts
async function pushDesiredPortToHost(port: number | null) {
  if (!nativePort) return;
  if (port === null) {
    // user cleared the desired port - host should fall back to a random port
    // this is a pseudo-rebind: pick a random ephemeral port and reconfigure
    nativePort.postMessage({ type: 'reconfigure-port', port: pickRandomPort() });
  } else {
    nativePort.postMessage({ type: 'reconfigure-port', port });
  }
}
\`\`\`

a \`null\` desired-port means "use a random port"; the extension picks a random ephemeral and sends that. there's no "go back to whatever you originally bound" frame because the host doesn't remember its initial port - it just takes whatever port it's currently on.

## the chrome-restart behavior

when chrome restarts:
1. service worker spawns cold
2. SW reads \`chromatika_mcp_v1.desiredListenPort\` from storage
3. SW reconnects to the native host via \`chrome.runtime.connectNative\`
4. host spawns fresh, binds to **a random port** initially (it doesn't know about the desired port yet)
5. host sends \`{ kind: 'listen', host: '127.0.0.1', port: <random> }\` via native messaging
6. SW receives, **then** sends \`{ type: 'reconfigure-port', port: <desiredPort> }\` if a desired port is set
7. host rebinds to the desired port, sends fresh \`kind: 'listen'\` frame
8. SW updates \`chromatika_mcp_v1.listenPort\` to the desired port

end result: after chrome restart, the listener is on \`desiredPort\` (or random if none was pinned). the rebind takes <100ms typically.

## error: bind failure

if the desired port is taken (another process bound it, or chromatika was started before the previous instance fully released the socket), the host reports:

\`\`\`jsonc
// host → extension
{
  "kind": "rebind-error",
  "desiredPort": 54321,
  "error": "EADDRINUSE: address already in use 127.0.0.1:54321"
}
\`\`\`

the extension surfaces this in the agent-settings UI as "couldn't bind requested port". the listener stays on whatever port it was on before (random if just spawned). user options: pick a different port, find what's using 54321 and kill it, retry.

## why not declare the port up-front in the native messaging manifest

native messaging manifests don't have a port field. they just declare the host binary path. the host process is responsible for any networking it does. plus, putting a port in the manifest would freeze it across all installs and conflict with users who want different ports.

## the \`setupBuffer\` quirk during rebind

when the host rebinds, the **old** server is in the middle of serving any in-flight requests. node's \`server.close()\` waits for active connections; new connections to the old port refuse immediately. so:
- new requests during rebind: routed to the new listener
- in-flight requests during rebind: complete on the old listener
- post-rebind: only the new listener accepts connections

agent clients with persistent connections to the old port may see disconnects when their connection drains; they reconnect to the new URL. acceptable.

## library

- \`node:http\` \`createServer\`, \`server.listen\`, \`server.close\`
- \`chrome.storage.local\` to persist \`desiredListenPort\`
- \`chrome.runtime.connectNative\` \`port.postMessage\` for the frame

## related

- [mcp-http-transport.md](/library/tech/mcp-http-transport) - the HTTP listener that gets rebound
- [mcp-native-messaging-frame.md](/library/tech/mcp-native-messaging-frame) - the frame format under \`reconfigure-port\`
- [mcp-host-spawn-and-setup.md](/library/tech/mcp-host-spawn-and-setup) - per-OS host registration that makes connectNative work in the first place
`,Nb=`# MCP stdio bridge mode (\`--stdio-bridge\`)

Claude Desktop's default MCP transport is **stdio** - the client spawns a child process and exchanges line-delimited JSON-RPC frames over stdin / stdout. chromatika's native host, when invoked with \`--stdio-bridge\`, acts as a thin shim: read JSON-RPC from stdin, forward to the chrome-spawned HTTP MCP host, write the response back to stdout. this lets stdio-only clients drive chromatika.

## why a separate mode

the chrome-spawned native messaging host (no flag) reads **4-byte-LE-length-prefixed** frames from stdin (chrome's wire format) and serves HTTP MCP on a localhost port. but Claude Desktop sends **line-delimited JSON** without length prefixes. we can't make one process speak both at the same time, so the same binary supports two modes:

| invocation | who spawns it | reads from stdin | writes to stdout | also serves HTTP MCP |
|------------|---------------|------------------|------------------|---------------------|
| (no flag) | chrome via \`chrome.runtime.connectNative\` | 4-byte-LE-length frames | 4-byte-LE-length frames | yes, on \`127.0.0.1:<port>/mcp\` |
| \`--stdio-bridge\` | stdio MCP client (Claude Desktop) | line-delimited JSON | line-delimited JSON | no - bridges to the existing HTTP transport |

both modes run from the **same binary** (\`chromatika-mcp-host.mjs\`). the flag dispatches at startup.

## startup

\`\`\`js
const STDIO_BRIDGE_FLAG = '--stdio-bridge';
const isStdioBridge = process.argv.includes(STDIO_BRIDGE_FLAG);

if (isStdioBridge) {
  await runStdioBridge();
  process.exit(0);
}

// otherwise: native messaging host mode
\`\`\`

\`runStdioBridge\` reads \`$CHROMATIKA_AGENT_URL\` (e.g. \`http://127.0.0.1:54321/mcp\`) and \`$CHROMATIKA_AGENT_TOKEN\` (the per-install bearer token) from environment variables. these point at the chrome-spawned HTTP MCP listener.

## the bridge loop

\`\`\`js
async function runStdioBridge() {
  const url = process.env.CHROMATIKA_AGENT_URL;
  const token = process.env.CHROMATIKA_AGENT_TOKEN;
  if (!url || !token) {
    console.error('missing CHROMATIKA_AGENT_URL or CHROMATIKA_AGENT_TOKEN env vars');
    process.exit(1);
  }

  const rl = createInterface({ input: process.stdin });

  for await (const line of rl) {
    if (!line.trim()) continue;

    let req;
    try { req = JSON.parse(line); }
    catch (e) {
      stdoutWrite({ jsonrpc: '2.0', id: null, error: { code: -32700, message: 'parse error' } });
      continue;
    }

    try {
      const httpResp = await fetch(url, {
        method: 'POST',
        headers: {
          'Authorization': \`Bearer \${token}\`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(req),
      });
      const body = await httpResp.json();
      stdoutWrite(body);
    } catch (e) {
      stdoutWrite({
        jsonrpc: '2.0',
        id: req.id ?? null,
        error: { code: -32603, message: 'bridge upstream error: ' + (e.message ?? String(e)) }
      });
    }
  }

  process.exit(0);
}

function stdoutWrite(obj) {
  process.stdout.write(JSON.stringify(obj) + '\\n');
}
\`\`\`

per-line read, fetch to the HTTP MCP, write the response with a trailing newline. exits when stdin closes (parent client disconnected).

## env-var configuration (the manual update problem)

\`\`\`
CHROMATIKA_AGENT_URL=http://127.0.0.1:54321/mcp
CHROMATIKA_AGENT_TOKEN=abcdef0123...
\`\`\`

these are baked into Claude Desktop's MCP config:
\`\`\`jsonc
{
  "mcpServers": {
    "chromatika": {
      "command": "node",
      "args": ["/path/to/chromatika-mcp-host.mjs", "--stdio-bridge"],
      "env": {
        "CHROMATIKA_AGENT_URL": "http://127.0.0.1:54321/mcp",
        "CHROMATIKA_AGENT_TOKEN": "abcdef0123..."
      }
    }
  }
}
\`\`\`

since the chrome-spawned HTTP host's port is **random by default**, every chrome restart can change the port → user has to update Claude Desktop config and restart Claude. fixing this is what \`mcpSetDesiredPort\` (see [mcp-reconfigure-port.md](/library/tech/mcp-reconfigure-port)) addresses: pin a fixed port (1024-65535) so the URL stays stable across restarts.

bearer token similarly persists in chromatika's \`chromatika_mcp_v1.tokenHex\` storage, but rotating it (via \`mcpRotateToken\`) requires updating the env var too. acceptable tradeoff for now; tracked future is "durable port registration so stdio-bridge users don't need to manually update env vars."

## what the bridge doesn't do

- no caching, no rate limiting, no transformation - it's a 1:1 forwarder
- no concurrency management - JSON-RPC is request/response, line-by-line; if a client sends two requests rapidly, they're processed sequentially (the next loop iteration only starts after the previous response is written)
- no SSE or other streaming - if MCP gains server-initiated notifications (\`tools/list_changed\`, sampling, etc.), the bridge will need event-stream support. tracked future
- no auth-elevation - stdio clients pass through the same bearer token the chrome-spawned host validates

## error semantics

- non-JSON input line → JSON-RPC parse error response with \`id: null\`
- HTTP fetch error (host not running, port wrong, etc.) → JSON-RPC \`-32603\` internal error response with \`id\` echoed
- bearer token rejected by HTTP host → returns 401, bridge wraps it as JSON-RPC \`-32603\` with the 401 message

## why not implement MCP fully in the bridge

the bridge could in principle implement MCP itself instead of forwarding. but then it'd need to:
- understand chromatika's tool catalog
- talk to the chrome extension over... some other channel
- duplicate auth logic

since the chrome-spawned host already does all this and exposes HTTP MCP, the simplest design is **bridge stdio → HTTP**. one source of truth.

## library

- \`node:readline\` for line-by-line stdin
- \`fetch\` (native in Node 18+)
- environment variables via \`process.env\`
- no npm deps

## related

- [mcp-protocol-overview.md](/library/tech/mcp-protocol-overview) - MCP method set
- [mcp-http-transport.md](/library/tech/mcp-http-transport) - the upstream the bridge forwards to
- [mcp-bearer-token-auth.md](/library/tech/mcp-bearer-token-auth) - token model the bridge passes through
- [mcp-reconfigure-port.md](/library/tech/mcp-reconfigure-port) - solves the "port changes on chrome restart" pain
`,Kb=`# MCP tool-call routing and correlation

an MCP \`tools/call\` lands on the native host's HTTP listener but the actual tool execution happens **in the chromatika service worker**. this doc walks how a single agent call is routed: HTTP request → native host → native messaging frame → service worker → tRPC procedure → (popup if needed) → result frame → HTTP response.

## the correlation problem

the host needs to:
1. accept an HTTP MCP request
2. forward it to the extension
3. wait for the extension to respond
4. write the response back over HTTP

native messaging is a single bidirectional pipe. multiple in-flight tool calls can exist simultaneously (an agent might call \`listVaults\` while a previous \`signMessage\` is still waiting on user approval). we need to **correlate** which incoming response belongs to which outgoing request.

solution: **per-call correlation ids** plus a \`pending\` Map keyed by id.

## the envelope

extension ↔ host:
\`\`\`jsonc
// host → extension (initiated by HTTP MCP request arrival)
{
  "type": "tool-call",
  "id": "<random-uuid>",
  "method": "tools/call",
  "params": { "name": "signMessage", "arguments": { ... } }
}

// extension → host (response after the SW handled it)
{
  "type": "tool-result",
  "id": "<same-uuid>",
  "result": { ... }
}
// or
{
  "type": "tool-result",
  "id": "<same-uuid>",
  "error": { "code": -32603, "message": "..." }
}
\`\`\`

\`id\` is generated fresh per HTTP request via \`randomUUID()\`. it has nothing to do with the JSON-RPC \`id\` from the agent client - that's preserved separately so the HTTP response can echo it.

## the pending Map

\`\`\`js
const pending = new Map();
// id → { resolve, timeoutId, ...metadata }

function dispatchToolCall(httpId, jsonRpcReq) {
  return new Promise((resolve, reject) => {
    const correlationId = randomUUID();
    const timeoutId = setTimeout(() => {
      if (pending.has(correlationId)) {
        pending.delete(correlationId);
        reject(new Error('tool call timed out'));
      }
    }, TOOL_CALL_TIMEOUT_MS);   // 300_000 = 5 minutes

    pending.set(correlationId, { resolve, reject, timeoutId, httpId });

    sendFrame({
      type: 'tool-call',
      id: correlationId,
      method: jsonRpcReq.method,
      params: jsonRpcReq.params,
    });
  });
}

function handleToolResult(msg) {
  const entry = pending.get(msg.id);
  if (!entry) return;   // already timed out / unknown
  pending.delete(msg.id);
  clearTimeout(entry.timeoutId);
  if (msg.error) entry.resolve({ error: msg.error });
  else entry.resolve({ result: msg.result });
}
\`\`\`

\`pending\` Map gets cleared by either the response arriving or the 5-minute timeout. the entry holds the resolve / reject so the awaiting HTTP handler can complete.

## the 5-minute timeout

\`\`\`js
const TOOL_CALL_TIMEOUT_MS = 300_000;
\`\`\`

300 seconds is long enough for the user to read an approval popup carefully (especially for transactions where they need to verify destination + amount). read-tier tools resolve in well under a second; the same cap applies to both because there's no harm in giving read-tier requests a long ceiling - they'll resolve on their own.

if a popup is dismissed without user action, the SW responds with an error frame; the timeout never fires. timeout only matters if the SW itself crashes mid-call.

## extension-side handling

\`\`\`ts
// in the SW's MCP bridge handler
nativePort.onMessage.addListener(async (msg) => {
  if (msg.type !== 'tool-call') return;
  const { id, method, params } = msg;

  try {
    let result;
    if (method === 'tools/list') {
      result = await listMcpTools();
    } else if (method === 'tools/call') {
      result = await runMcpTool(params.name, params.arguments);
    } else if (method === 'initialize') {
      result = { protocolVersion: '2025-03-26', serverInfo: ..., capabilities: ... };
    } else {
      throw { code: -32601, message: \`method not found: \${method}\` };
    }
    nativePort.postMessage({ type: 'tool-result', id, result });
  } catch (e) {
    nativePort.postMessage({
      type: 'tool-result',
      id,
      error: e.code ? e : { code: -32603, message: e.message ?? String(e) },
    });
  }
});
\`\`\`

\`runMcpTool\` dispatches to the right tool implementation. for read-tier, that's a direct tRPC call to \`listVaults\` etc. for approve-tier, it's an enqueue-and-wait pattern (see below).

## approve-tier wait pattern

\`\`\`ts
async function runApproveTierTool(name, args) {
  // 1. enqueue the request in mcp-pending-queue
  const requestId = randomUUID();
  await mcpPendingQueue.enqueue({
    id: requestId,
    name,
    args,
    enqueuedAtMs: Date.now(),
  });

  // 2. open the appropriate popup
  if (name === 'signMessage') {
    chrome.windows.create({
      url: chrome.runtime.getURL(\`index.html?mcpapprove=\${requestId}\`),
      type: 'popup', width: 400, height: 700,
    });
  } else if (name === 'sendEvmTx' || name === 'signTransaction') {
    chrome.windows.create({
      url: chrome.runtime.getURL(\`index.html?txapprove=\${requestId}&mcpMode=\${name}\`),
      type: 'popup', width: 400, height: 700,
    });
  }

  // 3. await user resolution (popup calls approveMcpSign or rejectMcpSign)
  return new Promise((resolve, reject) => {
    mcpPendingQueue.onResolve(requestId, (outcome) => {
      if (outcome.kind === 'approved') resolve(outcome.payload);
      else reject({ code: -32000, message: 'user canceled', data: outcome.reason });
    });
  });
}
\`\`\`

the popup \`?mcpapprove=<id>\` calls back via tRPC (\`approvePendingMcpSign\` / \`rejectPendingMcpSign\`), which writes the outcome to the pending queue. the awaiting promise resolves and the result frame goes back through native messaging to the host, which completes the HTTP response.

## the timing

\`\`\`
0ms     agent calls POST /mcp with tools/call signMessage
+1ms    host validates auth, parses JSON-RPC
+2ms    host writes 'tool-call' frame to native messaging stdin/stdout
+5ms    extension SW receives, parses
+10ms   SW dispatches to runMcpTool('signMessage', args)
+15ms   SW enqueues pending request, opens popup window
... user reads, clicks approve ...
+45_000ms   popup calls approvePendingMcpSign tRPC
+45_001ms   tRPC handler signs via ika MPC (~300-1000ms)
+45_500ms   sign completes, popup resolves promise
+45_502ms   SW posts 'tool-result' frame back to host
+45_504ms   host pops pending entry, resolves HTTP awaiter
+45_510ms   host writes JSON-RPC response with the signature
+45_550ms   agent receives the HTTP response
\`\`\`

most of the wall-clock time is **user reading the popup**. the actual MCP machinery is sub-second.

## list-changed notifications (not implemented)

MCP spec defines a server-to-client notification \`notifications/tools/list_changed\` for telling the client "hey, the tool list updated, re-fetch via tools/list". chromatika **doesn't push these today** because:
- HTTP MCP transport is request/response only - no SSE or websocket for server push
- the tool list is static (defined in code), so list-changed events are rare

future: add SSE transport for server-to-client streams. when that lands, list-changed (and possibly progress events for long approve-tier calls) become possible.

## library

- \`randomUUID\` from \`node:crypto\` for correlation ids
- \`Map\` for the pending registry
- \`chrome.runtime.connectNative\` + \`port.postMessage\` / \`port.onMessage\` on the extension side
- internal: \`mcp-pending-queue.ts\` for the approve-tier queue + popup orchestration
- internal: \`mcp-tools.ts\` for the tool catalog + dispatch

## related

- [mcp-native-messaging-frame.md](/library/tech/mcp-native-messaging-frame) - the wire format under tool-call / tool-result
- [mcp-http-transport.md](/library/tech/mcp-http-transport) - the HTTP listener that initiates the dispatch
- [mcp-bearer-token-auth.md](/library/tech/mcp-bearer-token-auth) - validates HTTP requests before they reach the dispatcher
`,Wb=`# multi-envelope unlock design (V4)

a chromatika vault can be unlocked by **any one of several credentials**: password, passkey, hardware-wallet signature (WAAP / Seeker / WalletConnect), or BIP39 recovery words. each credential is wrapped in its own "envelope" - a small encrypted bundle that contains the master key needed to decrypt the larger vault blob.

## the picture

\`\`\`
┌─────────────────────────────────────────────────────────┐
│  chromatika_vault_v3 (AES-GCM(masterKey, vault_payload))│
│                                                         │
│   masterKey is the same for every envelope on a vault   │
│                                                         │
│   ┌────────────────────────┐                            │
│   │ PasswordEnvelope       │  argon2id(password) → mk   │
│   ├────────────────────────┤                            │
│   │ PasskeyPRFEnvelope     │  HKDF(prf_secret) → mk     │
│   ├────────────────────────┤                            │
│   │ WalletSignatureEnvelope│  HKDF(signature) → mk      │
│   ├────────────────────────┤                            │
│   │ RecoveryWordsEnvelope  │  HKDF(bip39_seed) → mk     │
│   └────────────────────────┘                            │
└─────────────────────────────────────────────────────────┘
\`\`\`

every envelope wraps the **same** master key (mk). different credentials produce different KDF inputs but each KDF output is then used to AES-GCM unwrap the master key inside its envelope.

## why envelopes (vs deriving the AES key directly from each credential)

if the AES key were derived directly from each credential, **changing the password would require re-encrypting the entire vault payload** (mnemonic, ika keys, etc.). by introducing the envelope layer, changing the password just means re-wrapping \`mk\` under a new password-derived envelope key. the inner vault payload stays sealed under the same \`mk\`.

it also lets you **add or remove envelopes** without touching the vault payload. add a passkey to an existing password-only vault → wrap \`mk\` under a new PasskeyPRFEnvelope, append it to the vault's envelope list. remove an envelope → drop it from the list.

## envelope kinds

### PasswordEnvelope

\`\`\`
{
  "kind": "password",
  "label": "primary password",
  "kdfMeta": { argon2id params, salt },
  "wrappedMasterKeyB64": "AES-GCM(envKey, masterKey, envIv)",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

unlock: \`argon2id(password, salt) → envKey → unwrap masterKey via AES-GCM\`. then \`masterKey\` decrypts the vault payload.

### PasskeyPRFEnvelope

\`\`\`
{
  "kind": "passkey-prf",
  "label": "yubikey",
  "credentialIdB64Url": "<webauthn rawId>",
  "publicKeyCompressedB64": "<33-byte secp256r1 pubkey>",
  "rpId": "<extension origin>",
  "prfSaltB64": "<32 random bytes>",
  "wrappedMasterKeyB64": "AES-GCM(envKey, masterKey, envIv)",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

unlock: webauthn assertion with \`prf.eval.first = prfSalt\` → 32-byte PRF output → \`HKDF-SHA256(prf_output, info='chromatika passkey envelope', salt=...)\` → envKey → unwrap masterKey.

determinism comes from webauthn PRF (HMAC-secret) - same credential + same salt = same output, every assertion. see [webauthn-prf-hmac-secret.md](/library/tech/webauthn-prf-hmac-secret).

### WalletSignatureEnvelope

\`\`\`
{
  "kind": "wallet-signature",
  "source": "waap" | "seeker" | "walletconnect",
  "address": "<chain address that signs>",
  "label": "seeker",
  "hint": "<auth method or transport>",
  "challenge": "<the message that the wallet signs to unlock>",
  "kdfSaltB64": "<32 random bytes>",
  "wrappedMasterKeyB64": "AES-GCM(envKey, masterKey, envIv)",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

unlock: hardware wallet signs the envelope's challenge → \`HKDF-SHA256(signature, info='chromatika wallet-signature envelope', salt=kdfSalt)\` → envKey → unwrap masterKey.

determinism comes from RFC 8032 ed25519 deterministic signing - same key + same message = same signature, every time. WAAP wallets are probed at pairing time (sign twice, compare) to verify determinism; if non-deterministic, that wallet falls back to a recovery-words envelope.

### RecoveryWordsEnvelope

\`\`\`
{
  "kind": "recovery-words",
  "wordCount": 12 | 24,
  "label": "primary recovery",
  "kdfSaltB64": "<32 random bytes>",
  "wrappedMasterKeyB64": "AES-GCM(envKey, masterKey, envIv)",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

unlock: user types BIP39 phrase → \`mnemonicToSeedSync(words, "")\` → 64-byte BIP39 seed → \`HKDF-SHA256(bip39_seed, info='chromatika recovery-words envelope', salt=kdfSalt)\` → envKey → unwrap masterKey.

probes (the per-word fingerprints that let the unlock surface validate the phrase **before** running the full HKDF) are stored alongside the envelope. on type, the wallet hashes each word and compares - mismatch surfaces a "wrong phrase" error without spending the full KDF.

## what's in the wrapped masterKey

the \`masterKey\` is 32 random bytes generated **once** at vault creation. it never changes after creation (unless the user explicitly rotates it, which today means "recreate the vault"). every envelope wraps the same 32 bytes.

## adding an envelope

\`\`\`
1. unlock the vault via any existing envelope → masterKey in memory
2. produce the new credential's envKey (e.g. webauthn register + PRF)
3. wrap the masterKey under the new envKey:
   wrappedMK = AES-GCM(envKey, masterKey, randomIv)
4. append the new envelope to vault.envelopes[]
5. re-seal the vault blob (same masterKey, new envelopes list)
\`\`\`

## removing an envelope

\`\`\`
1. unlock the vault
2. filter vault.envelopes[] to drop the target id
3. re-seal the vault blob
\`\`\`

cannot remove the **last** envelope - that would brick the vault. UI / API enforces "at least one envelope per vault".

## listing envelope metadata (without unlocking)

\`listVaultEnvelopes\` returns the envelope metadata (kind, label, hint, address, etc.) **without** unlocking. this lets the unlock surface show "this vault has password + passkey + recovery words" before the user picks one. envelope \`wrappedMasterKey\` and \`kdfMeta.salt\` are exposed (they're not secrets - they're useless without the credential), but the \`masterKey\` itself is never exposed by this query.

## per-envelope KDF inputs

| envelope kind | KDF input | KDF | rationale |
|---------------|-----------|-----|-----------|
| password | password (string) | argon2id (slow, memory-hard) | resists offline brute force |
| passkey-prf | PRF output (32 bytes random) | HKDF-SHA256 (fast) | input is already high-entropy random |
| wallet-signature | signature (64 bytes ed25519 sig) | HKDF-SHA256 (fast) | input is already high-entropy |
| recovery-words | BIP39 seed (64 bytes from PBKDF2-HMAC-SHA512) | HKDF-SHA256 (fast) | BIP39's PBKDF2 already did the slow work |

password is the only envelope that uses argon2id because it's the only credential where the entropy comes directly from a human-chosen string. PRF / signatures / BIP39 seeds are all high-entropy random outputs that don't need additional stretching.

## a note on legacy formats

an older code path called \`vaultPersistSecrets\` and stored credentials in plaintext alongside the vault payload. the multi-envelope V4 design encrypts those credentials separately - moving forward, look for envelopes, not for plaintext credential fields. mnemonic and \`solanaSecretKeyB64\` / \`suiPrivateKeyBech32\` **still** sit in plaintext **inside the vault payload** because they're treated as the user's own secret material that the vault is supposed to remember; envelopes wrap the masterKey that decrypts that vault payload, not the credentials themselves.

## library

- \`crypto.subtle\` for AES-GCM, HKDF, and webauthn calls
- argon2id wasm port for the password envelope
- \`@scure/bip39\` for \`mnemonicToSeedSync\`
- internal helpers: \`buildPasswordEnvelope\`, \`buildPasskeyPrfEnvelope\`, \`buildWalletSignatureEnvelope\`, \`buildRecoveryWordsEnvelopeRef\`, \`unlockEnvelope*\`
`,jb=`# Mobile Wallet Adapter 2.0 spec + reflector protocol

Mobile Wallet Adapter (MWA) 2.0 is Solana's spec for "how a desktop / web app talks to a Solana wallet on a phone". the local transport (Android intent dispatch) works only when both sides are on the same Android device. the **remote transport** uses a websocket reflector that bridges the desktop ↔ phone connection over the network.

spec: [solana-mobile.github.io/mobile-wallet-adapter/spec/spec.html](https://solana-mobile.github.io/mobile-wallet-adapter/spec/spec.html)

reflector subspec: [reflector-protocol](https://solana-mobile.github.io/mobile-wallet-adapter/spec/spec.html#reflector-protocol)

## the protocol layers

\`\`\`
[ MWA-2 application layer ]
  - methods: authorize, sign_messages, sign_transactions, sign_and_send_transactions
  - JSON-RPC 2.0-shaped messages

[ MWA-2 session layer ]
  - encrypted session via X25519 key exchange + AES-GCM
  - association URL carries pubkey + reflector params

[ transport layer ]
  - local: Android intent (solana-wallet:// scheme)
  - remote: WebSocket to reflector at wss://reflect.solanamobile.com/reflect
\`\`\`

session-level encryption means the reflector (or any intermediate hop on the websocket) can't read the application-layer messages. it only forwards encrypted blobs.

## the association URL

at pairing time, the dapp generates an **association URL**:
\`\`\`
solana-wallet:/v1/associate/<base64-encoded-association-data>
\`\`\`

association data carries:
- ephemeral public key (X25519, used for the session key exchange)
- reflector hostname (where the wallet should connect to forward messages)
- reflector ID (the session topic on the reflector - both sides agree on this so they can reach each other through the relay)
- protocol version

the dapp shows this URL as a QR code on the desktop. the wallet scans the QR with the Seeker camera (or any MWA wallet's "scan link" input), parses the URL, connects to the reflector, completes the X25519 handshake.

## the reflector protocol

the reflector is a stateless message-forwarding server. clients connect via WebSocket, identified by a per-session topic:
\`\`\`
wss://reflect.solanamobile.com/reflect?id=<session-topic>
\`\`\`

both sides connect to the **same topic**. messages from one side are forwarded to the other. if one side disconnects, the reflector holds messages briefly waiting for reconnect.

\`\`\`
[ desktop chromatika ]                         [ phone wallet (Seeker) ]
  open wss://reflect.solanamobile.com           scan QR, parse URL
  ←← topic, ephemeral pubkey ←←                 connect wss
  →→ X25519 handshake initiation →→             →→ X25519 response →→
  shared session key derived                    shared session key derived
  send encrypted authorize request
  ←← encrypted authorize response ←←
  ...
\`\`\`

the reflector itself can't decrypt anything. session-layer crypto (X25519 + AES-GCM, all per-message) is end-to-end.

## the auth_token + persistence

after a successful \`authorize\` call, the wallet returns an \`auth_token\` that the dapp can store and use to **reauthorize** future sessions without re-pairing. chromatika persists \`auth_token\` plus the \`reflectorHost\` on the hardware vault record:

\`\`\`jsonc
record.hardwareAccountId = '<id>';
record.hardwareVendor = 'mwa';
record.mwaTransport = 'remote';
record.mwaAuthToken = '<base64 token>';
record.mwaReflectorHost = 'reflect.solanamobile.com';
\`\`\`

every subsequent sign:
1. desktop opens fresh WebSocket to the same reflector
2. sends \`reauthorize { auth_token }\`
3. wallet responds with a fresh authorize (or fails with \`ERROR_AUTHORIZATION_FAILED\` if the token is invalid)
4. on success, proceed to sign

so QR pairing happens **once** per device-pair. signing thereafter is QR-less.

## ERROR_AUTHORIZATION_FAILED handling

if the user uninstalls / reinstalls the wallet app, factory-resets the phone, or the wallet's session storage is cleared, the \`auth_token\` becomes invalid. chromatika handles:

\`\`\`ts
try {
  await mwaSign(...);
} catch (e) {
  if (e.code === 'ERROR_AUTHORIZATION_FAILED') {
    // flip the MwaSigner state to needsRepair
    sessionState.mwaSigner.needsRepair = true;
    // surface "your Seeker pairing expired - re-pair via QR"
  }
}
\`\`\`

re-pairing means generating a fresh association URL + new QR. user scans, fresh \`auth_token\` overwrites the old one.

## the can't-run-in-SW caveat

the MWA libraries (\`@solana-mobile/mobile-wallet-adapter-protocol-web3js\`) use:
- \`WebSocket\` API
- \`window.btoa\` / \`atob\` for base64
- DOM-related APIs in some code paths

the **service worker doesn't have these**. so MWA pairing + signing **must run in popup or side panel context**. chromatika does this in \`SeekerConnect.tsx\` and \`MwaSigner\` components.

## the host hard-coded constant

per CLAUDE.md:
- public reflector: \`development.reflector.solanamobile.com\`
- chromatika constant: \`MWA_REMOTE_HOST_AUTHORITY = 'development.reflector.solanamobile.com'\`

despite the spec calling reflectors "integrator-hosted" in theory, in practice all shipping wallets (Phantom Android, Solflare, Jupiter, Seeker) are tested against this Solana-Mobile-operated host. self-hosting a reflector causes silent UI freezes against most wallets even though the wire protocol works. **don't change this constant** unless the public host goes down.

a self-hosted Cloudflare-Workers reflector exists in \`/reflector\` (Durable Objects, ~150 lines TS) as a fallback. swap the constant to that hostname only if Solana Mobile's host goes down.

manifest CSP \`connect-src *\` covers wss today. tighten to an allowlist when the surface stabilizes.

## library

- \`@solana-mobile/mobile-wallet-adapter-protocol-web3js\` for \`transact\`, \`startRemoteScenario\`
- \`@solana-mobile/mobile-wallet-adapter-protocol\` for the lower-level type definitions
- internal: \`wallet-extension/src/background/hardware/mwa-remote.ts\` for the chromatika-facing shim
- internal: \`wallet-extension/src/ui/hardware/SeekerConnect.tsx\` for the QR pairing UI
- internal: \`wallet-extension/src/ui/hardware/MwaSigner.tsx\` for the sign popup

## related

- [mwa-local-android-intent.md](/library/tech/mwa-local-android-intent) - the same-device transport
- [mwa-remote-qr-pairing.md](/library/tech/mwa-remote-qr-pairing) - the desktop ↔ phone QR flow
- [wallet-signature-envelope.md](/library/tech/wallet-signature-envelope) - how MWA signatures unlock chromatika
- [ika-seed-solana-mwa-walletconnect.md](/library/tech/ika-seed-solana-mwa-walletconnect) - how MWA signatures derive the ika seed
`,Fb=`# MWA local transport (Android intent)

Mobile Wallet Adapter local transport works **only when chromatika and the wallet app are on the same Android device**. uses the Android intent system (\`solana-wallet://\` URI scheme) to launch the wallet, which signs and returns the result via the same intent. no reflector, no network round-trip.

## the transport flow

\`\`\`
[chromatika running in Android Chrome]
  1. transact() called from popup / side-panel
  2. lib generates a solana-wallet:// URI with the request payload
  3. browser dispatches Android intent with the URI
  4. Android sees the registered handler (Seeker built-in, Phantom Android, Solflare, Jupiter)
  5. user picks if multiple wallets installed
  6. wallet app launches, displays the request, user approves
  7. wallet builds the response, dispatches back via intent (same URI scheme + result extras)
  8. chromatika receives, parses, returns to caller
\`\`\`

session crypto (X25519 + AES-GCM) is the same as remote MWA (see [mwa-2-spec-and-reflector.md](/library/tech/mwa-2-spec-and-reflector)). the only difference is the transport hop: Android intent instead of WebSocket reflector.

## the UA gate

chromatika only surfaces the local-MWA option when the user agent matches Android:
\`\`\`ts
const isAndroid = /Android/i.test(navigator.userAgent);
const showMwaLocal = isAndroid;
const showMwaRemote = !isAndroid;
\`\`\`

on desktop / iOS, MWA local won't work (the intent dispatch has nothing to handle it), so the UI shows the remote QR option instead.

## the lib call

\`\`\`ts
import { transact } from '@solana-mobile/mobile-wallet-adapter-protocol-web3js';

const result = await transact(async (wallet) => {
  // authorize
  const auth = await wallet.authorize({
    cluster: 'mainnet-beta',
    identity: { name: 'Chromatika', uri: 'chrome-extension://<id>', icon: '...' },
  });
  // store auth.account, auth.auth_token
  return auth;
});
\`\`\`

\`transact()\` handles:
- generating the association URL with intent scheme
- launching the intent (browser handles automatically)
- waiting for the response intent
- decrypting the response with the session key

## the Android Chromium quirk

on Android Chromium browsers (Chrome, Edge, Brave), web pages can dispatch intents via:
\`\`\`
window.location.href = 'solana-wallet://v1/associate/...';
\`\`\`

or via a more structured form:
\`\`\`
const link = document.createElement('a');
link.href = 'solana-wallet://v1/associate/...';
link.click();
\`\`\`

the MWA lib internally uses one of these mechanisms. either way, chromatika doesn't construct the intent URI by hand - it calls \`transact\` and lets the lib handle it.

## sign request flow

after pairing, sign requests use the same \`transact\` pattern:
\`\`\`ts
const result = await transact(async (wallet) => {
  await wallet.reauthorize({ auth_token });
  const signed = await wallet.signTransactions({
    transactions: [base64SerializedTx],
  });
  return signed;
});
\`\`\`

each \`transact\` call is one full intent round-trip. for batched signing (multiple txs), pass them all in one \`signTransactions\` call to avoid multiple intent dispatches.

## the dispatch on \`mwaTransport\`

chromatika's hardware-sign popup checks \`record.mwaTransport\`:
\`\`\`ts
if (record.hardwareVendor === 'mwa' && record.mwaTransport === 'local') {
  await mwaLocalSign(record, message);
} else if (record.hardwareVendor === 'mwa' && record.mwaTransport === 'remote') {
  await mwaRemoteSign(record, message);
}
\`\`\`

records are persisted with \`mwaTransport: 'local'\` at pairing time. switching transports requires re-pairing.

## what doesn't work

- **desktop Chromium**: \`solana-wallet://\` intent dispatch silently fails (no handler). use remote transport
- **iOS Safari**: MWA spec-level support exists but the intent ergonomics differ; chromatika doesn't expose iOS local-MWA today
- **inside extension popup on Android**: the intent dispatch should work because popups are still web pages, but chromatika has tested primarily with the Android Chrome side-panel context

## library

- \`@solana-mobile/mobile-wallet-adapter-protocol-web3js\` \`transact\`
- internal: \`wallet-extension/src/background/hardware/mwa-local.ts\` for chromatika-facing wrappers
- internal: \`wallet-extension/src/ui/hardware/MwaSigner.tsx\` for the sign UI

## related

- [mwa-2-spec-and-reflector.md](/library/tech/mwa-2-spec-and-reflector) - the underlying spec
- [mwa-remote-qr-pairing.md](/library/tech/mwa-remote-qr-pairing) - the desktop ↔ phone alternative
- [seeker-local.md](/library/user/seeker-local) (user-guides) - the user-facing flow
`,Ub=`# MWA remote transport (desktop ↔ phone QR pairing)

MWA remote transport bridges chromatika running on a **desktop** browser with a Solana wallet on a **phone**. uses the Solana Mobile reflector (\`wss://reflect.solanamobile.com/reflect\`) as a relay. the user scans a QR code on the desktop with the Seeker camera (or any MWA-wallet's "scan link" input), and from then on signing flows are QR-less.

## the pairing

\`\`\`
desktop chromatika                                phone wallet (Seeker)

1. SeekerConnect.tsx: startRemoteScenario()
2. lib opens wss://reflect.solanamobile.com/reflect?id=<topic>
3. lib generates association URL:
   solana-wallet:/v1/associate/<base64-data>
4. UI renders this URL as a QR code

                                                  5. user scans QR with Seeker camera
                                                  6. Seeker parses URL, connects to same reflector
                                                  7. X25519 handshake completes
                                                  8. session key derived

9. lib sends authorize request (encrypted)
                                                  10. Seeker shows authorize prompt
                                                  11. user approves
                                                  12. Seeker returns authorize response with auth_token
13. desktop receives, derives address +
    persists auth_token to vault record
\`\`\`

## why remote when local works

\`@solana-mobile/mobile-wallet-adapter-protocol-web3js\` \`startRemoteScenario()\` is the entry point. the underlying transport opens a WebSocket. the reflector forwards encrypted messages between the two sides without seeing plaintext.

reasons to use remote:
- **desktop chromatika**: Android intents don't work; remote is the only option
- **iOS phone**: Android intent system isn't available; iOS wallets advertise themselves via custom URL schemes that work in browsers, but full iOS MWA is younger; remote is most robust

## the wss restriction in SW

the MWA library uses \`window.btoa\`, \`atob\`, and other window-context APIs. **service workers don't have these**. so \`startRemoteScenario\` must run in **popup or side-panel** context.

chromatika's \`SeekerConnect.tsx\` (popup component) drives the pairing. on success, it persists state to chrome.storage.local via tRPC, and the SW picks it up for subsequent ops.

## the persistent auth_token

\`\`\`jsonc
record.hardwareVendor = 'mwa';
record.mwaTransport = 'remote';
record.mwaAuthToken = '<base64 token>';
record.mwaReflectorHost = 'development.reflector.solanamobile.com';
record.address = '<base58 phone wallet address>';
\`\`\`

every subsequent sign:
1. open fresh wss to the same reflector with the persisted topic
2. lib sends \`reauthorize\` with the persisted \`auth_token\`
3. Seeker reauthorizes silently (no UI on phone if the token is still valid)
4. dispatch the sign request
5. Seeker shows sign UI on phone
6. user approves
7. signature returns through the reflector

QR appears **only at initial pairing** (and on re-pair after \`ERROR_AUTHORIZATION_FAILED\`).

## the IKA_USK_DERIVATION_MESSAGE sign

at pairing, chromatika asks the wallet to sign \`IKA_USK_DERIVATION_MESSAGE\` (\`'ika.chromatika.user-share-encryption-key.v1'\`). the resulting 64-byte ed25519 signature drives:
1. the ika seed: \`keccak256(signature || u32_le(0))\` → 32-byte seed for \`UserShareEncryptionKeys\`
2. the in-extension fee-payer keypair (separate index): \`keccak256(signature || u32_le(1))\` → seed for \`Keypair.fromSeed\`

determinism: same wallet on a different device produces the same signature → same ika seed → same dWallet → restore-on-new-machine works without seed phrase.

see [ika-seed-solana-mwa-walletconnect.md](/library/tech/ika-seed-solana-mwa-walletconnect) for the full derivation.

## the reflector hostname

\`MWA_REMOTE_HOST_AUTHORITY = 'development.reflector.solanamobile.com'\`

despite the MWA 2.0 spec describing reflectors as "integrator-hosted", in practice all shipping wallets are tested against this Solana-Mobile-operated host. self-hosted reflectors cause silent UI freezes (wallets fail to connect or hang at handshake) even though the wire protocol works correctly.

a self-hosted Cloudflare-Workers reflector exists in \`/reflector\` of the chromatika repo as a fallback (Durable Objects, ~150 lines TS). swap the constant only if the Solana-Mobile host goes down.

## the CSP relaxation

manifest CSP currently has \`connect-src *\` to allow wss to any reflector. when the surface stabilizes (only one reflector host needed), tighten to an allowlist:
\`\`\`jsonc
"connect-src": "'self' wss://reflect.solanamobile.com https://api.mainnet-beta.solana.com ..."
\`\`\`

## sign-request flow

\`\`\`ts
async function mwaRemoteSign(record, message: Uint8Array) {
  const transport = await startRemoteScenario({
    reflectorHost: record.mwaReflectorHost,
    sessionTopicSeed: deriveTopicFromAuthToken(record.mwaAuthToken),
  });

  const result = await transport.transact(async (wallet) => {
    // reauthorize silently
    await wallet.reauthorize({ auth_token: record.mwaAuthToken });

    // sign
    const signed = await wallet.signMessages({
      addresses: [record.address],
      payloads: [message],
    });

    return signed;
  });

  return result.signedPayloads[0];   // 64-byte ed25519 signature
}
\`\`\`

## error: needsRepair

if the wallet returns \`ERROR_AUTHORIZATION_FAILED\`:
\`\`\`ts
sessionState.mwaSigner.needsRepair = true;
\`\`\`

the next sign attempt surfaces "Seeker pairing expired - re-pair to continue". user clicks re-pair, fresh QR is generated.

## library

- \`@solana-mobile/mobile-wallet-adapter-protocol-web3js\` \`startRemoteScenario\`, \`transact\`
- \`qrcode\` for rendering the association URL as QR
- internal: \`wallet-extension/src/background/hardware/mwa-remote.ts\` for the orchestration layer
- internal: \`wallet-extension/src/ui/hardware/SeekerConnect.tsx\` for the pairing popup

## related

- [mwa-2-spec-and-reflector.md](/library/tech/mwa-2-spec-and-reflector) - the underlying spec
- [mwa-local-android-intent.md](/library/tech/mwa-local-android-intent) - the same-device alternative
- [seeker-remote.md](/library/user/seeker-remote) (user-guides) - the user-facing flow
- [ika-seed-solana-mwa-walletconnect.md](/library/tech/ika-seed-solana-mwa-walletconnect) - the seed derivation
- [wallet-signature-envelope.md](/library/tech/wallet-signature-envelope) - how the signature unlocks chromatika
`,zb=`# \`@mysten/kiosk\` \`KioskClient\`

Sui Kiosks are on-chain object containers that hold tradeable items (often NFTs) with optional listings, transfer policies, and royalties. chromatika exposes Kiosk reads via \`@mysten/kiosk\` \`KioskClient\` - listing kiosks owned / managed by an address, fetching items, transfer policies, royalty configs.

## construction

\`\`\`ts
import { KioskClient, Network } from '@mysten/kiosk';

const kioskClient = new KioskClient({
  client: suiGraphQLClient,                                 // shares the vault SuiGraphQLClient
  network: activeNetwork === 'mainnet' ? Network.MAINNET : Network.TESTNET,
});
\`\`\`

the \`client\` parameter accepts \`SuiClient\` or \`SuiGraphQLClient\` (per \`@mysten/kiosk\` 1.2.x). chromatika passes the GraphQL client.

## what we expose

- \`getOwnedKiosks(address)\` - kiosks owned by an address (returns kiosk ids)
- \`getKioskData(kioskId)\` - items in a kiosk + listings + transfer policy

## listing kiosks

\`\`\`ts
async function getOwnedKiosks({ address }) {
  const result = await kioskClient.getOwnedKiosks({ address });
  return result.kioskIds;
}
\`\`\`

returns kiosk ids the address controls. note: "owned" includes both owned kiosks (the address is the owner) and **managed** kiosks (the address has manager rights, e.g. a transfer policy creator). the response distinguishes if needed.

## fetching kiosk contents

\`\`\`ts
async function getKioskData({ kioskId }) {
  const data = await kioskClient.getKioskById({
    id: kioskId,
    options: {
      withObjects: true,
      withListings: true,
      withTransferPolicy: true,
    },
  });
  return {
    kioskId,
    items: data.items.map(item => ({
      objectId: item.objectId,
      type: item.type,
      display: item.display,                                // Display protocol metadata
      isLocked: item.isLocked,
      listing: item.listing,                                // null if not listed
    })),
    transferPolicies: data.transferPolicies,
  };
}
\`\`\`

each item carries:
- object id + type
- Display protocol metadata (name, image, description from on-chain Display::Display)
- locked status (locked items can't be taken without satisfying transfer policy)
- listing info if the item is listed for sale (price, listing id)

## transfer policies + royalties

a transfer policy is a Move object that defines rules for transferring items of a specific type out of a kiosk. typical rules:
- royalty (e.g. 5% of sale price to a designated address)
- floor price (refuse listings below a min)
- cooldown (refuse re-listings within N hours of last sale)

chromatika exposes the policy info so the UI can display "this NFT has a 5% royalty" before the user lists or transfers.

## what chromatika doesn't expose today

- **listing items for sale**: the \`place_and_list\` PTB call
- **taking items**: the \`purchase\` PTB call (must satisfy transfer policy)
- **creating new kiosks**: \`kiosk::new\` PTB call
- **transferring kiosk ownership**: less common operation

these are read-only kiosk ops in the current chromatika surface. write ops are in the kiosk product surface roadmap; the calls go through the kiosk client + an \`IkaTransaction\` PTB (or the dWallet sign path).

## the patched chunking

per [mysten-sui-pinning-and-patches.md](/library/tech/mysten-sui-pinning-and-patches), \`@mysten/sui\` GraphQL \`getObjects\` / \`multiGetObjects\` chunks object ids by 12 (not 50). this affects KioskClient indirectly because Kiosk reads do a lot of multi-object lookups (items + listings + display) - the chunking smooths over GraphQL body-size limits.

## the future "dedicated page" intent

per architecture-final.html, kiosks should eventually be a top-level UI tab rather than a panel inside NftsPage. tracked as future. doesn't affect the underlying KioskClient calls; only the UI layout.

## library

- \`@mysten/kiosk\` \`KioskClient\`, \`Network\` enum
- \`@mysten/sui/graphql\` \`SuiGraphQLClient\` (passed to KioskClient)
- internal: \`wallet-extension/src/background/services/sui-kiosk.ts\` for \`getOwnedKiosks\`, \`getKioskData\`

## related

- [sui-graphql-client.md](/library/tech/sui-graphql-client) - the underlying transport
- [nft-api-providers.md](/library/tech/nft-api-providers) - NFT discovery that overlaps with kiosk items
- [sui-kiosks.md](/library/user/sui-kiosks) (user-guides) - the user-facing flow
`,Ob=`# \`@mysten/sui\` pinning + patches

\`@mysten/sui\` is Mysten's TypeScript SDK for Sui. chromatika **pins it to a specific version** in npm \`overrides\` (so the app + ika SDK use the same hoisted copy) and **applies a pnpm patch** to chunk GraphQL \`getObjects\` calls more aggressively. plus a \`postinstall\` step removes a nested copy that ika's SDK ships, ensuring runtime resolution converges to the hoisted version.

## the version

per agent inventory: \`@mysten/sui\` is overridden to **\`2.16.0\`** (was 2.13.2 historically). bumping requires:
1. update direct dep in \`wallet-extension/package.json\`
2. update \`overrides\` block at root \`package.json\` and inside \`@ika.xyz/sdk\` override
3. refresh the patch (since patch is version-specific)
4. test ika ops still work (the \`IkaClient\` is built on \`client.core.*\` from this exact version)

## the npm \`overrides\` block

\`\`\`jsonc
// root package.json
{
  "overrides": {
    "@mysten/sui": "2.16.0",
    "@ika.xyz/sdk": {
      "@mysten/sui": "2.16.0"
    },
    "@ledgerhq/live-network": "2.4.3"
  },
  "pnpm": {
    "overrides": {
      "@ledgerhq/live-network": "link:./stubs/ledger-live-network",
      "@types/node": "^22"
    }
  }
}
\`\`\`

\`overrides\` forces transitive deps to use the pinned version. without this, \`@ika.xyz/sdk\` could pull a different \`@mysten/sui\` minor version, leading to type incompatibilities at the boundary (e.g. \`IkaTransaction\` expects Mysten's \`Transaction\`, but a different Mysten version's class doesn't \`instanceof\` the same).

## the pnpm patch

\`\`\`
wallet-extension/patches/@mysten__sui@2.13.2.patch
\`\`\`

(filename has the version pinned; refresh to \`@mysten__sui@2.16.0.patch\` after the bump.)

what the patch changes:
- \`getObjects\` and \`multiGetObjects\` chunk \`objectIds\` by **12** (instead of upstream's 50)
- 100ms pause between chunks

reason: Sui's GraphQL POST body grows linearly with the id list. a 50-id batch produces a body of ~5000 bytes that hits common edge proxy limits. 12 ids stays well under. 100ms pause reduces burst rate-limit hits.

## the postinstall script

after \`pnpm install\`, a postinstall step removes:
\`\`\`
node_modules/@ika.xyz/sdk/node_modules/@mysten/
\`\`\`

reason: Node's resolution algorithm prefers nested \`node_modules\` over hoisted ones. even with \`overrides\`, ika's SDK could ship its own nested \`@mysten/sui\` that shadows the hoisted version. removing the nested copy forces resolution to the hoisted (correct) version.

\`pnpm\` typically hoists by default but mixed-resolution scenarios still arise. the postinstall is the belt-and-suspenders fix.

## the \`npm ls @mysten/sui\` weirdness

per CLAUDE.md non-obvious bullet: \`npm ls @mysten/sui\` may exit \`1\` / show "invalid" because npm checks against the package's \`peerDependencies\` constraints, which the override technically violates. **runtime uses one tree** after the postinstall, so this is npm's static check vs override behavior - cosmetic, not a real issue.

## the vite resolve.dedupe

\`vite.config.ts\` adds \`resolve.dedupe: ['@mysten/sui']\` to ensure the bundler picks one version even if two land in node_modules during dev:

\`\`\`ts
export default defineConfig({
  resolve: {
    dedupe: ['@mysten/sui'],
  },
});
\`\`\`

without this, vite could include two copies in the bundle, breaking \`instanceof\` checks across the boundary.

## bundle size impact

\`@mysten/sui\` is large (~500 KB minified). combined with ika SDK + crypto, the **background bundle** is ~5.5 MB minified, ~1.6 MB gzipped. ika + wasm dominates. there's no easy way to slim this further without breaking ika protocol support.

cold load of the SW takes a few seconds on slow machines. chromatika displays a "wallet starting" state until the SW is ready.

## the patch refresh dance

bumping \`@mysten/sui\`:
\`\`\`sh
pnpm install @mysten/sui@<new-version>
# update overrides in root + wallet-extension package.json
# regenerate the patch:
pnpm patch @mysten/sui@<new-version>
# pnpm extracts the package, opens a temp dir, you edit getObjects to chunk by 12 + add 100ms pause
pnpm patch-commit /path/to/temp-dir
# new patch file lands in patches/
# delete the old patch file
git rm patches/@mysten__sui@<old-version>.patch
git add patches/@mysten__sui@<new-version>.patch
\`\`\`

then test ika ops + Sui sends + NFT reads. if anything breaks at the patch line, regenerate / debug.

## the IkaClient binding

ika's \`IkaClient\` 0.3.x runs on \`client.core.*\` from \`@mysten/sui\` 2.x. chromatika constructs:
\`\`\`ts
const ikaClient = new IkaClient({ suiClient: suiGraphQLClient });
\`\`\`

passing chromatika's vault-shared \`SuiGraphQLClient\`. ika's internal calls go through this client → through the patched \`getObjects\` chunking → through GraphQL.

## library

- \`@mysten/sui\` 2.16.0 (pinned)
- \`@ika.xyz/sdk\` 0.3.x (uses pinned mysten via override)
- internal: \`wallet-extension/postinstall.mjs\` (or similar) for the nested copy removal
- internal: \`wallet-extension/patches/@mysten__sui@2.16.0.patch\` (filename version-suffixed)

## related

- [sui-graphql-client.md](/library/tech/sui-graphql-client) - the patched client
- [2pc-mpc-overview.md](/library/tech/2pc-mpc-overview) - the ika SDK that builds on it
- [ika-dkg-flow.md](/library/tech/ika-dkg-flow), [ika-presign-pool-impl.md](/library/tech/ika-presign-pool-impl), [ika-sign-flow.md](/library/tech/ika-sign-flow) - ika ops over the patched client
`,Hb=`# NFT API providers (Alchemy, Helius, Hiro, Mysten Display, Aptos Indexer)

chromatika reads NFTs per chain from chain-specific indexers. no unified SDK - direct REST per provider. respects MediaSafetyMode at the URL-rendering layer (see [media-safety-mode.md](/library/user/media-safety-mode) user-guide).

## Sui (Mysten Display + GraphQL)

\`\`\`ts
async function getSuiNfts({ address }) {
  // Sui's own GraphQL exposes object listings + on-chain Display protocol
  const client = createSuiGraphQLClientFromRegistryNetworkId();
  const response = await client.queryObjects({
    filter: { ObjectOwner: address },
    options: { showDisplay: true, showContent: true, showType: true },
  });
  // each object can have a Display::Display { name, image_url, ... } field
  return response.data.map(obj => ({
    objectId: obj.objectId,
    name: obj.display?.data?.name,
    imageUrl: obj.display?.data?.image_url,
    type: obj.type,
  }));
}
\`\`\`

- no API key; Sui's GraphQL is the source of truth
- Display protocol is Sui's on-chain NFT metadata standard
- chunked by 12 object ids per query (per the \`@mysten/sui\` patch; see [mysten-sui-pinning-and-patches.md](/library/tech/mysten-sui-pinning-and-patches))

## EVM (Alchemy)

\`\`\`ts
async function getEvmNfts({ address, chainId }) {
  const apiKey = import.meta.env.VITE_ALCHEMY_KEY;
  if (!apiKey) return [];   // empty when not configured
  const network = ALCHEMY_NETWORKS[chainId];   // e.g. "eth-mainnet"
  const resp = await fetch(
    \`https://\${network}.g.alchemy.com/nft/v3/\${apiKey}/getNFTsForOwner?owner=\${address}\`
  );
  const data = await resp.json();
  return data.ownedNfts.map(nft => ({
    contract: nft.contract.address,
    tokenId: nft.tokenId,
    name: nft.name,
    imageUrl: nft.image?.cachedUrl ?? nft.image?.originalUrl,
  }));
}
\`\`\`

- requires \`VITE_ALCHEMY_KEY\` build-time env var
- without the key, \`getEvmNfts\` returns empty array (no error, just no data)
- supports mainnet ETH, Polygon, Arbitrum, Optimism, Base, etc. via Alchemy's per-network subdomains
- check \`getNftApiHints\` to see if \`alchemyConfigured: true\` in the running build

## Solana (Helius DAS API)

\`\`\`ts
async function getSolanaNfts({ address }) {
  const apiKey = import.meta.env.VITE_HELIUS_KEY;
  if (!apiKey) return [];
  const rpcUrl = \`https://mainnet.helius-rpc.com/?api-key=\${apiKey}\`;
  const resp = await fetch(rpcUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      jsonrpc: '2.0',
      id: '1',
      method: 'getAssetsByOwner',
      params: { ownerAddress: address, page: 1, limit: 100 },
    }),
  });
  const data = await resp.json();
  return data.result.items.map(asset => ({
    id: asset.id,
    name: asset.content?.metadata?.name,
    imageUrl: asset.content?.files?.[0]?.uri,
    compressed: asset.compression?.compressed === true,
  }));
}
\`\`\`

- requires \`VITE_HELIUS_KEY\`
- without the key, returns empty
- DAS (Digital Asset Standard) covers regular SPL NFTs **and compressed NFTs** (cNFTs)
- compressed NFT support is the main reason chromatika picked Helius over alternatives

## Bitcoin (Hiro Ordinals)

\`\`\`ts
async function getBtcOrdinals({ address }) {
  const resp = await fetch(\`https://api.hiro.so/ordinals/v1/inscriptions?address=\${address}\`);
  const data = await resp.json();
  return data.results.map(insc => ({
    inscriptionId: insc.id,
    inscriptionNumber: insc.number,
    contentType: insc.content_type,
    contentUrl: \`https://api.hiro.so/ordinals/v1/inscriptions/\${insc.id}/content\`,
  }));
}
\`\`\`

- no API key (Hiro provides free tier)
- Ordinals are bitcoin inscriptions (text, image, video, etc. embedded in tx witness data)
- not "NFTs" in the SPL or ERC-721 sense; the metadata model is different

## Aptos (Token v2 indexer)

\`\`\`ts
async function getAptosNfts({ address }) {
  const network = activeAptosNetwork === 'mainnet' ? 'mainnet' : 'testnet';
  const resp = await fetch(\`https://api.\${network}.aptoslabs.com/v1/accounts/\${address}/resources\`);
  // aptos token v2 indexer offers structured queries via a GraphQL endpoint:
  const gql = await fetch(\`https://api.\${network}.aptoslabs.com/v1/graphql\`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      query: \`query { current_token_ownerships_v2(where: {owner_address: {_eq: "\${address}"}}) { ... } }\`,
    }),
  });
  const data = await gql.json();
  return data.data.current_token_ownerships_v2.map(t => ({ ... }));
}
\`\`\`

- no API key (Aptos public-tier indexer)
- Token v2 is the Move standard for NFTs on Aptos
- separate from Token v1 (legacy) which most modern collections don't use

## the MediaSafetyMode filter

after retrieving NFT URLs, chromatika applies \`MediaSafetyMode\`:
- \`'all'\`: render everything
- \`'ipfs-arweave'\` (default): only render URLs whose host resolves to IPFS or Arweave gateways (includes a known-gateway allowlist)
- \`'none'\`: don't load images at all (text + metadata only)

filtering happens at the \`<img src=...>\` rendering layer, not at the API call. metadata is returned regardless; the image just doesn't load if blocked.

## the API hints helper

\`\`\`ts
getNftApiHints(): { alchemyConfigured: boolean, heliusConfigured: boolean }
\`\`\`

returns whether the build has the keys for Alchemy / Helius. UI surfaces use this to display "API key required" hints when the relevant chain returns empty results.

## why no SDKs

each provider has its own SDK (\`alchemy-sdk-js\`, \`helius-sdk\`, etc.). chromatika uses raw \`fetch\` because:
- bundle size: each SDK adds ~100-500 KB; multiplied across 5 providers, that's a lot
- features we use are tiny (one or two endpoints per provider)
- minimal abstraction layer - direct understanding of what the wire calls look like

if a provider requires sophisticated features (websockets, complex pagination, signature schemes), we'd reconsider. for read-only NFT lists, raw fetch wins.

## library

- \`fetch\` (browser native)
- \`@mysten/sui\` GraphQL client (for Sui Display reads)
- internal: \`wallet-extension/src/background/services/nft.ts\` orchestration
- internal: \`wallet-extension/src/background/services/sui-nft.ts\`, \`evm-nft.ts\`, \`solana-nft.ts\`, \`aptos-nft.ts\`, \`bitcoin-ordinals.ts\` per-chain
- internal: \`wallet-extension/src/config/nft-api.ts\` for the network maps + key resolution

## related

- [media-safety-mode.md](/library/user/media-safety-mode) (user-guides) - the URL filtering
- [sui-graphql-client.md](/library/tech/sui-graphql-client) - the Sui GraphQL transport
- [mysten-sui-pinning-and-patches.md](/library/tech/mysten-sui-pinning-and-patches) - the 12-id chunking patch
- [browse-nfts.md](/library/user/browse-nfts) (user-guides) - the user-facing surface
`,qb=`# \`@noble/*\` and \`@scure/*\` crypto libraries

chromatika's crypto primitives come from Paul Miller's two npm scopes: \`@noble/*\` (curves + hashes + ed25519 / secp256k1) and \`@scure/*\` (BIP39 / BIP32 / base encodings). both are audit-grade, dep-free, and browser-compatible. they replace older bundles that historically had supply-chain risk.

## the libs

| package | version | what |
|---------|---------|------|
| \`@noble/ed25519\` | ^3.1.0 | ed25519 keypair gen, sign, verify (RFC 8032) |
| \`@noble/secp256k1\` | ^3.1.0 | secp256k1 keypair gen, sign, verify, recover |
| \`@noble/hashes\` | ^2.2.0 | SHA-2, SHA-3 (Keccak), BLAKE2b, HMAC, PBKDF2, etc. |
| \`@scure/bip39\` | ^2.2.0 | BIP39 mnemonic gen, validate, seed derive |
| \`@scure/bip32\` | ^2.2.0 | BIP32 HD derivation (secp256k1 hardened + non-hardened) |
| \`@scure/base\` | ^2.2.0 | base58, base64, bech32, bech32m, hex |

## why noble / scure

historically, browser crypto came from:
- \`tweetnacl\` for ed25519 (small, but limited)
- \`elliptic\` for secp256k1 (lots of deps, audit issues)
- \`bip39\`, \`hdkey\`, \`secp256k1\` (separate packages, varying quality)

noble + scure consolidate. design principles:
- **zero dependencies** at runtime (no transitive supply chain)
- **TypeScript native** (no \`@types/*\` wrappers)
- **audit-grade** (paid audits + ongoing review)
- **constant-time** where it matters (resists timing side channels)
- **modular** (import only what you need; tree-shakes well)

bundle size: noble libs are ~5-15 KB minified per primitive. tweetnacl is ~25 KB for ed25519 alone. elliptic was ~80 KB. noble wins on every axis.

## what chromatika uses each for

### \`@noble/ed25519\`

\`\`\`ts
import * as ed from '@noble/ed25519';

const pubkey = await ed.getPublicKeyAsync(secretKey32);
const sig = await ed.signAsync(message, secretKey32);
const valid = await ed.verifyAsync(sig, message, pubkey);
\`\`\`

primarily used **internally** by \`@solana/web3.js\` Keypair, \`@mysten/sui\` Ed25519Keypair, and chromatika's seed-derivation paths. chromatika rarely imports noble/ed25519 directly - it goes through higher-level wrappers.

### \`@noble/secp256k1\`

\`\`\`ts
import * as secp from '@noble/secp256k1';

const pubkey = secp.getPublicKey(secretKey32, true);          // compressed
const sig = secp.sign(messageHash, secretKey32);
const valid = secp.verify(sig, messageHash, pubkey);
const recovered = secp.recoverPublicKey(messageHash, sig.toCompactRawBytes(), recoveryBit);
\`\`\`

used by \`bitcoinjs-lib\` internally and by chromatika for any direct secp256k1 math (rare since ika MPC handles signing; noble used for verification only).

### \`@noble/hashes\`

\`\`\`ts
import { sha256 } from '@noble/hashes/sha2';
import { keccak_256 } from '@noble/hashes/sha3';
import { blake2b } from '@noble/hashes/blake2b';
import { hmac } from '@noble/hashes/hmac';
import { pbkdf2 } from '@noble/hashes/pbkdf2';

const digest = keccak_256(preimage);                           // 32 bytes
const blakeDigest = blake2b(input, { dkLen: 32 });             // BLAKE2b-256
const hmacOut = hmac(sha512, key, message);
const seed = pbkdf2(sha512, password, salt, { c: 2048, dkLen: 64 });
\`\`\`

used **everywhere** in chromatika:
- \`keccak256\` for ika seed derivation, EVM digest, EVM address
- \`blake2b256\` for Sui address derivation, Sui intent digest
- \`sha512\` inside ed25519 / BIP32 / SLIP10 derivations
- \`pbkdf2\` inside BIP39 (mnemonic → seed)

### \`@scure/bip39\`

\`\`\`ts
import { generateMnemonic, validateMnemonic, mnemonicToSeedSync } from '@scure/bip39';
import { wordlist } from '@scure/bip39/wordlists/english';

const phrase = generateMnemonic(wordlist, 128);                // 12 words from 128-bit entropy
const isValid = validateMnemonic(phrase, wordlist);
const seed = mnemonicToSeedSync(phrase, '');                   // 64-byte BIP39 seed via PBKDF2
\`\`\`

chromatika uses for:
- generating mnemonics at vault create time
- validating mnemonics on import
- deriving the 64-byte BIP39 seed that feeds BIP32 / SLIP10

### \`@scure/bip32\`

\`\`\`ts
import { HDKey } from '@scure/bip32';

const root = HDKey.fromMasterSeed(bip39Seed);
const child = root.derive("m/44'/60'/0'/0/0");
const pubkey = child.publicKey;                                // 33-byte compressed secp256k1
const privkey = child.privateKey;                              // 32-byte secret
\`\`\`

used for EVM (\`m/44'/60'/...\`) and Bitcoin (\`m/84'/0'/...\`, \`m/86'/0'/...\`) derivation. for ed25519 (Sui / Solana / Aptos), chromatika uses an internal SLIP10 helper instead since BIP32 is secp256k1-only.

### \`@scure/base\`

\`\`\`ts
import { base58, base64, hex, bech32, bech32m } from '@scure/base';

const b58 = base58.encode(bytes);
const decoded = base58.decode(b58Str);
const bech = bech32.encode('bc', words);
\`\`\`

used by Bitcoin (bech32 / bech32m for segwit / taproot addresses), Solana (base58), and various encoding paths. modular: only import the formats you need.

## the security stance

noble's threat model: protect against **timing attacks** + **algorithmic correctness** + **supply chain integrity**. paid audits (Cure53, others). actively maintained.

scure inherits the same principles. both are sponsored by the broader Ethereum / wallet ecosystem (gitcoin grants, direct funding from major wallets).

chromatika's choice to standardize on noble / scure is a **deliberate move away from older crypto libs** with checkered audit / supply-chain history.

## library

- per package above
- internal: chromatika imports specific modules per file rather than re-exporting from a single helper. e.g. \`keyring/hd.ts\` imports \`keccak_256\`, \`mnemonicToSeedSync\`, \`HDKey\` directly

## related

- [bip39-mnemonic.md](/library/tech/bip39-mnemonic) - the BIP39 path
- [bip44-slip10-derivation.md](/library/tech/bip44-slip10-derivation) - the HD derivation paths
- [keccak256-uses.md](/library/tech/keccak256-uses) - keccak usage
- [sha512-and-blake2b.md](/library/tech/sha512-and-blake2b) - SHA-512 / BLAKE2b usage
- [secp256k1-ecdsa.md](/library/tech/secp256k1-ecdsa), [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - signing curves
`,Vb=`# passkey PRF envelope

a chromatika vault can have a **passkey** envelope that uses webauthn's PRF (HMAC-secret) extension to derive an envelope key. the same passkey credential, given the same per-vault salt, produces the same 32-byte PRF output every time - which lets us derive the envelope key deterministically without storing it.

## envelope record

\`\`\`jsonc
{
  "kind": "passkey-prf",
  "label": "yubikey-1",
  "credentialIdB64Url": "<webauthn credential.rawId>",
  "publicKeyCompressedB64": "<33-byte compressed secp256r1 pubkey>",
  "rpId": "<extension chrome-extension://... origin>",
  "prfSaltB64": "<32 random bytes>",
  "wrappedMasterKeyB64": "<AES-GCM ciphertext + tag>",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

\`prfSaltB64\` is the per-vault domain separator. webauthn PRF takes a salt and returns \`HMAC(hmac_secret, salt)\` - same secret + same salt = same output, deterministically.

## creation (registration)

\`\`\`
1. webauthn ceremony: navigator.credentials.create({
     publicKey: {
       challenge: random32,
       rp: { id: rpId, name: 'Chromatika' },
       user: { id, name, displayName },
       pubKeyCredParams: [{ alg: -7, type: 'public-key' }],   // ES256 (secp256r1)
       extensions: { prf: { eval: { first: prfSalt } } }      // optional eval at register
     }
   })
2. response includes:
   - credential.rawId → credentialIdB64Url
   - credential.response.attestationObject → cbor parse → public key bytes
   - extension result: prf.results.first (32 bytes) IF the authenticator supports PRF eval-at-register
3. some authenticators don't eval PRF at register; in that case do an immediate get() to extract PRF
4. envKey = HKDF-SHA256(prf_secret_32, info='chromatika passkey envelope v1', salt=prfSalt)
5. wrappedMK = AES-GCM-256.encrypt(envKey, masterKey, randomIv)
6. envelope = { kind, label, credentialIdB64Url, publicKeyCompressedB64, rpId, prfSaltB64, wrappedMasterKeyB64, envIvB64 }
\`\`\`

## unlock (assertion)

\`\`\`
1. webauthn ceremony: navigator.credentials.get({
     publicKey: {
       challenge: random32,
       rpId: rpId,
       allowCredentials: [{ id: credentialIdB64Url_decoded, type: 'public-key' }],
       extensions: { prf: { eval: { first: prfSalt_from_envelope } } }
     }
   })
2. user authenticates on device (Touch ID, Windows Hello, security key tap, etc.)
3. response.extensions.prf.results.first → 32 bytes (deterministic given same credential + salt)
4. envKey = HKDF-SHA256(prf_secret_32, info='chromatika passkey envelope v1', salt=prfSalt_from_envelope)
5. masterKey = AES-GCM-256.decrypt(envKey, wrappedMasterKey, envIv)
6. session unlocks
\`\`\`

## why HKDF-SHA256 and not argon2id

the PRF output is already 32 bytes of high-entropy random material - HMAC-secret outputs cannot be brute-forced because they're 256-bit pseudorandom. argon2id's memory-hard slowness exists to slow down brute force on low-entropy passwords; with PRF that entire threat model doesn't apply.

HKDF gives us domain separation (\`info='chromatika passkey envelope v1'\`) and re-randomization with the per-vault salt, so reusing the same credential across multiple chromatika vaults produces different envelope keys per vault.

## determinism guarantee

webauthn PRF spec mandates that for a given (credential, salt) pair, the output is identical across:
- different assertions on the same device
- different devices that the credential has been **synced to** (e.g. iCloud Passwords sync, Google Passkey sync)
- different chromatika installs that authenticate against the same credential

this is why a passkey vault can be **restored on a new install** by re-registering the same passkey - not by exporting / importing keys, but by re-asserting and getting the same PRF output.

## non-deterministic authenticators

some older or non-conformant authenticators don't expose PRF / HMAC-secret. chromatika **does not** create a passkey envelope on those - the registration ceremony returns no PRF result, and chromatika rejects the registration with an actionable error (use a different authenticator, or use a password / WAAP / hardware envelope instead).

## platform-bound vs roaming

- **platform-bound** passkeys (Touch ID, Windows Hello, Android device biometrics): tied to the device they were registered on. cross-device requires platform sync (iCloud / Google).
- **roaming** authenticators (yubikey, solokey, hardware token): work on any device that supports webauthn over USB / NFC / Bluetooth.

chromatika doesn't distinguish in storage - the envelope record is identical for both. the user picks based on threat model + convenience.

## webauthn extension fields chromatika sets

\`\`\`js
extensions: {
  prf: {
    eval: { first: prfSalt }    // 32-byte salt; we only use 'first', not 'second'
  }
}
\`\`\`

we don't use \`prf.evalByCredential\` (per-credential salt mapping) because chromatika sends one credential at a time via \`allowCredentials\`. we don't use \`largeBlob\` because the PRF output is enough for our envelope key.

## passkey rotation

chromatika can have **multiple** passkey envelopes on the same vault (yubikey + Touch ID, for example). each envelope wraps the same masterKey with its own credential's PRF output. dropping an envelope = removing one credential without affecting others.

if a user **loses** all their passkeys, they need a recovery-words envelope (registered at create time per the option in \`runPasskeyOnboarding\`) or the password envelope as backup.

## library

- browser native \`navigator.credentials\` (webauthn)
- \`crypto.subtle.deriveKey\` for HKDF
- \`crypto.subtle.encrypt\` / \`.decrypt\` for AES-GCM
- helper: \`wallet-extension/src/ui/passkey/passkey-provider-with-prf.ts\` wraps \`navigator.credentials\` with the PRF extension and chromatika-specific defaults
- helper: \`passkey-derive.ts\` for register-time + assertion-time validation, public-key parsing, Sui address derivation (BLAKE2b-256 of \`0x06 || pk_compressed\`)
`,Gb=`# password envelope

the most common chromatika unlock path: enter the app password, argon2id derives an envelope key, the envelope unwraps the master key, the master key decrypts the vault payload.

## envelope record

\`\`\`jsonc
{
  "kind": "password",
  "label": "primary password",   // human-readable, optional
  "kdfMeta": {
    "kind": "argon2id",
    "tCost": 3,
    "mCostKiB": 65536,
    "parallelism": 4,
    "saltB64": "<16 random bytes>",
    "outputLength": 32
  },
  "wrappedMasterKeyB64": "<AES-GCM ciphertext + tag>",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

each envelope has its own argon2id salt and its own AES-GCM IV. multiple password envelopes on the same vault is theoretically allowed (multi-password) but the UI / API expose one password envelope per vault.

## creation

\`\`\`
1. user picks a password (8+ chars enforced)
2. generate 16 random bytes → salt
3. envKey = argon2id(password_bytes, salt, t=3, m=65536KiB, p=4, output=32)
4. generate 12 random bytes → envIv
5. wrappedMK = AES-GCM-256.encrypt(envKey, masterKey, envIv) // 32+16 = 48-byte ciphertext+tag
6. append envelope to vault.envelopes[]
\`\`\`

password is **never** stored. the only persistent artifacts are: salt (random, public-equivalent), envIv (random, public-equivalent), wrappedMK (sealed). without the password, none of these reveal anything.

## unlock

\`\`\`
1. user types password
2. envKey = argon2id(password_bytes, kdfMeta.saltB64, kdfMeta.tCost, kdfMeta.mCostKiB, kdfMeta.parallelism, kdfMeta.outputLength)
3. masterKey = AES-GCM-256.decrypt(envKey, wrappedMasterKey, envIv)
   // throws if password is wrong (auth tag verification fails)
4. import masterKey via subtle.importKey('raw', masterKey, { name: 'AES-GCM' }, false, ['decrypt'])
5. masterKey is now a non-extractable CryptoKey held in session
6. session uses masterKey to decrypt the outer vault blob
\`\`\`

step 3 is the single point where wrong-password is detected. AES-GCM's auth tag verification fails on bad keys, throwing synchronously - the UI surfaces "incorrect password" without ever seeing partial plaintext.

## changing the password

\`\`\`
1. unlock with old password → masterKey
2. user picks new password
3. generate fresh 16-byte salt, fresh 12-byte envIv
4. envKey_new = argon2id(new_password, new_salt, ...)
5. wrappedMK_new = AES-GCM-256.encrypt(envKey_new, masterKey, envIv_new)
6. replace the password envelope with the new wrappedMK + meta
7. re-seal the vault blob (no payload changes - only the envelope updated)
\`\`\`

masterKey is **the same** before and after. inner vault payload doesn't get re-encrypted. password change is cheap.

## the unlock cache footnote

after unlock, the **derived 32-byte AES key bytes** (b64) plus argon2id params and salt are written to \`chromatika_unlock_cache_v1\` in \`chrome.storage.session\`. this is what lets a cold service worker (chrome unloaded the SW after idle but the user's autolock window hasn't expired) re-import the AES key without re-running argon2id (which would take seconds and re-prompt for password if we didn't cache).

**critical**: the cache holds the **post-argon2id key bytes**, not the password. the password is never written anywhere persistent. on lock or browser quit, \`chrome.storage.session\` clears.

## the legacy v2 reject

\`chromatika_vault_v2\` used PBKDF2-HMAC-SHA256 with \`iterations ≈ 900_000\` directly to produce the AES-256-GCM key (no envelope layer). on parse, v2 blobs are **rejected** - users clear extension storage and re-onboard. the password-envelope v3 design is incompatible with v2 because v2 didn't have a master-key indirection.

## attack model

- **offline brute force**: attacker exfiltrates \`chromatika_vault_v3\` and tries passwords. each guess costs an argon2id evaluation (~1-3 seconds × 64 MiB RAM). a 12-character random password ≈ 70 bits of entropy → 2^70 guesses → infeasible. a 6-character dictionary word ≈ 30 bits → still very expensive but not impossible if attacker has serious GPU farms. argon2id's memory hardness reduces GPU advantage.
- **session compromise**: if attacker reads \`chrome.storage.session\`, they get the **derived AES key bytes** and can decrypt the vault until the user locks. mitigations: lock on idle, OS screen-lock triggers wallet lock, manual lock button.
- **password capture**: if attacker has keylogger / clipboard access, they get the password directly. chromatika cannot defend against this; user must protect their endpoint.

## password length floor

8 characters is the minimum chromatika enforces. shorter passwords get rejected at the create / unlock surface with an actionable error. 8 chars is **not enough** for adversarial security - it's a "don't use 'password'" minimum. encourage users toward 16+ char random passphrases or pair with a passkey.
`,$b=`# price waterfall + sources

chromatika's USD price oracle is a **waterfall**: try sources in user-configured order until one returns a value, cache for ~60s. defaults: CoinGecko → DefiLlama → CoinMarketCap → Pyth → Chainlink → GeckoTerminal. user can reorder via \`setPricePreferences\`. no SDK imports - pure \`fetch()\` for off-chain sources, on-chain reads for Pyth / Chainlink.

## the cache

\`\`\`ts
const cache = new Map<string, { priceUsd: number, fetchedAtMs: number }>();
const CACHE_TTL_MS = 60_000;

async function getPrice(symbol: string): Promise<number> {
  const cached = cache.get(symbol);
  if (cached && Date.now() - cached.fetchedAtMs < CACHE_TTL_MS) {
    return cached.priceUsd;
  }
  const order = await getPricePreferences();
  for (const source of order) {
    try {
      const price = await sourceFns[source](symbol);
      if (price > 0) {
        cache.set(symbol, { priceUsd: price, fetchedAtMs: Date.now() });
        return price;
      }
    } catch (e) {
      // fall through to next source
    }
  }
  throw new Error(\`no price for \${symbol}\`);
}
\`\`\`

## CoinGecko

\`\`\`ts
async function fetchCoingeckoPrice(symbol: string): Promise<number> {
  const id = COINGECKO_IDS[symbol];   // e.g. "ethereum" for "eth"
  if (!id) throw 'unknown symbol';
  const resp = await fetch(\`https://api.coingecko.com/api/v3/simple/price?ids=\${id}&vs_currencies=usd\`);
  const data = await resp.json();
  return data[id]?.usd ?? 0;
}
\`\`\`

- no API key required (free tier)
- rate-limited (~30 req/min on free tier; chromatika's per-second usage is well under this)
- works for all major assets + many long-tail tokens

## DefiLlama

\`\`\`ts
async function fetchDefiLlamaPrice(symbol: string): Promise<number> {
  const id = DEFILLAMA_IDS[symbol];   // e.g. "coingecko:ethereum"
  const resp = await fetch(\`https://coins.llama.fi/prices/current/\${id}\`);
  const data = await resp.json();
  return data.coins[id]?.price ?? 0;
}
\`\`\`

- no API key
- aggregates from CoinGecko + on-chain DEX TWAPs
- slightly slower than CoinGecko but better coverage for DeFi tokens

## CoinMarketCap (key required)

\`\`\`ts
async function fetchCmcPrice(symbol: string): Promise<number> {
  const apiKey = import.meta.env.VITE_CMC_API_KEY;
  if (!apiKey) return 0;   // not configured
  const sym = symbol.toUpperCase();
  const resp = await fetch(
    \`https://pro-api.coinmarketcap.com/v1/cryptocurrency/quotes/latest?symbol=\${sym}\`,
    { headers: { 'X-CMC_PRO_API_KEY': apiKey } }
  );
  const data = await resp.json();
  return data.data?.[sym]?.quote?.USD?.price ?? 0;
}
\`\`\`

- requires \`VITE_CMC_API_KEY\` build-time env var
- adds redundancy beyond free tiers
- if not configured, the source is a no-op (returns 0, falls through)

## Pyth (on-chain oracle)

\`\`\`ts
async function fetchPythPrice(symbol: string, chain: 'solana' | 'evm' | 'sui' | 'aptos'): Promise<number> {
  const feedId = PYTH_FEED_IDS[chain]?.[symbol];
  if (!feedId) throw 'no Pyth feed';
  // Pyth has a "Hermes" service with REST + price update API
  const resp = await fetch(\`https://hermes.pyth.network/api/latest_price_feeds?ids[]=\${feedId}\`);
  const data = await resp.json();
  const feed = data[0];
  return Number(feed.price.price) * Math.pow(10, feed.price.expo);
}
\`\`\`

- Pyth aggregates publisher-signed prices on-chain across many feeds
- pull-model: clients request the latest price, optionally post the update on-chain to use it in a tx
- Hermes is Pyth's REST gateway for browsers; chromatika uses it for off-chain reads

## Chainlink (EVM-only)

\`\`\`ts
async function fetchChainlinkPrice(symbol: string, chainId: number): Promise<number> {
  const feedAddr = CHAINLINK_FEEDS[chainId]?.[symbol];
  if (!feedAddr) throw 'no Chainlink feed';
  const provider = await getRpcProviderForChain(chainId);
  const aggregator = new ethers.Contract(feedAddr, AGGREGATOR_V3_ABI, provider);
  const [, answer] = await aggregator.latestRoundData();
  const decimals = await aggregator.decimals();
  return Number(answer) / Math.pow(10, decimals);
}
\`\`\`

- direct on-chain read of Chainlink's \`AggregatorV3Interface\`
- only works for EVM chains where Chainlink has feeds (mainnet, BNB, Polygon, Arbitrum, etc.)
- accurate + decentralized but slow (one RPC round-trip per asset per call)

## GeckoTerminal (DEX TWAP)

\`\`\`ts
async function fetchGeckoTerminalPrice(symbol: string): Promise<number> {
  const route = DEX_PRICE_ROUTES[symbol];   // e.g. for IKA: { network, poolAddress }
  if (!route) throw 'no DEX route';
  const resp = await fetch(\`https://api.geckoterminal.com/api/v2/networks/\${route.network}/pools/\${route.poolAddress}\`);
  const data = await resp.json();
  return Number(data.data?.attributes?.base_token_price_usd ?? 0);
}
\`\`\`

- DEX TWAP from GeckoTerminal
- used primarily for IKA (which centralized aggregators don't always carry)
- no API key

## the SUI / IKA peculiarity

IKA price specifically falls to GeckoTerminal because:
- CoinGecko / DefiLlama / CMC don't always list IKA
- Pyth doesn't have a feed today
- Chainlink is EVM-only
- the DEX (Aftermath on Sui) is the canonical price source

so \`getPrice('ika')\` typically traverses several sources before landing on GeckoTerminal. cache TTL of 60s smooths the noise.

## price preferences API

\`\`\`ts
getPricePreferences(): { order: string[] }
setPricePreferences({ order: string[] }): void
// order is a list of source ids; first match wins
// must contain at least 1 entry; entries must be unique
\`\`\`

\`setPricePreferences\` clears the cache so the new order applies immediately.

## what got dropped

- **Switchboard**: listed in \`architecture-final.html\` but **explicitly dropped** from \`src/config/price-sources.ts\`. either remove from architecture-final or implement; tracked
- **on-chain Sui DEX TWAP** beyond GeckoTerminal: not implemented; we read GT instead of querying Sui pools directly. simpler, fewer chain-specific code paths

## library

- \`fetch\` (browser native)
- \`ethers\` v6 for the Chainlink on-chain read
- internal: \`wallet-extension/src/background/services/price.ts\` for \`getPrice\`, \`getPrices\`, the cache
- internal: \`wallet-extension/src/background/services/price-sources.ts\` for individual source implementations + maps
- internal: \`wallet-extension/src/config/price-sources.ts\` for source ids + default order

## related

- [price-source-priority.md](/library/user/price-source-priority) (user-guides) - the user-facing reorder flow
- [manage-networks.md](/library/user/manage-networks) (user-guides) - active EVM chain (used by Chainlink lookups)
`,Qb=`# recovery-words envelope

a BIP39 mnemonic phrase wrapped as an unlock envelope. used as a backup branch on passkey, WAAP, and Lazor envelopes - so if the primary credential is lost (passkey deleted, phone lost, WAAP account suspended), the user can still unlock by typing the words.

note: this is **not** the same as a mnemonic-vault \`seedSource: 'mnemonic'\`. that's the case where BIP39 is the **primary** source of all keys. recovery-words envelopes are an **additional unlock branch** on top of a non-mnemonic-rooted vault (passkey / WAAP / Lazor).

## envelope record

\`\`\`jsonc
{
  "kind": "recovery-words",
  "wordCount": 12 | 24,
  "label": "primary recovery",
  "kdfSaltB64": "<32 random bytes>",
  "probesB64": [
    "<HMAC-SHA256(probe_key, word_0)>",
    "<HMAC-SHA256(probe_key, word_1)>",
    ...
  ],
  "wrappedMasterKeyB64": "<AES-GCM(envKey, masterKey, iv)>",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

\`probes\` is an optional pre-validation array - lets the unlock surface check the words look right (HMAC of each word matches the registered probe) before spending the full HKDF + AES-GCM unwrap. helps catch typos early.

## creation

\`\`\`
1. user provides 12 or 24 BIP39 words at envelope-create time
2. validate words against the BIP39 English wordlist (checksum + dictionary)
3. bip39_seed = mnemonicToSeedSync(words, "")   // 64 bytes via PBKDF2-HMAC-SHA512 (2048 iters)
4. generate 32-byte kdfSalt
5. envKey = HKDF-SHA256(bip39_seed, info='chromatika recovery-words envelope v1', salt=kdfSalt)
6. wrappedMK = AES-GCM(envKey, masterKey, randomIv)
7. (optional) probes = HMAC-SHA256(probe_key, word) for each word, where probe_key is derived from the bip39_seed
8. envelope = { kind, wordCount, label, kdfSaltB64, probesB64, wrappedMasterKeyB64, envIvB64 }
\`\`\`

words are **not stored** in the envelope. only the wrappedMK + (optional) probes survive. typing the words again at unlock time reproduces the bip39_seed → envKey → unwrap.

(in some cases - notably **Lazor** vaults - the recovery words **are** stored as plaintext inside the encrypted vault payload at \`recoveryWordsEncryptedB64\`, alongside the deterministic Solana fee-payer keypair derived from those same words. this is so the wallet can rebuild the in-extension fee-payer keypair on a new install without prompting the user. the envelope itself still doesn't store words - the Lazor vault payload does. see [ika-seed-solana-lazor.md](/library/tech/ika-seed-solana-lazor).)

## unlock

\`\`\`
1. user types the BIP39 phrase
2. validate against BIP39 wordlist + checksum
3. (optional) verify probes match - if any word's HMAC doesn't match, surface "wrong phrase" without spending KDF
4. bip39_seed = mnemonicToSeedSync(words, "")
5. envKey = HKDF-SHA256(bip39_seed, info='chromatika recovery-words envelope v1', salt=kdfSalt)
6. masterKey = AES-GCM.decrypt(envKey, wrappedMK, envIv)
7. session unlocks
\`\`\`

## why HKDF and not argon2id

BIP39's PBKDF2-HMAC-SHA512 with 2048 iterations is itself a slow KDF that resists brute force on the low-entropy mnemonic input. once we have the 64-byte BIP39 seed, that's already 256 bits of entropy - HKDF is the right tool to extract + expand without additional stretching.

## the rotation story

after unlocking via recovery, the user usually wants to register a fresh primary credential:

1. unlock via recovery words
2. add new passkey / WAAP / Lazor envelope (\`addVaultPasskey\` / \`addVaultWaap\` / etc.)
3. optionally remove the old envelope if the user knows which credential was lost

the recovery-words envelope itself can also be re-registered (e.g. user wants to switch from 12 → 24 word phrase). that's "remove old recovery-words envelope, add new one with new phrase".

## the same words on multiple vaults

different vaults can have **different** recovery phrases. if a user reuses one phrase across vaults, that's user choice - the security model doesn't require uniqueness. probes are per-envelope so cross-vault leakage of one phrase doesn't reveal anything about other vaults beyond "are these the same words".

## not the same as a mnemonic vault root

a vault with \`seedSource: 'mnemonic'\` uses BIP39 to **derive every key in the vault** (sui keypair, solana keypair, ika seed). a vault with a recovery-words envelope but \`seedSource: 'passkey-prf'\` uses BIP39 only for the unlock branch - the **dWallet identity** comes from the passkey PRF or whatever the primary source is. typing recovery words on the passkey vault unlocks the same masterKey, which decrypts the same vault payload, which contains the same passkey-derived ika keys.

so:
- mnemonic vault → recovery phrase **is** the vault. losing it = losing everything.
- passkey vault + recovery branch → recovery phrase is a **backup unlock**. losing it = losing one of multiple unlock paths, but the vault still works as long as the passkey works.

## library

- \`@scure/bip39\` for \`validateWords\`, \`mnemonicToSeedSync\`
- \`crypto.subtle.deriveKey\` for HKDF-SHA256
- \`crypto.subtle.encrypt\` / \`.decrypt\` for AES-GCM
- internal helpers: \`buildRecoveryWordsEnvelopeRef(mk, bip39_seed, { wordCount, label })\`, \`unlockRecoveryWordsEnvelope\`, \`validateProbes\`
`,Jb=`# session state + multi-vault model

chromatika's \`SessionState\` is the in-memory snapshot of "what's unlocked right now". it holds the master AES key (as a non-extractable \`CryptoKey\`), the active vault id, per-vault dWallet meta, fee material, ika share keys, and pending request queues. switching active vault swaps a chunk of this state without re-prompting the password.

## the shape (high-level)

\`\`\`ts
type SessionState = {
  // multi-vault
  vaultKey: CryptoKey;                            // non-extractable AES-GCM master key (decrypts vault blob)
  vaultKdfMeta: KdfMeta;                          // argon2id salt + params (for re-deriving on switch if needed)
  activeVaultId: string;
  vaults: VaultRecord[];                          // all vault records (decrypted)

  // active-vault-scoped
  activeDwalletMeta: DwalletMeta[];               // active vault's dWallet list
  activeIkaShareKeys: { SECP256K1, ED25519 };     // per-curve UserShareEncryptionKeys instances
  activeFeeMaterial: FeeMaterial;                 // sui keypair + solana fee-payer keypair
  activePresignPools: { SECP256K1_ECDSA, SECP256K1_TAPROOT, ED25519_EDDSA };

  // hardware
  solanaMwaAccount: HardwareVaultRecord | null;   // active MWA Seeker account
  solanaWcAccount: HardwareVaultRecord | null;    // active WC account (for x402)

  // pending requests (in chrome.storage.session for SW restart survivability)
  pendingTxApprovals: Map<string, PendingTxApproval>;
  pendingHardwareSigns: Map<string, PendingHardwareSign>;
  pendingMcpSigns: Map<string, PendingMcpSign>;
  pendingX402Approvals: Map<string, PendingX402Approval>;
  pendingPasskeyRequests: Map<string, PendingPasskey>;

  // lifecycle
  unlockedAtMs: number;
  lockAtMs: number;                               // autolock timer end
};
\`\`\`

## the unlock flow → session populate

\`\`\`
1. user provides credential (password / passkey / signature / words)
2. derive envelope key
3. unwrap masterKey → import as non-extractable CryptoKey
4. read chromatika_vault_v3 from chrome.storage.local
5. AES-GCM decrypt → JSON.parse → { v: 3, vaults: [...], activeVaultId }
6. populate sessionState:
   - vaultKey, vaultKdfMeta
   - activeVaultId
   - vaults
7. for the active vault, populate scoped fields:
   - activeDwalletMeta (merge in-blob with overlay from chromatika_dwallet_meta_v2_<id>)
   - activeIkaShareKeys (deserialize from record.ikaShareKeysB64, or re-derive via makeSeed if missing)
   - activeFeeMaterial (mnemonic / privkey / hardware-record-derived)
   - activePresignPools (load from chromatika_presign_pools_v3_<id>)
8. write the unlock cache:
   chrome.storage.session.set({
     chromatika_unlock_cache_v1: { masterKeyBytesB64, vaultBlobIvB64, lockAtMs, ... }
   });
9. arm the autolock alarm
10. broadcast 'unlocked' event to all UI ports
\`\`\`

## the switch-vault flow

\`\`\`ts
async function switchVault(targetVaultId: string) {
  const targetRecord = sessionState.vaults.find(v => v.id === targetVaultId);
  if (!targetRecord) throw 'vault not found';

  // cleanup old active state
  await persistActiveVaultEphemera();   // flush dwalletMeta + presign pools to storage

  // swap active id
  sessionState.activeVaultId = targetVaultId;

  // populate new active scoped fields
  sessionState.activeDwalletMeta = mergeMeta(targetRecord.dwalletMeta, await loadDwalletMetaOverlay(targetVaultId));
  sessionState.activeIkaShareKeys = await buildIkaShareKeys(makeSeedForRecord(targetRecord), targetRecord.ikaShareKeysB64);
  sessionState.activeFeeMaterial = await feeMaterialFromVaultRecord(targetRecord);
  sessionState.activePresignPools = await loadPresignPools(targetVaultId);

  // persist activeVaultId in the vault blob
  await persistVaultBlob({ ...sessionState.vaults }, targetVaultId);

  // broadcast 'vault-changed' to UI ports + dapp bridge
  // dapp bridge re-emits accountsChanged to connected origins
}
\`\`\`

key observation: switchVault **doesn't re-prompt for password**. the masterKey already unwrapped each vault's data. switching just reloads the per-vault scoped fields.

multi-vault is one masterKey wrapping many vault records. each vault has its own credentials (mnemonic, privkey, etc.) but they all sit inside the same AES-GCM-sealed payload, decryptable by the masterKey held in session.

## the "session never has plaintext password" rule

the session holds:
- \`vaultKey\` (non-extractable \`CryptoKey\` - the imported AES key, can be used to encrypt / decrypt but bytes can't be extracted via JS)
- \`vaultKdfMeta\` (Argon2id salt + params - public-equivalent; useful only if combined with a password)

it does **not** hold:
- the password (string)
- any envelope's KDF input (PRF output, signature bytes) after the unwrap completes

if you read the session state via dev tools, you cannot recover the password.

## the unlock cache holds key bytes

the **post-argon2id key bytes** (b64) live in \`chrome.storage.session\` for cold-SW rehydrate. these are sensitive - anyone who can read session storage can decrypt the vault until lock. mitigations: lock-on-OS-screenlock, manual lock, autolock window. see [cold-sw-unlock-cache.md](/library/tech/cold-sw-unlock-cache).

## the dapp-bridge contract

connected origins see a stable mental model:
- \`accounts\` = the active dWallet's per-curve addresses
- \`chainId\` (EVM) = the active EVM chain id

switching active dWallet → emit \`accountsChanged\` to connected origins.
switching active EVM chain → emit \`chainChanged\`.
switching active vault (which usually changes both) → emit both, plus optionally \`disconnect\` if the new vault's dWallet doesn't have a permission for that origin.

## the pending request queues

for any flow that needs user approval (TX_APPROVE, HW_SIGN, MCP_APPROVE, X402_APPROVE, passkey ceremonies), chromatika enqueues the request in a \`Map<string, ...>\` keyed by random UUID. the pending entries live in \`chrome.storage.session\` so a service worker restart doesn't lose them.

\`\`\`ts
async function enqueuePending(kind: string, request: any) {
  const id = randomUUID();
  const stored = await chrome.storage.session.get('chromatika_pending');
  stored[kind] = stored[kind] ?? {};
  stored[kind][id] = request;
  await chrome.storage.session.set({ chromatika_pending: stored });
  return id;
}
\`\`\`

popups read by id (\`?txapprove=<id>\`, \`?hwsign=<id>\`, etc.) and call back via tRPC to resolve.

## the dWallet meta merge

\`activeDwalletMeta\` is the in-memory merge of:
1. **in-blob copy** - in \`record.dwalletMeta\` inside the vault payload (authoritative)
2. **overlay** - in \`chromatika_dwallet_meta_v2_<vaultId>\` (potentially fresher for some flows)

merge prefers the overlay timestamp where present. \`syncVaultMeta\` flushes session state into the overlay on demand.

## library

- internal: \`wallet-extension/src/background/session.ts\` for session orchestration
- internal: \`wallet-extension/src/background/wallet-service.ts\` for the unlock + switchVault impls
- internal: \`wallet-extension/src/background/keyring/hd.ts\` for \`feeMaterialFromVaultRecord\`, \`buildIkaShareKeys\`

## related

- [vault-blob-v3-format.md](/library/tech/vault-blob-v3-format) - the on-disk format
- [cold-sw-unlock-cache.md](/library/tech/cold-sw-unlock-cache) - the session-storage rehydrate
- [multi-envelope-design.md](/library/tech/multi-envelope-design) - how unlock unwraps the masterKey
- [ika-seed-derivation-overview.md](/library/tech/ika-seed-derivation-overview) - the seed factories that populate ikaShareKeys
`,Yb=`# SHA-512 and BLAKE2b in chromatika

two hash functions that show up specifically around Sui personal-message signing - the place where ika's signing scheme and Mysten's native intent format diverge. understanding the gap matters because it's the reason some Sui dapps may not verify chromatika's \`sui_signPersonalMessage\` output.

## SHA-512 (used by ika ed25519 signing)

ika's ed25519 EdDSA implementation uses SHA-512 as its hash function in two places per RFC 8032:
1. expanding the secret seed into the secret scalar + nonce prefix
2. hashing \`(R || A || M)\` to produce the challenge scalar \`k\`

this is **standard ed25519** - the function that signs is \`EdDSA(secret, message)\` where the implementation hashes the message internally with SHA-512.

when chromatika's \`sui_signPersonalMessage\` runs, it hands ika the **raw user-supplied message bytes** (the actual UTF-8 of "I agree to the terms" or whatever). ika hashes those with SHA-512 as part of standard ed25519 signing, produces a 64-byte ed25519 signature, returns it.

## BLAKE2b (used by Mysten's PersonalMessage intent)

Mysten's wallet-side native primitive \`signPersonalMessage\` does **not** sign raw bytes. it wraps them in a Sui *intent*:

\`\`\`
intent_message = [0x03, 0x00, 0x00] || bcs(PersonalMessage { message: bytes })
digest = blake2b_256(intent_message)
signature = ed25519_sign(digest, key)
\`\`\`

the leading \`[0x03, 0x00, 0x00]\` is the intent prefix: scope=PersonalMessage, version=V0, app=Sui. the digest is **BLAKE2b-256** (32-byte output, the hash function bitcoin / monero / sui / aptos use widely; not the same as SHA-2 family). the signature is then over the BLAKE2b digest, not over the original bytes.

## the divergence

dapps that verify with \`verifyPersonalMessageSignature\` from \`@mysten/sui\` compute the **BLAKE2b-of-intent** digest first and then verify the ed25519 signature against that digest. when chromatika hands them an ika-produced sig (which is over \`SHA512-of-raw-bytes\` per standard ed25519), the verification fails - same key, same logical message, different digest input.

this is **intentional today** because ika's signing path doesn't yet produce intent-wrapped digests on its server side, and chromatika cannot post-hoc wrap a finished signature. dapps that verify with \`tweetnacl-style\` raw ed25519 (sign over the literal bytes) accept the signature; dapps that strictly use Mysten's intent flow do not.

tracked future: add a parallel "Mysten BLAKE2b path" that wraps the message in the intent client-side, hashes BLAKE2b-256, and asks ika to ed25519-sign the digest as if it were the message. when that lands, both paths exist; the user picks (or chromatika dispatches based on dapp metadata).

## SHA-512 elsewhere

SHA-512 also surfaces in:
- BIP39 → BIP39-seed: PBKDF2-HMAC-SHA512 with 2048 iterations against the password "mnemonic" || passphrase. produces the 64-byte BIP39 seed that BIP44 and SLIP10 derive from
- HMAC-SHA512 inside BIP32 (master key + chain-code derivation)
- HMAC-SHA512 inside SLIP10 ed25519 derivation

## BLAKE2b elsewhere

- Sui address derivation (32-byte address = \`blake2b_256(scheme_flag || pubkey)\`)
- passkey-vault Sui address derivation: \`blake2b_256(0x06 || compressed_secp256r1_pubkey)\` per SIP-9 (flag \`0x06\` distinguishes passkey from plain secp256r1)
- Sui object id derivation, transaction digesting (out of scope for chromatika - we don't compute these, the Sui client / GraphQL does)

## libraries

- SHA-512: \`@noble/hashes/sha2\` (\`sha512\`); used inside \`@noble/ed25519\` so we rarely call it directly
- BLAKE2b-256: \`@noble/hashes/blake2b\` (\`blake2b256\`); used in the address derivation helpers in \`keyring/passkey-derive.ts\` and the Sui SDK internally

## quick lookup

| where | hash | input | output | who calls it |
|-------|------|-------|--------|--------------|
| ika ed25519 sign | SHA-512 | raw msg bytes | 64-byte sig | ika MPC server |
| Sui native PersonalMessage | BLAKE2b-256 | intent || bcs(msg) | 32-byte digest, then ed25519 sign | dapps using \`@mysten/sui\` |
| Sui address | BLAKE2b-256 | scheme_flag || pubkey | 32-byte address | chromatika + Sui SDK |
| Passkey Sui addr | BLAKE2b-256 | 0x06 || pk_compressed | 32-byte address | chromatika passkey-derive |
| BIP39 seed | PBKDF2-HMAC-SHA512 | mnemonic + "mnemonic" || passphrase | 64-byte seed | \`@scure/bip39\` |
| BIP32 / SLIP10 master | HMAC-SHA512 | seed | 64 bytes (key + chain) | \`@scure/bip32\` / SLIP10 helper |
`,Xb=`# \`parseSignatureFromSignOutput\` and signature normalization

ika returns signature bytes in protocol-specific formats. \`parseSignatureFromSignOutput(bytes, curve, algorithm)\` from \`@ika.xyz/sdk\` is the canonical helper that asserts the right length + decodes into typed \`(r, s)\` or \`(R, S)\` components. chromatika uses it on every signing path.

## the signatures we deal with

| curve | algorithm | output bytes | components |
|-------|-----------|--------------|------------|
| SECP256K1 | ECDSASecp256k1 | 64 bytes | \`r\` (32 bytes) + \`s\` (32 bytes), compact |
| SECP256K1 | Taproot (BIP340 Schnorr) | 64 bytes | \`R\` (32 bytes x-only) + \`s\` (32 bytes) |
| SECP256K1 | ECDSASecp256r1 | 64 bytes | \`r\` (32 bytes) + \`s\` (32 bytes), compact - **secp256r1 not used by chromatika today** |
| ED25519 | EdDSA | 64 bytes | \`R\` (32 bytes) + \`S\` (32 bytes) |
| RISTRETTO | SchnorrkelSubstrate | 64 bytes | not used by chromatika today |

every chromatika signature is 64 bytes from ika. the ECDSA outputs **don't** include \`v\` - chromatika recovers \`v\` separately for EVM (see [ecdsa-secp256k1.md](/library/tech/ecdsa-secp256k1)).

## the call

\`\`\`ts
import { parseSignatureFromSignOutput, Curve, SignatureAlgorithm } from '@ika.xyz/sdk';

const { r, s } = parseSignatureFromSignOutput(
  sigBytes,                              // Uint8Array from ika
  Curve.SECP256K1,                       // 0
  SignatureAlgorithm.ECDSASecp256k1,     // 0
);
// r, s are each Uint8Array(32)
\`\`\`

for ed25519 / Schnorr the components are named \`R\` and \`S\` (or in some helper signatures, \`R\` and \`s\`); the function returns the right shape per algorithm.

## what it asserts

- input length matches the expected signature size for the curve+algorithm
- internal byte ordering / encoding matches the protocol (e.g. ECDSA's \`(r, s)\` are big-endian scalars; Schnorr's \`R\` is x-only and big-endian; ed25519's \`R, S\` are little-endian per RFC 8032)
- canonical form (e.g. ECDSA \`s\` is in the lower-half of the curve order to prevent malleability - \`s ≤ n/2\`)

if anything's off, \`parseSignatureFromSignOutput\` throws. callers should not catch generically; a parse failure means ika produced something unexpected, which is a bug to surface.

## the ika constants (recap)

\`\`\`js
Curve.SECP256K1 = 0
Curve.SECP256R1 = 1
Curve.ED25519 = 2
Curve.RISTRETTO = 3

SignatureAlgorithm.ECDSASecp256k1 = 0
SignatureAlgorithm.Taproot = 1
SignatureAlgorithm.ECDSASecp256r1 = 2
SignatureAlgorithm.EdDSA = 3
SignatureAlgorithm.SchnorrkelSubstrate = 4
\`\`\`

per CLAUDE.md, \`fromCurveToNumber\` is **not** exported from \`@ika.xyz/sdk\` main entry - it's internal. hardcode the constants above; don't try to import the helper.

## EVM v-recovery (separate step)

ECDSA-secp256k1 doesn't include \`v\` in the standard signature - it's an EVM convention. chromatika picks \`v\` after parsing:

\`\`\`ts
const { r, s } = parseSignatureFromSignOutput(sigBytes, Curve.SECP256K1, SignatureAlgorithm.ECDSASecp256k1);
const digest = ethers.keccak256(preimage);
const knownAddress = await getEvmAddress();

let v: 27 | 28;
const candidate27 = ethers.recoverAddress(digest, { r, s, v: 27 }).toLowerCase();
const candidate28 = ethers.recoverAddress(digest, { r, s, v: 28 }).toLowerCase();

if (candidate27 === knownAddress.toLowerCase()) {
  v = 27;
} else if (candidate28 === knownAddress.toLowerCase()) {
  v = 28;
} else {
  throw new Error('signature does not recover to expected address');
}
\`\`\`

if neither candidate matches, the signature is for a different key OR the preimage was double-hashed somewhere. either way, abort.

## DER encoding (Bitcoin ECDSA)

bitcoin signatures use DER encoding instead of compact \`r||s\`. format:

\`\`\`
0x30 || total_length ||
0x02 || r_length || r_bytes ||
0x02 || s_length || s_bytes
\`\`\`

DER has variable-length encoding because both \`r\` and \`s\` may have different effective lengths (leading zeros stripped, and a leading 0x00 padding added if the high bit is set to indicate positive). bitcoinjs-lib has DER encode / decode helpers.

chromatika converts ika's compact \`(r, s)\` to DER for Bitcoin signing:
\`\`\`ts
const derSig = bitcoin.script.signature.encode(
  Buffer.concat([r, s]),    // 64-byte compact
  hashType,                  // 0x01 for SIGHASH_ALL
);
// derSig is the DER-encoded sig + 1-byte hash type
\`\`\`

## Schnorr is BIP340-encoded

Bitcoin Schnorr signatures (taproot) are 64 bytes (\`R || s\`) for SIGHASH_DEFAULT, or 65 bytes (\`R || s || hashType\`) for any other sighash. chromatika produces 64-byte SIGHASH_DEFAULT signatures by default.

\`R\` is the **x-only** representation (32 bytes, just the x-coordinate; y is implicitly even per BIP340). this differs from full \`R = (x, y)\` encoding ed25519 uses internally.

## ed25519 is RFC 8032

ed25519 signatures are 64 bytes per RFC 8032: \`R || S\` where both are little-endian. ika's output is already in this canonical form; just split the 64 bytes in half.

## the chromatika rule

across all paths:
1. call \`parseSignatureFromSignOutput(sigBytes, curve, algorithm)\` with the right enums
2. trust the returned \`(r, s)\` / \`(R, S)\` to be canonical
3. for EVM, recover \`v\` separately
4. for Bitcoin ECDSA, DER-encode + add hashType byte
5. for Bitcoin Schnorr, just \`R || s\` (or with hashType byte for non-default sighash)
6. for ed25519, \`R || S\` directly

never hand-write byte parsing of ika's output. the SDK helper is the canonical interpretation.

## library

- \`@ika.xyz/sdk\` \`parseSignatureFromSignOutput\`, \`Curve\`, \`SignatureAlgorithm\`
- \`ethers\` v6 \`recoverAddress\` for EVM v-recovery
- \`bitcoinjs-lib\` DER encode for Bitcoin ECDSA
- internal: chromatika never re-implements these conversions; always uses the SDK / library helpers

## related

- [ecdsa-secp256k1.md](/library/tech/ecdsa-secp256k1) - the ECDSA path + v-recovery details
- [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - the ed25519 path
- [taproot-schnorr.md](/library/tech/taproot-schnorr) - the Schnorr / BIP340 path
- [evm-send-flow.md](/library/tech/evm-send-flow), [evm-personal-sign-and-typeddata.md](/library/tech/evm-personal-sign-and-typeddata), [solana-tx-sign.md](/library/tech/solana-tx-sign), [btc-tx-sign-segwit-taproot.md](/library/tech/btc-tx-sign-segwit-taproot), [aptos-sign.md](/library/tech/aptos-sign), [sui-tx-sign-via-ika.md](/library/tech/sui-tx-sign-via-ika) - the per-chain signing flows that all call this helper
`,Zb=`# signed-tx record store + activity feed merge

\`SignedTxRecord\` is chromatika's local-first metadata about every transaction **this wallet** signed. captures stuff explorers don't expose (like dapp origin at sign time) plus pointer slots for encrypted user notes. lives in \`chrome.storage.local['chromatika_signed_txs_v1']\`, scoped per vault, FIFO-capped at 500. activity-feed merges these records onto explorer rows by tx hash.

## the record shape

\`\`\`ts
type SignedTxKind =
  | 'evm-send'
  | 'evm-message-sign'           // future
  | 'evm-typed-data'             // future
  | 'sui-send'                   // future
  | 'sui-message-sign'           // future
  | 'sol-send'                   // future
  | 'sol-message-sign'           // future
  | 'sol-tx-sign'                // future
  | 'btc-send'                   // future
  | 'apt-send';                  // future

type SignedTxChainId = number | string;   // number for EVM (chainId), string for others ('sui-mainnet', 'sol-mainnet', etc.)

interface SignedTxRecord {
  txHash: string;                // EVM tx hash, Sui digest, Solana signature, BTC txid, Aptos tx hash
  origin: string | null;         // dapp origin at sign time, or null for wallet-UI sends
  chainId: SignedTxChainId;
  vaultId: string;               // vault that signed
  timestampMs: number;           // local clock at broadcast (authoritative ordering)
  kind: SignedTxKind;
  encryptedNote?: EncryptedRef;  // optional encrypted user note (see activity-notes-encrypt-decrypt.md)
}
\`\`\`

\`origin\` is the load-bearing field: it captures **where the tx came from** (which dapp) - information no chain explorer can give you back later. when you look at your activity feed weeks later and see "sent USDC on Polygon," \`origin\` tells you "this was from uniswap.org" vs "this was from a phishing clone you don't remember."

## the storage shape

\`\`\`jsonc
chrome.storage.local["chromatika_signed_txs_v1"] = {
  "<vaultId-A>": [
    { txHash: "0x...", origin: "https://uniswap.org", chainId: 137, vaultId: "vault-A-id", timestampMs: ..., kind: "evm-send", encryptedNote: {...} },
    // ... up to 500 records, oldest pruned ...
  ],
  "<vaultId-B>": [
    // ... independent FIFO queue per vault ...
  ]
}
\`\`\`

per-vault scoping means switching active vaults loads a different record set. records from vault A are invisible while vault B is active.

## the API

\`\`\`ts
// write a record after broadcast success
async function recordSignedTx(rec: SignedTxRecord): Promise<void> {
  const all = await loadAll();
  const list = all[rec.vaultId] ?? [];
  list.unshift(rec);                       // newest first
  if (list.length > 500) list.length = 500; // FIFO cap
  all[rec.vaultId] = list;
  await chrome.storage.local.set({ chromatika_signed_txs_v1: all });
}

// look up a single record by hash for the active vault
async function getSignedTxByHash(txHash: string, vaultId: string): Promise<SignedTxRecord | null> {
  const all = await loadAll();
  const list = all[vaultId] ?? [];
  return list.find(r => r.txHash === txHash) ?? null;
}

// build a fast lookup map for the activity feed merge
async function getSignedTxsMap(vaultId: string): Promise<Map<string, SignedTxRecord>> {
  const all = await loadAll();
  const list = all[vaultId] ?? [];
  return new Map(list.map(r => [r.txHash, r]));
}

// patch the encryptedNote field (replace or clear)
async function updateSignedTxNote(txHash: string, vaultId: string, ref: EncryptedRef | null): Promise<boolean> {
  const all = await loadAll();
  const list = all[vaultId] ?? [];
  const rec = list.find(r => r.txHash === txHash);
  if (!rec) return false;
  if (ref === null) delete rec.encryptedNote;
  else rec.encryptedNote = ref;
  await chrome.storage.local.set({ chromatika_signed_txs_v1: all });
  return true;
}
\`\`\`

## who writes records

today: **EVM send paths only**.

- \`signAndBroadcastEvmTxForLedgerWithApprovalFlow\` in \`evm-send.ts\` (line 649): after broadcast success for a Ledger-signed EVM tx
- \`signAndBroadcastEvmTxForIkaWithApprovalFlow\` in \`evm-send.ts\` (line 713): after broadcast success for an ika-signed EVM tx

\`\`\`ts
// after broadcast success, both paths call:
await recordSignedTx({
  txHash: txHashOut,
  origin: params.dappOrigin ?? null,    // dapp-bridge sets this; null for wallet-UI sends
  chainId: filled.chainId,
  vaultId: ikaSession.activeVaultId,
  timestampMs: Date.now(),
  kind: 'evm-send',
});
\`\`\`

errors are caught + logged via console.warn but don't block the txHash return. the broadcast already happened; failing to record is non-fatal.

**not** writing records yet:
- \`signEvmTxOnly\` (sign-without-broadcast for relayer / bundler / abstract-wallet flows) - reserves nonce but doesn't broadcast, so recording would mislead the activity feed
- Sui sends (\`sendSuiNative\`)
- Solana sends (\`sendSolanaNative\`)
- Bitcoin sends (\`sendBtcNative\`)
- Aptos sends (\`sendAptosNative\`)
- message signing paths (evm-message-sign, sol-message-sign, etc.)
- dapp-bridge sign-only flows

filling these in is **tracked future hardening** per STATUS.md line 89. when those paths add \`recordSignedTx\` calls, encrypted-note attachments will work for them too.

## the activity feed merge

\`src/background/services/activity.ts\` merges signed-tx records onto explorer-fetched rows:

\`\`\`ts
async function getMultiChainActivity(s: Session, limitPerChain = 12): Promise<ActivityItem[]> {
  // 1. fetch chain-specific rows in parallel
  const [suiRows, evmRows, solRows, btcRows] = await Promise.all([
    fetchSuiActivity(s, limitPerChain),
    fetchEvmActivity(s, limitPerChain),
    fetchSolanaActivity(s, limitPerChain),
    fetchBitcoinActivity(s, limitPerChain),
  ]);

  // 2. merge into a single list
  const merged: ActivityItem[] = [...suiRows, ...evmRows, ...solRows, ...btcRows];

  // 3. fetch local signed-tx records for the active vault
  const txMap = await getSignedTxsMap(s.activeVaultId);

  // 4. join: for each merged row, look up by digest === txHash
  for (const item of merged) {
    const rec = txMap.get(item.id);   // item.id is the tx digest
    if (rec) {
      item.origin = rec.origin;
      item.signedByThisWallet = true;
      if (rec.encryptedNote) item.hasEncryptedNote = true;
    }
  }

  // 5. sort newest-first, cap at 24-30 items
  merged.sort((a, b) => b.timestampMs - a.timestampMs);
  return merged.slice(0, Math.max(limitPerChain * 2, 30));
}
\`\`\`

key fields the merge populates:
- \`item.origin\` (string | null | undefined) - dapp origin URL if known
- \`item.signedByThisWallet\` (boolean) - true if there's a local record for this digest
- \`item.hasEncryptedNote\` (boolean) - true if the record has an \`encryptedNote\` field

UI uses these to:
- render dapp origin hostname under each tx ("via uniswap.org")
- gate the "+ note" / "view note" buttons (only for \`signedByThisWallet === true\`)
- render the lock badge (only when \`hasEncryptedNote === true\`)

incoming transfers (txs from someone else to you) don't have a local record → \`signedByThisWallet\` is undefined → no note buttons. correct semantics: you can only attach notes to txs you signed.

## chain-specific quirks

- **Sui** (\`activity.ts\` lines 80-84): primary path is \`queryTransactionBlocksGraphQL(client, { filter: { affectedAddress }, limit })\` using \`@mysten/sui\` 2.13.2's hand-rolled \`transactionBlocks\` doc. JSON-RPC fallback is still live per STATUS.md (no GraphQL list wrapper for filtered/scoped queries yet)
- **EVM** (lines 204-231): only Blockscout v2 API is wired today. other explorers (Etherscan, etc.) return \`[]\`. tracked future
- **Solana** (lines 136-162): \`getSignaturesForAddress\` + optional Encrypt program label detection on top 12 results (when vault is on Solana ika base)
- **Bitcoin** (lines 169-190): Esplora API for address tx history

## what doesn't merge

- the merged list **drops** records that don't have a corresponding explorer row. so if you signed a tx that the explorer hasn't indexed yet (stale RPC), you won't see it in activity until the explorer catches up. a separate "drain analysis" / "panic forensics" view could surface these orphaned records (tracked future)
- records older than the explorer's window also drop. if you signed a tx 6 months ago and the explorer only returns last 100, the record is lost from the activity view (still in the local store, but unjoined)

## library

- internal: \`src/background/services/tx-record.ts\` for the store API
- internal: \`src/background/services/activity.ts\` for the merge
- internal: \`src/background/chains/evm-send.ts\` for the write hooks (lines 647, 711)
- \`chrome.storage.local\` for persistence

## related

- [encryption-backend-abstraction.md](/library/tech/encryption-backend-abstraction) - the \`EncryptedRef\` shape on \`encryptedNote\`
- [activity-notes-encrypt-decrypt.md](/library/tech/activity-notes-encrypt-decrypt) - how encryptedNote is set + read
- [view-activity-and-portfolio.md](/library/user/view-activity-and-portfolio) (user-guides) - the user-facing surface
`,eS=`# Solana transaction signing via ika MPC

Solana versioned transactions signed via ika MPC ED25519 EDDSA. dapp-originated \`solana_signTransaction\` and wallet-UI \`sendSolanaNative\` both end up here. on Sui ika base, signing produces real distributed MPC signatures; on Solana ika base (pre-alpha), signatures come from a single mock signer.

## the flow

\`\`\`ts
async function signSolanaTx(versionedTx: VersionedTransaction) {
  // 1. extract the signable message bytes
  const messageBytes = versionedTx.message.serialize();
  // this is the v0 message (header + accounts + recent_blockhash + instructions)
  // NOT the full tx (which would include signature slots)

  // 2. take a presign
  const presignId = takePresign('ED25519_EDDSA');

  // 3. sign via ika - PASS RAW MESSAGE BYTES (ika sha-512s internally per RFC 8032)
  const sigBytes = await ikaSign({
    dwalletId: activeEd25519DwalletId,
    curve: Curve.ED25519,
    algorithm: SignatureAlgorithm.EdDSA,
    message: messageBytes,
    presignId,
  });

  // 4. parse 64-byte ed25519 sig
  const { R, S } = parseSignatureFromSignOutput(sigBytes, Curve.ED25519, SignatureAlgorithm.EdDSA);
  const sig64 = new Uint8Array([...R, ...S]);

  // 5. assign signature to the tx's signature slot
  versionedTx.signatures[0] = sig64;

  // 6. tx is now ready to broadcast
  return versionedTx;
}
\`\`\`

## what gets signed

the **serialized v0 message** (\`versionedTx.message.serialize()\`), not the whole tx. v0 message layout (per Solana):

\`\`\`
[message_header (3 bytes)]
   numRequiredSignatures (u8)
   numReadonlySignedAccounts (u8)
   numReadonlyUnsignedAccounts (u8)
[static_account_keys (compact array of 32-byte pubkeys)]
[recent_blockhash (32 bytes)]
[instructions (compact array of CompiledInstruction)]
[address_table_lookups (compact array of MessageAddressTableLookup)]
\`\`\`

signers verify by:
\`\`\`
ed25519_verify(message_bytes, signature, signer_pubkey)
// internally: sha-512(message_bytes), then verify on the curve
\`\`\`

ika's ED25519 EdDSA signs raw bytes (sha-512s internally). hand it the message bytes; the signature works.

## broadcasting

\`\`\`ts
async function sendSolanaTx(tx: VersionedTransaction) {
  const signed = await signSolanaTx(tx);
  const connection = new Connection(SOLANA_RPC_URL);
  const sig = await connection.sendRawTransaction(signed.serialize(), {
    skipPreflight: false,
    preflightCommitment: 'processed',
  });
  await connection.confirmTransaction(sig, 'confirmed');
  return sig;
}
\`\`\`

\`sendRawTransaction\` returns the tx signature (which is the same 64-byte ed25519 sig in base58 form). \`confirmTransaction\` waits for the network to confirm at the requested commitment level.

## sendSolanaNative path

\`\`\`ts
async function sendSolanaNative({ to, amountSol }) {
  const lamports = BigInt(Math.round(amountSol * LAMPORTS_PER_SOL));
  const from = new PublicKey(await getSolanaAddress());
  const toPk = new PublicKey(to);

  const connection = new Connection(SOLANA_RPC_URL);
  const { blockhash } = await connection.getLatestBlockhash('finalized');

  const message = new TransactionMessage({
    payerKey: from,
    recentBlockhash: blockhash,
    instructions: [
      SystemProgram.transfer({
        fromPubkey: from,
        toPubkey: toPk,
        lamports: Number(lamports),
      }),
    ],
  }).compileToV0Message();

  const tx = new VersionedTransaction(message);
  return sendSolanaTx(tx);
}
\`\`\`

native SOL sends are exactly one SystemProgram.transfer instruction. SPL token sends would use spl-token's \`createTransferInstruction\` (see [x402-solana-tx-build.md](/library/tech/x402-solana-tx-build) for an example with USDC).

## dapp \`solana_signTransaction\` path

when a dapp asks for \`solana_signTransaction\` via the dapp bridge, chromatika:
1. enqueues the tx as a pending sign request
2. opens a sign-approval popup
3. on approve, runs \`signSolanaTx\`
4. returns the signed tx (or just the signature) to the dapp via the bridge

a sibling MCP tool (\`sendSolanaTx\` for agents) is **tracked future** - today MCP exposes only EVM \`sendEvmTx\` / \`signTransaction\` plus a chain-agnostic \`signMessage\`.

## SOL on Sui ika base vs Solana ika base

- **Sui ika base** + ED25519 dWallet: real ika 2PC-MPC ED25519 signing. neither chromatika nor the ika network alone can sign. production-grade
- **Solana ika base** + ED25519 dWallet: pre-alpha. **single mock signer**, not real distributed MPC. the disclaimer in CLAUDE.md is unambiguous: "never trust Solana-base signatures for real value"

both paths produce the same byte-shape ed25519 signature, but the trust model differs. don't confuse them.

## blockhash freshness

Solana txs include a recent_blockhash; the network rejects txs whose blockhash is older than ~150 blocks (~60 seconds). chromatika fetches the blockhash inside the build flow, right before signing. if the user takes >60s reading the popup, the blockhash may expire - on submit, the user gets "blockhash not found" and re-signs with a fresh blockhash.

## library

- \`@solana/web3.js\` \`Connection\`, \`VersionedTransaction\`, \`TransactionMessage\`, \`SystemProgram\`
- \`@solana/spl-token\` for SPL transfers
- internal: \`wallet-extension/src/background/chains/signing/solana.ts\`

## related

- [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - underlying signature algorithm
- [signature-normalization.md](/library/tech/signature-normalization) - parseSignatureFromSignOutput details
- [x402-solana-tx-build.md](/library/tech/x402-solana-tx-build) - SPL transfer construction example
`,nS="# `SuiGraphQLClient` (Mysten's Sui GraphQL transport)\n\nchromatika prefers `SuiGraphQLClient` (`@mysten/sui` `client.core.*`) for every Sui operation Mysten exposes on GraphQL. JSON-RPC stays for legacy gaps only. ika's `IkaClient` is wired to chromatika's vault-shared `SuiGraphQLClient` - never JSON-RPC for supported paths.\n\n## why GraphQL over JSON-RPC\n\nMysten is migrating Sui's read API from JSON-RPC to GraphQL. GraphQL:\n- single endpoint per network (cleaner than RPC method dispatch)\n- structured + typed queries (vs JSON-RPC's stringly-typed methods)\n- field selection (only fetch what you need; reduces wire traffic)\n- supports complex queries (graph traversal: object owner's other objects, balance changes per address, etc.)\n\n`client.core.*` is the canonical Mysten SDK surface for GraphQL. chromatika builds on this.\n\n## construction\n\n```ts\nimport { SuiGraphQLClient } from '@mysten/sui/graphql';\n\nconst client = new SuiGraphQLClient({\n  url: 'https://sui-mainnet.mystenlabs.com/graphql',\n});\n```\n\nper network:\n- mainnet: `https://sui-mainnet.mystenlabs.com/graphql`\n- testnet: `https://sui-testnet.mystenlabs.com/graphql`\n- devnet: `https://sui-devnet.mystenlabs.com/graphql`\n\ncustom networks can override via the network registry (`chromatika_custom_networks_v1`).\n\n## the chromatika wrapper\n\n```ts\nfunction createSuiGraphQLClientFromRegistryNetworkId(): SuiGraphQLClient {\n  const tier = sessionState.activeSuiNetworkTier;        // 'vault' or 'dwallet'\n  const networkId = tier === 'dwallet'\n    ? sessionState.activeDwalletSuiNetworkId\n    : sessionState.activeSuiNetworkId;\n  const url = SUI_NETWORK_URLS[networkId];\n  return new SuiGraphQLClient({ url });\n}\n```\n\nevery read scopes to the right network tier (vault vs dWallet), so a vault-tier op reading \"what dWallets exist on Sui mainnet\" won't accidentally hit a testnet GraphQL endpoint.\n\n## what we use it for\n\n- ika operations: `IkaClient` is constructed with this client (`IkaClient` 0.3.x runs on `client.core.*` for `ClientWithCoreApi`). DKG, presign, sign, re-encrypt - all over GraphQL\n- balance reads: `client.core.getCoins`, `getBalance` etc.\n- object reads: `client.core.getObjects`, `multiGetObjects` (chunked 12 at a time per the patch; see below)\n- NFT discovery (Sui via Display): `client.core.queryObjects` with `showDisplay: true`\n- transaction submission: `client.core.executeTransactionBlock`\n- coin pagination: `listCoins` returns `{ objects, hasNextPage, cursor }` (use `for(;;)` with explicit `break`)\n\n## the chunking patch (`@mysten/sui@2.13.2.patch`)\n\nchromatika applies a pnpm patch to `@mysten/sui` 2.13.2 (`wallet-extension/patches/@mysten__sui@2.13.2.patch`). it changes:\n- `getObjects` and `multiGetObjects` chunk `objectIds` by **12** (instead of upstream's 50)\n- 100ms pause between chunks\n\nreason: the GraphQL POST body grows linearly with the id list. 50 ids per chunk regularly exceeds common ~5000-byte GraphQL body limits at edge proxies. chunking by 12 keeps under the limit; 100ms pause reduces burst rate-limit hits.\n\nbumping `@mysten/sui` requires refreshing the patch. see [mysten-sui-pinning-and-patches.md](/library/tech/mysten-sui-pinning-and-patches).\n\n## what we still use JSON-RPC for\n\n- **activity feed**: `SuiJsonRpcClient.queryTransactionBlocks` because GraphQL `client.core.*` exposes `getTransaction` (by digest) but no filtered / address-scoped list equivalent (yet). on a Mysten SDK bump that adds `client.core.listTransactions` or a `Query.address.transactions` wrapper, migrate. tracked future\n- (was: NFT and Sui Kiosk, now both on GraphQL)\n\nif you find any ika or Sui op that's hitting JSON-RPC where GraphQL is available, that's a bug - fix at the call site.\n\n## the per-vault sharing\n\nika's `IkaClient` constructor takes a `SuiGraphQLClient`. chromatika constructs **one** `SuiGraphQLClient` per session per active network tier and reuses it across:\n- ika operations\n- direct GraphQL reads (NFTs, balances, etc.)\n- Aftermath's swap submission\n\navoids duplicating connection state, ensures consistent network targeting.\n\n## error handling\n\nGraphQL errors come back in the response body's `errors` array (HTTP 200 even on errors). `SuiGraphQLClient` parses these and throws. wrap with retry logic for transient network errors but not for `errors[].extensions.code === 'invalid_query'` (that's a code bug).\n\n## library\n\n- `@mysten/sui/graphql` `SuiGraphQLClient`\n- `@mysten/sui/client` `SuiClient` (the JSON-RPC client; used for activity feed only)\n- internal: `wallet-extension/src/background/sui-client.ts` `createSuiGraphQLClientFromRegistryNetworkId`\n\n## related\n\n- [mysten-sui-pinning-and-patches.md](/library/tech/mysten-sui-pinning-and-patches) - the chunking patch + version pinning\n- [2pc-mpc-overview.md](/library/tech/2pc-mpc-overview) - the ika client that builds on this transport\n- [nft-api-providers.md](/library/tech/nft-api-providers) - NFT reads that use this client\n",tS=`# Sui \`signPersonalMessage\` ika SHA-512 vs Mysten BLAKE2b divergence

\`sui_signPersonalMessage\` is where chromatika's ika signing path diverges from Mysten's native \`signPersonalMessage\`. the result: dapps that verify with \`verifyPersonalMessageSignature\` from \`@mysten/sui\` may **reject** chromatika's signatures even though the underlying ed25519 key is the same. this is a known gap; the fix is to add a parallel BLAKE2b-intent path.

## what Mysten does

Mysten's \`signPersonalMessage\`:
\`\`\`
intent = [0x03, 0x00, 0x00]    // [PersonalMessage, V0, Sui]
intent_message = intent || bcs(PersonalMessage { message: bytes })
digest = blake2b_256(intent_message)
signature = ed25519_sign(digest, key)   // ed25519 sha-512s digest internally
\`\`\`

dapp verification:
\`\`\`
intent_message = intent || bcs(PersonalMessage { message: bytes })
expected_digest = blake2b_256(intent_message)
ed25519_verify(expected_digest, signature, pubkey)
\`\`\`

the digest is BLAKE2b-256 of the intent-wrapped, BCS-encoded message. the signature is over that digest.

## what chromatika ika does today

\`sui_signPersonalMessage\` in chromatika hands ika the **raw user message bytes**:
\`\`\`
signature = ed25519_sign(raw_message_bytes, key)   // ika sha-512s message internally
\`\`\`

ika produces an ed25519 signature where the message is the **raw bytes** the user supplied. ika's protocol path doesn't yet support a "wrap in intent + BLAKE2b first" client-side preprocessing step.

## the verify mismatch

when a dapp tries to verify chromatika's signature with Mysten's helper:
\`\`\`
expected_digest = blake2b_256(intent || bcs(PersonalMessage(raw)))
ed25519_verify(expected_digest, sig, pubkey)
// FAILS - sig was over sha512(raw), not sha512(blake2b(intent || bcs(raw)))
\`\`\`

verification fails. the dapp sees "invalid signature" even though the key matches. user is stuck.

## what works today

dapps that verify the **raw bytes** (skip the intent wrapping) accept chromatika's signature. this includes some custom dapp logic, dev tooling, raw \`tweetnacl.sign.detached.verify\` calls, and anything that doesn't go through Mysten's \`verifyPersonalMessageSignature\`.

it's a coin flip per dapp. modern Sui dapps using the canonical Mysten verify will reject; older / custom code may accept.

## why ika is structured this way

ika's MPC protocol is curve+algorithm-driven. \`(curve = ED25519, algorithm = EdDSA)\` produces a standard RFC 8032 ed25519 signature where the implementation hashes with SHA-512. there's no parameter for "but first BLAKE2b the input on the client side before handing to MPC."

the fix is for chromatika to do the wrapping client-side and hand ika the **digest** (32-byte BLAKE2b output) instead of the raw message:
\`\`\`
1. wrap: intent_message = [0x03, 0x00, 0x00] || bcs(PersonalMessage { message: raw })
2. digest = blake2b_256(intent_message)
3. ika.sign(message: digest, curve: ED25519, algorithm: EdDSA)
   - ika sha-512s the digest internally
   - signature is over sha512(digest)
4. verifier:
   - recomputes digest = blake2b_256(intent_message)
   - ed25519_verify(digest, signature, pubkey)
   - internal: ed25519_verify sha-512s the digest, matches what ika signed
\`\`\`

so the **fix is doable client-side**: build the BLAKE2b-of-intent digest in chromatika, hand ika the 32-byte digest as the "message". ika sha-512s the digest, verifiers compute the same BLAKE2b digest and ed25519_verify also sha-512s it. same byte input to sha-512 → same signature.

## why we haven't shipped the fix yet

it's straightforward to implement but requires:
1. add a \`wrapWithSuiPersonalMessageIntent\` helper that BCS-encodes + prefixes
2. compute the BLAKE2b digest
3. hand the digest to ika instead of raw bytes
4. test against Mysten's verifier to confirm interop

the gap is tracked as **future hardening** in [WALLET_SECURITY.md](/library/tech/wallet_security). when it lands, the existing path stays available for back-compat with dapps that verify raw bytes; new path is opt-in or auto-detected per dapp.

## the related Sui transaction-data signing case

interestingly, Sui transaction signing **already** uses the BLAKE2b-of-intent path (see [sui-tx-sign-via-ika.md](/library/tech/sui-tx-sign-via-ika)). there:
- chromatika BCS-serializes the TransactionData
- wraps with \`[0x00, 0x00, 0x00]\` intent
- BLAKE2b-256s the intent message
- hands ika the digest
- ika sha-512s the digest, signs

verifiers (Sui validators) recompute BLAKE2b of intent, ed25519_verify with sha-512 of digest. matches.

so the **mechanism is already proven** for transaction-data; we just need to extend it to PersonalMessage.

## what to tell users today

documented in [WALLET_SECURITY.md](/library/tech/wallet_security):

> Sui \`signPersonalMessage\` uses ika SHA-512 path, NOT Mysten's BLAKE2b PersonalMessage intent. some dapps may not verify until aligned. tracked future.

dapp-side workaround: use raw-message verification (e.g. \`tweetnacl.sign.detached.verify(raw_message_bytes, signature, pubkey)\`) instead of \`verifyPersonalMessageSignature\`.

chromatika-side fix: implement BLAKE2b-intent wrapping client-side, hand ika the digest. tracked future.

## library

- \`@mysten/sui\` \`verifyPersonalMessageSignature\` (the dapp side)
- \`@noble/hashes/blake2b\` \`blake2b256\` (when chromatika implements the fix)
- internal: \`wallet-extension/src/background/chains/signing/sui.ts\` for the current path

## related

- [sui-tx-sign-via-ika.md](/library/tech/sui-tx-sign-via-ika) - the working BLAKE2b-of-intent flow for tx data
- [sha512-and-blake2b.md](/library/tech/sha512-and-blake2b) - the underlying hash function comparison
- [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - the ed25519 path that does sha-512 internally
- [WALLET_SECURITY.md](/library/tech/wallet_security) - the user-facing disclaimer
`,rS=`# Sui transaction signing via ika MPC

Sui transactions are signed in chromatika via two paths: **HD fee-payer signing** (\`sendSuiNative\`, ika PTB ops where the fee payer is the local ed25519 keypair) and **dWallet signing** (when a Sui-base dWallet is the canonical signer for the user identity). this doc covers the dWallet path - using ika MPC ED25519 EDDSA to sign Sui PTBs.

## the call shape

\`\`\`ts
async function signSuiTransaction({ transaction, dwalletId? }) {
  // transaction is a built @mysten/sui Transaction object
  // returns: { signature, transactionBytes }
}
\`\`\`

usually called from inside \`IkaTransaction.sign()\` flows or dapp \`sui_signTransactionBlock\` handler.

## step-by-step

\`\`\`ts
async function signSuiTxViaIka(tx: Transaction) {
  // 1. build to BCS-serialized bytes
  const txBytes = await tx.build({ client: suiGraphQLClient });
  // txBytes is the serialized TransactionData (BCS layout)

  // 2. wrap with Mysten intent prefix per Sui's signing scheme
  const intent = new Uint8Array([0x00, 0x00, 0x00]);   // [TransactionData, V0, Sui]
  const intentMessage = new Uint8Array(intent.length + txBytes.length);
  intentMessage.set(intent, 0);
  intentMessage.set(txBytes, intent.length);

  // 3. compute the BLAKE2b-256 digest (Sui native verification expects this)
  const digest = blake2b256(intentMessage);   // 32 bytes

  // 4. take a presign from ED25519_EDDSA pool
  const presignId = takePresign('ED25519_EDDSA');

  // 5. sign via ika - PASS THE DIGEST (32 bytes)
  // critical: Sui's intent path produces a digest before signing
  // ika ED25519 EdDSA signs RAW BYTES (per RFC 8032 sha-512s internally)
  // so we hand ika the digest as the "message" - ika will sha-512 it again
  // verifiers compute blake2b(intent || tx) digest, then verify
  // ed25519_verify(digest, signature, pubkey) - which sha-512s the digest
  // this matches what ika produced
  const sigBytes = await ikaSign({
    dwalletId: activeEd25519DwalletId,
    curve: Curve.ED25519,
    algorithm: SignatureAlgorithm.EdDSA,
    message: digest,                                  // 32-byte BLAKE2b digest
    presignId,
  });

  // 6. parse - 64-byte ed25519 sig
  const { R, S } = parseSignatureFromSignOutput(sigBytes, Curve.ED25519, SignatureAlgorithm.EdDSA);
  const sig64 = new Uint8Array([...R, ...S]);

  // 7. assemble Sui signature format: [scheme_flag(1) || sig(64) || pubkey(32)]
  // scheme_flag = 0x00 for ed25519
  const dwalletPubkey = await getDwalletEd25519PublicKey();
  const suiSig = new Uint8Array(1 + 64 + 32);
  suiSig[0] = 0x00;
  suiSig.set(sig64, 1);
  suiSig.set(dwalletPubkey, 65);

  return {
    signature: toBase64(suiSig),
    transactionBytes: toBase64(txBytes),
  };
}
\`\`\`

## the Sui intent prefix

Sui signs over an **intent message**: a 3-byte prefix + the BCS-encoded TransactionData. the prefix domain-separates transaction-data signing from PersonalMessage signing from other Sui signature contexts.

\`\`\`
intent = [scope, version, app]
       = [0x00 (TransactionData), 0x00 (V0), 0x00 (Sui)]
\`\`\`

the full intent message is \`intent || bcs(tx_data)\` = 3 bytes + N bytes. then \`blake2b_256(intent_message)\` is the digest, then \`ed25519_sign(digest, key)\` is the signature.

## the BLAKE2b vs SHA-512 question

Sui's native verifier:
\`\`\`
1. recompute digest = blake2b_256(intent || bcs(tx))
2. verify ed25519_signature(digest, sig, pubkey)
   - this internally sha-512s the digest per RFC 8032
\`\`\`

if chromatika hands ika the **digest** (the 32-byte BLAKE2b output), ika treats those 32 bytes as the message and sha-512s them, signing \`sha512(digest)\`. verifiers then compute \`blake2b(intent || tx)\` and call ed25519_verify, which also sha-512s the digest. **same input to sha-512** → **same signature**.

so for **Sui transaction signing** (where verifiers expect the BLAKE2b-of-intent digest as the implicit "message"), this works correctly.

contrast with **Sui personal-message** (\`signPersonalMessage\`) where chromatika passes the raw user message bytes to ika, ika sha-512s them, but Mysten's verifier expects \`blake2b(intent || PersonalMessage(raw))\` - **different digest**. that divergence is documented in [sui-personal-message-divergence.md](/library/tech/sui-personal-message-divergence).

## the assembled Sui signature format

Sui signatures aren't just 64 bytes - they're \`[scheme_flag(1) || raw_sig(64) || pubkey(32)]\` = 97 bytes for ed25519, base64-encoded. the format is per Sui's spec:
- byte 0: scheme flag (0x00 for ed25519, 0x01 for secp256k1, 0x02 for secp256r1, 0x06 for passkey/SIP-9)
- bytes 1-64: raw signature (64 bytes for ed25519 or secp256k1, 65 bytes for secp256r1 with recovery)
- bytes 65-96: public key (32 bytes for ed25519)

verifiers parse this layout to know "ed25519 sig from this pubkey".

## the HD fee-payer alternative path

\`sendSuiNative\` (and several internal ika PTB ops) sign with the **local HD ed25519 keypair**, not via ika MPC:

\`\`\`ts
async function signSuiTxWithHdKeypair(tx: Transaction) {
  const txBytes = await tx.build({ client: suiGraphQLClient });
  const intentMessage = wrapWithIntent([0x00, 0x00, 0x00], txBytes);
  const digest = blake2b256(intentMessage);

  const hdKeypair = sessionState.feeMaterial.suiKeypair;   // Mysten Ed25519Keypair
  const sigBytes = await hdKeypair.sign(digest);   // 64 bytes

  const dwalletPubkey = hdKeypair.getPublicKey().toRawBytes();
  return assembleSuiSignature(0x00, sigBytes, dwalletPubkey);
}
\`\`\`

faster than the MPC path (no presign consumption, no ika round-trip) but trades off the security property of "key never assembled" for "key sits in extension memory while unlocked".

chromatika uses HD fee-payer signing for:
- \`sendSuiNative\` (HD fee-payer is the canonical sender, not a dWallet)
- ika DKG / presign / sign PTBs (the fee-payer pays gas; the dWallet identity isn't involved at this layer)
- any Sui PTB where the wallet is just paying gas and not asserting user identity

dapp \`sui_signTransactionBlock\` and dWallet-anchored sends use the ika MPC path so the **user identity** signs.

## simulation

Sui PTBs don't have an \`eth_call\`-style simulator with the same finality semantics, but \`tx.devInspect(...)\` runs a dry-run on a Sui node. chromatika uses this for fee estimation and "would this tx succeed" checks before submission.

## library

- \`@mysten/sui\` \`Transaction\`, \`Ed25519Keypair\`, \`SuiGraphQLClient\`, \`toBase64\`
- \`@noble/hashes/blake2b\` \`blake2b256\`
- internal: \`wallet-extension/src/background/chains/signing/sui.ts\`
- internal: \`wallet-extension/src/background/ika/signing.ts\`

## related

- [sui-personal-message-divergence.md](/library/tech/sui-personal-message-divergence) - the BLAKE2b vs SHA-512 gap for personal messages
- [ed25519-eddsa.md](/library/tech/ed25519-eddsa) - the underlying signature algorithm
- [signature-normalization.md](/library/tech/signature-normalization) - parseSignatureFromSignOutput details
`,aS=`# BTC taproot Schnorr (BIP340) in chromatika

BIP340 Schnorr signatures over secp256k1 are what bitcoin taproot (P2TR) outputs use. chromatika supports P2TR sends from the active SECP256K1 dWallet by routing through the **SECP256K1_TAPROOT** ika presign pool (separate from \`SECP256K1_ECDSA\`).

## why a separate pool

ECDSA and Schnorr produce different signature shapes from the same secp256k1 key:
- ECDSA: \`(r, s)\` where \`r\` is x-coordinate of \`k*G mod n\`, \`s = k^(-1)(z + r*d) mod n\`
- Schnorr (BIP340): \`(R, s)\` where \`R = k*G\` is a 32-byte x-only point, \`s = k + e*d mod n\`, \`e = tagged_hash("BIP0340/challenge", R || P || m)\`

ika MPC has different presign material per algorithm because the math during the presign phase differs - SECP256K1_TAPROOT presigns commit to the x-only \`R\` form Schnorr expects, while SECP256K1_ECDSA presigns work for the \`(r, s)\` form ECDSA expects.

## tagged hash

BIP340 introduced "tagged hashes" to give domain separation:
\`\`\`
tagged_hash(tag, x) = SHA-256(SHA-256(tag) || SHA-256(tag) || x)
\`\`\`

specific tags used in taproot:
- \`"BIP0340/challenge"\` for \`e\` in signing
- \`"BIP0340/aux"\` for the auxiliary RNG input
- \`"TapLeaf"\`, \`"TapBranch"\`, \`"TapTweak"\` for the script tree commitment / output-key tweak

ika handles the BIP340 challenge hash internally during taproot signing. chromatika hands it the BIP341 sighash (the bytes that the BIP340 challenge hashes over).

## BIP341 sighash

BIP341 defines the message format that taproot signs over:
\`\`\`
sighash = tagged_hash("TapSighash", 0x00 || sighash_data)
sighash_data = (
  hash_type ||
  nVersion ||
  nLockTime ||
  sha256(amounts) ||  // sum of input amounts
  sha256(scriptPubKeys) ||  // input scripts
  sha256(sequences) ||
  sha256(outputs) ||
  spend_type ||
  input_index ||
  ...
)
\`\`\`

bitcoinjs-lib computes this via \`tx.hashForWitnessV1(...)\` per input. chromatika builds the tx, computes one sighash per P2TR input, hands each to ika via SECP256K1_TAPROOT, and assembles the final witness.

## key tweaking (BIP341 output key)

a P2TR output key is **tweaked** from the internal key:
\`\`\`
P_internal = derive_secp256k1_pubkey(secret)
t = tagged_hash("TapTweak", P_internal_x_only || merkle_root_of_script_tree)
P_output = P_internal + t * G
\`\`\`

if there are no scripts (key-path-only), \`merkle_root_of_script_tree\` is empty and \`t\` is just \`tagged_hash("TapTweak", P_internal_x_only)\`.

chromatika derives the P2TR address by computing the tweaked output key and bech32m-encoding it (\`bc1p…\` mainnet, \`tb1p…\` testnet). signing uses the **tweaked secret** so the signature verifies against the tweaked output key.

## key path vs script path

chromatika today only does **key-path spending** - the simpler taproot mode where the user signs with their tweaked key directly, no merkle proof, no tapscript. script-path spends (locktime, multisig, miniscript) are out of scope. if you want to spend from a P2TR output that requires script-path, chromatika can't do it today.

## the chromatika BTC signing pipeline (P2TR)

\`\`\`
1. build the BTC transaction via bitcoinjs-lib with a P2TR input
2. for each P2TR input, compute BIP341 sighash via tx.hashForWitnessV1(input_index, ...)
3. hand the sighash to ika as a SECP256K1_TAPROOT sign request
4. ika returns a 64-byte BIP340 Schnorr signature (R(32) || s(32))
5. write the signature into the input's witness:
   witness = [signature_64_bytes]   // key-path-only, no script
6. broadcast via the configured Esplora endpoint
\`\`\`

if \`hash_type\` is non-default (e.g. SIGHASH_ALL with anyonecanpay), the sighash includes a 65-byte signature (\`r || s || hashType\`); for SIGHASH_DEFAULT (0x00) it's 64 bytes.

## the SECP256K1_TAPROOT presign pool

per CLAUDE.md, chromatika maintains three ika presign pools per active dWallet Vault:
- \`SECP256K1_ECDSA\` (EVM, generic ECDSA)
- \`SECP256K1_TAPROOT\` (BTC P2TR)
- \`ED25519_EDDSA\` (Sui, Solana, Aptos)

each pool stores presign material specific to the algorithm. the 5-min auto-refill alarm and manual \`replenishPresign\` ops apply per-pool.

## library

- \`bitcoinjs-lib\` for tx building, BIP341 sighash, P2TR address derivation, witness assembly
- \`@noble/secp256k1\` (or \`@noble/curves\`) for curve math (x-only point operations)
- ika MPC for the actual Schnorr signature production via the SECP256K1_TAPROOT presign pool

## P2TR address format

\`\`\`
output_pubkey_x_only = (P_internal + t*G).x   // 32 bytes
witness_program = 0x51 0x20 || output_pubkey_x_only   // OP_1 PUSH32 <pubkey>
address = bech32m_encode(hrp="bc" or "tb", witness_version=1, output_pubkey_x_only)
\`\`\`

bech32m is the post-BIP350 encoding (replaces bech32 for witness version >= 1 to fix a checksum vulnerability that affected the original bech32 with non-zero witness versions).

## what chromatika doesn't do for taproot

- script-path spends (no tapscript)
- multisig P2TR (no MuSig2 / FROST aggregation)
- inscriptions / ordinals (display only, no minting / transferring inscription content)
- silent payments (BIP352)
`,sS=`# Trezor Connect (\`@trezor/connect-web\`)

chromatika supports Trezor devices via Trezor's hosted iframe at \`https://connect.trezor.io\`. the iframe handles WebUSB / WebHID transport + UI prompts; chromatika passes JSON-RPC-style requests through \`@trezor/connect-web\`. supports EVM (message + tx + typed data) and Solana (tx). does **not** support Sui or Bitcoin PSBT today.

## why an iframe (not direct WebHID)

Trezor's design is to host a single TypeScript SDK at \`connect.trezor.io\` that all dapps embed. benefits:
- single source of truth for device firmware compatibility
- centralized UI for confirmations + passphrase prompts
- no need for each dapp to handle USB device dialogs

trade-off: the iframe runs cross-origin, so chromatika has to communicate via \`postMessage\` and trust Trezor's hosted code.

## the CSP allowance

manifest CSP includes:
\`\`\`jsonc
"frame-src": "https://connect.trezor.io"
\`\`\`

without this, the iframe fails to load. chromatika's manifest already has it.

## the connection

\`\`\`ts
import TrezorConnect from '@trezor/connect-web';

// initialize once
TrezorConnect.init({
  manifest: {
    appUrl: 'chrome-extension://<chromatika-id>',
    email: 'support@chromatika.example',
  },
  lazyLoad: true,        // don't load the iframe until first use
  popup: false,          // use the embedded iframe, not a separate popup
});
\`\`\`

\`init\` lazily loads \`https://connect.trezor.io/9/popup.html\` into an iframe in the chromatika popup. subsequent calls go through the iframe via \`postMessage\`.

## the calls

\`\`\`ts
// EVM message sign
const result = await TrezorConnect.ethereumSignMessage({
  path: "m/44'/60'/0'/0/0",
  message: 'Hello, world',
  hex: false,
});
// result.payload = { signature: '0x...' }

// EVM typed data v4
const result = await TrezorConnect.ethereumSignTypedData({
  path: "m/44'/60'/0'/0/0",
  data: { domain, types, primaryType, message },
  metamask_v4_compat: true,
});

// EVM transaction
const result = await TrezorConnect.ethereumSignTransaction({
  path: "m/44'/60'/0'/0/0",
  transaction: {
    to: '0x...',
    value: '0x...',
    data: '0x',
    chainId: 1,
    nonce: '0x...',
    gasLimit: '0x...',
    maxFeePerGas: '0x...',
    maxPriorityFeePerGas: '0x...',
  },
});

// Solana transaction
const result = await TrezorConnect.solanaSignTransaction({
  path: "44'/501'/0'/0'",
  serializedTx: txBytes,
});
\`\`\`

each call:
1. chromatika invokes the SDK method
2. SDK posts to its iframe
3. iframe handles USB transport, displays confirm UI
4. user confirms on device
5. iframe returns result
6. chromatika receives \`{ success: bool, payload: ... }\`

## what doesn't work

### Bitcoin PSBT

Trezor wants **decomposed UTXO inputs** (raw \`TxInputType[]\` / \`TxOutputType[]\` with reference txs), not a raw PSBT blob. chromatika's BTC code uses PSBT. converting PSBT → Trezor's expected shape requires:
- decoding the PSBT
- fetching reference txs (\`refTxs\`) for each input
- converting to \`TxInputType[]\` / \`TxOutputType[]\`
- calling \`TrezorConnect.signTransaction\` with those

this isn't implemented today. attempting Trezor BTC sign throws with an actionable error pointing at Ledger.

### Sui

Trezor Connect doesn't expose Sui signing at the protocol level. no Sui app on Trezor firmware. chromatika doesn't pretend - Trezor Sui isn't an option.

## the EVM personal_sign convention

note: chromatika's main EVM signing path uses **ika MPC** (the dWallet path). Trezor is a hardware-account path - the user has imported a Trezor account via \`addHardwareAccount({ vendor: 'trezor', ... })\`. signing routes through Trezor's iframe, not ika.

so when a dapp calls \`personal_sign\` on an account that's a Trezor hardware account, chromatika's hardware-sign popup runs \`TrezorConnect.ethereumSignMessage\`. user confirms on Trezor, signature returns.

## the EVM derivation path quirk

chromatika uses BIP44 path \`m/44'/60'/0'/0/0\` for EVM. some legacy MetaMask extensions used \`m/44'/60'/0'/0\` (one segment less). Trezor accepts both but the addresses differ. chromatika sticks with the standard 5-segment form.

## library

- \`@trezor/connect-web\` ^9.7.3 (latest 9.x)
- internal: \`wallet-extension/src/ui/hardware/trezor-connect-web.ts\` for SDK init wrapper
- internal: \`wallet-extension/src/ui/hardware/TrezorSigner.tsx\` for the popup-side flow

## related

- [webhid-popup-context.md](/library/tech/webhid-popup-context) - the WebHID notes (Trezor uses WebUSB inside iframe rather than WebHID, but same popup-context constraint)
- [trezor.md](/library/user/trezor) (user-guides) - the user-facing flow
- [ledger-hw-app-libs.md](/library/tech/ledger-hw-app-libs) - the alternative hardware path
`,iS=`# vault blob v3 format (\`chromatika_vault_v3\`)

the encrypted blob that holds **every chromatika vault** on this install. lives in \`chrome.storage.local\` under the key \`chromatika_vault_v3\`. one blob holds many vault records (mnemonic vaults, imported-key vaults, passkey vaults, hardware vaults), each with their own credential material - all sealed under one AES-256-GCM encryption.

## storage location

- \`chrome.storage.local\` (persists across browser restarts; cleared on uninstall)
- key: \`chromatika_vault_v3\`
- the \`v3\` suffix denotes schema version. v2 (PBKDF2) is rejected on parse - chromatika is pre-release, no migrations from v2

## outer shape

\`\`\`jsonc
// stored at chrome.storage.local["chromatika_vault_v3"]
{
  "kdfMeta": {
    "kind": "argon2id",
    "tCost": 3,
    "mCostKiB": 65536,   // 64 MiB
    "parallelism": 4,
    "saltB64": "<16 random bytes b64>",
    "outputLength": 32   // 32-byte AES-256 key
  },
  "ivB64": "<12 random bytes b64>",   // AES-GCM IV
  "ciphertextB64": "<sealed bytes>"
}
\`\`\`

the \`ciphertextB64\` is \`AES-256-GCM(plaintext, key, iv)\` where \`key = argon2id(password, kdfMeta.saltB64, ...)\`. the AES-GCM tag (16 bytes) is appended to the ciphertext per webcrypto's default behavior.

## inner plaintext (after decrypt)

\`\`\`jsonc
{
  "v": 3,
  "vaults": [
    { /* VaultRecord */ },
    { /* VaultRecord */ }
  ],
  "activeVaultId": "<vault-id-string>"
}
\`\`\`

## VaultRecord shape

each vault record is a discriminated bag of credential material. fields present depend on \`seedSource\` and credential type:

\`\`\`jsonc
{
  "id": "<uuid>",
  "label": "main",
  "baseChain": "sui" | "solana",
  "createdAtMs": 1700000000000,

  // ika user-share encryption keys (per curve)
  "ikaShareKeysB64": {
    "SECP256K1": "<b64>",   // serialized UserShareEncryptionKeys for secp256k1
    "ED25519":   "<b64>"
  },

  // dWallet metadata cache (cap ids, addresses, attestation bytes for solana)
  "dwalletMeta": [
    {
      "dwalletId": "0x...",
      "curve": "SECP256K1",
      "baseChain": "sui",
      // solana base only:
      "dwalletAttestationBytesB64": "...",
      "dwalletPublicKeyB64": "..."
    }
  ],

  // credential-specific fields - exactly one branch is populated:
  "seedSource": "mnemonic" | "private-key" | "passkey-prf" | "waap-signature" |
                "recovery-words" | "mwa-signature" | "walletconnect-signature" |
                "lazor-recovery-words",

  // mnemonic vaults
  "mnemonic": "12 or 24 space-separated words",

  // imported private-key vaults
  "suiPrivateKeyBech32": "suiprivkey1...",   // sui base
  "solanaSecretKeyB64": "<64-byte b64>",     // solana base

  // passkey vaults
  "passkeyCredentialId": "<b64url credentialId>",
  "passkeyPublicKeyB64": "<33-byte compressed secp256r1 pubkey b64>",
  "passkeyRpId": "<extension origin>",
  "passkeyPrfSaltB64": "<32-byte salt b64>",

  // WAAP vaults (deterministic-signature path)
  "waapSuiAddress": "0x...",
  "waapSuiPublicKeyB64": "<b64>",
  "waapAuthMethod": "email" | "phone" | "social",
  "waapSocialProvider": "google" | "discord" | "twitter" | "github" | "bluesky" | null,
  "waapPairingSignatureB64": "<encrypted-in-envelope signature>",

  // Lazor vaults (Solana + recovery-words required)
  "lazorSmartWalletPubkeyB58": "<base58 PDA>",
  "lazorCredentialIdB64": "<b64>",
  "lazorPasskeyPubkeyB64": "<b64>",
  "lazorProgramId": "<base58 program id>",
  "lazorNetwork": "mainnet" | "devnet",
  "lazorPortalUrl": "https://portal.lazor.sh",
  "lazorWalletDevicePubkeyB58": "<base58>" | null,
  "lazorIkaFeePayerSolSecretKeyB64": "<64-byte solana secret key b64>",
  "recoveryWordsEncryptedB64": "<24 BIP39 words>",   // stored as plaintext inside the AES-GCM blob

  // hardware vaults (MWA / Seeker / WalletConnect / Ledger)
  "hardwareAccountId": "<id from getHardwareAccounts>",
  "hardwareVendor": "mwa" | "walletconnect" | "ledger" | "trezor",
  "hardwareChain": "solana" | "sui",
  "mwaTransport": "local" | "remote" | null,
  "ikaUskSignatureB64": "<encrypted-in-envelope signature>",
  "ikaGrpcFeePayerSolSecretKeyB64": "<64-byte b64, in_extension fee mode only>",
  "ledgerFeePayerSolPubkeyB58": "<base58, ledger-first solana>",
  "ledgerFeePayerEd25519PublicKeyB64": "<b64, ledger-first sui>",

  // multi-envelope unlock metadata
  "envelopes": [
    { /* PasswordEnvelope or PasskeyPRFEnvelope or WalletSignatureEnvelope or RecoveryWordsEnvelope */ }
  ],

  // ika fee-payer settings (solana base)
  "ikaFeeSettings": {
    "mode": "in_extension" | "seeker_direct",
    "autoRefill": true,
    "refillLamports": 50000000,
    "thresholdLamports": 5000000
  }
}
\`\`\`

not every field is present on every record. \`seedSource\` selects which branch is canonical. e.g. a Sui-mnemonic vault has \`mnemonic\` set, no \`solanaSecretKeyB64\`, no passkey fields, no hardware fields.

## the per-vault dWallet meta overlay

separately from the vault blob, each vault writes a \`chromatika_dwallet_meta_v2_<vaultId>\` overlay to \`chrome.storage.local\`. this overlay is a fresher snapshot of the vault's \`dwalletMeta\` field for some flows that read meta without unlocking the full blob (e.g. dapp-bridge reading the active dWallet address before unlock, status surfaces).

the in-blob \`dwalletMeta\` is the **authoritative copy**; the overlay is a cache. on unlock the session merges, preferring overlay timestamps where they're newer.

\`loadDwalletMeta(vaultId)\` and \`saveDwalletMeta(vaultId, meta)\` are the access helpers. \`syncVaultMeta\` flushes session state into the overlay on demand.

## per-vault presign pools

\`chromatika_presign_pools_v3_<vaultId>\` stores ika presign material per dWallet Vault. three pools per vault: \`SECP256K1_ECDSA\`, \`SECP256K1_TAPROOT\`, \`ED25519_EDDSA\`. switching active vault loads a different pool set.

## key naming convention

\`chromatika_<domain>_v<N>\` (or \`chromatika_<domain>_v<N>_<suffix>\` for per-instance scoping). the integer suffix bumps on schema change. complete list per CLAUDE.md:
- \`chromatika_vault_v3\` - the encrypted blob (this doc)
- \`chromatika_dwallet_meta_v2_<vaultId>\` - per-vault dWallet overlay
- \`chromatika_presign_pools_v3_<vaultId>\` - per-vault presign pools
- \`chromatika_active_networks_v1\` - active network selection per chain
- \`chromatika_custom_networks_v1\` - user-added custom networks
- \`chromatika_media_safety_v1\` - MediaSafetyMode value
- \`chromatika_dapp_permissions_v1\` - dapp connection permissions
- \`chromatika_hw_accounts_v1\` - hardware account list
- \`chromatika_advanced_mode_v1\` - advanced mode toggle
- \`chromatika_ika_base_mode_v1\` - global ika base preference
- \`chromatika_mcp_v1\` - MCP agent surface state (token, listen port, etc.)
- \`chromatika_x402_caps_v1\` - x402 spending caps
- \`chromatika_x402_receipts_v1\` - x402 payment receipts (capped 200)
- \`chromatika_unlock_cache_v1\` - **session storage only** (cold-SW rehydrate cache)

## what to do when the schema breaks

pre-release means **don't migrate**. when chromatika changes the v3 record shape (e.g. adding new credential fields, changing dWallet meta layout):
1. bump the version suffix → \`chromatika_vault_v4\`
2. on parse, if version != current, reject with a clear "clear extension storage and re-onboard" message
3. document the breaking change in commit message + STATUS.md
4. ship

users / devs clear \`chrome.storage.local\` (or uninstall + reinstall the extension) and re-onboard. this is fine because there are no end users to migrate.
`,oS=`# WAAP determinism probe

\`@human.tech/waap-sdk\` (Web3 Auth as a Platform) provides email / phone / social login that resolves to a Sui address + public key + wallet-standard signing capability. some WAAP wallet implementations are **deterministic** (RFC 8032 ed25519 - same key + same message = same signature) and some are **non-deterministic** (introduce randomness during signing). chromatika **probes** at pairing time to dispatch the right ika seed source.

## why determinism matters

chromatika's ika seed for WAAP-Sui vaults uses \`ikaRootSeedFromMwaSignature\` = \`keccak256(signature_over_IKA_USK_DERIVATION_MESSAGE || index_le)\`. for restore-on-new-device to work:
- WAAP login on the new device must produce the **same Sui keypair** (the user has the same email / phone)
- the wallet must produce the **same signature** for the same message (RFC 8032 deterministic ed25519)
- \`keccak256(signature || index)\` must produce the same 32-byte seed

if the WAAP wallet is non-deterministic, two signs of the same message produce different signatures, and \`keccak256\` produces different seeds → different ika identities. the user's dWallet on a new device wouldn't match what they had before.

so: **determinism is mandatory for the WAAP-signature ika seed path**. non-deterministic wallets fall back to a recovery-words branch.

## the probe

at pairing time, immediately after WAAP login returns:

\`\`\`ts
async function probeWaapDeterminism(wallet, message) {
  const sig1 = await wallet.signPersonalMessage(message);
  // first sign
  const sig2 = await wallet.signPersonalMessage(message);
  // second sign

  const sig1Bytes = decodeWalletStandardSignature(sig1);
  const sig2Bytes = decodeWalletStandardSignature(sig2);

  return arrayEquals(sig1Bytes, sig2Bytes);
  // deterministic if the bytes match
}

const message = new TextEncoder().encode(IKA_USK_DERIVATION_MESSAGE);
// IKA_USK_DERIVATION_MESSAGE = "ika.chromatika.user-share-encryption-key.v1"
const isDeterministic = await probeWaapDeterminism(waapWallet, message);
\`\`\`

two assertions of the same \`IKA_USK_DERIVATION_MESSAGE\`. compare the resulting signature bytes.

## the dispatch

\`\`\`ts
if (isDeterministic) {
  seedSource = 'waap-signature';
  // capture the signature (one of the two probe sigs - they're identical)
  pairingSignatureB64 = base64Encode(sig1Bytes);
  // ika seed = keccak256(signature || u32_le(0))
  // unlock envelope wraps masterKey under a key derived from same signature
} else {
  seedSource = 'recovery-words';
  // require user to provide BIP39 phrase
  // ika seed = keccak256(bip39_seed || u32_le(0))
  // unlock envelope wraps masterKey under recovery-words-derived key
}
\`\`\`

chromatika persists \`seedSource\` on the vault record so subsequent operations know which path the vault uses.

## what makes a WAAP wallet non-deterministic

ed25519 RFC 8032 specifies deterministic signing - same key + same message = same signature, every time. a conforming implementation can't be non-deterministic. but some implementations:
- introduce randomness during the nonce derivation (deviation from RFC 8032)
- use a TEE / HSM that injects per-session entropy
- proxy through a cloud signer that adds metadata (e.g. timestamp) to the signed payload before producing a sig

chromatika treats any of these as non-deterministic. the probe catches all such cases.

## the determinism property is per (wallet implementation, key)

different WAAP wallets behave differently. chromatika probes per-pairing rather than per-WAAP-provider, since:
- WAAP itself doesn't standardize how its sub-wallets sign
- a single user might have multiple WAAP wallets across providers
- a wallet provider could change behavior over a version update; we don't want to hardcode "vendor X is deterministic"

probing at pairing time is robust against implementation changes.

## the persisted state

\`\`\`jsonc
record.waapSuiAddress = '0x...';
record.waapSuiPublicKeyB64 = '<b64>';
record.waapAuthMethod = 'email' | 'phone' | 'social';
record.waapSocialProvider = 'google' | 'discord' | ... | null;
record.seedSource = 'waap-signature' | 'recovery-words';

// only present if seedSource === 'waap-signature':
record.waapPairingSignatureB64 = '<encrypted in WalletSignatureEnvelope>';

// only present if seedSource === 'recovery-words':
record.recoveryWordsEncryptedB64 = '<the 24 BIP39 words, plaintext inside the AES-GCM vault payload>';
\`\`\`

## the unlock signature vs the seed signature

note: the **unlock signature** and the **ika seed signature** can be the same in the WAAP-signature path. both come from signing \`IKA_USK_DERIVATION_MESSAGE\`. chromatika persists the signature once (in the wallet-signature envelope) and reuses for both purposes.

if WAAP login produces a stable Sui keypair AND the signing is deterministic, the user can:
- log in via WAAP on a new device
- chromatika asks WAAP to sign the derivation message
- gets the same 64-byte signature (RFC 8032 + same key)
- ika seed = keccak256 of the signature → same dWallet
- unlock envelope unwraps with the same signature-derived key
- session unlocks; dWallets reattach via discovery

deterministic WAAP = full restore without seed phrase.

non-deterministic WAAP = seed phrase required at create time + at restore.

## the recovery-words fallback

if the probe says non-deterministic, chromatika requires the user to provide 24 BIP39 words at create time. those words:
- back the \`RecoveryWordsEnvelope\` for unlock
- back the \`ikaRootSeedFromRecoveryWords\` for ika seed derivation
- get **stored encrypted inside the vault payload** so chromatika can re-derive the seed on every unlock without re-prompting

WAAP login still happens (to get the user's identifying address + an additional unlock branch via \`WalletSignatureEnvelope\`), but the **canonical seed source** is the BIP39 phrase, not the WAAP signature.

## the probe failure mode

if the WAAP wallet errors during the probe (network blip, user cancels mid-sign), chromatika:
1. surfaces the error
2. does not create a vault
3. lets the user retry

never proceeds with an inconclusive determinism state - that would risk creating a vault we can't unlock.

## library

- \`@human.tech/waap-sdk\` for the WAAP login + Sui-address resolution
- internal: \`wallet-extension/src/background/waap/probe.ts\` for the determinism probe
- internal: \`wallet-extension/src/background/keyring/hd.ts\` for the seed factories
- internal: \`waapSeedFactoryFromInput\` dispatcher in keyring/hd.ts (per agent exploration)

## related

- [ika-seed-sui-waap.md](/library/tech/ika-seed-sui-waap) - the ika seed derivation paths
- [wallet-signature-envelope.md](/library/tech/wallet-signature-envelope) - the unlock envelope
- [recovery-words-envelope.md](/library/tech/recovery-words-envelope) - the recovery branch
- [multi-envelope-design.md](/library/tech/multi-envelope-design) - the overall envelope model
`,lS=`# wallet-signature envelope

a chromatika vault can be unlocked by having an external wallet (WAAP / Seeker / Phantom-Solflare-Jupiter via WC) sign a fixed challenge string. the signature is then HKDF'd into an envelope key that unwraps the master key. relies on RFC 8032 ed25519 deterministic signing - same key + same message = same signature, every assertion.

## envelope record

\`\`\`jsonc
{
  "kind": "wallet-signature",
  "source": "waap" | "seeker" | "walletconnect",
  "address": "<chain address that signs>",
  "label": "Seeker phone",
  "hint": "email" | "phone" | "google" | "discord" | ... | null,
  "challenge": "<the deterministic message bytes the wallet signs>",
  "kdfSaltB64": "<32 random bytes for HKDF salt>",
  "wrappedMasterKeyB64": "<AES-GCM(envKey, masterKey, iv)>",
  "envIvB64": "<12 random bytes>"
}
\`\`\`

## the challenge

a fixed UTF-8 string the wallet always signs to unlock. typical content is a domain-separated phrase like:

\`\`\`
"chromatika.unlock.v1\\n" + envelope.id + "\\n" + vault.id
\`\`\`

the exact format is whatever \`buildWalletSignatureEnvelope\` constructs - includes envelope id and vault id so a captured signature for one envelope can't be replayed against another.

## creation (pairing)

\`\`\`
1. user pairs the wallet (MWA local / MWA remote / WalletConnect / WAAP login)
2. wallet returns: address, public-key, transport metadata
3. determinism probe (only for WAAP-style wallets that may be non-deterministic):
   - sign challenge twice
   - if signatures differ → wallet is non-deterministic → fall back to recovery-words envelope, do NOT create a wallet-signature envelope
4. challenge = build_challenge(envelope_id, vault_id)
5. signature = wallet.signMessage(challenge) // 64-byte ed25519 sig
6. envKey = HKDF-SHA256(signature, info='chromatika wallet-signature envelope v1', salt=kdfSalt)
7. wrappedMK = AES-GCM(envKey, masterKey, randomIv)
\`\`\`

the **signature itself** is not stored. only the wrapped masterKey. on every unlock, the wallet must re-sign the challenge to produce the same signature → same envKey → unwrap.

## unlock

\`\`\`
1. read envelope.challenge + envelope.kdfSalt
2. wallet.signMessage(challenge)   // re-sign deterministically
3. envKey = HKDF-SHA256(signature, info='chromatika wallet-signature envelope v1', salt=kdfSalt)
4. masterKey = AES-GCM.decrypt(envKey, wrappedMasterKey, envIv)
5. session unlocks
\`\`\`

## per-source notes

### Seeker (MWA remote)

- transport: \`wss://reflect.solanamobile.com\` reflector + persisted \`auth_token\`
- subsequent unlocks reauthorize against the cached \`auth_token\` to skip the QR rescan
- \`ERROR_AUTHORIZATION_FAILED\` from the wallet → \`MwaSigner\` flips into \`needsRepair\` state → re-pair via fresh QR
- the same Seeker on a different device produces the same signature (RFC 8032 determinism + Seed Vault holds the same secret) → unlock works on a fresh chromatika install with the same Seeker

### MWA local (Android same-device)

- transport: Android intent (\`solana-wallet://\`)
- only works when chromatika and the wallet app are on the same Android device
- otherwise identical to remote in terms of envelope structure

### WalletConnect

- transport: WalletConnect v2 relay
- session token + \`topic\` persisted on the envelope record
- session lives until revoked phone-side. on revoke, unlock fails with an actionable "re-pair WalletConnect" error

### WAAP

- transport: @human.tech web flow (email / phone / social login)
- WAAP may be deterministic (signature stable across sessions) or non-deterministic (e.g. randomized nonce in some flows)
- chromatika **probes** at pairing - sign twice, compare. if non-deterministic, no wallet-signature envelope is created; recovery-words is required instead

## determinism is mandatory

if any of these wallets ever produced different signatures for the same challenge between assertions, the envKey would change and unlock would fail. RFC 8032 mandates deterministic ed25519 signing - the secret expands to a deterministic per-message scalar, so given the same key + same message there is one and only one valid signature.

bitcoin / ECDSA wallets that use RFC 6979 are **also** deterministic - chromatika could in theory support a secp256k1 wallet-signature envelope, though today the surface is ed25519-only.

## the "ika USK derivation" overlap

note: the **vault unlock** challenge and the **ika user-share encryption key** derivation message (\`'ika.chromatika.user-share-encryption-key.v1'\`) are **different messages**. unlock signs the unlock challenge to get the envelope key. ika seed derivation signs the IKA_USK_DERIVATION_MESSAGE constant separately, and \`keccak256(signature)\` of that becomes the seed. two different signatures from the same wallet, two different cryptographic purposes.

## attack model

- **offline brute force**: useless. signature is high-entropy ed25519 output (~256 bits). HKDF can't be reversed without the signature.
- **wallet compromise**: if attacker controls the wallet device (Seeker stolen, Phantom seed leaked), they can produce the signature → unlock the chromatika vault. **this is by design** - if your hardware wallet is compromised, all derived flows are too. the recovery-words envelope provides an out-of-band fallback that doesn't depend on the wallet device.
- **transport replay**: signed message includes envelope id + vault id, so a captured signature for one vault doesn't unlock another. WC / MWA also include their own session-level auth.

## library

- transport-specific:
  - MWA: \`@solana-mobile/mobile-wallet-adapter-protocol-web3js\`
  - WalletConnect: \`@walletconnect/sign-client\`
  - WAAP: \`@human.tech/waap-sdk\`
- internal helpers: \`buildWalletSignatureEnvelope(mk, sigBytes, { source, address, label, hint })\`, \`unlockWalletSignatureEnvelope(envelope, signature)\`
- HKDF: \`crypto.subtle.deriveKey({ name: 'HKDF', hash: 'SHA-256', salt, info })\`
`,cS=`# Wallet Standard (Sui + Solana)

Wallet Standard is the cross-chain spec for "how dapps discover and talk to wallets". originally Solana, now multi-chain with chain-specific feature extensions. chromatika registers a Wallet Standard entry per chain (Sui via \`@mysten/wallet-standard\`, Solana via \`@solana/wallet-standard-features\`) so dapps using \`@wallet-standard/core\` can discover chromatika and call its features.

## the registration

Wallet Standard discovery happens via global window events:

\`\`\`ts
const wallet: Wallet = {
  version: '1.0.0',
  name: 'Chromatika',
  icon: 'data:image/svg+xml;base64,...',
  chains: ['sui:mainnet', 'sui:testnet', 'sui:devnet',
           'solana:mainnet', 'solana:devnet'],
  accounts: [/* current accounts */],
  features: {
    'standard:connect': { version: '1.0.0', connect: ... },
    'standard:events': { version: '1.0.0', on: ... },
    'standard:disconnect': { version: '1.0.0', disconnect: ... },
    'sui:signTransaction': { version: '2.0.0', signTransaction: ... },
    'sui:signAndExecuteTransaction': { version: '2.0.0', signAndExecuteTransaction: ... },
    'sui:signPersonalMessage': { version: '1.0.0', signPersonalMessage: ... },
    'solana:signTransaction': { version: '1.0.0', signTransaction: ... },
    'solana:signAndSendTransaction': { version: '1.0.0', signAndSendTransaction: ... },
    'solana:signMessage': { version: '1.0.0', signMessage: ... },
  },
};

// announce
window.dispatchEvent(new CustomEvent('wallet-standard:register-wallet', {
  detail: { register: (cb) => cb(wallet) },
}));
\`\`\`

dapps using \`getWallets()\` from \`@wallet-standard/core\` see chromatika in their list immediately.

## sui features chromatika implements

| feature | spec | what chromatika does |
|---------|------|---------------------|
| \`sui:signTransaction\` | \`@mysten/wallet-standard\` | builds intent, computes BLAKE2b digest, ika ED25519 sign, returns \`{ signature, transactionBytes }\` |
| \`sui:signAndExecuteTransaction\` | \`@mysten/wallet-standard\` | sign + submit via active Sui RPC, returns digest + effects |
| \`sui:signPersonalMessage\` | \`@mysten/wallet-standard\` | ika ED25519 SHA-512 path (BLAKE2b divergence per [sui-personal-message-divergence.md](/library/tech/sui-personal-message-divergence)) |
| \`standard:connect\` | wallet-standard core | opens dapp connect approval popup |
| \`standard:disconnect\` | wallet-standard core | revokes the origin's permission |
| \`standard:events\` | wallet-standard core | emits \`change\` events for accounts / chains |

note: Sui signature format is \`[scheme_flag(1) || sig(64) || pubkey(32)]\` = 97 bytes base64. see [sui-tx-sign-via-ika.md](/library/tech/sui-tx-sign-via-ika).

## solana features chromatika implements

| feature | spec | what chromatika does |
|---------|------|---------------------|
| \`solana:signTransaction\` | \`@solana/wallet-standard-features\` | sign serialized v0 message via ika ED25519, return signed tx |
| \`solana:signAndSendTransaction\` | \`@solana/wallet-standard-features\` | sign + broadcast via Solana RPC, return tx signature (base58) |
| \`solana:signMessage\` | \`@solana/wallet-standard-features\` | ika ED25519 sign of raw bytes, return 64-byte sig |
| \`standard:connect\` | wallet-standard core | opens dapp connect approval popup |
| \`standard:disconnect\` | wallet-standard core | revokes the origin's permission |

note: Solana signatures are bare 64-byte \`R || S\` ed25519, base58 or hex encoded depending on caller.

## bulk signing primitives

\`solana:signAllTransactions\` (older feature) lets a dapp sign multiple txs in one popup. chromatika supports this with the \`dapp-consent-mode\` setting (compat = one popup for all; strict = popup per tx). see [manage-dapp-permissions.md](/library/user/manage-dapp-permissions).

\`sui:signAllTransactionBlocks\` analog: not currently exposed by \`@mysten/wallet-standard\` 0.20.x; dapps batch via separate sign calls.

## per-origin account selection

connecting via \`standard:connect\` lets the user pick a dWallet for each curve they want to expose:

\`\`\`
[connect approval popup]
  pick SECP256K1 dWallet (for EVM)?  or skip
  pick ED25519 dWallet (for Sui / Solana / Aptos)?  or skip
  approve | reject
\`\`\`

dapp on Sui only needs ED25519 → user picks just that. dapp on EVM only needs SECP256K1 → user picks just that. multi-chain dapps need both.

internal: \`chromatika_dapp_permissions_v1\` stores \`{ origin, selectedSecpDwalletId, selectedEd25519DwalletId }\` per origin.

## the strict consent mode

\`dapp-consent-mode: 'strict'\` makes bulk-sign primitives prompt per tx. compat (default) bundles them.

\`getDappConsentMode()\` / \`setDappConsentMode(mode)\` is the toggle.

## the address change event

when the user switches the active dWallet for a curve via \`setActiveDwallet\`, chromatika re-emits the \`change\` event to all connected origins:

\`\`\`ts
features['standard:events'].emit('change', { accounts: newAccounts });
\`\`\`

dapps that subscribe via \`wallet.features['standard:events'].on('change', cb)\` get notified.

## library

- \`@wallet-standard/core\` for the discovery + Wallet shape
- \`@wallet-standard/wallet\`, \`@wallet-standard/base\`, \`@wallet-standard/features\` for shared types
- \`@mysten/wallet-standard\` for Sui-specific features
- \`@solana/wallet-standard-features\` for Solana-specific features
- internal: \`wallet-extension/src/dapp-interface/wallet-standard-register.ts\` for Sui + Solana registration
- internal: \`wallet-extension/src/dapp-interface/aptos-wallet-standard-register.ts\` for Aptos (separate registration)

## related

- [eip-1193-and-6963.md](/library/tech/eip-1193-and-6963) - the EVM equivalent
- [dapp-bridge-message-validation.md](/library/tech/dapp-bridge-message-validation) - the page ↔ background boundary
- [sui-tx-sign-via-ika.md](/library/tech/sui-tx-sign-via-ika), [solana-tx-sign.md](/library/tech/solana-tx-sign) - what the features call into
- [sui-personal-message-divergence.md](/library/tech/sui-personal-message-divergence) - the BLAKE2b parity gap on Sui personal-message
`,uS=`# WalletConnect v2 (\`@walletconnect/sign-client\`)

WalletConnect v2 is a relay-based protocol that lets a desktop app pair with a phone wallet. chromatika uses WalletConnect specifically for the **x402 Solana payment** path (Seeker / Phantom / Solflare phone signs) and as a generic hardware-sign vendor (\`vendor: 'walletconnect'\`). like MWA, it relies on a relay (WalletConnect Cloud or self-hosted) - chromatika uses WalletConnect Cloud's default relay.

## the protocol layers

\`\`\`
[ application layer: JSON-RPC method calls (eth_sign, solana_signTransaction, etc.) ]
[ session layer: encrypted via X25519 + ChaCha20-Poly1305 ]
[ relay transport: WebSocket to wss://relay.walletconnect.com ]
\`\`\`

similar to MWA's reflector but with different wire format + crypto primitive (ChaCha20 instead of AES-GCM).

## the deps

\`\`\`jsonc
"@walletconnect/sign-client": "^2.23.9"
"@walletconnect/utils": "^2.23.9"
\`\`\`

## the project id

WalletConnect requires a per-app \`projectId\` for relay access:
\`\`\`ts
const signClient = await SignClient.init({
  projectId: import.meta.env.VITE_WALLETCONNECT_PROJECT_ID,
  metadata: {
    name: 'Chromatika',
    description: 'Chromatika browser extension wallet',
    url: 'chrome-extension://<chromatika-id>',
    icons: ['data:image/svg+xml;base64,...'],
  },
});
\`\`\`

\`projectId\` is registered at [cloud.walletconnect.com](https://cloud.walletconnect.com). free tier covers chromatika's usage.

## the pairing flow

\`\`\`ts
// 1. propose a session
const { uri, approval } = await signClient.connect({
  requiredNamespaces: {
    solana: {
      methods: ['solana_signTransaction', 'solana_signMessage'],
      chains: ['solana:mainnet', 'solana:devnet'],
      events: [],
    },
  },
});

// 2. render uri as QR code, user scans with phone wallet's WC modal
//    or paste uri into the wallet's "scan link" input

// 3. wait for the user to approve in the wallet
const session = await approval();
// session = { topic, namespaces, expiry, ... }

// 4. extract the address(es)
const account = session.namespaces.solana.accounts[0];
// e.g. "solana:mainnet:<base58-address>"
\`\`\`

## the sign request

\`\`\`ts
const result = await signClient.request({
  topic: session.topic,
  chainId: 'solana:mainnet',
  request: {
    method: 'solana_signTransaction',
    params: { transaction: base64SerializedTx },
  },
});
// result = { signature: '<base58-or-base64>' }
\`\`\`

the phone wallet pops a UI for the user to approve. result returns over the relay.

## the x402 dispatcher integration

per CLAUDE.md, chromatika dispatches x402 Solana payments based on whether \`session.solanaWcAccount\` is set:

\`\`\`ts
// in x402-dispatch.ts
const signer = session.solanaWcAccount
  ? x402WalletConnectSign(...)    // route through WC
  : x402SolanaIkaSign(...);        // route through ika MPC (default)
\`\`\`

WC path: the phone wallet (Seeker / Phantom / Solflare) signs in its own Seed Vault. the ed25519 key never leaves the phone. ika MPC bypassed entirely. this is the cleanest mitigation against the "402-bridge breach" class - even a fully compromised chromatika can't produce signatures because the key is on the phone.

## the session lifecycle

WC sessions live until **revoked phone-side** or until expiry (default 7 days). chromatika persists session metadata to skip re-pair on chrome restart:

\`\`\`jsonc
record.hardwareVendor = 'walletconnect';
record.hardwareChain = 'solana';
record.wcSessionTopic = '<topic>';
record.wcAccount = 'solana:mainnet:<base58>';
record.wcSessionExpiryMs = ...;
\`\`\`

on chrome restart, chromatika reinitializes \`SignClient\` and the session is automatically resumed (the topic is in the WC SDK's session store).

## the disconnect

\`\`\`ts
await signClient.disconnect({
  topic: session.topic,
  reason: { code: 6000, message: 'User disconnected' },
});
\`\`\`

after disconnect:
1. the phone wallet's WC sessions list shows it as ended
2. chromatika removes \`wcSessionTopic\` from the vault record
3. subsequent sign attempts fail; user must re-pair

if the user revokes phone-side first (e.g. via Phantom's WC sessions UI), chromatika's session goes invalid silently. next sign attempt fails; chromatika surfaces "WC session expired - re-pair" and removes the stale topic.

## why WC alongside MWA

different wallets support different protocols:
- **Seeker**: built-in MWA (reflector) + WC v2
- **Phantom Android**: MWA + WC
- **Phantom iOS**: WC only (no MWA on iOS)
- **Solflare**: MWA + WC
- **Jupiter**: MWA + WC

chromatika supports both transports so users can pair with whichever protocol their phone wallet exposes. for iOS users especially, WC is the only option (since MWA Android intent doesn't work on iOS).

## EVM and BTC support

WC v2 supports EVM (\`eip155:\`) and Bitcoin (\`bip122:\`) namespaces. chromatika **doesn't expose** these today - WC integration is Solana-focused for x402. when chromatika adds EVM hardware-via-WC or BTC-via-WC paths, the same \`@walletconnect/sign-client\` machinery applies; just register additional \`requiredNamespaces\`.

## library

- \`@walletconnect/sign-client\` for the SDK
- \`@walletconnect/utils\` for shared utilities (encoding, error codes)
- internal: \`wallet-extension/src/background/hardware/walletconnect.ts\` for chromatika-facing wrappers

## related

- [mwa-2-spec-and-reflector.md](/library/tech/mwa-2-spec-and-reflector) - the analogous Solana-Mobile protocol
- [x402-solana-tx-build.md](/library/tech/x402-solana-tx-build) - what gets signed via WC for x402
- [walletconnect.md](/library/user/walletconnect) (user-guides) - the user-facing flow
- [ika-seed-solana-mwa-walletconnect.md](/library/tech/ika-seed-solana-mwa-walletconnect) - the seed derivation
`,dS=`# WebAuthn PRF / HMAC-secret extension

WebAuthn's PRF (Pseudo-Random Function) extension - also called \`hmac-secret\` at the CTAP2 layer - lets a relying party (chromatika) extract a deterministic 32-byte secret from a passkey credential per assertion. given the same credential + same salt, the output is identical across assertions. this is what makes a passkey usable as an **encryption credential**, not just an authentication credential.

spec: [WebAuthn Level 3 (working draft) §10.1.4](https://www.w3.org/TR/webauthn-3/) (PRF extension).

## why we use it

without PRF, a passkey can prove "you have this credential" (authentication) but can't produce stable secret material (encryption). chromatika needs encryption to:
- derive the AES key for the passkey-PRF unlock envelope (see [passkey-prf-envelope.md](/library/tech/passkey-prf-envelope))
- derive the ika \`UserShareEncryptionKeys\` root seed for Sui-base passkey vaults (see [ika-seed-sui-passkey.md](/library/tech/ika-seed-sui-passkey))

both want a deterministic 32-byte input. PRF provides exactly that.

## the eval input

the relying party supplies a **salt** in the PRF request:
\`\`\`ts
extensions: {
  prf: {
    eval: {
      first: prfSaltBytes,      // up to 32 bytes
      // second: optionalSecondSaltBytes,
    }
  }
}
\`\`\`

the authenticator computes:
\`\`\`
prf_output = HMAC-SHA256(per_credential_secret, salt)
\`\`\`

where \`per_credential_secret\` is a 32-byte secret the authenticator generated when the credential was created and never reveals. the user can't extract \`per_credential_secret\`; only HMAC outputs of (credential, salt) pairs.

determinism: same \`(credential, salt)\` always produces the same \`prf_output\`. across devices that have the credential synced (iCloud Passwords, Google Passkey), it's the same. across re-registration of a "different" credential (a new keypair), it's different.

## the registration ceremony

\`\`\`ts
const credential = await navigator.credentials.create({
  publicKey: {
    challenge: random32,
    rp: { id: rpId, name: 'Chromatika' },
    user: { id, name, displayName },
    pubKeyCredParams: [
      { alg: -7, type: 'public-key' },                // ES256 (secp256r1)
      { alg: -257, type: 'public-key' },              // RS256 (RSA, fallback for Windows Hello)
    ],
    extensions: {
      prf: {
        eval: { first: prfSaltBytes }                 // optional eval at register time
      }
    }
  }
});

const prfResult = credential.getClientExtensionResults().prf;
if (prfResult?.results?.first) {
  const prfSecret = prfResult.results.first;          // 32 bytes
  // success - we have the PRF output
} else if (prfResult?.enabled) {
  // PRF supported but not eval-d at register; do an immediate get() to extract
  const assertion = await navigator.credentials.get({
    publicKey: {
      challenge: random32,
      rpId,
      allowCredentials: [{ id: credential.rawId, type: 'public-key' }],
      extensions: { prf: { eval: { first: prfSaltBytes } } }
    }
  });
  const prfSecret = assertion.getClientExtensionResults().prf.results.first;
} else {
  // authenticator does not support PRF / hmac-secret
  // chromatika rejects: cannot create a PRF-backed vault on this authenticator
  throw new Error('authenticator does not support PRF extension');
}
\`\`\`

some authenticators evaluate PRF at register-time (\`prf.results.first\` populated immediately). some don't, only at assertion-time. chromatika handles both paths.

## the assertion ceremony

\`\`\`ts
const assertion = await navigator.credentials.get({
  publicKey: {
    challenge: random32,
    rpId,
    allowCredentials: [{ id: credentialIdBytes, type: 'public-key' }],
    extensions: { prf: { eval: { first: prfSaltBytes } } }
  }
});
const prfSecret = assertion.getClientExtensionResults().prf.results.first;
// 32 bytes
\`\`\`

every unlock-time assertion supplies the same \`prfSaltBytes\` (persisted in the envelope record) and gets the same \`prfSecret\` back, deterministically.

## chromatika's PasskeyProviderWithPrf wrapper

chromatika has a custom wrapper (\`wallet-extension/src/ui/passkey/passkey-provider-with-prf.ts\`) around \`navigator.credentials\` that:
- mirrors \`@mysten/sui\`'s \`PasskeyProvider\` interface (so existing Sui passkey code can use it)
- injects the PRF extension request
- handles the register-vs-assertion PRF availability split
- packages the result into chromatika's expected shape

this wrapper keeps the PRF integration localized; the rest of chromatika just sees "a passkey credential that produces a 32-byte secret on assertion."

## authenticator support

PRF / \`hmac-secret\` is supported by:
- **YubiKey** firmware 5.2.3+
- **SoloKey 2** (most firmware versions)
- **Touch ID** on macOS Sonoma+ (with iCloud Passwords)
- **Windows Hello** on Windows 11 22H2+
- **Android passkey** (Google Password Manager)

NOT supported by:
- older YubiKeys (firmware <5.2.3)
- some non-FIDO2 hardware tokens
- platform authenticators on older OS versions

chromatika tests for PRF availability at registration. if not supported, surfaces "authenticator does not support PRF; use a different one or use a password / WAAP envelope instead."

## the salt rotation

\`prfSalt\` is generated once at envelope create time (32 random bytes). rotation = creating a new envelope with a fresh salt + dropping the old one. the underlying credential stays the same; only the PRF salt changes. this means the same credential can back multiple chromatika vaults, each with its own salt → different PRF outputs → different envelope keys.

## the credential public-key info

after registration, chromatika persists:
- \`credentialIdB64Url\`: webauthn \`credential.rawId\` in base64url (RFC 4648 §5)
- \`publicKeyCompressedB64\`: 33-byte secp256r1 compressed pubkey from the attestation object
- \`rpId\`: relying-party id (chrome-extension origin)
- \`prfSaltB64\`: per-vault salt

the public key isn't strictly needed for unlock (PRF is independent of the keypair), but it's used:
- for SIP-9 Sui address derivation: \`blake2b_256(0x06 || pk_compressed)\` produces a Sui address that can be displayed alongside the passkey
- for verifiable signatures (passkey can also sign, separately from the PRF flow)

## library

- browser native \`navigator.credentials\`
- internal: \`wallet-extension/src/ui/passkey/passkey-provider-with-prf.ts\` for the wrapper
- internal: \`wallet-extension/src/background/keyring/passkey-derive.ts\` for register-time validation + Sui address derivation

## related

- [passkey-prf-envelope.md](/library/tech/passkey-prf-envelope) - the unlock-envelope use of PRF
- [ika-seed-sui-passkey.md](/library/tech/ika-seed-sui-passkey) - the ika seed derivation use of PRF
- [multi-envelope-design.md](/library/tech/multi-envelope-design) - the broader envelope model
`,hS=`# WebHID (popup / side-panel only)

WebHID is the browser API for talking to USB Human Interface Devices. chromatika uses it for **Ledger** signing (any chain). critically, **WebHID requires a user gesture** and is **not available in the service worker** - which is why hardware-sign always opens a separate popup window where the gesture happens and the transport runs.

## the popup-only constraint

\`\`\`ts
// SERVICE WORKER context - this fails:
const transport = await TransportWebHID.create();
// → SecurityError: Failed to execute 'create' on 'TransportWebHID': WebHID requires a window context

// POPUP / SIDE PANEL context - this works:
const transport = await TransportWebHID.create();
// → Transport instance
\`\`\`

chromatika handles this by:
1. background enqueues a hardware-sign request (\`enqueueHardwareSign(...)\`)
2. background opens \`chrome.windows.create({ url: 'index.html?hwsign=<id>', type: 'popup', ... })\`
3. the popup runs \`TransportWebHID.create()\` (window context, has access)
4. user clicks "connect to Ledger" - that's the user gesture WebHID requires
5. WebHID prompts user to pick a Ledger device + grant permission
6. transport opens; popup runs the chain-specific signing call (\`hw-app-eth.signTransaction\`, etc.)
7. popup returns the signature via \`resolveHardwareSign({ id, signature })\`
8. background completes the original signing flow

## the user gesture rule

WebHID enforces "user gesture required" on:
- \`navigator.hid.requestDevice(...)\` - prompt the user to pick a device
- (subsequent calls with already-permitted devices don't require fresh gesture, but the **first** time a device is used after page load does)

popup is a fresh window context, so any button click in the popup counts as a user gesture for WebHID. chromatika's "Connect Ledger" button satisfies this.

## the transport library

chromatika uses \`@ledgerhq/hw-transport-webhid\`:

\`\`\`ts
import TransportWebHID from '@ledgerhq/hw-transport-webhid';

const transport = await TransportWebHID.create();
const eth = new Eth(transport);
const signature = await eth.signTransaction(derivationPath, rawTxHex);
await transport.close();
\`\`\`

\`TransportWebHID.create()\`:
1. opens a system dialog for the user to pick the Ledger device
2. once selected, returns a Transport object
3. subsequent calls (sign, derive address, etc.) reuse the transport

\`transport.close()\` releases the device for other apps. chromatika closes after each operation.

## the per-chain Ledger apps

Ledger devices run separate firmware "apps" per chain. user must open the right app on-device before chromatika's WebHID call:
- Ethereum app for EVM (\`@ledgerhq/hw-app-eth\`)
- Sui app for Sui (\`@ledgerhq/hw-app-sui\`)
- Solana app for Solana (\`@ledgerhq/hw-app-solana\`)
- Bitcoin app for BTC (\`@ledgerhq/hw-app-btc\`)

if the wrong app is open, the WebHID call returns an error from the device. chromatika surfaces "open the X app on your Ledger" with a retry option.

## the WebHID permission model

once granted, the permission **persists** for the chromatika origin:
- user is prompted on first connection
- subsequent connections from chromatika reuse the permission silently (no dialog)
- user can revoke via chrome's \`chrome://settings/content/hidDevices\` (cumbersome)
- uninstalling chromatika revokes the permission

this means after the first Ledger connection, chromatika can re-open the transport without re-prompting **as long as** the user gesture rule is satisfied (which it is in the popup).

## the side-panel alternative

Chrome 116+ introduced \`chrome.sidePanel\` - a persistent side panel for extensions. chromatika supports running its UI as a side panel (alongside the popup variant). WebHID works in the side panel context too because side panels have a window context.

so hardware-sign popups can be replaced by side-panel-driven flows in some cases. the canonical implementation today opens a separate popup for clarity (user knows "this thin window is just for hardware signing").

## the not-in-content-script note

content scripts run in an isolated world but have access to the page's \`navigator\`. could WebHID work there? **no**:
- content scripts don't have user-gesture context for the typical case (gestures are page-context, not extension-context)
- WebHID device selection requires extension origin permissions, not page origin

content scripts pass requests to the SW which dispatches to popups. don't try to drive WebHID directly from a content script.

## the buffer polyfill connection

\`@ledgerhq/devices/hid-framing.js\` and \`bitcoinjs-lib\` (used in BTC PSBT signing) use Node's \`Buffer\` global. browsers don't have it. \`src/buffer-polyfill.ts\` shims it via the \`buffer\` npm package. **must be imported as the first line of every entry point** (popup, side panel, background).

## what doesn't work

- **WebHID in service worker**: rejected by the API. always.
- **WebHID without user gesture**: first-time access fails. button click → call works
- **multiple Ledger apps simultaneously**: only one app can be open at a time. chromatika handles by routing the user "switch to X app" before the call

## library

- browser native \`navigator.hid\`, \`navigator.hid.requestDevice\`
- \`@ledgerhq/hw-transport-webhid\` for the chromatika-friendly Transport wrapper
- \`@ledgerhq/hw-app-eth\`, \`hw-app-sui\`, \`hw-app-solana\`, \`hw-app-btc\` for chain-specific signers
- internal: \`wallet-extension/src/ui/hardware/\` for the popup-side WebHID orchestration

## related

- [ledger-hw-app-libs.md](/library/tech/ledger-hw-app-libs) - per-app driver details
- [chrome-runtime-connect-trpc-port.md](/library/tech/chrome-runtime-connect-trpc-port) - how the popup talks back to the background
- [ledger.md](/library/user/ledger) (user-guides) - the user-facing flow
`,pS=`# x402 spending caps + receipt log

chromatika gates every x402 payment through **daily USD spending caps** (per-counterparty + global) and persists a **receipt log** of the most recent 200 payments. caps reset at local-timezone midnight. receipts track status transitions from \`pending\` to \`settled\` / \`failed\` / \`rejected\` and let users flag good / bad payments for future allowlist seeding.

## storage

\`\`\`jsonc
chrome.storage.local["chromatika_x402_caps_v1"] = {
  "perCounterpartyDailyCapUsd": {                        // Map<host, capUsd>
    "api.example.com": 10,
    "api.openai.com": 50
  },
  "globalDailyCapUsd": 25,                               // total spend cap across all hosts
  "defaultPerCounterpartyDailyCapUsd": 5                 // applied to first-seen hosts
}

chrome.storage.local["chromatika_x402_receipts_v1"] = [
  {
    "id": "<uuid>",
    "host": "api.example.com",
    "amountUsd": 0.10,
    "status": "settled",                                 // 'pending' | 'settled' | 'failed' | 'rejected'
    "txHash": "<base58 solana sig>",
    "error": null,
    "quality": "good",                                   // 'good' | 'bad' | null
    "createdAtMs": 1700000000000,
    "settledAtMs": 1700000000500,
    "callerHint": { "url": "https://api.example.com/v1/...", "method": "GET" }
  },
  // ... up to 200 entries, oldest dropped on overflow
]
\`\`\`

## the cap check (pre-popup, synchronous)

\`\`\`ts
async function wouldExceedCaps(host: string, amountUsd: number): Promise<{
  exceeded: boolean,
  perHostSpentUsd: number,
  globalSpentUsd: number,
  perHostCapUsd: number | null,
  globalCapUsd: number | null,
}> {
  const caps = await readCaps();
  const receipts = await readReceipts();

  const todayStart = startOfLocalDay();
  const todayReceipts = receipts.filter(r =>
    r.createdAtMs >= todayStart && (r.status === 'pending' || r.status === 'settled')
    // failed / rejected receipts don't count against budget
  );

  const perHostSpent = todayReceipts
    .filter(r => r.host === host)
    .reduce((sum, r) => sum + r.amountUsd, 0);
  const globalSpent = todayReceipts.reduce((sum, r) => sum + r.amountUsd, 0);

  const perHostCap = caps.perCounterpartyDailyCapUsd[host]
    ?? caps.defaultPerCounterpartyDailyCapUsd
    ?? Infinity;
  const globalCap = caps.globalDailyCapUsd ?? Infinity;

  const exceeded = (perHostSpent + amountUsd > perHostCap) || (globalSpent + amountUsd > globalCap);
  return { exceeded, perHostSpent, globalSpent, perHostCap, globalCap };
}
\`\`\`

key behaviors:
- runs **synchronously before** opening the approval popup. if exceeded, no popup → no user attention wasted
- pending payments count against the budget (so a user can't approve 10 payments simultaneously hoping they all squeeze in - the second one sees the first as pending)
- failed / rejected payments **don't** count
- per-host check uses the explicit per-host cap, falling back to the default-for-new-hosts (\`defaultPerCounterpartyDailyCapUsd\`)
- global check is independent - even if a host has unlimited cap, the global total still gates
- \`null\` cap = unlimited

## defaults

\`\`\`ts
const DEFAULT_PER_COUNTERPARTY_DAILY_CAP_USD = 5;
const DEFAULT_GLOBAL_DAILY_CAP_USD = 25;
\`\`\`

new install starts with \`$5/seller\` and \`$25 total/day\` budgets. user can adjust via:
- \`x402SetPerCounterpartyCap({ host, capUsd | null })\` - set or clear per-host
- \`x402SetGlobalCap({ capUsd | null })\` - set or clear global
- \`x402SetDefaultCap({ capUsd })\` - default for new hosts

## the day-bucket boundary

\`\`\`ts
function startOfLocalDay(): number {
  const now = new Date();
  return new Date(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0, 0).getTime();
}
\`\`\`

local timezone, midnight. a payment at 11:59 pm and one at 12:00 am are in **different day buckets** even though they're back-to-back. by design - users plan budgets in their local day, not UTC.

if the user travels timezones, the boundary shifts with them. one minor edge case: a user who flies west at midnight could see two "fresh" daily budgets in 24 hours. acceptable for a budget feature; if it ever becomes an issue, swap to UTC.

## receipt creation

\`\`\`ts
async function recordPaymentInitiated(host, amountUsd, callerHint): Promise<string> {
  const receipts = await readReceipts();
  const id = crypto.randomUUID();
  receipts.unshift({
    id,
    host,
    amountUsd,
    status: 'pending',
    txHash: null,
    error: null,
    quality: null,
    createdAtMs: Date.now(),
    settledAtMs: null,
    callerHint,
  });
  // cap at 200 most recent
  if (receipts.length > 200) receipts.length = 200;
  await chrome.storage.local.set({ chromatika_x402_receipts_v1: receipts });
  return id;
}
\`\`\`

receipts are **prepended** (newest first). once the array hits 200, oldest entries are dropped via \`length = 200\` truncation.

## status transitions

\`\`\`
created → 'pending'   (signed, not yet seen settlement)
'pending' → 'settled' (payment-response received with tx hash)
'pending' → 'failed'  (payment-response indicated error, e.g. tx didn't land)
'pending' → 'rejected' (user canceled before signing - actually 'rejected' is set at the dispatcher, no receipt change here typically)
\`\`\`

\`settled\` is the happy path. \`failed\` means the server rejected after we signed (rare but possible). \`rejected\` means user clicked cancel on the popup.

## settlement update

\`x402-fetch-wrapper.ts\` watches the retry response for \`payment-response\` header. when present:

\`\`\`ts
chrome.runtime.sendMessage({
  type: 'chromatika_x402_record_settlement',
  receiptId,
  paymentResponseHeaderB64,
});
\`\`\`

background:
\`\`\`ts
async function recordX402Settlement(receiptId, paymentResponseHeaderB64) {
  const decoded = decodePaymentResponseHeader(paymentResponseHeaderB64);
  // { scheme, chain, txHash, settledAt, amountSettled }
  await updateReceipt(receiptId, {
    status: decoded.txHash ? 'settled' : 'failed',
    txHash: decoded.txHash,
    settledAtMs: decoded.settledAt * 1000,
  });
}
\`\`\`

fire-and-forget from the page side; non-blocking on the dapp. if the settlement record fails (network blip), the receipt stays \`pending\` - users can manually clear or wait for the next time the page makes a payment to the same host.

## the quality flag

\`\`\`ts
async function setReceiptQuality(id, quality: 'good' | 'bad' | null) {
  await updateReceipt(id, { quality });
}
\`\`\`

users mark thumbs-up / thumbs-down on receipts. the data is stored locally; future surfaces could:
- auto-allowlist hosts with high thumbs-up ratio (raise their cap automatically)
- auto-blocklist hosts with thumbs-down (set their cap to 0)
- aggregate across users (privacy-preserving) for community blocklists

today the flag is **just stored**. no automated action. user-driven curation only.

## the listing surface

\`x402ListReceipts({ limit?: 1-200 })\` returns receipts in newest-first order. used by the receipts UI for display.

\`x402GetCaps()\` returns the full caps state plus today's spend per host + global. used by the caps UI for display.

## tRPC procedure list

| procedure | what |
|-----------|------|
| \`x402GetCaps\` | returns caps + today's spend |
| \`x402SetPerCounterpartyCap({ host, capUsd \\| null })\` | set / clear per-host cap |
| \`x402SetGlobalCap({ capUsd \\| null })\` | set / clear global cap |
| \`x402SetDefaultCap({ capUsd })\` | default for new hosts |
| \`x402ListReceipts({ limit })\` | list receipts |
| \`x402SetReceiptQuality({ id, quality })\` | thumbs-up/down |
| \`x402QuoteAndSign(...)\` | dispatcher entry from fetch interception (not user-facing) |
| \`getPendingX402Request({ id })\` | popup reads pending request |
| \`approvePendingX402({ id })\` | popup approves, signing happens |
| \`rejectPendingX402({ id, reason })\` | popup rejects |
| \`x402RecordSettlement({ receiptId, paymentResponseHeaderB64 })\` | page-side settlement update |

## library

- \`chrome.storage.local\` for caps + receipts persistence
- internal: \`wallet-extension/src/background/x402/x402-caps.ts\`
- internal: \`wallet-extension/src/background/x402/x402-receipts.ts\`

## related

- [x402-fetch-interception.md](/library/tech/x402-fetch-interception) - where \`wouldExceedCaps\` is called
- [x402-spec-svm-exact.md](/library/tech/x402-spec-svm-exact) - the wire format
- [x402-solana-tx-build.md](/library/tech/x402-solana-tx-build) - what gets signed (after caps approve)
`,mS=`# x402 fetch interception (\`x402-fetch-wrapper.ts\`)

every page chromatika injects into gets a \`window.fetch\` wrapper that watches for \`HTTP 402 + payment-required\` responses. on detection, the wrapper hands the encoded payment-required header to the chromatika background, awaits user approval + signing, and retries the original request with \`payment-signature\` header. failure modes (cap exceeded, user rejected, signing error) surface the original 402 unchanged so explicit dapp 402 handlers still work.

## installation

\`inject.ts\` (the page-script injected by the content script in every frame) installs the wrapper:

\`\`\`ts
import { installX402FetchWrapper } from './x402-fetch-wrapper';
installX402FetchWrapper();
\`\`\`

happens at page load, **before** any dapp code runs (content script \`run_at: 'document_start'\`). by the time dapps reach for \`window.fetch\`, our wrapper is already in place.

## the wrapper

\`\`\`ts
function installX402FetchWrapper() {
  const originalFetch = window.fetch;

  window.fetch = async function wrappedFetch(input, init) {
    const response = await originalFetch.call(this, input, init);

    // not a 402 - pass through unchanged
    if (response.status !== 402) return response;

    const paymentRequiredHeader = response.headers.get('payment-required');
    if (!paymentRequiredHeader) return response;   // 402 without our header - dapp's own 402

    // we have an x402-shaped 402. hand off to chromatika
    let paymentSignatureHeader: string | null;
    try {
      const result = await chrome.runtime.sendMessage({
        type: 'chromatika_x402_handle_402',
        paymentRequiredHeaderB64: paymentRequiredHeader,
        callerHint: { url: input.toString(), method: init?.method ?? 'GET' },
      });
      if (!result?.ok) {
        // user rejected, cap exceeded, signing failed - return the original 402
        // so the dapp can fall back to its own logic
        return response;
      }
      paymentSignatureHeader = result.paymentSignatureHeaderB64;
    } catch (e) {
      // chromatika unreachable, content script disconnected, etc.
      return response;
    }

    // retry with payment-signature
    const newInit = { ...(init ?? {}), headers: {
      ...((init?.headers as any) ?? {}),
      'payment-signature': paymentSignatureHeader,
    } };
    const retryResponse = await originalFetch.call(this, input, newInit);

    // optionally record settlement
    const paymentResponseHeader = retryResponse.headers.get('payment-response');
    if (paymentResponseHeader && result.receiptId) {
      void chrome.runtime.sendMessage({
        type: 'chromatika_x402_record_settlement',
        receiptId: result.receiptId,
        paymentResponseHeaderB64: paymentResponseHeader,
      });
    }

    return retryResponse;
  };
}
\`\`\`

key invariants:
- non-402 responses pass through unchanged
- 402s without our \`payment-required\` header pass through unchanged (some dapps return 402 for their own reasons)
- chromatika failure modes return the **original 402** so the page can fall back to whatever it would normally do
- only the **first** retry happens. if the retry returns another 402, we don't loop - that goes back to the page

## the message types

extension message bus (chrome.runtime.sendMessage):

- \`chromatika_x402_handle_402\`: page → background. carries the \`payment-required\` header bytes and caller hint. background runs cap check, opens popup, signs via ika MPC or WC, returns \`{ ok, paymentSignatureHeaderB64, receiptId }\` or \`{ ok: false, reason }\`
- \`chromatika_x402_record_settlement\`: page → background. fire-and-forget. updates the receipt status from \`pending\` to \`settled\` with the on-chain tx hash from \`payment-response\`

these route through the existing dapp-bridge port; no new transport.

## the dispatcher

\`x402-dispatch.ts\` in the background handles \`chromatika_x402_handle_402\`:

\`\`\`ts
async function dispatchX402(paymentRequiredHeaderB64, callerHint) {
  // 1. decode + validate
  const requirements = decodePaymentRequiredHeader(paymentRequiredHeaderB64);
  if (requirements.scheme !== 'exact') return { ok: false, reason: 'unsupported scheme' };
  if (requirements.chain !== 'solana') return { ok: false, reason: 'unsupported chain' };
  if (requirements.asset !== USDC_MINT) return { ok: false, reason: 'unsupported asset' };
  if (Date.now() / 1000 > requirements.deadline) return { ok: false, reason: 'expired' };

  // 2. cap check (synchronous, pre-popup)
  const host = new URL(callerHint.url).host;
  const amountUsd = requirements.amount / 1e6;   // 6 decimals
  const capCheck = await wouldExceedCaps(host, amountUsd);
  if (capCheck.exceeded) return { ok: false, reason: 'cap-exceeded', detail: capCheck };

  // 3. enqueue + open approval popup
  const requestId = randomUUID();
  await x402PendingQueue.enqueue({ id: requestId, requirements, host, amountUsd, callerHint });
  chrome.windows.create({
    url: chrome.runtime.getURL(\`index.html?x402approve=\${requestId}\`),
    type: 'popup', width: 400, height: 700,
  });

  // 4. await user resolution
  const outcome = await x402PendingQueue.awaitResolve(requestId);
  if (outcome.kind !== 'approved') return { ok: false, reason: outcome.reason };

  // 5. dispatch to right signer
  const session = await getSession();
  const signedHeader = session.solanaWcAccount
    ? await x402WalletConnectSign(requirements, callerHint, session.solanaWcAccount)
    : await x402SolanaIkaSign(requirements, callerHint, session.activeDwallet);

  // 6. record receipt as 'pending' (will flip to 'settled' on payment-response)
  const receiptId = await x402Receipts.create({ host, amountUsd, status: 'pending', ... });

  return { ok: true, paymentSignatureHeaderB64: signedHeader, receiptId };
}
\`\`\`

## why pre-popup cap check

\`wouldExceedCaps(host, amountUsd)\` runs synchronously **before** opening the approval popup. if the user is over their daily limit:
- popup never opens
- user doesn't waste attention on a request that would be rejected anyway
- dapp gets the 402 back, can degrade gracefully (offer a cheaper tier, ask user to top up cap, etc.)

caps reset at local-timezone midnight. see [x402-caps-receipts.md](/library/tech/x402-caps-receipts).

## the failure-pass-through philosophy

if anything goes wrong on chromatika's side - cap exceeded, user closed popup, signing crashed, ika network down, WC session expired - the wrapper returns the **original 402** to the page. this means:
- explicit dapp 402 handlers (e.g. \`if (resp.status === 402) showPayWall(resp)\`) keep working
- dapps that built their own non-x402 payment flow on top of 402 are unaffected
- chromatika is opt-in at the **wallet** layer, not at the dapp layer

contrast with: chromatika returning a 200 with chromatika-shaped error JSON, which would break dapps. the wrapper is intentionally minimal.

## what the wrapper does **not** do

- doesn't wrap XHR (XMLHttpRequest). dapps using XHR for 402-aware endpoints don't get the integration. modern dapps use fetch
- doesn't wrap \`node-fetch\` or other server-side HTTP clients - this is a browser-only wrapper, in the page world
- doesn't auto-retry on transient failures - one shot at signing + retry, then back to the dapp
- doesn't aggregate multiple 402s into a single approval - each 402 is a separate user-approval popup. for high-frequency callers, future could batch (with explicit consent)

## library

- \`chrome.runtime.sendMessage\` for page → background message
- internal: \`inject.ts\` for the install hook
- internal: \`x402-fetch-wrapper.ts\` for the wrapper itself

## related

- [x402-spec-svm-exact.md](/library/tech/x402-spec-svm-exact) - what \`payment-required\` / \`payment-signature\` look like
- [x402-solana-tx-build.md](/library/tech/x402-solana-tx-build) - how the signed tx gets constructed
- [x402-caps-receipts.md](/library/tech/x402-caps-receipts) - the cap check + receipt logging
`,gS=`# x402 Solana tx build (\`x402-solana-build.ts\`)

both x402 signing paths (ika MPC and WalletConnect) share the same Solana versioned transaction construction. given x402 \`PaymentRequirements\`, build a tx that transfers USDC + writes a Memo v2 instruction with the nonce. the tx is then signed by either path.

## what the tx contains

\`\`\`
versioned tx (Message v0)
├─ recent_blockhash (fetched from Solana RPC)
├─ fee_payer = signer (the user's dWallet's Solana address, or WC-paired phone's address)
└─ instructions:
   ├─ [createAssociatedTokenAccountIdempotent]  // only if destination ATA doesn't exist
   ├─ [splToken.transfer]                        // USDC transfer
   └─ [memoV2]                                   // memo containing nonce + optional caller memo
\`\`\`

## the full build flow

\`\`\`ts
async function buildX402SolanaTx({
  requirements,                  // PaymentRequirements decoded from payment-required header
  callerHint,                    // { url, method }
  signerAddress,                 // base58 of the signer (dWallet or WC account)
}): Promise<{
  message: Uint8Array,           // serialized v0 message bytes (what gets signed)
  versionedTx: VersionedTransaction,
}> {
  const connection = new Connection(SOLANA_RPC_URL);
  const signer = new PublicKey(signerAddress);
  const usdcMint = new PublicKey(USDC_MINT);
  const amount = BigInt(requirements.amount);          // USDC base units (6 decimals)
  const destination = new PublicKey(requirements.payTo);

  // 1. derive sender ATA
  const senderAta = getAssociatedTokenAddressSync(usdcMint, signer, false);

  // 2. derive destination ATA
  const destAta = requirements.destinationAta
    ? new PublicKey(requirements.destinationAta)
    : getAssociatedTokenAddressSync(usdcMint, destination, false);

  // 3. check whether dest ATA exists; if not, prepend create-idempotent instruction
  const destAtaInfo = await connection.getAccountInfo(destAta);
  const instructions = [];

  if (!destAtaInfo) {
    instructions.push(createAssociatedTokenAccountIdempotentInstruction(
      signer,        // payer (signer pays the rent)
      destAta,       // ata to create
      destination,   // owner of the ata
      usdcMint,
    ));
  }

  // 4. SPL token transfer
  instructions.push(createTransferInstruction(
    senderAta,
    destAta,
    signer,
    Number(amount),   // u64 - chromatika's amounts fit; cast is safe
  ));

  // 5. memo v2 with nonce + optional caller memo
  const memoText = JSON.stringify({
    nonce: requirements.nonce,
    memo: requirements.memo ?? '',
    callerHint: callerHint.url,
  });
  instructions.push(new TransactionInstruction({
    keys: [],
    programId: MEMO_PROGRAM_ID_V2,   // MemoSq4gqABAXKb96qnH8TysNcWxMyWCqXgDLGmfcHr
    data: Buffer.from(memoText, 'utf-8'),
  }));

  // 6. fetch recent blockhash
  const { blockhash, lastValidBlockHeight } = await connection.getLatestBlockhash('finalized');

  // 7. compile to v0 message
  const messageV0 = new TransactionMessage({
    payerKey: signer,
    recentBlockhash: blockhash,
    instructions,
  }).compileToV0Message();

  const versionedTx = new VersionedTransaction(messageV0);
  const messageBytes = messageV0.serialize();

  return { message: messageBytes, versionedTx };
}
\`\`\`

## what gets signed

the **serialized v0 message bytes** (\`messageV0.serialize()\`) is what the signer signs. NOT the full tx; just the message portion (header + account keys + recent blockhash + instructions). after signing, the signature is written into the \`versionedTx.signatures[0]\` slot.

\`\`\`ts
versionedTx.signatures[0] = ed25519Signature;   // 64 bytes
const serializedSigned = versionedTx.serialize();   // full signed tx bytes
const paymentSignatureHeader = base64(JSON.stringify({
  scheme: 'exact',
  chain: 'solana',
  transaction: base64(serializedSigned),
  signature: base64(ed25519Signature),
  signer: signerAddress,
  nonce: requirements.nonce,
}));
\`\`\`

## why use ATAs

USDC on Solana lives in **associated token accounts** - one ATA per (mint, owner) pair. transferring USDC means transferring **between ATAs**, not between wallet addresses directly. you have to:
- know your sender ATA (deterministic via PDA derivation: \`getAssociatedTokenAddressSync(mint, owner)\`)
- know the destination ATA (either provided in \`requirements.destinationAta\` or derive from \`requirements.payTo\`)
- if destination ATA doesn't exist, create it (the sender pays the small rent)

\`createAssociatedTokenAccountIdempotentInstruction\` is the safe version - if the ATA already exists, it's a no-op. if it doesn't, it's created. this avoids race conditions where two clients try to create the same ATA simultaneously.

## the memo v2 program

Memo v2 (\`MemoSq4gqABAXKb96qnH8TysNcWxMyWCqXgDLGmfcHr\`) is a Solana program whose only purpose is to put text on-chain in a tx. it's the canonical way to attach metadata to a payment.

x402 \`exact\` SVM scheme uses Memo v2 to record the **nonce** (replay protection) and any caller-supplied memo. the server (or its facilitator) reads the memo to verify nonce uniqueness before settling.

memo content is JSON-encoded for forward compatibility - if we want to add fields (e.g. caller pubkey, ttl), we can without breaking parsers that only read \`nonce\` + \`memo\`.

## the blockhash freshness rule

Solana txs include a recent blockhash; the network rejects txs whose blockhash is more than 150 blocks (~60 seconds) old. so:
- chromatika fetches the blockhash inside the build flow, **right before** signing
- the signed tx must be submitted within ~60s or it expires
- x402 servers settle synchronously after receiving \`payment-signature\`, so this window is comfortable

if signing takes longer than 60s (e.g. user reads the popup carefully + slow MPC), the tx may expire. the user retries; chromatika fetches a fresh blockhash and rebuilds.

## the WC vs ika MPC fork

both signers receive the same \`messageBytes\`. they differ in **how** the signature is produced:

\`\`\`ts
// ika MPC path (default)
async function x402SolanaIkaSign(messageBytes, ...) {
  const { signature } = await signMessageSol(messageBytes);   // ika ED25519_EDDSA
  // signature is 64 bytes ed25519
  return assembleHeader(messageBytes, signature, dwalletAddress);
}

// WalletConnect path (when session.solanaWcAccount is set)
async function x402WalletConnectSign(messageBytes, ...) {
  const signature = await enqueueHardwareSign({
    vendor: 'walletconnect',
    kind: 'solanaTx',
    txBytes: serializedTx,   // WC signs the full tx, not just the message
  });
  return assembleHeader(messageBytes, signature, wcAccountAddress);
}
\`\`\`

ika MPC: chromatika holds half the share, ika network the other; collaboratively produce the ed25519 sig. the dWallet's Solana address is the signer.

WalletConnect: a phone wallet (Seeker / Phantom / Solflare) signs in its own Seed Vault. the ed25519 key never leaves the phone. the WC-paired account address is the signer.

both produce the same byte-shape signature; the server doesn't know which path was used.

## library

- \`@solana/web3.js\` \`Connection\`, \`PublicKey\`, \`TransactionMessage\`, \`VersionedTransaction\`
- \`@solana/spl-token\` \`getAssociatedTokenAddressSync\`, \`createTransferInstruction\`, \`createAssociatedTokenAccountIdempotentInstruction\`
- internal: \`wallet-extension/src/background/x402/x402-solana-build.ts\`

## related

- [x402-spec-svm-exact.md](/library/tech/x402-spec-svm-exact) - what \`PaymentRequirements\` looks like
- [x402-fetch-interception.md](/library/tech/x402-fetch-interception) - the page-side fetch wrapper that triggers this
- [x402-caps-receipts.md](/library/tech/x402-caps-receipts) - the spending controls that gate this
`,fS=`# x402 v2 spec - the SVM \`exact\` scheme

x402 is an HTTP-native micropayment protocol: server returns \`HTTP 402 Payment Required\` with a \`payment-required\` header describing what the client needs to pay. client signs a payment, retries with \`payment-signature\`. server settles, returns 200 with \`payment-response\`. spec is at \`github.com/x402-foundation/x402\`. chromatika implements **only the \`exact\` scheme on Solana** with **USDC mint**.

## what we implement

- spec target: x402 v2.0 ([github.com/x402-foundation/x402](https://github.com/x402-foundation/x402))
- scheme: \`exact\` (precise USDC amount per request, no price negotiation)
- chain: Solana
- token: USDC mint \`EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v\` (SPL token)
- per-scheme reference: [scheme_exact_svm (upstream spec)](https://github.com/x402-foundation/x402/blob/main/specs/schemes/exact/scheme_exact_svm.md)

## what we don't implement

- EVM \`exact\` scheme (different address format, different transfer mechanic)
- non-USDC tokens
- non-\`exact\` schemes (e.g. dynamic pricing, subscription bundles)
- HTTPS-CONNECT 402 flows (only HTTP/HTTPS request 402s)

## the wire shape (request)

\`\`\`
[client] GET /paid-resource HTTP/1.1
[server] HTTP/1.1 402 Payment Required
         content-type: application/json
         payment-required: <base64 PaymentRequirements>

         {"error":"payment required","scheme":"exact","chain":"solana","amount":"100000",...}

[client decodes payment-required, runs through chromatika x402 dispatcher,
 user approves, chromatika signs, retry...]

[client] GET /paid-resource HTTP/1.1
         payment-signature: <base64 PaymentSignature>
[server] HTTP/1.1 200 OK
         payment-response: <base64 PaymentResponse>
         content-type: application/json

         { ...resource body... }
\`\`\`

## PaymentRequirements

a base64-encoded JSON envelope with these fields (per \`exact\` SVM scheme):

\`\`\`jsonc
{
  "scheme": "exact",
  "chain": "solana",
  "network": "solana-mainnet" | "solana-devnet",
  "amount": "100000",                                    // USDC base units (6 decimals → 100000 = $0.10)
  "asset": "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v",  // USDC mint
  "payTo": "<base58 destination wallet address>",
  "destinationAta": "<base58 destination ATA, optional>",
  "memo": "<optional UTF-8 string>",
  "facilitator": "<facilitator URL or program id>",
  "deadline": 1700000000,                                // unix seconds
  "nonce": "<base64 16 random bytes>"
}
\`\`\`

key fields chromatika checks:
- \`scheme === "exact"\` (reject otherwise)
- \`chain === "solana"\` (reject otherwise)
- \`asset === USDC_MINT\` (reject otherwise)
- \`amount\` is a non-negative integer string in base units
- \`payTo\` is a valid base58 Solana address
- \`deadline\` is in the future (otherwise reject with "expired")

## PaymentSignature

what chromatika produces and the client retries with:

\`\`\`jsonc
{
  "scheme": "exact",
  "chain": "solana",
  "transaction": "<base64 serialized Solana versioned transaction>",
  "signature": "<base64 ed25519 signature on the transaction>",
  "signer": "<base58 signer address>",
  "nonce": "<echoed from PaymentRequirements>"
}
\`\`\`

the **transaction** is a Solana versioned tx that:
1. transfers \`amount\` USDC from \`signer\`'s ATA to \`payTo\`'s ATA (creating destination ATA if needed)
2. includes a Memo v2 instruction with the nonce + optional memo
3. has the right blockhash + fee payer

the client passes this to the server via \`payment-signature\` header. the server (or its facilitator) **submits** the transaction on-chain to settle, then returns the resource.

## PaymentResponse

after settlement, server returns:

\`\`\`jsonc
{
  "scheme": "exact",
  "chain": "solana",
  "txHash": "<base58 transaction signature>",
  "settledAt": 1700000123,
  "amountSettled": "100000"
}
\`\`\`

chromatika reads this in \`recordX402Settlement\` to flip the receipt status from \`pending\` to \`settled\` with the on-chain hash.

## why this design

x402 is "Stripe for HTTP, no Stripe". instead of:
- API keys (manual issuance, manual rotation, server has to issue + track)
- monthly subscriptions (per-user account creation)
- ad-supported (privacy-hostile, low margin)

x402 lets:
- API providers gate any endpoint on a per-request payment
- API consumers (especially AI agents) pay micropayments per call without prior signup
- the facilitator settles on-chain so neither party needs trust

USDC on Solana is the natural choice: cheap fees (~$0.0001/tx), fast finality (~400ms), stable value (USD-pegged).

## the ed25519 deterministic-sig advantage

because Solana ed25519 is deterministic per RFC 8032, the same inputs (USDC amount, recipient ATA, blockhash, fee payer, memo) **always produce the same signature**. this matters for two reasons:
1. the WalletConnect path (\`x402-walletconnect-signer.ts\`) where the signature comes from a Seeker / Phantom phone over the WC relay - the ed25519 key never leaves the phone, so determinism makes the wallet's signature predictable / verifiable
2. replay protection comes from \`nonce\` + \`deadline\` rather than from "the same tx can't be signed twice"; otherwise determinism would let an attacker re-submit the same tx multiple times. servers MUST validate nonce uniqueness

## chromatika's implementation files

- \`src/dapp-interface/x402-fetch-wrapper.ts\` - injected \`fetch\` interceptor on every page
- \`src/dapp-interface/x402-dispatch.ts\` - background dispatcher (cap check, popup orchestration)
- \`src/background/x402/x402-solana-build.ts\` - tx construction (versioned tx + ATA derivation + SPL transfer + Memo v2)
- \`src/background/x402/x402-solana-signer.ts\` - ika MPC signing path
- \`src/background/x402/x402-walletconnect-signer.ts\` - WC signing path
- \`src/background/x402/x402-caps.ts\` - per-counterparty + global daily caps
- \`src/background/x402/x402-receipts.ts\` - 200-most-recent receipt log
- \`src/ui/components/X402ApprovalScreen.tsx\` - approval popup

## related

- [x402-fetch-interception.md](/library/tech/x402-fetch-interception) - the page-side interception
- [x402-solana-tx-build.md](/library/tech/x402-solana-tx-build) - the tx construction details
- [x402-caps-receipts.md](/library/tech/x402-caps-receipts) - the spending controls + receipt log
`,yS=Object.assign({"../library/user-guides/README.md":Rv,"../library/user-guides/activity-notes.md":Mv,"../library/user-guides/add-custom-network.md":Dv,"../library/user-guides/agent-surface-mcp.md":Bv,"../library/user-guides/app-settings.md":Lv,"../library/user-guides/approve-dapp-tx.md":Nv,"../library/user-guides/browse-nfts.md":Kv,"../library/user-guides/connect-dapp.md":Wv,"../library/user-guides/create-dwallet.md":jv,"../library/user-guides/create-vault.md":Fv,"../library/user-guides/dapp-consent-mode.md":Uv,"../library/user-guides/encrypted-dwallet-labels.md":zv,"../library/user-guides/encryption-lab.md":Ov,"../library/user-guides/hardware-vault.md":Hv,"../library/user-guides/ika-fee-management.md":qv,"../library/user-guides/ika-staking.md":Vv,"../library/user-guides/import-vault-mnemonic.md":Gv,"../library/user-guides/import-vault-private-key.md":$v,"../library/user-guides/lazor-vault.md":Qv,"../library/user-guides/ledger.md":Jv,"../library/user-guides/manage-dapp-permissions.md":Yv,"../library/user-guides/manage-dwallets.md":Xv,"../library/user-guides/manage-networks.md":Zv,"../library/user-guides/manage-vaults.md":ek,"../library/user-guides/media-safety-mode.md":nk,"../library/user-guides/passkey-vault.md":tk,"../library/user-guides/phishing-protection.md":rk,"../library/user-guides/presign-pool.md":ak,"../library/user-guides/price-source-priority.md":sk,"../library/user-guides/recovery-words.md":ik,"../library/user-guides/safety-alerts.md":ok,"../library/user-guides/seeker-local.md":lk,"../library/user-guides/seeker-remote.md":ck,"../library/user-guides/send-bitcoin.md":uk,"../library/user-guides/send-evm.md":dk,"../library/user-guides/send-solana.md":hk,"../library/user-guides/send-sui.md":pk,"../library/user-guides/sign-messages.md":mk,"../library/user-guides/sui-ika-swap.md":gk,"../library/user-guides/sui-kiosks.md":fk,"../library/user-guides/transfer-dwallet.md":yk,"../library/user-guides/trezor.md":wk,"../library/user-guides/unlock-and-lock.md":vk,"../library/user-guides/view-activity-and-portfolio.md":kk,"../library/user-guides/waap-vault.md":bk,"../library/user-guides/walletconnect.md":Sk,"../library/user-guides/x402-payments.md":xk}),wS=Object.assign({"../library/tech-guides/2pc-mpc-overview.md":Pk,"../library/tech-guides/README.md":_k,"../library/tech-guides/activity-notes-encrypt-decrypt.md":Ek,"../library/tech-guides/aes-gcm-vault.md":Ck,"../library/tech-guides/aftermath-router.md":Ak,"../library/tech-guides/alerts-overview.md":Ik,"../library/tech-guides/alerts-poller-and-actions.md":Tk,"../library/tech-guides/alerts-publish-cli.md":Rk,"../library/tech-guides/alerts-publisher-allowlist.md":Mk,"../library/tech-guides/alerts-signed-feed-format.md":Dk,"../library/tech-guides/aptos-sign.md":Bk,"../library/tech-guides/argon2id-kdf.md":Lk,"../library/tech-guides/bip39-mnemonic.md":Nk,"../library/tech-guides/bip44-slip10-derivation.md":Kk,"../library/tech-guides/bitcoinjs-lib-usage.md":Wk,"../library/tech-guides/btc-tx-sign-segwit-taproot.md":jk,"../library/tech-guides/buffer-polyfill.md":Fk,"../library/tech-guides/chainlist-search.md":Uk,"../library/tech-guides/chrome-alarms-and-idle.md":zk,"../library/tech-guides/chrome-declarativenetrequest.md":Ok,"../library/tech-guides/chrome-runtime-connect-trpc-port.md":Hk,"../library/tech-guides/chrome-runtime-connectnative.md":qk,"../library/tech-guides/chrome-storage-local-and-session.md":Vk,"../library/tech-guides/cold-sw-unlock-cache.md":Gk,"../library/tech-guides/dapp-bridge-message-validation.md":$k,"../library/tech-guides/ecdsa-secp256k1.md":Qk,"../library/tech-guides/ed25519-eddsa.md":Jk,"../library/tech-guides/eip-1193-and-6963.md":Yk,"../library/tech-guides/eip-3085-3326.md":Xk,"../library/tech-guides/encrypt-17-byte-canonical-format.md":Zk,"../library/tech-guides/encrypt-create-input.md":eb,"../library/tech-guides/encrypt-grpc-web-fetch-transport.md":nb,"../library/tech-guides/encrypt-multi-chunk-labels.md":tb,"../library/tech-guides/encrypt-on-chain-status-polling.md":rb,"../library/tech-guides/encrypt-pre-alpha-overview.md":ab,"../library/tech-guides/encrypt-protobuf-wire.md":sb,"../library/tech-guides/encrypt-read-ciphertext-signed.md":ib,"../library/tech-guides/encryption-backend-abstraction.md":ob,"../library/tech-guides/eth-phishing-detect.md":lb,"../library/tech-guides/evm-personal-sign-and-typeddata.md":cb,"../library/tech-guides/evm-send-flow.md":ub,"../library/tech-guides/ika-accept-share-zerotrust.md":db,"../library/tech-guides/ika-dkg-flow.md":hb,"../library/tech-guides/ika-presign-pool-impl.md":pb,"../library/tech-guides/ika-re-encrypt-transfer.md":mb,"../library/tech-guides/ika-seed-derivation-overview.md":gb,"../library/tech-guides/ika-seed-solana-lazor.md":fb,"../library/tech-guides/ika-seed-solana-ledger-keycopy.md":yb,"../library/tech-guides/ika-seed-solana-mnemonic.md":wb,"../library/tech-guides/ika-seed-solana-mwa-walletconnect.md":vb,"../library/tech-guides/ika-seed-solana-private-key.md":kb,"../library/tech-guides/ika-seed-sui-ledger-keycopy.md":bb,"../library/tech-guides/ika-seed-sui-mnemonic.md":Sb,"../library/tech-guides/ika-seed-sui-passkey.md":xb,"../library/tech-guides/ika-seed-sui-private-key.md":Pb,"../library/tech-guides/ika-seed-sui-waap.md":_b,"../library/tech-guides/ika-sign-flow.md":Eb,"../library/tech-guides/keccak256-uses.md":Cb,"../library/tech-guides/ledger-hw-app-libs.md":Ab,"../library/tech-guides/mcp-bearer-token-auth.md":Ib,"../library/tech-guides/mcp-host-spawn-and-setup.md":Tb,"../library/tech-guides/mcp-http-transport.md":Rb,"../library/tech-guides/mcp-list-active-alerts-tool.md":Mb,"../library/tech-guides/mcp-native-messaging-frame.md":Db,"../library/tech-guides/mcp-protocol-overview.md":Bb,"../library/tech-guides/mcp-reconfigure-port.md":Lb,"../library/tech-guides/mcp-stdio-bridge.md":Nb,"../library/tech-guides/mcp-tool-routing.md":Kb,"../library/tech-guides/multi-envelope-design.md":Wb,"../library/tech-guides/mwa-2-spec-and-reflector.md":jb,"../library/tech-guides/mwa-local-android-intent.md":Fb,"../library/tech-guides/mwa-remote-qr-pairing.md":Ub,"../library/tech-guides/mysten-kiosk-client.md":zb,"../library/tech-guides/mysten-sui-pinning-and-patches.md":Ob,"../library/tech-guides/nft-api-providers.md":Hb,"../library/tech-guides/noble-and-scure-crypto-libs.md":qb,"../library/tech-guides/passkey-prf-envelope.md":Vb,"../library/tech-guides/password-envelope.md":Gb,"../library/tech-guides/price-waterfall-and-sources.md":$b,"../library/tech-guides/recovery-words-envelope.md":Qb,"../library/tech-guides/session-state-multi-vault.md":Jb,"../library/tech-guides/sha512-and-blake2b.md":Yb,"../library/tech-guides/signature-normalization.md":Xb,"../library/tech-guides/signed-tx-record.md":Zb,"../library/tech-guides/solana-tx-sign.md":eS,"../library/tech-guides/sui-graphql-client.md":nS,"../library/tech-guides/sui-personal-message-divergence.md":tS,"../library/tech-guides/sui-tx-sign-via-ika.md":rS,"../library/tech-guides/taproot-schnorr.md":aS,"../library/tech-guides/trezor-connect-iframe.md":sS,"../library/tech-guides/vault-blob-v3-format.md":iS,"../library/tech-guides/waap-determinism-probe.md":oS,"../library/tech-guides/wallet-signature-envelope.md":lS,"../library/tech-guides/wallet-standard-sui-and-solana.md":cS,"../library/tech-guides/walletconnect-v2.md":uS,"../library/tech-guides/webauthn-prf-hmac-secret.md":dS,"../library/tech-guides/webhid-popup-context.md":hS,"../library/tech-guides/x402-caps-receipts.md":pS,"../library/tech-guides/x402-fetch-interception.md":mS,"../library/tech-guides/x402-solana-tx-build.md":gS,"../library/tech-guides/x402-spec-svm-exact.md":fS});function Dm(e){var t;const n=new Map;for(const[r,a]of Object.entries(e)){const s=r.split(/[/\\]/).pop().replace(/\.md$/i,"").toLowerCase(),i=a.split(`
`);let o=s,l=0;if((t=i[0])!=null&&t.startsWith("# "))for(o=i[0].slice(2).trim(),l=1;l<i.length&&i[l].trim()==="";)l++;const c=i.slice(l).join(`
`);n.set(s,{title:o,bodyMd:c})}return n}const vS=Dm(yS),kS=Dm(wS);function hi(e){return e==="user"?vS:kS}function bS(e,n){var t;return(t=hi(e).get(n))==null?void 0:t.bodyMd}function SS(e,n){var t;return(t=hi(e).get(n))==null?void 0:t.title}function xS(e,n){return hi(e).has(n)}function PS(e,n){return e.slug==="readme"?-1:n.slug==="readme"?1:e.title.localeCompare(n.title,void 0,{sensitivity:"base"})}function cl(e){const n=hi(e),t=[];for(const[r,{title:a}]of n)t.push({slug:r,title:a});return t.sort(PS),t}const _S=(()=>{const e=[];for(const{slug:n,title:t}of cl("user"))e.push({kind:"lib-user",slug:n,title:t,summary:"user guide library (markdown)",href:`/library/user/${n}`});for(const{slug:n,title:t}of cl("tech"))e.push({kind:"lib-tech",slug:n,title:t,summary:"tech guide library (markdown)",href:`/library/tech/${n}`});return e})();function ES(e){const n=e.trim().toLowerCase();return n?_S.filter(t=>t.title.toLowerCase().includes(n)||t.slug.toLowerCase().includes(n)||t.summary.toLowerCase().includes(n)):[]}function cd(e,n,t,r){return n.toLowerCase().includes(e)||t.toLowerCase().includes(e)||r.toLowerCase().includes(e)}function CS(e){const n=e.trim().toLowerCase();if(!n)return[];const t=di.filter(s=>cd(n,s.title,s.summary,s.slug)).map(s=>({kind:"kb",slug:s.slug,title:s.title,summary:s.summary,href:`/article/${s.slug}`})),r=Pc.filter(s=>cd(n,s.title,s.summary,s.slug)).map(s=>({kind:"guide",slug:s.slug,title:s.title,summary:s.summary,href:`/guide/${s.slug}`})),a=ES(e).map(s=>({kind:s.kind,slug:s.slug,title:s.title,summary:s.summary,href:s.href}));return[...t,...r,...a]}const Bm=T.createContext(null);function AS(){const e=T.useContext(Bm);if(!e)throw new Error("useSiteSearch must be used within SearchModalProvider");return e}function IS({children:e}){const[n,t]=T.useState(!1),r=T.useCallback(()=>t(!0),[]),a=T.useCallback(()=>t(!1),[]),s=T.useMemo(()=>({openSearch:r,closeSearch:a,isOpen:n}),[n,r,a]);return h.jsxs(Bm.Provider,{value:s,children:[e,h.jsx(RS,{open:n,onClose:a})]})}function TS(){return h.jsxs("svg",{width:"20",height:"20",viewBox:"0 0 24 24",fill:"none","aria-hidden":!0,children:[h.jsx("path",{d:"M10.5 18a7.5 7.5 0 1 1 0-15 7.5 7.5 0 0 1 0 15Z",stroke:"currentColor",strokeWidth:"2"}),h.jsx("path",{d:"m16 16 5 5",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round"})]})}function RS({open:e,onClose:n}){const t=T.useId(),r=`${t}-search-title`,a=`${t}-search-input`,s=T.useRef(null),[i,o]=T.useState(""),l=T.useMemo(()=>CS(i),[i]);return T.useEffect(()=>{if(!e){o("");return}const c=window.setTimeout(()=>{var d;return(d=s.current)==null?void 0:d.focus()},0);return()=>window.clearTimeout(c)},[e]),T.useEffect(()=>{if(!e)return;function c(d){d.key==="Escape"&&n()}return window.addEventListener("keydown",c),()=>window.removeEventListener("keydown",c)},[e,n]),e?h.jsxs("div",{className:"search-modal-root",children:[h.jsx("button",{type:"button",className:"search-modal-backdrop",onClick:n,"aria-label":"close search"}),h.jsxs("div",{className:"search-modal-dialog",role:"dialog","aria-modal":"true","aria-labelledby":r,children:[h.jsxs("div",{className:"search-modal-header",children:[h.jsx("h2",{id:r,className:"search-modal-title",children:"search"}),h.jsx("button",{type:"button",className:"search-modal-close",onClick:n,"aria-label":"close",children:"×"})]}),h.jsx("label",{htmlFor:a,className:"visually-hidden",children:"search knowledge base, guide, and markdown library"}),h.jsx("input",{id:a,ref:s,type:"search",className:"search-modal-input",placeholder:"articles, guides, library pages…",value:i,onChange:c=>o(c.target.value),autoComplete:"off"}),h.jsxs("ul",{className:"search-modal-results","aria-label":"search results",children:[!i.trim()&&h.jsx("li",{className:"search-modal-empty",children:"type to search the knowledge base, user guide, and markdown library."}),i.trim()&&l.length===0&&h.jsx("li",{className:"search-modal-empty",children:"no matches. try another word."}),l.map(c=>h.jsx("li",{children:h.jsxs(z,{to:c.href,className:"search-modal-hit",onClick:()=>n(),children:[h.jsx("span",{className:"search-modal-hit-badge",children:c.kind==="kb"?"kb":c.kind==="guide"?"guide":c.kind==="lib-user"?"user md":"tech md"}),h.jsx("span",{className:"search-modal-hit-title",children:c.title}),h.jsx("span",{className:"search-modal-hit-sum",children:c.summary})]})},`${c.kind}-${c.slug}`))]})]})]}):null}function MS(){const{openSearch:e}=AS();return h.jsx("button",{type:"button",className:"header-search-btn",onClick:e,"aria-label":"Open site search",children:h.jsx(TS,{})})}function DS(e,n){const t={};return(e[e.length-1]===""?[...e,""]:e).join((t.padRight?" ":"")+","+(t.padLeft===!1?"":" ")).trim()}const BS=/^[$_\p{ID_Start}][$_\u{200C}\u{200D}\p{ID_Continue}]*$/u,LS=/^[$_\p{ID_Start}][-$_\u{200C}\u{200D}\p{ID_Continue}]*$/u,NS={};function ud(e,n){return(NS.jsx?LS:BS).test(e)}const KS=/[ \t\n\f\r]/g;function WS(e){return typeof e=="object"?e.type==="text"?dd(e.value):!1:dd(e)}function dd(e){return e.replace(KS,"")===""}class Ta{constructor(n,t,r){this.normal=t,this.property=n,r&&(this.space=r)}}Ta.prototype.normal={};Ta.prototype.property={};Ta.prototype.space=void 0;function Lm(e,n){const t={},r={};for(const a of e)Object.assign(t,a.property),Object.assign(r,a.normal);return new Ta(t,r,n)}function ul(e){return e.toLowerCase()}class Ge{constructor(n,t){this.attribute=t,this.property=n}}Ge.prototype.attribute="";Ge.prototype.booleanish=!1;Ge.prototype.boolean=!1;Ge.prototype.commaOrSpaceSeparated=!1;Ge.prototype.commaSeparated=!1;Ge.prototype.defined=!1;Ge.prototype.mustUseProperty=!1;Ge.prototype.number=!1;Ge.prototype.overloadedBoolean=!1;Ge.prototype.property="";Ge.prototype.spaceSeparated=!1;Ge.prototype.space=void 0;let jS=0;const V=Wt(),we=Wt(),dl=Wt(),R=Wt(),se=Wt(),lr=Wt(),Ye=Wt();function Wt(){return 2**++jS}const hl=Object.freeze(Object.defineProperty({__proto__:null,boolean:V,booleanish:we,commaOrSpaceSeparated:Ye,commaSeparated:lr,number:R,overloadedBoolean:dl,spaceSeparated:se},Symbol.toStringTag,{value:"Module"})),Yi=Object.keys(hl);class _c extends Ge{constructor(n,t,r,a){let s=-1;if(super(n,t),hd(this,"space",a),typeof r=="number")for(;++s<Yi.length;){const i=Yi[s];hd(this,Yi[s],(r&hl[i])===hl[i])}}}_c.prototype.defined=!0;function hd(e,n,t){t&&(e[n]=t)}function xr(e){const n={},t={};for(const[r,a]of Object.entries(e.properties)){const s=new _c(r,e.transform(e.attributes||{},r),a,e.space);e.mustUseProperty&&e.mustUseProperty.includes(r)&&(s.mustUseProperty=!0),n[r]=s,t[ul(r)]=r,t[ul(s.attribute)]=r}return new Ta(n,t,e.space)}const Nm=xr({properties:{ariaActiveDescendant:null,ariaAtomic:we,ariaAutoComplete:null,ariaBusy:we,ariaChecked:we,ariaColCount:R,ariaColIndex:R,ariaColSpan:R,ariaControls:se,ariaCurrent:null,ariaDescribedBy:se,ariaDetails:null,ariaDisabled:we,ariaDropEffect:se,ariaErrorMessage:null,ariaExpanded:we,ariaFlowTo:se,ariaGrabbed:we,ariaHasPopup:null,ariaHidden:we,ariaInvalid:null,ariaKeyShortcuts:null,ariaLabel:null,ariaLabelledBy:se,ariaLevel:R,ariaLive:null,ariaModal:we,ariaMultiLine:we,ariaMultiSelectable:we,ariaOrientation:null,ariaOwns:se,ariaPlaceholder:null,ariaPosInSet:R,ariaPressed:we,ariaReadOnly:we,ariaRelevant:null,ariaRequired:we,ariaRoleDescription:se,ariaRowCount:R,ariaRowIndex:R,ariaRowSpan:R,ariaSelected:we,ariaSetSize:R,ariaSort:null,ariaValueMax:R,ariaValueMin:R,ariaValueNow:R,ariaValueText:null,role:null},transform(e,n){return n==="role"?n:"aria-"+n.slice(4).toLowerCase()}});function Km(e,n){return n in e?e[n]:n}function Wm(e,n){return Km(e,n.toLowerCase())}const FS=xr({attributes:{acceptcharset:"accept-charset",classname:"class",htmlfor:"for",httpequiv:"http-equiv"},mustUseProperty:["checked","multiple","muted","selected"],properties:{abbr:null,accept:lr,acceptCharset:se,accessKey:se,action:null,allow:null,allowFullScreen:V,allowPaymentRequest:V,allowUserMedia:V,alt:null,as:null,async:V,autoCapitalize:null,autoComplete:se,autoFocus:V,autoPlay:V,blocking:se,capture:null,charSet:null,checked:V,cite:null,className:se,cols:R,colSpan:null,content:null,contentEditable:we,controls:V,controlsList:se,coords:R|lr,crossOrigin:null,data:null,dateTime:null,decoding:null,default:V,defer:V,dir:null,dirName:null,disabled:V,download:dl,draggable:we,encType:null,enterKeyHint:null,fetchPriority:null,form:null,formAction:null,formEncType:null,formMethod:null,formNoValidate:V,formTarget:null,headers:se,height:R,hidden:dl,high:R,href:null,hrefLang:null,htmlFor:se,httpEquiv:se,id:null,imageSizes:null,imageSrcSet:null,inert:V,inputMode:null,integrity:null,is:null,isMap:V,itemId:null,itemProp:se,itemRef:se,itemScope:V,itemType:se,kind:null,label:null,lang:null,language:null,list:null,loading:null,loop:V,low:R,manifest:null,max:null,maxLength:R,media:null,method:null,min:null,minLength:R,multiple:V,muted:V,name:null,nonce:null,noModule:V,noValidate:V,onAbort:null,onAfterPrint:null,onAuxClick:null,onBeforeMatch:null,onBeforePrint:null,onBeforeToggle:null,onBeforeUnload:null,onBlur:null,onCancel:null,onCanPlay:null,onCanPlayThrough:null,onChange:null,onClick:null,onClose:null,onContextLost:null,onContextMenu:null,onContextRestored:null,onCopy:null,onCueChange:null,onCut:null,onDblClick:null,onDrag:null,onDragEnd:null,onDragEnter:null,onDragExit:null,onDragLeave:null,onDragOver:null,onDragStart:null,onDrop:null,onDurationChange:null,onEmptied:null,onEnded:null,onError:null,onFocus:null,onFormData:null,onHashChange:null,onInput:null,onInvalid:null,onKeyDown:null,onKeyPress:null,onKeyUp:null,onLanguageChange:null,onLoad:null,onLoadedData:null,onLoadedMetadata:null,onLoadEnd:null,onLoadStart:null,onMessage:null,onMessageError:null,onMouseDown:null,onMouseEnter:null,onMouseLeave:null,onMouseMove:null,onMouseOut:null,onMouseOver:null,onMouseUp:null,onOffline:null,onOnline:null,onPageHide:null,onPageShow:null,onPaste:null,onPause:null,onPlay:null,onPlaying:null,onPopState:null,onProgress:null,onRateChange:null,onRejectionHandled:null,onReset:null,onResize:null,onScroll:null,onScrollEnd:null,onSecurityPolicyViolation:null,onSeeked:null,onSeeking:null,onSelect:null,onSlotChange:null,onStalled:null,onStorage:null,onSubmit:null,onSuspend:null,onTimeUpdate:null,onToggle:null,onUnhandledRejection:null,onUnload:null,onVolumeChange:null,onWaiting:null,onWheel:null,open:V,optimum:R,pattern:null,ping:se,placeholder:null,playsInline:V,popover:null,popoverTarget:null,popoverTargetAction:null,poster:null,preload:null,readOnly:V,referrerPolicy:null,rel:se,required:V,reversed:V,rows:R,rowSpan:R,sandbox:se,scope:null,scoped:V,seamless:V,selected:V,shadowRootClonable:V,shadowRootDelegatesFocus:V,shadowRootMode:null,shape:null,size:R,sizes:null,slot:null,span:R,spellCheck:we,src:null,srcDoc:null,srcLang:null,srcSet:null,start:R,step:null,style:null,tabIndex:R,target:null,title:null,translate:null,type:null,typeMustMatch:V,useMap:null,value:we,width:R,wrap:null,writingSuggestions:null,align:null,aLink:null,archive:se,axis:null,background:null,bgColor:null,border:R,borderColor:null,bottomMargin:R,cellPadding:null,cellSpacing:null,char:null,charOff:null,classId:null,clear:null,code:null,codeBase:null,codeType:null,color:null,compact:V,declare:V,event:null,face:null,frame:null,frameBorder:null,hSpace:R,leftMargin:R,link:null,longDesc:null,lowSrc:null,marginHeight:R,marginWidth:R,noResize:V,noHref:V,noShade:V,noWrap:V,object:null,profile:null,prompt:null,rev:null,rightMargin:R,rules:null,scheme:null,scrolling:we,standby:null,summary:null,text:null,topMargin:R,valueType:null,version:null,vAlign:null,vLink:null,vSpace:R,allowTransparency:null,autoCorrect:null,autoSave:null,disablePictureInPicture:V,disableRemotePlayback:V,prefix:null,property:null,results:R,security:null,unselectable:null},space:"html",transform:Wm}),US=xr({attributes:{accentHeight:"accent-height",alignmentBaseline:"alignment-baseline",arabicForm:"arabic-form",baselineShift:"baseline-shift",capHeight:"cap-height",className:"class",clipPath:"clip-path",clipRule:"clip-rule",colorInterpolation:"color-interpolation",colorInterpolationFilters:"color-interpolation-filters",colorProfile:"color-profile",colorRendering:"color-rendering",crossOrigin:"crossorigin",dataType:"datatype",dominantBaseline:"dominant-baseline",enableBackground:"enable-background",fillOpacity:"fill-opacity",fillRule:"fill-rule",floodColor:"flood-color",floodOpacity:"flood-opacity",fontFamily:"font-family",fontSize:"font-size",fontSizeAdjust:"font-size-adjust",fontStretch:"font-stretch",fontStyle:"font-style",fontVariant:"font-variant",fontWeight:"font-weight",glyphName:"glyph-name",glyphOrientationHorizontal:"glyph-orientation-horizontal",glyphOrientationVertical:"glyph-orientation-vertical",hrefLang:"hreflang",horizAdvX:"horiz-adv-x",horizOriginX:"horiz-origin-x",horizOriginY:"horiz-origin-y",imageRendering:"image-rendering",letterSpacing:"letter-spacing",lightingColor:"lighting-color",markerEnd:"marker-end",markerMid:"marker-mid",markerStart:"marker-start",navDown:"nav-down",navDownLeft:"nav-down-left",navDownRight:"nav-down-right",navLeft:"nav-left",navNext:"nav-next",navPrev:"nav-prev",navRight:"nav-right",navUp:"nav-up",navUpLeft:"nav-up-left",navUpRight:"nav-up-right",onAbort:"onabort",onActivate:"onactivate",onAfterPrint:"onafterprint",onBeforePrint:"onbeforeprint",onBegin:"onbegin",onCancel:"oncancel",onCanPlay:"oncanplay",onCanPlayThrough:"oncanplaythrough",onChange:"onchange",onClick:"onclick",onClose:"onclose",onCopy:"oncopy",onCueChange:"oncuechange",onCut:"oncut",onDblClick:"ondblclick",onDrag:"ondrag",onDragEnd:"ondragend",onDragEnter:"ondragenter",onDragExit:"ondragexit",onDragLeave:"ondragleave",onDragOver:"ondragover",onDragStart:"ondragstart",onDrop:"ondrop",onDurationChange:"ondurationchange",onEmptied:"onemptied",onEnd:"onend",onEnded:"onended",onError:"onerror",onFocus:"onfocus",onFocusIn:"onfocusin",onFocusOut:"onfocusout",onHashChange:"onhashchange",onInput:"oninput",onInvalid:"oninvalid",onKeyDown:"onkeydown",onKeyPress:"onkeypress",onKeyUp:"onkeyup",onLoad:"onload",onLoadedData:"onloadeddata",onLoadedMetadata:"onloadedmetadata",onLoadStart:"onloadstart",onMessage:"onmessage",onMouseDown:"onmousedown",onMouseEnter:"onmouseenter",onMouseLeave:"onmouseleave",onMouseMove:"onmousemove",onMouseOut:"onmouseout",onMouseOver:"onmouseover",onMouseUp:"onmouseup",onMouseWheel:"onmousewheel",onOffline:"onoffline",onOnline:"ononline",onPageHide:"onpagehide",onPageShow:"onpageshow",onPaste:"onpaste",onPause:"onpause",onPlay:"onplay",onPlaying:"onplaying",onPopState:"onpopstate",onProgress:"onprogress",onRateChange:"onratechange",onRepeat:"onrepeat",onReset:"onreset",onResize:"onresize",onScroll:"onscroll",onSeeked:"onseeked",onSeeking:"onseeking",onSelect:"onselect",onShow:"onshow",onStalled:"onstalled",onStorage:"onstorage",onSubmit:"onsubmit",onSuspend:"onsuspend",onTimeUpdate:"ontimeupdate",onToggle:"ontoggle",onUnload:"onunload",onVolumeChange:"onvolumechange",onWaiting:"onwaiting",onZoom:"onzoom",overlinePosition:"overline-position",overlineThickness:"overline-thickness",paintOrder:"paint-order",panose1:"panose-1",pointerEvents:"pointer-events",referrerPolicy:"referrerpolicy",renderingIntent:"rendering-intent",shapeRendering:"shape-rendering",stopColor:"stop-color",stopOpacity:"stop-opacity",strikethroughPosition:"strikethrough-position",strikethroughThickness:"strikethrough-thickness",strokeDashArray:"stroke-dasharray",strokeDashOffset:"stroke-dashoffset",strokeLineCap:"stroke-linecap",strokeLineJoin:"stroke-linejoin",strokeMiterLimit:"stroke-miterlimit",strokeOpacity:"stroke-opacity",strokeWidth:"stroke-width",tabIndex:"tabindex",textAnchor:"text-anchor",textDecoration:"text-decoration",textRendering:"text-rendering",transformOrigin:"transform-origin",typeOf:"typeof",underlinePosition:"underline-position",underlineThickness:"underline-thickness",unicodeBidi:"unicode-bidi",unicodeRange:"unicode-range",unitsPerEm:"units-per-em",vAlphabetic:"v-alphabetic",vHanging:"v-hanging",vIdeographic:"v-ideographic",vMathematical:"v-mathematical",vectorEffect:"vector-effect",vertAdvY:"vert-adv-y",vertOriginX:"vert-origin-x",vertOriginY:"vert-origin-y",wordSpacing:"word-spacing",writingMode:"writing-mode",xHeight:"x-height",playbackOrder:"playbackorder",timelineBegin:"timelinebegin"},properties:{about:Ye,accentHeight:R,accumulate:null,additive:null,alignmentBaseline:null,alphabetic:R,amplitude:R,arabicForm:null,ascent:R,attributeName:null,attributeType:null,azimuth:R,bandwidth:null,baselineShift:null,baseFrequency:null,baseProfile:null,bbox:null,begin:null,bias:R,by:null,calcMode:null,capHeight:R,className:se,clip:null,clipPath:null,clipPathUnits:null,clipRule:null,color:null,colorInterpolation:null,colorInterpolationFilters:null,colorProfile:null,colorRendering:null,content:null,contentScriptType:null,contentStyleType:null,crossOrigin:null,cursor:null,cx:null,cy:null,d:null,dataType:null,defaultAction:null,descent:R,diffuseConstant:R,direction:null,display:null,dur:null,divisor:R,dominantBaseline:null,download:V,dx:null,dy:null,edgeMode:null,editable:null,elevation:R,enableBackground:null,end:null,event:null,exponent:R,externalResourcesRequired:null,fill:null,fillOpacity:R,fillRule:null,filter:null,filterRes:null,filterUnits:null,floodColor:null,floodOpacity:null,focusable:null,focusHighlight:null,fontFamily:null,fontSize:null,fontSizeAdjust:null,fontStretch:null,fontStyle:null,fontVariant:null,fontWeight:null,format:null,fr:null,from:null,fx:null,fy:null,g1:lr,g2:lr,glyphName:lr,glyphOrientationHorizontal:null,glyphOrientationVertical:null,glyphRef:null,gradientTransform:null,gradientUnits:null,handler:null,hanging:R,hatchContentUnits:null,hatchUnits:null,height:null,href:null,hrefLang:null,horizAdvX:R,horizOriginX:R,horizOriginY:R,id:null,ideographic:R,imageRendering:null,initialVisibility:null,in:null,in2:null,intercept:R,k:R,k1:R,k2:R,k3:R,k4:R,kernelMatrix:Ye,kernelUnitLength:null,keyPoints:null,keySplines:null,keyTimes:null,kerning:null,lang:null,lengthAdjust:null,letterSpacing:null,lightingColor:null,limitingConeAngle:R,local:null,markerEnd:null,markerMid:null,markerStart:null,markerHeight:null,markerUnits:null,markerWidth:null,mask:null,maskContentUnits:null,maskUnits:null,mathematical:null,max:null,media:null,mediaCharacterEncoding:null,mediaContentEncodings:null,mediaSize:R,mediaTime:null,method:null,min:null,mode:null,name:null,navDown:null,navDownLeft:null,navDownRight:null,navLeft:null,navNext:null,navPrev:null,navRight:null,navUp:null,navUpLeft:null,navUpRight:null,numOctaves:null,observer:null,offset:null,onAbort:null,onActivate:null,onAfterPrint:null,onBeforePrint:null,onBegin:null,onCancel:null,onCanPlay:null,onCanPlayThrough:null,onChange:null,onClick:null,onClose:null,onCopy:null,onCueChange:null,onCut:null,onDblClick:null,onDrag:null,onDragEnd:null,onDragEnter:null,onDragExit:null,onDragLeave:null,onDragOver:null,onDragStart:null,onDrop:null,onDurationChange:null,onEmptied:null,onEnd:null,onEnded:null,onError:null,onFocus:null,onFocusIn:null,onFocusOut:null,onHashChange:null,onInput:null,onInvalid:null,onKeyDown:null,onKeyPress:null,onKeyUp:null,onLoad:null,onLoadedData:null,onLoadedMetadata:null,onLoadStart:null,onMessage:null,onMouseDown:null,onMouseEnter:null,onMouseLeave:null,onMouseMove:null,onMouseOut:null,onMouseOver:null,onMouseUp:null,onMouseWheel:null,onOffline:null,onOnline:null,onPageHide:null,onPageShow:null,onPaste:null,onPause:null,onPlay:null,onPlaying:null,onPopState:null,onProgress:null,onRateChange:null,onRepeat:null,onReset:null,onResize:null,onScroll:null,onSeeked:null,onSeeking:null,onSelect:null,onShow:null,onStalled:null,onStorage:null,onSubmit:null,onSuspend:null,onTimeUpdate:null,onToggle:null,onUnload:null,onVolumeChange:null,onWaiting:null,onZoom:null,opacity:null,operator:null,order:null,orient:null,orientation:null,origin:null,overflow:null,overlay:null,overlinePosition:R,overlineThickness:R,paintOrder:null,panose1:null,path:null,pathLength:R,patternContentUnits:null,patternTransform:null,patternUnits:null,phase:null,ping:se,pitch:null,playbackOrder:null,pointerEvents:null,points:null,pointsAtX:R,pointsAtY:R,pointsAtZ:R,preserveAlpha:null,preserveAspectRatio:null,primitiveUnits:null,propagate:null,property:Ye,r:null,radius:null,referrerPolicy:null,refX:null,refY:null,rel:Ye,rev:Ye,renderingIntent:null,repeatCount:null,repeatDur:null,requiredExtensions:Ye,requiredFeatures:Ye,requiredFonts:Ye,requiredFormats:Ye,resource:null,restart:null,result:null,rotate:null,rx:null,ry:null,scale:null,seed:null,shapeRendering:null,side:null,slope:null,snapshotTime:null,specularConstant:R,specularExponent:R,spreadMethod:null,spacing:null,startOffset:null,stdDeviation:null,stemh:null,stemv:null,stitchTiles:null,stopColor:null,stopOpacity:null,strikethroughPosition:R,strikethroughThickness:R,string:null,stroke:null,strokeDashArray:Ye,strokeDashOffset:null,strokeLineCap:null,strokeLineJoin:null,strokeMiterLimit:R,strokeOpacity:R,strokeWidth:null,style:null,surfaceScale:R,syncBehavior:null,syncBehaviorDefault:null,syncMaster:null,syncTolerance:null,syncToleranceDefault:null,systemLanguage:Ye,tabIndex:R,tableValues:null,target:null,targetX:R,targetY:R,textAnchor:null,textDecoration:null,textRendering:null,textLength:null,timelineBegin:null,title:null,transformBehavior:null,type:null,typeOf:Ye,to:null,transform:null,transformOrigin:null,u1:null,u2:null,underlinePosition:R,underlineThickness:R,unicode:null,unicodeBidi:null,unicodeRange:null,unitsPerEm:R,values:null,vAlphabetic:R,vMathematical:R,vectorEffect:null,vHanging:R,vIdeographic:R,version:null,vertAdvY:R,vertOriginX:R,vertOriginY:R,viewBox:null,viewTarget:null,visibility:null,width:null,widths:null,wordSpacing:null,writingMode:null,x:null,x1:null,x2:null,xChannelSelector:null,xHeight:R,y:null,y1:null,y2:null,yChannelSelector:null,z:null,zoomAndPan:null},space:"svg",transform:Km}),jm=xr({properties:{xLinkActuate:null,xLinkArcRole:null,xLinkHref:null,xLinkRole:null,xLinkShow:null,xLinkTitle:null,xLinkType:null},space:"xlink",transform(e,n){return"xlink:"+n.slice(5).toLowerCase()}}),Fm=xr({attributes:{xmlnsxlink:"xmlns:xlink"},properties:{xmlnsXLink:null,xmlns:null},space:"xmlns",transform:Wm}),Um=xr({properties:{xmlBase:null,xmlLang:null,xmlSpace:null},space:"xml",transform(e,n){return"xml:"+n.slice(3).toLowerCase()}}),zS={classId:"classID",dataType:"datatype",itemId:"itemID",strokeDashArray:"strokeDasharray",strokeDashOffset:"strokeDashoffset",strokeLineCap:"strokeLinecap",strokeLineJoin:"strokeLinejoin",strokeMiterLimit:"strokeMiterlimit",typeOf:"typeof",xLinkActuate:"xlinkActuate",xLinkArcRole:"xlinkArcrole",xLinkHref:"xlinkHref",xLinkRole:"xlinkRole",xLinkShow:"xlinkShow",xLinkTitle:"xlinkTitle",xLinkType:"xlinkType",xmlnsXLink:"xmlnsXlink"},OS=/[A-Z]/g,pd=/-[a-z]/g,HS=/^data[-\w.:]+$/i;function qS(e,n){const t=ul(n);let r=n,a=Ge;if(t in e.normal)return e.property[e.normal[t]];if(t.length>4&&t.slice(0,4)==="data"&&HS.test(n)){if(n.charAt(4)==="-"){const s=n.slice(5).replace(pd,GS);r="data"+s.charAt(0).toUpperCase()+s.slice(1)}else{const s=n.slice(4);if(!pd.test(s)){let i=s.replace(OS,VS);i.charAt(0)!=="-"&&(i="-"+i),n="data"+i}}a=_c}return new a(r,n)}function VS(e){return"-"+e.toLowerCase()}function GS(e){return e.charAt(1).toUpperCase()}const $S=Lm([Nm,FS,jm,Fm,Um],"html"),Ec=Lm([Nm,US,jm,Fm,Um],"svg");function QS(e){return e.join(" ").trim()}var Cc={},md=/\/\*[^*]*\*+([^/*][^*]*\*+)*\//g,JS=/\n/g,YS=/^\s*/,XS=/^(\*?[-#/*\\\w]+(\[[0-9a-z_-]+\])?)\s*/,ZS=/^:\s*/,ex=/^((?:'(?:\\'|.)*?'|"(?:\\"|.)*?"|\([^)]*?\)|[^};])+)/,nx=/^[;\s]*/,tx=/^\s+|\s+$/g,rx=`
`,gd="/",fd="*",xt="",ax="comment",sx="declaration";function ix(e,n){if(typeof e!="string")throw new TypeError("First argument must be a string");if(!e)return[];n=n||{};var t=1,r=1;function a(f){var k=f.match(JS);k&&(t+=k.length);var P=f.lastIndexOf(rx);r=~P?f.length-P:r+f.length}function s(){var f={line:t,column:r};return function(k){return k.position=new i(f),c(),k}}function i(f){this.start=f,this.end={line:t,column:r},this.source=n.source}i.prototype.content=e;function o(f){var k=new Error(n.source+":"+t+":"+r+": "+f);if(k.reason=f,k.filename=n.source,k.line=t,k.column=r,k.source=e,!n.silent)throw k}function l(f){var k=f.exec(e);if(k){var P=k[0];return a(P),e=e.slice(P.length),k}}function c(){l(YS)}function d(f){var k;for(f=f||[];k=u();)k!==!1&&f.push(k);return f}function u(){var f=s();if(!(gd!=e.charAt(0)||fd!=e.charAt(1))){for(var k=2;xt!=e.charAt(k)&&(fd!=e.charAt(k)||gd!=e.charAt(k+1));)++k;if(k+=2,xt===e.charAt(k-1))return o("End of comment missing");var P=e.slice(2,k-2);return r+=2,a(P),e=e.slice(k),r+=2,f({type:ax,comment:P})}}function p(){var f=s(),k=l(XS);if(k){if(u(),!l(ZS))return o("property missing ':'");var P=l(ex),g=f({type:sx,property:yd(k[0].replace(md,xt)),value:P?yd(P[0].replace(md,xt)):xt});return l(nx),g}}function m(){var f=[];d(f);for(var k;k=p();)k!==!1&&(f.push(k),d(f));return f}return c(),m()}function yd(e){return e?e.replace(tx,xt):xt}var ox=ix,lx=ws&&ws.__importDefault||function(e){return e&&e.__esModule?e:{default:e}};Object.defineProperty(Cc,"__esModule",{value:!0});Cc.default=ux;const cx=lx(ox);function ux(e,n){let t=null;if(!e||typeof e!="string")return t;const r=(0,cx.default)(e),a=typeof n=="function";return r.forEach(s=>{if(s.type!=="declaration")return;const{property:i,value:o}=s;a?n(i,o,s):o&&(t=t||{},t[i]=o)}),t}var pi={};Object.defineProperty(pi,"__esModule",{value:!0});pi.camelCase=void 0;var dx=/^--[a-zA-Z0-9_-]+$/,hx=/-([a-z])/g,px=/^[^-]+$/,mx=/^-(webkit|moz|ms|o|khtml)-/,gx=/^-(ms)-/,fx=function(e){return!e||px.test(e)||dx.test(e)},yx=function(e,n){return n.toUpperCase()},wd=function(e,n){return"".concat(n,"-")},wx=function(e,n){return n===void 0&&(n={}),fx(e)?e:(e=e.toLowerCase(),n.reactCompat?e=e.replace(gx,wd):e=e.replace(mx,wd),e.replace(hx,yx))};pi.camelCase=wx;var vx=ws&&ws.__importDefault||function(e){return e&&e.__esModule?e:{default:e}},kx=vx(Cc),bx=pi;function pl(e,n){var t={};return!e||typeof e!="string"||(0,kx.default)(e,function(r,a){r&&a&&(t[(0,bx.camelCase)(r,n)]=a)}),t}pl.default=pl;var Sx=pl;const xx=Sl(Sx),zm=Om("end"),Ac=Om("start");function Om(e){return n;function n(t){const r=t&&t.position&&t.position[e]||{};if(typeof r.line=="number"&&r.line>0&&typeof r.column=="number"&&r.column>0)return{line:r.line,column:r.column,offset:typeof r.offset=="number"&&r.offset>-1?r.offset:void 0}}}function Px(e){const n=Ac(e),t=zm(e);if(n&&t)return{start:n,end:t}}function ea(e){return!e||typeof e!="object"?"":"position"in e||"type"in e?vd(e.position):"start"in e||"end"in e?vd(e):"line"in e||"column"in e?ml(e):""}function ml(e){return kd(e&&e.line)+":"+kd(e&&e.column)}function vd(e){return ml(e&&e.start)+"-"+ml(e&&e.end)}function kd(e){return e&&typeof e=="number"?e:1}class Be extends Error{constructor(n,t,r){super(),typeof t=="string"&&(r=t,t=void 0);let a="",s={},i=!1;if(t&&("line"in t&&"column"in t?s={place:t}:"start"in t&&"end"in t?s={place:t}:"type"in t?s={ancestors:[t],place:t.position}:s={...t}),typeof n=="string"?a=n:!s.cause&&n&&(i=!0,a=n.message,s.cause=n),!s.ruleId&&!s.source&&typeof r=="string"){const l=r.indexOf(":");l===-1?s.ruleId=r:(s.source=r.slice(0,l),s.ruleId=r.slice(l+1))}if(!s.place&&s.ancestors&&s.ancestors){const l=s.ancestors[s.ancestors.length-1];l&&(s.place=l.position)}const o=s.place&&"start"in s.place?s.place.start:s.place;this.ancestors=s.ancestors||void 0,this.cause=s.cause||void 0,this.column=o?o.column:void 0,this.fatal=void 0,this.file="",this.message=a,this.line=o?o.line:void 0,this.name=ea(s.place)||"1:1",this.place=s.place||void 0,this.reason=this.message,this.ruleId=s.ruleId||void 0,this.source=s.source||void 0,this.stack=i&&s.cause&&typeof s.cause.stack=="string"?s.cause.stack:"",this.actual=void 0,this.expected=void 0,this.note=void 0,this.url=void 0}}Be.prototype.file="";Be.prototype.name="";Be.prototype.reason="";Be.prototype.message="";Be.prototype.stack="";Be.prototype.column=void 0;Be.prototype.line=void 0;Be.prototype.ancestors=void 0;Be.prototype.cause=void 0;Be.prototype.fatal=void 0;Be.prototype.place=void 0;Be.prototype.ruleId=void 0;Be.prototype.source=void 0;const Ic={}.hasOwnProperty,_x=new Map,Ex=/[A-Z]/g,Cx=new Set(["table","tbody","thead","tfoot","tr"]),Ax=new Set(["td","th"]),Hm="https://github.com/syntax-tree/hast-util-to-jsx-runtime";function Ix(e,n){if(!n||n.Fragment===void 0)throw new TypeError("Expected `Fragment` in options");const t=n.filePath||void 0;let r;if(n.development){if(typeof n.jsxDEV!="function")throw new TypeError("Expected `jsxDEV` in options when `development: true`");r=Kx(t,n.jsxDEV)}else{if(typeof n.jsx!="function")throw new TypeError("Expected `jsx` in production options");if(typeof n.jsxs!="function")throw new TypeError("Expected `jsxs` in production options");r=Nx(t,n.jsx,n.jsxs)}const a={Fragment:n.Fragment,ancestors:[],components:n.components||{},create:r,elementAttributeNameCase:n.elementAttributeNameCase||"react",evaluater:n.createEvaluater?n.createEvaluater():void 0,filePath:t,ignoreInvalidStyle:n.ignoreInvalidStyle||!1,passKeys:n.passKeys!==!1,passNode:n.passNode||!1,schema:n.space==="svg"?Ec:$S,stylePropertyNameCase:n.stylePropertyNameCase||"dom",tableCellAlignToStyle:n.tableCellAlignToStyle!==!1},s=qm(a,e,void 0);return s&&typeof s!="string"?s:a.create(e,a.Fragment,{children:s||void 0},void 0)}function qm(e,n,t){if(n.type==="element")return Tx(e,n,t);if(n.type==="mdxFlowExpression"||n.type==="mdxTextExpression")return Rx(e,n);if(n.type==="mdxJsxFlowElement"||n.type==="mdxJsxTextElement")return Dx(e,n,t);if(n.type==="mdxjsEsm")return Mx(e,n);if(n.type==="root")return Bx(e,n,t);if(n.type==="text")return Lx(e,n)}function Tx(e,n,t){const r=e.schema;let a=r;n.tagName.toLowerCase()==="svg"&&r.space==="html"&&(a=Ec,e.schema=a),e.ancestors.push(n);const s=Gm(e,n.tagName,!1),i=Wx(e,n);let o=Rc(e,n);return Cx.has(n.tagName)&&(o=o.filter(function(l){return typeof l=="string"?!WS(l):!0})),Vm(e,i,s,n),Tc(i,o),e.ancestors.pop(),e.schema=r,e.create(n,s,i,t)}function Rx(e,n){if(n.data&&n.data.estree&&e.evaluater){const r=n.data.estree.body[0];return r.type,e.evaluater.evaluateExpression(r.expression)}Sa(e,n.position)}function Mx(e,n){if(n.data&&n.data.estree&&e.evaluater)return e.evaluater.evaluateProgram(n.data.estree);Sa(e,n.position)}function Dx(e,n,t){const r=e.schema;let a=r;n.name==="svg"&&r.space==="html"&&(a=Ec,e.schema=a),e.ancestors.push(n);const s=n.name===null?e.Fragment:Gm(e,n.name,!0),i=jx(e,n),o=Rc(e,n);return Vm(e,i,s,n),Tc(i,o),e.ancestors.pop(),e.schema=r,e.create(n,s,i,t)}function Bx(e,n,t){const r={};return Tc(r,Rc(e,n)),e.create(n,e.Fragment,r,t)}function Lx(e,n){return n.value}function Vm(e,n,t,r){typeof t!="string"&&t!==e.Fragment&&e.passNode&&(n.node=r)}function Tc(e,n){if(n.length>0){const t=n.length>1?n:n[0];t&&(e.children=t)}}function Nx(e,n,t){return r;function r(a,s,i,o){const c=Array.isArray(i.children)?t:n;return o?c(s,i,o):c(s,i)}}function Kx(e,n){return t;function t(r,a,s,i){const o=Array.isArray(s.children),l=Ac(r);return n(a,s,i,o,{columnNumber:l?l.column-1:void 0,fileName:e,lineNumber:l?l.line:void 0},void 0)}}function Wx(e,n){const t={};let r,a;for(a in n.properties)if(a!=="children"&&Ic.call(n.properties,a)){const s=Fx(e,a,n.properties[a]);if(s){const[i,o]=s;e.tableCellAlignToStyle&&i==="align"&&typeof o=="string"&&Ax.has(n.tagName)?r=o:t[i]=o}}if(r){const s=t.style||(t.style={});s[e.stylePropertyNameCase==="css"?"text-align":"textAlign"]=r}return t}function jx(e,n){const t={};for(const r of n.attributes)if(r.type==="mdxJsxExpressionAttribute")if(r.data&&r.data.estree&&e.evaluater){const s=r.data.estree.body[0];s.type;const i=s.expression;i.type;const o=i.properties[0];o.type,Object.assign(t,e.evaluater.evaluateExpression(o.argument))}else Sa(e,n.position);else{const a=r.name;let s;if(r.value&&typeof r.value=="object")if(r.value.data&&r.value.data.estree&&e.evaluater){const o=r.value.data.estree.body[0];o.type,s=e.evaluater.evaluateExpression(o.expression)}else Sa(e,n.position);else s=r.value===null?!0:r.value;t[a]=s}return t}function Rc(e,n){const t=[];let r=-1;const a=e.passKeys?new Map:_x;for(;++r<n.children.length;){const s=n.children[r];let i;if(e.passKeys){const l=s.type==="element"?s.tagName:s.type==="mdxJsxFlowElement"||s.type==="mdxJsxTextElement"?s.name:void 0;if(l){const c=a.get(l)||0;i=l+"-"+c,a.set(l,c+1)}}const o=qm(e,s,i);o!==void 0&&t.push(o)}return t}function Fx(e,n,t){const r=qS(e.schema,n);if(!(t==null||typeof t=="number"&&Number.isNaN(t))){if(Array.isArray(t)&&(t=r.commaSeparated?DS(t):QS(t)),r.property==="style"){let a=typeof t=="object"?t:Ux(e,String(t));return e.stylePropertyNameCase==="css"&&(a=zx(a)),["style",a]}return[e.elementAttributeNameCase==="react"&&r.space?zS[r.property]||r.property:r.attribute,t]}}function Ux(e,n){try{return xx(n,{reactCompat:!0})}catch(t){if(e.ignoreInvalidStyle)return{};const r=t,a=new Be("Cannot parse `style` attribute",{ancestors:e.ancestors,cause:r,ruleId:"style",source:"hast-util-to-jsx-runtime"});throw a.file=e.filePath||void 0,a.url=Hm+"#cannot-parse-style-attribute",a}}function Gm(e,n,t){let r;if(!t)r={type:"Literal",value:n};else if(n.includes(".")){const a=n.split(".");let s=-1,i;for(;++s<a.length;){const o=ud(a[s])?{type:"Identifier",name:a[s]}:{type:"Literal",value:a[s]};i=i?{type:"MemberExpression",object:i,property:o,computed:!!(s&&o.type==="Literal"),optional:!1}:o}r=i}else r=ud(n)&&!/^[a-z]/.test(n)?{type:"Identifier",name:n}:{type:"Literal",value:n};if(r.type==="Literal"){const a=r.value;return Ic.call(e.components,a)?e.components[a]:a}if(e.evaluater)return e.evaluater.evaluateExpression(r);Sa(e)}function Sa(e,n){const t=new Be("Cannot handle MDX estrees without `createEvaluater`",{ancestors:e.ancestors,place:n,ruleId:"mdx-estree",source:"hast-util-to-jsx-runtime"});throw t.file=e.filePath||void 0,t.url=Hm+"#cannot-handle-mdx-estrees-without-createevaluater",t}function zx(e){const n={};let t;for(t in e)Ic.call(e,t)&&(n[Ox(t)]=e[t]);return n}function Ox(e){let n=e.replace(Ex,Hx);return n.slice(0,3)==="ms-"&&(n="-"+n),n}function Hx(e){return"-"+e.toLowerCase()}const Xi={action:["form"],cite:["blockquote","del","ins","q"],data:["object"],formAction:["button","input"],href:["a","area","base","link"],icon:["menuitem"],itemId:null,manifest:["html"],ping:["a","area"],poster:["video"],src:["audio","embed","iframe","img","input","script","source","track","video"]},qx={};function Mc(e,n){const t=qx,r=typeof t.includeImageAlt=="boolean"?t.includeImageAlt:!0,a=typeof t.includeHtml=="boolean"?t.includeHtml:!0;return $m(e,r,a)}function $m(e,n,t){if(Vx(e)){if("value"in e)return e.type==="html"&&!t?"":e.value;if(n&&"alt"in e&&e.alt)return e.alt;if("children"in e)return bd(e.children,n,t)}return Array.isArray(e)?bd(e,n,t):""}function bd(e,n,t){const r=[];let a=-1;for(;++a<e.length;)r[a]=$m(e[a],n,t);return r.join("")}function Vx(e){return!!(e&&typeof e=="object")}const Sd=document.createElement("i");function Dc(e){const n="&"+e+";";Sd.innerHTML=n;const t=Sd.textContent;return t.charCodeAt(t.length-1)===59&&e!=="semi"||t===n?!1:t}function nn(e,n,t,r){const a=e.length;let s=0,i;if(n<0?n=-n>a?0:a+n:n=n>a?a:n,t=t>0?t:0,r.length<1e4)i=Array.from(r),i.unshift(n,t),e.splice(...i);else for(t&&e.splice(n,t);s<r.length;)i=r.slice(s,s+1e4),i.unshift(n,0),e.splice(...i),s+=1e4,n+=1e4}function un(e,n){return e.length>0?(nn(e,e.length,0,n),e):n}const xd={}.hasOwnProperty;function Qm(e){const n={};let t=-1;for(;++t<e.length;)Gx(n,e[t]);return n}function Gx(e,n){let t;for(t in n){const a=(xd.call(e,t)?e[t]:void 0)||(e[t]={}),s=n[t];let i;if(s)for(i in s){xd.call(a,i)||(a[i]=[]);const o=s[i];$x(a[i],Array.isArray(o)?o:o?[o]:[])}}}function $x(e,n){let t=-1;const r=[];for(;++t<n.length;)(n[t].add==="after"?e:r).push(n[t]);nn(e,0,0,r)}function Jm(e,n){const t=Number.parseInt(e,n);return t<9||t===11||t>13&&t<32||t>126&&t<160||t>55295&&t<57344||t>64975&&t<65008||(t&65535)===65535||(t&65535)===65534||t>1114111?"�":String.fromCodePoint(t)}function Sn(e){return e.replace(/[\t\n\r ]+/g," ").replace(/^ | $/g,"").toLowerCase().toUpperCase()}const Ke=yt(/[A-Za-z]/),Me=yt(/[\dA-Za-z]/),Qx=yt(/[#-'*+\--9=?A-Z^-~]/);function Hs(e){return e!==null&&(e<32||e===127)}const gl=yt(/\d/),Jx=yt(/[\dA-Fa-f]/),Yx=yt(/[!-/:-@[-`{-~]/);function U(e){return e!==null&&e<-2}function ae(e){return e!==null&&(e<0||e===32)}function $(e){return e===-2||e===-1||e===32}const mi=yt(new RegExp("\\p{P}|\\p{S}","u")),Bt=yt(/\s/);function yt(e){return n;function n(t){return t!==null&&t>-1&&e.test(String.fromCharCode(t))}}function Pr(e){const n=[];let t=-1,r=0,a=0;for(;++t<e.length;){const s=e.charCodeAt(t);let i="";if(s===37&&Me(e.charCodeAt(t+1))&&Me(e.charCodeAt(t+2)))a=2;else if(s<128)/[!#$&-;=?-Z_a-z~]/.test(String.fromCharCode(s))||(i=String.fromCharCode(s));else if(s>55295&&s<57344){const o=e.charCodeAt(t+1);s<56320&&o>56319&&o<57344?(i=String.fromCharCode(s,o),a=1):i="�"}else i=String.fromCharCode(s);i&&(n.push(e.slice(r,t),encodeURIComponent(i)),r=t+a+1,i=""),a&&(t+=a,a=0)}return n.join("")+e.slice(r)}function X(e,n,t,r){const a=r?r-1:Number.POSITIVE_INFINITY;let s=0;return i;function i(l){return $(l)?(e.enter(t),o(l)):n(l)}function o(l){return $(l)&&s++<a?(e.consume(l),o):(e.exit(t),n(l))}}const Xx={tokenize:Zx};function Zx(e){const n=e.attempt(this.parser.constructs.contentInitial,r,a);let t;return n;function r(o){if(o===null){e.consume(o);return}return e.enter("lineEnding"),e.consume(o),e.exit("lineEnding"),X(e,n,"linePrefix")}function a(o){return e.enter("paragraph"),s(o)}function s(o){const l=e.enter("chunkText",{contentType:"text",previous:t});return t&&(t.next=l),t=l,i(o)}function i(o){if(o===null){e.exit("chunkText"),e.exit("paragraph"),e.consume(o);return}return U(o)?(e.consume(o),e.exit("chunkText"),s):(e.consume(o),i)}}const e1={tokenize:n1},Pd={tokenize:t1};function n1(e){const n=this,t=[];let r=0,a,s,i;return o;function o(w){if(r<t.length){const _=t[r];return n.containerState=_[1],e.attempt(_[0].continuation,l,c)(w)}return c(w)}function l(w){if(r++,n.containerState._closeFlow){n.containerState._closeFlow=void 0,a&&y();const _=n.events.length;let E=_,S;for(;E--;)if(n.events[E][0]==="exit"&&n.events[E][1].type==="chunkFlow"){S=n.events[E][1].end;break}g(r);let A=_;for(;A<n.events.length;)n.events[A][1].end={...S},A++;return nn(n.events,E+1,0,n.events.slice(_)),n.events.length=A,c(w)}return o(w)}function c(w){if(r===t.length){if(!a)return p(w);if(a.currentConstruct&&a.currentConstruct.concrete)return f(w);n.interrupt=!!(a.currentConstruct&&!a._gfmTableDynamicInterruptHack)}return n.containerState={},e.check(Pd,d,u)(w)}function d(w){return a&&y(),g(r),p(w)}function u(w){return n.parser.lazy[n.now().line]=r!==t.length,i=n.now().offset,f(w)}function p(w){return n.containerState={},e.attempt(Pd,m,f)(w)}function m(w){return r++,t.push([n.currentConstruct,n.containerState]),p(w)}function f(w){if(w===null){a&&y(),g(0),e.consume(w);return}return a=a||n.parser.flow(n.now()),e.enter("chunkFlow",{_tokenizer:a,contentType:"flow",previous:s}),k(w)}function k(w){if(w===null){P(e.exit("chunkFlow"),!0),g(0),e.consume(w);return}return U(w)?(e.consume(w),P(e.exit("chunkFlow")),r=0,n.interrupt=void 0,o):(e.consume(w),k)}function P(w,_){const E=n.sliceStream(w);if(_&&E.push(null),w.previous=s,s&&(s.next=w),s=w,a.defineSkip(w.start),a.write(E),n.parser.lazy[w.start.line]){let S=a.events.length;for(;S--;)if(a.events[S][1].start.offset<i&&(!a.events[S][1].end||a.events[S][1].end.offset>i))return;const A=n.events.length;let M=A,j,x;for(;M--;)if(n.events[M][0]==="exit"&&n.events[M][1].type==="chunkFlow"){if(j){x=n.events[M][1].end;break}j=!0}for(g(r),S=A;S<n.events.length;)n.events[S][1].end={...x},S++;nn(n.events,M+1,0,n.events.slice(A)),n.events.length=S}}function g(w){let _=t.length;for(;_-- >w;){const E=t[_];n.containerState=E[1],E[0].exit.call(n,e)}t.length=w}function y(){a.write([null]),s=void 0,a=void 0,n.containerState._closeFlow=void 0}}function t1(e,n,t){return X(e,e.attempt(this.parser.constructs.document,n,t),"linePrefix",this.parser.constructs.disable.null.includes("codeIndented")?void 0:4)}function fr(e){if(e===null||ae(e)||Bt(e))return 1;if(mi(e))return 2}function gi(e,n,t){const r=[];let a=-1;for(;++a<e.length;){const s=e[a].resolveAll;s&&!r.includes(s)&&(n=s(n,t),r.push(s))}return n}const fl={name:"attention",resolveAll:r1,tokenize:a1};function r1(e,n){let t=-1,r,a,s,i,o,l,c,d;for(;++t<e.length;)if(e[t][0]==="enter"&&e[t][1].type==="attentionSequence"&&e[t][1]._close){for(r=t;r--;)if(e[r][0]==="exit"&&e[r][1].type==="attentionSequence"&&e[r][1]._open&&n.sliceSerialize(e[r][1]).charCodeAt(0)===n.sliceSerialize(e[t][1]).charCodeAt(0)){if((e[r][1]._close||e[t][1]._open)&&(e[t][1].end.offset-e[t][1].start.offset)%3&&!((e[r][1].end.offset-e[r][1].start.offset+e[t][1].end.offset-e[t][1].start.offset)%3))continue;l=e[r][1].end.offset-e[r][1].start.offset>1&&e[t][1].end.offset-e[t][1].start.offset>1?2:1;const u={...e[r][1].end},p={...e[t][1].start};_d(u,-l),_d(p,l),i={type:l>1?"strongSequence":"emphasisSequence",start:u,end:{...e[r][1].end}},o={type:l>1?"strongSequence":"emphasisSequence",start:{...e[t][1].start},end:p},s={type:l>1?"strongText":"emphasisText",start:{...e[r][1].end},end:{...e[t][1].start}},a={type:l>1?"strong":"emphasis",start:{...i.start},end:{...o.end}},e[r][1].end={...i.start},e[t][1].start={...o.end},c=[],e[r][1].end.offset-e[r][1].start.offset&&(c=un(c,[["enter",e[r][1],n],["exit",e[r][1],n]])),c=un(c,[["enter",a,n],["enter",i,n],["exit",i,n],["enter",s,n]]),c=un(c,gi(n.parser.constructs.insideSpan.null,e.slice(r+1,t),n)),c=un(c,[["exit",s,n],["enter",o,n],["exit",o,n],["exit",a,n]]),e[t][1].end.offset-e[t][1].start.offset?(d=2,c=un(c,[["enter",e[t][1],n],["exit",e[t][1],n]])):d=0,nn(e,r-1,t-r+3,c),t=r+c.length-d-2;break}}for(t=-1;++t<e.length;)e[t][1].type==="attentionSequence"&&(e[t][1].type="data");return e}function a1(e,n){const t=this.parser.constructs.attentionMarkers.null,r=this.previous,a=fr(r);let s;return i;function i(l){return s=l,e.enter("attentionSequence"),o(l)}function o(l){if(l===s)return e.consume(l),o;const c=e.exit("attentionSequence"),d=fr(l),u=!d||d===2&&a||t.includes(l),p=!a||a===2&&d||t.includes(r);return c._open=!!(s===42?u:u&&(a||!p)),c._close=!!(s===42?p:p&&(d||!u)),n(l)}}function _d(e,n){e.column+=n,e.offset+=n,e._bufferIndex+=n}const s1={name:"autolink",tokenize:i1};function i1(e,n,t){let r=0;return a;function a(m){return e.enter("autolink"),e.enter("autolinkMarker"),e.consume(m),e.exit("autolinkMarker"),e.enter("autolinkProtocol"),s}function s(m){return Ke(m)?(e.consume(m),i):m===64?t(m):c(m)}function i(m){return m===43||m===45||m===46||Me(m)?(r=1,o(m)):c(m)}function o(m){return m===58?(e.consume(m),r=0,l):(m===43||m===45||m===46||Me(m))&&r++<32?(e.consume(m),o):(r=0,c(m))}function l(m){return m===62?(e.exit("autolinkProtocol"),e.enter("autolinkMarker"),e.consume(m),e.exit("autolinkMarker"),e.exit("autolink"),n):m===null||m===32||m===60||Hs(m)?t(m):(e.consume(m),l)}function c(m){return m===64?(e.consume(m),d):Qx(m)?(e.consume(m),c):t(m)}function d(m){return Me(m)?u(m):t(m)}function u(m){return m===46?(e.consume(m),r=0,d):m===62?(e.exit("autolinkProtocol").type="autolinkEmail",e.enter("autolinkMarker"),e.consume(m),e.exit("autolinkMarker"),e.exit("autolink"),n):p(m)}function p(m){if((m===45||Me(m))&&r++<63){const f=m===45?p:u;return e.consume(m),f}return t(m)}}const Ra={partial:!0,tokenize:o1};function o1(e,n,t){return r;function r(s){return $(s)?X(e,a,"linePrefix")(s):a(s)}function a(s){return s===null||U(s)?n(s):t(s)}}const Ym={continuation:{tokenize:c1},exit:u1,name:"blockQuote",tokenize:l1};function l1(e,n,t){const r=this;return a;function a(i){if(i===62){const o=r.containerState;return o.open||(e.enter("blockQuote",{_container:!0}),o.open=!0),e.enter("blockQuotePrefix"),e.enter("blockQuoteMarker"),e.consume(i),e.exit("blockQuoteMarker"),s}return t(i)}function s(i){return $(i)?(e.enter("blockQuotePrefixWhitespace"),e.consume(i),e.exit("blockQuotePrefixWhitespace"),e.exit("blockQuotePrefix"),n):(e.exit("blockQuotePrefix"),n(i))}}function c1(e,n,t){const r=this;return a;function a(i){return $(i)?X(e,s,"linePrefix",r.parser.constructs.disable.null.includes("codeIndented")?void 0:4)(i):s(i)}function s(i){return e.attempt(Ym,n,t)(i)}}function u1(e){e.exit("blockQuote")}const Xm={name:"characterEscape",tokenize:d1};function d1(e,n,t){return r;function r(s){return e.enter("characterEscape"),e.enter("escapeMarker"),e.consume(s),e.exit("escapeMarker"),a}function a(s){return Yx(s)?(e.enter("characterEscapeValue"),e.consume(s),e.exit("characterEscapeValue"),e.exit("characterEscape"),n):t(s)}}const Zm={name:"characterReference",tokenize:h1};function h1(e,n,t){const r=this;let a=0,s,i;return o;function o(u){return e.enter("characterReference"),e.enter("characterReferenceMarker"),e.consume(u),e.exit("characterReferenceMarker"),l}function l(u){return u===35?(e.enter("characterReferenceMarkerNumeric"),e.consume(u),e.exit("characterReferenceMarkerNumeric"),c):(e.enter("characterReferenceValue"),s=31,i=Me,d(u))}function c(u){return u===88||u===120?(e.enter("characterReferenceMarkerHexadecimal"),e.consume(u),e.exit("characterReferenceMarkerHexadecimal"),e.enter("characterReferenceValue"),s=6,i=Jx,d):(e.enter("characterReferenceValue"),s=7,i=gl,d(u))}function d(u){if(u===59&&a){const p=e.exit("characterReferenceValue");return i===Me&&!Dc(r.sliceSerialize(p))?t(u):(e.enter("characterReferenceMarker"),e.consume(u),e.exit("characterReferenceMarker"),e.exit("characterReference"),n)}return i(u)&&a++<s?(e.consume(u),d):t(u)}}const Ed={partial:!0,tokenize:m1},Cd={concrete:!0,name:"codeFenced",tokenize:p1};function p1(e,n,t){const r=this,a={partial:!0,tokenize:E};let s=0,i=0,o;return l;function l(S){return c(S)}function c(S){const A=r.events[r.events.length-1];return s=A&&A[1].type==="linePrefix"?A[2].sliceSerialize(A[1],!0).length:0,o=S,e.enter("codeFenced"),e.enter("codeFencedFence"),e.enter("codeFencedFenceSequence"),d(S)}function d(S){return S===o?(i++,e.consume(S),d):i<3?t(S):(e.exit("codeFencedFenceSequence"),$(S)?X(e,u,"whitespace")(S):u(S))}function u(S){return S===null||U(S)?(e.exit("codeFencedFence"),r.interrupt?n(S):e.check(Ed,k,_)(S)):(e.enter("codeFencedFenceInfo"),e.enter("chunkString",{contentType:"string"}),p(S))}function p(S){return S===null||U(S)?(e.exit("chunkString"),e.exit("codeFencedFenceInfo"),u(S)):$(S)?(e.exit("chunkString"),e.exit("codeFencedFenceInfo"),X(e,m,"whitespace")(S)):S===96&&S===o?t(S):(e.consume(S),p)}function m(S){return S===null||U(S)?u(S):(e.enter("codeFencedFenceMeta"),e.enter("chunkString",{contentType:"string"}),f(S))}function f(S){return S===null||U(S)?(e.exit("chunkString"),e.exit("codeFencedFenceMeta"),u(S)):S===96&&S===o?t(S):(e.consume(S),f)}function k(S){return e.attempt(a,_,P)(S)}function P(S){return e.enter("lineEnding"),e.consume(S),e.exit("lineEnding"),g}function g(S){return s>0&&$(S)?X(e,y,"linePrefix",s+1)(S):y(S)}function y(S){return S===null||U(S)?e.check(Ed,k,_)(S):(e.enter("codeFlowValue"),w(S))}function w(S){return S===null||U(S)?(e.exit("codeFlowValue"),y(S)):(e.consume(S),w)}function _(S){return e.exit("codeFenced"),n(S)}function E(S,A,M){let j=0;return x;function x(G){return S.enter("lineEnding"),S.consume(G),S.exit("lineEnding"),L}function L(G){return S.enter("codeFencedFence"),$(G)?X(S,W,"linePrefix",r.parser.constructs.disable.null.includes("codeIndented")?void 0:4)(G):W(G)}function W(G){return G===o?(S.enter("codeFencedFenceSequence"),J(G)):M(G)}function J(G){return G===o?(j++,S.consume(G),J):j>=i?(S.exit("codeFencedFenceSequence"),$(G)?X(S,te,"whitespace")(G):te(G)):M(G)}function te(G){return G===null||U(G)?(S.exit("codeFencedFence"),A(G)):M(G)}}}function m1(e,n,t){const r=this;return a;function a(i){return i===null?t(i):(e.enter("lineEnding"),e.consume(i),e.exit("lineEnding"),s)}function s(i){return r.parser.lazy[r.now().line]?t(i):n(i)}}const Zi={name:"codeIndented",tokenize:f1},g1={partial:!0,tokenize:y1};function f1(e,n,t){const r=this;return a;function a(c){return e.enter("codeIndented"),X(e,s,"linePrefix",5)(c)}function s(c){const d=r.events[r.events.length-1];return d&&d[1].type==="linePrefix"&&d[2].sliceSerialize(d[1],!0).length>=4?i(c):t(c)}function i(c){return c===null?l(c):U(c)?e.attempt(g1,i,l)(c):(e.enter("codeFlowValue"),o(c))}function o(c){return c===null||U(c)?(e.exit("codeFlowValue"),i(c)):(e.consume(c),o)}function l(c){return e.exit("codeIndented"),n(c)}}function y1(e,n,t){const r=this;return a;function a(i){return r.parser.lazy[r.now().line]?t(i):U(i)?(e.enter("lineEnding"),e.consume(i),e.exit("lineEnding"),a):X(e,s,"linePrefix",5)(i)}function s(i){const o=r.events[r.events.length-1];return o&&o[1].type==="linePrefix"&&o[2].sliceSerialize(o[1],!0).length>=4?n(i):U(i)?a(i):t(i)}}const w1={name:"codeText",previous:k1,resolve:v1,tokenize:b1};function v1(e){let n=e.length-4,t=3,r,a;if((e[t][1].type==="lineEnding"||e[t][1].type==="space")&&(e[n][1].type==="lineEnding"||e[n][1].type==="space")){for(r=t;++r<n;)if(e[r][1].type==="codeTextData"){e[t][1].type="codeTextPadding",e[n][1].type="codeTextPadding",t+=2,n-=2;break}}for(r=t-1,n++;++r<=n;)a===void 0?r!==n&&e[r][1].type!=="lineEnding"&&(a=r):(r===n||e[r][1].type==="lineEnding")&&(e[a][1].type="codeTextData",r!==a+2&&(e[a][1].end=e[r-1][1].end,e.splice(a+2,r-a-2),n-=r-a-2,r=a+2),a=void 0);return e}function k1(e){return e!==96||this.events[this.events.length-1][1].type==="characterEscape"}function b1(e,n,t){let r=0,a,s;return i;function i(u){return e.enter("codeText"),e.enter("codeTextSequence"),o(u)}function o(u){return u===96?(e.consume(u),r++,o):(e.exit("codeTextSequence"),l(u))}function l(u){return u===null?t(u):u===32?(e.enter("space"),e.consume(u),e.exit("space"),l):u===96?(s=e.enter("codeTextSequence"),a=0,d(u)):U(u)?(e.enter("lineEnding"),e.consume(u),e.exit("lineEnding"),l):(e.enter("codeTextData"),c(u))}function c(u){return u===null||u===32||u===96||U(u)?(e.exit("codeTextData"),l(u)):(e.consume(u),c)}function d(u){return u===96?(e.consume(u),a++,d):a===r?(e.exit("codeTextSequence"),e.exit("codeText"),n(u)):(s.type="codeTextData",c(u))}}class S1{constructor(n){this.left=n?[...n]:[],this.right=[]}get(n){if(n<0||n>=this.left.length+this.right.length)throw new RangeError("Cannot access index `"+n+"` in a splice buffer of size `"+(this.left.length+this.right.length)+"`");return n<this.left.length?this.left[n]:this.right[this.right.length-n+this.left.length-1]}get length(){return this.left.length+this.right.length}shift(){return this.setCursor(0),this.right.pop()}slice(n,t){const r=t??Number.POSITIVE_INFINITY;return r<this.left.length?this.left.slice(n,r):n>this.left.length?this.right.slice(this.right.length-r+this.left.length,this.right.length-n+this.left.length).reverse():this.left.slice(n).concat(this.right.slice(this.right.length-r+this.left.length).reverse())}splice(n,t,r){const a=t||0;this.setCursor(Math.trunc(n));const s=this.right.splice(this.right.length-a,Number.POSITIVE_INFINITY);return r&&jr(this.left,r),s.reverse()}pop(){return this.setCursor(Number.POSITIVE_INFINITY),this.left.pop()}push(n){this.setCursor(Number.POSITIVE_INFINITY),this.left.push(n)}pushMany(n){this.setCursor(Number.POSITIVE_INFINITY),jr(this.left,n)}unshift(n){this.setCursor(0),this.right.push(n)}unshiftMany(n){this.setCursor(0),jr(this.right,n.reverse())}setCursor(n){if(!(n===this.left.length||n>this.left.length&&this.right.length===0||n<0&&this.left.length===0))if(n<this.left.length){const t=this.left.splice(n,Number.POSITIVE_INFINITY);jr(this.right,t.reverse())}else{const t=this.right.splice(this.left.length+this.right.length-n,Number.POSITIVE_INFINITY);jr(this.left,t.reverse())}}}function jr(e,n){let t=0;if(n.length<1e4)e.push(...n);else for(;t<n.length;)e.push(...n.slice(t,t+1e4)),t+=1e4}function eg(e){const n={};let t=-1,r,a,s,i,o,l,c;const d=new S1(e);for(;++t<d.length;){for(;t in n;)t=n[t];if(r=d.get(t),t&&r[1].type==="chunkFlow"&&d.get(t-1)[1].type==="listItemPrefix"&&(l=r[1]._tokenizer.events,s=0,s<l.length&&l[s][1].type==="lineEndingBlank"&&(s+=2),s<l.length&&l[s][1].type==="content"))for(;++s<l.length&&l[s][1].type!=="content";)l[s][1].type==="chunkText"&&(l[s][1]._isInFirstContentOfListItem=!0,s++);if(r[0]==="enter")r[1].contentType&&(Object.assign(n,x1(d,t)),t=n[t],c=!0);else if(r[1]._container){for(s=t,a=void 0;s--;)if(i=d.get(s),i[1].type==="lineEnding"||i[1].type==="lineEndingBlank")i[0]==="enter"&&(a&&(d.get(a)[1].type="lineEndingBlank"),i[1].type="lineEnding",a=s);else if(!(i[1].type==="linePrefix"||i[1].type==="listItemIndent"))break;a&&(r[1].end={...d.get(a)[1].start},o=d.slice(a,t),o.unshift(r),d.splice(a,t-a+1,o))}}return nn(e,0,Number.POSITIVE_INFINITY,d.slice(0)),!c}function x1(e,n){const t=e.get(n)[1],r=e.get(n)[2];let a=n-1;const s=[];let i=t._tokenizer;i||(i=r.parser[t.contentType](t.start),t._contentTypeTextTrailing&&(i._contentTypeTextTrailing=!0));const o=i.events,l=[],c={};let d,u,p=-1,m=t,f=0,k=0;const P=[k];for(;m;){for(;e.get(++a)[1]!==m;);s.push(a),m._tokenizer||(d=r.sliceStream(m),m.next||d.push(null),u&&i.defineSkip(m.start),m._isInFirstContentOfListItem&&(i._gfmTasklistFirstContentOfListItem=!0),i.write(d),m._isInFirstContentOfListItem&&(i._gfmTasklistFirstContentOfListItem=void 0)),u=m,m=m.next}for(m=t;++p<o.length;)o[p][0]==="exit"&&o[p-1][0]==="enter"&&o[p][1].type===o[p-1][1].type&&o[p][1].start.line!==o[p][1].end.line&&(k=p+1,P.push(k),m._tokenizer=void 0,m.previous=void 0,m=m.next);for(i.events=[],m?(m._tokenizer=void 0,m.previous=void 0):P.pop(),p=P.length;p--;){const g=o.slice(P[p],P[p+1]),y=s.pop();l.push([y,y+g.length-1]),e.splice(y,2,g)}for(l.reverse(),p=-1;++p<l.length;)c[f+l[p][0]]=f+l[p][1],f+=l[p][1]-l[p][0]-1;return c}const P1={resolve:E1,tokenize:C1},_1={partial:!0,tokenize:A1};function E1(e){return eg(e),e}function C1(e,n){let t;return r;function r(o){return e.enter("content"),t=e.enter("chunkContent",{contentType:"content"}),a(o)}function a(o){return o===null?s(o):U(o)?e.check(_1,i,s)(o):(e.consume(o),a)}function s(o){return e.exit("chunkContent"),e.exit("content"),n(o)}function i(o){return e.consume(o),e.exit("chunkContent"),t.next=e.enter("chunkContent",{contentType:"content",previous:t}),t=t.next,a}}function A1(e,n,t){const r=this;return a;function a(i){return e.exit("chunkContent"),e.enter("lineEnding"),e.consume(i),e.exit("lineEnding"),X(e,s,"linePrefix")}function s(i){if(i===null||U(i))return t(i);const o=r.events[r.events.length-1];return!r.parser.constructs.disable.null.includes("codeIndented")&&o&&o[1].type==="linePrefix"&&o[2].sliceSerialize(o[1],!0).length>=4?n(i):e.interrupt(r.parser.constructs.flow,t,n)(i)}}function ng(e,n,t,r,a,s,i,o,l){const c=l||Number.POSITIVE_INFINITY;let d=0;return u;function u(g){return g===60?(e.enter(r),e.enter(a),e.enter(s),e.consume(g),e.exit(s),p):g===null||g===32||g===41||Hs(g)?t(g):(e.enter(r),e.enter(i),e.enter(o),e.enter("chunkString",{contentType:"string"}),k(g))}function p(g){return g===62?(e.enter(s),e.consume(g),e.exit(s),e.exit(a),e.exit(r),n):(e.enter(o),e.enter("chunkString",{contentType:"string"}),m(g))}function m(g){return g===62?(e.exit("chunkString"),e.exit(o),p(g)):g===null||g===60||U(g)?t(g):(e.consume(g),g===92?f:m)}function f(g){return g===60||g===62||g===92?(e.consume(g),m):m(g)}function k(g){return!d&&(g===null||g===41||ae(g))?(e.exit("chunkString"),e.exit(o),e.exit(i),e.exit(r),n(g)):d<c&&g===40?(e.consume(g),d++,k):g===41?(e.consume(g),d--,k):g===null||g===32||g===40||Hs(g)?t(g):(e.consume(g),g===92?P:k)}function P(g){return g===40||g===41||g===92?(e.consume(g),k):k(g)}}function tg(e,n,t,r,a,s){const i=this;let o=0,l;return c;function c(m){return e.enter(r),e.enter(a),e.consume(m),e.exit(a),e.enter(s),d}function d(m){return o>999||m===null||m===91||m===93&&!l||m===94&&!o&&"_hiddenFootnoteSupport"in i.parser.constructs?t(m):m===93?(e.exit(s),e.enter(a),e.consume(m),e.exit(a),e.exit(r),n):U(m)?(e.enter("lineEnding"),e.consume(m),e.exit("lineEnding"),d):(e.enter("chunkString",{contentType:"string"}),u(m))}function u(m){return m===null||m===91||m===93||U(m)||o++>999?(e.exit("chunkString"),d(m)):(e.consume(m),l||(l=!$(m)),m===92?p:u)}function p(m){return m===91||m===92||m===93?(e.consume(m),o++,u):u(m)}}function rg(e,n,t,r,a,s){let i;return o;function o(p){return p===34||p===39||p===40?(e.enter(r),e.enter(a),e.consume(p),e.exit(a),i=p===40?41:p,l):t(p)}function l(p){return p===i?(e.enter(a),e.consume(p),e.exit(a),e.exit(r),n):(e.enter(s),c(p))}function c(p){return p===i?(e.exit(s),l(i)):p===null?t(p):U(p)?(e.enter("lineEnding"),e.consume(p),e.exit("lineEnding"),X(e,c,"linePrefix")):(e.enter("chunkString",{contentType:"string"}),d(p))}function d(p){return p===i||p===null||U(p)?(e.exit("chunkString"),c(p)):(e.consume(p),p===92?u:d)}function u(p){return p===i||p===92?(e.consume(p),d):d(p)}}function na(e,n){let t;return r;function r(a){return U(a)?(e.enter("lineEnding"),e.consume(a),e.exit("lineEnding"),t=!0,r):$(a)?X(e,r,t?"linePrefix":"lineSuffix")(a):n(a)}}const I1={name:"definition",tokenize:R1},T1={partial:!0,tokenize:M1};function R1(e,n,t){const r=this;let a;return s;function s(m){return e.enter("definition"),i(m)}function i(m){return tg.call(r,e,o,t,"definitionLabel","definitionLabelMarker","definitionLabelString")(m)}function o(m){return a=Sn(r.sliceSerialize(r.events[r.events.length-1][1]).slice(1,-1)),m===58?(e.enter("definitionMarker"),e.consume(m),e.exit("definitionMarker"),l):t(m)}function l(m){return ae(m)?na(e,c)(m):c(m)}function c(m){return ng(e,d,t,"definitionDestination","definitionDestinationLiteral","definitionDestinationLiteralMarker","definitionDestinationRaw","definitionDestinationString")(m)}function d(m){return e.attempt(T1,u,u)(m)}function u(m){return $(m)?X(e,p,"whitespace")(m):p(m)}function p(m){return m===null||U(m)?(e.exit("definition"),r.parser.defined.push(a),n(m)):t(m)}}function M1(e,n,t){return r;function r(o){return ae(o)?na(e,a)(o):t(o)}function a(o){return rg(e,s,t,"definitionTitle","definitionTitleMarker","definitionTitleString")(o)}function s(o){return $(o)?X(e,i,"whitespace")(o):i(o)}function i(o){return o===null||U(o)?n(o):t(o)}}const D1={name:"hardBreakEscape",tokenize:B1};function B1(e,n,t){return r;function r(s){return e.enter("hardBreakEscape"),e.consume(s),a}function a(s){return U(s)?(e.exit("hardBreakEscape"),n(s)):t(s)}}const L1={name:"headingAtx",resolve:N1,tokenize:K1};function N1(e,n){let t=e.length-2,r=3,a,s;return e[r][1].type==="whitespace"&&(r+=2),t-2>r&&e[t][1].type==="whitespace"&&(t-=2),e[t][1].type==="atxHeadingSequence"&&(r===t-1||t-4>r&&e[t-2][1].type==="whitespace")&&(t-=r+1===t?2:4),t>r&&(a={type:"atxHeadingText",start:e[r][1].start,end:e[t][1].end},s={type:"chunkText",start:e[r][1].start,end:e[t][1].end,contentType:"text"},nn(e,r,t-r+1,[["enter",a,n],["enter",s,n],["exit",s,n],["exit",a,n]])),e}function K1(e,n,t){let r=0;return a;function a(d){return e.enter("atxHeading"),s(d)}function s(d){return e.enter("atxHeadingSequence"),i(d)}function i(d){return d===35&&r++<6?(e.consume(d),i):d===null||ae(d)?(e.exit("atxHeadingSequence"),o(d)):t(d)}function o(d){return d===35?(e.enter("atxHeadingSequence"),l(d)):d===null||U(d)?(e.exit("atxHeading"),n(d)):$(d)?X(e,o,"whitespace")(d):(e.enter("atxHeadingText"),c(d))}function l(d){return d===35?(e.consume(d),l):(e.exit("atxHeadingSequence"),o(d))}function c(d){return d===null||d===35||ae(d)?(e.exit("atxHeadingText"),o(d)):(e.consume(d),c)}}const W1=["address","article","aside","base","basefont","blockquote","body","caption","center","col","colgroup","dd","details","dialog","dir","div","dl","dt","fieldset","figcaption","figure","footer","form","frame","frameset","h1","h2","h3","h4","h5","h6","head","header","hr","html","iframe","legend","li","link","main","menu","menuitem","nav","noframes","ol","optgroup","option","p","param","search","section","summary","table","tbody","td","tfoot","th","thead","title","tr","track","ul"],Ad=["pre","script","style","textarea"],j1={concrete:!0,name:"htmlFlow",resolveTo:z1,tokenize:O1},F1={partial:!0,tokenize:q1},U1={partial:!0,tokenize:H1};function z1(e){let n=e.length;for(;n--&&!(e[n][0]==="enter"&&e[n][1].type==="htmlFlow"););return n>1&&e[n-2][1].type==="linePrefix"&&(e[n][1].start=e[n-2][1].start,e[n+1][1].start=e[n-2][1].start,e.splice(n-2,2)),e}function O1(e,n,t){const r=this;let a,s,i,o,l;return c;function c(b){return d(b)}function d(b){return e.enter("htmlFlow"),e.enter("htmlFlowData"),e.consume(b),u}function u(b){return b===33?(e.consume(b),p):b===47?(e.consume(b),s=!0,k):b===63?(e.consume(b),a=3,r.interrupt?n:v):Ke(b)?(e.consume(b),i=String.fromCharCode(b),P):t(b)}function p(b){return b===45?(e.consume(b),a=2,m):b===91?(e.consume(b),a=5,o=0,f):Ke(b)?(e.consume(b),a=4,r.interrupt?n:v):t(b)}function m(b){return b===45?(e.consume(b),r.interrupt?n:v):t(b)}function f(b){const H="CDATA[";return b===H.charCodeAt(o++)?(e.consume(b),o===H.length?r.interrupt?n:W:f):t(b)}function k(b){return Ke(b)?(e.consume(b),i=String.fromCharCode(b),P):t(b)}function P(b){if(b===null||b===47||b===62||ae(b)){const H=b===47,Se=i.toLowerCase();return!H&&!s&&Ad.includes(Se)?(a=1,r.interrupt?n(b):W(b)):W1.includes(i.toLowerCase())?(a=6,H?(e.consume(b),g):r.interrupt?n(b):W(b)):(a=7,r.interrupt&&!r.parser.lazy[r.now().line]?t(b):s?y(b):w(b))}return b===45||Me(b)?(e.consume(b),i+=String.fromCharCode(b),P):t(b)}function g(b){return b===62?(e.consume(b),r.interrupt?n:W):t(b)}function y(b){return $(b)?(e.consume(b),y):x(b)}function w(b){return b===47?(e.consume(b),x):b===58||b===95||Ke(b)?(e.consume(b),_):$(b)?(e.consume(b),w):x(b)}function _(b){return b===45||b===46||b===58||b===95||Me(b)?(e.consume(b),_):E(b)}function E(b){return b===61?(e.consume(b),S):$(b)?(e.consume(b),E):w(b)}function S(b){return b===null||b===60||b===61||b===62||b===96?t(b):b===34||b===39?(e.consume(b),l=b,A):$(b)?(e.consume(b),S):M(b)}function A(b){return b===l?(e.consume(b),l=null,j):b===null||U(b)?t(b):(e.consume(b),A)}function M(b){return b===null||b===34||b===39||b===47||b===60||b===61||b===62||b===96||ae(b)?E(b):(e.consume(b),M)}function j(b){return b===47||b===62||$(b)?w(b):t(b)}function x(b){return b===62?(e.consume(b),L):t(b)}function L(b){return b===null||U(b)?W(b):$(b)?(e.consume(b),L):t(b)}function W(b){return b===45&&a===2?(e.consume(b),de):b===60&&a===1?(e.consume(b),oe):b===62&&a===4?(e.consume(b),N):b===63&&a===3?(e.consume(b),v):b===93&&a===5?(e.consume(b),F):U(b)&&(a===6||a===7)?(e.exit("htmlFlowData"),e.check(F1,O,J)(b)):b===null||U(b)?(e.exit("htmlFlowData"),J(b)):(e.consume(b),W)}function J(b){return e.check(U1,te,O)(b)}function te(b){return e.enter("lineEnding"),e.consume(b),e.exit("lineEnding"),G}function G(b){return b===null||U(b)?J(b):(e.enter("htmlFlowData"),W(b))}function de(b){return b===45?(e.consume(b),v):W(b)}function oe(b){return b===47?(e.consume(b),i="",B):W(b)}function B(b){if(b===62){const H=i.toLowerCase();return Ad.includes(H)?(e.consume(b),N):W(b)}return Ke(b)&&i.length<8?(e.consume(b),i+=String.fromCharCode(b),B):W(b)}function F(b){return b===93?(e.consume(b),v):W(b)}function v(b){return b===62?(e.consume(b),N):b===45&&a===2?(e.consume(b),v):W(b)}function N(b){return b===null||U(b)?(e.exit("htmlFlowData"),O(b)):(e.consume(b),N)}function O(b){return e.exit("htmlFlow"),n(b)}}function H1(e,n,t){const r=this;return a;function a(i){return U(i)?(e.enter("lineEnding"),e.consume(i),e.exit("lineEnding"),s):t(i)}function s(i){return r.parser.lazy[r.now().line]?t(i):n(i)}}function q1(e,n,t){return r;function r(a){return e.enter("lineEnding"),e.consume(a),e.exit("lineEnding"),e.attempt(Ra,n,t)}}const V1={name:"htmlText",tokenize:G1};function G1(e,n,t){const r=this;let a,s,i;return o;function o(v){return e.enter("htmlText"),e.enter("htmlTextData"),e.consume(v),l}function l(v){return v===33?(e.consume(v),c):v===47?(e.consume(v),E):v===63?(e.consume(v),w):Ke(v)?(e.consume(v),M):t(v)}function c(v){return v===45?(e.consume(v),d):v===91?(e.consume(v),s=0,f):Ke(v)?(e.consume(v),y):t(v)}function d(v){return v===45?(e.consume(v),m):t(v)}function u(v){return v===null?t(v):v===45?(e.consume(v),p):U(v)?(i=u,oe(v)):(e.consume(v),u)}function p(v){return v===45?(e.consume(v),m):u(v)}function m(v){return v===62?de(v):v===45?p(v):u(v)}function f(v){const N="CDATA[";return v===N.charCodeAt(s++)?(e.consume(v),s===N.length?k:f):t(v)}function k(v){return v===null?t(v):v===93?(e.consume(v),P):U(v)?(i=k,oe(v)):(e.consume(v),k)}function P(v){return v===93?(e.consume(v),g):k(v)}function g(v){return v===62?de(v):v===93?(e.consume(v),g):k(v)}function y(v){return v===null||v===62?de(v):U(v)?(i=y,oe(v)):(e.consume(v),y)}function w(v){return v===null?t(v):v===63?(e.consume(v),_):U(v)?(i=w,oe(v)):(e.consume(v),w)}function _(v){return v===62?de(v):w(v)}function E(v){return Ke(v)?(e.consume(v),S):t(v)}function S(v){return v===45||Me(v)?(e.consume(v),S):A(v)}function A(v){return U(v)?(i=A,oe(v)):$(v)?(e.consume(v),A):de(v)}function M(v){return v===45||Me(v)?(e.consume(v),M):v===47||v===62||ae(v)?j(v):t(v)}function j(v){return v===47?(e.consume(v),de):v===58||v===95||Ke(v)?(e.consume(v),x):U(v)?(i=j,oe(v)):$(v)?(e.consume(v),j):de(v)}function x(v){return v===45||v===46||v===58||v===95||Me(v)?(e.consume(v),x):L(v)}function L(v){return v===61?(e.consume(v),W):U(v)?(i=L,oe(v)):$(v)?(e.consume(v),L):j(v)}function W(v){return v===null||v===60||v===61||v===62||v===96?t(v):v===34||v===39?(e.consume(v),a=v,J):U(v)?(i=W,oe(v)):$(v)?(e.consume(v),W):(e.consume(v),te)}function J(v){return v===a?(e.consume(v),a=void 0,G):v===null?t(v):U(v)?(i=J,oe(v)):(e.consume(v),J)}function te(v){return v===null||v===34||v===39||v===60||v===61||v===96?t(v):v===47||v===62||ae(v)?j(v):(e.consume(v),te)}function G(v){return v===47||v===62||ae(v)?j(v):t(v)}function de(v){return v===62?(e.consume(v),e.exit("htmlTextData"),e.exit("htmlText"),n):t(v)}function oe(v){return e.exit("htmlTextData"),e.enter("lineEnding"),e.consume(v),e.exit("lineEnding"),B}function B(v){return $(v)?X(e,F,"linePrefix",r.parser.constructs.disable.null.includes("codeIndented")?void 0:4)(v):F(v)}function F(v){return e.enter("htmlTextData"),i(v)}}const Bc={name:"labelEnd",resolveAll:Y1,resolveTo:X1,tokenize:Z1},$1={tokenize:e0},Q1={tokenize:n0},J1={tokenize:t0};function Y1(e){let n=-1;const t=[];for(;++n<e.length;){const r=e[n][1];if(t.push(e[n]),r.type==="labelImage"||r.type==="labelLink"||r.type==="labelEnd"){const a=r.type==="labelImage"?4:2;r.type="data",n+=a}}return e.length!==t.length&&nn(e,0,e.length,t),e}function X1(e,n){let t=e.length,r=0,a,s,i,o;for(;t--;)if(a=e[t][1],s){if(a.type==="link"||a.type==="labelLink"&&a._inactive)break;e[t][0]==="enter"&&a.type==="labelLink"&&(a._inactive=!0)}else if(i){if(e[t][0]==="enter"&&(a.type==="labelImage"||a.type==="labelLink")&&!a._balanced&&(s=t,a.type!=="labelLink")){r=2;break}}else a.type==="labelEnd"&&(i=t);const l={type:e[s][1].type==="labelLink"?"link":"image",start:{...e[s][1].start},end:{...e[e.length-1][1].end}},c={type:"label",start:{...e[s][1].start},end:{...e[i][1].end}},d={type:"labelText",start:{...e[s+r+2][1].end},end:{...e[i-2][1].start}};return o=[["enter",l,n],["enter",c,n]],o=un(o,e.slice(s+1,s+r+3)),o=un(o,[["enter",d,n]]),o=un(o,gi(n.parser.constructs.insideSpan.null,e.slice(s+r+4,i-3),n)),o=un(o,[["exit",d,n],e[i-2],e[i-1],["exit",c,n]]),o=un(o,e.slice(i+1)),o=un(o,[["exit",l,n]]),nn(e,s,e.length,o),e}function Z1(e,n,t){const r=this;let a=r.events.length,s,i;for(;a--;)if((r.events[a][1].type==="labelImage"||r.events[a][1].type==="labelLink")&&!r.events[a][1]._balanced){s=r.events[a][1];break}return o;function o(p){return s?s._inactive?u(p):(i=r.parser.defined.includes(Sn(r.sliceSerialize({start:s.end,end:r.now()}))),e.enter("labelEnd"),e.enter("labelMarker"),e.consume(p),e.exit("labelMarker"),e.exit("labelEnd"),l):t(p)}function l(p){return p===40?e.attempt($1,d,i?d:u)(p):p===91?e.attempt(Q1,d,i?c:u)(p):i?d(p):u(p)}function c(p){return e.attempt(J1,d,u)(p)}function d(p){return n(p)}function u(p){return s._balanced=!0,t(p)}}function e0(e,n,t){return r;function r(u){return e.enter("resource"),e.enter("resourceMarker"),e.consume(u),e.exit("resourceMarker"),a}function a(u){return ae(u)?na(e,s)(u):s(u)}function s(u){return u===41?d(u):ng(e,i,o,"resourceDestination","resourceDestinationLiteral","resourceDestinationLiteralMarker","resourceDestinationRaw","resourceDestinationString",32)(u)}function i(u){return ae(u)?na(e,l)(u):d(u)}function o(u){return t(u)}function l(u){return u===34||u===39||u===40?rg(e,c,t,"resourceTitle","resourceTitleMarker","resourceTitleString")(u):d(u)}function c(u){return ae(u)?na(e,d)(u):d(u)}function d(u){return u===41?(e.enter("resourceMarker"),e.consume(u),e.exit("resourceMarker"),e.exit("resource"),n):t(u)}}function n0(e,n,t){const r=this;return a;function a(o){return tg.call(r,e,s,i,"reference","referenceMarker","referenceString")(o)}function s(o){return r.parser.defined.includes(Sn(r.sliceSerialize(r.events[r.events.length-1][1]).slice(1,-1)))?n(o):t(o)}function i(o){return t(o)}}function t0(e,n,t){return r;function r(s){return e.enter("reference"),e.enter("referenceMarker"),e.consume(s),e.exit("referenceMarker"),a}function a(s){return s===93?(e.enter("referenceMarker"),e.consume(s),e.exit("referenceMarker"),e.exit("reference"),n):t(s)}}const r0={name:"labelStartImage",resolveAll:Bc.resolveAll,tokenize:a0};function a0(e,n,t){const r=this;return a;function a(o){return e.enter("labelImage"),e.enter("labelImageMarker"),e.consume(o),e.exit("labelImageMarker"),s}function s(o){return o===91?(e.enter("labelMarker"),e.consume(o),e.exit("labelMarker"),e.exit("labelImage"),i):t(o)}function i(o){return o===94&&"_hiddenFootnoteSupport"in r.parser.constructs?t(o):n(o)}}const s0={name:"labelStartLink",resolveAll:Bc.resolveAll,tokenize:i0};function i0(e,n,t){const r=this;return a;function a(i){return e.enter("labelLink"),e.enter("labelMarker"),e.consume(i),e.exit("labelMarker"),e.exit("labelLink"),s}function s(i){return i===94&&"_hiddenFootnoteSupport"in r.parser.constructs?t(i):n(i)}}const eo={name:"lineEnding",tokenize:o0};function o0(e,n){return t;function t(r){return e.enter("lineEnding"),e.consume(r),e.exit("lineEnding"),X(e,n,"linePrefix")}}const fs={name:"thematicBreak",tokenize:l0};function l0(e,n,t){let r=0,a;return s;function s(c){return e.enter("thematicBreak"),i(c)}function i(c){return a=c,o(c)}function o(c){return c===a?(e.enter("thematicBreakSequence"),l(c)):r>=3&&(c===null||U(c))?(e.exit("thematicBreak"),n(c)):t(c)}function l(c){return c===a?(e.consume(c),r++,l):(e.exit("thematicBreakSequence"),$(c)?X(e,o,"whitespace")(c):o(c))}}const Ue={continuation:{tokenize:h0},exit:m0,name:"list",tokenize:d0},c0={partial:!0,tokenize:g0},u0={partial:!0,tokenize:p0};function d0(e,n,t){const r=this,a=r.events[r.events.length-1];let s=a&&a[1].type==="linePrefix"?a[2].sliceSerialize(a[1],!0).length:0,i=0;return o;function o(m){const f=r.containerState.type||(m===42||m===43||m===45?"listUnordered":"listOrdered");if(f==="listUnordered"?!r.containerState.marker||m===r.containerState.marker:gl(m)){if(r.containerState.type||(r.containerState.type=f,e.enter(f,{_container:!0})),f==="listUnordered")return e.enter("listItemPrefix"),m===42||m===45?e.check(fs,t,c)(m):c(m);if(!r.interrupt||m===49)return e.enter("listItemPrefix"),e.enter("listItemValue"),l(m)}return t(m)}function l(m){return gl(m)&&++i<10?(e.consume(m),l):(!r.interrupt||i<2)&&(r.containerState.marker?m===r.containerState.marker:m===41||m===46)?(e.exit("listItemValue"),c(m)):t(m)}function c(m){return e.enter("listItemMarker"),e.consume(m),e.exit("listItemMarker"),r.containerState.marker=r.containerState.marker||m,e.check(Ra,r.interrupt?t:d,e.attempt(c0,p,u))}function d(m){return r.containerState.initialBlankLine=!0,s++,p(m)}function u(m){return $(m)?(e.enter("listItemPrefixWhitespace"),e.consume(m),e.exit("listItemPrefixWhitespace"),p):t(m)}function p(m){return r.containerState.size=s+r.sliceSerialize(e.exit("listItemPrefix"),!0).length,n(m)}}function h0(e,n,t){const r=this;return r.containerState._closeFlow=void 0,e.check(Ra,a,s);function a(o){return r.containerState.furtherBlankLines=r.containerState.furtherBlankLines||r.containerState.initialBlankLine,X(e,n,"listItemIndent",r.containerState.size+1)(o)}function s(o){return r.containerState.furtherBlankLines||!$(o)?(r.containerState.furtherBlankLines=void 0,r.containerState.initialBlankLine=void 0,i(o)):(r.containerState.furtherBlankLines=void 0,r.containerState.initialBlankLine=void 0,e.attempt(u0,n,i)(o))}function i(o){return r.containerState._closeFlow=!0,r.interrupt=void 0,X(e,e.attempt(Ue,n,t),"linePrefix",r.parser.constructs.disable.null.includes("codeIndented")?void 0:4)(o)}}function p0(e,n,t){const r=this;return X(e,a,"listItemIndent",r.containerState.size+1);function a(s){const i=r.events[r.events.length-1];return i&&i[1].type==="listItemIndent"&&i[2].sliceSerialize(i[1],!0).length===r.containerState.size?n(s):t(s)}}function m0(e){e.exit(this.containerState.type)}function g0(e,n,t){const r=this;return X(e,a,"listItemPrefixWhitespace",r.parser.constructs.disable.null.includes("codeIndented")?void 0:5);function a(s){const i=r.events[r.events.length-1];return!$(s)&&i&&i[1].type==="listItemPrefixWhitespace"?n(s):t(s)}}const Id={name:"setextUnderline",resolveTo:f0,tokenize:y0};function f0(e,n){let t=e.length,r,a,s;for(;t--;)if(e[t][0]==="enter"){if(e[t][1].type==="content"){r=t;break}e[t][1].type==="paragraph"&&(a=t)}else e[t][1].type==="content"&&e.splice(t,1),!s&&e[t][1].type==="definition"&&(s=t);const i={type:"setextHeading",start:{...e[r][1].start},end:{...e[e.length-1][1].end}};return e[a][1].type="setextHeadingText",s?(e.splice(a,0,["enter",i,n]),e.splice(s+1,0,["exit",e[r][1],n]),e[r][1].end={...e[s][1].end}):e[r][1]=i,e.push(["exit",i,n]),e}function y0(e,n,t){const r=this;let a;return s;function s(c){let d=r.events.length,u;for(;d--;)if(r.events[d][1].type!=="lineEnding"&&r.events[d][1].type!=="linePrefix"&&r.events[d][1].type!=="content"){u=r.events[d][1].type==="paragraph";break}return!r.parser.lazy[r.now().line]&&(r.interrupt||u)?(e.enter("setextHeadingLine"),a=c,i(c)):t(c)}function i(c){return e.enter("setextHeadingLineSequence"),o(c)}function o(c){return c===a?(e.consume(c),o):(e.exit("setextHeadingLineSequence"),$(c)?X(e,l,"lineSuffix")(c):l(c))}function l(c){return c===null||U(c)?(e.exit("setextHeadingLine"),n(c)):t(c)}}const w0={tokenize:v0};function v0(e){const n=this,t=e.attempt(Ra,r,e.attempt(this.parser.constructs.flowInitial,a,X(e,e.attempt(this.parser.constructs.flow,a,e.attempt(P1,a)),"linePrefix")));return t;function r(s){if(s===null){e.consume(s);return}return e.enter("lineEndingBlank"),e.consume(s),e.exit("lineEndingBlank"),n.currentConstruct=void 0,t}function a(s){if(s===null){e.consume(s);return}return e.enter("lineEnding"),e.consume(s),e.exit("lineEnding"),n.currentConstruct=void 0,t}}const k0={resolveAll:sg()},b0=ag("string"),S0=ag("text");function ag(e){return{resolveAll:sg(e==="text"?x0:void 0),tokenize:n};function n(t){const r=this,a=this.parser.constructs[e],s=t.attempt(a,i,o);return i;function i(d){return c(d)?s(d):o(d)}function o(d){if(d===null){t.consume(d);return}return t.enter("data"),t.consume(d),l}function l(d){return c(d)?(t.exit("data"),s(d)):(t.consume(d),l)}function c(d){if(d===null)return!0;const u=a[d];let p=-1;if(u)for(;++p<u.length;){const m=u[p];if(!m.previous||m.previous.call(r,r.previous))return!0}return!1}}}function sg(e){return n;function n(t,r){let a=-1,s;for(;++a<=t.length;)s===void 0?t[a]&&t[a][1].type==="data"&&(s=a,a++):(!t[a]||t[a][1].type!=="data")&&(a!==s+2&&(t[s][1].end=t[a-1][1].end,t.splice(s+2,a-s-2),a=s+2),s=void 0);return e?e(t,r):t}}function x0(e,n){let t=0;for(;++t<=e.length;)if((t===e.length||e[t][1].type==="lineEnding")&&e[t-1][1].type==="data"){const r=e[t-1][1],a=n.sliceStream(r);let s=a.length,i=-1,o=0,l;for(;s--;){const c=a[s];if(typeof c=="string"){for(i=c.length;c.charCodeAt(i-1)===32;)o++,i--;if(i)break;i=-1}else if(c===-2)l=!0,o++;else if(c!==-1){s++;break}}if(n._contentTypeTextTrailing&&t===e.length&&(o=0),o){const c={type:t===e.length||l||o<2?"lineSuffix":"hardBreakTrailing",start:{_bufferIndex:s?i:r.start._bufferIndex+i,_index:r.start._index+s,line:r.end.line,column:r.end.column-o,offset:r.end.offset-o},end:{...r.end}};r.end={...c.start},r.start.offset===r.end.offset?Object.assign(r,c):(e.splice(t,0,["enter",c,n],["exit",c,n]),t+=2)}t++}return e}const P0={42:Ue,43:Ue,45:Ue,48:Ue,49:Ue,50:Ue,51:Ue,52:Ue,53:Ue,54:Ue,55:Ue,56:Ue,57:Ue,62:Ym},_0={91:I1},E0={[-2]:Zi,[-1]:Zi,32:Zi},C0={35:L1,42:fs,45:[Id,fs],60:j1,61:Id,95:fs,96:Cd,126:Cd},A0={38:Zm,92:Xm},I0={[-5]:eo,[-4]:eo,[-3]:eo,33:r0,38:Zm,42:fl,60:[s1,V1],91:s0,92:[D1,Xm],93:Bc,95:fl,96:w1},T0={null:[fl,k0]},R0={null:[42,95]},M0={null:[]},D0=Object.freeze(Object.defineProperty({__proto__:null,attentionMarkers:R0,contentInitial:_0,disable:M0,document:P0,flow:C0,flowInitial:E0,insideSpan:T0,string:A0,text:I0},Symbol.toStringTag,{value:"Module"}));function B0(e,n,t){let r={_bufferIndex:-1,_index:0,line:t&&t.line||1,column:t&&t.column||1,offset:t&&t.offset||0};const a={},s=[];let i=[],o=[];const l={attempt:A(E),check:A(S),consume:y,enter:w,exit:_,interrupt:A(S,{interrupt:!0})},c={code:null,containerState:{},defineSkip:k,events:[],now:f,parser:e,previous:null,sliceSerialize:p,sliceStream:m,write:u};let d=n.tokenize.call(c,l);return n.resolveAll&&s.push(n),c;function u(L){return i=un(i,L),P(),i[i.length-1]!==null?[]:(M(n,0),c.events=gi(s,c.events,c),c.events)}function p(L,W){return N0(m(L),W)}function m(L){return L0(i,L)}function f(){const{_bufferIndex:L,_index:W,line:J,column:te,offset:G}=r;return{_bufferIndex:L,_index:W,line:J,column:te,offset:G}}function k(L){a[L.line]=L.column,x()}function P(){let L;for(;r._index<i.length;){const W=i[r._index];if(typeof W=="string")for(L=r._index,r._bufferIndex<0&&(r._bufferIndex=0);r._index===L&&r._bufferIndex<W.length;)g(W.charCodeAt(r._bufferIndex));else g(W)}}function g(L){d=d(L)}function y(L){U(L)?(r.line++,r.column=1,r.offset+=L===-3?2:1,x()):L!==-1&&(r.column++,r.offset++),r._bufferIndex<0?r._index++:(r._bufferIndex++,r._bufferIndex===i[r._index].length&&(r._bufferIndex=-1,r._index++)),c.previous=L}function w(L,W){const J=W||{};return J.type=L,J.start=f(),c.events.push(["enter",J,c]),o.push(J),J}function _(L){const W=o.pop();return W.end=f(),c.events.push(["exit",W,c]),W}function E(L,W){M(L,W.from)}function S(L,W){W.restore()}function A(L,W){return J;function J(te,G,de){let oe,B,F,v;return Array.isArray(te)?O(te):"tokenize"in te?O([te]):N(te);function N(ee){return Ae;function Ae($e){const sn=$e!==null&&ee[$e],on=$e!==null&&ee.null,wt=[...Array.isArray(sn)?sn:sn?[sn]:[],...Array.isArray(on)?on:on?[on]:[]];return O(wt)($e)}}function O(ee){return oe=ee,B=0,ee.length===0?de:b(ee[B])}function b(ee){return Ae;function Ae($e){return v=j(),F=ee,ee.partial||(c.currentConstruct=ee),ee.name&&c.parser.constructs.disable.null.includes(ee.name)?Se():ee.tokenize.call(W?Object.assign(Object.create(c),W):c,l,H,Se)($e)}}function H(ee){return L(F,v),G}function Se(ee){return v.restore(),++B<oe.length?b(oe[B]):de}}}function M(L,W){L.resolveAll&&!s.includes(L)&&s.push(L),L.resolve&&nn(c.events,W,c.events.length-W,L.resolve(c.events.slice(W),c)),L.resolveTo&&(c.events=L.resolveTo(c.events,c))}function j(){const L=f(),W=c.previous,J=c.currentConstruct,te=c.events.length,G=Array.from(o);return{from:te,restore:de};function de(){r=L,c.previous=W,c.currentConstruct=J,c.events.length=te,o=G,x()}}function x(){r.line in a&&r.column<2&&(r.column=a[r.line],r.offset+=a[r.line]-1)}}function L0(e,n){const t=n.start._index,r=n.start._bufferIndex,a=n.end._index,s=n.end._bufferIndex;let i;if(t===a)i=[e[t].slice(r,s)];else{if(i=e.slice(t,a),r>-1){const o=i[0];typeof o=="string"?i[0]=o.slice(r):i.shift()}s>0&&i.push(e[a].slice(0,s))}return i}function N0(e,n){let t=-1;const r=[];let a;for(;++t<e.length;){const s=e[t];let i;if(typeof s=="string")i=s;else switch(s){case-5:{i="\r";break}case-4:{i=`
`;break}case-3:{i=`\r
`;break}case-2:{i=n?" ":"	";break}case-1:{if(!n&&a)continue;i=" ";break}default:i=String.fromCharCode(s)}a=s===-2,r.push(i)}return r.join("")}function K0(e){const r={constructs:Qm([D0,...(e||{}).extensions||[]]),content:a(Xx),defined:[],document:a(e1),flow:a(w0),lazy:{},string:a(b0),text:a(S0)};return r;function a(s){return i;function i(o){return B0(r,s,o)}}}function W0(e){for(;!eg(e););return e}const Td=/[\0\t\n\r]/g;function j0(){let e=1,n="",t=!0,r;return a;function a(s,i,o){const l=[];let c,d,u,p,m;for(s=n+(typeof s=="string"?s.toString():new TextDecoder(i||void 0).decode(s)),u=0,n="",t&&(s.charCodeAt(0)===65279&&u++,t=void 0);u<s.length;){if(Td.lastIndex=u,c=Td.exec(s),p=c&&c.index!==void 0?c.index:s.length,m=s.charCodeAt(p),!c){n=s.slice(u);break}if(m===10&&u===p&&r)l.push(-3),r=void 0;else switch(r&&(l.push(-5),r=void 0),u<p&&(l.push(s.slice(u,p)),e+=p-u),m){case 0:{l.push(65533),e++;break}case 9:{for(d=Math.ceil(e/4)*4,l.push(-2);e++<d;)l.push(-1);break}case 10:{l.push(-4),e=1;break}default:r=!0,e=1}u=p+1}return o&&(r&&l.push(-5),n&&l.push(n),l.push(null)),l}}const F0=/\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;function U0(e){return e.replace(F0,z0)}function z0(e,n,t){if(n)return n;if(t.charCodeAt(0)===35){const a=t.charCodeAt(1),s=a===120||a===88;return Jm(t.slice(s?2:1),s?16:10)}return Dc(t)||e}const ig={}.hasOwnProperty;function O0(e,n,t){return n&&typeof n=="object"&&(t=n,n=void 0),H0(t)(W0(K0(t).document().write(j0()(e,n,!0))))}function H0(e){const n={transforms:[],canContainEols:["emphasis","fragment","heading","paragraph","strong"],enter:{autolink:s(jt),autolinkProtocol:j,autolinkEmail:j,atxHeading:s(_r),blockQuote:s(on),characterEscape:j,characterReference:j,codeFenced:s(wt),codeFencedFenceInfo:i,codeFencedFenceMeta:i,codeIndented:s(wt,i),codeText:s(Da,i),codeTextData:j,data:j,codeFlowValue:j,definition:s(vi),definitionDestinationString:i,definitionLabelString:i,definitionTitleString:i,emphasis:s(Ba),hardBreakEscape:s(La),hardBreakTrailing:s(La),htmlFlow:s(Na,i),htmlFlowData:j,htmlText:s(Na,i),htmlTextData:j,image:s(ki),label:i,link:s(jt),listItem:s(bi),listItemValue:p,listOrdered:s(Er,u),listUnordered:s(Er),paragraph:s(Cr),reference:b,referenceString:i,resourceDestinationString:i,resourceTitleString:i,setextHeading:s(_r),strong:s(qn),thematicBreak:s(Si)},exit:{atxHeading:l(),atxHeadingSequence:E,autolink:l(),autolinkEmail:sn,autolinkProtocol:$e,blockQuote:l(),characterEscapeValue:x,characterReferenceMarkerHexadecimal:Se,characterReferenceMarkerNumeric:Se,characterReferenceValue:ee,characterReference:Ae,codeFenced:l(P),codeFencedFence:k,codeFencedFenceInfo:m,codeFencedFenceMeta:f,codeFlowValue:x,codeIndented:l(g),codeText:l(G),codeTextData:x,data:x,definition:l(),definitionDestinationString:_,definitionLabelString:y,definitionTitleString:w,emphasis:l(),hardBreakEscape:l(W),hardBreakTrailing:l(W),htmlFlow:l(J),htmlFlowData:x,htmlText:l(te),htmlTextData:x,image:l(oe),label:F,labelText:B,lineEnding:L,link:l(de),listItem:l(),listOrdered:l(),listUnordered:l(),paragraph:l(),referenceString:H,resourceDestinationString:v,resourceTitleString:N,resource:O,setextHeading:l(M),setextHeadingLineSequence:A,setextHeadingText:S,strong:l(),thematicBreak:l()}};og(n,(e||{}).mdastExtensions||[]);const t={};return r;function r(C){let D={type:"root",children:[]};const q={stack:[D],tokenStack:[],config:n,enter:o,exit:c,buffer:i,resume:d,data:t},Y=[];let re=-1;for(;++re<C.length;)if(C[re][1].type==="listOrdered"||C[re][1].type==="listUnordered")if(C[re][0]==="enter")Y.push(re);else{const gn=Y.pop();re=a(C,gn,re)}for(re=-1;++re<C.length;){const gn=n[C[re][0]];ig.call(gn,C[re][1].type)&&gn[C[re][1].type].call(Object.assign({sliceSerialize:C[re][2].sliceSerialize},q),C[re][1])}if(q.tokenStack.length>0){const gn=q.tokenStack[q.tokenStack.length-1];(gn[1]||Rd).call(q,void 0,gn[0])}for(D.position={start:$n(C.length>0?C[0][1].start:{line:1,column:1,offset:0}),end:$n(C.length>0?C[C.length-2][1].end:{line:1,column:1,offset:0})},re=-1;++re<n.transforms.length;)D=n.transforms[re](D)||D;return D}function a(C,D,q){let Y=D-1,re=-1,gn=!1,vt,Rn,Ir,Tr;for(;++Y<=q;){const Qe=C[Y];switch(Qe[1].type){case"listUnordered":case"listOrdered":case"blockQuote":{Qe[0]==="enter"?re++:re--,Tr=void 0;break}case"lineEndingBlank":{Qe[0]==="enter"&&(vt&&!Tr&&!re&&!Ir&&(Ir=Y),Tr=void 0);break}case"linePrefix":case"listItemValue":case"listItemMarker":case"listItemPrefix":case"listItemPrefixWhitespace":break;default:Tr=void 0}if(!re&&Qe[0]==="enter"&&Qe[1].type==="listItemPrefix"||re===-1&&Qe[0]==="exit"&&(Qe[1].type==="listUnordered"||Qe[1].type==="listOrdered")){if(vt){let Ft=Y;for(Rn=void 0;Ft--;){const Mn=C[Ft];if(Mn[1].type==="lineEnding"||Mn[1].type==="lineEndingBlank"){if(Mn[0]==="exit")continue;Rn&&(C[Rn][1].type="lineEndingBlank",gn=!0),Mn[1].type="lineEnding",Rn=Ft}else if(!(Mn[1].type==="linePrefix"||Mn[1].type==="blockQuotePrefix"||Mn[1].type==="blockQuotePrefixWhitespace"||Mn[1].type==="blockQuoteMarker"||Mn[1].type==="listItemIndent"))break}Ir&&(!Rn||Ir<Rn)&&(vt._spread=!0),vt.end=Object.assign({},Rn?C[Rn][1].start:Qe[1].end),C.splice(Rn||Y,0,["exit",vt,Qe[2]]),Y++,q++}if(Qe[1].type==="listItemPrefix"){const Ft={type:"listItem",_spread:!1,start:Object.assign({},Qe[1].start),end:void 0};vt=Ft,C.splice(Y,0,["enter",Ft,Qe[2]]),Y++,q++,Ir=void 0,Tr=!0}}}return C[D][1]._spread=gn,q}function s(C,D){return q;function q(Y){o.call(this,C(Y),Y),D&&D.call(this,Y)}}function i(){this.stack.push({type:"fragment",children:[]})}function o(C,D,q){this.stack[this.stack.length-1].children.push(C),this.stack.push(C),this.tokenStack.push([D,q||void 0]),C.position={start:$n(D.start),end:void 0}}function l(C){return D;function D(q){C&&C.call(this,q),c.call(this,q)}}function c(C,D){const q=this.stack.pop(),Y=this.tokenStack.pop();if(Y)Y[0].type!==C.type&&(D?D.call(this,C,Y[0]):(Y[1]||Rd).call(this,C,Y[0]));else throw new Error("Cannot close `"+C.type+"` ("+ea({start:C.start,end:C.end})+"): it’s not open");q.position.end=$n(C.end)}function d(){return Mc(this.stack.pop())}function u(){this.data.expectingFirstListItemValue=!0}function p(C){if(this.data.expectingFirstListItemValue){const D=this.stack[this.stack.length-2];D.start=Number.parseInt(this.sliceSerialize(C),10),this.data.expectingFirstListItemValue=void 0}}function m(){const C=this.resume(),D=this.stack[this.stack.length-1];D.lang=C}function f(){const C=this.resume(),D=this.stack[this.stack.length-1];D.meta=C}function k(){this.data.flowCodeInside||(this.buffer(),this.data.flowCodeInside=!0)}function P(){const C=this.resume(),D=this.stack[this.stack.length-1];D.value=C.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g,""),this.data.flowCodeInside=void 0}function g(){const C=this.resume(),D=this.stack[this.stack.length-1];D.value=C.replace(/(\r?\n|\r)$/g,"")}function y(C){const D=this.resume(),q=this.stack[this.stack.length-1];q.label=D,q.identifier=Sn(this.sliceSerialize(C)).toLowerCase()}function w(){const C=this.resume(),D=this.stack[this.stack.length-1];D.title=C}function _(){const C=this.resume(),D=this.stack[this.stack.length-1];D.url=C}function E(C){const D=this.stack[this.stack.length-1];if(!D.depth){const q=this.sliceSerialize(C).length;D.depth=q}}function S(){this.data.setextHeadingSlurpLineEnding=!0}function A(C){const D=this.stack[this.stack.length-1];D.depth=this.sliceSerialize(C).codePointAt(0)===61?1:2}function M(){this.data.setextHeadingSlurpLineEnding=void 0}function j(C){const q=this.stack[this.stack.length-1].children;let Y=q[q.length-1];(!Y||Y.type!=="text")&&(Y=Ar(),Y.position={start:$n(C.start),end:void 0},q.push(Y)),this.stack.push(Y)}function x(C){const D=this.stack.pop();D.value+=this.sliceSerialize(C),D.position.end=$n(C.end)}function L(C){const D=this.stack[this.stack.length-1];if(this.data.atHardBreak){const q=D.children[D.children.length-1];q.position.end=$n(C.end),this.data.atHardBreak=void 0;return}!this.data.setextHeadingSlurpLineEnding&&n.canContainEols.includes(D.type)&&(j.call(this,C),x.call(this,C))}function W(){this.data.atHardBreak=!0}function J(){const C=this.resume(),D=this.stack[this.stack.length-1];D.value=C}function te(){const C=this.resume(),D=this.stack[this.stack.length-1];D.value=C}function G(){const C=this.resume(),D=this.stack[this.stack.length-1];D.value=C}function de(){const C=this.stack[this.stack.length-1];if(this.data.inReference){const D=this.data.referenceType||"shortcut";C.type+="Reference",C.referenceType=D,delete C.url,delete C.title}else delete C.identifier,delete C.label;this.data.referenceType=void 0}function oe(){const C=this.stack[this.stack.length-1];if(this.data.inReference){const D=this.data.referenceType||"shortcut";C.type+="Reference",C.referenceType=D,delete C.url,delete C.title}else delete C.identifier,delete C.label;this.data.referenceType=void 0}function B(C){const D=this.sliceSerialize(C),q=this.stack[this.stack.length-2];q.label=U0(D),q.identifier=Sn(D).toLowerCase()}function F(){const C=this.stack[this.stack.length-1],D=this.resume(),q=this.stack[this.stack.length-1];if(this.data.inReference=!0,q.type==="link"){const Y=C.children;q.children=Y}else q.alt=D}function v(){const C=this.resume(),D=this.stack[this.stack.length-1];D.url=C}function N(){const C=this.resume(),D=this.stack[this.stack.length-1];D.title=C}function O(){this.data.inReference=void 0}function b(){this.data.referenceType="collapsed"}function H(C){const D=this.resume(),q=this.stack[this.stack.length-1];q.label=D,q.identifier=Sn(this.sliceSerialize(C)).toLowerCase(),this.data.referenceType="full"}function Se(C){this.data.characterReferenceType=C.type}function ee(C){const D=this.sliceSerialize(C),q=this.data.characterReferenceType;let Y;q?(Y=Jm(D,q==="characterReferenceMarkerNumeric"?10:16),this.data.characterReferenceType=void 0):Y=Dc(D);const re=this.stack[this.stack.length-1];re.value+=Y}function Ae(C){const D=this.stack.pop();D.position.end=$n(C.end)}function $e(C){x.call(this,C);const D=this.stack[this.stack.length-1];D.url=this.sliceSerialize(C)}function sn(C){x.call(this,C);const D=this.stack[this.stack.length-1];D.url="mailto:"+this.sliceSerialize(C)}function on(){return{type:"blockquote",children:[]}}function wt(){return{type:"code",lang:null,meta:null,value:""}}function Da(){return{type:"inlineCode",value:""}}function vi(){return{type:"definition",identifier:"",label:null,title:null,url:""}}function Ba(){return{type:"emphasis",children:[]}}function _r(){return{type:"heading",depth:0,children:[]}}function La(){return{type:"break"}}function Na(){return{type:"html",value:""}}function ki(){return{type:"image",title:null,url:"",alt:null}}function jt(){return{type:"link",title:null,url:"",children:[]}}function Er(C){return{type:"list",ordered:C.type==="listOrdered",start:null,spread:C._spread,children:[]}}function bi(C){return{type:"listItem",spread:C._spread,checked:null,children:[]}}function Cr(){return{type:"paragraph",children:[]}}function qn(){return{type:"strong",children:[]}}function Ar(){return{type:"text",value:""}}function Si(){return{type:"thematicBreak"}}}function $n(e){return{line:e.line,column:e.column,offset:e.offset}}function og(e,n){let t=-1;for(;++t<n.length;){const r=n[t];Array.isArray(r)?og(e,r):q0(e,r)}}function q0(e,n){let t;for(t in n)if(ig.call(n,t))switch(t){case"canContainEols":{const r=n[t];r&&e[t].push(...r);break}case"transforms":{const r=n[t];r&&e[t].push(...r);break}case"enter":case"exit":{const r=n[t];r&&Object.assign(e[t],r);break}}}function Rd(e,n){throw e?new Error("Cannot close `"+e.type+"` ("+ea({start:e.start,end:e.end})+"): a different token (`"+n.type+"`, "+ea({start:n.start,end:n.end})+") is open"):new Error("Cannot close document, a token (`"+n.type+"`, "+ea({start:n.start,end:n.end})+") is still open")}function V0(e){const n=this;n.parser=t;function t(r){return O0(r,{...n.data("settings"),...e,extensions:n.data("micromarkExtensions")||[],mdastExtensions:n.data("fromMarkdownExtensions")||[]})}}function G0(e,n){const t={type:"element",tagName:"blockquote",properties:{},children:e.wrap(e.all(n),!0)};return e.patch(n,t),e.applyData(n,t)}function $0(e,n){const t={type:"element",tagName:"br",properties:{},children:[]};return e.patch(n,t),[e.applyData(n,t),{type:"text",value:`
`}]}function Q0(e,n){const t=n.value?n.value+`
`:"",r={},a=n.lang?n.lang.split(/\s+/):[];a.length>0&&(r.className=["language-"+a[0]]);let s={type:"element",tagName:"code",properties:r,children:[{type:"text",value:t}]};return n.meta&&(s.data={meta:n.meta}),e.patch(n,s),s=e.applyData(n,s),s={type:"element",tagName:"pre",properties:{},children:[s]},e.patch(n,s),s}function J0(e,n){const t={type:"element",tagName:"del",properties:{},children:e.all(n)};return e.patch(n,t),e.applyData(n,t)}function Y0(e,n){const t={type:"element",tagName:"em",properties:{},children:e.all(n)};return e.patch(n,t),e.applyData(n,t)}function X0(e,n){const t=typeof e.options.clobberPrefix=="string"?e.options.clobberPrefix:"user-content-",r=String(n.identifier).toUpperCase(),a=Pr(r.toLowerCase()),s=e.footnoteOrder.indexOf(r);let i,o=e.footnoteCounts.get(r);o===void 0?(o=0,e.footnoteOrder.push(r),i=e.footnoteOrder.length):i=s+1,o+=1,e.footnoteCounts.set(r,o);const l={type:"element",tagName:"a",properties:{href:"#"+t+"fn-"+a,id:t+"fnref-"+a+(o>1?"-"+o:""),dataFootnoteRef:!0,ariaDescribedBy:["footnote-label"]},children:[{type:"text",value:String(i)}]};e.patch(n,l);const c={type:"element",tagName:"sup",properties:{},children:[l]};return e.patch(n,c),e.applyData(n,c)}function Z0(e,n){const t={type:"element",tagName:"h"+n.depth,properties:{},children:e.all(n)};return e.patch(n,t),e.applyData(n,t)}function e2(e,n){if(e.options.allowDangerousHtml){const t={type:"raw",value:n.value};return e.patch(n,t),e.applyData(n,t)}}function lg(e,n){const t=n.referenceType;let r="]";if(t==="collapsed"?r+="[]":t==="full"&&(r+="["+(n.label||n.identifier)+"]"),n.type==="imageReference")return[{type:"text",value:"!["+n.alt+r}];const a=e.all(n),s=a[0];s&&s.type==="text"?s.value="["+s.value:a.unshift({type:"text",value:"["});const i=a[a.length-1];return i&&i.type==="text"?i.value+=r:a.push({type:"text",value:r}),a}function n2(e,n){const t=String(n.identifier).toUpperCase(),r=e.definitionById.get(t);if(!r)return lg(e,n);const a={src:Pr(r.url||""),alt:n.alt};r.title!==null&&r.title!==void 0&&(a.title=r.title);const s={type:"element",tagName:"img",properties:a,children:[]};return e.patch(n,s),e.applyData(n,s)}function t2(e,n){const t={src:Pr(n.url)};n.alt!==null&&n.alt!==void 0&&(t.alt=n.alt),n.title!==null&&n.title!==void 0&&(t.title=n.title);const r={type:"element",tagName:"img",properties:t,children:[]};return e.patch(n,r),e.applyData(n,r)}function r2(e,n){const t={type:"text",value:n.value.replace(/\r?\n|\r/g," ")};e.patch(n,t);const r={type:"element",tagName:"code",properties:{},children:[t]};return e.patch(n,r),e.applyData(n,r)}function a2(e,n){const t=String(n.identifier).toUpperCase(),r=e.definitionById.get(t);if(!r)return lg(e,n);const a={href:Pr(r.url||"")};r.title!==null&&r.title!==void 0&&(a.title=r.title);const s={type:"element",tagName:"a",properties:a,children:e.all(n)};return e.patch(n,s),e.applyData(n,s)}function s2(e,n){const t={href:Pr(n.url)};n.title!==null&&n.title!==void 0&&(t.title=n.title);const r={type:"element",tagName:"a",properties:t,children:e.all(n)};return e.patch(n,r),e.applyData(n,r)}function i2(e,n,t){const r=e.all(n),a=t?o2(t):cg(n),s={},i=[];if(typeof n.checked=="boolean"){const d=r[0];let u;d&&d.type==="element"&&d.tagName==="p"?u=d:(u={type:"element",tagName:"p",properties:{},children:[]},r.unshift(u)),u.children.length>0&&u.children.unshift({type:"text",value:" "}),u.children.unshift({type:"element",tagName:"input",properties:{type:"checkbox",checked:n.checked,disabled:!0},children:[]}),s.className=["task-list-item"]}let o=-1;for(;++o<r.length;){const d=r[o];(a||o!==0||d.type!=="element"||d.tagName!=="p")&&i.push({type:"text",value:`
`}),d.type==="element"&&d.tagName==="p"&&!a?i.push(...d.children):i.push(d)}const l=r[r.length-1];l&&(a||l.type!=="element"||l.tagName!=="p")&&i.push({type:"text",value:`
`});const c={type:"element",tagName:"li",properties:s,children:i};return e.patch(n,c),e.applyData(n,c)}function o2(e){let n=!1;if(e.type==="list"){n=e.spread||!1;const t=e.children;let r=-1;for(;!n&&++r<t.length;)n=cg(t[r])}return n}function cg(e){const n=e.spread;return n??e.children.length>1}function l2(e,n){const t={},r=e.all(n);let a=-1;for(typeof n.start=="number"&&n.start!==1&&(t.start=n.start);++a<r.length;){const i=r[a];if(i.type==="element"&&i.tagName==="li"&&i.properties&&Array.isArray(i.properties.className)&&i.properties.className.includes("task-list-item")){t.className=["contains-task-list"];break}}const s={type:"element",tagName:n.ordered?"ol":"ul",properties:t,children:e.wrap(r,!0)};return e.patch(n,s),e.applyData(n,s)}function c2(e,n){const t={type:"element",tagName:"p",properties:{},children:e.all(n)};return e.patch(n,t),e.applyData(n,t)}function u2(e,n){const t={type:"root",children:e.wrap(e.all(n))};return e.patch(n,t),e.applyData(n,t)}function d2(e,n){const t={type:"element",tagName:"strong",properties:{},children:e.all(n)};return e.patch(n,t),e.applyData(n,t)}function h2(e,n){const t=e.all(n),r=t.shift(),a=[];if(r){const i={type:"element",tagName:"thead",properties:{},children:e.wrap([r],!0)};e.patch(n.children[0],i),a.push(i)}if(t.length>0){const i={type:"element",tagName:"tbody",properties:{},children:e.wrap(t,!0)},o=Ac(n.children[1]),l=zm(n.children[n.children.length-1]);o&&l&&(i.position={start:o,end:l}),a.push(i)}const s={type:"element",tagName:"table",properties:{},children:e.wrap(a,!0)};return e.patch(n,s),e.applyData(n,s)}function p2(e,n,t){const r=t?t.children:void 0,s=(r?r.indexOf(n):1)===0?"th":"td",i=t&&t.type==="table"?t.align:void 0,o=i?i.length:n.children.length;let l=-1;const c=[];for(;++l<o;){const u=n.children[l],p={},m=i?i[l]:void 0;m&&(p.align=m);let f={type:"element",tagName:s,properties:p,children:[]};u&&(f.children=e.all(u),e.patch(u,f),f=e.applyData(u,f)),c.push(f)}const d={type:"element",tagName:"tr",properties:{},children:e.wrap(c,!0)};return e.patch(n,d),e.applyData(n,d)}function m2(e,n){const t={type:"element",tagName:"td",properties:{},children:e.all(n)};return e.patch(n,t),e.applyData(n,t)}const Md=9,Dd=32;function g2(e){const n=String(e),t=/\r?\n|\r/g;let r=t.exec(n),a=0;const s=[];for(;r;)s.push(Bd(n.slice(a,r.index),a>0,!0),r[0]),a=r.index+r[0].length,r=t.exec(n);return s.push(Bd(n.slice(a),a>0,!1)),s.join("")}function Bd(e,n,t){let r=0,a=e.length;if(n){let s=e.codePointAt(r);for(;s===Md||s===Dd;)r++,s=e.codePointAt(r)}if(t){let s=e.codePointAt(a-1);for(;s===Md||s===Dd;)a--,s=e.codePointAt(a-1)}return a>r?e.slice(r,a):""}function f2(e,n){const t={type:"text",value:g2(String(n.value))};return e.patch(n,t),e.applyData(n,t)}function y2(e,n){const t={type:"element",tagName:"hr",properties:{},children:[]};return e.patch(n,t),e.applyData(n,t)}const w2={blockquote:G0,break:$0,code:Q0,delete:J0,emphasis:Y0,footnoteReference:X0,heading:Z0,html:e2,imageReference:n2,image:t2,inlineCode:r2,linkReference:a2,link:s2,listItem:i2,list:l2,paragraph:c2,root:u2,strong:d2,table:h2,tableCell:m2,tableRow:p2,text:f2,thematicBreak:y2,toml:es,yaml:es,definition:es,footnoteDefinition:es};function es(){}const ug=-1,fi=0,ta=1,qs=2,Lc=3,Nc=4,Kc=5,Wc=6,dg=7,hg=8,Ld=typeof self=="object"?self:globalThis,v2=(e,n)=>{const t=(a,s)=>(e.set(s,a),a),r=a=>{if(e.has(a))return e.get(a);const[s,i]=n[a];switch(s){case fi:case ug:return t(i,a);case ta:{const o=t([],a);for(const l of i)o.push(r(l));return o}case qs:{const o=t({},a);for(const[l,c]of i)o[r(l)]=r(c);return o}case Lc:return t(new Date(i),a);case Nc:{const{source:o,flags:l}=i;return t(new RegExp(o,l),a)}case Kc:{const o=t(new Map,a);for(const[l,c]of i)o.set(r(l),r(c));return o}case Wc:{const o=t(new Set,a);for(const l of i)o.add(r(l));return o}case dg:{const{name:o,message:l}=i;return t(new Ld[o](l),a)}case hg:return t(BigInt(i),a);case"BigInt":return t(Object(BigInt(i)),a);case"ArrayBuffer":return t(new Uint8Array(i).buffer,i);case"DataView":{const{buffer:o}=new Uint8Array(i);return t(new DataView(o),i)}}return t(new Ld[s](i),a)};return r},Nd=e=>v2(new Map,e)(0),zt="",{toString:k2}={},{keys:b2}=Object,Fr=e=>{const n=typeof e;if(n!=="object"||!e)return[fi,n];const t=k2.call(e).slice(8,-1);switch(t){case"Array":return[ta,zt];case"Object":return[qs,zt];case"Date":return[Lc,zt];case"RegExp":return[Nc,zt];case"Map":return[Kc,zt];case"Set":return[Wc,zt];case"DataView":return[ta,t]}return t.includes("Array")?[ta,t]:t.includes("Error")?[dg,t]:[qs,t]},ns=([e,n])=>e===fi&&(n==="function"||n==="symbol"),S2=(e,n,t,r)=>{const a=(i,o)=>{const l=r.push(i)-1;return t.set(o,l),l},s=i=>{if(t.has(i))return t.get(i);let[o,l]=Fr(i);switch(o){case fi:{let d=i;switch(l){case"bigint":o=hg,d=i.toString();break;case"function":case"symbol":if(e)throw new TypeError("unable to serialize "+l);d=null;break;case"undefined":return a([ug],i)}return a([o,d],i)}case ta:{if(l){let p=i;return l==="DataView"?p=new Uint8Array(i.buffer):l==="ArrayBuffer"&&(p=new Uint8Array(i)),a([l,[...p]],i)}const d=[],u=a([o,d],i);for(const p of i)d.push(s(p));return u}case qs:{if(l)switch(l){case"BigInt":return a([l,i.toString()],i);case"Boolean":case"Number":case"String":return a([l,i.valueOf()],i)}if(n&&"toJSON"in i)return s(i.toJSON());const d=[],u=a([o,d],i);for(const p of b2(i))(e||!ns(Fr(i[p])))&&d.push([s(p),s(i[p])]);return u}case Lc:return a([o,i.toISOString()],i);case Nc:{const{source:d,flags:u}=i;return a([o,{source:d,flags:u}],i)}case Kc:{const d=[],u=a([o,d],i);for(const[p,m]of i)(e||!(ns(Fr(p))||ns(Fr(m))))&&d.push([s(p),s(m)]);return u}case Wc:{const d=[],u=a([o,d],i);for(const p of i)(e||!ns(Fr(p)))&&d.push(s(p));return u}}const{message:c}=i;return a([o,{name:l,message:c}],i)};return s},Kd=(e,{json:n,lossy:t}={})=>{const r=[];return S2(!(n||t),!!n,new Map,r)(e),r},Vs=typeof structuredClone=="function"?(e,n)=>n&&("json"in n||"lossy"in n)?Nd(Kd(e,n)):structuredClone(e):(e,n)=>Nd(Kd(e,n));function x2(e,n){const t=[{type:"text",value:"↩"}];return n>1&&t.push({type:"element",tagName:"sup",properties:{},children:[{type:"text",value:String(n)}]}),t}function P2(e,n){return"Back to reference "+(e+1)+(n>1?"-"+n:"")}function _2(e){const n=typeof e.options.clobberPrefix=="string"?e.options.clobberPrefix:"user-content-",t=e.options.footnoteBackContent||x2,r=e.options.footnoteBackLabel||P2,a=e.options.footnoteLabel||"Footnotes",s=e.options.footnoteLabelTagName||"h2",i=e.options.footnoteLabelProperties||{className:["sr-only"]},o=[];let l=-1;for(;++l<e.footnoteOrder.length;){const c=e.footnoteById.get(e.footnoteOrder[l]);if(!c)continue;const d=e.all(c),u=String(c.identifier).toUpperCase(),p=Pr(u.toLowerCase());let m=0;const f=[],k=e.footnoteCounts.get(u);for(;k!==void 0&&++m<=k;){f.length>0&&f.push({type:"text",value:" "});let y=typeof t=="string"?t:t(l,m);typeof y=="string"&&(y={type:"text",value:y}),f.push({type:"element",tagName:"a",properties:{href:"#"+n+"fnref-"+p+(m>1?"-"+m:""),dataFootnoteBackref:"",ariaLabel:typeof r=="string"?r:r(l,m),className:["data-footnote-backref"]},children:Array.isArray(y)?y:[y]})}const P=d[d.length-1];if(P&&P.type==="element"&&P.tagName==="p"){const y=P.children[P.children.length-1];y&&y.type==="text"?y.value+=" ":P.children.push({type:"text",value:" "}),P.children.push(...f)}else d.push(...f);const g={type:"element",tagName:"li",properties:{id:n+"fn-"+p},children:e.wrap(d,!0)};e.patch(c,g),o.push(g)}if(o.length!==0)return{type:"element",tagName:"section",properties:{dataFootnotes:!0,className:["footnotes"]},children:[{type:"element",tagName:s,properties:{...Vs(i),id:"footnote-label"},children:[{type:"text",value:a}]},{type:"text",value:`
`},{type:"element",tagName:"ol",properties:{},children:e.wrap(o,!0)},{type:"text",value:`
`}]}}const yi=function(e){if(e==null)return I2;if(typeof e=="function")return wi(e);if(typeof e=="object")return Array.isArray(e)?E2(e):C2(e);if(typeof e=="string")return A2(e);throw new Error("Expected function, string, or object as test")};function E2(e){const n=[];let t=-1;for(;++t<e.length;)n[t]=yi(e[t]);return wi(r);function r(...a){let s=-1;for(;++s<n.length;)if(n[s].apply(this,a))return!0;return!1}}function C2(e){const n=e;return wi(t);function t(r){const a=r;let s;for(s in e)if(a[s]!==n[s])return!1;return!0}}function A2(e){return wi(n);function n(t){return t&&t.type===e}}function wi(e){return n;function n(t,r,a){return!!(T2(t)&&e.call(this,t,typeof r=="number"?r:void 0,a||void 0))}}function I2(){return!0}function T2(e){return e!==null&&typeof e=="object"&&"type"in e}const pg=[],R2=!0,yl=!1,M2="skip";function mg(e,n,t,r){let a;typeof n=="function"&&typeof t!="function"?(r=t,t=n):a=n;const s=yi(a),i=r?-1:1;o(e,void 0,[])();function o(l,c,d){const u=l&&typeof l=="object"?l:{};if(typeof u.type=="string"){const m=typeof u.tagName=="string"?u.tagName:typeof u.name=="string"?u.name:void 0;Object.defineProperty(p,"name",{value:"node ("+(l.type+(m?"<"+m+">":""))+")"})}return p;function p(){let m=pg,f,k,P;if((!n||s(l,c,d[d.length-1]||void 0))&&(m=D2(t(l,d)),m[0]===yl))return m;if("children"in l&&l.children){const g=l;if(g.children&&m[0]!==M2)for(k=(r?g.children.length:-1)+i,P=d.concat(g);k>-1&&k<g.children.length;){const y=g.children[k];if(f=o(y,k,P)(),f[0]===yl)return f;k=typeof f[1]=="number"?f[1]:k+i}}return m}}}function D2(e){return Array.isArray(e)?e:typeof e=="number"?[R2,e]:e==null?pg:[e]}function jc(e,n,t,r){let a,s,i;typeof n=="function"&&typeof t!="function"?(s=void 0,i=n,a=t):(s=n,i=t,a=r),mg(e,s,o,a);function o(l,c){const d=c[c.length-1],u=d?d.children.indexOf(l):void 0;return i(l,u,d)}}const wl={}.hasOwnProperty,B2={};function L2(e,n){const t=n||B2,r=new Map,a=new Map,s=new Map,i={...w2,...t.handlers},o={all:c,applyData:K2,definitionById:r,footnoteById:a,footnoteCounts:s,footnoteOrder:[],handlers:i,one:l,options:t,patch:N2,wrap:j2};return jc(e,function(d){if(d.type==="definition"||d.type==="footnoteDefinition"){const u=d.type==="definition"?r:a,p=String(d.identifier).toUpperCase();u.has(p)||u.set(p,d)}}),o;function l(d,u){const p=d.type,m=o.handlers[p];if(wl.call(o.handlers,p)&&m)return m(o,d,u);if(o.options.passThrough&&o.options.passThrough.includes(p)){if("children"in d){const{children:k,...P}=d,g=Vs(P);return g.children=o.all(d),g}return Vs(d)}return(o.options.unknownHandler||W2)(o,d,u)}function c(d){const u=[];if("children"in d){const p=d.children;let m=-1;for(;++m<p.length;){const f=o.one(p[m],d);if(f){if(m&&p[m-1].type==="break"&&(!Array.isArray(f)&&f.type==="text"&&(f.value=Wd(f.value)),!Array.isArray(f)&&f.type==="element")){const k=f.children[0];k&&k.type==="text"&&(k.value=Wd(k.value))}Array.isArray(f)?u.push(...f):u.push(f)}}}return u}}function N2(e,n){e.position&&(n.position=Px(e))}function K2(e,n){let t=n;if(e&&e.data){const r=e.data.hName,a=e.data.hChildren,s=e.data.hProperties;if(typeof r=="string")if(t.type==="element")t.tagName=r;else{const i="children"in t?t.children:[t];t={type:"element",tagName:r,properties:{},children:i}}t.type==="element"&&s&&Object.assign(t.properties,Vs(s)),"children"in t&&t.children&&a!==null&&a!==void 0&&(t.children=a)}return t}function W2(e,n){const t=n.data||{},r="value"in n&&!(wl.call(t,"hProperties")||wl.call(t,"hChildren"))?{type:"text",value:n.value}:{type:"element",tagName:"div",properties:{},children:e.all(n)};return e.patch(n,r),e.applyData(n,r)}function j2(e,n){const t=[];let r=-1;for(n&&t.push({type:"text",value:`
`});++r<e.length;)r&&t.push({type:"text",value:`
`}),t.push(e[r]);return n&&e.length>0&&t.push({type:"text",value:`
`}),t}function Wd(e){let n=0,t=e.charCodeAt(n);for(;t===9||t===32;)n++,t=e.charCodeAt(n);return e.slice(n)}function jd(e,n){const t=L2(e,n),r=t.one(e,void 0),a=_2(t),s=Array.isArray(r)?{type:"root",children:r}:r||{type:"root",children:[]};return a&&s.children.push({type:"text",value:`
`},a),s}function F2(e,n){return e&&"run"in e?async function(t,r){const a=jd(t,{file:r,...n});await e.run(a,r)}:function(t,r){return jd(t,{file:r,...e||n})}}function Fd(e){if(e)throw e}var ys=Object.prototype.hasOwnProperty,gg=Object.prototype.toString,Ud=Object.defineProperty,zd=Object.getOwnPropertyDescriptor,Od=function(n){return typeof Array.isArray=="function"?Array.isArray(n):gg.call(n)==="[object Array]"},Hd=function(n){if(!n||gg.call(n)!=="[object Object]")return!1;var t=ys.call(n,"constructor"),r=n.constructor&&n.constructor.prototype&&ys.call(n.constructor.prototype,"isPrototypeOf");if(n.constructor&&!t&&!r)return!1;var a;for(a in n);return typeof a>"u"||ys.call(n,a)},qd=function(n,t){Ud&&t.name==="__proto__"?Ud(n,t.name,{enumerable:!0,configurable:!0,value:t.newValue,writable:!0}):n[t.name]=t.newValue},Vd=function(n,t){if(t==="__proto__")if(ys.call(n,t)){if(zd)return zd(n,t).value}else return;return n[t]},U2=function e(){var n,t,r,a,s,i,o=arguments[0],l=1,c=arguments.length,d=!1;for(typeof o=="boolean"&&(d=o,o=arguments[1]||{},l=2),(o==null||typeof o!="object"&&typeof o!="function")&&(o={});l<c;++l)if(n=arguments[l],n!=null)for(t in n)r=Vd(o,t),a=Vd(n,t),o!==a&&(d&&a&&(Hd(a)||(s=Od(a)))?(s?(s=!1,i=r&&Od(r)?r:[]):i=r&&Hd(r)?r:{},qd(o,{name:t,newValue:e(d,i,a)})):typeof a<"u"&&qd(o,{name:t,newValue:a}));return o};const no=Sl(U2);function vl(e){if(typeof e!="object"||e===null)return!1;const n=Object.getPrototypeOf(e);return(n===null||n===Object.prototype||Object.getPrototypeOf(n)===null)&&!(Symbol.toStringTag in e)&&!(Symbol.iterator in e)}function z2(){const e=[],n={run:t,use:r};return n;function t(...a){let s=-1;const i=a.pop();if(typeof i!="function")throw new TypeError("Expected function as last argument, not "+i);o(null,...a);function o(l,...c){const d=e[++s];let u=-1;if(l){i(l);return}for(;++u<a.length;)(c[u]===null||c[u]===void 0)&&(c[u]=a[u]);a=c,d?O2(d,o)(...c):i(null,...c)}}function r(a){if(typeof a!="function")throw new TypeError("Expected `middelware` to be a function, not "+a);return e.push(a),n}}function O2(e,n){let t;return r;function r(...i){const o=e.length>i.length;let l;o&&i.push(a);try{l=e.apply(this,i)}catch(c){const d=c;if(o&&t)throw d;return a(d)}o||(l&&l.then&&typeof l.then=="function"?l.then(s,a):l instanceof Error?a(l):s(l))}function a(i,...o){t||(t=!0,n(i,...o))}function s(i){a(null,i)}}const En={basename:H2,dirname:q2,extname:V2,join:G2,sep:"/"};function H2(e,n){if(n!==void 0&&typeof n!="string")throw new TypeError('"ext" argument must be a string');Ma(e);let t=0,r=-1,a=e.length,s;if(n===void 0||n.length===0||n.length>e.length){for(;a--;)if(e.codePointAt(a)===47){if(s){t=a+1;break}}else r<0&&(s=!0,r=a+1);return r<0?"":e.slice(t,r)}if(n===e)return"";let i=-1,o=n.length-1;for(;a--;)if(e.codePointAt(a)===47){if(s){t=a+1;break}}else i<0&&(s=!0,i=a+1),o>-1&&(e.codePointAt(a)===n.codePointAt(o--)?o<0&&(r=a):(o=-1,r=i));return t===r?r=i:r<0&&(r=e.length),e.slice(t,r)}function q2(e){if(Ma(e),e.length===0)return".";let n=-1,t=e.length,r;for(;--t;)if(e.codePointAt(t)===47){if(r){n=t;break}}else r||(r=!0);return n<0?e.codePointAt(0)===47?"/":".":n===1&&e.codePointAt(0)===47?"//":e.slice(0,n)}function V2(e){Ma(e);let n=e.length,t=-1,r=0,a=-1,s=0,i;for(;n--;){const o=e.codePointAt(n);if(o===47){if(i){r=n+1;break}continue}t<0&&(i=!0,t=n+1),o===46?a<0?a=n:s!==1&&(s=1):a>-1&&(s=-1)}return a<0||t<0||s===0||s===1&&a===t-1&&a===r+1?"":e.slice(a,t)}function G2(...e){let n=-1,t;for(;++n<e.length;)Ma(e[n]),e[n]&&(t=t===void 0?e[n]:t+"/"+e[n]);return t===void 0?".":$2(t)}function $2(e){Ma(e);const n=e.codePointAt(0)===47;let t=Q2(e,!n);return t.length===0&&!n&&(t="."),t.length>0&&e.codePointAt(e.length-1)===47&&(t+="/"),n?"/"+t:t}function Q2(e,n){let t="",r=0,a=-1,s=0,i=-1,o,l;for(;++i<=e.length;){if(i<e.length)o=e.codePointAt(i);else{if(o===47)break;o=47}if(o===47){if(!(a===i-1||s===1))if(a!==i-1&&s===2){if(t.length<2||r!==2||t.codePointAt(t.length-1)!==46||t.codePointAt(t.length-2)!==46){if(t.length>2){if(l=t.lastIndexOf("/"),l!==t.length-1){l<0?(t="",r=0):(t=t.slice(0,l),r=t.length-1-t.lastIndexOf("/")),a=i,s=0;continue}}else if(t.length>0){t="",r=0,a=i,s=0;continue}}n&&(t=t.length>0?t+"/..":"..",r=2)}else t.length>0?t+="/"+e.slice(a+1,i):t=e.slice(a+1,i),r=i-a-1;a=i,s=0}else o===46&&s>-1?s++:s=-1}return t}function Ma(e){if(typeof e!="string")throw new TypeError("Path must be a string. Received "+JSON.stringify(e))}const J2={cwd:Y2};function Y2(){return"/"}function kl(e){return!!(e!==null&&typeof e=="object"&&"href"in e&&e.href&&"protocol"in e&&e.protocol&&e.auth===void 0)}function X2(e){if(typeof e=="string")e=new URL(e);else if(!kl(e)){const n=new TypeError('The "path" argument must be of type string or an instance of URL. Received `'+e+"`");throw n.code="ERR_INVALID_ARG_TYPE",n}if(e.protocol!=="file:"){const n=new TypeError("The URL must be of scheme file");throw n.code="ERR_INVALID_URL_SCHEME",n}return Z2(e)}function Z2(e){if(e.hostname!==""){const r=new TypeError('File URL host must be "localhost" or empty on darwin');throw r.code="ERR_INVALID_FILE_URL_HOST",r}const n=e.pathname;let t=-1;for(;++t<n.length;)if(n.codePointAt(t)===37&&n.codePointAt(t+1)===50){const r=n.codePointAt(t+2);if(r===70||r===102){const a=new TypeError("File URL path must not include encoded / characters");throw a.code="ERR_INVALID_FILE_URL_PATH",a}}return decodeURIComponent(n)}const to=["history","path","basename","stem","extname","dirname"];class fg{constructor(n){let t;n?kl(n)?t={path:n}:typeof n=="string"||eP(n)?t={value:n}:t=n:t={},this.cwd="cwd"in t?"":J2.cwd(),this.data={},this.history=[],this.messages=[],this.value,this.map,this.result,this.stored;let r=-1;for(;++r<to.length;){const s=to[r];s in t&&t[s]!==void 0&&t[s]!==null&&(this[s]=s==="history"?[...t[s]]:t[s])}let a;for(a in t)to.includes(a)||(this[a]=t[a])}get basename(){return typeof this.path=="string"?En.basename(this.path):void 0}set basename(n){ao(n,"basename"),ro(n,"basename"),this.path=En.join(this.dirname||"",n)}get dirname(){return typeof this.path=="string"?En.dirname(this.path):void 0}set dirname(n){Gd(this.basename,"dirname"),this.path=En.join(n||"",this.basename)}get extname(){return typeof this.path=="string"?En.extname(this.path):void 0}set extname(n){if(ro(n,"extname"),Gd(this.dirname,"extname"),n){if(n.codePointAt(0)!==46)throw new Error("`extname` must start with `.`");if(n.includes(".",1))throw new Error("`extname` cannot contain multiple dots")}this.path=En.join(this.dirname,this.stem+(n||""))}get path(){return this.history[this.history.length-1]}set path(n){kl(n)&&(n=X2(n)),ao(n,"path"),this.path!==n&&this.history.push(n)}get stem(){return typeof this.path=="string"?En.basename(this.path,this.extname):void 0}set stem(n){ao(n,"stem"),ro(n,"stem"),this.path=En.join(this.dirname||"",n+(this.extname||""))}fail(n,t,r){const a=this.message(n,t,r);throw a.fatal=!0,a}info(n,t,r){const a=this.message(n,t,r);return a.fatal=void 0,a}message(n,t,r){const a=new Be(n,t,r);return this.path&&(a.name=this.path+":"+a.name,a.file=this.path),a.fatal=!1,this.messages.push(a),a}toString(n){return this.value===void 0?"":typeof this.value=="string"?this.value:new TextDecoder(n||void 0).decode(this.value)}}function ro(e,n){if(e&&e.includes(En.sep))throw new Error("`"+n+"` cannot be a path: did not expect `"+En.sep+"`")}function ao(e,n){if(!e)throw new Error("`"+n+"` cannot be empty")}function Gd(e,n){if(!e)throw new Error("Setting `"+n+"` requires `path` to be set too")}function eP(e){return!!(e&&typeof e=="object"&&"byteLength"in e&&"byteOffset"in e)}const nP=function(e){const r=this.constructor.prototype,a=r[e],s=function(){return a.apply(s,arguments)};return Object.setPrototypeOf(s,r),s},tP={}.hasOwnProperty;class Fc extends nP{constructor(){super("copy"),this.Compiler=void 0,this.Parser=void 0,this.attachers=[],this.compiler=void 0,this.freezeIndex=-1,this.frozen=void 0,this.namespace={},this.parser=void 0,this.transformers=z2()}copy(){const n=new Fc;let t=-1;for(;++t<this.attachers.length;){const r=this.attachers[t];n.use(...r)}return n.data(no(!0,{},this.namespace)),n}data(n,t){return typeof n=="string"?arguments.length===2?(oo("data",this.frozen),this.namespace[n]=t,this):tP.call(this.namespace,n)&&this.namespace[n]||void 0:n?(oo("data",this.frozen),this.namespace=n,this):this.namespace}freeze(){if(this.frozen)return this;const n=this;for(;++this.freezeIndex<this.attachers.length;){const[t,...r]=this.attachers[this.freezeIndex];if(r[0]===!1)continue;r[0]===!0&&(r[0]=void 0);const a=t.call(n,...r);typeof a=="function"&&this.transformers.use(a)}return this.frozen=!0,this.freezeIndex=Number.POSITIVE_INFINITY,this}parse(n){this.freeze();const t=ts(n),r=this.parser||this.Parser;return so("parse",r),r(String(t),t)}process(n,t){const r=this;return this.freeze(),so("process",this.parser||this.Parser),io("process",this.compiler||this.Compiler),t?a(void 0,t):new Promise(a);function a(s,i){const o=ts(n),l=r.parse(o);r.run(l,o,function(d,u,p){if(d||!u||!p)return c(d);const m=u,f=r.stringify(m,p);sP(f)?p.value=f:p.result=f,c(d,p)});function c(d,u){d||!u?i(d):s?s(u):t(void 0,u)}}}processSync(n){let t=!1,r;return this.freeze(),so("processSync",this.parser||this.Parser),io("processSync",this.compiler||this.Compiler),this.process(n,a),Qd("processSync","process",t),r;function a(s,i){t=!0,Fd(s),r=i}}run(n,t,r){$d(n),this.freeze();const a=this.transformers;return!r&&typeof t=="function"&&(r=t,t=void 0),r?s(void 0,r):new Promise(s);function s(i,o){const l=ts(t);a.run(n,l,c);function c(d,u,p){const m=u||n;d?o(d):i?i(m):r(void 0,m,p)}}}runSync(n,t){let r=!1,a;return this.run(n,t,s),Qd("runSync","run",r),a;function s(i,o){Fd(i),a=o,r=!0}}stringify(n,t){this.freeze();const r=ts(t),a=this.compiler||this.Compiler;return io("stringify",a),$d(n),a(n,r)}use(n,...t){const r=this.attachers,a=this.namespace;if(oo("use",this.frozen),n!=null)if(typeof n=="function")l(n,t);else if(typeof n=="object")Array.isArray(n)?o(n):i(n);else throw new TypeError("Expected usable value, not `"+n+"`");return this;function s(c){if(typeof c=="function")l(c,[]);else if(typeof c=="object")if(Array.isArray(c)){const[d,...u]=c;l(d,u)}else i(c);else throw new TypeError("Expected usable value, not `"+c+"`")}function i(c){if(!("plugins"in c)&&!("settings"in c))throw new Error("Expected usable value but received an empty preset, which is probably a mistake: presets typically come with `plugins` and sometimes with `settings`, but this has neither");o(c.plugins),c.settings&&(a.settings=no(!0,a.settings,c.settings))}function o(c){let d=-1;if(c!=null)if(Array.isArray(c))for(;++d<c.length;){const u=c[d];s(u)}else throw new TypeError("Expected a list of plugins, not `"+c+"`")}function l(c,d){let u=-1,p=-1;for(;++u<r.length;)if(r[u][0]===c){p=u;break}if(p===-1)r.push([c,...d]);else if(d.length>0){let[m,...f]=d;const k=r[p][1];vl(k)&&vl(m)&&(m=no(!0,k,m)),r[p]=[c,m,...f]}}}}const rP=new Fc().freeze();function so(e,n){if(typeof n!="function")throw new TypeError("Cannot `"+e+"` without `parser`")}function io(e,n){if(typeof n!="function")throw new TypeError("Cannot `"+e+"` without `compiler`")}function oo(e,n){if(n)throw new Error("Cannot call `"+e+"` on a frozen processor.\nCreate a new processor first, by calling it: use `processor()` instead of `processor`.")}function $d(e){if(!vl(e)||typeof e.type!="string")throw new TypeError("Expected node, got `"+e+"`")}function Qd(e,n,t){if(!t)throw new Error("`"+e+"` finished async. Use `"+n+"` instead")}function ts(e){return aP(e)?e:new fg(e)}function aP(e){return!!(e&&typeof e=="object"&&"message"in e&&"messages"in e)}function sP(e){return typeof e=="string"||iP(e)}function iP(e){return!!(e&&typeof e=="object"&&"byteLength"in e&&"byteOffset"in e)}const oP="https://github.com/remarkjs/react-markdown/blob/main/changelog.md",Jd=[],Yd={allowDangerousHtml:!0},lP=/^(https?|ircs?|mailto|xmpp)$/i,cP=[{from:"astPlugins",id:"remove-buggy-html-in-markdown-parser"},{from:"allowDangerousHtml",id:"remove-buggy-html-in-markdown-parser"},{from:"allowNode",id:"replace-allownode-allowedtypes-and-disallowedtypes",to:"allowElement"},{from:"allowedTypes",id:"replace-allownode-allowedtypes-and-disallowedtypes",to:"allowedElements"},{from:"className",id:"remove-classname"},{from:"disallowedTypes",id:"replace-allownode-allowedtypes-and-disallowedtypes",to:"disallowedElements"},{from:"escapeHtml",id:"remove-buggy-html-in-markdown-parser"},{from:"includeElementIndex",id:"#remove-includeelementindex"},{from:"includeNodeIndex",id:"change-includenodeindex-to-includeelementindex"},{from:"linkTarget",id:"remove-linktarget"},{from:"plugins",id:"change-plugins-to-remarkplugins",to:"remarkPlugins"},{from:"rawSourcePos",id:"#remove-rawsourcepos"},{from:"renderers",id:"change-renderers-to-components",to:"components"},{from:"source",id:"change-source-to-children",to:"children"},{from:"sourcePos",id:"#remove-sourcepos"},{from:"transformImageUri",id:"#add-urltransform",to:"urlTransform"},{from:"transformLinkUri",id:"#add-urltransform",to:"urlTransform"}];function uP(e){const n=dP(e),t=hP(e);return pP(n.runSync(n.parse(t),t),e)}function dP(e){const n=e.rehypePlugins||Jd,t=e.remarkPlugins||Jd,r=e.remarkRehypeOptions?{...e.remarkRehypeOptions,...Yd}:Yd;return rP().use(V0).use(t).use(F2,r).use(n)}function hP(e){const n=e.children||"",t=new fg;return typeof n=="string"&&(t.value=n),t}function pP(e,n){const t=n.allowedElements,r=n.allowElement,a=n.components,s=n.disallowedElements,i=n.skipHtml,o=n.unwrapDisallowed,l=n.urlTransform||mP;for(const d of cP)Object.hasOwn(n,d.from)&&(""+d.from+(d.to?"use `"+d.to+"` instead":"remove it")+oP+d.id,void 0);return jc(e,c),Ix(e,{Fragment:h.Fragment,components:a,ignoreInvalidStyle:!0,jsx:h.jsx,jsxs:h.jsxs,passKeys:!0,passNode:!0});function c(d,u,p){if(d.type==="raw"&&p&&typeof u=="number")return i?p.children.splice(u,1):p.children[u]={type:"text",value:d.value},u;if(d.type==="element"){let m;for(m in Xi)if(Object.hasOwn(Xi,m)&&Object.hasOwn(d.properties,m)){const f=d.properties[m],k=Xi[m];(k===null||k.includes(d.tagName))&&(d.properties[m]=l(String(f||""),m,d))}}if(d.type==="element"){let m=t?!t.includes(d.tagName):s?s.includes(d.tagName):!1;if(!m&&r&&typeof u=="number"&&(m=!r(d,u,p)),m&&p&&typeof u=="number")return o&&d.children?p.children.splice(u,1,...d.children):p.children.splice(u,1),u}}}function mP(e){const n=e.indexOf(":"),t=e.indexOf("?"),r=e.indexOf("#"),a=e.indexOf("/");return n===-1||a!==-1&&n>a||t!==-1&&n>t||r!==-1&&n>r||lP.test(e.slice(0,n))?e:""}function Xd(e,n){const t=String(e);if(typeof n!="string")throw new TypeError("Expected character");let r=0,a=t.indexOf(n);for(;a!==-1;)r++,a=t.indexOf(n,a+n.length);return r}function gP(e){if(typeof e!="string")throw new TypeError("Expected a string");return e.replace(/[|\\{}()[\]^$+*?.]/g,"\\$&").replace(/-/g,"\\x2d")}function fP(e,n,t){const a=yi((t||{}).ignore||[]),s=yP(n);let i=-1;for(;++i<s.length;)mg(e,"text",o);function o(c,d){let u=-1,p;for(;++u<d.length;){const m=d[u],f=p?p.children:void 0;if(a(m,f?f.indexOf(m):void 0,p))return;p=m}if(p)return l(c,d)}function l(c,d){const u=d[d.length-1],p=s[i][0],m=s[i][1];let f=0;const P=u.children.indexOf(c);let g=!1,y=[];p.lastIndex=0;let w=p.exec(c.value);for(;w;){const _=w.index,E={index:w.index,input:w.input,stack:[...d,c]};let S=m(...w,E);if(typeof S=="string"&&(S=S.length>0?{type:"text",value:S}:void 0),S===!1?p.lastIndex=_+1:(f!==_&&y.push({type:"text",value:c.value.slice(f,_)}),Array.isArray(S)?y.push(...S):S&&y.push(S),f=_+w[0].length,g=!0),!p.global)break;w=p.exec(c.value)}return g?(f<c.value.length&&y.push({type:"text",value:c.value.slice(f)}),u.children.splice(P,1,...y)):y=[c],P+y.length}}function yP(e){const n=[];if(!Array.isArray(e))throw new TypeError("Expected find and replace tuple or list of tuples");const t=!e[0]||Array.isArray(e[0])?e:[e];let r=-1;for(;++r<t.length;){const a=t[r];n.push([wP(a[0]),vP(a[1])])}return n}function wP(e){return typeof e=="string"?new RegExp(gP(e),"g"):e}function vP(e){return typeof e=="function"?e:function(){return e}}const lo="phrasing",co=["autolink","link","image","label"];function kP(){return{transforms:[CP],enter:{literalAutolink:SP,literalAutolinkEmail:uo,literalAutolinkHttp:uo,literalAutolinkWww:uo},exit:{literalAutolink:EP,literalAutolinkEmail:_P,literalAutolinkHttp:xP,literalAutolinkWww:PP}}}function bP(){return{unsafe:[{character:"@",before:"[+\\-.\\w]",after:"[\\-.\\w]",inConstruct:lo,notInConstruct:co},{character:".",before:"[Ww]",after:"[\\-.\\w]",inConstruct:lo,notInConstruct:co},{character:":",before:"[ps]",after:"\\/",inConstruct:lo,notInConstruct:co}]}}function SP(e){this.enter({type:"link",title:null,url:"",children:[]},e)}function uo(e){this.config.enter.autolinkProtocol.call(this,e)}function xP(e){this.config.exit.autolinkProtocol.call(this,e)}function PP(e){this.config.exit.data.call(this,e);const n=this.stack[this.stack.length-1];n.type,n.url="http://"+this.sliceSerialize(e)}function _P(e){this.config.exit.autolinkEmail.call(this,e)}function EP(e){this.exit(e)}function CP(e){fP(e,[[/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi,AP],[new RegExp("(?<=^|\\s|\\p{P}|\\p{S})([-.\\w+]+)@([-\\w]+(?:\\.[-\\w]+)+)","gu"),IP]],{ignore:["link","linkReference"]})}function AP(e,n,t,r,a){let s="";if(!yg(a)||(/^w/i.test(n)&&(t=n+t,n="",s="http://"),!TP(t)))return!1;const i=RP(t+r);if(!i[0])return!1;const o={type:"link",title:null,url:s+n+i[0],children:[{type:"text",value:n+i[0]}]};return i[1]?[o,{type:"text",value:i[1]}]:o}function IP(e,n,t,r){return!yg(r,!0)||/[-\d_]$/.test(t)?!1:{type:"link",title:null,url:"mailto:"+n+"@"+t,children:[{type:"text",value:n+"@"+t}]}}function TP(e){const n=e.split(".");return!(n.length<2||n[n.length-1]&&(/_/.test(n[n.length-1])||!/[a-zA-Z\d]/.test(n[n.length-1]))||n[n.length-2]&&(/_/.test(n[n.length-2])||!/[a-zA-Z\d]/.test(n[n.length-2])))}function RP(e){const n=/[!"&'),.:;<>?\]}]+$/.exec(e);if(!n)return[e,void 0];e=e.slice(0,n.index);let t=n[0],r=t.indexOf(")");const a=Xd(e,"(");let s=Xd(e,")");for(;r!==-1&&a>s;)e+=t.slice(0,r+1),t=t.slice(r+1),r=t.indexOf(")"),s++;return[e,t]}function yg(e,n){const t=e.input.charCodeAt(e.index-1);return(e.index===0||Bt(t)||mi(t))&&(!n||t!==47)}wg.peek=FP;function MP(){this.buffer()}function DP(e){this.enter({type:"footnoteReference",identifier:"",label:""},e)}function BP(){this.buffer()}function LP(e){this.enter({type:"footnoteDefinition",identifier:"",label:"",children:[]},e)}function NP(e){const n=this.resume(),t=this.stack[this.stack.length-1];t.type,t.identifier=Sn(this.sliceSerialize(e)).toLowerCase(),t.label=n}function KP(e){this.exit(e)}function WP(e){const n=this.resume(),t=this.stack[this.stack.length-1];t.type,t.identifier=Sn(this.sliceSerialize(e)).toLowerCase(),t.label=n}function jP(e){this.exit(e)}function FP(){return"["}function wg(e,n,t,r){const a=t.createTracker(r);let s=a.move("[^");const i=t.enter("footnoteReference"),o=t.enter("reference");return s+=a.move(t.safe(t.associationId(e),{after:"]",before:s})),o(),i(),s+=a.move("]"),s}function UP(){return{enter:{gfmFootnoteCallString:MP,gfmFootnoteCall:DP,gfmFootnoteDefinitionLabelString:BP,gfmFootnoteDefinition:LP},exit:{gfmFootnoteCallString:NP,gfmFootnoteCall:KP,gfmFootnoteDefinitionLabelString:WP,gfmFootnoteDefinition:jP}}}function zP(e){let n=!1;return e&&e.firstLineBlank&&(n=!0),{handlers:{footnoteDefinition:t,footnoteReference:wg},unsafe:[{character:"[",inConstruct:["label","phrasing","reference"]}]};function t(r,a,s,i){const o=s.createTracker(i);let l=o.move("[^");const c=s.enter("footnoteDefinition"),d=s.enter("label");return l+=o.move(s.safe(s.associationId(r),{before:l,after:"]"})),d(),l+=o.move("]:"),r.children&&r.children.length>0&&(o.shift(4),l+=o.move((n?`
`:" ")+s.indentLines(s.containerFlow(r,o.current()),n?vg:OP))),c(),l}}function OP(e,n,t){return n===0?e:vg(e,n,t)}function vg(e,n,t){return(t?"":"    ")+e}const HP=["autolink","destinationLiteral","destinationRaw","reference","titleQuote","titleApostrophe"];kg.peek=QP;function qP(){return{canContainEols:["delete"],enter:{strikethrough:GP},exit:{strikethrough:$P}}}function VP(){return{unsafe:[{character:"~",inConstruct:"phrasing",notInConstruct:HP}],handlers:{delete:kg}}}function GP(e){this.enter({type:"delete",children:[]},e)}function $P(e){this.exit(e)}function kg(e,n,t,r){const a=t.createTracker(r),s=t.enter("strikethrough");let i=a.move("~~");return i+=t.containerPhrasing(e,{...a.current(),before:i,after:"~"}),i+=a.move("~~"),s(),i}function QP(){return"~"}function JP(e){return e.length}function YP(e,n){const t=n||{},r=(t.align||[]).concat(),a=t.stringLength||JP,s=[],i=[],o=[],l=[];let c=0,d=-1;for(;++d<e.length;){const k=[],P=[];let g=-1;for(e[d].length>c&&(c=e[d].length);++g<e[d].length;){const y=XP(e[d][g]);if(t.alignDelimiters!==!1){const w=a(y);P[g]=w,(l[g]===void 0||w>l[g])&&(l[g]=w)}k.push(y)}i[d]=k,o[d]=P}let u=-1;if(typeof r=="object"&&"length"in r)for(;++u<c;)s[u]=Zd(r[u]);else{const k=Zd(r);for(;++u<c;)s[u]=k}u=-1;const p=[],m=[];for(;++u<c;){const k=s[u];let P="",g="";k===99?(P=":",g=":"):k===108?P=":":k===114&&(g=":");let y=t.alignDelimiters===!1?1:Math.max(1,l[u]-P.length-g.length);const w=P+"-".repeat(y)+g;t.alignDelimiters!==!1&&(y=P.length+y+g.length,y>l[u]&&(l[u]=y),m[u]=y),p[u]=w}i.splice(1,0,p),o.splice(1,0,m),d=-1;const f=[];for(;++d<i.length;){const k=i[d],P=o[d];u=-1;const g=[];for(;++u<c;){const y=k[u]||"";let w="",_="";if(t.alignDelimiters!==!1){const E=l[u]-(P[u]||0),S=s[u];S===114?w=" ".repeat(E):S===99?E%2?(w=" ".repeat(E/2+.5),_=" ".repeat(E/2-.5)):(w=" ".repeat(E/2),_=w):_=" ".repeat(E)}t.delimiterStart!==!1&&!u&&g.push("|"),t.padding!==!1&&!(t.alignDelimiters===!1&&y==="")&&(t.delimiterStart!==!1||u)&&g.push(" "),t.alignDelimiters!==!1&&g.push(w),g.push(y),t.alignDelimiters!==!1&&g.push(_),t.padding!==!1&&g.push(" "),(t.delimiterEnd!==!1||u!==c-1)&&g.push("|")}f.push(t.delimiterEnd===!1?g.join("").replace(/ +$/,""):g.join(""))}return f.join(`
`)}function XP(e){return e==null?"":String(e)}function Zd(e){const n=typeof e=="string"?e.codePointAt(0):0;return n===67||n===99?99:n===76||n===108?108:n===82||n===114?114:0}function ZP(e,n,t,r){const a=t.enter("blockquote"),s=t.createTracker(r);s.move("> "),s.shift(2);const i=t.indentLines(t.containerFlow(e,s.current()),e_);return a(),i}function e_(e,n,t){return">"+(t?"":" ")+e}function n_(e,n){return eh(e,n.inConstruct,!0)&&!eh(e,n.notInConstruct,!1)}function eh(e,n,t){if(typeof n=="string"&&(n=[n]),!n||n.length===0)return t;let r=-1;for(;++r<n.length;)if(e.includes(n[r]))return!0;return!1}function nh(e,n,t,r){let a=-1;for(;++a<t.unsafe.length;)if(t.unsafe[a].character===`
`&&n_(t.stack,t.unsafe[a]))return/[ \t]/.test(r.before)?"":" ";return`\\
`}function t_(e,n){const t=String(e);let r=t.indexOf(n),a=r,s=0,i=0;if(typeof n!="string")throw new TypeError("Expected substring");for(;r!==-1;)r===a?++s>i&&(i=s):s=1,a=r+n.length,r=t.indexOf(n,a);return i}function r_(e,n){return!!(n.options.fences===!1&&e.value&&!e.lang&&/[^ \r\n]/.test(e.value)&&!/^[\t ]*(?:[\r\n]|$)|(?:^|[\r\n])[\t ]*$/.test(e.value))}function a_(e){const n=e.options.fence||"`";if(n!=="`"&&n!=="~")throw new Error("Cannot serialize code with `"+n+"` for `options.fence`, expected `` ` `` or `~`");return n}function s_(e,n,t,r){const a=a_(t),s=e.value||"",i=a==="`"?"GraveAccent":"Tilde";if(r_(e,t)){const u=t.enter("codeIndented"),p=t.indentLines(s,i_);return u(),p}const o=t.createTracker(r),l=a.repeat(Math.max(t_(s,a)+1,3)),c=t.enter("codeFenced");let d=o.move(l);if(e.lang){const u=t.enter(`codeFencedLang${i}`);d+=o.move(t.safe(e.lang,{before:d,after:" ",encode:["`"],...o.current()})),u()}if(e.lang&&e.meta){const u=t.enter(`codeFencedMeta${i}`);d+=o.move(" "),d+=o.move(t.safe(e.meta,{before:d,after:`
`,encode:["`"],...o.current()})),u()}return d+=o.move(`
`),s&&(d+=o.move(s+`
`)),d+=o.move(l),c(),d}function i_(e,n,t){return(t?"":"    ")+e}function Uc(e){const n=e.options.quote||'"';if(n!=='"'&&n!=="'")throw new Error("Cannot serialize title with `"+n+"` for `options.quote`, expected `\"`, or `'`");return n}function o_(e,n,t,r){const a=Uc(t),s=a==='"'?"Quote":"Apostrophe",i=t.enter("definition");let o=t.enter("label");const l=t.createTracker(r);let c=l.move("[");return c+=l.move(t.safe(t.associationId(e),{before:c,after:"]",...l.current()})),c+=l.move("]: "),o(),!e.url||/[\0- \u007F]/.test(e.url)?(o=t.enter("destinationLiteral"),c+=l.move("<"),c+=l.move(t.safe(e.url,{before:c,after:">",...l.current()})),c+=l.move(">")):(o=t.enter("destinationRaw"),c+=l.move(t.safe(e.url,{before:c,after:e.title?" ":`
`,...l.current()}))),o(),e.title&&(o=t.enter(`title${s}`),c+=l.move(" "+a),c+=l.move(t.safe(e.title,{before:c,after:a,...l.current()})),c+=l.move(a),o()),i(),c}function l_(e){const n=e.options.emphasis||"*";if(n!=="*"&&n!=="_")throw new Error("Cannot serialize emphasis with `"+n+"` for `options.emphasis`, expected `*`, or `_`");return n}function xa(e){return"&#x"+e.toString(16).toUpperCase()+";"}function Gs(e,n,t){const r=fr(e),a=fr(n);return r===void 0?a===void 0?t==="_"?{inside:!0,outside:!0}:{inside:!1,outside:!1}:a===1?{inside:!0,outside:!0}:{inside:!1,outside:!0}:r===1?a===void 0?{inside:!1,outside:!1}:a===1?{inside:!0,outside:!0}:{inside:!1,outside:!1}:a===void 0?{inside:!1,outside:!1}:a===1?{inside:!0,outside:!1}:{inside:!1,outside:!1}}bg.peek=c_;function bg(e,n,t,r){const a=l_(t),s=t.enter("emphasis"),i=t.createTracker(r),o=i.move(a);let l=i.move(t.containerPhrasing(e,{after:a,before:o,...i.current()}));const c=l.charCodeAt(0),d=Gs(r.before.charCodeAt(r.before.length-1),c,a);d.inside&&(l=xa(c)+l.slice(1));const u=l.charCodeAt(l.length-1),p=Gs(r.after.charCodeAt(0),u,a);p.inside&&(l=l.slice(0,-1)+xa(u));const m=i.move(a);return s(),t.attentionEncodeSurroundingInfo={after:p.outside,before:d.outside},o+l+m}function c_(e,n,t){return t.options.emphasis||"*"}function u_(e,n){let t=!1;return jc(e,function(r){if("value"in r&&/\r?\n|\r/.test(r.value)||r.type==="break")return t=!0,yl}),!!((!e.depth||e.depth<3)&&Mc(e)&&(n.options.setext||t))}function d_(e,n,t,r){const a=Math.max(Math.min(6,e.depth||1),1),s=t.createTracker(r);if(u_(e,t)){const d=t.enter("headingSetext"),u=t.enter("phrasing"),p=t.containerPhrasing(e,{...s.current(),before:`
`,after:`
`});return u(),d(),p+`
`+(a===1?"=":"-").repeat(p.length-(Math.max(p.lastIndexOf("\r"),p.lastIndexOf(`
`))+1))}const i="#".repeat(a),o=t.enter("headingAtx"),l=t.enter("phrasing");s.move(i+" ");let c=t.containerPhrasing(e,{before:"# ",after:`
`,...s.current()});return/^[\t ]/.test(c)&&(c=xa(c.charCodeAt(0))+c.slice(1)),c=c?i+" "+c:i,t.options.closeAtx&&(c+=" "+i),l(),o(),c}Sg.peek=h_;function Sg(e){return e.value||""}function h_(){return"<"}xg.peek=p_;function xg(e,n,t,r){const a=Uc(t),s=a==='"'?"Quote":"Apostrophe",i=t.enter("image");let o=t.enter("label");const l=t.createTracker(r);let c=l.move("![");return c+=l.move(t.safe(e.alt,{before:c,after:"]",...l.current()})),c+=l.move("]("),o(),!e.url&&e.title||/[\0- \u007F]/.test(e.url)?(o=t.enter("destinationLiteral"),c+=l.move("<"),c+=l.move(t.safe(e.url,{before:c,after:">",...l.current()})),c+=l.move(">")):(o=t.enter("destinationRaw"),c+=l.move(t.safe(e.url,{before:c,after:e.title?" ":")",...l.current()}))),o(),e.title&&(o=t.enter(`title${s}`),c+=l.move(" "+a),c+=l.move(t.safe(e.title,{before:c,after:a,...l.current()})),c+=l.move(a),o()),c+=l.move(")"),i(),c}function p_(){return"!"}Pg.peek=m_;function Pg(e,n,t,r){const a=e.referenceType,s=t.enter("imageReference");let i=t.enter("label");const o=t.createTracker(r);let l=o.move("![");const c=t.safe(e.alt,{before:l,after:"]",...o.current()});l+=o.move(c+"]["),i();const d=t.stack;t.stack=[],i=t.enter("reference");const u=t.safe(t.associationId(e),{before:l,after:"]",...o.current()});return i(),t.stack=d,s(),a==="full"||!c||c!==u?l+=o.move(u+"]"):a==="shortcut"?l=l.slice(0,-1):l+=o.move("]"),l}function m_(){return"!"}_g.peek=g_;function _g(e,n,t){let r=e.value||"",a="`",s=-1;for(;new RegExp("(^|[^`])"+a+"([^`]|$)").test(r);)a+="`";for(/[^ \r\n]/.test(r)&&(/^[ \r\n]/.test(r)&&/[ \r\n]$/.test(r)||/^`|`$/.test(r))&&(r=" "+r+" ");++s<t.unsafe.length;){const i=t.unsafe[s],o=t.compilePattern(i);let l;if(i.atBreak)for(;l=o.exec(r);){let c=l.index;r.charCodeAt(c)===10&&r.charCodeAt(c-1)===13&&c--,r=r.slice(0,c)+" "+r.slice(l.index+1)}}return a+r+a}function g_(){return"`"}function Eg(e,n){const t=Mc(e);return!!(!n.options.resourceLink&&e.url&&!e.title&&e.children&&e.children.length===1&&e.children[0].type==="text"&&(t===e.url||"mailto:"+t===e.url)&&/^[a-z][a-z+.-]+:/i.test(e.url)&&!/[\0- <>\u007F]/.test(e.url))}Cg.peek=f_;function Cg(e,n,t,r){const a=Uc(t),s=a==='"'?"Quote":"Apostrophe",i=t.createTracker(r);let o,l;if(Eg(e,t)){const d=t.stack;t.stack=[],o=t.enter("autolink");let u=i.move("<");return u+=i.move(t.containerPhrasing(e,{before:u,after:">",...i.current()})),u+=i.move(">"),o(),t.stack=d,u}o=t.enter("link"),l=t.enter("label");let c=i.move("[");return c+=i.move(t.containerPhrasing(e,{before:c,after:"](",...i.current()})),c+=i.move("]("),l(),!e.url&&e.title||/[\0- \u007F]/.test(e.url)?(l=t.enter("destinationLiteral"),c+=i.move("<"),c+=i.move(t.safe(e.url,{before:c,after:">",...i.current()})),c+=i.move(">")):(l=t.enter("destinationRaw"),c+=i.move(t.safe(e.url,{before:c,after:e.title?" ":")",...i.current()}))),l(),e.title&&(l=t.enter(`title${s}`),c+=i.move(" "+a),c+=i.move(t.safe(e.title,{before:c,after:a,...i.current()})),c+=i.move(a),l()),c+=i.move(")"),o(),c}function f_(e,n,t){return Eg(e,t)?"<":"["}Ag.peek=y_;function Ag(e,n,t,r){const a=e.referenceType,s=t.enter("linkReference");let i=t.enter("label");const o=t.createTracker(r);let l=o.move("[");const c=t.containerPhrasing(e,{before:l,after:"]",...o.current()});l+=o.move(c+"]["),i();const d=t.stack;t.stack=[],i=t.enter("reference");const u=t.safe(t.associationId(e),{before:l,after:"]",...o.current()});return i(),t.stack=d,s(),a==="full"||!c||c!==u?l+=o.move(u+"]"):a==="shortcut"?l=l.slice(0,-1):l+=o.move("]"),l}function y_(){return"["}function zc(e){const n=e.options.bullet||"*";if(n!=="*"&&n!=="+"&&n!=="-")throw new Error("Cannot serialize items with `"+n+"` for `options.bullet`, expected `*`, `+`, or `-`");return n}function w_(e){const n=zc(e),t=e.options.bulletOther;if(!t)return n==="*"?"-":"*";if(t!=="*"&&t!=="+"&&t!=="-")throw new Error("Cannot serialize items with `"+t+"` for `options.bulletOther`, expected `*`, `+`, or `-`");if(t===n)throw new Error("Expected `bullet` (`"+n+"`) and `bulletOther` (`"+t+"`) to be different");return t}function v_(e){const n=e.options.bulletOrdered||".";if(n!=="."&&n!==")")throw new Error("Cannot serialize items with `"+n+"` for `options.bulletOrdered`, expected `.` or `)`");return n}function Ig(e){const n=e.options.rule||"*";if(n!=="*"&&n!=="-"&&n!=="_")throw new Error("Cannot serialize rules with `"+n+"` for `options.rule`, expected `*`, `-`, or `_`");return n}function k_(e,n,t,r){const a=t.enter("list"),s=t.bulletCurrent;let i=e.ordered?v_(t):zc(t);const o=e.ordered?i==="."?")":".":w_(t);let l=n&&t.bulletLastUsed?i===t.bulletLastUsed:!1;if(!e.ordered){const d=e.children?e.children[0]:void 0;if((i==="*"||i==="-")&&d&&(!d.children||!d.children[0])&&t.stack[t.stack.length-1]==="list"&&t.stack[t.stack.length-2]==="listItem"&&t.stack[t.stack.length-3]==="list"&&t.stack[t.stack.length-4]==="listItem"&&t.indexStack[t.indexStack.length-1]===0&&t.indexStack[t.indexStack.length-2]===0&&t.indexStack[t.indexStack.length-3]===0&&(l=!0),Ig(t)===i&&d){let u=-1;for(;++u<e.children.length;){const p=e.children[u];if(p&&p.type==="listItem"&&p.children&&p.children[0]&&p.children[0].type==="thematicBreak"){l=!0;break}}}}l&&(i=o),t.bulletCurrent=i;const c=t.containerFlow(e,r);return t.bulletLastUsed=i,t.bulletCurrent=s,a(),c}function b_(e){const n=e.options.listItemIndent||"one";if(n!=="tab"&&n!=="one"&&n!=="mixed")throw new Error("Cannot serialize items with `"+n+"` for `options.listItemIndent`, expected `tab`, `one`, or `mixed`");return n}function S_(e,n,t,r){const a=b_(t);let s=t.bulletCurrent||zc(t);n&&n.type==="list"&&n.ordered&&(s=(typeof n.start=="number"&&n.start>-1?n.start:1)+(t.options.incrementListMarker===!1?0:n.children.indexOf(e))+s);let i=s.length+1;(a==="tab"||a==="mixed"&&(n&&n.type==="list"&&n.spread||e.spread))&&(i=Math.ceil(i/4)*4);const o=t.createTracker(r);o.move(s+" ".repeat(i-s.length)),o.shift(i);const l=t.enter("listItem"),c=t.indentLines(t.containerFlow(e,o.current()),d);return l(),c;function d(u,p,m){return p?(m?"":" ".repeat(i))+u:(m?s:s+" ".repeat(i-s.length))+u}}function x_(e,n,t,r){const a=t.enter("paragraph"),s=t.enter("phrasing"),i=t.containerPhrasing(e,r);return s(),a(),i}const P_=yi(["break","delete","emphasis","footnote","footnoteReference","image","imageReference","inlineCode","inlineMath","link","linkReference","mdxJsxTextElement","mdxTextExpression","strong","text","textDirective"]);function __(e,n,t,r){return(e.children.some(function(i){return P_(i)})?t.containerPhrasing:t.containerFlow).call(t,e,r)}function E_(e){const n=e.options.strong||"*";if(n!=="*"&&n!=="_")throw new Error("Cannot serialize strong with `"+n+"` for `options.strong`, expected `*`, or `_`");return n}Tg.peek=C_;function Tg(e,n,t,r){const a=E_(t),s=t.enter("strong"),i=t.createTracker(r),o=i.move(a+a);let l=i.move(t.containerPhrasing(e,{after:a,before:o,...i.current()}));const c=l.charCodeAt(0),d=Gs(r.before.charCodeAt(r.before.length-1),c,a);d.inside&&(l=xa(c)+l.slice(1));const u=l.charCodeAt(l.length-1),p=Gs(r.after.charCodeAt(0),u,a);p.inside&&(l=l.slice(0,-1)+xa(u));const m=i.move(a+a);return s(),t.attentionEncodeSurroundingInfo={after:p.outside,before:d.outside},o+l+m}function C_(e,n,t){return t.options.strong||"*"}function A_(e,n,t,r){return t.safe(e.value,r)}function I_(e){const n=e.options.ruleRepetition||3;if(n<3)throw new Error("Cannot serialize rules with repetition `"+n+"` for `options.ruleRepetition`, expected `3` or more");return n}function T_(e,n,t){const r=(Ig(t)+(t.options.ruleSpaces?" ":"")).repeat(I_(t));return t.options.ruleSpaces?r.slice(0,-1):r}const Rg={blockquote:ZP,break:nh,code:s_,definition:o_,emphasis:bg,hardBreak:nh,heading:d_,html:Sg,image:xg,imageReference:Pg,inlineCode:_g,link:Cg,linkReference:Ag,list:k_,listItem:S_,paragraph:x_,root:__,strong:Tg,text:A_,thematicBreak:T_};function R_(){return{enter:{table:M_,tableData:th,tableHeader:th,tableRow:B_},exit:{codeText:L_,table:D_,tableData:ho,tableHeader:ho,tableRow:ho}}}function M_(e){const n=e._align;this.enter({type:"table",align:n.map(function(t){return t==="none"?null:t}),children:[]},e),this.data.inTable=!0}function D_(e){this.exit(e),this.data.inTable=void 0}function B_(e){this.enter({type:"tableRow",children:[]},e)}function ho(e){this.exit(e)}function th(e){this.enter({type:"tableCell",children:[]},e)}function L_(e){let n=this.resume();this.data.inTable&&(n=n.replace(/\\([\\|])/g,N_));const t=this.stack[this.stack.length-1];t.type,t.value=n,this.exit(e)}function N_(e,n){return n==="|"?n:e}function K_(e){const n=e||{},t=n.tableCellPadding,r=n.tablePipeAlign,a=n.stringLength,s=t?" ":"|";return{unsafe:[{character:"\r",inConstruct:"tableCell"},{character:`
`,inConstruct:"tableCell"},{atBreak:!0,character:"|",after:"[	 :-]"},{character:"|",inConstruct:"tableCell"},{atBreak:!0,character:":",after:"-"},{atBreak:!0,character:"-",after:"[:|-]"}],handlers:{inlineCode:p,table:i,tableCell:l,tableRow:o}};function i(m,f,k,P){return c(d(m,k,P),m.align)}function o(m,f,k,P){const g=u(m,k,P),y=c([g]);return y.slice(0,y.indexOf(`
`))}function l(m,f,k,P){const g=k.enter("tableCell"),y=k.enter("phrasing"),w=k.containerPhrasing(m,{...P,before:s,after:s});return y(),g(),w}function c(m,f){return YP(m,{align:f,alignDelimiters:r,padding:t,stringLength:a})}function d(m,f,k){const P=m.children;let g=-1;const y=[],w=f.enter("table");for(;++g<P.length;)y[g]=u(P[g],f,k);return w(),y}function u(m,f,k){const P=m.children;let g=-1;const y=[],w=f.enter("tableRow");for(;++g<P.length;)y[g]=l(P[g],m,f,k);return w(),y}function p(m,f,k){let P=Rg.inlineCode(m,f,k);return k.stack.includes("tableCell")&&(P=P.replace(/\|/g,"\\$&")),P}}function W_(){return{exit:{taskListCheckValueChecked:rh,taskListCheckValueUnchecked:rh,paragraph:F_}}}function j_(){return{unsafe:[{atBreak:!0,character:"-",after:"[:|-]"}],handlers:{listItem:U_}}}function rh(e){const n=this.stack[this.stack.length-2];n.type,n.checked=e.type==="taskListCheckValueChecked"}function F_(e){const n=this.stack[this.stack.length-2];if(n&&n.type==="listItem"&&typeof n.checked=="boolean"){const t=this.stack[this.stack.length-1];t.type;const r=t.children[0];if(r&&r.type==="text"){const a=n.children;let s=-1,i;for(;++s<a.length;){const o=a[s];if(o.type==="paragraph"){i=o;break}}i===t&&(r.value=r.value.slice(1),r.value.length===0?t.children.shift():t.position&&r.position&&typeof r.position.start.offset=="number"&&(r.position.start.column++,r.position.start.offset++,t.position.start=Object.assign({},r.position.start)))}}this.exit(e)}function U_(e,n,t,r){const a=e.children[0],s=typeof e.checked=="boolean"&&a&&a.type==="paragraph",i="["+(e.checked?"x":" ")+"] ",o=t.createTracker(r);s&&o.move(i);let l=Rg.listItem(e,n,t,{...r,...o.current()});return s&&(l=l.replace(/^(?:[*+-]|\d+\.)([\r\n]| {1,3})/,c)),l;function c(d){return d+i}}function z_(){return[kP(),UP(),qP(),R_(),W_()]}function O_(e){return{extensions:[bP(),zP(e),VP(),K_(e),j_()]}}const H_={tokenize:J_,partial:!0},Mg={tokenize:Y_,partial:!0},Dg={tokenize:X_,partial:!0},Bg={tokenize:Z_,partial:!0},q_={tokenize:eE,partial:!0},Lg={name:"wwwAutolink",tokenize:$_,previous:Kg},Ng={name:"protocolAutolink",tokenize:Q_,previous:Wg},Hn={name:"emailAutolink",tokenize:G_,previous:jg},Tn={};function V_(){return{text:Tn}}let kt=48;for(;kt<123;)Tn[kt]=Hn,kt++,kt===58?kt=65:kt===91&&(kt=97);Tn[43]=Hn;Tn[45]=Hn;Tn[46]=Hn;Tn[95]=Hn;Tn[72]=[Hn,Ng];Tn[104]=[Hn,Ng];Tn[87]=[Hn,Lg];Tn[119]=[Hn,Lg];function G_(e,n,t){const r=this;let a,s;return i;function i(u){return!bl(u)||!jg.call(r,r.previous)||Oc(r.events)?t(u):(e.enter("literalAutolink"),e.enter("literalAutolinkEmail"),o(u))}function o(u){return bl(u)?(e.consume(u),o):u===64?(e.consume(u),l):t(u)}function l(u){return u===46?e.check(q_,d,c)(u):u===45||u===95||Me(u)?(s=!0,e.consume(u),l):d(u)}function c(u){return e.consume(u),a=!0,l}function d(u){return s&&a&&Ke(r.previous)?(e.exit("literalAutolinkEmail"),e.exit("literalAutolink"),n(u)):t(u)}}function $_(e,n,t){const r=this;return a;function a(i){return i!==87&&i!==119||!Kg.call(r,r.previous)||Oc(r.events)?t(i):(e.enter("literalAutolink"),e.enter("literalAutolinkWww"),e.check(H_,e.attempt(Mg,e.attempt(Dg,s),t),t)(i))}function s(i){return e.exit("literalAutolinkWww"),e.exit("literalAutolink"),n(i)}}function Q_(e,n,t){const r=this;let a="",s=!1;return i;function i(u){return(u===72||u===104)&&Wg.call(r,r.previous)&&!Oc(r.events)?(e.enter("literalAutolink"),e.enter("literalAutolinkHttp"),a+=String.fromCodePoint(u),e.consume(u),o):t(u)}function o(u){if(Ke(u)&&a.length<5)return a+=String.fromCodePoint(u),e.consume(u),o;if(u===58){const p=a.toLowerCase();if(p==="http"||p==="https")return e.consume(u),l}return t(u)}function l(u){return u===47?(e.consume(u),s?c:(s=!0,l)):t(u)}function c(u){return u===null||Hs(u)||ae(u)||Bt(u)||mi(u)?t(u):e.attempt(Mg,e.attempt(Dg,d),t)(u)}function d(u){return e.exit("literalAutolinkHttp"),e.exit("literalAutolink"),n(u)}}function J_(e,n,t){let r=0;return a;function a(i){return(i===87||i===119)&&r<3?(r++,e.consume(i),a):i===46&&r===3?(e.consume(i),s):t(i)}function s(i){return i===null?t(i):n(i)}}function Y_(e,n,t){let r,a,s;return i;function i(c){return c===46||c===95?e.check(Bg,l,o)(c):c===null||ae(c)||Bt(c)||c!==45&&mi(c)?l(c):(s=!0,e.consume(c),i)}function o(c){return c===95?r=!0:(a=r,r=void 0),e.consume(c),i}function l(c){return a||r||!s?t(c):n(c)}}function X_(e,n){let t=0,r=0;return a;function a(i){return i===40?(t++,e.consume(i),a):i===41&&r<t?s(i):i===33||i===34||i===38||i===39||i===41||i===42||i===44||i===46||i===58||i===59||i===60||i===63||i===93||i===95||i===126?e.check(Bg,n,s)(i):i===null||ae(i)||Bt(i)?n(i):(e.consume(i),a)}function s(i){return i===41&&r++,e.consume(i),a}}function Z_(e,n,t){return r;function r(o){return o===33||o===34||o===39||o===41||o===42||o===44||o===46||o===58||o===59||o===63||o===95||o===126?(e.consume(o),r):o===38?(e.consume(o),s):o===93?(e.consume(o),a):o===60||o===null||ae(o)||Bt(o)?n(o):t(o)}function a(o){return o===null||o===40||o===91||ae(o)||Bt(o)?n(o):r(o)}function s(o){return Ke(o)?i(o):t(o)}function i(o){return o===59?(e.consume(o),r):Ke(o)?(e.consume(o),i):t(o)}}function eE(e,n,t){return r;function r(s){return e.consume(s),a}function a(s){return Me(s)?t(s):n(s)}}function Kg(e){return e===null||e===40||e===42||e===95||e===91||e===93||e===126||ae(e)}function Wg(e){return!Ke(e)}function jg(e){return!(e===47||bl(e))}function bl(e){return e===43||e===45||e===46||e===95||Me(e)}function Oc(e){let n=e.length,t=!1;for(;n--;){const r=e[n][1];if((r.type==="labelLink"||r.type==="labelImage")&&!r._balanced){t=!0;break}if(r._gfmAutolinkLiteralWalkedInto){t=!1;break}}return e.length>0&&!t&&(e[e.length-1][1]._gfmAutolinkLiteralWalkedInto=!0),t}const nE={tokenize:cE,partial:!0};function tE(){return{document:{91:{name:"gfmFootnoteDefinition",tokenize:iE,continuation:{tokenize:oE},exit:lE}},text:{91:{name:"gfmFootnoteCall",tokenize:sE},93:{name:"gfmPotentialFootnoteCall",add:"after",tokenize:rE,resolveTo:aE}}}}function rE(e,n,t){const r=this;let a=r.events.length;const s=r.parser.gfmFootnotes||(r.parser.gfmFootnotes=[]);let i;for(;a--;){const l=r.events[a][1];if(l.type==="labelImage"){i=l;break}if(l.type==="gfmFootnoteCall"||l.type==="labelLink"||l.type==="label"||l.type==="image"||l.type==="link")break}return o;function o(l){if(!i||!i._balanced)return t(l);const c=Sn(r.sliceSerialize({start:i.end,end:r.now()}));return c.codePointAt(0)!==94||!s.includes(c.slice(1))?t(l):(e.enter("gfmFootnoteCallLabelMarker"),e.consume(l),e.exit("gfmFootnoteCallLabelMarker"),n(l))}}function aE(e,n){let t=e.length;for(;t--;)if(e[t][1].type==="labelImage"&&e[t][0]==="enter"){e[t][1];break}e[t+1][1].type="data",e[t+3][1].type="gfmFootnoteCallLabelMarker";const r={type:"gfmFootnoteCall",start:Object.assign({},e[t+3][1].start),end:Object.assign({},e[e.length-1][1].end)},a={type:"gfmFootnoteCallMarker",start:Object.assign({},e[t+3][1].end),end:Object.assign({},e[t+3][1].end)};a.end.column++,a.end.offset++,a.end._bufferIndex++;const s={type:"gfmFootnoteCallString",start:Object.assign({},a.end),end:Object.assign({},e[e.length-1][1].start)},i={type:"chunkString",contentType:"string",start:Object.assign({},s.start),end:Object.assign({},s.end)},o=[e[t+1],e[t+2],["enter",r,n],e[t+3],e[t+4],["enter",a,n],["exit",a,n],["enter",s,n],["enter",i,n],["exit",i,n],["exit",s,n],e[e.length-2],e[e.length-1],["exit",r,n]];return e.splice(t,e.length-t+1,...o),e}function sE(e,n,t){const r=this,a=r.parser.gfmFootnotes||(r.parser.gfmFootnotes=[]);let s=0,i;return o;function o(u){return e.enter("gfmFootnoteCall"),e.enter("gfmFootnoteCallLabelMarker"),e.consume(u),e.exit("gfmFootnoteCallLabelMarker"),l}function l(u){return u!==94?t(u):(e.enter("gfmFootnoteCallMarker"),e.consume(u),e.exit("gfmFootnoteCallMarker"),e.enter("gfmFootnoteCallString"),e.enter("chunkString").contentType="string",c)}function c(u){if(s>999||u===93&&!i||u===null||u===91||ae(u))return t(u);if(u===93){e.exit("chunkString");const p=e.exit("gfmFootnoteCallString");return a.includes(Sn(r.sliceSerialize(p)))?(e.enter("gfmFootnoteCallLabelMarker"),e.consume(u),e.exit("gfmFootnoteCallLabelMarker"),e.exit("gfmFootnoteCall"),n):t(u)}return ae(u)||(i=!0),s++,e.consume(u),u===92?d:c}function d(u){return u===91||u===92||u===93?(e.consume(u),s++,c):c(u)}}function iE(e,n,t){const r=this,a=r.parser.gfmFootnotes||(r.parser.gfmFootnotes=[]);let s,i=0,o;return l;function l(f){return e.enter("gfmFootnoteDefinition")._container=!0,e.enter("gfmFootnoteDefinitionLabel"),e.enter("gfmFootnoteDefinitionLabelMarker"),e.consume(f),e.exit("gfmFootnoteDefinitionLabelMarker"),c}function c(f){return f===94?(e.enter("gfmFootnoteDefinitionMarker"),e.consume(f),e.exit("gfmFootnoteDefinitionMarker"),e.enter("gfmFootnoteDefinitionLabelString"),e.enter("chunkString").contentType="string",d):t(f)}function d(f){if(i>999||f===93&&!o||f===null||f===91||ae(f))return t(f);if(f===93){e.exit("chunkString");const k=e.exit("gfmFootnoteDefinitionLabelString");return s=Sn(r.sliceSerialize(k)),e.enter("gfmFootnoteDefinitionLabelMarker"),e.consume(f),e.exit("gfmFootnoteDefinitionLabelMarker"),e.exit("gfmFootnoteDefinitionLabel"),p}return ae(f)||(o=!0),i++,e.consume(f),f===92?u:d}function u(f){return f===91||f===92||f===93?(e.consume(f),i++,d):d(f)}function p(f){return f===58?(e.enter("definitionMarker"),e.consume(f),e.exit("definitionMarker"),a.includes(s)||a.push(s),X(e,m,"gfmFootnoteDefinitionWhitespace")):t(f)}function m(f){return n(f)}}function oE(e,n,t){return e.check(Ra,n,e.attempt(nE,n,t))}function lE(e){e.exit("gfmFootnoteDefinition")}function cE(e,n,t){const r=this;return X(e,a,"gfmFootnoteDefinitionIndent",5);function a(s){const i=r.events[r.events.length-1];return i&&i[1].type==="gfmFootnoteDefinitionIndent"&&i[2].sliceSerialize(i[1],!0).length===4?n(s):t(s)}}function uE(e){let t=(e||{}).singleTilde;const r={name:"strikethrough",tokenize:s,resolveAll:a};return t==null&&(t=!0),{text:{126:r},insideSpan:{null:[r]},attentionMarkers:{null:[126]}};function a(i,o){let l=-1;for(;++l<i.length;)if(i[l][0]==="enter"&&i[l][1].type==="strikethroughSequenceTemporary"&&i[l][1]._close){let c=l;for(;c--;)if(i[c][0]==="exit"&&i[c][1].type==="strikethroughSequenceTemporary"&&i[c][1]._open&&i[l][1].end.offset-i[l][1].start.offset===i[c][1].end.offset-i[c][1].start.offset){i[l][1].type="strikethroughSequence",i[c][1].type="strikethroughSequence";const d={type:"strikethrough",start:Object.assign({},i[c][1].start),end:Object.assign({},i[l][1].end)},u={type:"strikethroughText",start:Object.assign({},i[c][1].end),end:Object.assign({},i[l][1].start)},p=[["enter",d,o],["enter",i[c][1],o],["exit",i[c][1],o],["enter",u,o]],m=o.parser.constructs.insideSpan.null;m&&nn(p,p.length,0,gi(m,i.slice(c+1,l),o)),nn(p,p.length,0,[["exit",u,o],["enter",i[l][1],o],["exit",i[l][1],o],["exit",d,o]]),nn(i,c-1,l-c+3,p),l=c+p.length-2;break}}for(l=-1;++l<i.length;)i[l][1].type==="strikethroughSequenceTemporary"&&(i[l][1].type="data");return i}function s(i,o,l){const c=this.previous,d=this.events;let u=0;return p;function p(f){return c===126&&d[d.length-1][1].type!=="characterEscape"?l(f):(i.enter("strikethroughSequenceTemporary"),m(f))}function m(f){const k=fr(c);if(f===126)return u>1?l(f):(i.consume(f),u++,m);if(u<2&&!t)return l(f);const P=i.exit("strikethroughSequenceTemporary"),g=fr(f);return P._open=!g||g===2&&!!k,P._close=!k||k===2&&!!g,o(f)}}}class dE{constructor(){this.map=[]}add(n,t,r){hE(this,n,t,r)}consume(n){if(this.map.sort(function(s,i){return s[0]-i[0]}),this.map.length===0)return;let t=this.map.length;const r=[];for(;t>0;)t-=1,r.push(n.slice(this.map[t][0]+this.map[t][1]),this.map[t][2]),n.length=this.map[t][0];r.push(n.slice()),n.length=0;let a=r.pop();for(;a;){for(const s of a)n.push(s);a=r.pop()}this.map.length=0}}function hE(e,n,t,r){let a=0;if(!(t===0&&r.length===0)){for(;a<e.map.length;){if(e.map[a][0]===n){e.map[a][1]+=t,e.map[a][2].push(...r);return}a+=1}e.map.push([n,t,r])}}function pE(e,n){let t=!1;const r=[];for(;n<e.length;){const a=e[n];if(t){if(a[0]==="enter")a[1].type==="tableContent"&&r.push(e[n+1][1].type==="tableDelimiterMarker"?"left":"none");else if(a[1].type==="tableContent"){if(e[n-1][1].type==="tableDelimiterMarker"){const s=r.length-1;r[s]=r[s]==="left"?"center":"right"}}else if(a[1].type==="tableDelimiterRow")break}else a[0]==="enter"&&a[1].type==="tableDelimiterRow"&&(t=!0);n+=1}return r}function mE(){return{flow:{null:{name:"table",tokenize:gE,resolveAll:fE}}}}function gE(e,n,t){const r=this;let a=0,s=0,i;return o;function o(x){let L=r.events.length-1;for(;L>-1;){const te=r.events[L][1].type;if(te==="lineEnding"||te==="linePrefix")L--;else break}const W=L>-1?r.events[L][1].type:null,J=W==="tableHead"||W==="tableRow"?S:l;return J===S&&r.parser.lazy[r.now().line]?t(x):J(x)}function l(x){return e.enter("tableHead"),e.enter("tableRow"),c(x)}function c(x){return x===124||(i=!0,s+=1),d(x)}function d(x){return x===null?t(x):U(x)?s>1?(s=0,r.interrupt=!0,e.exit("tableRow"),e.enter("lineEnding"),e.consume(x),e.exit("lineEnding"),m):t(x):$(x)?X(e,d,"whitespace")(x):(s+=1,i&&(i=!1,a+=1),x===124?(e.enter("tableCellDivider"),e.consume(x),e.exit("tableCellDivider"),i=!0,d):(e.enter("data"),u(x)))}function u(x){return x===null||x===124||ae(x)?(e.exit("data"),d(x)):(e.consume(x),x===92?p:u)}function p(x){return x===92||x===124?(e.consume(x),u):u(x)}function m(x){return r.interrupt=!1,r.parser.lazy[r.now().line]?t(x):(e.enter("tableDelimiterRow"),i=!1,$(x)?X(e,f,"linePrefix",r.parser.constructs.disable.null.includes("codeIndented")?void 0:4)(x):f(x))}function f(x){return x===45||x===58?P(x):x===124?(i=!0,e.enter("tableCellDivider"),e.consume(x),e.exit("tableCellDivider"),k):E(x)}function k(x){return $(x)?X(e,P,"whitespace")(x):P(x)}function P(x){return x===58?(s+=1,i=!0,e.enter("tableDelimiterMarker"),e.consume(x),e.exit("tableDelimiterMarker"),g):x===45?(s+=1,g(x)):x===null||U(x)?_(x):E(x)}function g(x){return x===45?(e.enter("tableDelimiterFiller"),y(x)):E(x)}function y(x){return x===45?(e.consume(x),y):x===58?(i=!0,e.exit("tableDelimiterFiller"),e.enter("tableDelimiterMarker"),e.consume(x),e.exit("tableDelimiterMarker"),w):(e.exit("tableDelimiterFiller"),w(x))}function w(x){return $(x)?X(e,_,"whitespace")(x):_(x)}function _(x){return x===124?f(x):x===null||U(x)?!i||a!==s?E(x):(e.exit("tableDelimiterRow"),e.exit("tableHead"),n(x)):E(x)}function E(x){return t(x)}function S(x){return e.enter("tableRow"),A(x)}function A(x){return x===124?(e.enter("tableCellDivider"),e.consume(x),e.exit("tableCellDivider"),A):x===null||U(x)?(e.exit("tableRow"),n(x)):$(x)?X(e,A,"whitespace")(x):(e.enter("data"),M(x))}function M(x){return x===null||x===124||ae(x)?(e.exit("data"),A(x)):(e.consume(x),x===92?j:M)}function j(x){return x===92||x===124?(e.consume(x),M):M(x)}}function fE(e,n){let t=-1,r=!0,a=0,s=[0,0,0,0],i=[0,0,0,0],o=!1,l=0,c,d,u;const p=new dE;for(;++t<e.length;){const m=e[t],f=m[1];m[0]==="enter"?f.type==="tableHead"?(o=!1,l!==0&&(ah(p,n,l,c,d),d=void 0,l=0),c={type:"table",start:Object.assign({},f.start),end:Object.assign({},f.end)},p.add(t,0,[["enter",c,n]])):f.type==="tableRow"||f.type==="tableDelimiterRow"?(r=!0,u=void 0,s=[0,0,0,0],i=[0,t+1,0,0],o&&(o=!1,d={type:"tableBody",start:Object.assign({},f.start),end:Object.assign({},f.end)},p.add(t,0,[["enter",d,n]])),a=f.type==="tableDelimiterRow"?2:d?3:1):a&&(f.type==="data"||f.type==="tableDelimiterMarker"||f.type==="tableDelimiterFiller")?(r=!1,i[2]===0&&(s[1]!==0&&(i[0]=i[1],u=rs(p,n,s,a,void 0,u),s=[0,0,0,0]),i[2]=t)):f.type==="tableCellDivider"&&(r?r=!1:(s[1]!==0&&(i[0]=i[1],u=rs(p,n,s,a,void 0,u)),s=i,i=[s[1],t,0,0])):f.type==="tableHead"?(o=!0,l=t):f.type==="tableRow"||f.type==="tableDelimiterRow"?(l=t,s[1]!==0?(i[0]=i[1],u=rs(p,n,s,a,t,u)):i[1]!==0&&(u=rs(p,n,i,a,t,u)),a=0):a&&(f.type==="data"||f.type==="tableDelimiterMarker"||f.type==="tableDelimiterFiller")&&(i[3]=t)}for(l!==0&&ah(p,n,l,c,d),p.consume(n.events),t=-1;++t<n.events.length;){const m=n.events[t];m[0]==="enter"&&m[1].type==="table"&&(m[1]._align=pE(n.events,t))}return e}function rs(e,n,t,r,a,s){const i=r===1?"tableHeader":r===2?"tableDelimiter":"tableData",o="tableContent";t[0]!==0&&(s.end=Object.assign({},Ot(n.events,t[0])),e.add(t[0],0,[["exit",s,n]]));const l=Ot(n.events,t[1]);if(s={type:i,start:Object.assign({},l),end:Object.assign({},l)},e.add(t[1],0,[["enter",s,n]]),t[2]!==0){const c=Ot(n.events,t[2]),d=Ot(n.events,t[3]),u={type:o,start:Object.assign({},c),end:Object.assign({},d)};if(e.add(t[2],0,[["enter",u,n]]),r!==2){const p=n.events[t[2]],m=n.events[t[3]];if(p[1].end=Object.assign({},m[1].end),p[1].type="chunkText",p[1].contentType="text",t[3]>t[2]+1){const f=t[2]+1,k=t[3]-t[2]-1;e.add(f,k,[])}}e.add(t[3]+1,0,[["exit",u,n]])}return a!==void 0&&(s.end=Object.assign({},Ot(n.events,a)),e.add(a,0,[["exit",s,n]]),s=void 0),s}function ah(e,n,t,r,a){const s=[],i=Ot(n.events,t);a&&(a.end=Object.assign({},i),s.push(["exit",a,n])),r.end=Object.assign({},i),s.push(["exit",r,n]),e.add(t+1,0,s)}function Ot(e,n){const t=e[n],r=t[0]==="enter"?"start":"end";return t[1][r]}const yE={name:"tasklistCheck",tokenize:vE};function wE(){return{text:{91:yE}}}function vE(e,n,t){const r=this;return a;function a(l){return r.previous!==null||!r._gfmTasklistFirstContentOfListItem?t(l):(e.enter("taskListCheck"),e.enter("taskListCheckMarker"),e.consume(l),e.exit("taskListCheckMarker"),s)}function s(l){return ae(l)?(e.enter("taskListCheckValueUnchecked"),e.consume(l),e.exit("taskListCheckValueUnchecked"),i):l===88||l===120?(e.enter("taskListCheckValueChecked"),e.consume(l),e.exit("taskListCheckValueChecked"),i):t(l)}function i(l){return l===93?(e.enter("taskListCheckMarker"),e.consume(l),e.exit("taskListCheckMarker"),e.exit("taskListCheck"),o):t(l)}function o(l){return U(l)?n(l):$(l)?e.check({tokenize:kE},n,t)(l):t(l)}}function kE(e,n,t){return X(e,r,"whitespace");function r(a){return a===null?t(a):n(a)}}function bE(e){return Qm([V_(),tE(),uE(e),mE(),wE()])}const SE={};function xE(e){const n=this,t=e||SE,r=n.data(),a=r.micromarkExtensions||(r.micromarkExtensions=[]),s=r.fromMarkdownExtensions||(r.fromMarkdownExtensions=[]),i=r.toMarkdownExtensions||(r.toMarkdownExtensions=[]);a.push(bE(t)),s.push(z_()),i.push(O_(t))}function PE({markdown:e,className:n}){return h.jsx("div",{className:n?`md-doc ${n}`:"md-doc",children:h.jsx(uP,{remarkPlugins:[xE],components:{a:({href:t,children:r,...a})=>t!=null&&t.startsWith("/")?h.jsx(z,{to:t,...a,children:r}):h.jsx("a",{href:t,target:"_blank",rel:"noopener noreferrer",...a,children:r}),pre:({children:t})=>h.jsx("pre",{className:"md-doc-pre",children:t})},children:e})})}function _E({kind:e,activeSlug:n,basePath:t}){const r=e==="user"?"user guides":"tech guides";return h.jsxs("aside",{className:"docs-sidebar library-sidebar","aria-label":r,children:[h.jsx("p",{className:"docs-sidebar-title",children:h.jsx(z,{to:t,children:r})}),h.jsx("ul",{className:"docs-nav-list library-nav-list",children:cl(e).map(({slug:a,title:s})=>{const i=`${t}/${a}`,o=n===a;return h.jsx("li",{children:h.jsx(z,{to:i,className:o?"docs-nav-link docs-nav-link--active":"docs-nav-link",children:s})},a)})})]})}function Fg({kind:e,basePath:n,activeSlug:t,children:r}){return h.jsxs("div",{className:"docs-layout library-layout",children:[h.jsx(_E,{kind:e,activeSlug:t,basePath:n}),h.jsx("div",{className:"docs-main library-main",children:r})]})}function EE(){return h.jsxs("div",{className:"page-library-hub",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:"library"})]}),h.jsxs("header",{className:"page-header",children:[h.jsx("h1",{children:"markdown library"}),h.jsx("p",{className:"page-lead",children:"full-length reference docs maintained next to development: task-style user coverage and deep technical notes. start from each index or jump in from search."})]}),h.jsxs("ul",{className:"resources-cards",children:[h.jsx("li",{children:h.jsxs(z,{to:"/library/user/readme",className:"resources-card",children:[h.jsx("span",{className:"resources-card-kicker",children:"index + pages"}),h.jsx("span",{className:"resources-card-title",children:"user guides"}),h.jsx("span",{className:"resources-card-body",children:"what the wallet supports per feature: prerequisites, steps, options (no UI wireframes)."})]})}),h.jsx("li",{children:h.jsxs(z,{to:"/library/tech/readme",className:"resources-card",children:[h.jsx("span",{className:"resources-card-kicker",children:"index + pages"}),h.jsx("span",{className:"resources-card-title",children:"tech guides"}),h.jsx("span",{className:"resources-card-body",children:"how chromatika implements flows: crypto, ika, bridge, chrome APIs, integrations."})]})})]}),h.jsxs("p",{className:"resources-prose",children:["contributors: drop updated ",h.jsx("code",{className:"inline-code",children:".md"})," files into"," ",h.jsx("code",{className:"inline-code",children:"src/library/"}),", then run"," ",h.jsx("code",{className:"inline-code",children:"pnpm run sync:library"})," so in-doc links stay routable."]})]})}function Ug({kind:e,basePath:n}){const{slug:t}=Kt(),r=(t??"readme").toLowerCase();if(!xS(e,r))return h.jsx(Ia,{to:e==="user"?"/library/user/readme":"/library/tech/readme",replace:!0});const a=SS(e,r),s=bS(e,r),i=e==="user"?"user guides":"tech guides";return h.jsxs("article",{className:"page-article library-article",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx(z,{to:"/library",children:"library"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx(z,{to:n,children:i}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:a})]}),h.jsxs("header",{className:"article-header",children:[h.jsx("p",{className:"article-eyebrow",children:i}),h.jsx("h1",{children:a})]}),h.jsx(PE,{markdown:s})]})}function sh(){const{slug:e}=Kt(),n=(e??"readme").toLowerCase();return h.jsx(Fg,{kind:"user",basePath:"/library/user",activeSlug:n,children:h.jsx(Ug,{kind:"user",basePath:"/library/user"})})}function ih(){const{slug:e}=Kt(),n=(e??"readme").toLowerCase();return h.jsx(Fg,{kind:"tech",basePath:"/library/tech",activeSlug:n,children:h.jsx(Ug,{kind:"tech",basePath:"/library/tech"})})}function zg({title:e,children:n}){return h.jsxs("article",{className:"page-legal",children:[h.jsx("header",{className:"page-header",children:h.jsx("h1",{children:e})}),h.jsx("div",{className:"legal-body",children:n}),h.jsxs("footer",{className:"article-footer",children:[h.jsx(z,{to:"/legal/privacy",className:"text-link",children:"privacy"}),h.jsx(z,{to:"/legal/terms",className:"text-link",children:"terms"}),h.jsx(z,{to:"/",className:"text-link",children:"home"})]})]})}function CE(){return h.jsxs("div",{className:"page-legal-wrap",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:"privacy"})]}),h.jsxs(zg,{title:"privacy policy",children:[h.jsx("p",{className:"legal-updated",children:"last updated: 2026-04-30"}),h.jsxs("p",{children:['this policy covers the chromatika marketing and documentation site (the "site") and describes our current pre-release posture. the chromatika browser extension handles sensitive key material locally; this page is not a substitute for reading'," ",h.jsx("code",{className:"inline-code",children:"wallet-extension/docs/WALLET_SECURITY.md"})," in the repository."]}),h.jsx("h2",{children:"what we collect on this site"}),h.jsxs("p",{children:["the static site as built in ",h.jsx("code",{className:"inline-code",children:"website/"})," does not embed third-party analytics in tree. if a future deploy adds analytics or hosted forms, this section will name the vendor, purpose, and retention."]}),h.jsx("h2",{children:"extension data"}),h.jsx("p",{children:"wallet secrets stay on your device inside the extension sandbox. we do not operate a hosted backend that stores your seed phrase or app password for the pre-release builds described in the open source repo."}),h.jsx("h2",{children:"contact"}),h.jsxs("p",{children:["questions: reach the maintainer on"," ",h.jsx("a",{href:"https://x.com/kitty4dhd",target:"_blank",rel:"noopener noreferrer",children:"@kitty4dhd"})," ","or via the"," ",h.jsx("a",{href:"https://github.com/kitty4D/chromatika",target:"_blank",rel:"noopener noreferrer",children:"github repository"}),"."]})]})]})}function AE(){return h.jsxs("div",{className:"page-legal-wrap",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:"terms"})]}),h.jsxs(zg,{title:"terms of service",children:[h.jsx("p",{className:"legal-updated",children:"last updated: 2026-04-30"}),h.jsxs("p",{children:["by using the chromatika site, extension builds from the open repository, or any linked pre-release artifacts, you agree you are doing so at your own risk. chromatika is"," ",h.jsx("strong",{children:"pre-release"})," software: features, cryptography, and storage formats may change without migration paths for older developer installs."]}),h.jsx("h2",{children:"no warranty"}),h.jsx("p",{children:'the software and documentation are provided "as is" without warranty of any kind. we disclaim liability for lost funds, failed transactions, phishing, or incorrect documentation.'}),h.jsx("h2",{children:"not financial or legal advice"}),h.jsx("p",{children:"nothing on the site or in the repo is investment, tax, or legal advice. you are responsible for compliance in your jurisdiction and for verifying every transaction you sign."}),h.jsx("h2",{children:"third parties"}),h.jsx("p",{children:"dapps, RPC endpoints, indexers, and chain protocols are third parties. your use of them is between you and those services."}),h.jsx("h2",{children:"changes"}),h.jsx("p",{children:"we may update these terms as the project matures. continued use after changes means you accept the revised terms."})]})]})}function IE(){return h.jsxs("div",{className:"page-resources",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:"resource library"})]}),h.jsxs("header",{className:"page-header",children:[h.jsx("h1",{children:"resource library"}),h.jsxs("p",{className:"page-lead",children:["four surfaces: themed KB articles, the in-app-style user guide, and the full user + tech markdown stacks rendered under ",h.jsx(z,{to:"/library",children:"/library"}),"."]})]}),h.jsxs("section",{className:"resources-section","aria-labelledby":"resources-on-site",children:[h.jsx("h2",{id:"resources-on-site",className:"resources-section-title",children:"on this site"}),h.jsxs("ul",{className:"resources-cards resources-cards--triple",children:[h.jsx("li",{children:h.jsxs(z,{to:"/category/start",className:"resources-card",children:[h.jsx("span",{className:"resources-card-kicker",children:"browse"}),h.jsx("span",{className:"resources-card-title",children:"knowledge base"}),h.jsx("span",{className:"resources-card-body",children:"short articles by theme: start here, identity, security, chains, hardware, product status. good first stop for humans and for copy that stays close to shipped behavior."})]})}),h.jsx("li",{children:h.jsxs(z,{to:"/guide",className:"resources-card",children:[h.jsx("span",{className:"resources-card-kicker",children:"walkthroughs"}),h.jsx("span",{className:"resources-card-title",children:"user guide"}),h.jsx("span",{className:"resources-card-body",children:"task-based pages with room for screenshots and clips. aimed at onboarding and everyday flows rather than protocol archaeology."})]})}),h.jsx("li",{children:h.jsxs(z,{to:"/library",className:"resources-card",children:[h.jsx("span",{className:"resources-card-kicker",children:"reference"}),h.jsx("span",{className:"resources-card-title",children:"markdown library"}),h.jsx("span",{className:"resources-card-body",children:"exhaustive user-facing feature reference plus deep technical notes, as authored in markdown with working internal links."})]})})]})]}),h.jsxs("section",{className:"resources-section","aria-labelledby":"resources-local",children:[h.jsx("h2",{id:"resources-local",className:"resources-section-title",children:"maintaining the markdown stacks"}),h.jsxs("p",{className:"resources-prose",children:["sources of truth for those pages live under"," ",h.jsx("code",{className:"inline-code",children:"website/src/library/"})," in-repo (user + tech directories). they are website-only: the extension bundle does not ship this prose."]}),h.jsxs("div",{className:"resources-dual",children:[h.jsxs("div",{className:"resources-dual-card",children:[h.jsx("h3",{className:"resources-dual-title",children:"user guides"}),h.jsxs("p",{className:"resources-prose",children:["high-level how-to for ",h.jsx("strong",{children:"what"})," the wallet can do: prerequisites, steps, and options per feature, without leaning on specific UI chrome."]}),h.jsx("p",{className:"resources-prose resources-prose--muted",children:h.jsx(z,{to:"/library/user/readme",children:"open user guide index →"})})]}),h.jsxs("div",{className:"resources-dual-card",children:[h.jsx("h3",{className:"resources-dual-title",children:"tech guides"}),h.jsxs("p",{className:"resources-prose",children:["deep-tech notes for ",h.jsx("strong",{children:"how"})," chromatika implements things: bytes, KDF envelopes, ika flows, bridge validation, and service boundaries."]}),h.jsx("p",{className:"resources-prose resources-prose--muted",children:h.jsx(z,{to:"/library/tech/readme",children:"open tech guide index →"})})]})]}),h.jsxs("p",{className:"resources-prose",children:["after replacing ",h.jsx("code",{className:"inline-code",children:".md"})," files, run"," ",h.jsx("code",{className:"inline-code",children:"pnpm run sync:library"})," so sibling links stay mapped to"," ",h.jsx("code",{className:"inline-code",children:"/library/…"})," routes. keep pre-release and Solana ika disclaimers aligned with"," ",h.jsx("code",{className:"inline-code",children:"wallet-extension/docs/STATUS.md"}),"."]})]})]})}const TE="https://github.com/kitty4D/chromatika";function RE(){return h.jsx("footer",{className:"site-footer",children:h.jsxs("div",{className:"site-footer-inner",children:[h.jsxs("div",{className:"site-footer-top",children:[h.jsxs("div",{className:"site-footer-brand",children:[h.jsx("span",{className:"site-footer-mark",children:"chromatika"}),h.jsx("span",{className:"site-footer-tagline",children:"pre-release browser wallet"})]}),h.jsxs("div",{className:"site-footer-cols",role:"navigation","aria-label":"footer",children:[h.jsxs("div",{className:"site-footer-col",children:[h.jsx("h2",{className:"site-footer-heading",children:"library"}),h.jsxs("ul",{className:"site-footer-links",children:[h.jsx("li",{children:h.jsx(z,{to:"/resources",children:"resource library"})}),h.jsx("li",{children:h.jsx(z,{to:"/library",children:"markdown library"})}),h.jsx("li",{children:h.jsx(z,{to:"/guide",children:"user guide"})}),h.jsx("li",{children:h.jsx(z,{to:"/category/start",children:"knowledge base"})})]})]}),h.jsxs("div",{className:"site-footer-col",children:[h.jsx("h2",{className:"site-footer-heading",children:"legal"}),h.jsxs("ul",{className:"site-footer-links",children:[h.jsx("li",{children:h.jsx(z,{to:"/legal/privacy",children:"privacy policy"})}),h.jsx("li",{children:h.jsx(z,{to:"/legal/terms",children:"terms of service"})})]})]}),h.jsxs("div",{className:"site-footer-col",children:[h.jsx("h2",{className:"site-footer-heading",children:"code & social"}),h.jsxs("ul",{className:"site-footer-links",children:[h.jsx("li",{children:h.jsx("a",{href:TE,target:"_blank",rel:"noopener noreferrer",children:"github / kitty4D/chromatika"})}),h.jsx("li",{children:h.jsx("a",{href:"https://x.com/kitty4dhd",target:"_blank",rel:"noopener noreferrer",children:"@kitty4dhd"})}),h.jsx("li",{children:h.jsx("a",{href:"https://x.com/chromat_ika",target:"_blank",rel:"noopener noreferrer",children:"@chromat_ika"})})]})]})]})]}),h.jsxs("p",{className:"site-footer-bottom",children:["© ",new Date().getFullYear()," chromatika. pre-release software and site copy; when anything disagrees with ",h.jsx("code",{className:"inline-code",children:"wallet-extension/docs/"})," in the repo, trust the repo."]})]})})}const ME=7e3;function DE(){return h.jsx("svg",{className:"tech-carousel-chevron",viewBox:"0 0 24 24",width:"22",height:"22","aria-hidden":"true",children:h.jsx("path",{d:"M14 7l-5 5 5 5",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"})})}function BE(){return h.jsx("svg",{className:"tech-carousel-chevron",viewBox:"0 0 24 24",width:"22",height:"22","aria-hidden":"true",children:h.jsx("path",{d:"M10 7l5 5-5 5",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"})})}function LE({className:e}){return h.jsxs("svg",{xmlns:"http://www.w3.org/2000/svg",viewBox:"0 0 100 100",fill:"none",className:e,"aria-hidden":"true",children:[h.jsx("rect",{width:"100",height:"100",rx:"22",fill:"#7c3aed"}),h.jsx("path",{d:"M28 30 Q24 30 24.5 32.5 L44 48 Q50 52 56 48 L72 32 Q74 30 70 30 Z",fill:"#fff"}),h.jsx("path",{d:"M28 70 Q24 70 24.5 67.5 L44 52 Q50 48 56 52 L72 68 Q74 70 70 70 Z",fill:"#fff"})]})}function NE(){const[e,n]=T.useState(0),[t,r]=T.useState(!1),[a,s]=T.useState(!1),i=Ji.length,o=Ji[e],l=t||a,c=T.useCallback(d=>{n(u=>(u+d+i)%i)},[i]);return T.useEffect(()=>{function d(u){u.key==="ArrowLeft"&&c(-1),u.key==="ArrowRight"&&c(1)}return window.addEventListener("keydown",d),()=>window.removeEventListener("keydown",d)},[c]),T.useEffect(()=>{if(typeof window>"u"||window.matchMedia("(prefers-reduced-motion: reduce)").matches||l)return;const d=window.setInterval(()=>{n(u=>(u+1)%i)},ME);return()=>window.clearInterval(d)},[l,i]),h.jsxs("div",{className:"tech-carousel","aria-roledescription":"carousel","aria-label":"technologies used in chromatika",onMouseEnter:()=>r(!0),onMouseLeave:()=>r(!1),onFocusCapture:()=>s(!0),onBlurCapture:d=>{const u=d.currentTarget;window.requestAnimationFrame(()=>{u.contains(document.activeElement)||s(!1)})},children:[h.jsxs("div",{className:"tech-carousel-header",children:[h.jsx("p",{className:"tech-carousel-kicker",children:"technology used"}),h.jsxs("div",{className:"tech-carousel-controls",children:[h.jsx("button",{type:"button",className:"tech-carousel-btn tech-carousel-btn--nav","aria-label":"Previous panel",onClick:()=>c(-1),children:h.jsx(DE,{})}),h.jsx("span",{className:"tech-carousel-dots","aria-hidden":"true",children:Ji.map((d,u)=>h.jsx("button",{type:"button",className:`tech-carousel-dot ${u===e?"tech-carousel-dot--active":""}`,"aria-label":`Show ${d.title}`,"aria-current":u===e?"true":void 0,onClick:()=>n(u)},d.id))}),h.jsx("button",{type:"button",className:"tech-carousel-btn tech-carousel-btn--nav","aria-label":"Next panel",onClick:()=>c(1),children:h.jsx(BE,{})})]})]}),h.jsxs("article",{className:"tech-carousel-panel","aria-live":"polite",children:[h.jsxs("div",{className:"tech-carousel-panel-head",children:[h.jsx("h2",{className:"tech-carousel-title",children:o.title}),h.jsx("div",{className:"tech-carousel-logo-corner",children:o.id==="lazorkit"?h.jsx(LE,{className:"tech-carousel-logo tech-carousel-logo--inline"}):o.logoSrc?h.jsx("img",{src:o.logoSrc,alt:"",className:o.id==="encrypt"?"tech-carousel-logo tech-carousel-logo--encrypt":"tech-carousel-logo",decoding:"async"}):null})]}),h.jsx("p",{className:"tech-carousel-intro",children:o.intro}),h.jsx("hr",{className:"tech-carousel-split","aria-hidden":"true"}),h.jsx("h3",{className:"tech-carousel-how-title",children:"how we use it"}),h.jsx("p",{className:"tech-carousel-body",children:o.howWeUse}),o.howWeUseBullets&&o.howWeUseBullets.length>0?h.jsx("ul",{className:"tech-carousel-bullets",children:o.howWeUseBullets.map((d,u)=>h.jsx("li",{children:d},`${o.id}-b-${u}`))}):null,h.jsx("nav",{className:"tech-carousel-links","aria-label":`${o.title} external links`,children:o.links.map(d=>h.jsxs("a",{className:"tech-carousel-link",href:d.href,target:"_blank",rel:"noreferrer",children:[d.label," ↗"]},d.href))})]},o.id)]})}function KE(){return h.jsx("section",{className:"wallet-preview","aria-label":"wallet preview and tech highlights",children:h.jsxs("div",{className:"wallet-preview-split",children:[h.jsx("div",{className:"wallet-preview-aside",children:h.jsx(NE,{})}),h.jsx("div",{className:"wallet-preview-frame-outer",children:h.jsx("div",{className:"wallet-preview-frame",role:"img","aria-label":"mock Chromatika vault tab",children:h.jsxs("div",{className:"wallet-preview-chrome",children:[h.jsx("p",{className:"wallet-preview-kicker",children:"fixture · vault tab"}),h.jsxs("div",{className:"wallet-preview-body",children:[h.jsxs("div",{className:"wallet-preview-cockpit",children:[h.jsxs("div",{className:"wp-rocket",children:[h.jsxs("div",{className:"wp-pilots","aria-label":"cockpit pilots",children:[h.jsxs("span",{className:"wp-pilot",children:[h.jsx("span",{className:"wp-pilot-dot"}),"David"]}),h.jsxs("span",{className:"wp-pilot wp-pilot--co",children:[h.jsx("span",{className:"wp-pilot-dot wp-pilot-dot--co"}),"Toly"]})]}),h.jsxs("div",{className:"wp-gauge-row",children:[h.jsxs("div",{className:"wp-gauge wp-gauge--sui","data-health":"green",children:[h.jsx("div",{className:"wp-gauge-track",children:h.jsx("div",{className:"wp-gauge-fill",style:{transform:"scaleX(0.82)"}})}),h.jsx("span",{className:"wp-gauge-cap",children:"SUI 12.40"})]}),h.jsxs("div",{className:"wp-gauge wp-gauge--ika","data-health":"green",children:[h.jsx("div",{className:"wp-gauge-track",children:h.jsx("div",{className:"wp-gauge-fill",style:{transform:"scaleX(0.71)"}})}),h.jsx("span",{className:"wp-gauge-cap",children:"IKA 8.2"})]})]}),h.jsx("div",{className:"wp-rocket-illus","aria-hidden":"true",children:h.jsxs("svg",{className:"wp-rocket-svg",viewBox:"0 0 200 48",fill:"none",children:[h.jsx("path",{d:"M20 38L100 8l80 30-80 6L20 38z",stroke:"currentColor",strokeWidth:"1.2",opacity:"0.35"}),h.jsx("path",{d:"M96 12l8 26M72 22l56 8",stroke:"currentColor",strokeWidth:"0.8",opacity:"0.25"})]})})]}),h.jsx("div",{className:"wp-base-card","data-vault-health":"green",children:h.jsxs("div",{className:"wp-base-inner",children:[h.jsxs("div",{className:"wp-base-copy",children:[h.jsx("div",{className:"wp-vault-name",children:"main vault"}),h.jsx("div",{className:"wp-vault-kicker",children:"dWallet Vault Account"}),h.jsx("div",{className:"wp-addr",children:"0x7a3f…c21e"})]}),h.jsxs("div",{className:"wp-base-rail","aria-hidden":"true",children:[h.jsx("span",{className:"wp-icon-faux"}),h.jsx("span",{className:"wp-icon-faux"}),h.jsx("span",{className:"wp-icon-faux"})]})]})})]}),h.jsxs("div",{className:"wallet-preview-dwallets",children:[h.jsxs("div",{className:"wp-section-head",children:[h.jsx("span",{className:"wp-section-title",children:"your dWallets"}),h.jsx("span",{className:"wp-manage-faux","aria-hidden":"true"})]}),h.jsxs("ul",{className:"wp-dw-list",children:[h.jsxs("li",{className:"wp-dw-card",children:[h.jsxs("div",{className:"wp-dw-top",children:[h.jsx("span",{className:"wp-dw-name",children:"David · secp"}),h.jsx("span",{className:"wp-dw-pill",children:"main"})]}),h.jsx("div",{className:"wp-dw-addr",children:"0x4b8e…91aa"}),h.jsxs("div",{className:"wp-dw-chips",children:[h.jsx("span",{children:"evm"}),h.jsx("span",{children:"btc"})]})]}),h.jsxs("li",{className:"wp-dw-card",children:[h.jsxs("div",{className:"wp-dw-top",children:[h.jsx("span",{className:"wp-dw-name",children:"Toly · ed25519"}),h.jsx("span",{className:"wp-dw-pill wp-dw-pill--alt",children:"sol · sui · apt"})]}),h.jsx("div",{className:"wp-dw-addr",children:"6Fh9…qR2m"}),h.jsxs("div",{className:"wp-dw-chips",children:[h.jsx("span",{children:"solana"}),h.jsx("span",{children:"sui"})]})]})]})]})]})]})})})]})})}function WE({media:e}){const n=e.filter(t=>{var r;return(r=t.src)==null?void 0:r.trim()});return n.length===0?null:h.jsxs("section",{className:"guide-media","aria-label":"screenshots and video",children:[h.jsx("h2",{className:"guide-media-heading",children:"media"}),h.jsx("div",{className:"guide-media-grid",children:n.map((t,r)=>t.kind==="video"?h.jsxs("figure",{className:"guide-media-figure",children:[h.jsx("video",{className:"guide-media-video",controls:!0,preload:"metadata",src:t.src}),t.caption?h.jsx("figcaption",{className:"guide-media-caption",children:t.caption}):null]},r):h.jsxs("figure",{className:"guide-media-figure",children:[h.jsx("img",{className:"guide-media-img",src:t.src,alt:t.alt,loading:"lazy"}),t.caption?h.jsx("figcaption",{className:"guide-media-caption",children:t.caption}):null]},r))})]})}function jE(){return h.jsxs("div",{className:"home",children:[h.jsx(Av,{}),h.jsx(KE,{}),h.jsxs("section",{className:"guide-spot-row","aria-label":"guides and resources",children:[h.jsx("section",{className:"guide-spot","aria-label":"user guide",children:h.jsxs(z,{to:"/guide",className:"guide-spot-card",children:[h.jsx("h2",{className:"guide-spot-title",children:"user guide"}),h.jsx("p",{className:"guide-spot-body",children:"step-by-step wallet tasks: onboarding, everyday use, advanced options, hardware. screenshots and clips stack here as we capture them."}),h.jsx("span",{className:"guide-spot-cta",children:"open the guide →"})]})}),h.jsx("section",{className:"guide-spot","aria-label":"resource library",children:h.jsxs(z,{to:"/resources",className:"guide-spot-card",children:[h.jsx("h2",{className:"guide-spot-title",children:"resource library"}),h.jsx("p",{className:"guide-spot-body",children:"how the on-site KB and guide relate to optional user and tech guide markdown libraries you keep beside the extension in a developer checkout."}),h.jsx("span",{className:"guide-spot-cta",children:"browse layers →"})]})})]}),h.jsx("section",{className:"grid-categories","aria-label":"browse by category",children:Sc.map(e=>{const n=Rm(e.id).length;return h.jsxs(z,{className:"cat-card",to:`/category/${e.id}`,children:[h.jsx("h2",{className:"cat-title",children:e.label}),h.jsx("p",{className:"cat-blurb",children:e.blurb}),h.jsxs("span",{className:"cat-meta",children:[n," article",n===1?"":"s"]})]},e.id)})}),h.jsxs("section",{className:"all-articles","aria-label":"all articles",children:[h.jsx("h2",{className:"section-title",children:"all articles"}),h.jsx("ul",{className:"article-index",children:di.map(e=>h.jsx("li",{children:h.jsxs(z,{to:`/article/${e.slug}`,className:"article-link",children:[h.jsx("span",{className:"article-link-title",children:e.title}),h.jsx("span",{className:"article-link-sum",children:e.summary})]})},e.slug))})]})]})}function FE(){const{categoryId:e}=Kt(),n=Sc.find(r=>r.id===e),t=n?Rm(n.id):[];return n?h.jsxs("div",{className:"page-category",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{children:n.label})]}),h.jsxs("header",{className:"page-header",children:[h.jsx("h1",{children:n.label}),h.jsx("p",{className:"page-lead",children:n.blurb})]}),h.jsx("ul",{className:"article-index tight",children:t.map(r=>h.jsx("li",{children:h.jsxs(z,{to:`/article/${r.slug}`,className:"article-link",children:[h.jsx("span",{className:"article-link-title",children:r.title}),h.jsx("span",{className:"article-link-sum",children:r.summary})]})},r.slug))})]}):h.jsx(Ia,{to:"/",replace:!0})}function UE(){const{slug:e}=Kt(),n=e?Iv(e):void 0,t=n?Sc.find(r=>r.id===n.categoryId):void 0;return!n||!t?h.jsx(Ia,{to:"/",replace:!0}):h.jsxs("article",{className:"page-article",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx(z,{to:`/category/${n.categoryId}`,children:t.label}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:n.title})]}),h.jsxs("header",{className:"article-header",children:[h.jsx("p",{className:"article-eyebrow",children:t.label}),h.jsx("h1",{children:n.title}),h.jsx("p",{className:"article-summary",children:n.summary})]}),h.jsx(Tm,{body:n.body}),h.jsxs("footer",{className:"article-footer",children:[h.jsxs(z,{to:`/category/${n.categoryId}`,className:"text-link",children:["← ",t.label]}),h.jsx(z,{to:"/",className:"text-link",children:"all topics"})]})]})}function zE({activeSlug:e}){return h.jsxs("aside",{className:"docs-sidebar","aria-label":"guide sections",children:[h.jsx("p",{className:"docs-sidebar-title",children:h.jsx(z,{to:"/guide",children:"user guide"})}),xc.map(n=>{const t=Mm(n.id);return h.jsxs("div",{className:"docs-nav-group",children:[h.jsx("h3",{className:"docs-nav-heading",children:n.label}),h.jsx("ul",{className:"docs-nav-list",children:t.map(r=>{const a=`/guide/${r.slug}`,s=e===r.slug;return h.jsx("li",{children:h.jsx(z,{to:a,className:s?"docs-nav-link docs-nav-link--active":"docs-nav-link",children:r.title})},r.slug)})})]},n.id)})]})}function Og({activeSlug:e,children:n}){return h.jsxs("div",{className:"docs-layout",children:[h.jsx(zE,{activeSlug:e}),h.jsx("div",{className:"docs-main",children:n})]})}function OE(){return h.jsxs("div",{className:"guide-hub",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:"user guide"})]}),h.jsxs("header",{className:"page-header",children:[h.jsx("h1",{children:"user guide"}),h.jsxs("p",{className:"page-lead",children:["task-based walkthroughs for Chromatika: onboarding, everyday flows, advanced toggles, and hardware paths. screenshots and video appear in each page as we capture them; assets live under ",h.jsx("code",{className:"inline-code",children:"/guide/assets/"}),". search guide pages from the header."]})]}),xc.map(e=>{const n=Mm(e.id);return n.length===0?null:h.jsxs("section",{className:"guide-hub-section",children:[h.jsx("h2",{className:"guide-hub-section-title",children:e.label}),h.jsx("p",{className:"guide-hub-section-blurb",children:e.blurb}),h.jsx("ul",{className:"article-index tight",children:n.map(t=>h.jsx("li",{children:h.jsxs(z,{to:`/guide/${t.slug}`,className:"article-link",children:[h.jsx("span",{className:"article-link-title",children:t.title}),h.jsx("span",{className:"article-link-sum",children:t.summary})]})},t.slug))})]},e.id)})]})}function HE(){const{slug:e}=Kt(),n=e?Tv(e):void 0,t=n?xc.find(r=>r.id===n.sectionId):void 0;return!n||!t?h.jsx(Ia,{to:"/guide",replace:!0}):h.jsxs("article",{className:"page-article guide-article",children:[h.jsxs("nav",{className:"crumbs","aria-label":"breadcrumb",children:[h.jsx(z,{to:"/",children:"home"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx(z,{to:"/guide",children:"user guide"}),h.jsx("span",{"aria-hidden":"true",children:"/"}),h.jsx("span",{className:"crumbs-current",children:n.title})]}),h.jsxs("header",{className:"article-header",children:[h.jsx("p",{className:"article-eyebrow",children:t.label}),h.jsx("h1",{children:n.title}),h.jsx("p",{className:"article-summary",children:n.summary}),n.lastUpdated?h.jsxs("p",{className:"article-meta",children:[h.jsx("span",{className:"article-meta-label",children:"last updated"})," ",h.jsx("time",{dateTime:n.lastUpdated,children:n.lastUpdated})]}):null]}),h.jsx(Tm,{body:n.body}),n.media&&n.media.length>0?h.jsx(WE,{media:n.media}):null,h.jsx("footer",{className:"article-footer",children:h.jsx(z,{to:"/guide",className:"text-link",children:"← user guide"})})]})}function Je({children:e}){const n=Sr(),t=n.pathname.startsWith("/guide"),r=n.pathname.startsWith("/library"),a=n.pathname.startsWith("/article/")||n.pathname.startsWith("/category/"),s=t||a||r;return h.jsxs("div",{className:s?"site site--docs":"site",children:[h.jsx("div",{className:"bg-aurora","aria-hidden":"true"}),h.jsx("header",{className:"site-header",children:h.jsxs("div",{className:"header-bleed",children:[h.jsx("div",{className:"header-spectral","aria-hidden":"true"}),h.jsx("div",{className:"header-sweep","aria-hidden":"true"}),h.jsx("div",{className:"header-glass-stack",children:h.jsxs("div",{className:"header-constrain header-top-bar",children:[h.jsx(z,{to:"/",className:"header-bar-wordmark",children:"chromatika"}),h.jsxs("div",{className:"header-top-actions",children:[h.jsxs("nav",{className:"header-nav","aria-label":"primary",children:[h.jsx(z,{to:"/",className:n.pathname==="/"?"nav-link active":"nav-link",children:"home"}),h.jsx(z,{to:"/guide",className:t?"nav-link active":"nav-link",children:"user guide"}),h.jsx(z,{to:"/resources",className:n.pathname==="/resources"?"nav-link active":"nav-link",children:"resources"}),h.jsx(z,{to:"/library",className:r?"nav-link active":"nav-link",children:"library"}),h.jsx(z,{to:"/article/getting-help",className:"nav-link",children:"knowledge base"})]}),h.jsx("div",{className:"header-toolbar",children:h.jsx(MS,{})})]})]})})]})}),h.jsx("main",{id:"main",className:t||r?"site-main site-main--guide":"site-main",children:e}),h.jsx(RE,{})]})}function qE(){return h.jsxs(uv,{children:[h.jsx(Le,{path:"/",element:h.jsx(Je,{children:h.jsx(jE,{})})}),h.jsx(Le,{path:"/category/:categoryId",element:h.jsx(Je,{children:h.jsx(FE,{})})}),h.jsx(Le,{path:"/article/:slug",element:h.jsx(Je,{children:h.jsx(UE,{})})}),h.jsx(Le,{path:"/resources",element:h.jsx(Je,{children:h.jsx(IE,{})})}),h.jsx(Le,{path:"/library",element:h.jsx(Je,{children:h.jsx(EE,{})})}),h.jsx(Le,{path:"/library/user",element:h.jsx(Je,{children:h.jsx(sh,{})})}),h.jsx(Le,{path:"/library/user/:slug",element:h.jsx(Je,{children:h.jsx(sh,{})})}),h.jsx(Le,{path:"/library/tech",element:h.jsx(Je,{children:h.jsx(ih,{})})}),h.jsx(Le,{path:"/library/tech/:slug",element:h.jsx(Je,{children:h.jsx(ih,{})})}),h.jsx(Le,{path:"/legal/privacy",element:h.jsx(Je,{children:h.jsx(CE,{})})}),h.jsx(Le,{path:"/legal/terms",element:h.jsx(Je,{children:h.jsx(AE,{})})}),h.jsx(Le,{path:"/guide",element:h.jsx(Je,{children:h.jsx(Og,{children:h.jsx(OE,{})})})}),h.jsx(Le,{path:"/guide/:slug",element:h.jsx(Je,{children:h.jsx(VE,{})})}),h.jsx(Le,{path:"*",element:h.jsx(Ia,{to:"/",replace:!0})})]})}function VE(){const{slug:e}=Kt();return h.jsx(Og,{activeSlug:e,children:h.jsx(HE,{})})}function GE(){return h.jsx(yv,{children:h.jsx(IS,{children:h.jsx(qE,{})})})}po.createRoot(document.getElementById("root")).render(h.jsx(yh.StrictMode,{children:h.jsx(GE,{})}));
