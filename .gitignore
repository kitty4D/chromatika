# dependencies
node_modules/

# build output
wallet-extension/dist/
wallet-extension/.vite/

website/dist/

# anchor / rust build outputs (solana policy program)
wallet-extension/solana/chromatika-policy/target/
wallet-extension/solana/chromatika-policy/.anchor/

# sui move build output (chromatika-policy package)
wallet-extension/move/chromatika-policy/build/

# test artifacts
wallet-extension/test-results/
wallet-extension/playwright-report/
wallet-extension/blob-report/
wallet-extension/.playwright/
wallet-extension/coverage/

# `wallet-extension/preview/` stays tracked: CI's `Build preview mockups` step
# (.github/workflows/ci.yml) runs `pnpm run preview:build` which loads
# `preview/build/vite.preview-build.config.ts`.

website/DESIGN-BRIEF.md

# env / secrets (track .env.example, ignore real .env and per-env overrides)
.env
.env.*
!.env.example
!.env.*.example

# local / scratch / credentials
local/

# editor + agent configs and caches (per-user, not shared)
.claude/
.cursor/
.superpowers/
.vscode/
.impeccable.md
.playwright-mcp/

# superpowers planning artifacts (per-session, not shared)
docs/superpowers/

# agent skills catalog (local reference only)
skills/

# os junk
.DS_Store
Thumbs.db
desktop.ini

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# misc
*.tsbuildinfo
.vite/

# skill audit local state
.skill-audit.json

CLAUDE.md
